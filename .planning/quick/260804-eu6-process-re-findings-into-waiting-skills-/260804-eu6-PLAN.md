---
id: 260804-eu6
title: Process RE-FINDINGS into the waiting skills; match crash content against the rewritten VICE MCP
date: 2026-08-04
mode: quick
executed_inline: true
---

# Process RE-FINDINGS into the waiting skills, and reconcile crash content with the rewritten MCP

**Executed inline, no subagents.** The session forbids spawning agents unless the user asks;
the GSD guarantees kept are the ones that matter here — a planning artifact, atomic commits,
STATE.md tracking.

## What was actually waiting

Surveyed first, because the request's premise ("all skills that are waiting to be created") is
partly already discharged:

| Candidate | State on 2026-08-04 before this task |
|---|---|
| The RE skill (`2026-08-01-collect-...-fast-re-skill`) | **Built**, as four skills. Todo's own resolution says so. |
| The provenance-diff skill (`...surviving-tooling...` still-open 1) | **Built** — `c64-provenance-diff`, `260804-dih`. |
| Wedged-VICE triage (`2026-08-02-supervisor-skill-...`) | **NOT built.** Part 2 (where it lives) settled; part 1 (the triage narrative) outstanding. Severity `blocker`. |
| Non-game-layer scope rule (`2026-08-02-strip-cracktro-...`) | **NOT landed.** Content waiting for a home in `c64-program-recon`. |
| Widened vector sweep (`2026-08-02-widen-the-re-vector-sweep-...`) | **NOT landed.** Static half is implementable offline; live half is not. |
| N≥3 stability gate (`move-drift-classification-into-ram-compare`) | Deliberately left as an undecided decision. **Not in scope here** — out of respect for "this todo is a decision, not a move". |

So: one skill to create, two content bodies to land, plus the reconciliation the request asks for.

## Tasks

1. **`derive.mjs vectors` — widen the sweep.** All vector blocks (`$0300-$030B` BASIC
   indirects, `$0314-$0319` IRQ/BRK/NMI, `$031A-$0333` KERNAL I/O indirects, `$8000` cold /
   `$8002` warm / `$8004` `CBM80`, `$A000/$A002` BASIC ROM entry, `$FFFA-$FFFF` hardware),
   flagging `$0328` STOP and `$0330/$0332` LOAD/SAVE as cracker-hook sites and marking any
   ROM-window target as bank-ambiguous. Verify against both committed gameentry captures.
   *Verify:* `node derive.mjs vectors recovery/{danish,saeger}/dumps/*-gameentry-run1.bin`
   reproduces `$1103` and reports `CBM80` absent.

2. **New skill `vice-wedge-triage`.** Part 1 of the blocker todo: the triage narrative. Calls
   `vice_diagnose`/`vice_recycle` rather than restating them; carries the manual fallback for a
   session with no broker; states plainly what is not recoverable.
   *Verify:* frontmatter checker passes; every cited path exists; no script contacts anything.

3. **`c64-program-recon` — the non-game-layer scope step**, ahead of control-flow discovery,
   with the evidence bar in both directions and the two recorded failures as worked examples.

4. **Reconcile skill text with the rewritten MCP.** Four corrections, each traced to source:
   the epoch instruction that no exposed tool can perform; hazard 2 now implemented inside
   `vice_diagnose`; `vice_run_until`'s unimplemented `cycles` timeout; the widened-vector
   bank-disambiguation rule in `control-flow.md`.

5. **Log findings, file the MCP gaps.** Append to `.planning/RE-FINDINGS.md`; file one pending
   todo for the two MCP gaps rather than editing `.claude/mcp/` (CLAUDE.md's triage rule).

6. **Update the affected pending todos; register the new skill by hand in CLAUDE.md.** No
   `gsd-tools generate-claude-md` — it regresses the hand-refined constraints (measured
   2026-08-04, its own pending todo).

## Constraints honoured

- `mcp__vice__*` stays the only route to the emulator. `derive.mjs` imports `node:fs` only.
- `.claude/mcp/` was **read, not edited** — the task is reconciling skill text against it, and
  the two defects found are filed as a todo, not fixed inline.
- No live verification was possible: `vice_diagnose` reports no `broker.json` record exists on
  this host, so everything MCP-derived here is graded from source-read plus one schema
  cross-check, never claimed as live.
