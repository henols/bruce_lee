---
phase: quick-260806-q2g
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/c64-program-recon/references/reconstruction.md
  - .claude/skills/c64-program-recon/SKILL.md
autonomous: true
requirements:
  - SKILL-RECON-BEHAVIOURAL-ONLY

must_haves:
  truths:
    - "`references/reconstruction.md` states behavioural equivalence -- scripted replay plus checkpoint comparison -- as the ONLY definition of correctness. No competing bar, staged achievement, or 'stronger, earlier check' is offered anywhere in the file."
    - "No byte-comparison recipe survives in that file. The fenced `bash` block holding the two shell comparison commands is gone, and no replacement recipe (checksum, diff, hash) takes its place -- the file's fenced-block count drops from 16 to 14 and no new block is added."
    - "The blockquote asserting that reassembly must reproduce the original extracted region exactly is gone -- that sentence IS the claim being removed, not a caveat on it."
    - "The genuinely-useful hazard SURVIVES, re-anchored: reorganising source changes addresses, which breaks self-modifying code and timing-sensitive raster routines, and re-verification happens after EACH reorganisation rather than after several. Its trigger for re-verification is now a behavioural replay through the checkpoint set, not the re-establishment of a byte comparison."
    - "The cross-reference to `.claude/CLAUDE.md` § Constraints survives as the cited source of the behavioural-equivalence definition, and the 'anything not observable at a checkpoint is not verified' consequence survives with it."
    - "The file's own lead-in (line 8) no longer promises a staged verification achievement -- it names what the file actually now covers, so the intro and the section it introduces agree."
    - "The rewritten section reads as if it were always written that way: em-dash prose, bolded lead-ins, no fenced block (the file's convention reserves those for concrete artifacts, and this section shows none)."
    - "`SKILL.md`'s `references/reconstruction.md` row describes what that file now covers. The row's other three items, the table's 6 reference rows, and the `|---|---|` unpadded format are unchanged."
    - "`references/observation-hazards.md` is byte-for-byte identical to its pre-change state -- md5 `1f006c10ba40a337a3b0731fca8ae977`. Its § 7 and § 2 are explicitly out of scope."
    - "No test anywhere in the repo asserts the removed text -- CONFIRMED by grep at plan time and re-confirmed by a gate, not assumed."
    - "Exactly two files under `.claude/` change across this plan's commits."
  artifacts:
    - .claude/skills/c64-program-recon/references/reconstruction.md
    - .claude/skills/c64-program-recon/SKILL.md
  key_links:
    - "`reconstruction.md` <-> `.claude/CLAUDE.md` § Constraints: CLAUDE.md's `**Verification**: Behavioural equivalence only -- Correctness is defined by replay + checkpoint comparison. Anything not observable at a checkpoint is not verified` is the AUTHORITY for the rewritten section. The section restates it and cites it; it must not paraphrase it into something weaker or invent a second bar alongside it."
    - "`reconstruction.md` <-> `.planning/REQUIREMENTS.md` § Out of Scope row 122: the project ALREADY rejects byte-identical rebuild as the acceptance gate, on the grounds that it would forbid restructuring source for readability. The rewritten section is that rejection arriving in the skill. Do NOT edit REQUIREMENTS.md, and do NOT import its `still used as a development-time check` clause into the skill -- the developer's instruction for this task is removal, and re-importing it as a lesser recommendation reinstates the thing being removed."
    - "`SKILL.md` References row <-> `references/reconstruction.md`: the path must still resolve. The `skill-writer` validation checklist requires every path a skill cites to resolve to a file that exists; only the row's DESCRIPTION changes, never the path."
    - "`reconstruction.md` <-> `observation-hazards.md` § 7: the § 7 pointer is OPTIONAL (executor's judgement, rule in task 1). Reachability does not depend on it -- `SKILL.md` line 152's References row and line 172's Troubleshooting row both already route the reader to that file. If the pointer is kept it must still resolve; if dropped, nothing breaks."
    - "the rewrite <-> the `skill-writer` house style: imperative and terse, tables for contracts, state the provenance of any claim. The section's one claim (what correctness means here) carries its provenance as the CLAUDE.md citation -- that citation IS the provenance, so removing it would break a house rule as well as a link."
---

<objective>
Stop `c64-program-recon` presenting byte-identity as a correctness milestone, a gate, or a
"stronger, earlier check". Behavioural equivalence -- scripted replay plus checkpoint comparison --
becomes the sole stated definition of correctness in the skill.

Purpose: the developer's instruction is decisive and verbatim -- *"I want you to remove the byte
identical, it is absolutely not a requirement, the only requirement is that it has the same
behaviour. so correct the skill"*. The skill currently contradicts both `.claude/CLAUDE.md`
§ Constraints (`Behavioural equivalence only`) and `.planning/REQUIREMENTS.md` § Out of Scope, which
already rejects a byte-identical rebuild as the acceptance gate because it would forbid restructuring
source for readability. A reference doc that offers a competing bar is a doc that will be followed.

Output: two edited files -- one rewritten section plus its lead-in in
`references/reconstruction.md`, and one reference-table row in `SKILL.md`. No new files, no test
changes (confirmed: nothing asserts this text).
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.claude/skills/c64-program-recon/references/reconstruction.md
@.claude/skills/c64-program-recon/SKILL.md
@.claude/skills/skill-writer/SKILL.md

Deliberately NOT included, and why -- this plan targets ~30% context and these would blow it for no
gain:

- `.planning/STATE.md` -- 640 lines / ~56k tokens of broker-lifecycle execution narrative. Nothing
  in it bears on a two-file prose edit. Reading it is the single largest avoidable context cost in
  this repo.
- `.planning/PROJECT.md`, `.planning/ROADMAP.md` -- no phase work here; the governing statements are
  quoted inline in `key_links` above.
- `references/observation-hazards.md` -- 119 lines for a 7-line section. § 7's full content is
  quoted in `<planning_notes>`. Read ONLY § 7 (lines 113-118), and only if you keep its pointer.
- `.planning/REQUIREMENTS.md` -- row 122 is quoted in `key_links`. Out of scope to edit.
</context>

<planning_notes>
**Baselines measured at plan time (HEAD `3701429`, working tree clean under `.claude/skills/`).**
Every count in a `<verify>` gate below is a real before/after pair, not a guess:

| Token in `reconstruction.md` | Before | After must be |
|---|---|---|
| `milestone` (case-insensitive) | 2 (line 8 lead-in, line 37 heading) | 0 |
| `sha256sum`, `\bcmp\b` | 1, 1 | 0 |
| fenced-block delimiters `^``` ` | 16 | 14 (the bash block's 2 lines removed, none added) |
| blockquote lines `^>` | 1 | 0 |
| `identity` (case-insensitive) | 5 | <= 1 (only the optional § 7 pointer may carry it) |
| `reorganis` | 3 | >= 2 (the retained hazard) |
| `timing-sensitive raster` | 1 | >= 1 |
| `CLAUDE.md` | 1 | >= 1 |

`SKILL.md`: 6 rows begin `` | `references/ `` and all 6 cited reference paths resolve. Keep both true.

**Grep-hygiene note for a reviewer.** The usual `grep -v '^#'` comment filter is deliberately NOT
applied to these gates. These are markdown files where `#` opens a heading, not a comment -- and the
heading is precisely what the `milestone` gate must inspect. Filtering `^#` here would blind the
gate rather than harden it.

**§ 7 of `observation-hazards.md`, quoted so the file need not be loaded** (do not edit it; md5
`1f006c10ba40a337a3b0731fca8ae977`):

> ## 7. Full-64K byte-identity is impossible in principle
> Never-written RAM drifts continuously, so two captures of the same checkpoint will not match
> across all 64K. The recovery procedure is deterministic **for the program image**, not for 64K.
> `c64-ram-capture` § Compare two captures carries the volatility regions and the drift
> discriminator; use them rather than treating any difference as a divergence.

That is a NEGATIVE claim which already supports behavioural-only correctness. It stays verbatim.

**Confirmed at plan time, so the executor does not re-litigate it:** no test asserts the removed
text. `grep -rl` over `*.test.*`, `*.spec.*`, `*.mjs`, `*.mts`, `*.ts`, `*.py` for `reconstruction.md`
and for the removed phrasing returns nothing; the skill's only script is `scripts/derive.mjs`
(VIC-derivation, unrelated). Task 2 re-runs that grep as a gate rather than trusting this paragraph.

**Why no tracer task, and why no `tdd="true"`.** Tracer-first decomposition degenerates here: there
is one layer (markdown prose) and no architecture to prove end-to-end, so a "thin slice through
every layer" and the full change are the same edit. Task 1 IS the substantive change and lands
first. `tdd="true"` is skipped under the documented documentation-only exception; the `<verify>`
gates are the executable substitute and they run in well under a second.

**Comment-text discipline.** This plan's acceptance criteria negative-grep words that the task is
about deleting. The action bodies avoid `sha256sum`, `cmp` and the staged-achievement framing word
by describing them structurally instead. `identity` is named in prose because its gate is a bound
(`<= 1`), not a prohibition -- allowlisted below.

<!-- planner-discipline-allow: identity -->
<!-- planner-discipline-allow: byte-identity -->
</planning_notes>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite reconstruction.md's correctness section so behavioural equivalence is the only bar</name>
  <files>.claude/skills/c64-program-recon/references/reconstruction.md</files>
  <precondition>`git status --porcelain -- .claude/skills/` is empty. Task 2's "exactly two files changed" gate cannot distinguish this plan's edits from pre-existing ones otherwise. Halt if it is dirty.</precondition>
  <reversibility rating="reversible">A prose edit to one markdown reference file; a single `git revert` restores it exactly. The one judgement call (keep or drop the § 7 pointer) is equally reversible and is recorded in the SUMMARY either way.</reversibility>
  <action>
Record `git rev-parse HEAD` as `BASE` in the SUMMARY before editing -- task 2's changed-file gate
needs it.

Two edits to `references/reconstruction.md`, both driven by the developer's verbatim instruction
quoted in `<objective>`.

**Edit A -- the lead-in, line 8.** The second clause currently promises that this file covers a
staged verification achievement. Retarget it to what the file will actually cover: what makes a
reconstruction correct. One clause, same sentence shape, same semicolon structure after the
`acme-build` clause. Do not lengthen it.

**Edit B -- replace the whole section that currently spans line 37's heading through line 62**,
stopping immediately before `## Self-modifying code needs explicit labels`. Nothing else in the file
moves; the section that follows and everything above line 37 stay untouched.

REMOVE, completely:
  - the heading's framing of byte-identity as a staged achievement to reach -- the new heading names
    correctness, or behavioural equivalence, and names neither byte-identity nor any staged
    achievement;
  - the blockquote directly under it asserting that reassembly must reproduce the original extracted
    region exactly -- that sentence is the claim being removed, so it does not survive as a caveat;
  - the fenced `bash` block holding the two byte-comparison shell commands. Delete the block and its
    fences. Add NO replacement recipe of any kind -- no checksum, no diff, no hash. The section ends
    up with no fenced block at all, which matches this file's convention of reserving fences for
    concrete artifacts (source snippets, a directory tree, a label declaration). This section shows
    none;
  - the claim that byte-identity proves addresses, code/data boundaries, alignment, padding and
    binary inclusions are right;
  - "a much stronger signal than 'it seems to work'";
  - "the stronger, earlier check on the *reconstruction*" -- there is no second, weaker check to be
    stronger than. Do not soften this into "a useful development-time check" either; the instruction
    is removal, and a demoted recommendation is still a competing bar.

WRITE, in the file's existing voice -- em-dash prose, bolded lead-ins, imperative and terse:
  1. Behavioural equivalence is this project's definition of correctness: scripted replay plus
     comparison at checkpoints. State it as the only one. Cite `.claude/CLAUDE.md` § Constraints as
     the source, exactly as the current text does.
  2. The consequence, kept verbatim in substance: anything not observable at a checkpoint is not
     verified. Draw the conclusion the constraint itself draws -- that this makes checkpoint design
     part of the reconstruction work rather than an afterthought.
  3. The retained hazard, re-anchored. You are free to rename routines, reorganise files, replace
     constants with symbols and add macros -- a behavioural bar is exactly what buys that freedom
     (`.planning/REQUIREMENTS.md` § Out of Scope makes the same argument). **But** reorganising
     changes addresses, which breaks self-modifying code and timing-sensitive raster routines.
     Re-verify after EACH reorganisation, not at the end of several -- and say what re-verification
     now means: replay through the checkpoint set. Keep the reason the granularity matters: one
     changed address per failing replay is a short diagnosis; ten is a bisect. Remove the
     "After identity holds you can safely..." precondition -- there is no such precondition now.
  4. OPTIONAL, your judgement -- the pointer to `observation-hazards.md` § 7. KEEP it only if it
     lands in at most one sentence as background on why a 64K RAM image is not a comparison surface
     either, WITHOUT implying that a comparison was something you were hoping to make and without
     reading as a caveat on a claim that no longer exists. DROP it otherwise: `SKILL.md` line
     152's References row and line 172's Troubleshooting row already route the reader to that file,
     so reachability does not depend on this pointer. State which way you went, and why, in the
     SUMMARY.

The rewritten section must read as though it were always written this way. It is a prose rewrite,
not a find-and-replace: no residue of the removed framing, no "no longer", no "formerly", no
apology for the change, no sentence whose shape only makes sense against the deleted text.

Do NOT touch `observation-hazards.md`, `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, or any
other skill. Those are explicitly out of scope.
  </action>
  <verify>
    <automated>
F=.claude/skills/c64-program-recon/references/reconstruction.md
N="$(tr '\n' ' ' < "$F" | tr -s ' ')"
r=0
t(){ printf '%-40s %s\n' "$1" "$2"; [ "$2" = PASS ] || r=1; }
[ "$(grep -ci 'milestone' "$F")" -eq 0 ]                && t "staged-achievement framing gone" PASS || t "staged-achievement framing gone" FAIL
[ "$(grep -ciE 'sha256sum|\bcmp\b' "$F")" -eq 0 ]       && t "byte-comparison recipe gone"    PASS || t "byte-comparison recipe gone"    FAIL
[ "$(grep -c '^```' "$F")" -eq 14 ]                     && t "fence count 16 -> 14"           PASS || t "fence count 16 -> 14"           FAIL
[ "$(grep -c '^>' "$F")" -eq 0 ]                        && t "blockquote claim gone"          PASS || t "blockquote claim gone"          FAIL
[ "$(grep -ci identity "$F")" -le 1 ]                   && t "at most the hazard-7 pointer"   PASS || t "at most the hazard-7 pointer"   FAIL
printf '%s' "$N" | grep -qi 'behavioural equivalence'   && t "behavioural equivalence stated" PASS || t "behavioural equivalence stated" FAIL
printf '%s' "$N" | grep -qi 'not observable at a checkpoint is not verified' && t "checkpoint consequence kept" PASS || t "checkpoint consequence kept" FAIL
printf '%s' "$N" | grep -qi 'timing-sensitive raster'   && t "SMC/raster hazard kept"         PASS || t "SMC/raster hazard kept"         FAIL
[ "$(grep -ci reorganis "$F")" -ge 2 ]                  && t "reorganisation warning kept"    PASS || t "reorganisation warning kept"    FAIL
grep -q 'CLAUDE.md' "$F"                                && t "CLAUDE.md cross-ref kept"       PASS || t "CLAUDE.md cross-ref kept"       FAIL
[ "$(md5sum .claude/skills/c64-program-recon/references/observation-hazards.md | cut -d' ' -f1)" = 1f006c10ba40a337a3b0731fca8ae977 ] \
  && t "observation-hazards.md untouched" PASS || t "observation-hazards.md untouched" FAIL
exit $r
    </automated>
  </verify>
  <done>All 11 gates PASS. `reconstruction.md` offers exactly one definition of correctness -- behavioural equivalence, cited to `.claude/CLAUDE.md` § Constraints -- with no comparison recipe, no blockquote claim, and no staged achievement anywhere in the file. The SMC/raster reorganisation hazard survives, re-anchored to replay-through-checkpoints. Line 8's lead-in agrees with the section it introduces. `BASE` and the § 7 pointer decision are recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Retarget SKILL.md's reconstruction.md reference row, then gate the whole change</name>
  <files>.claude/skills/c64-program-recon/SKILL.md</files>
  <action>
In the References table, the row for `` `references/reconstruction.md` `` (line ~154) has four
comma-separated items in its Covers cell. **Replace the second item only.** It currently describes
the scope of the byte-identity claim, which task 1 removed; make it describe what that section now
covers -- behavioural equivalence as the correctness bar, in the same clipped noun-phrase register as
its three neighbours (`Binary inclusion`, `SMC labels`, `label vocabulary`). Keep those three
neighbours verbatim, keep their order, and keep the row's leading path cell exactly as it is -- the
`skill-writer` checklist requires every cited path to resolve, and only the description changes.

Keep the table's format intact: unpadded `|` delimiters against the existing `|---|---|` separator,
one row per reference, six reference rows plus the `templates/` row. Do not re-align, re-pad, or
reorder the table.

Change nothing else in `SKILL.md`. In particular leave line 172's Troubleshooting row
(`Two captures of the same checkpoint differ | Expected. Full-64K identity is impossible in
principle...`) exactly as it stands -- that is the § 7 negative claim, correct as written, and it is
what keeps `observation-hazards.md` reachable regardless of task 1's optional-pointer decision.

Then run the gate below. It re-confirms the no-test finding rather than trusting the plan's
paragraph, and it enforces the non-scope boundary: exactly two files under `.claude/` may differ
between `BASE` (recorded by task 1) and `HEAD`.
  </action>
  <verify>
    <automated>
S=.claude/skills/c64-program-recon/SKILL.md
D=.claude/skills/c64-program-recon
r=0
t(){ printf '%-40s %s\n' "$1" "$2"; [ "$2" = PASS ] || r=1; }
[ "$(grep -c 'byte-identity scope' "$S")" -eq 0 ]     && t "stale row item gone"        PASS || t "stale row item gone"        FAIL
grep -q '^| `references/reconstruction.md` |' "$S"    && t "row path cell intact"       PASS || t "row path cell intact"       FAIL
[ "$(grep -c '^| `references/' "$S")" -eq 6 ]         && t "6 reference rows"           PASS || t "6 reference rows"           FAIL
grep -q 'Full-64K identity is impossible in principle' "$S" && t "troubleshooting row kept" PASS || t "troubleshooting row kept" FAIL
m=0; for p in $(grep -o 'references/[a-z0-9-]*\.md' "$S" | sort -u); do [ -f "$D/$p" ] || m=1; done
[ "$m" -eq 0 ]                                        && t "every cited path resolves"  PASS || t "every cited path resolves"  FAIL
[ -z "$(grep -rl 'reconstruction\.md' --include='*.test.*' --include='*.spec.*' --include='*.mjs' --include='*.mts' --include='*.ts' --include='*.py' . 2>/dev/null)" ] \
                                                      && t "no test cites the doc"      PASS || t "no test cites the doc"      FAIL
[ "$(md5sum "$D/references/observation-hazards.md" | cut -d' ' -f1)" = 1f006c10ba40a337a3b0731fca8ae977 ] \
                                                      && t "observation-hazards untouched" PASS || t "observation-hazards untouched" FAIL
# BASE is the sha task 1 recorded in the SUMMARY; substitute it here.
CH="$(git diff --name-only "$BASE"..HEAD -- .claude/ | sort)"
[ "$(printf '%s\n' "$CH" | grep -c .)" -eq 2 ] && printf '%s\n' "$CH" | grep -q 'references/reconstruction.md' && printf '%s\n' "$CH" | grep -q 'c64-program-recon/SKILL.md' \
  && t "exactly the 2 in-scope files" PASS || { t "exactly the 2 in-scope files" FAIL; printf 'changed: %s\n' "$CH"; }
exit $r
    </automated>
  </verify>
  <done>All 8 gates PASS. `SKILL.md`'s References row describes `reconstruction.md`'s actual current coverage; its path cell, its three other items, the six-row table shape and line 172's Troubleshooting row are unchanged. No test in the repo cites the edited doc. `observation-hazards.md` hashes to its pre-change value. Exactly two files under `.claude/` differ between `BASE` and `HEAD`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill reference doc -> future agent behaviour | An agent loads `reconstruction.md` on demand and acts on it. A stated bar in this file becomes work a future session performs; a competing bar becomes work it performs against the developer's instruction. This edit is the only enforcement mechanism -- nothing else in the pipeline stops an agent chasing a byte comparison. |
| this edit -> the wider skill corpus | Five other skills mention byte-identity for unrelated reasons (assembler behaviour, capture reproducibility, input-integrity hashing, deterministic rendering, annotation pass-through). A word-level sweep would corrupt them. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q2g-01 | Tampering | `observation-hazards.md` § 7 and § 2 | high | mitigate | § 7 is a NEGATIVE claim that already supports behavioural-only correctness; § 2's "byte-identical PC" is a wedge diagnostic. A find-and-replace on the word would destroy both. Countered by quoting § 7 in `<planning_notes>` so the file is never opened for editing, and by an md5 gate (`1f006c10ba40a337a3b0731fca8ae977`) in BOTH tasks' verify blocks. |
| T-q2g-02 | Tampering | the four other in-scope skills, `ROADMAP.md`, `REQUIREMENTS.md` | high | mitigate | Every one was audited this session and is correct as it stands; `REQUIREMENTS.md` row 122 and the ROADMAP's development-time checks are explicitly out of scope. Countered by task 2's `git diff --name-only "$BASE"..HEAD -- .claude/` gate, which fails on a third changed file, and by the task actions naming the forbidden paths. |
| T-q2g-03 | Repudiation | a demoted recommendation read as compliance | high | mitigate | The likeliest failure is not deletion but softening -- byte comparison reappearing as "a useful development-time check". That satisfies a naive reader and defeats the instruction. Countered by the fence-count gate (16 -> 14, so no recipe can return), the `identity <= 1` bound, and an explicit prohibition in task 1's action. |
| T-q2g-04 | Information Disclosure | -- | low | accept | Two markdown reference docs in a public-shaped repo. No secrets, no credentials, no host paths, no emulator state. Nothing crosses a trust boundary that did not already. |
| T-q2g-05 | Tampering | a stale gate that passes on the wrong file state | medium | mitigate | Every count in every gate is a measured before/after pair taken at HEAD `3701429` with a clean tree, recorded in `<planning_notes>`. `grep -v '^#'` is deliberately not applied because `#` is a markdown heading here and the heading is what one gate must inspect -- stated in `<planning_notes>` so a reviewer does not "fix" it into blindness. |
| T-q2g-SC | Tampering | package-manager installs | low | accept | No installs. No `npm`/`pip`/`cargo` command appears in this plan, so `RESEARCH.md`'s Package Legitimacy Audit is not engaged. |
</threat_model>

<verification>
1. Task 1's 11 gates and task 2's 8 gates all PASS.
2. Read the rewritten section end to end as a first-time reader. It must offer exactly one answer to
   "how do I know the reconstruction is correct", and that answer must be behavioural. If a reader
   could come away thinking a byte comparison is expected of them at any stage, the rewrite failed
   even with green gates.
3. The section must not read as an edit. No "no longer", no "formerly", no residual sentence shape
   that only parses against the deleted text.
4. `git diff` the two files and confirm the diff contains no incidental reflow of untouched
   paragraphs -- a rewrap of neighbouring prose makes the substantive change unreviewable.
5. `.claude/CLAUDE.md` is unchanged. This task does not register a skill, so the `skill-writer`
   warning about `gsd-tools generate-claude-md` regressing that file's hard rules does not apply --
   and must not be triggered by running it "to be safe".
</verification>

<success_criteria>
- Behavioural equivalence (scripted replay + checkpoint comparison) is the ONLY definition of
  correctness stated anywhere in `c64-program-recon`.
- Zero byte-comparison recipes and zero staged-achievement framing survive in `reconstruction.md`.
- The SMC / timing-sensitive-raster reorganisation hazard and its after-EACH-reorganisation
  granularity survive, re-anchored to behavioural re-verification.
- The `.claude/CLAUDE.md` § Constraints citation and the "not observable at a checkpoint is not
  verified" consequence survive.
- `SKILL.md`'s reference row honestly describes the rewritten section; all six cited reference paths
  resolve.
- `observation-hazards.md` hashes to `1f006c10ba40a337a3b0731fca8ae977`. Exactly two files under
  `.claude/` changed.
- No test needed adding or updating (confirmed by gate, not assumed).
</success_criteria>

<output>
Create `.planning/quick/260806-q2g-remove-byte-identity-as-a-correctness-mi/260806-q2g-SUMMARY.md` when done.

Record in it: `BASE` sha; the § 7-pointer decision (kept or dropped) with the reasoning; the
before/after token counts actually observed; and the full rewritten section quoted, so a reviewer
can judge the prose without reconstructing the diff.
</output>
