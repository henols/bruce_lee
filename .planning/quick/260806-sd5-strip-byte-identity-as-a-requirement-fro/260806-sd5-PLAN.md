---
phase: quick-260806-sd5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PLANNING-BEHAVIOURAL-GATE-ONLY

must_haves:
  truths:
    - "No byte-identity requirement, gate, promotion bar, standing principle, or definition-of-done survives anywhere in `.planning/REQUIREMENTS.md` or `.planning/ROADMAP.md`. Behaviour -- checkpoint replay against the baselines -- is the sole gate at every level: per-region, per-format-spec, and whole-rebuild."
    - "The hedge is GONE, not relocated. `REQUIREMENTS.md`'s Out-of-Scope row no longer preserves byte comparison as a development-time check or as a structural invariant on reorganisation commits. That clause is the exact thing the developer rejected on the second ruling; it does not reappear in either file in any form or at any strength."
    - "19 sites are rewritten: 3 in `REQUIREMENTS.md` (BUILD-02, DATA-06, the v2.0 milestone row) plus the Out-of-Scope row, and 16 in `ROADMAP.md`. THREE of the ROADMAP sites were NOT in the task brief's enumeration and were found by grep at plan time -- L1275 (Pitfall 3 asserting the byte diff is the *only* check), L1311 (Phase 6 Goal), L1335 (a spec is not validated until re-serialisation reproduces the original bytes). All three are gate claims and all three fall under the ruling."
    - "Requirement IDs `BUILD-02` and `DATA-06` still exist with their definitions intact-but-rewritten. Both are load-bearing: `BUILD-02` is cited by ROADMAP plan 04-02 and traceability row L159; `DATA-06` by plan 06-05 and row L176. Renumbering or dropping either breaks the coverage gate."
    - "The real engineering worries underneath the removed gates SURVIVE, re-anchored to replay: ACME silently widening a zero-page operand to 16-bit absolute shifts every following address (BUILD-02, Phase 4 criterion 2); an illegal opcode misdecoded upstream assembles cleanly (Pitfall 3); a format spec can be plausible and wrong (DATA-06, 06-05); the split is where branch-out-of-range and silent segment overlap appear (Phase 7 Pitfall 6). None is softened into a nice-to-have."
    - "Two ordering/force constraints survive verbatim because they are correct independent of mechanism: `Non-optional, every region, every phase from 4 onward` on the standing principle, and `the gate must exist before any region is promoted from blob to source` on plan 04-02."
    - "`--strict-segments` survives on its own merits everywhere it appears -- it is an assembly-time segment-overlap check, not a byte comparison, and Phase 7's Pitfall 6 note still credits it."
    - "The falsification tests survive as falsification tests. Phase 4 criterion 2 still injects a deliberate zero-page-vs-absolute drift and still demands the failure be LOCALISED -- now via VERIFY-05's divergence report naming which checkpoint and which memory region, which is the direct behavioural replacement for what the byte diff was providing (a named address range)."
    - "The tolerated survivors are untouched and PROVEN untouched by the gate, not assumed: ROADMAP L145/L164 (Phase 01-01, `[x]` COMPLETE -- dump-procedure reproducibility against the ORIGINAL, already-recorded evidence), L1241 (Pitfall 7 harness self-validation before the rebuild exists), L259/L263/L273/L276 (MCP request-response round trips), L333 (wedge-diagnostic register snapshot), L612 (committed-equals-deployed TS build property), L620 (bash/JS regex parity), L34 + REQUIREMENTS L15/L104/L114/L123/L128 (the v3.0 editable-asset pipeline, whose NAME is Round-Trip Assets), and REQUIREMENTS L33 (RECOVER-05, `[x]` COMPLETE -- byte-level diff BETWEEN THE TWO CRACKED RELEASES, which is provenance work, not rebuild correctness)."
    - "Voice matches the surrounding files: terse em-dash criteria lines with bolded lead-ins. Zero hedging -- no `where practical`, no `as a sanity check`, no `still useful for`. A hedge reintroduces exactly what was removed."
    - "Exactly two files change. `.claude/` is untouched, gated rather than trusted -- the skills were already corrected in commits `9c63239` and `1d2cfb0` and are explicitly out of scope here."
    - "The traceability tables still hold 44 requirement rows and the coverage block's numbers are unchanged -- this edit rewords requirement TEXT and touches no count."
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "the developer's two rulings <-> every site in this plan: *\"I want you to remove the byte identical, it is absolutely not a requirement, the only requirement is that it has the same behaviour\"*, then when offered a choice of three strip scopes with the tradeoffs spelled out, *\"this is the rule and it must be followed no matter what\"*. That second ruling removed the latitude the first left. WIDEST SCOPE. Do not re-argue it in a SUMMARY, do not preserve any byte gate as a lesser recommendation, and do not treat the brief's 17-site enumeration as a ceiling -- grep found 19."
    - "`REQUIREMENTS.md` VERIFY-01..07 <-> every rewritten gate: the behavioural replacement vocabulary ALREADY EXISTS and must be reused rather than reinvented. VERIFY-03 defines the checkpoint set as curated game-state regions plus framebuffer and explicitly `not a blind full-RAM hash`; VERIFY-04 commits the original's baselines as the reference; VERIFY-05 requires divergence reported as `which checkpoint, which memory region, what differed`; VERIFY-07 is `the reconstruction's definition of done`. Every rewritten line points into that set."
    - "`.claude/CLAUDE.md` § Constraints <-> this edit: `**Verification**: Behavioural equivalence only -- Correctness is defined by replay + checkpoint comparison. Anything not observable at a checkpoint is not verified` is the AUTHORITY. These two planning files currently contradict the project's own constraint block; this edit ends the contradiction. CLAUDE.md itself is NOT edited."
    - "`.claude/skills/c64-program-recon/references/reconstruction.md` § `Behavioural equivalence is this project's definition of correctness` <-> the voice of the rewritten lines: that section was rewritten one commit ago in this same effort and is the model to match. Note it cites `.planning/REQUIREMENTS.md` § Out of Scope as making the same argument -- so the Out-of-Scope row must still make that argument after this edit, only without the hedge clause. Breaking that would orphan a live cross-reference in a corrected skill."
    - "`BUILD-02` <-> ROADMAP plan `04-02` and traceability row L159; `DATA-06` <-> ROADMAP plan `06-05` and traceability row L176. The IDs are the join. Rewrite the requirement text, never the ID."
    - "ROADMAP L19 standing principle <-> every phase from 4 onward: this row is inherited by `/gsd-plan-phase` for Phases 4, 5, 6 and 7 rather than rediscovered, which makes it the single most load-bearing site in either file. A stale byte gate here re-enters every future phase plan even with all 18 other sites fixed."
---

<objective>
Strip byte-identity as a requirement from `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md`.
Behaviour -- checkpoint replay against the baselines -- becomes the only gate, at every level and in
every role.

Purpose: the developer has ruled twice, the second time removing all remaining latitude. These two
files are where byte-identity still has teeth: a project-wide standing principle declaring the byte
diff `Non-optional, every region, every phase from 4 onward`, two requirement definitions, a
promotion gate, four phase success criteria, and an Out-of-Scope row that nominally excludes
byte-identity while quietly preserving it as a development-time check. The skills were corrected in
`9c63239` and `1d2cfb0`; the planning artifacts still contradict `.claude/CLAUDE.md` § Constraints.

Output: two edited files, 19 rewritten sites, no new files, no deletions of requirement IDs.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/bruce_lee/.planning/REQUIREMENTS.md
@/workspaces/bruce_lee/.claude/skills/c64-program-recon/references/reconstruction.md

`.planning/ROADMAP.md` is ~1400 lines and mostly broker-lifecycle narrative. Do NOT read it whole.
Read only the regions each task names. Do NOT read `.planning/STATE.md` -- 640 lines, no bearing here.

The behavioural replacement vocabulary is already written and complete in `REQUIREMENTS.md`
VERIFY-01..07. Reuse it; do not invent parallel language.
</context>

<dry_run_evidence>
Every gate in this plan was executed against the UNEDITED files at plan time. Recorded here because
a gate that passes before the edit is not a gate.

**Failed pre-edit (these are the real gates):**

- `UNSTRIPPED` -- fired on exactly 19 lines: ROADMAP 19, 33, 130, 1254, 1263, 1264, 1275, 1288, 1311,
  1320, 1321, 1331, 1335, 1348, 1358, 1365 (16) and REQUIREMENTS 14, 62, 89 (3). This set IS the
  work. If the count is not 19 pre-edit, the file has moved since planning -- re-derive before editing.
- `MISSING` -- fired on the three replacement anchors, correctly absent pre-edit:
  `Checkpoint replay is the transcription gate`, `Behaviour is the only gate`,
  `rather than merely plausible` (the last is present in ROADMAP but absent in REQUIREMENTS).
- `HEDGE SURVIVES` -- fired on REQUIREMENTS L122.

**Passed pre-edit by construction, so falsified by mutation test instead:**

- `OVERREACH` -- proved live by mutating `byte-identical re-run proof` (the protected, COMPLETE
  Phase 01-01 record) to `behavioural re-run proof` in a scratch copy: gate fired
  `OVERREACH .planning/ROADMAP.md -> byte-identical re-run proof`. This is the branch that stops an
  over-reaching strip from falsifying already-recorded evidence.
- `ID LOST` -- proved live by deleting the `- **DATA-06**:` definition line in a scratch copy: gate
  fired `ID LOST: DATA-06 appears 1 time(s)`.
- `TRACEABILITY ROWS MOVED` -- 44 rows counted pre-edit; the count must not move.
- `.claude TOUCHED` -- `git status --porcelain -- .claude/` empty pre-edit, working tree clean.

**Gate hygiene note:** every negative grep is bounded to `.planning/ROADMAP.md` or
`.planning/REQUIREMENTS.md` by explicit path. This PLAN.md quotes the byte-identity vocabulary
freely (it has to, to name what is being replaced) and cannot self-invalidate the sweep. The two
hedge probes -- `just not as the definition of done` and
`structural invariant on pure-reorganisation commits` -- are deliberately NOT quoted anywhere in the
task actions below, for the same reason.
</dry_run_evidence>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md -- rewrite BUILD-02 and DATA-06 behaviourally, delete the hedge</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Four edits. Locate each by content match, not line number -- earlier edits in the same file shift
the later ones. Keep every requirement ID exactly as it is.

**Edit 1 -- BUILD-02 (currently around L62).** The current line makes a round-trip byte diff against
the canonical image the transcription-time check. Replace the whole bullet with:

  - [ ] **BUILD-02**: A transcribed region is promoted only when replaying the scenarios that exercise it diverges nowhere against the baselines, so addressing-mode drift and alignment mistakes -- ACME silently widening a zero-page operand to 16-bit absolute shifts every following address -- surface at transcription time rather than at the end

The engineering worry is the reason this requirement exists and must survive in the text. The
`- [ ] ` checkbox prefix and the `**BUILD-02**:` label stay exactly as-is.

**Edit 2 -- DATA-06 (currently around L89).** The current line requires re-serialisation to reproduce
the original bytes exactly. Replace the whole bullet with:

  - **DATA-06**: Each format spec is validated by feeding the re-serialised extraction back into the build and replaying the scenarios that exercise it -- a spec whose output drives the game to identical behaviour at every checkpoint is correct rather than merely plausible

This is a strictly stronger test than the byte compare it replaces, and it keeps the
`correct rather than merely plausible` intent the original was built around. Note the v2.0 bullets
carry no `- [ ] ` checkbox, only `- **ID**:` -- match the neighbouring DATA-05 line.

**Edit 3 -- the Out-of-Scope row (currently around L122).** This row currently excludes
byte-identity as the acceptance gate and then, in a second sentence beginning
`Byte comparison is still used`, preserves it as a development-time check per transcribed block and
as an invariant on reorganisation commits. That second sentence is precisely what the developer
rejected. Delete it entirely -- through the end of the sentence, including its closing clause -- and
widen the row. Replace the whole row with:

  | Byte-identical rebuild, at any stage and in any role | Would forbid restructuring source for readability, which conflicts directly with the "base to build on" driver. Behaviour is the only gate -- checkpoint replay against the baselines decides whether a region, a format spec, or the whole rebuild is correct (BUILD-02, DATA-06, VERIFY-05, VERIFY-07). No byte comparison is a gate, a promotion bar, or a definition of done anywhere in this project. |

The row title widens from `as the acceptance gate` to `at any stage and in any role` deliberately:
the narrow title is what made the hedge look consistent. Keep the leading `| ` and trailing ` |`
pipes and the two-column shape of the surrounding table.

**Edit 4 -- the v2.0 milestone row (currently around L14).** Within that row, replace the fragment
`formats proven by round-trip` with `formats proven by replaying their re-serialised output`. Change
nothing else in the row -- the `**This is where "fully documented and recompiled" is met.**` bold
tail and the `| 5-7 | 20 |` cells stay.

**Do not touch** RECOVER-05 (the byte-level diff between the two cracked releases -- provenance work,
already `[x]`), the `### Round-Trip Assets` heading or any ASSET-01..04 / EXT-02 line (that phrase is
the NAME of the v3.0 editable-asset feature), the `Round-trip asset converters before v3.0` and
`Level editor before v3.0` Out-of-Scope rows, the traceability table, or the coverage block.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee &amp;&amp; F=.planning/REQUIREMENTS.md; fail=0; for s in 'Behaviour is the only gate' 'rather than merely plausible' 'replaying their re-serialised output' 'zero-page operand to 16-bit absolute' 'Byte-identical rebuild'; do grep -qF -- "$s" $F || { echo "MISSING: $s"; fail=1; }; done; for s in 'just not as the definition of done' 'structural invariant on pure-reorganisation commits' 'round-trip byte diff' 'reproduces the original bytes'; do grep -qF -- "$s" $F &amp;&amp; { echo "SURVIVED: $s"; fail=1; }; done; for id in BUILD-02 DATA-06; do c=$(grep -cF "$id" $F); [ "$c" -ge 2 ] || { echo "ID LOST: $id ($c)"; fail=1; }; done; [ "$(grep -cE '^\| (RECOVER|MAP|VERIFY|DOCS|DATA|BUILD)-[0-9]+ \|' $F)" = 44 ] || { echo "TRACEABILITY ROWS MOVED"; fail=1; }; grep -qF 'byte-level diff between them is meaningful' $F || { echo "OVERREACH: RECOVER-05 damaged"; fail=1; }; grep -qF 'Round-Trip Assets' $F || { echo "OVERREACH: v3.0 asset feature name damaged"; fail=1; }; [ "$fail" = 0 ] &amp;&amp; echo "TASK1 PASS" || exit 1</automated>
  </verify>
  <done>BUILD-02 and DATA-06 exist under their original IDs with behavioural definitions. The Out-of-Scope row excludes byte-identity in any role and preserves no byte comparison. RECOVER-05, the v3.0 asset-pipeline naming, and the 44-row traceability table are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md -- strip the standing principle and all 15 phase-level byte gates</name>
  <files>.planning/ROADMAP.md</files>
  <action>
16 edits. Read ONLY these regions: 15-35, 128-132, 1250-1268, 1273-1277, 1286-1290, 1309-1336,
1345-1366. Locate each site by content match. Do not read the file whole.

**Edit 1 -- the standing principle row (around L19). The single most load-bearing site: it is
inherited by every phase plan from 4 onward.** Replace the whole row with:

  | **Checkpoint replay is the transcription gate** | A region is not "transcribed" until replaying the scenarios that exercise it diverges nowhere against the baselines. Behaviour is the only gate -- no byte comparison against the canonical image gates anything. Non-optional, every region, every phase from 4 onward. |

`Non-optional, every region, every phase from 4 onward` is copied verbatim: the force of the
principle is correct, only its mechanism changes.

**Edit 2 -- v2.0 milestone row (around L33).** Replace `all data formats proven by round-trip` with
`all data formats proven by replaying their re-serialised output`.

**Edit 3 -- Phase 6 one-liner (around L130).** Replace `prove every data format spec by round-trip`
with `prove every data format spec by replay`.

**Edit 4 -- Phase 4 success criterion 2 (around L1254).** Replace the whole numbered criterion with:

  2. The per-region promotion gate is wired into the build as a checkpoint replay against the Phase 3 baselines, and no transcribed region diverges; a deliberately introduced zero-page-vs-absolute drift in one instruction makes the replay fail and the divergence report names which checkpoint and which memory region moved.

Keep the injected-fault falsification and keep the demand that the failure be localised. VERIFY-05's
`which checkpoint, which memory region, what differed` is the direct behavioural replacement for the
named address range the byte diff used to give.

**Edit 5 -- plan 04-01 (around L1263).** Replace the fragment
`whole-image verbatim transcription that round-trips byte-identically` with
`whole-image verbatim transcription that boots and reaches gameplay`. `verbatim transcription`
itself stays -- it names `!binary` whole-image inclusion, not a byte gate. Leave the rest of the
bullet, including `(BUILD-01, BUILD-03, BUILD-04)` and `-- *strictly first*`.

**Edit 6 -- plan 04-02 (around L1264).** Replace `Round-trip diff harness` with
`Checkpoint-replay harness`. **The ordering constraint after the em-dash is correct and must survive
verbatim** -- a promotion gate must exist before anything is promoted:
`-- *strictly second; the gate must exist before any region is promoted from blob to source*`.

**Edit 7 -- Phase 4 Risks, the Pitfall 3 clause (around L1275). NOT in the task brief's
enumeration; found by grep at plan time.** It currently asserts the round-trip diff is the ONLY check
that catches an illegal opcode misdecoded upstream. Replace that clause with:

  **Pitfall 3** -- an illegal opcode misdecoded upstream assembles cleanly and only shows up as divergence under replay; "it assembles" proves nothing, so the region is not promoted until the replay covering it passes.

Leave the Pitfall 6 clause and the three Anti-Pattern clauses in that paragraph untouched.

**Edit 8 -- Phase 5 success criterion 2 (around L1288).** Replace the whole criterion with:

  2. Every region these four subsystems occupy is annotated ACME source rather than a verbatim blob, and each passes the checkpoint replay covering it with zero divergence against the baselines.

**Edit 9 -- Phase 6 Goal line (around L1311). NOT in the task brief's enumeration; found by grep.**
Replace the tail `every data format spec is proven correct by round-trip rather than merely plausible`
with `every data format spec is proven correct by replaying its re-serialised output rather than merely plausible`.

**Edit 10 -- Phase 6 success criterion 4 (around L1320).** Replace the whole criterion with:

  4. A format-validation tool re-serialises every extracted representation -- chamber, sprite, charset, animation, music -- builds each back into the game in place of the original table, and replays the scenarios that exercise it, exiting non-zero on any divergence.

The non-zero exit survives; only what it is measuring changes.

**Edit 11 -- Phase 6 success criterion 5 (around L1321).** Replace
`Every region these subsystems occupy passes the round-trip byte diff` with
`Every region these subsystems occupy passes the checkpoint replay covering it`. Leave the
`docs/coverage.md` regeneration half of the criterion intact.

**Edit 12 -- plan 06-05 (around L1331).** Replace
`Format-spec round-trip validation across all five formats` with
`Format-spec replay validation across all five formats -- re-serialise, rebuild, replay`. Leave
`(DATA-06)` and the whole `-- *strictly last; needs ...*` dependency tail.

**Edit 13 -- Phase 6 Risks, the "Looks done but isn't" clause (around L1335). NOT in the task
brief's enumeration; found by grep.** It currently ends by saying a spec is not validated until
re-serialisation reproduces the original bytes. Replace that final clause with:

  **"Looks done but isn't"** -- a format spec that is plausible but wrong is exactly what 06-05 exists to catch; a spec is not validated until the game built from its re-serialised output replays with zero divergence.

Leave the three effort traps and the Pitfall 8 clause in that paragraph untouched.

**Edit 14 -- Phase 7 success criterion 2 (around L1348).** Replace the whole criterion with:

  2. `src/` is split into per-subsystem files mirroring `docs/systems/`, and the post-split build passes the **full replay suite with zero divergence** against the pre-split build's baselines; every region `docs/hazards.md` marks unsafe to split is recorded as deliberately left intact, with the reason, at its site.

The bold emphasis moves from the byte claim to the replay claim; the hazards half is unchanged.

**Edit 15 -- plan 07-02 (around L1358).** Replace ``gated on a byte-identical `.prg` diff`` with
`gated on a zero-divergence full-suite replay`. Leave `behaviour-preserving refactor`, the `!source`
ordering note, `(BUILD-07)` and `-- *sequential before 07-03*`.

**Edit 16 -- Phase 7 Risks, the Pitfall 6 clause (around L1365).** Both byte sentences go. The
branch-out-of-range and silent-segment-overlap worry is real and gets a behavioural answer;
`--strict-segments` survives on its own, because it is an assembly-time check and not a byte
comparison. Replace the Pitfall 6 clause with:

  **Pitfall 6** -- the split is exactly where branch-out-of-range and silent segment overlap appear; `--strict-segments` catches the overlap at assembly time and a branch out of +/-127 range fails the assembly outright, so both are mechanical without any byte comparison. A pure-reorganisation commit that diverges under replay has changed behaviour, and the divergence report says which checkpoint and which region.

Write the range as the ASCII `+/-127` only if the surrounding file does not already use the `±`
character -- it does, at L1275, so use `±127` to match. Leave the Pitfall 7, Anti-Pattern 5 and
Pitfall 8 clauses in that paragraph untouched.

**Do not touch, and do not add a task for:** L145 and L164 (Phase 01-01, marked `[x]` COMPLETE --
byte-identical re-run proof of the DUMP PROCEDURE against the original, already-verified recorded
evidence; rewriting it would falsify what was actually proven), L1241 (Pitfall 7 -- capturing twice
from one original run before the rebuild exists, and it already subordinates itself), L34 (v3.0
Round-trip asset converters -- a feature name), L259/L263/L273/L276 (MCP request-response round
trips), L333 (wedge-diagnostic register snapshot), L612 (committed-equals-deployed TS build
property), L620 (bash/JS regex parity).
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee &amp;&amp; F=.planning/ROADMAP.md; fail=0; for s in 'Checkpoint replay is the transcription gate' 'Non-optional, every region, every phase from 4 onward' 'the gate must exist before any region is promoted from blob to source' 'Checkpoint-replay harness' 'boots and reaches gameplay' 'names which checkpoint and which memory region moved' 'only shows up as divergence under replay' 'full replay suite with zero divergence' 'zero-divergence full-suite replay' 'catches the overlap at assembly time' '--strict-segments'; do grep -qF -- "$s" $F || { echo "MISSING: $s"; fail=1; }; done; for s in 'round-trip byte diff' 'Round-trip diff harness' 'round-trips byte-identically' 'reproduces the canonical' 'reproduces the original bytes' 'proven correct by round-trip' 'reports byte-identical output' 'byte-identical `.prg`' 'Format-spec round-trip validation' 'proven by round-trip'; do grep -qF -- "$s" $F &amp;&amp; { echo "SURVIVED: $s"; fail=1; }; done; for s in 'byte-identical re-run proof' 're-running the recorded procedure produces a byte-identical dump' 'requiring byte-identical output' 'Round-trip asset converters' 'real MCP round-trip' 'byte-for-byte identical snapshot' 'REQUEST_ID_PATTERN'; do grep -qF -- "$s" $F || { echo "OVERREACH: protected survivor gone -> $s"; fail=1; }; done; for id in BUILD-02 BUILD-07 DATA-06 VERIFY-05; do grep -qF "$id" $F || { echo "ID LOST: $id"; fail=1; }; done; [ "$fail" = 0 ] &amp;&amp; echo "TASK2 PASS" || exit 1</automated>
  </verify>
  <done>The standing principle gates on checkpoint replay while keeping its non-optional force. All 15 phase-level byte gates are behavioural. Both ordering constraints, all four preserved engineering worries, the injected-fault falsification tests, and every protected survivor are intact.</done>
</task>

<task type="auto">
  <name>Task 3: Whole-file sweep gate -- prove the strip is complete AND did not over-reach</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
Run the sweep below over both files as a single gate. It is the same script that was dry-run against
the unedited files at plan time, where it failed on 19 `UNSTRIPPED` lines, 3 `MISSING` anchors and
`HEDGE SURVIVES`, and whose `OVERREACH` and `ID LOST` branches were separately falsified by mutation.

The gate asserts the EXPECTED SURVIVING SET by line content, in both directions. A naive
`grep -c 'byte-identi'` returning zero would be WRONG here: 15 legitimate survivors carry that
vocabulary in a different sense -- completed dump-procedure records, MCP round trips, the v3.0
asset-pipeline feature name, a wedge diagnostic, the TS build property, and the bash/JS regex parity
test. So the gate fails an incomplete strip AND an over-reaching one.

If `UNSTRIPPED` fires, a site was missed -- fix it, do not add it to the allowlist. If `OVERREACH`
fires, a protected survivor was damaged -- restore it. If `MISSING` fires, a replacement did not land
as written, most likely line-wrapped: check that each replaced line is a single unwrapped line, since
a wrapped table row or criterion silently breaks both the markdown and the grep.

Do not extend `ALLOW_*` to make the gate pass. The allowlist is the specification of what may
survive, established by grep at plan time; editing it to accommodate a missed site defeats the gate.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee &amp;&amp; RX='byte-identical|byte identical|byte-level|byte diff|byte-diff|round.trip|reproduces the (canonical|original)|byte-exact|identical output|identical `?\.prg'; fail=0; ALLOW_R=( 're-running the recorded procedure produces a byte-identical dump' 'byte-identical re-run proof' 'host round trip' 'real MCP round-trip' 'release round trip end-to-end' 'byte-for-byte identical snapshot' 'byte-identical to what is deployed' 'REQUEST_ID_PATTERN' 'requiring byte-identical output' 'Round-trip asset converters' ); ALLOW_Q=( 'Round-trip asset converters' 'byte-level diff between them is meaningful' 'Round-Trip Assets' 'round-trip pipeline' 'Byte-identical rebuild' ); check(){ f="$1"; shift; a=( "$@" ); while IFS= read -r l; do n="${l%%:*}"; b="${l#*:}"; ok=0; for s in "${a[@]}"; do case "$b" in *"$s"*) ok=1; break;; esac; done; [ "$ok" = 0 ] &amp;&amp; { echo "UNSTRIPPED $f:$n -> $(printf '%.90s' "$b")"; fail=1; }; done < <(grep -nEi "$RX" "$f"); for s in "${a[@]}"; do grep -qF -- "$s" "$f" || { echo "OVERREACH $f -> $s"; fail=1; }; done; }; check .planning/ROADMAP.md "${ALLOW_R[@]}"; check .planning/REQUIREMENTS.md "${ALLOW_Q[@]}"; for s in 'Checkpoint replay is the transcription gate' 'Non-optional, every region, every phase from 4 onward' 'the gate must exist before any region is promoted from blob to source' '--strict-segments'; do grep -qF -- "$s" .planning/ROADMAP.md || { echo "MISSING ROADMAP -> $s"; fail=1; }; done; for s in 'Behaviour is the only gate' 'rather than merely plausible' 'BUILD-02' 'DATA-06'; do grep -qF -- "$s" .planning/REQUIREMENTS.md || { echo "MISSING REQUIREMENTS -> $s"; fail=1; }; done; for s in 'just not as the definition of done' 'structural invariant on pure-reorganisation commits'; do grep -qF -- "$s" .planning/REQUIREMENTS.md &amp;&amp; { echo "HEDGE SURVIVES -> $s"; fail=1; }; done; for id in BUILD-02 DATA-06; do c=$(grep -cF "$id" .planning/REQUIREMENTS.md); [ "$c" -ge 2 ] || { echo "ID LOST $id ($c)"; fail=1; }; grep -qF "$id" .planning/ROADMAP.md || { echo "ID LOST $id in ROADMAP"; fail=1; }; done; [ "$(grep -cE '^\| (RECOVER|MAP|VERIFY|DOCS|DATA|BUILD)-[0-9]+ \|' .planning/REQUIREMENTS.md)" = 44 ] || { echo "TRACEABILITY ROWS MOVED"; fail=1; }; [ -z "$(git status --porcelain -- .claude/)" ] || { echo ".claude TOUCHED"; fail=1; }; [ "$(git status --porcelain -- .planning/ROADMAP.md .planning/REQUIREMENTS.md | wc -l)" = 2 ] || { echo "EXPECTED EXACTLY 2 CHANGED PLANNING FILES"; fail=1; }; [ "$fail" = 0 ] &amp;&amp; echo "SWEEP PASS" || { echo "SWEEP FAIL"; exit 1; }</automated>
  </verify>
  <done>`SWEEP PASS`. No byte-identity requirement language survives in either file; all 15 tolerated survivors are intact; both requirement IDs resolve; the traceability table holds 44 rows; `.claude/` is unmodified and exactly the two `.planning/` files changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ROADMAP.md` § standing principles -> every future phase plan | The L19 row is explicitly inherited by `/gsd-plan-phase` for Phases 4, 5, 6 and 7 rather than rediscovered. A byte gate left standing there re-enters every future phase plan even if all 18 other sites are fixed. This edit is the only enforcement mechanism. |
| requirement text -> the coverage/traceability gates | `BUILD-02` and `DATA-06` are join keys, cited from two traceability tables and two ROADMAP plan bullets. Rewriting text is safe; touching an ID silently breaks the requirement-coverage gate. |
| this edit -> already-verified completed records | Three sites in scope-adjacent positions are `[x]` COMPLETE evidence of what was actually proven (ROADMAP L145, L164; REQUIREMENTS L33). They describe capturing the ORIGINAL twice, not the rebuild. Rewriting them falsifies the record. |
| this edit -> the v3.0 feature namespace | `Round-Trip Assets` is the NAME of the deferred editable-asset feature across six lines in `REQUIREMENTS.md` plus ROADMAP L34. A word-level sweep renames a feature. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-sd5-01 | Tampering | ROADMAP L145/L164, REQUIREMENTS L33 (`[x]` COMPLETE records) | high | mitigate | An over-reaching strip rewrites verified evidence into a claim nobody tested. Countered by the sweep's `OVERREACH` branch, which asserts each protected survivor is still present by exact content -- and which was FALSIFIED at plan time by mutating `byte-identical re-run proof` in a scratch copy and confirming the gate fires. Task 2's action names all eight forbidden ROADMAP lines. |
| T-sd5-02 | Repudiation | the Out-of-Scope row and every rewritten criterion | high | mitigate | The likeliest failure is not omission but softening -- a byte gate returning as "still useful as a development-time check", which is verbatim what the developer rejected on the second ruling and satisfies a naive reader. Countered by two hedge probes (`just not as the definition of done`, `structural invariant on pure-reorganisation commits`), by the widened row title, and by a human read-back of the four rewritten prose paragraphs in `<verification>` -- the sweep cannot catch a hedge phrased in behavioural vocabulary. |
| T-sd5-03 | Tampering | v3.0 `Round-Trip Assets` / ASSET-01..04 / EXT-02 lines | high | mitigate | These carry the vocabulary in a wholly different sense (convert out, edit, convert back in). Countered by allowlist signatures `Round-Trip Assets`, `round-trip pipeline` and `Round-trip asset converters`, each of which is ALSO asserted still-present, so deleting one fails the gate. Task 1's action names them as do-not-touch. |
| T-sd5-04 | Tampering | `BUILD-02`, `DATA-06` | high | mitigate | Dropping or renumbering either orphans a traceability row and a ROADMAP plan reference. Countered by the `ID LOST` branch (each ID must appear >=2 times in `REQUIREMENTS.md` and >=1 in `ROADMAP.md`) plus a 44-row traceability count -- FALSIFIED at plan time by deleting the `DATA-06` definition line in a scratch copy and confirming the gate fires. |
| T-sd5-05 | Tampering | `.claude/` skills and `CLAUDE.md` | medium | mitigate | Already corrected in `9c63239` and `1d2cfb0`; a re-sweep here would churn or regress them. Countered by `git status --porcelain -- .claude/` asserted empty, and by an exactly-two-changed-planning-files assertion. |
| T-sd5-06 | Tampering | a stale gate that passes on the wrong file state | medium | mitigate | Line numbers shift as earlier edits land, so a line-addressed edit silently hits the wrong row. Countered by requiring content match rather than line number in both task actions, and by recording the pre-edit failure set (19 `UNSTRIPPED`, 3 `MISSING`, 2 `HEDGE`) in `<dry_run_evidence>` so a changed count is visible as drift. The `MISSING` branch specifically catches the line-wrapping bug that hit quick task 260806-q2g. |
| T-sd5-07 | Information Disclosure | -- | low | accept | Two markdown planning documents. No secrets, no credentials, no host paths, no emulator state. Nothing crosses a boundary that did not already. |
| T-sd5-SC | Tampering | package-manager installs | low | accept | No installs. No `npm`/`pip`/`cargo` command appears in this plan, so `RESEARCH.md`'s Package Legitimacy Audit is not engaged. |
</threat_model>

<verification>
Run the Task 3 sweep as the phase gate. It subsumes Tasks 1 and 2's verifies and adds the
cross-file assertions (`.claude/` untouched, exactly two changed files, traceability row count).

Then read the four rewritten prose paragraphs back once -- ROADMAP L19, Phase 4 criterion 2, Phase 7
Pitfall 6, and the REQUIREMENTS Out-of-Scope row -- and confirm no hedge crept in. `where practical`,
`as a sanity check`, `still useful as`, `optional`, `may also` and `for development` are all
reintroductions of what was removed. The sweep cannot catch a hedge written in behavioural
vocabulary; a human read can.
</verification>

<success_criteria>
Byte-identity exists in these two files only as an explicit exclusion and in the tolerated
different-sense survivors. Behaviour -- checkpoint replay against the baselines, divergence reported
as which checkpoint and which memory region -- is the only gate at every level. `BUILD-02` and
`DATA-06` still resolve from both traceability tables and both ROADMAP plan references. The four
engineering worries that motivated the removed gates survive, re-anchored to replay. Two files
changed, `.claude/` untouched.
</success_criteria>

<output>
Create `.planning/quick/260806-sd5-strip-byte-identity-as-a-requirement-fro/260806-sd5-SUMMARY.md` when done.

State in the SUMMARY that three sites beyond the task brief's enumeration were found and stripped
(ROADMAP L1275, L1311, L1335), and list the tolerated survivors that were deliberately left. Do not
re-argue the decision.
</output>
