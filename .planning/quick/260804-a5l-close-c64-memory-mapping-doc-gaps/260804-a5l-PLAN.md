---
phase: quick-260804-a5l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/c64-memory-mapping/scripts/driver.mjs
  - .claude/skills/c64-memory-mapping/SKILL.md
autonomous: true
requirements:
  - GAP-01
  - GAP-02
  - GAP-03
  - GAP-04
  - GAP-05
  - GAP-06
  - GAP-VALIDATE

must_haves:
  truths:
    - "GAP-01: `.claude/skills/c64-memory-mapping/SKILL.md` ends in a `## Troubleshooting` section whose body is a two-column `| Symptom | Fix |` table, matching the closing shape of `.claude/skills/acme-build/SKILL.md:154-163`. The previous closing text (the four-source table and its paragraph) is still present, immediately above it."
    - "GAP-02: The Options list states that `--max-span`'s default is 4096 bytes, that at that default a hit as wide as screen RAM ($0400-$07E7, 1000 bytes) or the $C000-$CFFF block still earns an inline comment while the 8 KB BASIC/KERNAL ROM blocks do not, and that the span cap applies only to non-flow instructions (flow instructions are capped at 2 bytes regardless)."
    - "GAP-03: The `## Reading the annotations` section documents the region-only `lookup` outcome — an address with no specific entry prints its enclosing region lines and nothing else, no error and no not-found message — names `$1234` as the worked case with the real region lines it prints, says this is the dominant case for the game's own code, and distinguishes it from the genuine zero-hit `(not in memory map)` branch at `driver.mjs:485-487`."
    - "GAP-04: `node driver.mjs annotate --no-header` runs and emits the annotated body with no header block and no leading blank line; the flag is listed in the Options list and in the script's own `usage:` text; and SKILL.md states the measured header-to-body ratio that justifies it, with the input it was measured on named."
    - "GAP-04 (no regression): `annotate` without `--no-header` produces output byte-identical to the pre-change script for the same input."
    - "GAP-05: The Options list documents that `--file -` means stdin and is identical to a bare `annotate`."
    - "GAP-06: The `## Where the data comes from` closing paragraph states that `memmap` overwrites the committed, git-tracked `memmap.json` (235,925 bytes) in place with no backup and no diff, that one of the four sources is fetched over plain `http://` with no TLS so a partially-reachable source set can silently replace tracked data, that the rebuild must therefore be followed by `git diff --stat` on `memmap.json` before it is accepted, and that because it mutates the repo it belongs behind a GSD command (`/gsd-quick`)."
    - "GAP-VALIDATE: Every command shown in SKILL.md has been re-run after the edits and its shown output still matches reality byte-for-byte, including both annotate examples reproduced from inputs reconstructed out of the shown blocks."
    - "GAP-VALIDATE: The mechanical frontmatter checker from `.claude/skills/skill-writer/SKILL.md:160-175` prints `ok   c64-memory-mapping` and no `FAIL` line for any skill."
    - "`memmap.json` is unmodified — `node driver.mjs memmap` was never run, and `git status --porcelain` reports changes to exactly two files: `SKILL.md` and `scripts/driver.mjs`."
    - "Everything the review confirmed correct is byte-identical: the frontmatter, all fenced blocks (the four-command summary and the three worked example outputs), the address-format paragraph, the two region-authority bullets, and the four-source table."
  artifacts:
    - .claude/skills/c64-memory-mapping/SKILL.md
    - .claude/skills/c64-memory-mapping/scripts/driver.mjs
  key_links:
    - "`--no-header` in `commands.annotate` (driver.mjs:506-522) <-> the `noHeader` option on `annotate()` (driver.mjs:357): the existing `flag()` helper at driver.mjs:470-473 returns the *next* argv element, so using it for a valueless flag silently consumes whatever follows. The flag must be read as a presence test on argv."
    - "SKILL.md's measured header ratio <-> the two reconstructed example inputs: the ratio is only a defensible claim if it was measured on an input the reader can rebuild from the file itself. Measuring it on some other listing makes the number unreproducible."
    - "SKILL.md's shown output blocks <-> `--no-header`: the new flag is what makes the two annotate examples exactly reproducible for the first time (the header no longer has to be elided by hand), which is how GAP-VALIDATE gets a real diff instead of an eyeball."
    - "The GAP-06 paragraph <-> `writeFileSync(MEMMAP_JSON, ...)` at driver.mjs:270: that single line is the whole hazard. A reader who does not know it is destructive will run `memmap` to 'check for updates' and commit a truncated table."
---

<objective>
Close the six documented gaps in the `c64-memory-mapping` skill: add the missing house-style
Troubleshooting table, document `--max-span`'s default, the region-only `lookup` outcome, the
`--file -` stdin form and the destructive `memmap` rebuild — and add the one code change the
review already settled, an `--no-header` flag on `annotate`.

Purpose: this skill is the front door to every annotated-disassembly session in this project, and
five of the six gaps are things a reader gets wrong *silently*. The region-only `lookup` result
(GAP-03) is the dominant case for Bruce Lee's own code and is currently undocumented; the
destructive `memmap` rebuild (GAP-06) can replace 235,925 bytes of tracked reference data with a
partial fetch and nobody would notice.

Output: an edited `SKILL.md` ending in a `Symptom | Fix` table, and a `driver.mjs` carrying one
new opt-in flag. Nothing else in either file moves.

Tracer note: there is no separate tracer slice. The only code change is a single opt-in flag
threaded through one function signature and one argv parse in one already-proven file — there is
no architecture to prove end-to-end, and a thinner slice than "the flag works" does not exist.
Task 1 is nonetheless deliberately first: `--no-header` is what makes Task 3's byte-for-byte
revalidation of the two annotate examples a mechanical `diff` rather than a manual read.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/workspaces/bruce_lee/.claude/CLAUDE.md
@/workspaces/bruce_lee/.claude/skills/c64-memory-mapping/SKILL.md
@/workspaces/bruce_lee/.claude/skills/c64-memory-mapping/scripts/driver.mjs
@/workspaces/bruce_lee/.claude/skills/skill-writer/SKILL.md

`.planning/STATE.md` is deliberately NOT referenced: it is ~37k tokens of Phase 01.6.2 broker
state with nothing to say about this skill, and this plan targets ~30% context.

`.claude/skills/acme-build/SKILL.md` is deliberately NOT @-referenced either. Only its closing
`## Troubleshooting` section (lines 154-163) is relevant, and its shape is reproduced in Task 2's
action, so the file only needs opening if that shape is unclear: an `## Troubleshooting` heading,
a blank line, then a `| Symptom | Fix |` / `|---|---|` table whose left column is the literal
error text or observed behaviour and whose right column is one imperative sentence. It is the
last thing in the file.
</context>

<reference>
## Reconstructing the example inputs (used by Task 2 and Task 3)

Both annotate examples in SKILL.md show *output*. `annotate` pads each body with `padEnd(44)`
before appending its comment (driver.mjs:394), and no shown body reaches 44 characters, so the
input is recoverable by cutting at column 44 and stripping trailing whitespace. Confirm that
assumption first: on every shown line that carries a comment, column 45 must be `;`.

Copy the shown block verbatim into a scratch file, then:

```bash
D=.claude/skills/c64-memory-mapping/scripts/driver.mjs
cut -c1-44 shown-block.txt | sed 's/[[:space:]]*$//' > reconstructed.asm

# example 1 (SKILL.md fenced block at lines 66-78, default span):
node $D annotate --no-header --file reconstructed.asm | diff - shown-block.txt

# example 2 (SKILL.md fenced block at lines 95-105, terser span):
node $D annotate --no-header --max-span 2 --file reconstructed.asm | diff - shown-block.txt
```

Scratch files go under the session scratchpad, never into the repo. The second block is a
non-contiguous excerpt of a longer listing (its addresses skip), which is harmless: `annotate`
resolves each line independently, so an excerpt reproduces exactly as an excerpt.
</reference>

<tasks>

<task type="auto">
  <name>Task 1: Add `--no-header` to `annotate` (GAP-04, code half)</name>
  <files>.claude/skills/c64-memory-mapping/scripts/driver.mjs</files>
  <reversibility rating="reversible">One opt-in flag defaulting to off in a git-tracked, dependency-free script; reverting is a `git checkout` of one file.</reversibility>
  <action>
Extend the `annotate()` options object at driver.mjs:357 — currently `{ maxSpan = INLINE_SPAN_MAX } = {}` — with a second destructured option defaulting to `false`, named `noHeader`. Gate the header-assembly block at driver.mjs:397-411 on it so that when the option is set the function returns the annotated body alone. The gate must also suppress the trailing blank line that block pushes at driver.mjs:410, so the first line of output is the first line of input; do not leave a leading empty line behind. The existing `if (referenced.size)` condition stays — the option composes with it rather than replacing it.

Parse the flag in `commands.annotate` (driver.mjs:506-522) as a **presence test on argv**, not through the `flag()` helper at driver.mjs:470-473. `flag()` returns the argv element *following* the named flag, which for a valueless flag silently swallows the next argument — put `--no-header` before `--file game.asm` and `flag()` would hand back the string `--file`. Read it with an argv membership check instead, and pass it into the options object at the `annotate(lines, { maxSpan })` call on driver.mjs:515. Leave the `maxSpan` parse on driver.mjs:509 and the stdin handling on driver.mjs:511-512 untouched.

Add the flag to the script's own `usage:` text at driver.mjs:534-541, on the existing continuation line that already carries `[--out f.asm] [--max-span N]`, keeping that block's column alignment.

Change nothing else. No new import, no new dependency — the file stays standard-library-only as its header comment at driver.mjs:9-16 promises. Do not touch `INLINE_SPAN_MAX`, the flow/span gate at driver.mjs:386-387, `cmdMemmap`, or `SOURCES`. Do not run `memmap`.

Capture the pre-edit ref before touching the file — `BASE=$(git rev-parse HEAD)` — and run this task's checks *before* committing, or the comparison below becomes vacuous.

To prove the default path did not move, copy the pre-change script from git into the *same* directory as `driver-pre.mjs` before verifying — it resolves `memmap.json` via `HERE/".."` (driver.mjs:22-25), so it only works from `scripts/`. Delete it again as the last step of this task and confirm the working tree carries no trace of it.
  </action>
  <verify>
    <automated>
D=.claude/skills/c64-memory-mapping/scripts/driver.mjs
P=.claude/skills/c64-memory-mapping/scripts/driver-pre.mjs
IN=$'        lda $d012\n        sta $d021\n        jsr $ffd2'
git show ${BASE:-HEAD}:$D > $P
A=$(printf '%s\n' "$IN" | node $D annotate)
B=$(printf '%s\n' "$IN" | node $D annotate --no-header)
C=$(printf '%s\n' "$IN" | node $P annotate)
rm -f $P
test "$A" = "$C"                                        # default path unchanged vs pre-change script
test "$(printf '%s\n' "$B" | wc -l)" -eq 3              # body only, one line per input line
test "$(printf '%s\n' "$B" | grep -c '^;;' || true)" -eq 0   # no header lines
test "$(printf '%s\n' "$A" | grep -c '^;;' || true)" -ge 4   # header still present by default
test "$(printf '%s\n' "$B" | head -1)" != ""            # no leading blank line
test "$(printf '%s\n' "$A" | tail -3)" = "$B"           # body identical in both modes
printf '%s\n' "$IN" | node $D annotate --no-header --max-span 2 >/dev/null   # composes with --max-span
printf '%s\n' "$IN" | node $D annotate --no-header --file - | grep -q 'd012' # composes with --file -
node $D 2>&1 | grep -q -- '--no-header'                 # listed in usage
test -z "$(git status --porcelain .claude/skills/c64-memory-mapping/scripts/ | grep -v ' driver.mjs$')"
echo PASS
    </automated>
  </verify>
  <done>`annotate --no-header` emits the annotated body alone with no header and no leading blank line; default-mode output is byte-identical to the pre-change script; the flag composes with `--max-span` and `--file -`; it appears in `usage:`; and `driver-pre.mjs` is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Document all six gaps in SKILL.md (GAP-01…GAP-06, doc half)</name>
  <files>.claude/skills/c64-memory-mapping/SKILL.md</files>
  <action>
Capture the pre-edit ref first — `BASE=$(git rev-parse HEAD)` — because the preservation check in `<verify>` diffs against it.

Then measure the header ratio for GAP-04, using the `<reference>` recipe: reconstruct both example inputs, run each through `annotate` in default mode, and record for each one the input line count, the total output line count, and how many of those lines are header. The review measured roughly 23-25 header lines against a 9-11 line input — a little over 2x — but write down the numbers you actually get, on inputs the reader can rebuild from the file's own examples. If your figures differ from the review's, yours are the ones that go in, because they are the reproducible ones.

Make five scoped edits. Nothing outside them changes — in particular leave the frontmatter, every fenced block, the address-format paragraph at SKILL.md:20-22, and the two region-authority bullets at SKILL.md:111-122 exactly as they are.

1. **The elision sentence (SKILL.md:80-81), for GAP-04.** It currently says a header block "is prepended (elided above)", which is true but hides the ratio. Restate it with the measured figures and name the input they came from, and point at `--no-header` as the way out. This is the sentence that explains why the flag exists, so the reason has to be visible here, not only in the Options list.

2. **The Options list (SKILL.md:83-87), for GAP-02, GAP-04, GAP-05.** Keep the `--out FILE` bullet as it stands. Rewrite the `--max-span N` bullet to say the default is 4096 bytes, and what that admits and excludes: at 4096 a hit as wide as screen RAM ($0400-$07E7, 1000 bytes) or the $C000-$CFFF block still earns an inline comment, while the 8 KB BASIC and KERNAL ROM blocks do not — which is the point, since a branch annotated "KERNAL ROM (8192 bytes)" teaches nobody anything. Say that the cap applies only to non-flow instructions: flow instructions are held to 2 bytes whatever `--max-span` says, so only a specific vector such as $0314 gets a comment (driver.mjs:386-387). Keep the existing note that `--max-span 2` yields register- and variable-level comments only. Add a `--no-header` bullet: suppresses the prepended header block, for piping annotated output straight into a file across a large listing set. Add a `--file -` bullet: reads stdin, identical to a bare `annotate` (driver.mjs:511-512).

3. **`## Reading the annotations` (after the second bullet, SKILL.md:117-122), for GAP-03.** Add the missing outcome. The existing bullets warn about a *wrong* name; nothing warns about *no* name. An address with no specific entry — anywhere in the game's own code — prints only the wider regions enclosing it, with no error and no not-found line. Run `node $D lookup '$1234'` and quote the real output: the two region entries it prints (the review saw `$0801-$9FFF … Default BASIC area` and `$0800-$9FFF … Normal BASIC Program space`). Say plainly that this is the dominant case for Bruce Lee's own code, and that a region-only answer means "the four tables do not name this address", not "this address is unmapped". Then distinguish the genuine zero-hit case: `lookup` prints `(not in memory map)` when nothing at all covers the address (driver.mjs:485-487). Before asserting anything about how reachable that branch is, scan the committed table offline for addresses with zero covering entries and word the sentence to match what you find — a conditional claim if you find none, a stated one with an example address if you do. Do not run `memmap` to find out; the answer is in the committed JSON. State how you established it, per this project's provenance rule.

4. **The `## Where the data comes from` closing paragraph (SKILL.md:140-142), for GAP-06.** It currently says only "Rebuild when a source publishes a correction". Keep that and add the hazard: `memmap` overwrites the committed, git-tracked `memmap.json` in place (driver.mjs:270 — 235,925 bytes as of 2026-08-04, confirm with `wc -c`), with no backup and no diff; one of the four sources is fetched over plain `http://` with no TLS (driver.mjs:33), and the only guard is a per-source emptiness check plus a 600-entry floor (driver.mjs:262, 268), so a partially-reachable or partially-changed source set can silently replace good tracked data with less of it. So: rebuild, then `git diff --stat` on `memmap.json` before accepting the result, and `git checkout` the file if the diff is not explainable as the correction you were expecting. Add that because it mutates the repo it belongs behind a GSD command (`/gsd-quick`), per this project's GSD Workflow Enforcement rule. Leave the four-source table itself untouched.

5. **A new `## Troubleshooting` section at the end of the file, for GAP-01.** Same shape as `acme-build`: heading, blank line, `| Symptom | Fix |` table. It goes after the four-source paragraph, and it becomes the last thing in the file. Symptoms are what a reader observes or the literal text they see; fixes are one imperative sentence. Cover, at minimum, these seven rows, drawn from GAP-02 through GAP-06:
   - Comments land on regions too wide to be useful → `--max-span 2`; the default is 4096.
   - A `JSR` or branch target got no comment at all → flow instructions are capped at 2 bytes regardless of `--max-span`; `lookup` the target directly for the ROM routine name.
   - The output is mostly header → `--no-header`.
   - `lookup` printed only wide region lines and no specific name → nothing in the four tables names that address; expected for the game's own code, so take the region and name the address from what the code does with it.
   - `lookup` printed `(not in memory map)` → nothing covers it; check the address parsed as intended, since a bare `1234` reads as decimal.
   - `no memmap.json; run: node driver.mjs memmap` → restore the committed table with `git checkout` rather than rebuilding; the rebuild needs network and overwrites tracked data.
   - `memmap` rewrote `memmap.json` and the diff is large or negative → a source was unreachable or changed; `git checkout` the file.

Match the house voice throughout: imperative, terse, provenance stated. Do not soften a claim you did not check, and do not add a claim this plan does not name.
  </action>
  <verify>
    <automated>
S=.claude/skills/c64-memory-mapping/SKILL.md
FEN='/^```/{f=!f;next} f'
# every fenced block (command summary + three worked outputs) byte-identical to the pre-edit file
diff <(awk "$FEN" $S) <(git show ${BASE:-HEAD}:$S | awk "$FEN")
# frontmatter byte-identical
diff <(sed -n '1,4p' $S) <(git show ${BASE:-HEAD}:$S | sed -n '1,4p')
grep -q '^## Troubleshooting$' $S                          # GAP-01 section exists
test "$(grep -n '^## ' $S | tail -1 | cut -d: -f2-)" = '## Troubleshooting'   # ...and is last
awk '/^## Troubleshooting$/,0' $S | grep -c '^|' | xargs -I{} test {} -ge 9   # header+sep+>=7 rows
grep -q '4096' $S                                          # GAP-02 default documented
grep -q -- '--no-header' $S                                 # GAP-04 flag documented
grep -q -- '`--file -`' $S                                  # GAP-05 stdin form documented
grep -q 'not in memory map' $S                              # GAP-03 zero-hit case named
grep -q '\$1234' $S                                         # GAP-03 worked case named
grep -q 'git diff --stat' $S                                # GAP-06 review step
grep -q '/gsd-quick' $S                                     # GAP-06 gate named
test -z "$(git status --porcelain .claude/skills/c64-memory-mapping/memmap.json)"   # table untouched
echo PASS
    </automated>
    <human-check>Read the diff of SKILL.md end to end. Every removed line should belong to the elision sentence or the `--max-span` bullet; anything else removed is a mistake.</human-check>
  </verify>
  <done>All six gaps are documented in SKILL.md; the file ends in a `Symptom | Fix` table with at least seven rows; every fenced block and the frontmatter are byte-identical to the pre-edit file; `memmap.json` is unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Revalidate the whole skill against reality (GAP-VALIDATE)</name>
  <files>.claude/skills/c64-memory-mapping/SKILL.md (read; corrections land here if a mismatch appears)</files>
  <precondition>Tasks 1 and 2 are complete and `node` (>= 18) runs in this container — the skill's own stated floor, and what `driver.mjs` needs for global `fetch` and `readFileSync(0)`.</precondition>
  <action>
This is the repo's stated bar for a finished skill — "every command in the skill has been run, and the shown output is real" (`.claude/skills/skill-writer/SKILL.md:150`). Re-run it after the edits, because Task 2 moved text next to output blocks that must still be true.

Re-run all three shown command forms and compare byte-for-byte:

1. `node $D lookup '$D011' '$FFD2'` against the block at SKILL.md:31-50. That block is truncated with a `…` line, so compare the shown lines in order against the corresponding real lines; the ellipsis marks elision, not content. Every shown line must appear, in that order, with that exact spacing and wrapping.
2. Example 1 (SKILL.md:66-78) via the `<reference>` recipe, default span, with `--no-header`. Must `diff` clean.
3. Example 2 (SKILL.md:95-105) via the same recipe, `--max-span 2`, with `--no-header`. Must `diff` clean.

Also re-run the new `$1234` lookup and the `wc -c` on `memmap.json` if Task 2 quoted either, so no number in the file is unbacked.

Then run the mechanical frontmatter checker from `.claude/skills/skill-writer/SKILL.md:160-175` verbatim, over all skills, not just this one. Require the `ok` line for `c64-memory-mapping`, and require that no skill reports a failure line.

Do **not** run `node driver.mjs memmap`. It needs network and overwrites the committed table — the exact hazard GAP-06 documents. Confirm at the end that `memmap.json` is untouched and that the working tree carries changes to exactly two files.

If any comparison fails, the file's text is what is wrong, not reality: fix SKILL.md to match what the command actually prints. The one exception is a mismatch your own Task 1 or Task 2 edit introduced — revert that edit instead. Either way, do not adjust a shown output block to something you did not run.
  </action>
  <verify>
    <automated>
D=.claude/skills/c64-memory-mapping/scripts/driver.mjs
node $D lookup '$D011' '$FFD2' > /dev/null            # both addresses resolve, exit 0
node $D lookup '$1234' | grep -q '9FFF'               # region-only case still behaves as documented

# the checker from skill-writer/SKILL.md:160-175, verbatim, captured once and gated twice
CHECK=$(node -e '
const fs=require("fs"),p=".claude/skills";
for (const d of fs.readdirSync(p)) {
  const f=`${p}/${d}/SKILL.md`;
  if (!fs.existsSync(f)) { console.log(`FAIL ${d}: no SKILL.md`); continue; }
  const m=fs.readFileSync(f,"utf8").match(/^---\r?\n([\s\S]*?)\r?\n---/);
  if (!m) { console.log(`FAIL ${d}: no frontmatter`); continue; }
  const name=(m[1].match(/^name:\s*(.+)$/m)||[])[1]?.trim();
  const desc=(m[1].match(/^description:\s*([\s\S]+?)(?=\n\S+:|$)/m)||[])[1]?.trim();
  const bad=[];
  if (name!==d) bad.push(`name "${name}" != dir "${d}"`);
  if (!desc) bad.push("no description");
  else if (!/\buse when\b/i.test(desc)) bad.push("description has no \"Use when\" trigger clause");
  console.log(bad.length ? `FAIL ${d}: ${bad.join("; ")}` : `ok   ${d}`);
}')
printf '%s\n' "$CHECK"
printf '%s\n' "$CHECK" | grep -q '^ok   c64-memory-mapping$'
! printf '%s\n' "$CHECK" | grep -q '^FAIL'            # no skill reports a failure line

test "$(git status --porcelain .claude/skills/c64-memory-mapping/ | wc -l)" -eq 2
test -z "$(git status --porcelain .claude/skills/c64-memory-mapping/memmap.json)"
echo PASS
    </automated>
    <human-check>Paste the two annotate `diff` runs from the `<reference>` recipe and the checker output into the SUMMARY. Both diffs must be empty, and the checker must show no `FAIL` line for any skill.</human-check>
  </verify>
  <done>All three shown command forms reproduce their SKILL.md output byte-for-byte (the two annotate examples by empty `diff`, the lookup block line-for-line through its elision); the frontmatter checker reports `ok` for this skill and no `FAIL` for any other; `memmap` was never run; exactly two files are modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SKILL.md → every future agent session | The skill is loaded as instruction, so a wrong or missing warning in it is executed rather than reviewed |
| `memmap` → the committed table, from four remote sources | A rebuild replaces 235,925 bytes of tracked reference data in place, from sources that are partly plaintext HTTP |
| shown output blocks → readers who never re-run them | A stale example is indistinguishable from a verified one, which is exactly what the byte-for-byte bar exists to prevent |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-A5L-01 | Tampering | `writeFileSync(MEMMAP_JSON, …)` at driver.mjs:270 | high | mitigate | This plan never runs `memmap`. SKILL.md gains the in-place-overwrite warning, the mandatory `git diff --stat` review before accepting a rebuild, and the `/gsd-quick` gate (GAP-06). Task 2 and Task 3 both assert `memmap.json` is unmodified. |
| T-A5L-02 | Spoofing | the `http://unusedino.de/…` source at driver.mjs:33, fetched without TLS | medium | mitigate (document) | Documented as a reason to diff a rebuild before accepting it. The URL is deliberately **not** switched to `https` here: whether that host serves TLS is unverified from this container, and asserting it would be an unchecked claim. Hardening it is a separate task. |
| T-A5L-03 | Tampering | the `annotate()` signature change | medium | mitigate | `noHeader` defaults to `false`, so no existing invocation or shown example changes behaviour. Proved, not assumed: Task 1 diffs default-mode output against the pre-change script pulled from git. |
| T-A5L-04 | Repudiation | a documented figure with no run behind it (the header ratio, the 235,925-byte size, the `$1234` region lines) | medium | mitigate | Every number is re-measured in the task that writes it and re-checked in Task 3. The ratio is deliberately measured on inputs reconstructable from the file's own examples, so a later reader can reproduce it. |
| T-A5L-05 | Information Disclosure | none — offline, no credentials, no emulator contact, no network call in any task | low | accept | `lookup` and `annotate` run purely off the committed JSON (driver.mjs:9-14). The only networked command in the script is excluded from this plan by name. |
| T-A5L-SC | Tampering | npm/pip/cargo installs | low | accept | **No package-manager installs occur in this plan.** `driver.mjs` stays standard-library-only with zero dependencies, so the Package Legitimacy Gate does not apply. |
</threat_model>

<verification>
- `git status --porcelain` lists exactly two modified files: `.claude/skills/c64-memory-mapping/SKILL.md` and `.claude/skills/c64-memory-mapping/scripts/driver.mjs`.
- `memmap.json` is byte-identical to HEAD; no command in the run touched the network.
- No `driver-pre.mjs` (or any other scratch file) is left anywhere under `.claude/skills/`.
- SKILL.md's fenced blocks and frontmatter are byte-identical to the pre-edit file.
- Both annotate examples reproduce by empty `diff`; the lookup block reproduces line-for-line.
- The frontmatter checker prints no `FAIL` for any skill.
- `.claude/CLAUDE.md` is unmodified — the skill is already registered and its `description` is unchanged, so no row moves, and `gsd-tools generate-claude-md` is not run (it regresses this project's hard rules, per `.claude/skills/skill-writer/SKILL.md:118-129`).
</verification>

<success_criteria>
- All six gaps closed: Troubleshooting table (GAP-01), `--max-span` default and its span/flow semantics (GAP-02), the region-only `lookup` outcome distinguished from `(not in memory map)` (GAP-03), a working documented `--no-header` plus the measured header ratio (GAP-04), `--file -` (GAP-05), and the destructive-`memmap` warning with its `git diff --stat` step and `/gsd-quick` gate (GAP-06).
- Exactly one code change: the `--no-header` flag. Default behaviour byte-identical to before.
- Every command shown in SKILL.md re-run and its shown output still real, byte-for-byte.
- Nothing the review confirmed correct was rewritten.
</success_criteria>

<out_of_scope>
- Re-reviewing the skill. The review is done; its six findings are the whole job.
- Rewriting the frontmatter/description, the three worked examples and their output, the
  address-format paragraph, the region-authority bullets, or the four-source table — beyond the
  additions the six gaps require.
- Any second code change: no `https` switch on the unusedino source, no backup-before-write in
  `cmdMemmap`, no `--max-span` default change. All three are defensible follow-ups and all three
  are separate tasks; GAP-06 documents the hazard rather than fixing it, by decision.
- Running `node driver.mjs memmap` for any reason.
- Running `gsd-tools generate-claude-md`. No CLAUDE.md change is needed, and the generator is
  known to regress this project's hard rules.
- A test suite for the skill. There is no skill-test harness in this repo, and this plan's
  assertions run as shell checks against the CLI.
</out_of_scope>

<output>
Create `.planning/quick/260804-a5l-close-c64-memory-mapping-doc-gaps/260804-a5l-SUMMARY.md` when done
</output>
