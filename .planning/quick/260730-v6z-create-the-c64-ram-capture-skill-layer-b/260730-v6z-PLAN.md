---
phase: quick-260730-v6z
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260730-v6z]
files_modified:
  - .claude/skills/c64-ram-capture/SKILL.md
  - .claude/skills/c64-ram-capture/scripts/ram-capture.mjs
  - .claude/skills/c64-ram-capture/scripts/ram-compare.mjs
  - .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs
  - .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs
  - tools/recover.mjs
  - tools/recover.test.mjs
  - .planning/todos/completed/new-ram-capture-skill.md
  - .planning/todos/completed/extract-sync-primitives-to-vice-session.md

must_haves:
  truths:
    - "`.claude/skills/c64-ram-capture/` exists as a skill: `SKILL.md` at the root, `.mjs` modules under `scripts/` — the per-skill layout every existing skill in this repo uses. Its `SKILL.md` frontmatter carries the resolved name `c64-ram-capture` and the description drafted in the todo, verbatim."
    - "`scripts/ram-capture.mjs` is the skill's single documented entry point and exports the whole layer-B surface: `assembleChunks`, `captureImage`, `captureWithFallback`, `attachAndStart`, `findEntry`, `capture`, `voidRun`, `captureBaseline`, `captureDecayReference`, `snapshotName`, plus `classifyRuns` and `VOLATILE_RANGES` re-exported from its sibling module. `tools/recover.mjs` imports the ones it uses and defines none of them."
    - "Nothing in layer B knows what a release is: no import of the registry accessor, no `recovery/`-shaped path literal, and no release identifier in any signature. `capture(triggerAddress, opts)` and `findEntry(opts)` have both dropped their dead first parameter; `captureBaseline({ outDir })` and `captureDecayReference({ outDir, runMs })` take the output directory and reject a missing one BEFORE touching the emulator; `snapshotName(port, namespace, runLabel)`'s middle parameter is named as the plain string namespace it always was (D-4 port prefixing unchanged)."
    - "All six do-not-lose items survive the move, in the code and not in a summary: the `bank:\"ram\"` vs `bank:\"rom\"` disagreement check at `$E000` is still a HARD ERROR that throws (D-08 confirmation); `capture()` still runs three `assertSameMachine` gates (`before-arm`, `after-trigger-wait`, `before-declare-good`) and still NEVER auto-resets, auto-reboots or auto-resumes on a detected restart (D-3); held keys are released AT the trigger checkpoint before any memory read; `classifyRuns` counts and RETURNS every single-bit drift candidate rather than swallowing it; and the measured numbers 994/1014/993, 2551, 137-diffs-at-Hamming-distance-1 and 264 all still sit beside the code they justify."
    - "`boot()` is split along the seam the todo names: layer B's `attachAndStart({ diskPath })` owns attach → autostart → resume → PC-moved confirmation → keyboard `LOAD\"*\",8,1` + `RUN` fallback, while `tools/recover.mjs`'s `boot()` keeps the registry read, the checkpoint-gated gate-walking loop with its press-and-HOLD rationale, the boot screenshot and the `upsertRelease` write."
    - "The moved suite lives beside the code it covers at `.claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs`, `tools/recover.test.mjs` no longer exists, and the suite still reports 27 passing tests and 0 failures — the measured pre-move baseline."
    - "A durable, directory-enumerating `node:test` gate inside the new skill fails if any `.mjs` under its own `scripts/` except the documented entry point is named in its `SKILL.md`, so a module added later cannot silently leak into the usage guide. Neither skill contains an `INTERNALS.md`, and nothing points at a maintainer document — each module's rationale lives in its own header comment."
    - "Both pending todo files are retired into `.planning/todos/completed/` — the convention gsd-core's own todo workflows already read and write — leaving `.planning/todos/pending/` empty."
    - "No behaviour change anywhere: `node tools/recover.mjs` still prints the same seven-verb usage table, every verb keeps its arguments and its output lines, and the sequence of `vice_*` calls each verb makes is unchanged in order and arguments."
  artifacts:
    - .claude/skills/c64-ram-capture/SKILL.md
    - .claude/skills/c64-ram-capture/scripts/ram-capture.mjs
    - .claude/skills/c64-ram-capture/scripts/ram-compare.mjs
    - .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs
    - .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs
    - tools/recover.mjs
    - .planning/todos/completed/new-ram-capture-skill.md
    - .planning/todos/completed/extract-sync-primitives-to-vice-session.md
  key_links:
    - "layer B's import list <-> layer A's export list (`vice-sync.mjs`: `addrNum`, `hex4`, `waitCheckpointHit`, `armedCheckpoints`) — a name mismatch is a load-time crash on every CLI verb, caught by the module-surface check rather than at the emulator."
    - "the `armedCheckpoints` tracker (layer A) <-> `capture()`'s hand-rolled arm → wait → identity-check → key-release → delete (layer B) <-> `assertSameMachine`'s injected `armedCheckpoints` parameter in `vice.mjs`. If `capture()` stops registering its trigger id through the tracker, the D-3 checkpoint-presence fallback probes ids that were never armed and a restarted machine passes as unchanged."
    - "`boot()`'s press-and-HOLD gate walk (layer C) <-> `capture()`'s release-at-trigger loop (layer B). These two halves are now in DIFFERENT FILES, connected only by the `heldKeys` -> `releaseKeys` hand-off; the reproducible dump depends on both, so each half's comment must point at the other."
    - "`SKILL.md` <-> the new skill's leak gate — the only durable thing keeping module names out of the usage guide, deliberately one-directional because there is no companion document to check names against."
    - "the `baseline` / `decay-reference` CLI verbs <-> `outDir` — after this move `tools/recover.mjs` is the ONLY place that knows the `recovery/machine` layout, so the two verbs must pass it explicitly or the artifacts land nowhere."
---

<objective>
Create the `c64-ram-capture` skill — layer B of the three-layer seam in `tools/recover.mjs` — by moving the reproducible-RAM-capture surface out of that tool into `.claude/skills/c64-ram-capture/scripts/`, and retire both pending todos.

Purpose: layer A (the checkpoint-synchronisation primitives) already landed in `vice-session` as `vice-sync.mjs` (quick task 260730-u9w). Layer B is the other half of the same seam: everything that decides what makes a 64K dump trustworthy and reproducible, with no concept of a release, no `recovery/` layout and no registry. ROADMAP Phase 3 (Verification Harness & Original Baselines) is layers A + B again with no registry involved, so making this surface take plain paths rather than a release id is what Phase 3 needs, not speculative future-proofing.

Output: a new skill with a usage-only `SKILL.md`, two modules carrying their rationale in their own header comments, the 333-line suite moved to sit beside the code it covers, a slimmer `tools/recover.mjs` holding only layer C (registry reads, the gate walk, the `recovery/<id>/dumps/` layout, the CLI and the lease), a durable module-leak gate for the new skill, and an empty `.planning/todos/pending/`.

Explicitly NOT in scope: any behaviour change; any new CLI (the verbs all stay in `tools/recover.mjs`); moving `tools/releases.mjs`; any maintainer document. `INTERNALS.md` was deleted by developer decision and must not come back under any name, in either skill.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/todos/pending/new-ram-capture-skill.md
@.planning/todos/pending/extract-sync-primitives-to-vice-session.md
@.planning/notes/reusable-capture-harness-seam.md
@.planning/STATE.md
@.claude/CLAUDE.md
@tools/recover.mjs
@tools/recover.test.mjs
@.claude/skills/vice-session/scripts/vice-sync.mjs
@.claude/skills/vice-session/scripts/skill-docs.test.mjs
@.claude/skills/vice-session/SKILL.md
@.claude/skills/devcontainer-host-path/SKILL.md
</context>

<measured_baselines>
Measured in this container while planning — not recalled. The gates below key off these numbers.

- `VICE_SKIP_RESOURCE_INSTALL=1 node --test tools/recover.test.mjs` → `tests 27 / suites 0 / pass 27 / fail 0`. The summary lines are prefixed with `ℹ`, not `#` — grep for `pass 27` / `fail 0` as substrings.
- `wc -l`: `tools/recover.mjs` 874, `tools/recover.test.mjs` 333, `vice-sync.mjs` 261.
- `grep -n 'call("vice_execution_run"' tools/recover.mjs` → exactly 3 sites: lines 170 and 182 (both inside `boot()`'s generic autostart/fallback half) and line 723 (`captureDecayReference`). ALL THREE move to layer B, so `tools/recover.mjs` ends with zero.
- `grep -c 'assertSameMachine(' tools/recover.mjs` → 3, all three inside `capture()`. All three move; `tools/recover.mjs` ends with zero.
- `findEntry(releaseId, …)` never uses `releaseId` in its body either — the same vestigial parameter the todo flags on `capture()`. The todo's table already writes it as `findEntry(opts)`, so it drops there too.
- `captureWithFallback` is currently module-private (`async function`, no `export`). It becomes an export: three layer-B functions call it, and Phase 3's harness will want it directly. Adding an export is not a behaviour change.
- **VICE is NOT reachable from this container right now**: `node tools/recover.mjs` warns `the default instance (port 6510) is not answering -- ECONNREFUSED`. It then still prints the usage table and exits 0, because with no registry `acquire()` returns the default instance anyway (D-7). So `node tools/recover.mjs | grep -q ...` IS a usable static gate — it proves every import resolves and the verb table is intact without an emulator — but `node tools/recover.mjs reproduce danish` cannot be run unless the host emulator comes back. Task 3 handles that honestly; do not fabricate a live pass.
- `grep` in this container is `ugrep 7.5.0` presenting as `grep`. Stick to `-q -c -F -E -n`; those behave as expected here.
- `.planning/todos/completed/` does not exist yet, but it IS the established convention: gsd-core's own `check-todos.md`, `add-todo.md`, `note.md` and `execute-phase.md` all read and write that exact path. Both todo files are git-tracked (`git ls-files .planning/todos/`), so retire them with `git mv`.
- The `## Project Skills` table in `.claude/CLAUDE.md` is a GSD-MANAGED generated section (`gsd-core/bin/lib/profile-output.cjs`), regenerated from `.claude/skills/*/SKILL.md`. Do NOT hand-edit it — the new skill appears there on the next profile regeneration.
- `.planning/config.json` carries an unrelated pre-existing modification. Do not stage it, do not revert it.
</measured_baselines>

<source_coverage_audit>
Every item in the primary spec (`.planning/todos/pending/new-ram-capture-skill.md`) and every numbered scope specific from the orchestrator, mapped to the task that covers it. No item is deferred.

| Item | Source | Covered by |
|---|---|---|
| SPEC-01 `assembleChunks`, `captureImage`, `captureWithFallback` move | todo table | Task 1 |
| SPEC-02 `capture()` moves, dead `releaseId` param dropped | todo table + scope 7 | Task 1 |
| SPEC-03 `findEntry(opts)` moves, vestigial first param dropped | todo table | Task 1 |
| SPEC-04 `classifyRuns` + `VOLATILE_RANGES` move with all three per-range reasons | todo table | Task 1 |
| SPEC-05 `voidRun` moves (rename + evidence note) | todo table | Task 1 |
| SPEC-06 `captureBaseline` / `captureDecayReference` move, output dir parameterized | todo table + scope 7 | Task 1 |
| SPEC-07 `snapshotName` moves, middle param renamed | todo table + scope 7 | Task 1 |
| SPEC-08 `boot()` split: generic `attachAndStart({ diskPath })` out, gate loop + `upsertRelease` stay | todo "Also split" + scope 6 | Task 1 |
| SPEC-09 do-not-lose list (all six items) | todo "Do not lose" + scope 5 | Task 1, gated in verify |
| SPEC-10 all 333 test lines move with the code; count re-verified | todo table + scope 3 | Task 1 |
| SPEC-11 cross-skill import of layer A from `../../vice-session/scripts/` | todo + scope 2 | Task 1 |
| SPEC-12 skill named `c64-ram-capture`, drafted description used verbatim | todo "Naming" + scope 1 | Task 2 |
| SPEC-13 usage-only `SKILL.md` naming no internal module; NO `INTERNALS.md`, no pointer to one | todo doc posture + scope 4 | Task 2 |
| SPEC-14 module-leak gate covers the new skill directory | todo + scope 4 | Task 2 |
| SPEC-15 behaviour-equivalence check, with an honest static fallback if VICE is unreachable | scope 8 | Task 3 |
| SPEC-16 both pending todos retired using the repo's established convention | scope 9 | Task 3 |
| SPEC-17 no behaviour change anywhere | scope, closing line | gated in every task's verify |
</source_coverage_audit>

<tasks>

<!-- planner-discipline-allow: ram-capture.mjs -->
<!-- planner-discipline-allow: ram-compare.mjs -->
<!-- planner-discipline-allow: skill-docs.test.mjs -->
<!-- planner-discipline-allow: .test.mjs -->
<!-- planner-discipline-allow: releaseId -->
<!-- planner-discipline-allow: releases.mjs -->
<!-- planner-discipline-allow: recovery -->
<!-- planner-discipline-allow: INTERNALS -->
<!-- planner-discipline-allow: vice_execution_run -->
<!-- planner-discipline-allow: vice-sync.mjs -->
<!-- planner-discipline-allow: armedCheckpoints -->

<task type="tracer">
  <name>Task 1: Move layer B into the new skill's two modules, move its suite, rewire tools/recover.mjs</name>
  <files>.claude/skills/c64-ram-capture/scripts/ram-capture.mjs, .claude/skills/c64-ram-capture/scripts/ram-compare.mjs, .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs, tools/recover.mjs, tools/recover.test.mjs</files>
  <read_first>tools/recover.mjs (all 874 lines — the source of every symbol moving), tools/recover.test.mjs (all 333 lines — moves verbatim), .claude/skills/vice-session/scripts/vice-sync.mjs (lines 1-48 and 100-131: the header-comment voice to match, and the `armedCheckpoints` tracker contract layer B must write through)</read_first>
  <precondition>`.claude/skills/vice-session/scripts/vice-sync.mjs` exists and exports `addrNum`, `hex4`, `waitCheckpointHit` and `armedCheckpoints` — layer A landed in quick task 260730-u9w and layer B imports it rather than reimplementing it.</precondition>
  <reversibility rating="reversible">Pure code motion. `git checkout -- tools/recover.mjs tools/recover.test.mjs` plus deleting the new skill directory restores the starting state exactly.</reversibility>
  <action>
This is a MOVE, not a rewrite. Cut each region out of `tools/recover.mjs`, paste it into the new module with its comments intact, and change nothing else. Comments carrying measured numbers and recorded incidents are the evidence for the design — they travel verbatim. Do not reword, condense, renumber or "modernise" any of them. Do not change a function body, an error message, a control flow, or the order/arguments of any `vice_*` call. The one exception to verbatim: where a comment refers to a function that now lives in the OTHER file, add a pointer to its new home — add, never remove.

Every emulator call still goes through `call()` from `vice-session`; no new tool name is introduced anywhere in this task.

**Create `.claude/skills/c64-ram-capture/scripts/ram-compare.mjs`** — the reproducibility verdict, and the only module here that never touches the emulator. It must have NO import statements at all: pure functions over two buffers.

- Move `VOLATILE_RANGES` with all three entries and each entry's recorded `reason` string unchanged (the 6510 on-chip port pair, the stack page, the KERNAL work area / BASIC input buffer), and the doc block above it explaining that volatile scratch is excluded from the reproducibility assertion but RECORDED with a reason, never silently dropped.
- Move `classifyRuns` and its private `inRanges` / `popcount` helpers, and the whole doc block above it — every clause: why not all 64K (994 bytes between two 20s idle runs, 1014 on a repeat, 993 between two back-to-back baselines with the machine never deliberately run); why no baseline (a power-on baseline was TRIED and REFUTED, `mode:"hard"` reports a power cycle but does not restore pristine RAM once the machine has run, real hardware behaves the same, and the drift accumulates during the capture itself); why not address-set exclusion (refuted too — drift is stochastic per run and an idle control yielding 1014 drift-prone addresses covered only 2 of 137 real diffs); the surviving discriminator being a property of the VALUE (all 137 differing bytes outside volatile scratch had Hamming distance exactly 1, a real write differs in ~4 bits on average, so multi-bit FAILS and the contract stays falsifiable rather than vacuous); and the honest limit stated as a limit (a genuine divergence differing in exactly one bit would be misclassified, ~3% for random byte pairs, which is WHY every drift candidate is counted and returned for inspection instead of swallowed).
- Give the module a short header comment saying what it is and why it is separate: the verdict is a property of the two byte images, provable without an emulator, and keeping it emulator-free is what lets the reproducibility rule be tested exhaustively in-process.

**Create `.claude/skills/c64-ram-capture/scripts/ram-capture.mjs`** — the skill's single documented entry point.

Its header comment is load-bearing and is the ONLY home for this layer's rationale. This skill ships no maintainer document and none may be created: a file enumerating module names, internal functions and state files is itself the internal-mechanics disclosure the usage-only guide exists to prevent. Write it in `vice-sync.mjs`'s voice, carrying in full:

- What this layer is — capture a running C64's full 64K RAM as a verified flat image and prove two captures are equivalent under RAM drift. It knows nothing about releases, disk registries or any `recovery/` directory layout; every path and namespace comes in as an argument.
- Why it is a separate SKILL rather than more of `vice-session`: `vice-session` owns how to reach the emulator, survive it and synchronise with it; this owns what makes a dump TRUSTWORTHY and REPRODUCIBLE. Record the naming decision too — "RAM", not "memory", because `c64-memory-mapping` already exists and does something completely different (address lookup and disassembly annotation), so a `c64-memory-capture` sibling would be a confusing pair.
- Its two sibling-skill dependencies and that copying this skill means copying all three: layer A's synchronisation primitives plus the transport/session seam from `vice-session`, and host-path translation from `devcontainer-host-path` (VICE attaches disks and writes screenshots on the HOST, so a container path silently fails). Note that address normalisation and the checkpoint wait come from layer A and must NEVER be hand-rolled here — the deleted paused-poll helper is the bug that happens when a primitive drifts from the rationale that shaped it.
- The three identity gates in `capture()` and the rule that a detected restart NEVER triggers an automatic reset, reboot or resume (D-3): captures are deterministic and cheap to repeat, and a wrong dump is not something to paper over automatically.
- The `bank:"ram"` vs `bank:"rom"` disagreement check at `$E000` and why it is a HARD ERROR rather than a warning (D-08 confirmation: research verified bank scoping only against the idle pre-boot machine, so it is re-confirmed while the game is actually running, and a dump taken with broken bank scoping is not a RAM image at all).
- The key-release rule: the caller presses and HOLDS a gate key, and this layer releases it AT the trigger checkpoint — a program event, therefore the same CPU cycle every run — before any memory is read. Point at the caller's gate walk (`tools/recover.mjs`'s `boot()`) as the other half of that contract.

Imports: `call`, `beginSession`, `assertSameMachine`, `serverInfo`, `readEpoch`, `lastToolCall` from `../../vice-session/scripts/vice.mjs`; `addrNum`, `hex4`, `waitCheckpointHit`, `armedCheckpoints` from `../../vice-session/scripts/vice-sync.mjs`; `tryHostPaths` from `../../devcontainer-host-path/scripts/hostpath.mjs`; `createHash` from `node:crypto`; `existsSync`, `mkdirSync`, `renameSync`, `writeFileSync` from `node:fs`; `join` from `node:path`. Give the module its own private one-line `sleep` helper (`captureDecayReference` needs it) — `tools/recover.mjs`'s copy becomes dead and is deleted there.

Move, with comments intact:

1. `assembleChunks(chunks)` — order-independent assembly, every one of its three throw sites (out-of-range start, overrun, overlap, uncovered address) and the doc note that it throws WITHOUT returning a partial image.
2. `captureImage({ call: callFn, ranges, chunkSize })` — unchanged, still taking `call` as an injected parameter (this is what the suite drives with a stub).
3. `captureWithFallback(callFn)` — now `export`ed. One 65536-byte read first, 4096-byte chunks on failure, still returning `largeReadError` on the fallback path.
4. `attachAndStart({ diskPath })` — the generic half of `boot()`. Take the FIRST of `boot()`'s two stacked doc comments (attach + autostart, confirm progress by checking PC actually moved from the immediate post-reset value, keyboard `LOAD"*",8,1` + `RUN` fallback) and reword only "the release's disk image" to "the given disk image". Body: `vice_registers_get` for the pre-value, `tryHostPaths` wrapping `vice_disk_attach` unit 8 + `vice_autostart`, then the resume with its comment explaining that autostart only ARMS the load and the CPU is still halted from the hard reset (which uses `run_after:false` deliberately, so attach happens against a stopped machine) — without this the loader never executes at all. Then the 5-attempt PC-moved loop, then the keyboard fallback. Return `{ method, fallbackUsed, hostPath }`. It does NOT take a screenshot and does NOT write to any registry.
5. `findEntry({ batchSize = 400, maxBatches = 150 } = {})` — first parameter dropped (it was never read). Full doc comment kept: bounded generic search, never an unbounded `run_until` on an unverified address, watching call-stack depth for two consecutive batches at the same depth as the steady-state-loop signature, `vice_disassemble` sanity-checking the landing address. The failure message keeps its host-side-restart instruction verbatim.
6. `capture(triggerAddress, { releaseKeys = [], session } = {})` — first parameter dropped. Everything else byte-for-byte: the own-session fallback so a standalone caller is covered by identity checking too (D-3); the cheap epoch-only `before-arm` gate with its comment about not arming against a machine that was never verified; `vice_checkpoint_add` and registering the id through layer A's tracker; the long comment recording why `vice_run_until` is deliberately NOT used (its own temporary checkpoint at the same address, two live checkpoints observed at one address after a failed attempt, both host-server crashes so far during checkpoint + run_until work) and why waiting on `hit_count` beats a paused-poll; the wait via layer A; the `after-trigger-wait` gate with its comment that the long wait is where an outage is most likely to have landed and that the still-armed trigger id is the fallback-probe target; the key-release loop with its full "NOW -- at the trigger, which is a program event and therefore the same CPU cycle on every run" comment and its warn-and-continue error handling; the checkpoint delete + untrack, also warn-and-continue; the `$E000` ram-vs-rom read pair and its HARD `throw`; the `$01` read; `captureWithFallback`; the `before-declare-good` gate with its comment that this is the last thing to touch the machine before the image is trusted; the sha256; the `vice_ping`; and the returned record with the `videoStandard: "PAL"` line and its provenance comment.
7. `voidRun({ binPath, capturePath, reason, baselineEpoch, currentEpoch, lastToolCall })` — unchanged, with the whole doc block: rename to `*.VOID-<ISO timestamp>` so it can never be mistaken for a valid capture, sibling `.VOID.json` evidence note so a voided run is itself evidence rather than merely discarded, missing artifacts are a silent no-op because `capture()` can fail before either file was written, and the explicit "deliberately NOT a reset/reboot/resume of any kind" clause.
8. `captureBaseline({ outDir } = {})` — output directory parameterized. VALIDATE `outDir` as the very FIRST statement and throw a message naming `outDir` before any emulator call, so a caller that forgot it cannot power-cycle the machine as a side effect. Everything else unchanged: the `run_after:false` hard reset with the whole "MUST be the first emulator action after a fresh emulator process starts" block (the 2551-byte mid-epoch drift measurement, and that a mid-epoch baseline is NOT a power-on baseline and must not be treated as one), the 65536-byte length assertion, `poweron.bin` + `poweron.json` written into `outDir`, the recorded epoch, and the `caveat` string verbatim.
9. `captureDecayReference({ outDir, runMs = 20000 } = {})` — same `outDir` validation first, before anything else. Doc block kept whole: build the decay-prone set empirically, no disk and no keypress so nothing a program did can be confused with drift, and two samples under-cover a stochastic effect so this is a FLOOR not a complete set — residual drift surfaces as a reported mismatch rather than being silently absorbed, which is the honest failure direction. `decay-prone.json` goes into `outDir`, `note` string verbatim.
10. `snapshotName(port, namespace, runLabel)` — middle parameter renamed only; output format and the whole D-4 doc comment (name-only tool, shared host directory, no overwrite-safe alternative, applied UNCONDITIONALLY including the port-6510 fallback so a name never depends on whether a pool happened to be running) unchanged.
11. Re-export the comparison surface from the sibling module in one line, so callers and the usage guide need exactly one module path.

**Move the suite** to `.claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs` with `git mv`, then change ONLY: (a) the import of the capture surface to `./ram-capture.mjs`; (b) `classifyRuns` / `VOLATILE_RANGES` to come from `./ram-compare.mjs`; (c) the two `vice-session` import paths to `../../vice-session/scripts/…`; (d) the first sentence of the header comment, which names the old file — repoint it at the two modules and leave the rest of that comment (the recorded deviation from the no-test-framework line, and why this is the one place a silent bug corrupts every downstream provenance verdict) untouched. Every test body, name and assertion stays byte-identical. The count must still be 27.

**Rewire `tools/recover.mjs`** — it keeps ONLY layer C.

- Import from `../.claude/skills/c64-ram-capture/scripts/ram-capture.mjs`: `attachAndStart`, `findEntry`, `capture`, `voidRun`, `snapshotName`, `captureBaseline`, `captureDecayReference`, `classifyRuns`. Keep its existing imports of `call`, `beginSession`, `MachineRestartedError`, `useInstance`, `activeInstance`, the pool `acquire`, the three registry accessors, and layer A's `addrNum`, `hex4`, `runToCheckpoint`, `screenshot`, `reset as syncReset` (the `reset` re-export stays exactly as it is). DELETE the imports that no longer have a consumer: `createHash`, `existsSync`, `renameSync`, `serverInfo`, `readEpoch`, `assertSameMachine`, `lastToolCall`, `armedCheckpoints`, `waitCheckpointHit`, `tryHostPaths`, and the now-dead local `sleep`.
- `boot(releaseId)` becomes: registry read, one call to `attachAndStart` with the joined disk path, then the gate-walking loop UNCHANGED — including the entire press-and-HOLD comment with both measured delivery mechanisms (the 264-of-65536-byte drift from a timed release, `$0049` among the differing bytes, the whole stack page; and the cycle-identical stepping that the crack NEVER sees, the machine sitting at one address for 150s) and its conclusion that the release is gated on a program event rather than elapsed time. ADD one clause pointing at the capture layer as where that release now happens. Keep the second stacked doc comment (gates come from the registry, so a third release is one more registry entry), the boot screenshot, the `upsertRelease` write and the returned shape.
- `recover()` and `reproduce()` keep their bodies and every comment; only the call sites shorten (`findEntry()`, `capture(triggerAddress, { releaseKeys, session })`).
- The CLI keeps ALL seven verbs with their exact argument parsing, usage strings, output lines and `process.exitCode` behaviour, and the whole lease doc comment. The `find-entry` and `capture` verbs still REQUIRE their release-id argument (that is the existing surface; only the internal call loses the parameter). The `baseline` and `decay-reference` verbs now pass `outDir: join(REPO_ROOT, "recovery", "machine")` — this file is the only place that layout is known.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; LOG=$(mktemp); VICE_SKIP_RESOURCE_INSTALL=1 node --test .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs > "$LOG" 2>&1 || { tail -25 "$LOG"; echo "moved suite FAILED"; exit 1; }; grep -q "pass 27" "$LOG" || { tail -14 "$LOG"; echo "expected 27 passing (measured pre-move baseline)"; exit 1; }; grep -q "fail 0" "$LOG" || { tail -14 "$LOG"; exit 1; }; test ! -e tools/recover.test.mjs || { echo "old suite path still exists -- the file was copied, not moved"; exit 1; }; echo SUITE-OK'</automated>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; VICE_SKIP_RESOURCE_INSTALL=1 node --input-type=module -e "
import * as B from \"./.claude/skills/c64-ram-capture/scripts/ram-capture.mjs\";
import * as K from \"./.claude/skills/c64-ram-capture/scripts/ram-compare.mjs\";
import * as R from \"./tools/recover.mjs\";
const moved = [\"assembleChunks\",\"captureImage\",\"captureWithFallback\",\"attachAndStart\",\"findEntry\",\"capture\",\"voidRun\",\"captureBaseline\",\"captureDecayReference\",\"snapshotName\",\"classifyRuns\",\"VOLATILE_RANGES\"];
for (const n of moved) if (!(n in B)) throw new Error(\"entry point missing export: \" + n);
for (const n of [\"classifyRuns\",\"VOLATILE_RANGES\"]) if (!(n in K)) throw new Error(\"compare module missing: \" + n);
for (const n of moved) if (n in R) throw new Error(\"recover.mjs still carries a moved symbol (duplication): \" + n);
for (const n of [\"boot\",\"recover\",\"reproduce\",\"reset\"]) if (!(n in R)) throw new Error(\"recover.mjs lost layer C symbol: \" + n);
if (B.capture.length !== 1) throw new Error(\"capture must be (triggerAddress, opts=…): arity \" + B.capture.length);
if (B.findEntry.length !== 0) throw new Error(\"findEntry must be (opts=…): arity \" + B.findEntry.length);
if (B.snapshotName(6510, \"ns\", \"run1\") !== \"p6510_ns_gameentry_run1\") throw new Error(\"snapshotName output changed\");
if (K.VOLATILE_RANGES.length !== 3 || K.VOLATILE_RANGES.some(r => !r.reason)) throw new Error(\"a volatile range lost its recorded reason\");
for (const [fn, label] of [[B.captureBaseline, \"captureBaseline\"], [B.captureDecayReference, \"captureDecayReference\"]]) {
  let msg = null; await fn({}).then(() => { throw new Error(label + \" accepted a missing output dir\"); }, e => { msg = e.message; });
  if (!/outDir/.test(msg)) throw new Error(label + \" must reject a missing outDir before touching the emulator, got: \" + msg);
}
console.log(\"SURFACE-OK\");
"'</automated>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; B=.claude/skills/c64-ram-capture/scripts/ram-capture.mjs; K=.claude/skills/c64-ram-capture/scripts/ram-compare.mjs; for n in 994 1014 993 137; do grep -qF "$n" "$K" || { echo "measured number $n missing from the comparison module"; exit 1; }; done; grep -qF 2551 "$B" || { echo "the 2551-byte mid-epoch baseline measurement was lost"; exit 1; }; grep -qF 264 tools/recover.mjs || { echo "the 264-byte timed-release measurement was lost from the gate walk"; exit 1; }; test "$(grep -cE "^import " "$K")" = 0 || { echo "the comparison module must stay emulator-free (no imports)"; exit 1; }; test "$(grep -cF releaseId "$B")" = 0 || { echo "a release identifier still appears in layer B"; exit 1; }; test "$(grep -cE "join\([^)]*\"recovery\"" "$B")" = 0 || { echo "layer B still knows a recovery/ path layout"; exit 1; }; test "$(grep -cF releases.mjs "$B")" = 0 || { echo "layer B imports the registry accessor"; exit 1; }; test "$(grep -c "assertSameMachine(" "$B")" = 3 || { echo "expected exactly 3 identity gates in layer B"; exit 1; }; test "$(grep -cF "call(\"vice_execution_run\"" tools/recover.mjs)" = 0 || { echo "a resume site stayed behind in recover.mjs"; exit 1; }; test "$(grep -cF "call(\"vice_execution_run\"" "$B")" = 3 || { echo "expected 3 resume sites in layer B (2 autostart, 1 decay reference)"; exit 1; }; grep -qF "bank: \"rom\"" "$B" || { echo "the ram-vs-rom bank check was lost"; exit 1; }; node tools/recover.mjs 2>/dev/null | grep -q "reproduce <release>" || { echo "CLI verb table changed or an import failed to resolve"; exit 1; }; echo EVIDENCE-OK'</automated>
  </verify>
  <done>The new skill's two modules exist and hold the whole layer-B surface with their rationale in their own headers; the suite sits beside them and still reports 27 passing / 0 failing from its new path; `tools/recover.mjs` imports what it uses, defines no moved symbol, keeps `boot()`'s gate walk with the 264-byte comment and prints an unchanged verb table; layer B contains no release identifier, no registry import and no `recovery/` path; the three identity gates, the ram-vs-rom hard error, the three resume sites and every measured number are present and gated.</done>
</task>

<task type="auto">
  <name>Task 2: Write the usage-only SKILL.md and give the new skill its own module-leak gate</name>
  <files>.claude/skills/c64-ram-capture/SKILL.md, .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs</files>
  <read_first>.claude/skills/vice-session/SKILL.md (the usage-only posture to mirror, including its "Copying this skill elsewhere" section), .claude/skills/devcontainer-host-path/SKILL.md (the "## From a script" precedent for a skill whose surface is an import, not a CLI), .claude/skills/vice-session/scripts/skill-docs.test.mjs (all 71 lines — the gate this one is a deliberate sibling of)</read_first>
  <action>
Write `.claude/skills/c64-ram-capture/SKILL.md` as a USAGE-ONLY guide. Frontmatter: `name: c64-ram-capture`, and `description:` exactly the draft the todo resolved — "Capture a running C64's full 64K RAM as a verified flat image, and prove two captures are equivalent under RAM drift. Use when asked to dump RAM, depack a program by running it, capture a memory image at a checkpoint, or compare two captures for reproducibility." It is deliberately scoped so it does not fire on "annotate this listing" (that is `c64-memory-mapping`) or "assemble this source" (that is `acme-build`).

Body: what a caller does, never how it works inside. Follow `devcontainer-host-path`'s "## From a script" shape, since this skill's surface is an import rather than a CLI. Name exactly ONE module path — the entry point — and route everything through it, including the comparison functions (that is what the re-export is for). Cover: capturing at a trigger address and what comes back (the 64K image, its sha256, the chunk size actually used, the machine/chip fields); handing over any keys the caller is holding so they are released at the trigger; comparing two captures for reproducibility and how to read the verdict (multi-bit differences fail, single-bit drift candidates are returned for inspection, volatile scratch is excluded and counted); voiding a run's artifacts when the machine's identity could not be proven; and the two `machine/` writers taking the directory to write into. Add a "## Copying this skill elsewhere" section stating that it depends on the sibling `vice-session` and `devcontainer-host-path` skills and that all three must be copied together — without naming any script path or import path from those skills.

Do NOT name any other module of this skill. Do NOT create an `INTERNALS.md` or any companion maintainer document, and do NOT add a "see also" pointing at one — a doc enumerating module names, internal functions and state files is itself the internal-mechanics disclosure the usage-only split exists to prevent, and `vice-session`'s was deleted deliberately for exactly that reason. Do not hand-edit the `## Project Skills` table in `.claude/CLAUDE.md`: it is a GSD-managed generated section and picks this skill up from the filesystem on the next regeneration.

Then write `.claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs` as a deliberate sibling of the gate `vice-session` already ships: same structure, this skill's own directory, this skill's own entry point as the single exemption, and the same three tests — the enumeration is non-empty and anchored on the comparison module (so a broken glob or path resolution cannot pass vacuously), no enumerated module except the entry point is named in `SKILL.md`, and the entry point IS still named there (a rewrite that dropped it is a regression, not a cleanup). Enumerate the directory rather than hardcoding a name list: that is what makes a module added later covered the moment it lands on disk.

Its header comment must record why this is a copy rather than a shared helper: each skill has to carry its own doc-hygiene gate for the skill to stay copyable on its own terms, and a gate living in a sibling and reaching across directories would make each skill's exportability a lie — the same false-copyability trap already corrected once in this repo. Keep the ONE-DIRECTIONAL note too: there is no companion document to cross-check removed names against, by decision, and rationale for each module lives in that module's own header comment.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; LOG=$(mktemp); VICE_SKIP_RESOURCE_INSTALL=1 node --test .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs .claude/skills/vice-session/scripts/skill-docs.test.mjs > "$LOG" 2>&1 || { tail -20 "$LOG"; echo "a module-leak gate FAILED"; exit 1; }; grep -q "fail 0" "$LOG" || { tail -20 "$LOG"; exit 1; }; echo GATES-OK'</automated>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; S=.claude/skills/c64-ram-capture/SKILL.md; grep -q "^name: c64-ram-capture$" "$S" || { echo "SKILL.md frontmatter name is wrong"; exit 1; }; grep -q "prove two captures are equivalent under RAM drift" "$S" || { echo "the resolved description was not used verbatim"; exit 1; }; test "$(grep -cF ram-compare.mjs "$S")" = 0 || { echo "an internal module is named in the usage guide"; exit 1; }; test "$(grep -cF ".test.mjs" "$S")" = 0 || { echo "a test module is named in the usage guide"; exit 1; }; grep -qF ram-capture.mjs "$S" || { echo "the documented entry point is missing from SKILL.md"; exit 1; }; test "$(grep -ciF INTERNALS "$S")" = 0 || { echo "SKILL.md points at a maintainer document"; exit 1; }; test ! -e .claude/skills/c64-ram-capture/INTERNALS.md || { echo "a maintainer document was created"; exit 1; }; test ! -e .claude/skills/vice-session/INTERNALS.md || { echo "the deleted maintainer document came back"; exit 1; }; grep -q "vice-session" "$S" && grep -q "devcontainer-host-path" "$S" || { echo "the copy-this-skill section must name both sibling skills"; exit 1; }; echo DOCS-OK'</automated>
  </verify>
  <done>`SKILL.md` carries the resolved name and the drafted description verbatim, documents the skill as an import surface through its single entry point, names no other module and no maintainer document, and states that both sibling skills travel with it; the new gate runs green alongside `vice-session`'s and would fail the moment a future module leaked into the guide; no `INTERNALS.md` exists in either skill.</done>
</task>

<task type="auto">
  <name>Task 3: Prove behaviour equivalence honestly, then retire both todos</name>
  <files>.planning/todos/completed/new-ram-capture-skill.md, .planning/todos/completed/extract-sync-primitives-to-vice-session.md</files>
  <read_first>.planning/todos/pending/new-ram-capture-skill.md and .planning/todos/pending/extract-sync-primitives-to-vice-session.md (confirm every table row and every do-not-lose bullet landed before retiring either file)</read_first>
  <precondition>Tasks 1 and 2 are committed. The predecessor todo `extract-sync-primitives-to-vice-session` is ALREADY IMPLEMENTED (quick task 260730-u9w, commits adb485e / 9424395 / 0b24684 / 32ff69d landed `vice-sync.mjs` with all ten symbols) — it needs retiring only, and no part of it may be re-planned or re-implemented here.</precondition>
  <reversibility rating="reversible">Two tracked file moves; `git mv` back restores the starting state.</reversibility>
  <action>
**First, the behaviour-equivalence check.** `node tools/recover.mjs reproduce danish` is the real check, and it needs the host emulator. Probe reachability first with the `vice-session` skill's own ping command. Two outcomes, both acceptable, one of them mandatory to report:

- **Emulator reachable:** run `node tools/recover.mjs reproduce danish`, and record its actual `run1`/`run2` sha256 lines, the identical-bytes count, the volatile-scratch count, the single-bit drift-candidate count, the program-image mismatch count and the final verdict line in the SUMMARY, verbatim from the output.
- **Emulator unreachable** (the state measured while planning: port 6510 answering with ECONNREFUSED): do NOT run it, do NOT retry in a loop, and do NOT infer a pass. Record in the SUMMARY, in plain words, that **live `reproduce` was not run because the host emulator was unreachable**, and that equivalence rests on the static evidence instead: the moved suite at 27 passing / 0 failing, both module-leak gates green, every import resolving and the seven-verb CLI table printing unchanged, no moved symbol present in two places, the three identity gates and the ram-vs-rom hard error present in layer B, and every measured number still beside the code it justifies. State it as a gap in the evidence, not as a pass — and note that the first future run of `reproduce danish` is the outstanding confirmation.

Do not call `vice_disk_list` under any circumstance (it crashes the host MCP server; recovery needs a manual restart on the host).

**Then retire both todos.** `.planning/todos/completed/` is the convention gsd-core's own todo workflows already read and write, so use it rather than inventing one; it does not exist yet, so create it. Both files are git-tracked, so move them with `git mv` (not `cp`, not a rewrite): `new-ram-capture-skill.md` — implemented by this task — and `extract-sync-primitives-to-vice-session.md` — implemented by quick task 260730-u9w and outstanding only as a file. `.planning/todos/pending/` must end up empty. Do not edit either file's contents; the retirement is the move. Do not stage `.planning/config.json`, which carries an unrelated pre-existing modification.
  </action>
  <verify>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; test -d .planning/todos/completed || { echo "completed/ was not created"; exit 1; }; for f in new-ram-capture-skill.md extract-sync-primitives-to-vice-session.md; do test -e ".planning/todos/completed/$f" || { echo "not retired: $f"; exit 1; }; test ! -e ".planning/todos/pending/$f" || { echo "still pending: $f"; exit 1; }; done; test -z "$(ls -A .planning/todos/pending 2>/dev/null)" || { echo "pending/ is not empty"; ls -A .planning/todos/pending; exit 1; }; git ls-files .planning/todos/completed | grep -q new-ram-capture-skill || { echo "the retired todos are not tracked at their new path -- use git mv"; exit 1; }; echo TODOS-OK'</automated>
    <automated>bash -c 'set -e; cd /workspaces/bruce_lee; LOG=$(mktemp); VICE_SKIP_RESOURCE_INSTALL=1 node --test .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs .claude/skills/vice-session/scripts/skill-docs.test.mjs .claude/skills/vice-session/scripts/vice-pool.test.mjs > "$LOG" 2>&1 || { tail -25 "$LOG"; echo "full suite FAILED"; exit 1; }; grep -q "fail 0" "$LOG" || { tail -20 "$LOG"; exit 1; }; node tools/recover.mjs 2>/dev/null | grep -q "reproduce <release>" || { echo "CLI verb table changed"; exit 1; }; echo FINAL-OK'</automated>
    <human-check>Read the SUMMARY's equivalence section: it must say either what `reproduce danish` actually printed, or plainly that live reproduce was NOT run because the emulator was unreachable. Neither a fabricated pass nor silence about it is acceptable.</human-check>
  </verify>
  <done>Both todo files sit under `.planning/todos/completed/` at their tracked new paths and `.planning/todos/pending/` is empty; every test file in both skills passes with 0 failures; the CLI verb table is unchanged; and the SUMMARY states honestly whether live `reproduce danish` ran, with its real output if it did and the static evidence list plus the outstanding-confirmation note if it did not.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container → host VICE (MCP `vice_*`) | Every emulator call leaves the container; the host server is known to die on `vice_execution_run` and during checkpoint work, and can be respawned underneath a retry as a DIFFERENT machine. |
| emulator RAM → committed artifact (`.bin` / `.capture.json`) | The point where bytes stop being live state and start being evidence a rebuild will be judged against. |
| skill directory → `SKILL.md` | The only surface an agent reads before using the skill; internal mechanics disclosed here become load-bearing on future work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-v6z-01 | Tampering | the moved `capture()` in layer B | high | mitigate | A silently-dropped safety gate would let a bad dump pass as evidence. Task 1's verify asserts exactly 3 `assertSameMachine` gates and the presence of the `bank:"rom"` comparison in layer B, and the moved suite (27 tests) re-proves the drift contract from its new path. |
| T-v6z-02 | Tampering | resume-site accounting across the split | high | mitigate | An extra `vice_execution_run` is both a crash-risk multiplier and a wrong capture point. Verify pins the counts: 0 in `tools/recover.mjs`, exactly 3 in layer B (2 autostart, 1 decay reference). |
| T-v6z-03 | Repudiation | `armedCheckpoints` tracker across the layer A/B seam | medium | mitigate | If layer B's hand-rolled arm/wait/delete stops writing through layer A's tracker, the D-3 checkpoint-presence fallback probes ids that were never armed and a restarted machine passes as unchanged — a capture that cannot be trusted but claims it can. The surface check imports both modules and the suite exercises `assertSameMachine`'s injected parameter directly. |
| T-v6z-04 | Information disclosure | `.claude/skills/c64-ram-capture/SKILL.md` | medium | mitigate | Internal module names, functions and state files leaking into the usage guide is the exact disclosure the usage-only split exists to prevent. A durable directory-enumerating gate ships inside the new skill, and Task 2's verify also asserts no `INTERNALS.md` exists in either skill. |
| T-v6z-05 | Denial of service | host MCP server | high | mitigate | `vice_disk_list` crashes it and recovery needs a manual host restart; no task calls it, no new `vice_*` tool name is introduced anywhere in this plan, and every static gate runs without the emulator. |
| T-v6z-06 | Spoofing | a fabricated equivalence result | medium | mitigate | Claiming a live `reproduce` pass that never ran would forge the project's only behavioural-equivalence evidence. Task 3 requires either the real output verbatim or an explicit statement that it was not run, with a `<human-check>` on that section. |
| T-v6z-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install occurs in this plan: zero new dependencies, Node stdlib and `node:test` only. The RESEARCH package-legitimacy gate therefore has nothing to check here. |
</threat_model>

<verification>
1. `VICE_SKIP_RESOURCE_INSTALL=1 node --test .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs` → `pass 27 / fail 0` (the measured pre-move baseline, unchanged).
2. Both module-leak gates green: the new skill's and `vice-session`'s.
3. `VICE_SKIP_RESOURCE_INSTALL=1 node --test` over every test file in both skills → `fail 0`.
4. The module-surface check: layer B exports all twelve symbols, `tools/recover.mjs` exports none of them, `capture`/`findEntry` arities prove the dead parameters are gone, both `machine/` writers reject a missing `outDir` before touching the emulator, and `snapshotName` still produces `p6510_ns_gameentry_run1`.
5. `node tools/recover.mjs` prints the unchanged seven-verb usage table (proves every import resolves without an emulator).
6. Evidence greps: 994 / 1014 / 993 / 137 in the comparison module, 2551 in layer B, 264 in `tools/recover.mjs`'s gate walk; comparison module has zero imports; layer B has zero occurrences of a release identifier, no `recovery/` path literal and no registry import.
7. `.planning/todos/pending/` empty, both files tracked under `.planning/todos/completed/`.
8. Behaviour equivalence: `node tools/recover.mjs reproduce danish` if the host emulator is reachable, otherwise the static evidence above plus an explicit SUMMARY statement that live reproduce was not run.
</verification>

<success_criteria>
- The `c64-ram-capture` skill exists with the resolved name, the drafted description, a usage-only `SKILL.md` naming only its entry point, and no maintainer document anywhere.
- Every symbol in the todo's move table lives in the new skill; `tools/recover.mjs` holds only layer C (registry reads, the gate walk, the dumps layout, the CLI, the lease) and duplicates nothing.
- All four signature changes are in place: `capture()` and `findEntry()` lost their dead first parameter, both `machine/` writers take `outDir`, and `snapshotName`'s middle parameter is a plain namespace.
- `boot()` is split at the seam: `attachAndStart({ diskPath })` in layer B, gate walk + screenshot + `upsertRelease` in layer C, each half's comment pointing at the other.
- All six do-not-lose items are present in code and gated by an automated check.
- 27 tests pass from the suite's new home; both leak gates pass.
- Both todos retired; `.planning/todos/pending/` empty.
- No behaviour change: identical verb table, identical arguments, identical output lines, identical `vice_*` call order.
</success_criteria>

<output>
Create `.planning/quick/260730-v6z-create-the-c64-ram-capture-skill-layer-b/260730-v6z-SUMMARY.md` when done, including the behaviour-equivalence section required by Task 3.
</output>