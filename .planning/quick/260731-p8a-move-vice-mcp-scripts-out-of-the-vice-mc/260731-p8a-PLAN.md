---
phase: quick-260731-p8a
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
requirements: [QUICK-260731-p8a]
files_modified:
  - .claude/mcp/vice/vice-proxy.mjs
  - .claude/mcp/vice/vice.mjs
  - .claude/mcp/vice/vice-pool.mjs
  - .claude/mcp/vice/vice-session.mjs
  - .claude/mcp/vice/vice-probe.mjs
  - .claude/mcp/vice/vice-sync.mjs
  - .claude/mcp/vice/repo-root.mjs
  - .claude/mcp/vice/install-resources.mjs
  - .claude/mcp/vice/refresh-manifest.mjs
  - .claude/mcp/vice/tools-manifest.json
  - .claude/mcp/vice/vice-proxy.test.mjs
  - .claude/mcp/vice/vice-pool.test.mjs
  - .claude/mcp/vice/skill-docs.test.mjs
  - .claude/mcp/vice/vice-mcp-selector-docs.test.mjs
  - .claude/mcp/vice/resources/vice-pool.sh
  - .claude/mcp/vice/resources/vice-supervisor.sh
  - .claude/mcp/vice/resources/lib/container-guard.sh
  - .claude/mcp/vice/resources/lib/repo-root.sh
  - .mcp.json
  - .gitignore
  - tools/README.md
  - tools/chip-state.mjs
  - tools/recover.mjs
  - tools/watch-loads.mjs
  - .claude/skills/c64-ram-capture/SKILL.md
  - .claude/skills/c64-ram-capture/scripts/ram-capture.mjs
  - .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs
  - .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs

must_haves:
  truths:
    - "With the emulator down, `node .claude/mcp/vice/vice-proxy.mjs` answers `initialize` then `tools/list` with exactly 64 tools (63 committed manifest entries, already deny-filtered at write time, plus the synthetic result-continuation tool)."
    - "The full suite reports 170 pass / 0 fail — the same count measured before the move (per D-6, the suite is the acceptance gate, not inspection). A LOWER count means a moved test file dropped out of the runner's glob, which is a failure even if nothing reports red."
    - "The `.vice-supervisor` directory the Node and shell halves agree on is still `<repo>/.vice-supervisor` and is still NOT under `.claude` — vice-pool.test.mjs's path-agreement assertion (the one this move could plausibly break) still holds."
    - "`install-resources.mjs` still deploys to exactly the four repo-root paths `.gitignore` pins: tools/vice-supervisor.sh, tools/vice-pool.sh, tools/lib/container-guard.sh, tools/lib/repo-root.sh."
    - "`git log --follow` on each of the 18 relocated files reaches commits made before this task (D-4: history follows the move)."
    - "`.claude/skills/vice-mcp-selector/` contains SKILL.md and nothing else — the expected end state per D-5, not an oversight."
    - "The docs gate still ENUMERATES the relocated modules, so criterion 9's hostpath-consumer closure and the vice-session import ban are still actually enforced rather than silently vacated."
    - "No file outside `.planning/` names the pre-move location — including the gitignored, host-executed copies under `tools/`, which a `.gitignore`-aware grep would miss."
  artifacts:
    - ".claude/mcp/vice/ holding 14 flattened files (9 .mjs, tools-manifest.json, 4 .test.mjs)"
    - ".claude/mcp/vice/resources/ holding vice-pool.sh, vice-supervisor.sh and lib/{container-guard,repo-root}.sh with its subfolder structure intact (D-3)"
    - ".mcp.json whose vice entry args[0] is .claude/mcp/vice/vice-proxy.mjs"
    - "tools/README.md describing .claude/mcp/vice/ as the implementation home rather than a skill scripts/ directory"
  key_links:
    - ".mcp.json args[0] -> .claude/mcp/vice/vice-proxy.mjs (the only wiring Claude Code reads)"
    - "vice-proxy.mjs HERE_DIR -> tools-manifest.json, resolved by co-location; the manifest MUST land in the same directory as the proxy"
    - "4 hostpath imports -> ../../skills/devcontainer-host-path/scripts/hostpath.mjs (the fixed-depth cross-tree edge; ../../ meant .claude/skills/ before and means .claude/ now)"
    - "install-resources.mjs RESOURCES_DIR -> .claude/mcp/vice/resources (was a ..-hop over scripts/; flattening makes resources/ a plain sibling)"
    - "vice-mcp-selector-docs.test.mjs enumerateModules() -> must walk .claude/mcp as well as .claude/skills and tools"
    - "13 import lines across 5 external consumers -> .claude/mcp/vice/{vice,vice-pool,vice-sync}.mjs"
---

<objective>
Relocate the VICE MCP server implementation out of the skill directory
`.claude/skills/vice-mcp-selector/` into a new non-skill directory `.claude/mcp/vice/`,
updating every reference so nothing behaves differently.

Purpose: the modules under that skill are load-bearing implementation, not skill content —
`.mcp.json` runs the proxy, three `tools/` CLIs and the `c64-ram-capture` skill import the
transport as a library, and `.gitignore` tracks its `resources/` as a source of truth. Moving
them to a directory that says "MCP server" separates the implementation from the skill
declaration, which is what unblocks the follow-up todo that deletes the skill.

Output: 18 relocated files, 30 functional reference updates, ~35 prose/comment updates, and a
green test suite proving the relocation changed nothing observable.

## Locked user decisions (source: task brief, not a CONTEXT.md)

These are NON-NEGOTIABLE. Every task action below cites the ones it implements.

- **D-1** Target directory is `.claude/mcp/vice/`. Not `tools/`, not a differently-named
  directory, not a second skill.
- **D-2** Scope is the vice MCP implementation ONLY. The other four skills (`acme-build`,
  `c64-memory-mapping`, `c64-ram-capture`, `devcontainer-host-path`) keep their own scripts
  exactly where they are. `c64-ram-capture` is edited only where it *references* the moved
  files.
- **D-3** Flatten `scripts/` — the 14 files land directly in `.claude/mcp/vice/`. `resources/`
  keeps its subfolder structure, including `resources/lib/`.
- **D-4** Use `git mv` so history follows.
- **D-5** Do NOT delete `.claude/skills/vice-mcp-selector/SKILL.md`. Its deletion is tracked in
  `.planning/todos/pending/collapse-vice-selector-skill-into-proxy.md` and must happen only
  after the polling contract has been moved into the MCP tool descriptions — deleting it now
  destroys information with no other home. After this task the skill directory holds SKILL.md
  alone. **That is the correct end state, not an incomplete move.**
- **D-6** The test suite is the acceptance gate, not visual inspection. Verification runs real
  commands.
- **D-7** Do NOT rewrite path references inside `.planning/`. It holds historical records that
  must stay accurate about what the old path WAS
  (`vice-mcp-selector-docs.test.mjs` records this rule at its document-enumeration filter).

## Pre-measured baselines (taken during planning, 2026-07-31, at the OLD location)

Facts to compare against, so a post-move "pass" is a real pass:

- Full suite: **170 pass / 0 fail** in ~5.2s via
  `node --test '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs'` on Node v24.18.1.
  Per file: vice-proxy 18, vice-pool 73, vice-mcp-selector-docs 5, skill-docs 3,
  ram-capture 28, c64-ram-capture skill-docs 3, plus 40 under `tools/`.
- `tools/list` over a real stdio handshake: **64 tools**. `tools-manifest.json` holds 63
  entries and `generated_at: 2026-07-31T15:56:00.302Z`; `handleToolsList()` appends one
  synthetic result-continuation tool, which is the entire difference. 64 is the number to
  preserve — do not "correct" it to 63.
- Stale-reference grep (see Task 3) lists **18 files**.

## Environment hazard the verify commands depend on

`grep` in this container's shell is a **function** wrapping `ugrep --ignore-files`, which
honours `.gitignore`. The deployed host launchers `tools/vice-supervisor.sh`,
`tools/vice-pool.sh` and `tools/lib/repo-root.sh` are gitignored, so a plain `grep -r` reports
a **false PASS** while three host-executed files still carry the stale path — one of them in an
operator-facing `echo`. Every grep gate below therefore uses `command grep` to bypass the
wrapper. Do not "simplify" it back to `grep`.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/todos/pending/collapse-vice-selector-skill-into-proxy.md

# The depth-coupled resolvers — read these two headers before touching either file.
# Both exist BECAUSE a previous move of these same modules silently resolved the wrong
# directory with nothing erroring anywhere.
@.claude/skills/vice-mcp-selector/scripts/repo-root.mjs
@.claude/skills/vice-mcp-selector/resources/lib/repo-root.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: git mv the 18 files and make the code resolve from the new depth</name>
  <precondition>`git status --porcelain` is empty — a clean tree is what makes `git mv`'s rename detection unambiguous and the move commit reviewable.</precondition>
  <reversibility rating="costly">Re-moving the files is trivial, but editing `.mcp.json`'s `args[0]` changes the MCP server definition, and whether that re-triggers project-scope MCP approval is unverified and lives outside the repo — a revert cannot undo an approval prompt the user already saw.</reversibility>
  <files>.claude/mcp/vice/ (18 relocated files), .mcp.json, tools/chip-state.mjs, tools/recover.mjs, tools/watch-loads.mjs, .claude/skills/c64-ram-capture/scripts/ram-capture.mjs, .claude/skills/c64-ram-capture/scripts/ram-capture.test.mjs</files>
  <read_first>
    Read `.claude/skills/vice-mcp-selector/scripts/install-resources.mjs` lines 22-42 (the
    RESOURCES_DIR hop and the header explaining why getting it wrong is silent and total) and
    `.claude/skills/vice-mcp-selector/scripts/skill-docs.test.mjs` lines 36-50 (SCRIPTS_DIR /
    SKILL_MD resolution) before editing either.
  </read_first>
  <action>
Per D-1, D-3 and D-4: `mkdir -p .claude/mcp/vice/resources/lib`, then `git mv` — never `cp`,
never a plain `mv` — each of the 14 files under
`.claude/skills/vice-mcp-selector/scripts/` directly into `.claude/mcp/vice/` (flattened: the
9 .mjs modules, tools-manifest.json, and the 4 .test.mjs files), and each of the 4 files under
that skill's `resources/` into `.claude/mcp/vice/resources/`, preserving the `lib/` subfolder.
Per D-5, leave SKILL.md untouched where it is; per D-2, touch no other skill's scripts.

Then repair every reference that affects execution. All line numbers are pre-move.

The cross-tree hostpath edge is the one with real teeth: `../../` used to resolve to
`.claude/skills/` and now resolves to `.claude/`. In vice-proxy.mjs:23, vice-sync.mjs:47,
install-resources.mjs:26 and vice-proxy.test.mjs:27 the specifier must gain a `skills/`
segment so it points at `../../skills/devcontainer-host-path/scripts/hostpath.mjs`. The prose
naming that same specifier in vice-sync.mjs:39 must match. Do not verify this by reading — a
wrong specifier here is a module-resolution error the suite catches in Task 2, and the proxy
handshake in this task's verify catches it immediately.

install-resources.mjs:36 computes RESOURCES_DIR with a parent hop that existed only to climb
out of `scripts/`. Flattening removes that directory, so `resources/` is now a plain sibling
of the module and the hop must go — `join(HERE, "resources")`. Rewrite the doc comment
immediately above it too: it currently asserts the directory is one level up and explicitly
"NOT a sibling", which is now exactly backwards. Keep its warning about the failure being
silent; that part is still true and still the reason this line matters.

skill-docs.test.mjs:43 resolves the SKILL.md it guards by hopping out of `scripts/`. The
guarded SKILL.md deliberately stays behind (D-5), so this gate now spans two trees: point it
at `join(HERE, "..", "..", "skills", "vice-mcp-selector", "SKILL.md")` and rename the
SCRIPTS_DIR binding and the file's prose to describe the module directory it actually
enumerates. Add a short note in its header that this gate should be deleted together with the
SKILL.md it guards when the pending todo retires that document.

Repoint the 13 import lines in the five external consumers: tools/chip-state.mjs:32-34,
tools/recover.mjs:20,21,29 and tools/watch-loads.mjs:29,30,32 each import
vice.mjs / vice-pool.mjs / vice-sync.mjs and become `../.claude/mcp/vice/<module>`;
.claude/skills/c64-ram-capture/scripts/ram-capture.mjs:44,45 and
ram-capture.test.mjs:17,18 climb out of that skill instead, becoming
`../../../mcp/vice/<module>`. Count the `../` hops against the file's own directory rather
than copying the shape from a sibling — the two groups sit at different depths.

Finally, `.mcp.json`'s single `args[0]` becomes `.claude/mcp/vice/vice-proxy.mjs`. The
manifest is resolved by co-location with the proxy, so confirm tools-manifest.json landed in
the same directory as vice-proxy.mjs rather than assuming the `git mv` batch caught it.

Leave repo-root.mjs entirely alone in this task — its intra-directory imports are unaffected
by flattening, and its one depth-coupled branch is Task 2's, paired with the test that pins it.
  </action>
  <verify>
    <automated>printf '%s\n' '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2024-11-05","capabilities":{},"clientInfo":{"name":"t","version":"0"}}}' '{"jsonrpc":"2.0","id":2,"method":"tools/list"}' | node .claude/mcp/vice/vice-proxy.mjs 2>/dev/null | node -e 'let b="";process.stdin.on("data",d=>b+=d).on("end",()=>{const t=b.trim().split("\n").map(JSON.parse).find(m=>m.id===2).result.tools.length;if(t!==64)throw new Error("tools/list returned "+t+", expected 64");console.log("tools/list OK: "+t)})' && node --input-type=module -e 'await import("./tools/recover.mjs");await import("./tools/watch-loads.mjs");await import("./tools/chip-state.mjs");console.log("consumer import graphs resolve")' && node -e 'const j=require("./.mcp.json");const p=j.mcpServers.vice.args[0];if(p!==".claude/mcp/vice/vice-proxy.mjs")throw new Error("args[0] is "+p);require("fs").accessSync(p);console.log(".mcp.json args[0] resolves")' && git log --follow --oneline -- .claude/mcp/vice/vice-proxy.mjs | tail -3</automated>
  </verify>
  <done>
    An end-to-end stdio MCP handshake against the NEW path returns 64 tools with the emulator
    down (proving the flattened manifest co-location and the rewritten hostpath edge both
    resolve); all three `tools/` CLI import graphs resolve; `.mcp.json` names a file that
    exists; and `git log --follow` on a relocated file reaches pre-move commits. The full suite
    is expected to be RED at this point — the gates that pin the old paths are Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: retarget the test gates and the depth-coupled last-resort hop</name>
  <files>.claude/mcp/vice/repo-root.mjs, .claude/mcp/vice/vice-pool.test.mjs, .claude/mcp/vice/vice-mcp-selector-docs.test.mjs</files>
  <read_first>
    Read `.claude/mcp/vice/vice-pool.test.mjs` lines 924-1075 (the path-agreement assertion,
    the repoRoot ladder tests and the last-resort fallback test) and lines 1610-1629 (the
    RESOURCES_DIR anchor test), plus `.claude/mcp/vice/vice-mcp-selector-docs.test.mjs`
    lines 165-240 (enumerateModules and the two assertions built on it).
  </read_first>
  <action>
Three gates pin the pre-move location, and one production branch is coupled to the old
directory depth. Fix them together, per D-6, so the suite becomes the real proof.

repo-root.mjs's ladder is depth-independent for branches 1-3 (env var, then a `.git` walk) —
those need no change and must not be touched. Branch 4, the fixed-hop last resort, is the
exception: it climbs four levels because `<root>/.claude/skills/<skill>/scripts/` is four deep.
The new location is three deep, so the hop count, the JSDoc describing which shape it implies,
and the warn string that prints the fallback all move to three levels. Update the file's
header narrative in the same pass — it recounts two previous moves of these modules and should
name this one, since that history is the entire justification for the resolver existing. Note
while editing: no branch other than #4 is depth-sensitive, and in this repo branch 4 never runs
(there is always a `.git` ancestor), which is exactly why a wrong hop count here would go
unnoticed without the paired test below.

vice-pool.test.mjs's fallback test at 1049-1067 builds a synthetic root with no `.git` and no
env var specifically to force branch 4. Rebuild its synthetic path to the new
`<root>/.claude/mcp/<server>/` shape, and update the test's name and comment: both currently
assert FOUR levels "not three", which inverts after this change. Keep the test's structure —
its value is that it is the only thing exercising branch 4 at all.

Three assertions in that same file resolve the tracked shell scripts through an explicit
segment array naming the skills tree; at lines 938, 939 and 1002 those segments become the new
directory's. The RESOURCES_DIR anchor test at 1617-1620 asserts the directory ends with the old
skill-plus-resources pair — retarget it to end with the new `mcp`/`vice`/`resources` triple
(include the `mcp` segment; a bare directory-named-`vice` suffix is too weak to catch a wrong
hop) and fix the assertion message that repeats the old path. Its companion assertion that
RESOURCES_DIR passes through no `scripts` segment still holds after flattening — leave it, it
is now enforcing the flattening itself. The stale test name at 1069 naming the retired
vice-session location should be corrected while in the file.

vice-mcp-selector-docs.test.mjs is the one with a silent-failure mode rather than a red test.
Its `enumerateModules()` walks only the skills tree and `tools/`; the relocated modules now sit
outside both, so criterion 9's hostpath-consumer closure and the vice-session import ban would
stop covering them. Add the new directory to that walk. The consumer allow-list itself needs no
new entry — it is keyed by basename, and the same four production modules are still the
importers; what changed is only which directories get enumerated. After the fix, confirm the
consumer assertion still names four importers rather than having quietly shrunk. Leave that
file's document enumeration and its CLAUDE.md assertion alone: SKILL.md stays (D-5), so the
skills-table assertion is still correct.
  </action>
  <verify>
    <automated>node --test '.claude/mcp/**/*.test.mjs' '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs' 2>&1 | command grep -E '^ℹ (tests|pass|fail)' && node --test '.claude/mcp/**/*.test.mjs' '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs' 2>&1 | command grep -qE '^ℹ pass 170$' && node --test '.claude/mcp/**/*.test.mjs' '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs' 2>&1 | command grep -qE '^ℹ fail 0$' && echo "SUITE OK: 170 pass / 0 fail"</automated>
  </verify>
  <done>
    The full suite reports exactly `pass 170` and `fail 0` through the three-glob runner — the
    same count measured before the move. A count below 170 fails this task even with zero
    failures, because it means a relocated test file is no longer being enumerated. Within that
    run: vice-pool.test.mjs's path-agreement assertion still confirms the agreed
    `.vice-supervisor` directory is not under `.claude` and that the `resources/` and deployed
    `tools/` copies still print byte-identical `--print-paths` output; and the docs gate's
    consumer assertion still names four production importers of hostpath.mjs.
  </done>
</task>

<task type="auto">
  <name>Task 3: update the prose, comments and docs that name the old location</name>
  <files>.claude/mcp/vice/vice-session.mjs, .claude/mcp/vice/vice-pool.mjs, .claude/mcp/vice/resources/vice-supervisor.sh, .claude/mcp/vice/resources/vice-pool.sh, .claude/mcp/vice/resources/lib/repo-root.sh, .gitignore, tools/README.md, .claude/skills/c64-ram-capture/SKILL.md, .claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs</files>
  <action>
Nothing here changes behaviour, but several of these strings are read by a human mid-incident
and one is echoed at runtime, so a stale path actively misleads. Per D-7, make no edits under
`.planning/`.

vice-session.mjs embeds the old path in agent-visible recovery instructions at lines 5, 134,
220, 277, 278, 307 and 308 — the "release it first" / "recover with" messages an agent reads
when a session conflict or epoch discontinuity is detected. These must name the new location or
the recovery instruction is unfollowable. vice-pool.mjs:203 has one comment in the same shape.

The three shell files under `resources/` name the old path in header comments and, at
vice-supervisor.sh:238, inside an `echo` that tells a host operator where the container-side
harness reads the epoch from: update lines 2, 31, 35, 72, 238 and 272 there, lines 2, 20, 79
and 534 in vice-pool.sh, and lines 1, 12 and 55 in lib/repo-root.sh. Leave repo-root.sh's
own last-resort logic alone — its `resources`-basename branch climbs four levels, which is
still correct from `.claude/mcp/vice/resources`; only the prose citing the old path changes.
Do not touch lib/container-guard.sh, which names no path.

`.gitignore` lines 78-80 describe the skill's `resources/` as the tracked source of truth for
the four deployed copies below them. Reword to name the new directory; the four ignore patterns
themselves are unchanged and must stay unchanged — they are what Task 2's deployment assertion
depends on.

tools/README.md carries twelve slash-form references plus a "Layout note" (roughly lines
128-162) that needs real rewriting rather than path substitution: it currently explains that
every skill keeps its `.mjs` modules in its own `scripts/` subdirectory and that these modules
live in one such directory, which stops being true here. Say instead that the vice MCP
implementation is not skill content and now lives outside `.claude/skills/`, that
`vice-mcp-selector` retains only its SKILL.md, and keep the still-accurate parts: `resources/`
is the single tracked source of truth, `tools/` holds gitignored disposable deployed copies,
deployment fires from a module body rather than a CLI verb, and the reason a second tracked copy
was removed. Update the embedded `.mcp.json` snippet around line 169 and the refresh-manifest
command around line 419 to the new paths. The `install --force` command around line 156 keeps
its `install` verb — that verb is deliberately exempt from the docs gate's shell-invocation
pattern because it deploys host scripts and touches no emulator; do not change the verb while
changing the path, and do not introduce any of the emulator-reaching verbs into a document.

`.claude/skills/c64-ram-capture/SKILL.md:113` names `vice-mcp-selector` as a sibling-skill
dependency for anyone copying the skill elsewhere, and that skill's own
`scripts/skill-docs.test.mjs:4-5` cites the sibling gate it was copied from. Both dependencies
are still real — only the location changed — so reword them to point at the new implementation
directory rather than deleting them (D-2: this is the only kind of edit `c64-ram-capture`
receives). Note in SKILL.md that the dependency is now on a directory outside `.claude/skills/`,
which is what a copier actually needs to know.

Then refresh the deployed host copies, whose comments and echoed message are now stale:
`node .claude/mcp/vice/vice.mjs install --force`. This is required, not optional — the
installer never overwrites a diverged target without it, so without this step three
host-executed files keep the old path and the gate below fails.
  </action>
  <verify>
    <automated>if command grep -rlE 'vice-mcp-selector/(scripts|resources)' . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=node_modules --exclude-dir=worktrees; then echo "FAIL: files above still name the pre-move location"; exit 1; fi; if command grep -rn '"skills", "vice-mcp-selector"' --include=*.mjs . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=worktrees; then echo "FAIL: segment-array form above still names the skills tree"; exit 1; fi; echo "PASS: no stale references outside .planning/" && node --test '.claude/mcp/**/*.test.mjs' '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs' 2>&1 | command grep -qE '^ℹ pass 170$' && node --test '.claude/mcp/**/*.test.mjs' '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs' 2>&1 | command grep -qE '^ℹ fail 0$' && echo "SUITE STILL OK: 170 pass / 0 fail"</automated>
    <human-check>Not required for sign-off, but worth knowing: whether restarting the Claude Code session re-prompts for project-scope approval of the `vice` MCP server now that `args[0]` changed. Record the observation in SUMMARY.md; do not attempt to work around it either way.</human-check>
  </verify>
  <done>
    Both grep gates return no matches and the suite still reports 170 pass / 0 fail. The gates
    are non-vacuous: run the FIRST grep before making these edits and confirm it lists 18 files
    (the count measured during planning). A clean result on the first run means the grep
    invocation is broken — most likely the `command` prefix was dropped, which lets the
    `.gitignore`-aware wrapper hide the three deployed `tools/` copies — not that the work is
    already done.
    <!-- planner-discipline-allow: vice-mcp-selector/scripts -->
    <!-- planner-discipline-allow: vice-mcp-selector/resources -->
    <!-- planner-discipline-allow: "skills", "vice-mcp-selector" -->
    The grep gates exclude `.planning/`, where this plan file lives, so the literals quoted in
    this plan cannot invalidate their own criteria.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| container → host VICE MCP endpoint | every tool argument, including container paths translated to host paths, crosses here through vice-proxy.mjs |
| repo tree → deployed `tools/*.sh` | these files are executed on the HOST, outside the container sandbox, by a human operator |
| skill tree → new `.claude/mcp/` tree | the directory-enumerating security gates were written against the skill tree's shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-p8a-01 | Elevation of Privilege | `vice-mcp-selector-docs.test.mjs` `enumerateModules()` | high | mitigate | The walker covers only the skills tree and `tools/`; after the move the relocated modules fall outside it, silently vacating criterion 9's hostpath-consumer closure and the vice-session import ban — a security control that passes while enforcing nothing. Task 2 adds the new directory to the walk and re-confirms the consumer assertion still names four production importers. |
| T-p8a-02 | Tampering | vice-proxy.mjs deny-list and epoch guards | high | mitigate | The three-layer `vice_disk_list` deny-list and the before/after epoch re-check must survive the relocation untouched. No task edits DENY_LIST or epoch logic; vice-proxy.test.mjs's 18 assertions (including the deny-list and path-escaping cases) must pass in Task 2's full-suite run. |
| T-p8a-03 | Tampering | deployed host-executed `tools/*.sh` copies | medium | mitigate | Comment-only edits to `resources/` leave the gitignored `tools/` copies diverged, and the installer skips diverged targets by contract — so three files executed on the host would keep a stale, operator-facing path. Task 3 runs `install --force` and the suite re-asserts `resources/` and `tools/` still print byte-identical `--print-paths`. |
| T-p8a-04 | Information Disclosure | `command grep` in every verify gate | medium | mitigate | The shell's `grep` wrapper honours `.gitignore`, so a plain recursive grep reports a clean tree while gitignored host-executed files still carry the old path. Every gate uses `command grep`, and Task 3's done-criteria require proving the gate lists 18 files before the edits. |
| T-p8a-05 | Tampering | `.mcp.json` server definition | medium | accept | Changing `args[0]` alters the registered server definition; whether that re-triggers project-scope MCP approval is unverified, and the approval state lives outside the repo so it cannot be asserted from here. Accepted and flagged in SUMMARY.md per the task brief rather than worked around. |
| T-p8a-06 | Denial of Service | repo-root.mjs branch-4 hop count | low | mitigate | A wrong last-resort hop resolves a permanently-empty `.vice-supervisor` directory with nothing erroring — the exact failure class that resolver exists to prevent. Never reached in this repo (a `.git` ancestor always exists), so the paired synthetic test in Task 2 is the only thing that can catch it; that test is updated rather than deleted. |
| T-p8a-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this plan — no dependency is added, removed or upgraded, and no `package.json` exists in this repo. Nothing to audit; the legitimacy gate is inapplicable rather than skipped. |
</threat_model>

<verification>
1. `node --test '.claude/mcp/**/*.test.mjs' '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs'`
   reports **170 pass / 0 fail** — same count as the pre-move baseline.
2. A real stdio handshake against `.claude/mcp/vice/vice-proxy.mjs` returns **64** tools with
   the emulator down.
3. Both stale-reference greps (slash form and segment-array form), run with `command grep`,
   return no matches outside `.planning/`.
4. `.claude/skills/vice-mcp-selector/` contains SKILL.md and nothing else.
5. `git log --follow` on relocated files reaches pre-move commits.
6. Deployed copies exist at the four `.gitignore`-pinned paths and match `resources/`.

Human observation to record, not a gate: whether a fresh session re-prompts for project-scope
MCP approval after the `args[0]` change.
</verification>

<success_criteria>
- 18 files relocated to `.claude/mcp/vice/` with `scripts/` flattened and `resources/lib/`
  preserved, via `git mv`.
- All 30 execution-affecting references updated: 4 hostpath imports, 13 external consumer
  imports, RESOURCES_DIR, the SKILL.md guard path, branch-4's hop count, 5 test-gate sites,
  the docs-gate walk, and `.mcp.json`.
- Suite green at the pre-move count (170/0); proxy answers `tools/list` with 64 tools offline.
- No file outside `.planning/` names the pre-move location, including the gitignored
  host-executed copies.
- `.claude/skills/vice-mcp-selector/SKILL.md` still exists and is unmodified; no other skill's
  scripts were touched.
- SUMMARY.md records: (a) the `.mcp.json` server-definition change and that its
  approval-re-trigger behaviour is UNVERIFIED — the existing reasoning about approval
  invalidation covers a changing `url`, not a changing `args` path; (b) that the skill directory
  now holds SKILL.md alone, by design, with the pending todo as the follow-up; (c) that
  `skill-docs.test.mjs` now guards a SKILL.md in another tree and should be retired together
  with it; (d) that the docs gate's *document* enumeration still does not cover `.claude/mcp/`,
  so a future README added there would escape the shell-invocation guard — deliberately left
  out of scope.
</success_criteria>

<output>
Create `.planning/quick/260731-p8a-move-vice-mcp-scripts-out-of-the-vice-mc/260731-p8a-SUMMARY.md` when done
</output>
