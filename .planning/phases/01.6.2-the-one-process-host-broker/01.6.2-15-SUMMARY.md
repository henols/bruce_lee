---
phase: 01.6.2-the-one-process-host-broker
plan: 15
subsystem: infra
tags: [documentation, validation, gap-closure, testing]

# Dependency graph
requires:
  - phase: 01.6.2 plan 12
    provides: "withCrashSupervision() wired into both real launch paths, closing the crash-supervision gap this ledger correction cites"
  - phase: 01.6.2 plan 13
    provides: "the deliberateKill/respawnAfterKill marker split and the two verbatim end-to-end test titles ('wired recycle...' and 'wired release...') this plan cites and confirms by literal search"
  - phase: 01.6.2 plan 14
    provides: "the two closed grant-outlives-connection leaks and their own declared test-count deltas, folded into this plan's suite-arithmetic reconciliation"
provides:
  - "01.6.2-VALIDATION.md ledger row 51, corrected to cite the wired recycle end-to-end test by verbatim title, plus a dated correction note and a recounted, unchanged tally"
  - "01.6.2-VALIDATION.md's own Suite Arithmetic (Plan 15) section: a live-measured 390/385/0/0/5 total, reconciled addition-by-addition against plans 12/13/14's own declared deltas, and the older 371-vs-368 discrepancy restated side by side rather than resolved"
  - "Two dated, self-contained pending-todos (WR-02, WR-04) surviving independently of the phase's own REVIEW.md/VERIFICATION.md, which are archived at milestone completion"
  - "The one carried deferred item's disposition re-evaluated and recorded: not free at this gap closure, remains deferred"
affects: ["01.6.2.1 (broker lifecycle policy) -- inherits a validation ledger whose row 51 is now honest, and a measured suite figure to compare its own baseline against", "any future maintainer of vice.ts, broker-control.mts or vice-broker.mts who picks up the deferred comment fix or either filed todo"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Additive correction, never overwrite: a ledger row's evidence is replaced in place but a dated correction note beneath the ledger records the prior citation, why it did not hold, and which plans made the new one true -- the same append-a-record discipline this project already applies to confidence-grade promotions."
    - "Suite arithmetic reconciled two ways in one section: forward (pre-gap-closure baseline + each plan's own declared delta = measured now) and backward (measured now - declared deltas = pre-gap-closure baseline, cross-checking an older figure via a completely different arithmetic path)."
    - "A deliberately-excluded defect gets a todo; a defect needing no fix gets a SUMMARY disposition and no todo -- filing a no-op as a todo is itself noise that makes a real pending item easier to skip."

key-files:
  created:
    - .planning/todos/pending/2026-08-04-broker-control-oversize-line-guard-measures-the-whole-buffer-not-the-unterminated-tail.md
    - .planning/todos/pending/2026-08-04-broker-heartbeat-and-evaluation-pass-interval-handles-are-never-captured-or-cleared.md
  modified:
    - .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md
    - .planning/phases/01.6.2-the-one-process-host-broker/deferred-items.md

key-decisions:
  - "Row 51's new citation cites ONLY plan 13's recycle end-to-end test, not both of plan 13's two new e2e tests -- the release test proves a different property (kill-never-recycle, WR-01) that a neighbouring row does not need row 51 to also carry, per the plan's own read_first note naming the recycle test specifically as 'this row's new citation'."
  - "The acknowledgement test half of row 51's prior citation (broker-control.test.ts's kill+ack test) was kept, not replaced -- it still legitimately covers the kill-and-acknowledge half of the row's subject; only the isolated-module supervision test was removed and explained."
  - "The pre-existing 371-vs-368 suite-total discrepancy is restated, not resolved, even though 01.6.2-11-SUMMARY.md already fully investigated and explained it (two additional test-file deletions plan 10's simpler arithmetic did not anticipate) -- this plan cites that existing explanation rather than treating it as a fresh finding or silently adopting whichever number is tidier, matching the plan's own prohibition against resolving it by preference."
  - "WR-02 and WR-04 are filed as durable pending-todos rather than fixed -- both are review warnings a developer decision at this gap closure's planning gate deliberately left open; filing (not fixing) is this plan's own declared scope."

requirements-completed: ["A", "C2", "WR-02", "WR-04", "V-truth-7"]

coverage:
  - id: D1
    description: "Ledger row 51 corrected to cite the wired recycle end-to-end test by verbatim title (confirmed by literal search), with a dated correction note and a recounted, explicitly-unchanged tally"
    requirement: "A"
    verification:
      - kind: other
        ref: "grep -n 'wired recycle: a recycle over the real control plane' .claude/mcp/vice/broker-e2e.test.ts -- found at line 445, matching the citation verbatim"
        status: pass
      - kind: other
        ref: "git diff fd9b0fe~1 fd9b0fe -- .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md shows changes confined to row 51, the new correction note, and the reconciliation line"
        status: pass
      - kind: unit
        ref: "node --test '.claude/mcp/vice/'*.test.* at commit fd9b0fe -- 390 tests, 385 pass, 0 fail, 5 todo; npm --prefix .claude/mcp/vice run typecheck exits 0"
        status: pass
    human_judgment: false
  - id: D0
    description: "C2 (all shell logic moved, nothing carved out) carried forward unchanged -- this plan's own file list touches the same 01.6.2-VALIDATION.md artifact that already discharges C2 via its Function Disposition Table, and no change to that table was made or needed"
    requirement: "C2"
    verification:
      - kind: other
        ref: "git diff across all three of this plan's commits shows zero lines changed under '## Function Disposition Table (Plan 10)'"
        status: pass
    human_judgment: false
  - id: D2
    description: "A new Suite Arithmetic (Plan 15) section records a live-measured 390/385/0/0/5 total, reconciles it addition-by-addition against plans 12/13/14's own declared deltas (368+4+8+10=390, exact), and restates the older 371-vs-368 discrepancy side by side with both figures rather than resolving it"
    requirement: "V-truth-7"
    verification:
      - kind: other
        ref: "node --test '.claude/mcp/vice/'*.test.* run live in this worktree, twice (before Task 1's commit and again before Task 2's commit) -- 390/385/0/0/5 both times, matching the section's own recorded figures"
        status: pass
    human_judgment: false
  - id: D3
    description: "WR-02 (oversize-line guard measures the whole buffer, not the unterminated tail) filed as a self-contained, dated pending-todo naming the file, current behaviour, consequence, proposed fix, and what a test would assert -- no fix applied"
    requirement: "WR-02"
    verification:
      - kind: other
        ref: "test -f .planning/todos/pending/2026-08-04-broker-control-oversize-line-guard-measures-the-whole-buffer-not-the-unterminated-tail.md; git diff --name-only across this plan's commits contains no change to broker-control.mts"
        status: pass
    human_judgment: false
  - id: D4
    description: "WR-04 (heartbeat/evaluation-pass interval handles never captured or cleared) filed as a self-contained, dated pending-todo naming the file, current behaviour, consequence, proposed fix, and what a test would assert -- no fix applied"
    requirement: "WR-04"
    verification:
      - kind: other
        ref: "test -f .planning/todos/pending/2026-08-04-broker-heartbeat-and-evaluation-pass-interval-handles-are-never-captured-or-cleared.md; git diff --name-only across this plan's commits contains no change to vice-broker.mts"
        status: pass
    human_judgment: false
  - id: D5
    description: "IN-01 (recycle-ack outcome vocabulary covers only a documented, accepted subset) dispositioned in writing as needing no fix, with its one instruction to a future maintainer -- recorded here, no todo opened"
    verification: []
    human_judgment: true
    rationale: "A disposition of 'no fix required' is a documentation-completeness judgment (did the SUMMARY actually state the reason and the instruction, not just the verdict), not something a test asserts pass/fail on."
  - id: D6
    description: "The one carried deferred item (vice.ts's stale EPOCH_FILE comment) re-evaluated against plans 12-14's actual file sets and found not free at this gap closure -- a dated re-evaluation line appended to deferred-items.md rather than left silently pending"
    verification:
      - kind: other
        ref: "grep -n 'Re-evaluation, 2026-08-04' .planning/phases/01.6.2-the-one-process-host-broker/deferred-items.md"
        status: pass
    human_judgment: false

# Metrics
duration: ~25min (setup/reads plus first commit 09:53:48Z, last commit 09:59:06Z)
completed: 2026-08-04
status: complete
---

# Phase 01.6.2 Plan 15: Correct the Ledger, Measure the Suite, File What Was Deliberately Left Undone Summary

**Ledger row 51 now cites plan 13's own wired recycle-respawn end-to-end test by verbatim title instead of an isolated unit test of a function the shipped broker never called; the suite total is measured live (390/385/0/0/5) and reconciled against plans 12-14's own declared additions rather than carried forward; and the two review warnings this gap closure deliberately left unfixed now exist as durable, dated pending-todos that outlive the phase's own archived artifacts.**

## Performance

- **Duration:** ~25 min (setup/reads plus first commit 2026-08-04T09:53:48Z, last commit 2026-08-04T09:59:06Z)
- **Started:** 2026-08-04
- **Completed:** 2026-08-04T09:59:56Z
- **Tasks:** 3
- **Files modified:** 5 (2 new pending-todos, 2 modified planning artifacts across 3 commits — no authored TypeScript touched)

## Accomplishments

- Corrected `01.6.2-VALIDATION.md` ledger row 51: the citation now reads `broker-control.test.ts#"recycle: a grant this connection holds resolves, kills identity-verified, and answers an ack carrying the stage word and outcome"` (kept, still covers the kill-and-acknowledge half) + `broker-e2e.test.ts#"wired recycle: a recycle over the real control plane kills the granted child and the real broker brings a new one back on the SAME port with the epoch advanced"` (new, copied verbatim from `01.6.2-13-SUMMARY.md` and confirmed present by literal search — found at `broker-e2e.test.ts:445`). The removed half — `broker-launch.test.ts`'s isolated `superviseChild` unit test — is named and explained in a new dated correction note beneath the ledger, which also states the prior citation, the gap report that flagged it, and which plans (12, 13) made the claim true. The reconciliation tally was recounted: still 61 rows, still 4 REPLACED / 31 RE-OBSERVED / 26 DELETED — explicitly stated as unchanged, not silently assumed.
- Added a new `## Suite Arithmetic (Plan 15 — gap closure, 2026-08-04)` section to `01.6.2-VALIDATION.md`, built from a live run in this worktree (390 tests, 385 pass, 0 fail, 0 skipped, 5 pre-existing todo — matching the orchestrator's own given baseline exactly). Reconciled addition-by-addition against plans 12, 13 and 14's own declared per-plan deltas (368 + 4 + 8 + 10 = 390, exact — no undeclared test added or removed). Separately restated the older, pre-existing discrepancy between plan 10's computed prediction (371, by subtraction) and the measured post-deletion total (368, established independently by both `01.6.2-11-SUMMARY.md` and `01.6.2-VERIFICATION.md`) side by side with today's own backward-derived confirmation of 368 (390 − 22 = 368) — named, not resolved, with an explicit note that `01.6.2-11-SUMMARY.md` already investigated and explained the −3 (its own two additional, plan-10-unanticipated test-file deletions), so this plan carries that explanation forward rather than treating it as newly resolved or re-deriving it.
- Filed two self-contained, dated pending-todos for the two review warnings this gap closure deliberately did not fix: WR-02 (`broker-control.mts`'s oversize-line guard measures the whole accumulated buffer, not the unterminated tail — a burst of small, valid, complete lines can destroy a well-formed connection) and WR-04 (`vice-broker.mts`'s heartbeat and evaluation-pass `setInterval` handles are never captured or cleared — harmless in production, but blocks a future in-process test harness). Each names its file/region, current behaviour, why it is wrong, the consequence in terms a reader can evaluate without the review report, the proposed fix, that it is untested today, and what a test for it would assert — and each states plainly that the exclusion was a developer decision at this gap closure's planning gate, not an oversight. Neither fix was applied.
- Dispositioned the remaining review finding, IN-01 (`handleRecycleForRealBroker()`'s `outcome`/`reason` vocabulary covers a documented, accepted subset of what the container-side renderer switches on), as needing no fix — recorded here, in this SUMMARY, with its one instruction to a future maintainer: reuse an existing outcome value rather than invent a new one, since `recycleAckOutcomeMessage()`'s `default` case would silently swallow an unrecognised value. No todo opened for it.
- Re-evaluated the one carried deferred item (`vice.ts`'s `EPOCH_FILE` comment still misattributing epoch-writing to `vice-supervisor.sh`) against plans 12, 13 and 14's actual file sets. `vice.ts` appears in none of them, so the fix was not free at this gap closure — appended a dated re-evaluation line to `deferred-items.md` stating exactly that, leaving the item deferred rather than silently pending.

## Task Commits

Each task was committed atomically:

1. **Task 1: correct ledger row 51, re-reconcile the tally** — `fd9b0fe` (docs)
2. **Task 2: measure the suite for this gap closure, state the pre-existing discrepancy** — `49a9ff1` (docs)
3. **Task 3: file the two excluded review warnings as pending-todos, dispose of the informational finding and the carried deferred item** — `8bac37c` (docs)

**Plan metadata:** committed by the orchestrator after this worktree merges (worktree mode — this executor does not write STATE.md/ROADMAP.md/the metadata commit itself).

_Note: all three tasks are documentation-only (`docs`) — no authored TypeScript, generated `resources/`, or test file is touched by this plan, per its own frontmatter's explicit statement._

## Files Created/Modified

- `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md` — row 51's citation and disposition-note, the recounted reconciliation line, and the new Suite Arithmetic (Plan 15) section.
- `.planning/phases/01.6.2-the-one-process-host-broker/deferred-items.md` — a dated re-evaluation line appended for the one carried item.
- `.planning/todos/pending/2026-08-04-broker-control-oversize-line-guard-measures-the-whole-buffer-not-the-unterminated-tail.md` — new, WR-02.
- `.planning/todos/pending/2026-08-04-broker-heartbeat-and-evaluation-pass-interval-handles-are-never-captured-or-cleared.md` — new, WR-04.

## Decisions Made

- Row 51's new citation names only plan 13's recycle end-to-end test, not also its release test — the release test proves a different property (WR-01's kill-never-recycle) that this row's own subject does not need it to also carry, per the plan's own read_first note.
- The acknowledgement-test half of row 51's prior citation was kept rather than replaced wholesale, since it still legitimately covers real ground (the kill-and-acknowledge half of the row's subject); only the isolated-module supervision test was removed, with the removal explained in the correction note.
- The 371-vs-368 suite-total discrepancy is restated rather than resolved, even though `01.6.2-11-SUMMARY.md` already fully investigated and explained it — this plan cites that existing explanation rather than treating it as newly discovered or picking whichever number is more convenient.
- WR-02 and WR-04 are filed, not fixed — matching this gap closure's own declared scope and the project's own established convention (`.claude/CLAUDE.md` § Emulator Access: "Log VICE MCP quirks observed... as a pending todo rather than fixing them inline").

## Deviations from Plan

None — plan executed exactly as written. All three tasks' acceptance criteria were met without needing to invoke deviation Rules 1–4: this plan is documentation-only by its own explicit design (no source file is touched), so none of the usual bug/missing-functionality/blocking-issue triggers applied.

## Issues Encountered

The fresh worktree had no `.claude/mcp/vice/node_modules` (needed for `npm --prefix .claude/mcp/vice run typecheck` and for `node build.ts`'s `resources-sync.test.ts` dependency on `tsc`). Resolved by running `npm ci` in `.claude/mcp/vice` before Task 1's verify step, matching the precondition-handling precedent plans 12 and 13 already established for the same situation. Not a deviation — a documented, expected fresh-worktree setup step, not a plan or code defect.

## Verification Details

**Full suite, measured live in this worktree** (`node --test '.claude/mcp/vice/'*.test.*`, run three times — once before any change, once before Task 1's commit, once before Task 2's commit — all three identical):

390 tests, 385 pass, 0 fail, 0 skipped, 5 todo. Matches the orchestrator's given measured baseline exactly (`b43d409d5017a042108be282e8bc829b1fc58b0a`, this plan's own base commit) — no drift to investigate between the orchestrator's measurement and this session's own.

**`npm --prefix .claude/mcp/vice run typecheck`:** exits 0 (`tsc --noEmit -p tsconfig.json`, no output).

**Diff confinement, confirmed directly:**
- `git diff fd9b0fe~1 fd9b0fe -- 01.6.2-VALIDATION.md` touches only row 51, the new correction note, and the reconciliation line.
- `git diff b43d409d5017a042108be282e8bc829b1fc58b0a HEAD --name-only` contains no file matching `01.6.2-0*-PLAN.md` or `01.6.2-1[01]-PLAN.md`.
- No commit in this plan touches `.claude/mcp/vice/broker-control.mts` or `.claude/mcp/vice/vice-broker.mts` — the two files named in the filed-not-fixed todos.

**Citation verbatim-search, confirmed directly:**
- `grep -n "wired recycle: a recycle over the real control plane" .claude/mcp/vice/broker-e2e.test.ts` → line 445, matching row 51's new citation exactly.
- `grep -n "recycle: a grant this connection holds resolves, kills identity-verified" .claude/mcp/vice/broker-control.test.ts` → line 135, matching the kept half of the citation exactly.
- `grep -n "superviseChild: a stub child that exits on its own is respawned" .claude/mcp/vice/broker-launch.test.ts` → line 711, confirming the removed test still exists and passes (it was never claimed to be wrong or broken — only wrong as evidence for row 51's specific claim).

## Known Stubs

None — this plan produces no code, no UI, and no data-wiring; it corrects documentation and files two records of deliberately deferred work.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- All four gap-closure plans (12–15) are now executed and committed. `01.6.2-VALIDATION.md`'s disposition ledger is honest about row 51 (the last of `01.6.2-VERIFICATION.md`'s four failed truths), and its suite figure is a live measurement rather than a carried-forward one — the artifact the next re-verification (`/gsd-verify-work 01.6.2` or a fresh `/gsd-plan-phase 01.6.2 --gaps` pass) should compare against.
- Both filed todos (WR-02, WR-04) and the re-evaluated deferred item are durable records independent of this phase's own `REVIEW.md`/`VERIFICATION.md`, which are expected to be archived at milestone completion — a future session picking up either todo does not need this phase's artifacts to still be readable.
- Criterion E's own carry-forward list remains Phase 01.6.2.1's deliverable, per this plan's own frontmatter comment and the ROADMAP's ownership table — nothing in this plan widens into that scope.
- This plan is documentation-only; there is no new source surface for a future maintainer to be aware of beyond the two filed todos' own proposed fixes (neither applied here).

## Self-Check: PASSED

- Task commits found in `git log --oneline --all`: `fd9b0fe`, `49a9ff1`, `8bac37c`.
- Files confirmed present on disk: `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md`, `.planning/phases/01.6.2-the-one-process-host-broker/deferred-items.md`, `.planning/todos/pending/2026-08-04-broker-control-oversize-line-guard-measures-the-whole-buffer-not-the-unterminated-tail.md`, `.planning/todos/pending/2026-08-04-broker-heartbeat-and-evaluation-pass-interval-handles-are-never-captured-or-cleared.md`.
- No missing items.

---

*Phase: 01.6.2-the-one-process-host-broker*
*Completed: 2026-08-04*
