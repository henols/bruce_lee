---
phase: 01.6.2-the-one-process-host-broker
plan: 11
type: execute
wave: 11
depends_on: ["01.6.2-10"]
files_modified:
  - .claude/mcp/vice/resources/vice-launcher.sh
  - .claude/mcp/vice/resources/vice-broker.sh
  - .claude/mcp/vice/resources/vice-supervisor.sh
  - .claude/mcp/vice/resources/lib/container-guard.sh
  - .claude/mcp/vice/resources/lib/repo-root.sh
  - .claude/mcp/vice/vice-broker.test.mjs
  - .claude/mcp/vice/host-scripts.test.ts
  - .claude/mcp/vice/repo-root.test.ts
  - .claude/mcp/vice/repo-root.ts
  - .claude/mcp/vice/install-resources.test.ts
  - .claude/mcp/vice/incident-record.ts
  - .claude/mcp/vice/vice-broker-launch.test.ts
  - .gitignore
autonomous: false
requirements: ["C2", "C6", "C7", "D-01", "D-24", "PD-04"]
user_setup: []

must_haves:
  truths:
    - "`git ls-files -- '*.sh'` outside the container-provisioning directory returns exactly one path: the surviving launcher"
    - "The surviving launcher resolves the repository root with a function defined in that one file, sourcing nothing, and its four-branch precedence produces the same result the shared library produced for every input"
    - "The launcher's resolved-paths output is byte-identical before and after the inline"
    - "The retiring bash daemon's test file no longer exists, and the suite's total equals the expected total the validation artifact recorded"
    - "The two structural script arrays in the host-scripts test file no longer exist, and the tests driven by them are either deleted with a reason or reduced to the one invariant that still has a subject"
    - "The launcher's exec-into-the-node-entry-point invariant is still asserted, because signal delivery passing straight through to the broker depends on it"
    - "The deployed-path block in the ignore file and the resource-entry set are in two-way parity after the deletion"
    - "The projection file is not written by anything in the repository, and no test asserts on it"
    - "The structural gate that forbids the retiring daemon filenames now covers the resources directory too, since the files themselves are gone"
    - "The request-id pattern exists in exactly one place in the repository"
    - "`npm --prefix .claude/mcp/vice run typecheck` exits 0 and the full suite reports 0 failures"
    - { statement: "The surviving launcher resolves the correct repository root on the developer's real host, where no container workspace variable is set and the git-marker branch is the one that runs", verification: backstop }
    - { statement: "The deployment prune step removes the stale deployed copies of the four deleted scripts from the host's deployment directory on the next installer run", verification: backstop }
  prohibitions:
    - statement: "The four bash files are deleted in one commit — a partial deletion turns three existing gates red for a mechanical reason and is not an acceptable intermediate state"
      status: pending
      verification: flagged-unverified
    - statement: "No test is deleted in this plan without a reason recorded in the SUMMARY"
      status: pending
      verification: flagged-unverified
    - statement: "Nothing under the resources directory is hand-edited except the surviving launcher, and nothing under the deployment directory is hand-edited at all"
      status: pending
      verification: flagged-unverified
    - statement: "No second copy of the request-id pattern is introduced anywhere, and the surviving copy is the container-side export"
      status: pending
      verification: flagged-unverified
  artifacts: []
  key_links:
    - "The two shared library files are sourced by the two daemons, and the ignore-parity gate is driven by the resource-entry walk, so a partial deletion turns three existing gates red for a mechanical reason — this is why it is one commit"
    - "The repository-root inline must land in the same commit as the library file's deletion, because the launcher needs the resolved root before it can construct the command that starts the broker"
    - "The retiring test file's deletion is gated on plan 10's ledger being complete, and the expected suite total plan 10 recorded is what this plan asserts against"
    - "The existing manifest-scoped prune step already handles removing the stale deployed copies, so no new prune logic is needed here"
    - "C7's bash copy of the request-id pattern retires as a pure consequence of deleting the file that holds it"
---

<objective>
Delete the four retiring bash files in one commit, together with the retiring test file, the structural
arrays that enumerate them, and the tests whose subjects go with them — after inlining the repository-root
ladder into the surviving launcher and repointing every test that used a retiring script as its fixture.

Purpose: this is the irreversible moment of the phase. The two shared library files are sourced by the two
daemons, and the ignore-parity gate is driven by the resource-entry walk, so a partial deletion turns three
existing gates red for a purely mechanical reason and produces a state nobody can tell apart from a real
regression. The deletion is also the point at which criterion C6's exactly-one-shell-script becomes true
and criterion C7's duplicated request-id pattern retires as a pure consequence rather than as work.

Output: one shell script left in the repository, a launcher that resolves its own repository root, no
retiring test file, and a suite whose total matches the number plan 10 wrote down.
</objective>

## Artifacts this plan produces

Beyond the phase-level roster in `01.6.2-01-PLAN.md`: no new files. This plan deletes.

**Deleted files:** `resources/vice-broker.sh`, `resources/vice-supervisor.sh`,
`resources/lib/container-guard.sh`, `resources/lib/repo-root.sh` (and with them the now-empty `resources/lib/`
directory), and `vice-broker.test.mjs`.

**Deleted test constructs:** the host-scripts test file's two structural script arrays, its
signal-handling test for the retiring per-instance supervisor, its trap-registration loop, and its syntax-check
loop.

**Added to the surviving launcher:** a `resolve_repo_root` function defined in that one file, replacing the
sourced library.

**Removed from the ignore file:** the deployed-path lines for the four deleted scripts.

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-CONTEXT.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-RESEARCH.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-PATTERNS.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-10-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <reversibility rating="one-way">Deleting 2,546 lines of host-side bash plus their two shared libraries and a 2,685-line test suite is a one-way door: the replacement is a different protocol, not a reimplementation, so reverting means restoring a file protocol that this phase's container half no longer speaks.</reversibility>
  <decision>Confirm the deletion of the four retiring bash files and the retiring test suite, now that the replacement is green and the disposition ledger is complete.</decision>
  <context>
Everything that was going to replace these files exists and is green. What remains is the deletion, and
it cannot be undone by a revert of this commit alone — the container half of the file protocol was
deleted in plan 07, so restoring the bash daemon would restore a server nothing speaks to.

**What is being deleted.** The bash broker and the per-instance supervisor, together the 2,546 lines this
phase's goal sentence names; the two shared library files they source, whose logic survives — the
repository-root ladder inlined into the surviving launcher by the next task, the container guard ported to
TypeScript in plan 01; and the 2,685-line, sixty-one-test suite that verified the daemon by spawning it and
inspecting on-disk files.

**Why one commit.** The two library files are sourced by the two daemons, and the ignore-parity gate is
driven by the resource-entry walk. Deleting any subset turns three existing gates red for a mechanical
reason, producing a state indistinguishable from a real regression.

**What backs the decision.** Plan 10's disposition ledger gives every one of the sixty-one tests an
outcome, with each replacement named and confirmed to exist and pass, and every deletion carrying a
classed reason. Plan 10's function disposition table gives every retiring shell function an outcome.
Plan 07's structural closure gate, with demonstrated ability to fail, asserts none of the retiring lease
mechanisms survives. The frozen epoch and broker fixtures captured in plan 01 preserve the on-disk
contracts as evidence independent of the scripts.

**What is knowingly given up.** No live verification exists until Phase 01.4, so this whole replacement is
proven at unit and integration level and at nothing above that. The developer has already accepted that
(recorded as D-03). This checkpoint is not asking to re-open it; it is the last point at which the bash
implementation is still available to compare against.

**Precedent.** The equivalent deletion in Phase 01.6 plan 04 sat behind exactly this kind of blocking
decision checkpoint, and that is the pattern being followed.
  </context>
  <options>
    <option id="delete">
      <name>Delete all four scripts and the retiring test suite now</name>
      <pros>Criterion C6's exactly-one-shell-script becomes true; criterion C7's duplicated pattern retires as a consequence; no window exists in which two implementations coexist untested against each other</pros>
      <cons>Irreversible in practice; the bash implementation stops being available as a reference for comparison during the remaining sub-phase</cons>
    </option>
    <option id="delete-scripts-keep-suite">
      <name>Delete the four scripts but keep the retiring test suite a while longer</name>
      <pros>The old test bodies stay readable in-tree during Phase 01.6.2.1</pros>
      <cons>A suite whose subject is gone cannot run, so it is either skipped wholesale or red wholesale; a skipped suite is exactly the kind of dead weight this project deletes rather than carries, and the ledger already preserves what each test asserted</cons>
    </option>
    <option id="defer">
      <name>Defer the deletion to Phase 01.6.2.1</name>
      <pros>Keeps the reference implementation available while the lifecycle-policy changes land</pros>
      <cons>Leaves the phase goal literally false — exactly one shell script left in the repository is this phase's own criterion — and leaves a window in which two implementations coexist with neither tested against the other, which is the window the split's seal boundary exists to close</cons>
    </option>
  </options>
  <resume-signal>Select: delete, delete-scripts-keep-suite, or defer</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Inline the repository-root ladder into the surviving launcher and repoint every test that used a retiring script as its fixture (PD-04)</name>
  <files>.claude/mcp/vice/resources/vice-launcher.sh, .claude/mcp/vice/repo-root.test.ts, .claude/mcp/vice/install-resources.test.ts, .claude/mcp/vice/repo-root.ts, .claude/mcp/vice/incident-record.ts</files>
  <read_first>
    - `.claude/mcp/vice/resources/lib/repo-root.sh` — all 103 lines. The four-branch precedence ladder, the two one-time warning latches, its header's explanation of why a fixed parent-directory hop is wrong and fails silently, and its deliberate refusal to set shell options of its own
    - `.claude/mcp/vice/resources/vice-launcher.sh` — as plan 01 left it. Where the library is sourced and where the resolved root is used
    - `.claude/mcp/vice/repo-root.ts` — the Node-side ladder the bash one mirrors, so the inline stays in step with it, plus its comment referencing the retiring scripts
    - `.claude/mcp/vice/repo-root.test.ts` — the whole file. Its parity tests compare the resolved-paths output of the two retiring daemons against their deployed copies; the same property is testable against the surviving launcher, which has the same flag. Read every occurrence before deciding which are repointed and which lose their subject
    - `.claude/mcp/vice/install-resources.test.ts` — the whole file. It uses a retiring script as its stand-in for "a tracked resource entry" across install, skip, diverge, force, executable-mode and prune tests. Each needs a surviving entry instead
    - `.claude/mcp/vice/incident-record.ts` — its comment referencing the retiring daemon's atomic-write helper
    - `.claude/mcp/vice/vice-broker-launch.test.ts` — its launcher flag tests and its syntax check, which stay and which make the host-scripts file's loop redundant
  </read_first>
  <action>
Do everything that must be true before the deletion, while the files being deleted still exist, so this
task's commit is green and the deletion commit is a pure removal.

**Inline the repository-root ladder.** Define a `resolve_repo_root` function inside the surviving launcher
and remove the line that sources the shared library. This is not a free choice: the shell must resolve the
root before it can construct the command that starts the broker, so by the time a Node process exists there
is nothing left to decide. Reproduce all four branches in their existing precedence and with their existing
warning behaviour: the container workspace variable when it is set and contains the caller; otherwise a walk
upward for a git marker, testing for existence so a worktree's marker file matches as well as a directory;
otherwise the container workspace variable on its own with a one-time note naming both paths; otherwise a
location-shaped last resort with a one-time note. Keep the one-time latches — a long-lived process or a test
driving the function repeatedly must not spam its error stream. Carry the library header's explanation of why
a fixed parent-directory hop is wrong forward into the launcher, because that reasoning is the reason the
function exists and it is unaffected by which file holds it.

**Repoint the resolved-paths parity tests.** The tests comparing a retiring daemon's resolved-paths output
against its deployed copy are asserting a real property — that the committed source and the deployed copy
resolve identically — and the surviving launcher has the same flag, so repoint them at it. Where a test's
subject is a behaviour only a retiring daemon had, mark it for deletion in the next task rather than
repointing it at a launcher that does not do that thing, and record the reason.

**Repoint the installer's fixture entries.** The installer's tests use a retiring script as their stand-in for
a tracked resource entry. Replace it with a surviving entry — the launcher, or one of the emitted broker
artifacts — chosen so the property under test still holds: the executable-mode test needs an entry that is
deployed executable, and the prune test needs an entry it can retire without disturbing the others. Read each
test before choosing, because they are not all testing the same thing.

**Update the comment-only references.** The Node-side ladder and the incident-record module each mention a
retiring script in a comment. Rewrite each to name what actually holds that logic now, rather than deleting
the sentence: both comments exist to tell a reader where the paired implementation lives, and that pairing
still exists at a new address.

Change no behaviour in this task beyond the launcher's sourcing. Nothing is deleted here.
  </action>
  <verify>
    <automated>node --test .claude/mcp/vice/repo-root.test.ts .claude/mcp/vice/install-resources.test.ts .claude/mcp/vice/vice-broker-launch.test.ts .claude/mcp/vice/host-scripts.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'lib/repo-root.sh' .claude/mcp/vice/resources/vice-launcher.sh` prints `0`, and `grep -c 'resolve_repo_root()' .claude/mcp/vice/resources/vice-launcher.sh` prints at least `1`
    - `bash -n .claude/mcp/vice/resources/vice-launcher.sh` exits 0
    - The launcher's resolved-paths output is byte-identical to the output captured from it before this task, verified by capturing before and diffing after
    - A differential check over at least six inputs — inside the workspace with the variable set, inside a git repository with the variable unset, in a directory with neither, and the resources-named and other-named last-resort shapes — produces identical results from the inlined function and the still-present shared library, recorded in the SUMMARY as a table
    - Calling the inlined function repeatedly emits each warning at most once, asserted by a test capturing its error stream across three calls
    - Every repointed parity test in the repository-root test file passes and names the surviving launcher
    - Every repointed installer fixture test passes, and the executable-mode and prune tests each still assert the property they were written for
    - The comment-only references in the Node-side ladder and the incident-record module name a module that exists
    - `node --test '.claude/mcp/vice/'*.test.*` reports 0 failures and `npm --prefix .claude/mcp/vice run typecheck` exits 0
  </acceptance_criteria>
  <done>The launcher resolves its own repository root with the same four-branch behaviour and the same warning discipline, every test that used a retiring script as a fixture now uses a surviving one, and the tree is green with nothing yet deleted.</done>
</task>

<task type="auto">
  <name>Task 3: THE DELETION — four bash files, the retiring test suite, the structural arrays and the tests whose subjects went with them, in one commit</name>
  <files>.claude/mcp/vice/resources/vice-broker.sh, .claude/mcp/vice/resources/vice-supervisor.sh, .claude/mcp/vice/resources/lib/container-guard.sh, .claude/mcp/vice/resources/lib/repo-root.sh, .claude/mcp/vice/vice-broker.test.mjs, .claude/mcp/vice/host-scripts.test.ts, .gitignore</files>
  <read_first>
    - `.claude/mcp/vice/host-scripts.test.ts` — the whole file as task 2 left it. Both structural arrays, the signal-handling test for the retiring per-instance supervisor, the trap-registration loop, the syntax-check loop, the ignore-parity gate and the tracked-script allowlist test, plus the array comment that already names this phase as the one that shrinks it
    - `.gitignore` lines 95–115 — the deployed-path block, and which lines correspond to the four files being deleted
    - `.claude/mcp/vice/install-resources.ts` — the resource-entry walk that drives the ignore-parity gate, so the parity is restored in the same commit
    - `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md` — plan 10's recorded expected suite total after this deletion, which is the number this task asserts against
    - `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-10-SUMMARY.md` — the ledger's confirmation that every replacement exists and passes, which is the precondition for deleting the suite
    - `.claude/mcp/vice/vice-broker-launch.test.ts` — its own syntax check and flag tests for the launcher, which make the host-scripts file's loop redundant rather than lost
    - `.claude/mcp/vice/broker-kill.test.ts` — plan 04's shutdown tests, which are the replacement for the deleted signal-handling test and which the SUMMARY must name as such
  </read_first>
  <precondition>Plan 10's disposition ledger is complete in the validation artifact, every REPLACED and RE-OBSERVED row's named test has been confirmed to exist and pass, and the expected post-deletion suite total is recorded there. If any of those is missing, HALT — the deletion would be made on a false premise.</precondition>
  <action>
Delete, in one commit.

Remove the bash broker, the per-instance supervisor, and both shared library files, along with the
now-empty library directory. One commit, not four: the library files are sourced by the two daemons and the
ignore-parity gate is driven by the resource-entry walk, so any subset leaves three existing gates red for a
mechanical reason and produces a state nobody can tell apart from a real regression.

Remove the retiring test file. Its subject is gone and plan 10's ledger records what each of its tests
asserted and where that assertion now lives.

In the host-scripts test file, remove both structural script arrays. With them go the tests they drove that
have no subject left: the signal-handling test for the retiring per-instance supervisor — replaced by the
broker's own shutdown tests, which cover every catchable path rather than one signal — the trap-registration
loop, and the syntax-check loop, which is redundant because the launcher's own test file already checks its
syntax. Keep the launcher's exec-into-the-node-entry-point invariant as a standalone test: it is load-bearing,
because that exec is what makes signal delivery pass straight through to the broker with no shell trap in
between, and it is the one assertion in that group whose subject survives. Keep the ignore-parity gate and the
tracked-script allowlist test, reducing the allowlist to exactly the one surviving path, which the array's own
comment has anticipated since it was written.

Remove the deployed-path lines for the four deleted files from the ignore file, in this same commit, so the
two-way parity gate stays green. Add nothing: the existing manifest-scoped prune step already removes the
stale deployed copies from the host's deployment directory on the next installer run, which is a solved
problem and not a new gap.

Widen the structural gate plan 09 added so it now covers the resources directory as well as the module set.
Its own comment records that the directory was deliberately out of scope while the files still existed; that
reason is gone, so the exclusion goes with it.

Confirm, do not assume, three consequences and record each in the SUMMARY: that the tracked shell-script set
outside the container-provisioning directory is exactly one path; that the request-id pattern now exists in
exactly one place in the repository, which is criterion C7 satisfied as a pure consequence of this deletion;
and that the suite's total equals the expected total plan 10 recorded, with any difference investigated
rather than accepted.

Record every deleted test with its reason in the SUMMARY, including the four constructs removed from the
host-scripts file. A deletion with no recorded reason is the failure the ledger discipline exists to prevent,
and it applies to this file as much as to the retiring suite.
  </action>
  <verify>
    <automated>node --test '.claude/mcp/vice/'*.test.* && node .claude/mcp/vice/build.ts && npm --prefix .claude/mcp/vice run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -- '*.sh' | grep -v '^.devcontainer/' | wc -l` prints `1`, and the single path is the surviving launcher
    - `git ls-files -- '*.sh' | grep -c '^.devcontainer/'` still prints `2`, so the container-provisioning scripts are untouched
    - `test -e .claude/mcp/vice/resources/lib` returns non-zero — the library directory is gone, not merely emptied
    - `test -e .claude/mcp/vice/vice-broker.test.mjs` returns non-zero
    - `node --test .claude/mcp/vice/host-scripts.test.ts` exits 0, the tracked-script allowlist holds exactly one entry, and the ignore-parity gate passes
    - A test asserting the launcher's exec into the node entry point still exists and passes
    - `grep -c '^/tools/' .gitignore` equals the number of resource entries plus one for the deployment manifest, asserted by the existing two-way parity gate rather than by hand
    - The structural gate from plan 09 now includes the resources directory in its scan, and the comment recording the earlier exclusion is removed
    - `grep -rn 'req-\[0-9\]' --include='*.ts' --include='*.mts' --include='*.sh' . | grep -v node_modules | wc -l` prints `1`, and that occurrence is the container-side exported pattern — criterion C7 satisfied as a consequence
    - `node --test '.claude/mcp/vice/'*.test.*` reports 0 failures and a total equal to the expected total recorded in the validation artifact, with any difference investigated and explained in the SUMMARY
    - `node .claude/mcp/vice/build.ts` exits 0 and `node --test .claude/mcp/vice/resources-sync.test.ts` passes
    - `npm --prefix .claude/mcp/vice run typecheck` exits 0
    - The SUMMARY lists every deleted test and construct with its reason, including the four removed from the host-scripts file
  </acceptance_criteria>
  <done>Exactly one shell script remains in the repository outside container provisioning, the retiring suite and the structural arrays are gone with every deletion reasoned in writing, the ignore-parity and resources-sync gates are green, the request-id pattern exists once, and the suite total matches the number written down before the deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| committed source → deployed host copy | Deleting a source file must propagate to the host's deployment directory, or a stale executable survives |
| repository-root resolution → state directory | A wrongly resolved root produces a permanently empty state directory with no error at all |
| deletion → verification coverage | Removing a test suite removes what it proved unless the replacement genuinely covers it |

## STRIDE Threat Register

ASVS level 1. Blocking disposition on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.6.2-65 | Tampering | a stale deployed copy of a deleted script surviving on the host | high | mitigate | The existing manifest-scoped prune step removes deployed entries no longer in the resource set; the ignore-parity gate keeps the manifest and the ignore block in step in the same commit. No new prune logic is added |
| T-01.6.2-66 | Tampering | the inlined repository-root ladder resolving differently from the library it replaces | high | mitigate | A differential check over at least six inputs, comparing the inlined function against the still-present library before the deletion, recorded as a table; plus a byte-identical resolved-paths comparison before and after |
| T-01.6.2-67 | Denial of Service | a warning latch lost in the inline, flooding the error stream | medium | mitigate | The one-time latches are reproduced and asserted by a test capturing the error stream across three calls |
| T-01.6.2-68 | Repudiation | verification coverage silently lost with the deleted suite | high | mitigate | Deletion is gated on plan 10's ledger being complete with every named replacement confirmed to exist and pass; the task's precondition halts if it is not, and the expected suite total is asserted against a number recorded before the deletion |
| T-01.6.2-69 | Repudiation | a partial deletion read as a real regression | high | mitigate | All four files go in one commit, for the stated mechanical reason; the three gates that would otherwise turn red are asserted green in the same commit |
| T-01.6.2-70 | Tampering | a second copy of the request-id pattern surviving | medium | mitigate | Asserted by a repository-wide count returning exactly one occurrence, which is criterion C7 satisfied as a consequence of the deletion rather than as separate work |
| T-01.6.2-71 | Tampering | a retiring daemon filename reintroduced once the files are gone | medium | mitigate | The structural gate from plan 09 is widened to cover the resources directory, and the comment recording the earlier exclusion is removed with its reason |
</threat_model>

<verification>
- `git ls-files -- '*.sh'` returns exactly one non-provisioning path
- `node --test '.claude/mcp/vice/'*.test.*` reports 0 failures and a total matching the expected total plan 10 recorded
- `node .claude/mcp/vice/build.ts` exits 0, the resources-sync gate passes, the ignore-parity gate passes, and `npm --prefix .claude/mcp/vice run typecheck` exits 0
- The differential repository-root check is recorded as a table with at least six inputs
- Every deleted test and construct has a recorded reason in the SUMMARY
- Two claims are backstops: the launcher resolving the correct root on the developer's real host where the git-marker branch is the one that runs, and the prune step clearing the stale deployed copies. No live verification exists until Phase 01.4.
</verification>

<success_criteria>
- Exactly one shell script survives in the repository outside container provisioning, and the container guard survives with it in TypeScript at broker startup
- The repository-root ladder's logic survives in the launcher with identical behaviour, proven differentially before the source of truth was removed
- The retiring suite is gone against a complete ledger, and the suite's total matches a number written down beforehand
- Criterion C7 is satisfied as a pure consequence: the request-id pattern exists exactly once
- Every gate that a partial deletion would have broken is green in the same commit that does the deletion
- Every deletion has a reason in writing
</success_criteria>

<output>
Create `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-11-SUMMARY.md` when done.
</output>
