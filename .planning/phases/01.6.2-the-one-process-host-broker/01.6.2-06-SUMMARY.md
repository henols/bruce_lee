---
phase: 01.6.2-the-one-process-host-broker
plan: 06
subsystem: infra
tags: [typescript, tcp, control-plane, client, broker, emulator-tooling]

# Dependency graph
requires:
  - phase: 01.6.2 plan 05
    provides: "the complete control-plane message set (acquire/release/recycle/status/host_state) and the final fourteen-field broker.json discovery record this plan's client speaks to"
  - phase: 01.6.2 plan 01
    provides: "the wire-format decision (token auth, 0.0.0.0 bind, port 19510, newline-delimited JSON) and acquireOverControlPlane(), the tracer this plan's session API sits alongside"
provides:
  - "openBrokerControl(): the completed container-side TCP control client. One session-lifetime connection, one discovery-record read, all five request kinds (acquire/release/recycle/status/hostState), per-request deadlines (ACQUIRE_TIMEOUT_MS/RECYCLE_TIMEOUT_MS carried forward unchanged; CONTROL_CONNECT_TIMEOUT_MS genuinely new), and a distinct broker_gone outcome when the broker drops the connection mid-request"
  - "BrokerControlSession and its five typed result unions (ControlAcquireResult/ControlReleaseResult/ControlRecycleResult/ControlStatusResult/ControlHostStateResult), plus ControlFailureKind covering every way a request can fail without ever rejecting a promise"
  - "The full criterion-A-second-half disposition table: every one of the 14 tests in the file BEFORE this plan carries a named, written SURVIVES/RE-OBSERVED/DELETED disposition -- no silent drops -- which plan 07 executes against verbatim"
  - "A region-bounded structural test (marker-pair extraction, not a whole-file scan) proving the new client region writes nothing to the filesystem, with its discriminating power verified live (a write was temporarily inserted, the test was confirmed to fail, then reverted)"
affects: ["01.6.2 plan 07 (the file-protocol deletion and proxy swap onto this session client -- executes this plan's DELETED rows verbatim)", "01.6.2 plan 08 (D-14's broker-death discipline is built on this plan's broker_gone outcome)", "01.6.2.1 (final ACQUIRE_TIMEOUT_MS/RECYCLE_TIMEOUT_MS/CONTROL_CONNECT_TIMEOUT_MS tuning against the measured tool-call budget, and the connection's real idle-period survival, are both explicit backstops here)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Never-reject session API: every failure mode (deadline, refused connection, malformed line, broker-gone, the broker's own error code) resolves a typed { ok: false, kind, message } value rather than throwing/rejecting -- sidesteps any possibility of an unhandled rejection escaping the client entirely, a stronger posture than the tracer's own acquireOverControlPlane() (which still rejects)."
    - "FIFO pending-request queue keyed by arrival order, not by echoing the request id -- sound because every session method awaits its own sendAndAwaitLine() call to settle before this client ever writes a second request line, and several response kinds (released/status/host_state) do not even carry an id on the wire to correlate against."
    - "classifyLivenessFromRecord() factored out of readBrokerLiveness() as a pure function over an already-parsed record -- lets openBrokerControl() classify liveness against the SAME broker.json read it performs for control_host/control_port/control_token, rather than a second file read for the same record. readBrokerLiveness()'s own exported behaviour is unchanged."
    - "Per-call ControlDeadlineOptions ({ timeoutMs }) on acquire()/recycle()/status()/hostState(), matching the existing PollOptions precedent (pollGrant()/pollRecycleAck() already take an optional timeoutMs the same way) -- lets tests inject a short bound to prove a deadline actually elapses, without waiting out the real one."
    - "Region-bounded structural test: two comment markers (BROKER-CONTROL-CLIENT REGION START/END) bound exactly the new client code, and the test extracts that substring rather than scanning the whole file -- so the retiring file protocol's own filesystem writes (still present above the marker until plan 07) never mask a new one introduced inside the region."

key-files:
  created: []
  modified:
    - .claude/mcp/vice/vice-broker-client.ts
    - .claude/mcp/vice/vice-broker-client.test.ts

key-decisions:
  - "acquireOverControlPlane() (plan 01's tracer) is left completely untouched, not renamed or folded into the new session API -- it is still the acquire helper broker-e2e.test.ts and broker-kill.test.ts drive directly against the SERVER side, and this plan is additive-only. openBrokerControl()/BrokerControlSession is a second, independent, more complete client living alongside it in the same file."
  - "No import from broker-control.mts into vice-broker-client.ts (or vice versa beyond what already existed) -- the new client defines its own local, duplicated wire-format types (ControlFailureKind mirroring broker-control.mts's ControlErrorCode, AcquireGrant already matching the wire's grant fields) rather than sharing a TypeScript import across the container/host module boundary. The two processes' only real contract is the wire format, not a shared type; this also avoids reproducing plan 05's own 'host importing container pulls in transitive deps' finding in the reverse direction."
  - "ACQUIRE_TIMEOUT_MS and RECYCLE_TIMEOUT_MS are declared as direct references to the tracer's existing CONTROL_ACQUIRE_TIMEOUT_MS and the retiring pollRecycleAck()'s RECYCLE_ACK_TIMEOUT_MS respectively (not re-parsed from the same env vars a second time) -- makes 'carried forward unchanged, not re-tuned' a structural guarantee (the two constants literally cannot drift apart) rather than a comment-only claim."
  - "CONTROL_CONNECT_TIMEOUT_MS (5000ms) is a plain constant, not read from any environment variable -- it is genuinely new (the file protocol never 'connected' anywhere, so there is no retiring value to carry forward), and 01.6.2-06-PLAN.md's own scope is exactly the two deadline variables 01.6.2-01-PLAN.md already named. Final tuning is Phase 01.6.2.1's item, same as the other two."
  - "The 'reads broker.json exactly once' claim is proven by deleting the file after the session opens and confirming five subsequent requests still succeed, rather than by instrumenting/counting fs.readFileSync calls -- a live experiment (recorded below) confirmed that node:test's mock.method() on a separately-imported fs binding does NOT affect vice-broker-client.ts's own already-bound named import, so a call-count spy was not a viable technique in this module system. The delete-then-verify technique is a stronger regression guard in practice: a future regression that reintroduced a per-request re-read would fail this test the moment the file disappeared, whereas a call-count assertion could be satisfied by an unrelated single extra read."
  - "The 'never-started/stale, connect function never called' claim is proven by pointing control_host at a TEST-NET-1 address (RFC 5737, 203.0.113.1) and asserting the result resolves in under 500ms -- if a connection had actually been attempted, the promise would take far longer (OS-level SYN-retransmit timeout, or this client's own 5000ms CONTROL_CONNECT_TIMEOUT_MS), so the elapsed-time bound is real evidence of no connection attempt, not just an assertion on the returned kind. The equivalent monkey-patch-and-count-calls approach for node:net's connect() was ruled out for the same reason as the readFileSync case above."

requirements-completed: ["A", "C7", "D-01", "D-12", "D-27"]

coverage:
  - id: D1
    description: "openBrokerControl() reads broker.json exactly once for control_host/control_port/control_token, classifying liveness against that SAME read, and refuses to connect when never_started or stale"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: "vice-broker-client.test.ts#openBrokerControl(): reads broker.json exactly once -- deleting it after the session opens does not affect five subsequent requests"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#openBrokerControl(): a never-started record (missing heartbeat_at) returns a typed failure fast, without attempting a connection"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#openBrokerControl(): a stale record returns a typed failure fast, without attempting a connection"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#openBrokerControl(): a refused connection returns a typed connect_refused failure naming the refusal"
        status: pass
    human_judgment: false
  - id: D2
    description: "BrokerControlSession drives all five request kinds over one connection, every request carrying the discovery record's token; acquire/recycle results carry exactly the key sets the proxy's own grant containerizer / outcome renderer read"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: "vice-broker-client.test.ts#openBrokerControl(): opens a session and drives all five request kinds, every one carrying the discovery record's token"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#acquire result: the grant object has exactly the key set containerizeGrant() reads"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#recycle result: the ack object has exactly the key set the proxy's outcome renderer reads"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#acquire: resolves a typed failure carrying the broker's own error code and message when the broker answers an error"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every request kind resolves against its own deadline and never hangs; the broker dropping the connection mid-request settles every in-flight request with a distinct broker_gone outcome"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: "vice-broker-client.test.ts#acquire: resolves a typed deadline failure within its own bound and does not hang, using a short bound injected for the test"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#session: the broker closing the connection mid-request settles it with a distinct broker_gone outcome, never a request-level error"
        status: pass
    human_judgment: false
  - id: D4
    description: "Newline framing handles both directions correctly (two responses in one chunk, one response split across two chunks), and a malformed response line settles as a protocol failure without ever producing an unhandled rejection"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: "vice-broker-client.test.ts#session: two responses arriving in one chunk are both delivered"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#session: one response split across two chunks is delivered exactly once"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#session: a malformed response line settles the pending request as a protocol failure, and no unhandled rejection occurs"
        status: pass
      - kind: unit
        ref: "vice-broker-client.test.ts#session: a second release() resolves without throwing"
        status: pass
    human_judgment: false
  - id: D5
    description: "Nothing in the new control-client region writes to the filesystem -- proven by a region-bounded structural test whose discriminating power was verified live"
    requirement: "D-12"
    verification:
      - kind: unit
        ref: "vice-broker-client.test.ts#structural: the new control-client region (between the plan-06 marker pair) contains no filesystem-write construct"
        status: pass
      - kind: other
        ref: "manual discriminating-power check: a writeFileSync() call was temporarily inserted inside the marked region, the structural test above was re-run and confirmed to FAIL (ERR_ASSERTION, actual !== null), then the insertion was reverted and the test re-confirmed passing"
        status: pass
    human_judgment: false
  - id: D6
    description: "Criterion A's second half: every one of the 14 tests present in vice-broker-client.test.ts before this plan carries a named written disposition (SURVIVES/RE-OBSERVED/DELETED), with every DELETED row carrying a reason -- the full table is reproduced below in this SUMMARY's own body"
    requirement: "A"
    verification:
      - kind: other
        ref: "this SUMMARY's own '## Disposition Table' section -- 14 rows, one per test present in the file before task 1, each with exactly one disposition"
        status: pass
    human_judgment: false
  - id: D7
    description: "The full test suite runs green, reconciled against the running baseline"
    verification:
      - kind: other
        ref: "node --test '.claude/mcp/vice/'*.test.* (417 tests, 412 pass, 0 fail, 5 pre-existing todo; baseline was 402/397/0/5 -- this plan added 15 tests, all passing)"
        status: pass
    human_judgment: false
  - id: D8
    description: "One claim remains an explicit backstop: a session-lifetime connection surviving a real agent session's idle periods, since there is no longer any idle timeout to trip"
    verification: []
    human_judgment: true
    rationale: "Stated as a backstop truth in this plan's own must_haves (verification: backstop). No live agent-session-length idle period exists to test from inside a test process; this is carried forward exactly as prior plans in this milestone have carried forward their own no-live-host-session claims, with no live-session verification until Phase 01.4."

# Metrics
duration: ~30min
completed: 2026-08-04
status: complete
---

# Phase 01.6.2 Plan 06: The Container-Side TCP Control Client Summary

**openBrokerControl()/BrokerControlSession completes the container half of D-01 -- one session-lifetime connection, one discovery-record read, all five request kinds, real per-request deadlines, and a distinct broker_gone outcome -- alongside a full, named disposition table for every test whose subject retires under D-12.**

## Performance

- **Duration:** ~30 min
- **Started:** 2026-08-04T01:31:00Z
- **Completed:** 2026-08-04T02:01:00Z
- **Tasks:** 2 (both `type="auto"`, task 1 `tdd="true"`)
- **Files modified:** 2 (`.claude/mcp/vice/vice-broker-client.ts`, `.claude/mcp/vice/vice-broker-client.test.ts`)

## Accomplishments

- Completed the container-side TCP control client: `openBrokerControl()` reads `broker.json` exactly once for `control_host`/`control_port`/`control_token`, classifies liveness against that SAME read (a new `classifyLivenessFromRecord()` helper factored out of `readBrokerLiveness()`, whose own exported behaviour is unchanged), and refuses to even attempt a connection when the classification is `never_started` or `stale`
- `BrokerControlSession` exposes all five request kinds (`acquire`/`release`/`recycle`/`status`/`hostState`) over one connection held for the session's lifetime -- the connection IS the lease, matching the tolerance decision in `broker-control-plane-over-tcp.md`. Every request carries the token; `acquire`'s grant object and `recycle`'s ack object carry exactly the key sets `vice-proxy.ts`'s `containerizeGrant()` and `recycleAckOutcomeMessage()`/caller respectively read (5 and 3 fields, verified against documented, sourced lists in the tests)
- Real per-request deadlines: `ACQUIRE_TIMEOUT_MS`/`RECYCLE_TIMEOUT_MS` are direct references to the tracer's existing `CONTROL_ACQUIRE_TIMEOUT_MS` and the retiring `pollRecycleAck()`'s `RECYCLE_ACK_TIMEOUT_MS` (carried forward unchanged, never re-tuned, and structurally unable to drift apart); `CONTROL_CONNECT_TIMEOUT_MS` (5000ms) is genuinely new and deliberately not environment-configurable, per this plan's own scope
- A distinct `broker_gone` outcome settles every in-flight request when the broker closes the connection mid-request, tellable apart from any request-level error outcome (`internal`/`denied`/etc.) -- this is what Phase 01.6.2 plan 08's D-14 discipline is built on
- Newline framing handles both directions: two responses arriving in one chunk are both delivered to their correct FIFO-queued caller, and one response split across two writes is delivered exactly once; a malformed response line settles its pending request as a `protocol` failure without ever producing an unhandled rejection -- the client never rejects a promise, resolving a typed `{ ok: false, kind, message }` for every failure mode instead
- A region-bounded structural test (marker-pair extraction between `BROKER-CONTROL-CLIENT REGION START`/`END`, not a whole-file scan) proves the new client writes nothing to the filesystem; its discriminating power was verified LIVE -- a `writeFileSync()` call was temporarily inserted inside the marked region, the test was re-run and confirmed to fail (`ERR_ASSERTION`), then the insertion was reverted and the passing state re-confirmed
- Criterion A's second half discharged: every one of the 14 tests present in `vice-broker-client.test.ts` before this plan carries a named, written disposition (see the Disposition Table below) -- no silent drops. One `RE-OBSERVED` row (the retiring `pollGrant()` denial-reason test) required adding a genuinely new test (`acquire: resolves a typed failure carrying the broker's own error code...`), since the client's own error-code-surfacing behavior was a real coverage gap in task 1's first pass, caught while building the table rather than shipped silently missing
- `acquireOverControlPlane()` (plan 01's tracer) is completely untouched -- still used as-is by `broker-e2e.test.ts`/`broker-kill.test.ts` for their own server-side testing; nothing from the file protocol is deleted, matching this plan's additive-only scope

## Task Commits

1. **Task 1: Complete the container-side control client -- all five kinds, one connection, one record read, real deadlines** - `cc595d9` (feat)
2. **Task 2: Redesign vice-broker-client.test.ts against the new transport, with a named disposition for every retiring test (criterion A, second half)** - `d19c856` (test)

**Plan metadata:** pending (this SUMMARY's own commit)

## Files Created/Modified

- `.claude/mcp/vice/vice-broker-client.ts` - `readBrokerLiveness()` split to extract `classifyLivenessFromRecord()` (pure, no behavior change); new region added (marked `BROKER-CONTROL-CLIENT REGION START/END`): `ACQUIRE_TIMEOUT_MS`, `RECYCLE_TIMEOUT_MS`, `CONTROL_CONNECT_TIMEOUT_MS`, `ControlFailureKind`, `ControlAcquireResult`/`ControlReleaseResult`/`ControlRecycleResult`/`ControlStatusResult`/`ControlHostStateResult`, `ControlDeadlineOptions`, `BrokerControlSession`, `OpenBrokerControlOptions`/`OpenBrokerControlOutcome`, `createSession()`, `openBrokerControl()`. 1042 lines total (was 579)
- `.claude/mcp/vice/vice-broker-client.test.ts` - 15 new tests covering the new client (happy path across all five kinds, both key-set assertions, the acquire error-code path, both liveness-precheck paths, the exactly-once-read proof, the refused-connection path, the deadline proof, the broker-gone proof, both framing directions, the malformed-line proof, the idempotent-release proof, and the region-bounded structural test) plus a retiring-region comment naming plan 07. 837 lines total (was 241); 29 tests total (was 14)

## Decisions Made

See `key-decisions` in this SUMMARY's own frontmatter for the full rationale on: leaving `acquireOverControlPlane()` untouched, avoiding a cross-module TypeScript import between the container and host sides, structurally tying `ACQUIRE_TIMEOUT_MS`/`RECYCLE_TIMEOUT_MS` to their retiring predecessors by direct reference, `CONTROL_CONNECT_TIMEOUT_MS`'s genuinely-new non-configurable status, and the two black-box techniques (delete-then-verify for the exactly-once-read claim; unroutable-address-plus-elapsed-time for the never-started/stale no-connection-attempted claim) substituted for call-count instrumentation after a live experiment confirmed `node:test`'s `mock.method()` does not affect this module's own already-bound `node:fs`/`node:net` named imports.

## Disposition Table

Criterion A's second half: every test present in `vice-broker-client.test.ts` **before** task 1 began (14 tests, counted via `grep -c '^test('` against the pre-task-1 file), with its disposition. `readBrokerLiveness()` is the one surviving function from the retiring file-protocol region (per `01.6.2-01-PLAN.md`'s own surviving-exports list); `newRequestId()`/`isValidRequestId()` also survive as the shared request-id primitives the new client itself uses. Every other test's subject (`writeRequest`/`createLease`/`touchLease`/`releaseLease`/`pollGrant`) is part of the file-messaging protocol D-01/D-12 replace wholesale, retiring in Phase 01.6.2 plan 07 alongside its own deletion.

| # | Original test | Disposition | Detail |
|---|---|---|---|
| 1 | `newRequestId()/isValidRequestId(): accepts its own output and rejects a hostile corpus` | **SURVIVES** | `REQUEST_ID_PATTERN`/`newRequestId`/`isValidRequestId` are explicitly named as surviving exports (`01.6.2-01-PLAN.md`'s artifact list); the new client's `acquire()`/`recycle()` call `newRequestId()` directly, unchanged. Test itself untouched. |
| 2 | `writeRequest()/createLease(): write final files with every documented field, no tmp file left behind` | **DELETED** | `writeRequest()`/`createLease()` retire wholesale under D-01/D-12 (the container↔broker seam is replaced wholesale by the TCP control plane -- `01.6.2-CONTEXT.md` line 435). The new transport writes no files at all -- there is no successor file-writing behavior to re-observe. Deletion belongs in the same commit as `writeRequest()`/`createLease()`'s own deletion, in plan 07. |
| 3 | `writeRequest()/createLease(): reject an invalid request id before touching the filesystem` | **DELETED** | Same retiring subjects as row 2. The new transport never touches the filesystem per request at all, so "before touching the filesystem" has no equivalent concern to re-observe. |
| 4 | `touchLease(): advances the lease file's mtime` | **DELETED** | `touchLease()` and the mtime-as-heartbeat convention are two of D-12's six explicitly-named retiring mechanisms. The connection itself is the lease now (open = claim, close = release, kernel-enforced) -- there is nothing to "touch." |
| 5 | `touchLease(): a silent no-op against a lease that does not exist` | **DELETED** | Same subject as row 4 -- the heartbeat mechanism this test exercises retires in full, with no successor to re-observe against. |
| 6 | `releaseLease(): the entire release removes the lease file` | **RE-OBSERVED** | Replacement: `openBrokerControl(): opens a session and drives all five request kinds, every one carrying the discovery record's token` (asserts `session.release()` resolves `{ ok: true }`). The connection-close release this new test exercises is the direct successor to file-lease removal, per the tolerance decision in `broker-control-plane-over-tcp.md` ("the connection IS the lease"). |
| 7 | `releaseLease(): a silent no-op on a missing lease file -- idempotent double release` | **RE-OBSERVED** | Replacement: `session: a second release() resolves without throwing` -- a direct match for the same idempotent-double-release property, translated from "unlink an already-gone file" to "destroy an already-destroyed socket." |
| 8 | `pollGrant(): resolves granted:true once a grant appears mid-poll` | **RE-OBSERVED** | Replacement: `openBrokerControl(): opens a session and drives all five request kinds...` plus `acquire result: the grant object has exactly the key set containerizeGrant() reads`. `acquire()` over the new transport (an immediate response, no polling) is the direct successor to polling for a grant file to appear. |
| 9 | `pollGrant(): resolves granted:false and surfaces the denial's reason verbatim` | **RE-OBSERVED** | Replacement: `acquire: resolves a typed failure carrying the broker's own error code and message when the broker answers an error` -- a NEW test added specifically to close this row (task 1's own `<behavior>` list already required this path; it was a genuine coverage gap in task 1's first pass, caught while building this table). The broker's own error-CODE semantics stay covered by `broker-control.test.ts`, per task 2's own action text ("leave the server's own error-code semantics to the server's test file") -- this new test covers only the CLIENT's own job of surfacing that code and message. |
| 10 | `pollGrant(): treats a truncated, half-written grant file as not-yet-there, not a throw` | **RE-OBSERVED** | Replacement: `session: a malformed response line settles the pending request as a protocol failure, and no unhandled rejection occurs` -- the same "tolerate a malformed/incomplete read without throwing" concern, translated from a half-written JSON file to a malformed newline-delimited response line. |
| 11 | `pollGrant(): resolves granted:false with a reason after its deadline, never hanging` | **RE-OBSERVED** | Replacement: `acquire: resolves a typed deadline failure within its own bound and does not hang, using a short bound injected for the test` -- a direct match, same "resolve promptly after the deadline elapses, never hang" property, now via a real per-request timer instead of a poll loop's own deadline check. |
| 12 | `readBrokerLiveness(): classifies an absent broker.json as never_started` | **SURVIVES** | `readBrokerLiveness()` is an explicitly-named surviving export (`01.6.2-01-PLAN.md`'s artifact list: "readBrokerLiveness survives"). Test itself untouched; the function's own implementation was refactored (see `classifyLivenessFromRecord()` decision above) but its observable behavior for this exact input is unchanged. |
| 13 | `readBrokerLiveness(): classifies a fresh heartbeat as alive` | **SURVIVES** | Same as row 12. |
| 14 | `readBrokerLiveness(): classifies a stale heartbeat as stale` | **SURVIVES** | Same as row 12. |

**Reconciliation:** 14 rows total, matching the file's prior test count exactly (`grep -c '^test('` against the file as it stood before task 1). 3 SURVIVES, 4 RE-OBSERVED with a distinct replacement each, 7 DELETED each with its own reason. Every DELETED row's subject remains present and passing in the file (nothing is deleted yet -- that is plan 07's job, executing these rows verbatim, in the same commit as each named subject's own deletion).

**New test count:** `vice-broker-client.test.ts` now has **29 tests** (was 14): the 14 rows above (all SURVIVING/kept-until-plan-07 tests still present and passing) plus **15 new tests** covering the new client (14 added in task 1, 1 added in task 2 to close row 9's disposition). Running suite total: **417 tests, 412 pass, 0 fail, 5 pre-existing todo** (baseline after plan 05 was 402/397/0/5 -- delta of +15 tests, all passing, matching the 15 new tests added here exactly).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Four new raw-socket tests left open sockets/servers un-destroyed, hanging the test process after all assertions passed**
- **Found during:** Task 1, first full run of the new test file
- **Issue:** `startRawSocketServer()`-based tests (the deadline, broker-gone, chunk-framing x2, and malformed-line tests) called `server.close()` in their `finally` blocks, but `server.close()` only stops accepting NEW connections -- it does not force-close already-open sockets. Every one of these tests left at least one client-side or server-side socket open, keeping Node's event loop non-empty. All 28 individual tests reported `✔` (passing) immediately, but the `node --test` PROCESS itself never printed its final summary or exited, appearing as a hang under a piped/backgrounded invocation.
- **Fix:** Added `await opened.session.release()` (destroys the client-side socket) and `for (const s of sockets) s.destroy()` (destroys any leftover server-side accepted sockets) to each affected test's cleanup path.
- **Files modified:** `.claude/mcp/vice/vice-broker-client.test.ts`
- **Verification:** Re-ran with a plain redirect (not piped through `tail`, which was itself masking the hang by buffering until EOF) -- process now exits 0 with the full summary printed; confirmed via `ps` that no test-related process remained running afterward.
- **Committed in:** `cc595d9` (Task 1 commit)

**2. [Rule 2 - Missing Critical] The client's own error-code surfacing (an `acquire` resolving a typed failure carrying the broker's `error` response's code) had no test in task 1's first pass**
- **Found during:** Task 2, while building the disposition table and needing a genuine RE-OBSERVED replacement for row 9 (the retiring `pollGrant()` denial-reason test)
- **Issue:** Task 1's own `<behavior>` list explicitly requires "Acquire resolves with a typed failure carrying the broker's error code when the broker answers an error," but no test in the first pass exercised the `sendAndAwaitLine()` branch that maps a `{ kind: "error", code, message }` wire response into `{ ok: false, kind: code, message }`. This is a real, distinct client-side responsibility (translating the wire's error shape), not duplicate coverage of the server's own error-code semantics (which `broker-control.test.ts` already covers, per this task's own action text).
- **Fix:** Added `acquire: resolves a typed failure carrying the broker's own error code and message when the broker answers an error`, driving a real `startControlListener` whose stubbed `onAcquire` returns `{ ok: false, reason: "at_capacity" }` and asserting the client surfaces `{ ok: false, kind: "at_capacity", message: /at_capacity/ }`.
- **Files modified:** `.claude/mcp/vice/vice-broker-client.test.ts`
- **Verification:** New test passes; full suite re-run green (417/412/0/5)
- **Committed in:** `d19c856` (Task 2 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical coverage).
**Impact on plan:** Neither expands scope beyond what the two tasks already specified -- both were necessary to make the plan's own stated verification commands (a clean, exiting `node --test` run; a complete disposition table with no silently-missing behavior) actually true rather than apparently true.

## Issues Encountered

- **A one-off, non-reproducing 5-test failure occurred in one full-suite run**, entirely in real-subprocess broker tests this plan does not touch (`vice-broker-launch.test.ts`'s bind-conflict and resources-sync tests, `broker-control.test.ts`'s liveness round trip, `broker-e2e.test.ts`'s end-to-end acquire/banner tests). An immediate re-run of the exact same full suite reproduced 0 failures (417/412/0/5). This matches plan 01's own SUMMARY, which recorded an identical class of one-off async-broker-test flakiness ("across roughly ten total full-suite runs, and did not reproduce"). No code change resulted; recorded here as an observed anomaly consistent with prior precedent, not a regression this plan introduced -- none of the failing tests' own files (`vice-broker.mts`, `broker-control.mts`, `broker-kill.mts`, `broker-epoch.mts`) were touched by either of this plan's two commits.
- **A live experiment was needed to choose the "reads broker.json exactly once" test technique.** `node:test`'s `mock.method()` was tried against a separately-imported `node:fs`/`node:net` binding to count `readFileSync`/`connect` calls; a minimal reproduction (in the scratchpad, not committed) confirmed the mock does NOT affect `vice-broker-client.ts`'s own already-bound named imports of those functions -- monkey-patching a module's exports object after another module has already destructured a named import from it does not retroactively redirect that other module's calls. This ruled out call-count instrumentation as a viable technique in this module system; the delete-then-verify and unroutable-address-plus-elapsed-time techniques (recorded in `key-decisions`) were used instead.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The container-side TCP control client is complete and additive: `openBrokerControl()`/`BrokerControlSession` sit alongside the untouched file protocol and the untouched `acquireOverControlPlane()` tracer, exactly as this plan's own scope required.
- Criterion A's second half is fully discharged: plan 07 has an unambiguous, row-by-row instruction set for exactly which tests to delete (7 DELETED rows) and which stay (3 SURVIVES + 4 RE-OBSERVED replacement tests, all already passing against the new transport), reconciled against the file's exact prior test count.
- **One claim remains an explicit backstop, per this plan's own must_haves:** a session-lifetime connection surviving a real agent session's idle periods, since there is no longer any idle timeout to trip. No live-session verification exists until Phase 01.4 lands, consistent with every prior plan in this milestone.
- Full suite: **417 tests, 412 pass, 0 fail, 5 pre-existing todo** (baseline after plan 05 was 402/397/0/5 -- this plan added 15 tests, all passing).

## Self-Check: PASSED

- `.claude/mcp/vice/vice-broker-client.ts` - FOUND, contains `openBrokerControl`, `BrokerControlSession`, `ControlFailureKind`, `ACQUIRE_TIMEOUT_MS`/`RECYCLE_TIMEOUT_MS`/`CONTROL_CONNECT_TIMEOUT_MS`, region markers present
- `.claude/mcp/vice/vice-broker-client.test.ts` - FOUND, 29 tests
- `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-06-SUMMARY.md` - FOUND (this file)
- Commit `cc595d9` (Task 1) - FOUND in `git log --oneline --all`
- Commit `d19c856` (Task 2) - FOUND in `git log --oneline --all`
- `node --test .claude/mcp/vice/vice-broker-client.test.ts` - 29/29 pass, process exits 0
- `npm --prefix .claude/mcp/vice run typecheck` - exits 0
- `node --test '.claude/mcp/vice/'*.test.*` - 417 tests, 412 pass, 0 fail, 5 todo (baseline was 402/397/0/5 after plan 05; this plan added 15 tests, all passing; one non-reproducing 5-failure run recorded under Issues Encountered)

---
*Phase: 01.6.2-the-one-process-host-broker*
*Completed: 2026-08-04*
