---
phase: 01.6.2-the-one-process-host-broker
plan: 11
subsystem: infra
tags: [bash, typescript, testing, deletion, emulator-tooling, broker, cleanup]

# Dependency graph
requires:
  - phase: 01.6.2 plan 10
    provides: "the complete 61-row test disposition ledger and function disposition table this plan executes against row by row, plus the reconciled suite arithmetic (432 measured -> 371 predicted after this plan's own deletion)"
  - phase: 01.6.2 plan 09
    provides: "every host-instruction message repointed at the surviving vice-launcher.sh, so this plan's inlining and deletion do not orphan any agent-facing or operator-facing message"
  - phase: 01.6.2 plan 01
    provides: "vice-launcher.sh itself (the hand-authored survivor) and container-guard.mts (the TypeScript port this plan's deleted lib/container-guard.sh already had a replacement for)"
provides:
  - "Exactly one shell script left in the repository outside container provisioning (resources/vice-launcher.sh), proven structurally by a reduced-to-one-entry allowlist and a widened retiring-filename gate"
  - "resolve_repo_root() inlined into vice-launcher.sh, verified byte-identical to the deleted library across a 6-input differential check plus a before/after print-paths diff, before the library was removed"
  - "The retiring 61-test suite (vice-broker.test.mjs) and the two retiring bash daemons plus their two shared libraries deleted in one commit, against plan 10's complete disposition ledger"
  - "Criterion C7 (the request-id pattern exists in exactly one place) satisfied as a pure consequence of the deletion"
  - "WINDOWS.md id 2 closed (discharged by the deletion); id 3 left open for Phase 01.6.2.1"
affects: ["01.6.2.1 (broker lifecycle policy) -- inherits a repository with zero bash daemon machinery and a single surviving launcher; criterion M's defect-5 finding from plan 10 is its own pickup, unaffected by this plan"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Differential-before-deletion verification: when a piece of logic is being inlined and its source deleted in a later commit, prove the inline byte-identical against the still-present original FIRST (6+ synthetic inputs plus a real before/after print-paths diff), so the deletion commit is a pure removal with nothing left to verify."
    - "Per-file-type comment stripping for a structural grep gate: the existing RETIRING_DAEMON_FILENAMES gate stripped only `//`/`/* */` (correct for the flat TypeScript module set); widening it to also scan resources/'s one surviving `.sh` file required a second, `#`-based stripper, or the launcher's own legitimate historical comments (naming the retiring scripts it copied its shape from) would have self-tripped the gate."
    - "One commit for a set of files whose partial deletion would turn multiple gates red for a purely mechanical reason (the ignore-parity gate, the tracked-script allowlist, the resources-sync gate) -- verified by running all three gates green in the same commit that does the deletion, not asserted."

key-files:
  created: []
  modified:
    - .claude/mcp/vice/resources/vice-launcher.sh
    - .claude/mcp/vice/repo-root.test.ts
    - .claude/mcp/vice/install-resources.test.ts
    - .claude/mcp/vice/repo-root.ts
    - .claude/mcp/vice/incident-record.ts
    - .claude/mcp/vice/host-scripts.test.ts
    - .gitignore
    - .planning/WINDOWS.md
    - .planning/RE-FINDINGS.md
  deleted:
    - .claude/mcp/vice/resources/vice-broker.sh
    - .claude/mcp/vice/resources/vice-supervisor.sh
    - .claude/mcp/vice/resources/lib/container-guard.sh
    - .claude/mcp/vice/resources/lib/repo-root.sh
    - .claude/mcp/vice/vice-broker.test.mjs

key-decisions:
  - "Re-dispatch: the checkpoint:decision was already resolved (delete) by the developer in a prior, since-reaped worktree run that halted with zero commits. This execution recorded that decision (RE-FINDINGS.md, commit c132876) before proceeding, per the re-dispatch instructions, rather than re-asking."
  - "'Both structural script arrays' (deletion_scope's own phrasing) is read as SURVIVING_HOST_SCRIPTS -- the one array whose entire purpose (enumerating the two retiring daemons plus the launcher for trap-registration and syntax-check loops) vanishes once two of its three entries no longer exist. EXPECTED_TRACKED_SHELL_SCRIPTS is explicitly named separately in the same plan text as KEPT (reduced to one entry), and RETIRING_DAEMON_FILENAMES is explicitly named as the gate to WIDEN, not remove -- so 'both' most coherently refers to the two now-dead structural TESTS that array drove (trap-registration loop, syntax-check loop), not a second array declaration. Recorded here in case a reviewer reads the plan text differently."
  - "The retiring per-instance supervisor's SIGHUP signal-handling test and the trap-registration loop are DELETED, not repointed: their replacement (broker-kill.test.ts's registerShutdownHandlers()/shutdown() tests) already existed before this plan (confirmed present, passing) -- ported in plan 04, named in plan 10's ledger row 11. This plan's own job was removing the now-subjectless original, not writing a new replacement."
  - "The RETIRING_DAEMON_FILENAMES gate widened to scan resources/ needed a SEPARATE, shell-aware (`#`) comment stripper, not reuse of the existing `//`/`/* */` stripper -- vice-launcher.sh's own header comment legitimately names both retiring scripts (explaining what it copied its shape from), and the JS-style stripper would have left those `#`-prefixed lines intact, self-tripping the gate the moment it was widened. Verified live: the gate passes with the shell stripper in place."
  - "A stale, shared main-repo tools/ deployment (outside this worktree, since CONTAINER_WORKSPACE_PATH resolves to /workspaces/bruce_lee regardless of which worktree is running) was force-resynced via installResources({force:true}) after this plan's own test runs pruned some of its now-retired entries but left its diverged vice-launcher.sh untouched -- see Deviations."

requirements-completed: ["C2", "C6", "C7", "D-01", "D-24", "PD-04"]

coverage:
  - id: D1
    description: "resolve_repo_root() inlined into vice-launcher.sh, all four precedence branches and both one-time warning latches reproduced exactly, verified against the still-present library before its deletion"
    requirement: "PD-04"
    verification:
      - kind: other
        ref: "6-input differential check (env-contains, git-walk at two depths, resources-shaped and other-shaped last-resort, env-set-but-not-containing) comparing the inlined function against lib/repo-root.sh -- all 6 match byte-for-byte, recorded as a table in this SUMMARY's Verification Details section"
        status: pass
      - kind: other
        ref: "print-paths output byte-identical before/after the inline, both with ambient CONTAINER_WORKSPACE_PATH and with it explicitly set to the worktree root"
        status: pass
      - kind: other
        ref: "both one-time warning latches (env-outside, no-marker) confirmed to fire exactly once across three repeated calls in the same process"
        status: pass
    human_judgment: false
  - id: D2
    description: "repo-root.test.ts's two path-agreement parity tests repointed off the deleted vice-supervisor.sh/vice-broker.sh onto the surviving launcher"
    requirement: "PD-04"
    verification:
      - kind: unit
        ref: "repo-root.test.ts#\"path agreement (D-3, D-6...): the launcher's own repo_root (resources/ and tools/ copies) agrees with Node's supervisorDir()/dirname(EPOCH_FILE), and the agreed path is not under .claude\""
        status: pass
      - kind: unit
        ref: "repo-root.test.ts#\"path agreement without CONTAINER_WORKSPACE_PATH (D-6): the .git-walk branch...\""
        status: pass
    human_judgment: false
  - id: D3
    description: "The four bash files (the retiring broker, the retiring per-instance supervisor, and their two shared libraries) and the retiring 61-test suite deleted in one commit, against plan 10's complete disposition ledger"
    requirement: "C2"
    verification:
      - kind: other
        ref: "git ls-files -- '*.sh' | grep -v '^.devcontainer/' | wc -l -> 1 (resources/vice-launcher.sh); test -e resources/lib -> gone; test -e vice-broker.test.mjs -> gone"
        status: pass
      - kind: unit
        ref: "host-scripts.test.ts#\"structural: git ls-files enumerates the tracked shell-script set as exactly EXPECTED_TRACKED_SHELL_SCRIPTS...\" (allowlist reduced to one entry)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Criterion C7: the request-id pattern exists in exactly one place in the repository, as a pure consequence of the deletion"
    requirement: "C7"
    verification:
      - kind: other
        ref: "grep -rn 'req-\\[0-9\\]' --include='*.ts' --include='*.mts' --include='*.sh' . | grep -v node_modules | wc -l -> 1 (vice-broker-client.ts's REQUEST_ID_PATTERN, the container-side export)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The ignore-parity gate and the widened retiring-filename structural gate are green in the same commit that does the deletion"
    requirement: "C6"
    verification:
      - kind: unit
        ref: "host-scripts.test.ts#\"\\`.gitignore\\` and install-resources.ts's deployed set...are in two-way parity\" (9 /tools/ lines = 8 resource entries + 1 manifest, matching resourceEntries() exactly)"
        status: pass
      - kind: unit
        ref: "host-scripts.test.ts#\"structural: neither retiring daemon filename...appears anywhere in the module's non-test TypeScript source or in resources/'s surviving deployed files\" (widened to scan resources/, shell-comment-aware)"
        status: pass
    human_judgment: false
  - id: D6
    description: "Full suite green with a reconciled test count, resources-sync gate green, typecheck exits 0"
    verification:
      - kind: unit
        ref: "node --test '.claude/mcp/vice/'*.test.* -- 368 tests, 363 pass, 0 fail, 0 skipped, 5 pre-existing todo (reconciled against plan 10's 371 prediction: -3 explained by this plan's own -1 install-resources.test.ts deletion and -2 net host-scripts.test.ts change, neither anticipated by plan 10's simpler -61-only arithmetic)"
        status: pass
      - kind: other
        ref: "node .claude/mcp/vice/build.ts (0 tracked-byte diff) and npm --prefix .claude/mcp/vice run typecheck (exits 0)"
        status: pass
    human_judgment: false
  - id: D7
    description: "WINDOWS.md id 2 closed; id 3 correctly left open (owned by Phase 01.6.2.1, not this plan)"
    verification:
      - kind: other
        ref: "WINDOWS.md id 2 status: fixed, resolved_at set, reason naming broker-e2e.test.ts's end-to-end test as the RE-OBSERVED replacement; id 3 unchanged (status: open)"
        status: pass
    human_judgment: false
  - id: D8
    description: "Two claims remain explicit backstops per this plan's own must_haves: the launcher resolving the correct root on the developer's real host (no container workspace variable, git-marker branch), and the deployment prune step clearing stale deployed copies on the next real installer run"
    verification: []
    human_judgment: true
    rationale: "No live host-session verification exists until Phase 01.4, consistent with every prior plan in this milestone. The prune step's own mechanism is exercised by install-resources.test.ts's existing pruneResources() tests (unit-verified against a synthetic root); this plan added no new prune logic per its own instruction, so the backstop is specifically about a REAL host's next installer run, not the mechanism's unit-level correctness."

# Metrics
duration: ~1h30min (estimated; no precise start timestamp captured at session start)
completed: 2026-08-04
status: complete
---

# Phase 01.6.2 Plan 11: Inline the Repo-Root Ladder, Then Delete the Four Retiring Bash Files and Their 61-Test Suite Summary

**The repository now has exactly one shell script outside container provisioning (resources/vice-launcher.sh), which resolves its own repository root with an inlined four-branch ladder verified byte-identical against the library it replaces; the retiring bash broker, per-instance supervisor, both their shared libraries, and their 2,697-line/61-test suite are gone in one commit, against plan 10's complete disposition ledger, with the suite settling at 368 tests (a fully-reconciled -3 from plan 10's simpler 371 prediction).**

## Performance

- **Duration:** ~1h30min (estimated; no precise start timestamp captured at session start)
- **Tasks:** 3 (1 `checkpoint:decision` — pre-resolved by re-dispatch, 2 `type="auto"`)
- **Files modified:** 9 modified, 5 deleted

## Accomplishments

- **Task 1's checkpoint:decision recorded, not re-asked.** This was a re-dispatch: a prior agent in a since-reaped worktree reached this exact blocking checkpoint, correctly halted with zero commits, and lost nothing (git history and all five retiring files were untouched). The developer's decision (`delete`, over `delete-scripts-keep-suite` and `defer`) was recorded in `.planning/RE-FINDINGS.md` (commit `c132876`), matching the precedent `01.6.2-01-SUMMARY.md` set for its own Task 2 wire-format checkpoint, before proceeding to Tasks 2 and 3.
- **The repo-root ladder inlined into vice-launcher.sh (Task 2), verified before the library it replaces was deleted.** All four precedence branches and both one-time warning latches reproduced exactly. Verified three ways, all before the deletion: a 6-input differential check comparing the inlined function against the still-present `lib/repo-root.sh` (table below — all 6 match byte-for-byte); the launcher's `--print-paths` output captured byte-identical before and after the inline in two scenarios (ambient `CONTAINER_WORKSPACE_PATH`, and explicitly set to the worktree root); and both warning latches confirmed to fire exactly once across three repeated calls in the same process.
- **Every test that used a retiring script as its fixture, repointed (Task 2).** `repo-root.test.ts`'s two path-agreement parity tests moved off `vice-supervisor.sh`/`vice-broker.sh`'s own `supervisor_dir`/`pool_dir` fields onto a comparison against the surviving launcher's own printed `repo_root` (`<launcher repo_root>/.vice-supervisor` vs. Node's `supervisorDir()`/`dirname(EPOCH_FILE)`) — same property, new anchor. `install-resources.test.ts`'s eight fixture-entry references to `vice-supervisor.sh` repointed onto `vice-broker.mjs` (a surviving compiled artifact) or `vice-launcher.sh` (the surviving executable entry); one test (`lib/ deployed`) deleted outright since its entire subject (`resources/lib/` existing) retires wholesale in Task 3 with no successor to name. `repo-root.ts` and `incident-record.ts`'s two comment-only references to a retiring script's filename rewritten to name where that logic (or its pattern) lives now.
- **THE DELETION (Task 3), one commit.** `resources/vice-broker.sh` (2,103 lines), `resources/vice-supervisor.sh` (443 lines), `resources/lib/container-guard.sh` (184 lines), `resources/lib/repo-root.sh` (103 lines, and the then-empty `resources/lib/` directory with it), and `vice-broker.test.mjs` (2,697 lines, 61 tests) all deleted together — the two shared library files were sourced by both daemons and the ignore-parity gate is driven by the resource-entry walk, so a partial deletion would have turned three gates red for a purely mechanical reason.
- **`host-scripts.test.ts` shrunk to match.** The `SURVIVING_HOST_SCRIPTS`-driven signal-handling test, trap-registration loop, and syntax-check loop are gone (their subjects retired or their assertions became redundant); the launcher's exec-into-node invariant survives as its own standalone test, since that exec is what makes signal delivery pass straight through with no bash trap of its own needed. `EXPECTED_TRACKED_SHELL_SCRIPTS` reduced to its single surviving entry. The `RETIRING_DAEMON_FILENAMES` structural gate (plan 09) widened to also scan `resources/`'s surviving files, now that the "still holds the retiring scripts' own bytes" exclusion reason is gone — with a new shell-aware (`#`) comment stripper, since the JS-style one would have let `vice-launcher.sh`'s own legitimate historical comments (naming what it copied its shape from) self-trip the gate.
- **Two criteria confirmed as pure consequences, not separate work.** C6: `git ls-files -- '*.sh'` outside `.devcontainer/` returns exactly one path. C7: the request-id pattern (`req-[0-9]+-...`) now exists in exactly one place in the repository (`vice-broker-client.ts`'s `REQUEST_ID_PATTERN`, the container-side export).
- **`.gitignore` updated in the same commit as the deletion.** The four deployed-path lines for the deleted scripts removed; two-way parity confirmed (8 resource entries + 1 manifest = 9 `/tools/` lines, matching `resourceEntries()` exactly).
- **`WINDOWS.md` id 2 closed.** The one skipped tracer test in `vice-broker.test.mjs` is RE-OBSERVED by `broker-e2e.test.ts`'s own end-to-end test, and the file itself is now gone. Id 3 (the `handleAcquire()`/warm-pool gap) is explicitly NOT this plan's — left open for Phase 01.6.2.1's criterion M.

## Task Commits

1. **Task 1: Confirm the deletion of the four retiring bash files and the retiring test suite (checkpoint:decision, pre-resolved by re-dispatch)** - `c132876` (docs, RE-FINDINGS.md)
2. **Task 2: Inline the repository-root ladder into the surviving launcher and repoint every test that used a retiring script as its fixture (PD-04)** - `422141b` (feat)
3. **Task 3: THE DELETION — four bash files, the retiring test suite, the structural arrays and the tests whose subjects went with them, in one commit** - `b161d61` (feat)
4. **WINDOWS.md id 2 closure (byproduct of Task 3, not itself a task)** - `f54bef1` (docs)

**Plan metadata:** pending (this SUMMARY's own commit)

## Files Created/Modified

- `.claude/mcp/vice/resources/vice-launcher.sh` — `resolve_repo_root()` inlined (all four precedence branches, both one-time warning latches), the `source "$SELF_DIR/lib/repo-root.sh"` line removed
- `.claude/mcp/vice/repo-root.test.ts` — both path-agreement parity tests repointed off the deleted daemons onto the surviving launcher's own printed `repo_root`
- `.claude/mcp/vice/install-resources.test.ts` — eight `vice-supervisor.sh` fixture references repointed onto `vice-broker.mjs`/`vice-launcher.sh`; one test (`lib/ deployed`) deleted (subject retires wholesale)
- `.claude/mcp/vice/repo-root.ts` — comment-only reference to the retiring shell scripts rewritten to name the surviving launcher
- `.claude/mcp/vice/incident-record.ts` — comment-only reference to `write_json_atomic()` (`resources/vice-broker.sh`) rewritten to name its TypeScript successors
- `.claude/mcp/vice/host-scripts.test.ts` — SIGHUP signal-handling test, trap-registration loop, and syntax-check loop removed; standalone exec-into-node test added; `EXPECTED_TRACKED_SHELL_SCRIPTS` reduced to one entry; `RETIRING_DAEMON_FILENAMES` gate widened to scan `resources/`, with a new shell-comment stripper
- `.gitignore` — four deployed-path lines for the deleted scripts removed
- `.planning/WINDOWS.md` — id 2 marked `fixed`
- `.planning/RE-FINDINGS.md` — Task 1's checkpoint:decision resolution recorded
- `.claude/mcp/vice/resources/vice-broker.sh` — **DELETED** (2,103 lines)
- `.claude/mcp/vice/resources/vice-supervisor.sh` — **DELETED** (443 lines)
- `.claude/mcp/vice/resources/lib/container-guard.sh` — **DELETED** (184 lines)
- `.claude/mcp/vice/resources/lib/repo-root.sh` — **DELETED** (103 lines), and the then-empty `resources/lib/` directory with it
- `.claude/mcp/vice/vice-broker.test.mjs` — **DELETED** (2,697 lines, 61 tests)

## Verification Details

### 6-input differential check (Task 2, before the library's deletion)

Comparing `lib/repo-root.sh`'s `resolve_repo_root()` against the function now inlined into `vice-launcher.sh`, each run in its own subshell:

| # | Case | lib/repo-root.sh | inlined (vice-launcher.sh) | Match |
|---|------|-------------------|----------------------------|-------|
| 1 | inside workspace, `CONTAINER_WORKSPACE_PATH` set and containing | `<tmp>/repo-root-diff` | `<tmp>/repo-root-diff` | YES |
| 2 | inside a git repo (immediate `.git` dir), env unset | `<tmp>/repo-root-diff/case2_git_walk` | `<tmp>/repo-root-diff/case2_git_walk` | YES |
| 3 | neither `.git` nor env — last resort, `resources`-named (4 up) | `<tmp>/repo-root-diff/case3_resources_lastresort` | `<tmp>/repo-root-diff/case3_resources_lastresort` | YES |
| 4 | neither `.git` nor env — last resort, other-named (1 up) | `<tmp>/repo-root-diff/case4_other_lastresort/a` | `<tmp>/repo-root-diff/case4_other_lastresort/a` | YES |
| 5 | env set but NOT containing `from`, no `.git` ancestor either | `/some/unrelated/path` | `/some/unrelated/path` | YES |
| 6 | inside a DEEPER git repo (3 levels below `.git`) | `<tmp>/repo-root-diff/case6_git_walk_deep` | `<tmp>/repo-root-diff/case6_git_walk_deep` | YES |

All 6 cases match byte-for-byte. Both one-time warning latches (branch 3's "env set but not containing", branch 4's "no marker found") were separately confirmed to fire exactly once across three repeated calls to the inlined function in the same process.

### Suite arithmetic reconciliation

Plan 10 predicted **371** (432 measured after plan 10's own additions, minus `vice-broker.test.mjs`'s straight 61). This plan's own test-file edits (not anticipated by that simpler arithmetic) account for the remaining difference:

- 432 (plan 10 baseline)
- − 61 (`vice-broker.test.mjs`, Task 3)
- − 1 (`install-resources.test.ts`'s `lib/ deployed` test, Task 2 — no successor, subject retires wholesale)
- − 2 (`host-scripts.test.ts` net, Task 3 — 3 removed: SIGHUP test, trap-registration loop, syntax-check loop; 1 added: exec-into-node standalone test)
- = **368**, measured exactly.

Full suite: **368 tests, 363 pass, 0 fail, 0 skipped, 5 pre-existing todo.** The "1 skipped" from plan 10's baseline is gone because the skipped test (`vice-broker.test.mjs`'s tracer, WINDOWS.md id 2) was deleted along with its file.

## Decisions Made

See `key-decisions` in this SUMMARY's own frontmatter for full rationale on: the re-dispatch decision-recording (not re-asking), the "both structural script arrays" interpretation, the SIGHUP test's deletion-not-repointing (its replacement already existed pre-plan), the widened daemon-filename gate needing a separate shell-comment stripper, and the stale shared-deployment resync (see Deviations below).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] My own Task 2 edit's new comment accidentally contained the literal string "container-guard.sh", tripping `vice-broker-launch.test.ts`'s own pre-existing structural test**
- **Found during:** Task 2, first verify run of the four-file test set
- **Issue:** `vice-broker-launch.test.ts`'s `"the launcher no longer sources or references the bash container-guard module"` test does a blunt substring check for `container-guard.sh` anywhere in `vice-launcher.sh`'s text (no comment-stripping). My own new header comment, explaining what the same commit deletes, named `resources/lib/container-guard.sh` literally, self-tripping that pre-existing gate.
- **Fix:** Reworded the comment to describe the retiring container guard module without using its literal filename.
- **Files modified:** `.claude/mcp/vice/resources/vice-launcher.sh`
- **Verification:** `node --test vice-broker-launch.test.ts` — 45/45 pass (was 44/45 before the fix)
- **Committed in:** `422141b` (Task 2 commit — caught before that commit landed)

**2. [Rule 1 - Bug] My own Task 2 edit's comment also accidentally contained the literal string "lib/repo-root.sh", violating this plan's own stated acceptance criterion**
- **Found during:** Task 2, verifying the acceptance criterion `grep -c 'lib/repo-root.sh' resources/vice-launcher.sh` prints `0`
- **Issue:** Two of my own explanatory comments named the deleted library file by its literal path (`lib/repo-root.sh`) to explain provenance, which is exactly the string the plan's own acceptance criterion requires to be absent.
- **Fix:** Reworded both comments to describe the former library without repeating its literal path.
- **Files modified:** `.claude/mcp/vice/resources/vice-launcher.sh`
- **Verification:** `grep -c 'lib/repo-root.sh' resources/vice-launcher.sh` now prints `0`; re-ran the print-paths byte-identical check and the full four-file verify set — still green
- **Committed in:** `422141b` (Task 2 commit — caught before that commit landed)

**3. [Rule 1 - Bug] A JSDoc comment in my own Task 3 edit contained a literal `*/`, prematurely closing the block comment and producing a TypeScript syntax error**
- **Found during:** Task 3, first run of `host-scripts.test.ts`
- **Issue:** The new `stripShellCommentsForDaemonGate()` doc comment described the existing stripper as handling `` `//`/`/* */` `` syntax — the `*/` inside that inline description closed the JSDoc block early, corrupting everything after it as top-level code and producing `SyntaxError [ERR_INVALID_TYPESCRIPT_SYNTAX]`.
- **Fix:** Reworded the description to avoid an embedded `*/` sequence ("double-slash or slash-star style" instead of the literal tokens).
- **Files modified:** `.claude/mcp/vice/host-scripts.test.ts`
- **Verification:** `node --test host-scripts.test.ts` — 4/4 pass (was a parse failure before the fix)
- **Committed in:** `b161d61` (Task 3 commit — caught before that commit landed)

**4. [Rule 3 - Blocking] A stale, shared main-repo `tools/` deployment blocked `repo-root.test.ts`'s own path-agreement tests**
- **Found during:** Task 3, first full-suite run after the deletion
- **Issue:** `repo-root.test.ts`'s path-agreement tests call `installResources({ root: repoRoot() })`, and `repoRoot()`'s own documented precedence resolves `CONTAINER_WORKSPACE_PATH` (`/workspaces/bruce_lee`, the MAIN checkout) first — unconditionally, regardless of which worktree is actually running (`host-scripts.test.ts`'s own header comment names exactly this hazard, which is why that file deliberately uses a plain `.git`-walk instead). Earlier test runs in this same session had already pruned `tools/lib/container-guard.sh`/`tools/lib/repo-root.sh` from the shared `/workspaces/bruce_lee/tools/` directory (since this worktree's `resourceEntries()` no longer lists them), but left the stale, pre-Task-2 `tools/vice-launcher.sh` untouched — `installResources()` never overwrites a "diverged" entry without `force: true`. The result: a deployed launcher that still tried to `source` a now-missing library file, failing both path-agreement tests with `No such file or directory`.
- **Fix:** Ran `installResources({ root: "/workspaces/bruce_lee", force: true })` once to force-resync the shared deployment to this worktree's current `resources/` content, and removed the now-empty stale `tools/lib/` directory (`pruneResources()` unlinks files but does not `rmdir` an emptied directory).
- **Files modified:** None tracked — `tools/` is gitignored, disposable deployment output by design ("a real host setup keeps working exactly as before because they still land at these same paths... regenerated automatically"); no source change was needed or made.
- **Verification:** `bash /workspaces/bruce_lee/tools/vice-launcher.sh --print-paths` succeeds; `node --test repo-root.test.ts` — both path-agreement tests pass; full suite re-run clean afterward.
- **Committed in:** N/A (no trackable change — a runtime deployment artifact, not source)

---

**Total deviations:** 4 auto-fixed (3 Rule 1 — bugs introduced by this plan's own edits, all caught and fixed before their task commit landed; 1 Rule 3 — a blocking environment-state issue from running tests in a worktree against a shared, gitignored deployment directory, not caused by any source change).
**Impact on plan:** All four were necessary corrections to reach the plan's own stated verification commands' green state. None expanded scope beyond what the plan's three tasks already specified.

## Issues Encountered

- The `resources-sync.test.ts` flake documented in plans 09/10's own SUMMARYs (a fresh `npm ci`/`node build.ts` needed in this worktree before any test count could be trusted) recurred here, matching precedent exactly. `npm ci` installed the pinned dependencies (gitignored, nothing tracked); `node build.ts` reproduced `resources/`'s surviving `.mjs` artifacts byte-identical (confirmed via `git status --short` showing only the already-staged deletions, zero new diff).
- No other issues beyond the four deviations above.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 01.6.2.1 (Broker Lifecycle Policy) inherits a repository with zero bash daemon machinery: one shell script (`resources/vice-launcher.sh`), a TypeScript broker, and no shared bash libraries left to drift out of sync with it.
- WINDOWS.md id 3 (the `handleAcquire()`/warm-pool gap, filed as Defect 5 in the existing spare-warming-and-stale-grant todo) remains open and is explicitly Phase 01.6.2.1's criterion M to pick up — this plan did not touch it.
- **Two claims remain explicit backstops**, per this plan's own `must_haves`: the launcher resolving the correct repository root on the developer's real host (no container workspace variable set, the git-marker branch is the one that runs), and the deployment prune step clearing the four deleted scripts' stale deployed copies on the next real installer run. No live host-session verification exists until Phase 01.4, consistent with every prior plan in this milestone.
- Full suite: **368 tests, 363 pass, 0 fail, 0 skipped, 5 pre-existing todo.** `npm run typecheck` exits 0. `node build.ts` reproduces `resources/` byte-identical.

## Known Stubs

None. This plan deletes and repoints tests; it introduces no new UI-facing or data-facing code paths.

## Threat Flags

None. This plan touches no new network endpoint, auth path, file-access pattern, or schema at a trust boundary — it deletes retired surface and repoints existing tests at what already replaced it.

## Self-Check: PASSED

- `.claude/mcp/vice/resources/vice-launcher.sh` — FOUND, contains `resolve_repo_root()` (3 occurrences), does not contain `lib/repo-root.sh` or `container-guard.sh` (0 occurrences each)
- `.claude/mcp/vice/resources/vice-broker.sh` — MISSING (confirmed deleted)
- `.claude/mcp/vice/resources/vice-supervisor.sh` — MISSING (confirmed deleted)
- `.claude/mcp/vice/resources/lib` — MISSING (confirmed deleted, directory gone entirely)
- `.claude/mcp/vice/vice-broker.test.mjs` — MISSING (confirmed deleted)
- `.planning/WINDOWS.md` — FOUND, id 2 status `fixed`, id 3 status `open`
- `.planning/RE-FINDINGS.md` — FOUND, contains the Task 1 checkpoint:decision entry
- Commit `c132876` (Task 1) — FOUND in `git log --oneline --all`
- Commit `422141b` (Task 2) — FOUND in `git log --oneline --all`
- Commit `b161d61` (Task 3) — FOUND in `git log --oneline --all`
- Commit `f54bef1` (WINDOWS.md closure) — FOUND in `git log --oneline --all`
- `git ls-files -- '*.sh' | grep -v '^.devcontainer/' | wc -l` — `1`
- `grep -rn 'req-\[0-9\]' --include='*.ts' --include='*.mts' --include='*.sh' . | grep -v node_modules | wc -l` — `1`
- `npm --prefix .claude/mcp/vice run typecheck` — exits 0
- `node --test '.claude/mcp/vice/'*.test.*` — 368 tests, 363 pass, 0 fail, 0 skipped, 5 todo

---
*Phase: 01.6.2-the-one-process-host-broker*
*Completed: 2026-08-04*
