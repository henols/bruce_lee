---
phase: 01.6.2-the-one-process-host-broker
verified: 2026-08-04T10:30:00Z
status: passed
score: 20/20 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 16/20
  gaps_closed:
    - "The 61-test disposition ledger in 01.6.2-VALIDATION.md honestly represents what the real (non-test) broker does (criterion A) — row 51 now cites the wired end-to-end recycle test, verbatim, with a dated correction note"
    - "Per-instance supervision (crash-respawn) is a real, wired property of the running one-process broker — withCrashSupervision() is composed into both real launch paths in vice-broker.mts, held by a structural anti-regression gate"
    - "vice_recycle respawns the instance it kills, on the same port, matching its own tool-facing description — handleExit()'s recycle branch relaunches through launchSupervised() directly, with no crash accounting/backoff, and restores granted state"
    - "A client disconnecting while its acquire is still queued does not cause a real, silently-leaked emulator launch — attemptAcquire() now pre-checks socket.destroyed before onAcquire() and releases a late-arriving grant"
  gaps_remaining: []
  regressions: []
human_verification: []
---

# Phase 01.6.2: The One-Process Host Broker Verification Report

**Phase Goal:** Replace the bash host daemon with a one-process TypeScript broker holding all
coordination state in process, replace the file-messaging protocol with a TCP control plane, and
delete the retiring bash implementation. Seal boundary: "the new machine exists and the file
protocol is dead" — lifecycle-policy correction (criteria E, L, M) is explicitly out of scope,
owned by Phase 01.6.2.1.

**Verified:** 2026-08-04T10:30:00Z
**Status:** passed
**Re-verification:** Yes — after gap closure (plans 12–15)

## Scope Note

Same boundary as the initial verification: Criteria **E, L, M** and CONTEXT decisions **D-02,
D-05…D-08, D-10, D-11, D-19…D-22** belong to Phase 01.6.2.1 (2026-08-03 split). None are scored
here. Commit `fac2c6e` corrected plan 15's own frontmatter to stop over-claiming criterion E and
D-19/D-21 (moved to `governed_by:`, confirmed by direct `git show --stat`) — matches plan 10's own
precedent for the same artifact/criterion-A distinction.

Requirement IDs: none declared for this phase (v1.1, 0-requirement tooling milestone) — confirmed
again against `.planning/REQUIREMENTS.md` (no `01.6.2`/`CR-`/`WR-` hits) and every plan's
`requirements:` frontmatter, which carries criteria/decision/defect IDs instead. Not a gap.

## Re-Verification Method

This is a re-verification of the four truths `01.6.2-VERIFICATION.md` (2026-08-04T06:30:00Z) scored
FAILED, plus a regression check of the 16 it scored VERIFIED. Per the critical-context instruction,
every claim below was re-derived against the assembled system, not accepted from any SUMMARY:

- Every wiring claim was confirmed by `grep`/direct read of the real entry point
  (`vice-broker.mts`), not by trusting a unit test that injects a stub.
- Every cited test was located by exact name in the file it is cited from, and **run directly by me**
  (not merely re-stated as "reported green" by an executor) — all six e2e tests plus both structural
  gates pass in isolation.
- The full suite was re-run at HEAD: `node --test '.claude/mcp/vice/'*.test.*` → 390 tests, 385 pass,
  0 fail, 5 todo — matching the orchestrator's own measured_state exactly.
- `node build.ts` + `git status --porcelain resources/` → clean (generated tier byte-identical to a
  fresh build). `npm run typecheck` → exit 0.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | C1 — the host's actual `node --version` is recorded | VERIFIED | Unchanged since initial verification; `process.version` → `BrokerRecord.node_version` (`vice-broker.mts`) |
| 2 | C2 — all three host-side shell scripts retire into one application | VERIFIED | Unchanged; confirmed no plan 12–15 commit touches a `.sh` file (`git log --name-only` for those commits shows only `.mts`/`.ts`/`.md` files) |
| 3 | C4 — coordination state lives in one place, in process | VERIFIED | Unchanged; `InstanceRecord` gained fields (`respawnAfterKill`, honest `epoch`) but the single in-memory `BrokerState` map shape is intact |
| 4 | C5 — catchable shutdown kills every child, identity-verified | VERIFIED | Unchanged; confirmed `broker-kill.mts`'s shutdown path never references `respawnAfterKill` (`grep -v '^\s*//' broker-kill.mts \| grep -c 'respawnAfterKill'` → `0`), so plan 13's marker split cannot make a shutdown respawn what it kills |
| 5 | C6 — exactly one shell script survives | VERIFIED | Unchanged; only `resources/vice-launcher.sh` |
| 6 | C7 — the bash half of the request-id regex is carried | VERIFIED | Unchanged; not touched by plans 12–15 |
| 7 | **A** — the 61-test disposition ledger honestly represents the real broker | **VERIFIED** | Row 51 now cites `broker-control.test.ts#"recycle: a grant this connection holds resolves, kills identity-verified, and answers an ack carrying the stage word and outcome"` + `broker-e2e.test.ts#"wired recycle: a recycle over the real control plane kills the granted child and the real broker brings a new one back on the SAME port with the epoch advanced"` — confirmed present verbatim at `broker-e2e.test.ts:445` by direct `grep`, and confirmed **passing** by direct test run. A dated correction note beneath the ledger states the prior citation, why it was false, the new citation, and which plans made it true. Tally recounted, unchanged (61 rows, 4/31/26). No other row's disposition touched (`git diff` on the file at commit `fd9b0fe` confined to row 51 + note + tally line) |
| 8 | B (D-04) — the per-instance epoch file survives, contract unchanged, new writer | VERIFIED | Unchanged, plus honest: `writeEpochForLaunch()` now also sets `record.epoch` (plan 12), so `handleStatus()`/`handleRecycleForRealBroker()` report a real integer instead of absent |
| 9 | C — the single `in_flight` owner survives the move to an event loop | VERIFIED | Unchanged; concurrency race test in `broker-launch.test.ts` still passes, confirmed in the full suite re-run |
| 10 | D — identity-verified kill reused with corrected identity | VERIFIED | Unchanged; `verifiedKill()` untouched by plans 12–15 |
| 11 | F — the lease is the connection; six retiring mechanisms gone | VERIFIED | Unchanged |
| 12 | G — bootstrap stays a file; port bands separated | VERIFIED | Unchanged |
| 13 | H — the emulator data plane is unchanged | VERIFIED | Unchanged |
| 14 | I — broker restart reaps every instance unconditionally | VERIFIED | Unchanged; `reapOrphanedInstances()` call site untouched by plans 12–15 |
| 15 | J — a lost machine costs one acquisition, told plainly | VERIFIED | Unchanged in effect; plan 14 changed the *cost profile* of the same-session branch (see Anti-Patterns below) but the caller-facing contract — one replacement acquisition, reported plainly — still holds, confirmed by `vice-proxy.test.ts`'s D-13/D-14 test group (updated, still passing) |
| 16 | K — two brokers cannot run at once | VERIFIED | Unchanged; `EADDRINUSE` handling untouched by plans 12–15 |
| 17 | N — control listener reachable from container, token-gated | VERIFIED | Unchanged; confirmed no new control-plane message kind was added (`grep -c 'ControlRequestKind' broker-control.mts` still finds exactly the one type declaration; plan 14's own dedicated unit test asserts the union still has its original five members) |
| 18 | **Per-instance supervision (crash-respawn) is a real, wired property of the running broker** | **VERIFIED** | `withCrashSupervision()` (`broker-launch.mts:765`) is composed into BOTH real launch paths: `handleAcquire()`'s `spawnFactory` (`vice-broker.mts:345`) and `maintainWarmFloorForRealBroker()`'s `spawnFactory` (`vice-broker.mts:479`), confirmed by direct read. The exit-listener installation point is exactly 1 (comment-stripped `grep -c 'once("exit"'` → `1`). Both e2e proofs run directly by me and pass: `"wired supervision: a granted stub child killed..."` and `"wired supervision: a warm-floor stub child killed..."`. A structural anti-regression gate (`broker-launch.test.ts`) equating spawn-factory count to wrapper-call-site count also passes, and its own SUMMARY records its discriminating power demonstrated live (reverted composition → red, restored → green) |
| 19 | **`vice_recycle` respawns the killed instance, as its own tool description promises** | **VERIFIED** | `handleRecycleForRealBroker()`'s header comment now describes the wired behaviour (no surviving "Deliberately does NOT relaunch" text — confirmed `grep -c` → `0` for both retired disclaiming sentences). `markDeliberateDeath(instance, true)` is called before the kill (`vice-broker.mts:445`); `handleExit()`'s recycle branch (`broker-launch.mts:698-714`) relaunches directly through `launchSupervised()` with no crash accounting/backoff and restores granted state. The e2e test `"wired recycle: a recycle over the real control plane kills the granted child and the real broker brings a new one back on the SAME port with the epoch advanced"` was run directly by me and passes. The companion `"wired release..."` test (proving release does NOT respawn) also passes, confirming the marker split did not regress kill-never-recycle |
| 20 | **A client disconnecting while queued does not leak a real emulator launch** | **VERIFIED** | `attemptAcquire()` (`broker-control.mts:355-399`) now checks `socket.destroyed` BEFORE calling `opts.onAcquire()` (line 360) — confirmed by direct read — and releases a grant that settles after the socket is gone (lines 370-373) rather than dropping it. The e2e test `"wired disconnect-while-queued: a genuinely queued acquire whose client disconnects leaves exactly one instance behind, not two"` was run directly by me and passes; its own precondition-reproduction assertion (exactly one of two connections served) is part of the passing run |

**Score:** 20/20 truths verified (0 present-but-behavior-unverified — every behavior-dependent truth
in this table is backed by an e2e test that I ran directly, not merely cited)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.claude/mcp/vice/vice-broker.mts` | One-process broker entry point owning coordination, supervision, warm pool | ✓ VERIFIED | Both real launch paths now compose through `withCrashSupervision()`; both destructive handlers (`handleRelease`, `handleRecycleForRealBroker`) call `markDeliberateDeath()` before signalling |
| `.claude/mcp/vice/broker-control.mts` | TCP control plane (acquire/release/recycle/status/host_state) | ✓ VERIFIED | `attemptAcquire()`'s disconnect-while-queued leak closed (pre-check + late-grant release); no wire-format change (five-member `ControlRequestKind` union confirmed unchanged by a dedicated test) |
| `.claude/mcp/vice/broker-launch.mts` | Per-child supervisor (`superviseChild`/`withCrashSupervision`), single in_flight owner | ✓ VERIFIED — no longer orphaned | `withCrashSupervision()` extracted as the sole exit-listener installation point and reached from the real entry point via two call sites, held by a structural gate |
| `.claude/mcp/vice/broker-state.mts` | Instance record shape, port allocator | ✓ VERIFIED | Gained `respawnAfterKill?: boolean` (additive, documented); `nextFreePort()` gained a liveness yield (plan 14 deviation) covered by its own targeted unit test |
| `.claude/mcp/vice/broker-epoch.mts` | Epoch writer, D-04 contract | ✓ VERIFIED | Unchanged; still fixture-diff tested |
| `.claude/mcp/vice/broker-kill.mts` | Identity-verified kill, shutdown, unconditional reap | ✓ VERIFIED | Unchanged; confirmed it never references `respawnAfterKill` |
| `.claude/mcp/vice/vice-proxy.ts` | Container-side control session, recycle tool, lost-machine replacement | ✓ VERIFIED | `handleGrantedInstanceUnreachable()`'s same-session branch now releases before acquiring, closing WR-03; the design tradeoff this creates is noted under Anti-Patterns (not a gap) |
| `resources/vice-launcher.sh` | Sole surviving shell script | ✓ VERIFIED | Unchanged |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `vice-broker.mts` `handleAcquire()` | `broker-launch.mts` `withCrashSupervision()` | Exit-aware spawn wrapper | **WIRED** | `vice-broker.mts:345`, confirmed by direct read and passing e2e test |
| `vice-broker.mts` `maintainWarmFloorForRealBroker()` | `broker-launch.mts` `withCrashSupervision()` | Exit-aware spawn wrapper | **WIRED** | `vice-broker.mts:479`, confirmed by direct read and passing e2e test |
| `vice-broker.mts` `handleRecycleForRealBroker()` | Instance respawn | `markDeliberateDeath(instance, true)` + supervision exit handler | **WIRED** | Marker set before kill (`vice-broker.mts:445`); `handleExit()`'s recycle branch relaunches (`broker-launch.mts:699-713`); e2e-proved |
| `vice-broker.mts` `handleRelease()` | No respawn | `markDeliberateDeath(instance, false)` | **WIRED** | Opposite answer set before kill (`vice-broker.mts:510`); e2e-proved no replacement appears across two evaluation passes |
| `broker-control.mts` `attemptAcquire()` | Abandoned-connection guard | `socket.destroyed` pre-check before `onAcquire()` | **WIRED** | Checked before (line 360) AND after (lines 370-373), each bounding a different, correctly-scoped failure; e2e-proved |
| `vice-proxy.ts` `handleGrantedInstanceUnreachable()` | Prior-grant release | `session.release()` before `session.acquire()` | **WIRED (with a noted cost tradeoff)** | Confirmed by direct read and by two passing structural tests; see Anti-Patterns |

### Behavioral Spot-Checks

All six behavior-dependent tests below were **run directly by me** in this session (not merely
cited from a SUMMARY), each in isolation via `--test-name-pattern`, immediately followed by a full
suite re-run:

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| A granted stub child killed under the real broker respawns on the same port | `node --test --test-name-pattern="wired supervision: a granted" broker-e2e.test.ts` | 1 pass | ✓ PASS |
| A warm-floor stub child killed under the real broker respawns via the same wrapper | `node --test --test-name-pattern="wired supervision: a warm-floor" broker-e2e.test.ts` | 1 pass | ✓ PASS |
| A recycle over the real control plane respawns on the same port, epoch advanced | `node --test --test-name-pattern="wired recycle"` | 1 pass | ✓ PASS |
| A release over the real control plane leaves no replacement | `node --test --test-name-pattern="wired release"` | 1 pass | ✓ PASS |
| A disconnected queued acquirer leaves exactly one instance, not two | `node --test --test-name-pattern="wired disconnect-while-queued"` | 1 pass | ✓ PASS |
| Structural gate: exit-listener count 1, spawn-factory count == wrapper call-site count | `node --test --test-name-pattern="structural: broker-launch.mts's child exit listener"` | 1 pass | ✓ PASS |
| Structural gate: release/recycle handlers set marker before kill, opposite answers | `node --test --test-name-pattern="structural: the release and recycle handlers"` | 1 pass | ✓ PASS |
| Full suite | `node --test '.claude/mcp/vice/'*.test.*` | 390 total, 385 pass, 0 fail, 5 todo | ✓ PASS |
| Generated resources match authored source | `node build.ts && git status --porcelain resources/` | clean | ✓ PASS |
| Typecheck | `npm run typecheck` | exit 0 | ✓ PASS |

### Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` convention in this repository. Step 7c skipped, as
in the initial verification.

### Requirements Coverage

No requirement IDs declared for this phase or milestone. Confirmed again against
`.planning/REQUIREMENTS.md` (zero hits for `01.6.2`, `CR-`, `WR-`). Not a gap — matches this
project's established convention.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/mcp/vice/vice-proxy.ts` | 2347-2349 | `handleGrantedInstanceUnreachable()`'s same-session replacement now calls `session.release()` (which synchronously closes the connection, per D-12) immediately before `session.acquire()` on that SAME session object — so the same-session acquire attempt always finds the connection already gone and unconditionally falls through to the fresh-session branch. The function's own header comment ("Attempt 1: a replacement over the SAME session... reusing it is the whole point of 'costs one acquisition, not the session'") is no longer true in practice: every replacement now pays for a full fresh session open, not merely one acquisition | ℹ️ Info | This is a deliberate, transparently-documented tradeoff (both in the source comment and in `01.6.2-14-SUMMARY.md`'s own Decisions section), not a hidden defect: it trades away D-13's "same session" cost optimization to close the WR-03 grant leak, and the fallback path (fresh session, D-14) is the SAME pre-existing branch this file already had for a genuinely dead connection. Functionally correct (confirmed by two passing structural tests and an updated pre-existing D-13/D-14 test asserting the actually-reachable shape) — flagged here as a design-quality observation for whoever next revisits D-13's cost model, not as a phase-blocking gap. No must-have truth of this phase claims same-session reuse as a required behavior |

No debt markers (`TBD`/`FIXME`/`XXX`) found in any file touched by plans 12–15.

### Human Verification Required

None. Every finding above is directly provable from source, from a directly-executed test, or from
a directly-executed full suite run — no live emulator or real host session required.

The following backstops are carried forward as-is, exactly as the initial verification recorded
them, and are NOT gaps of this re-verification:
- No live-session or real-host verification exists until Phase 01.4 (every plan 01–15's own SUMMARY
  states this identically; nothing in 01.6.2 was A/B'd against a real emulator + real host broker).
  Plans 12–14 each declared one `verification: backstop` truth in their own frontmatter for exactly
  this reason (a real x64sc crash respawning; an agent's real `vice_recycle` call; a real session's
  lost-machine replacement) — none of the three is claimed as covered by this report.
- Real-host startup-reap verification remains plan 04's own stated backstop.
- Control port `19510` default remains MEDIUM confidence (unchanged).

### Gaps Summary

None. All four truths the initial verification (2026-08-04T06:30:00Z) scored FAILED are now
VERIFIED against the assembled system, each backed by an end-to-end test against the real spawned
broker artifact that I ran directly in this session (not merely cited from a SUMMARY):

1. **CR-01 (per-instance supervision never wired)** — closed by plan 12. `withCrashSupervision()` is
   composed into both real launch paths, held by a structural anti-regression gate whose
   discriminating power was demonstrated live.
2. **Ledger row 51 dishonesty (criterion A)** — closed by plans 12/13/15 together. The row now cites
   the wired e2e recycle test verbatim, with a dated correction note recording the prior citation and
   why it was false.
3. **`vice_recycle` not actually respawning** — closed by plan 13. The recycle handler marks the
   death as broker-ordered-and-replaced before killing; the exit handler's recycle branch relaunches
   directly, bypassing crash accounting and backoff, and restores granted state. Release was proved,
   in the same plan, to still NOT respawn (kill-never-recycle survives the supervision wiring).
4. **CR-02 (disconnect-while-queued leak)** — closed by plan 14. `attemptAcquire()` now pre-checks
   `socket.destroyed` before the launch callback and releases a grant that arrives too late, proved
   against the real spawned artifact with a widened-by-construction queueing window and an explicit
   precondition assertion.

All 16 previously-VERIFIED truths were regression-checked and hold: the full suite (390 tests, 385
pass, 0 fail, 5 pre-existing todo) is unchanged in shape from the initial verification's baseline
(368) plus exactly the test counts each of plans 12/13/14 declared (368+4+8+10=390, reconciled in
`01.6.2-VALIDATION.md`'s own Suite Arithmetic section), the generated `resources/` tier is
byte-identical to a fresh build, and typecheck is clean.

One design-quality observation (not a gap) is recorded under Anti-Patterns: closing the WR-03 grant
leak on the proxy side traded away the same-session replacement's cost optimization, a tradeoff the
authoring plan documented transparently in both source and SUMMARY and that does not violate any
must-have truth of this phase.

The phase goal — "the new machine exists and the file protocol is dead," with the broker owning
coordination, per-instance supervision and the warm pool — is now true of the assembled system, not
merely of its constituent modules. Phase 01.6.2 is ready to seal; Phase 01.6.2.1 (lifecycle policy,
criteria E/L/M) remains the next phase in sequence.

---

_Verified: 2026-08-04T10:30:00Z_
_Verifier: Claude (gsd-verifier)_
