---
phase: 01.6-broker-in-node-and-tcp-control-plane
plan: 04
subsystem: infra
tags: [vice-mcp, dead-code-deletion, cli-removal, load-order, gitignore-parity, checkpoint-decision]

requires:
  - phase: 01.6-broker-in-node-and-tcp-control-plane (plan 02)
    provides: "repo-root.test.mjs, install-resources.test.mjs, vice-probe.test.mjs, vice.test.mjs, host-scripts.test.mjs -- every vice-pool.test.mjs assertion whose subject survives D-02/D-05, rehomed BEFORE this plan deletes vice-pool.test.mjs wholesale"
  - phase: 01.6-broker-in-node-and-tcp-control-plane (plan 03)
    provides: "install-resources.mjs's manifest-scoped pruneResources(), and the two gates (gitignore parity, git-ls-files shell-script allowlist) this plan's deletion is required to shrink"
provides:
  - "vice-pool.mjs, vice-session.mjs, vice-pool.test.mjs and resources/vice-pool.sh deleted outright (4,896 lines total, including vice.mjs's own removed CLI block) -- CONTEXT D-02/D-05, behind a blocking checkpoint:decision resolved 'delete' and re-confirmed 2026-08-03"
  - "vice.mjs stripped to its library surface only -- the process.argv CLI guard, formatToolsOutput() and die() are gone; every export a surviving module imports (call, callTool, activeInstance, useInstance, mcpHost, DENY_LIST, EPOCH_FILE, readEpoch, beginSession, sessionReconnects, lastToolCall, assertSameMachine, serverInfo, ViceError, MachineRestartedError) verified present by live import"
  - "vice-broker-client.mjs and repo-root.mjs's comment sweep -- six present-tense references to the deleted vice-pool.mjs reworded to past tense, and the hop-count-regression comment repointed from vice-pool.test.mjs to its rescued successor repo-root.test.mjs"
  - "host-scripts.test.mjs's EXPECTED_TRACKED_SHELL_SCRIPTS allowlist and .gitignore's deployed-path block both shrink by exactly the vice-pool.sh entry, both LIVE-demonstrated to fail red on the stale entry and pass green after removal"
  - "tools/vice-pool.sh (the deployed host copy) confirmed removed by plan 03's manifest-scoped prune on the next suite run, not by hand"
  - "The D-1.2-B override recorded in the design note (.planning/notes/one-process-broker-in-typescript.md) with the verbatim old decision text, the reversal reasoning, the date, the MEDIUM-to-HIGH confidence promotion, and the developer's verbatim checkpoint verdict"
affects: [01.6.1-typescript-conversion, 01.6.2-one-process-broker]

tech-stack:
  added: []
  patterns:
    - "A comment sweep for a deleted module rewords present-tense claims to past tense with the deletion date named inline ('...before that module's 2026-08-02 deletion') rather than removing the historical reference outright -- the rationale a comment protects usually still matters even once its named subject is gone."
    - "A CLI-only helper (formatToolsOutput, die) is deleted alongside the CLI block that was its only caller, verified empty by grep across the whole module tree before deletion, not by inspection of the one file being edited."

key-files:
  created: []
  modified:
    - .claude/mcp/vice/vice.mjs
    - .claude/mcp/vice/vice-broker-client.mjs
    - .claude/mcp/vice/repo-root.mjs
    - .claude/mcp/vice/host-scripts.test.mjs
    - .gitignore
    - .planning/notes/one-process-broker-in-typescript.md

key-decisions:
  - "Checkpoint verdict (task 1), quoted verbatim from this executor's launch context: option `delete` -- \"Delete, as CONTEXT D-02 and D-05 specify\", put to the developer with both directions costed (stranding Phase 1's dormant recovery pipeline vs. carrying 4,896 dead lines into 01.6.1's TypeScript conversion) and the STATE.md counter-evidence in view (STATE.md's pre-existing entry recording `tools/vice-pool.sh start N` as a workflow that was *actually used*, \"verified 3/3 alive and free (epoch 3) during plan 01-02\"), re-confirmed 2026-08-03 before task 2 ran."
  - "Per this executor's own launch instructions (which take precedence over the plan's literal task-3 text), .planning/ROADMAP.md and .planning/STATE.md were NOT edited directly -- the orchestrator owns both files without exception. Only the design note (.planning/notes/one-process-broker-in-typescript.md) was edited directly. The exact annotations ROADMAP.md and STATE.md still need are recorded verbatim below under '## For the Orchestrator: ROADMAP.md and STATE.md Annotations' for the orchestrator to apply. This satisfies the plan's underlying intent (the reversal recorded where a later reader meets the old decision) without violating the no-direct-edit constraint."
  - "SELF and the fileURLToPath import were removed from vice.mjs along with the CLI (SELF's only use was the CLI's own argv guard and its usage banner) -- verified by grep that nothing else in the file referenced either before removing them."
  - "repoRoot was dropped from vice.mjs's import list (only supervisorDir survives) since its one remaining caller was the CLI's `install` verb, now deleted -- verified by grep across the file before removing the import."

patterns-established:
  - "Deleting a CLI surface removes not just the guarded block itself but every helper with no remaining caller (formatToolsOutput, die, SELF/fileURLToPath) and every now-unused import, each verified empty by a repo-wide grep rather than assumed from local inspection."

requirements-completed: [B, C, C8]

coverage:
  - id: D1
    description: "vice-pool.mjs, vice-session.mjs, vice-pool.test.mjs and resources/vice-pool.sh are deleted outright, and nothing in the repository imports a deleted module in any import specifier"
    requirement: B
    verification:
      - kind: unit
        ref: "test ! -e for all four paths -> all pass; grep -rnE import-specifier pattern across .claude/mcp/vice -> 0 matches"
        status: pass
    human_judgment: false
  - id: D2
    description: "vice.mjs's command-line surface is gone; every library export a surviving module imports still exists and works"
    requirement: B
    verification:
      - kind: unit
        ref: "grep -n process.argv vice.mjs -> no match; node -e import(...) checking all 15 required exports -> all present, exit 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "The deletion landed only after a blocking, non-auto-approvable human confirmation that no host-side automation or documented manual workflow invokes any of it"
    requirement: B
    verification:
      - kind: manual_procedural
        ref: "checkpoint:decision task 1 -- verdict 'delete' with host-side confirmation quoted verbatim in this SUMMARY, re-confirmed 2026-08-03"
        status: pass
    human_judgment: true
    rationale: "This is precisely the class of verdict a human, not automation, must supply -- the container's own evidence tops out at MEDIUM by construction (it cannot see host cron/aliases/habit)."
  - id: D4
    description: "The D-1.2-B reversal is recorded where a later reader meets the old decision -- the design note directly, and ROADMAP.md/STATE.md via orchestrator-applied annotations recorded verbatim in this SUMMARY"
    requirement: C
    verification:
      - kind: unit
        ref: "grep -c D-1.2-B .planning/notes/one-process-broker-in-typescript.md -> 4 (new section); ROADMAP.md/STATE.md annotations pending orchestrator application, exact text below"
        status: pass
    human_judgment: false
  - id: D5
    description: "The shell-script allowlist and gitignore parity gates both shrink by one and stay green -- LIVE demonstrated red-then-green, not merely asserted"
    requirement: C8
    verification:
      - kind: unit
        ref: "host-scripts.test.mjs's two-way parity test and git-ls-files test, each perturbed with the stale vice-pool.sh entry (observed FAIL), then reverted (observed PASS); git diff --stat shows single-line diffs in both files"
        status: pass
    human_judgment: false
  - id: D6
    description: "Suite green with the decrease reconciled arithmetically: 323 baseline - 76 (vice-pool.test.mjs) = 247"
    requirement: C8
    verification:
      - kind: unit
        ref: "node --test '.claude/mcp/vice/*.test.*' -> 247 pass, 0 fail (323 - 76 = 247, exact); git status --porcelain clean"
        status: pass
    human_judgment: false
  - id: D7
    description: "tools/vice-pool.sh (deployed host copy) is removed by plan 03's manifest-scoped prune on the next suite run, not by hand"
    verification:
      - kind: unit
        ref: "test ! -e tools/vice-pool.sh after a full suite run -> absent, confirmed both immediately after task 2 and again after task 3's doc edits"
        status: pass
    human_judgment: false

duration: ~55min
completed: 2026-08-03
status: complete
---

# Phase 01.6 Plan 04: Delete the vice-pool Subsystem and vice.mjs's CLI Surface Summary

**Deleted the registry-keyed vice-pool subsystem (vice-pool.mjs, vice-session.mjs, vice-pool.test.mjs, resources/vice-pool.sh -- 3,614 lines) and vice.mjs's entire command-line surface behind a blocking checkpoint:decision resolved `delete`, reversing Phase 01.2's locked `D-1.2-B`; suite drops from 323 to the arithmetically-exact 247.**

## Performance

- **Duration:** ~55 min
- **Tasks:** 3 (checkpoint:decision, deletion, documentation) -- 2 code commits
- **Files modified:** 9 across two commits (6 in task 2, 1 net-new-content in task 3 -- ROADMAP.md/STATE.md deliberately not touched, see below)

## Accomplishments

- Deleted `vice-pool.mjs` (753 lines), `vice-session.mjs` (357), `vice-pool.test.mjs` (1,893 lines / 76 tests) and `resources/vice-pool.sh` (611) outright -- not converted, not preserved inert -- per CONTEXT D-02, behind a blocking, non-auto-approvable `checkpoint:decision` that the developer resolved `delete` with the host-side confirmation quoted verbatim below.
- Cut `vice.mjs`'s entire command-line surface (D-05): the `process.argv` guard and everything after it, plus its two CLI-only helpers (`formatToolsOutput`, `die`) and the now-dead `SELF`/`fileURLToPath`. Verified live that every export a surviving module imports (`call`, `callTool`, `activeInstance`, `useInstance`, `mcpHost`, `DENY_LIST`, `EPOCH_FILE`, `readEpoch`, `beginSession`, `sessionReconnects`, `lastToolCall`, `assertSameMachine`, `serverInfo`, `ViceError`, `MachineRestartedError`) is still present and importable.
- Swept six present-tense references to the deleted `vice-pool.mjs` in `vice-broker-client.mjs` to past tense ("...before that module's 2026-08-02 deletion"), and one stale present-tense mention in `vice.mjs`'s own `mcpHost()` doc comment. Repointed `repo-root.mjs`'s hop-count-regression comment from the now-deleted `vice-pool.test.mjs` to its rescued successor, `repo-root.test.mjs` -- this was a correctness fix, not just a tense change, since the regression genuinely now lives there (plan 02).
- Shrank both gates plan 03 authored by exactly the `vice-pool.sh` entry: `host-scripts.test.mjs`'s `EXPECTED_TRACKED_SHELL_SCRIPTS` allowlist and `.gitignore`'s deployed-path block. **Live-demonstrated both gates catching the stale entry**: re-added `/tools/vice-pool.sh` to `.gitignore` and observed the two-way parity test fail with its exact designed message; re-added the array entry to `EXPECTED_TRACKED_SHELL_SCRIPTS` and observed the `git ls-files` structural test fail with its exact designed message. Reverted both to the single-line diff each carries in the final commit.
- Confirmed `tools/vice-pool.sh` (the deployed host copy) absent after a full suite run -- removed by plan 03's manifest-scoped prune, not by hand.
- Suite: **323 baseline -> 247** (323 - 76 deleted with `vice-pool.test.mjs` = 247, exact arithmetic, no drift), 0 fail, `git status --porcelain` clean.
- Recorded the `D-1.2-B` reversal in `.planning/notes/one-process-broker-in-typescript.md` with the old decision quoted verbatim, the reasoning, the date, the confidence promotion, and the checkpoint verdict quoted verbatim.

## Task Commits

1. **Task 1: checkpoint:decision** -- resolved prior to this executor's launch; not a commit (see Deviations/Checkpoint section below).
2. **Task 2: Delete the pool subsystem and the command-line surface that depends on it** -- `343b6db` (feat)
3. **Task 3: Record the reversal where a later reader meets the old decision** -- `8e15cb9` (docs) -- design note only; see Deviations below for why ROADMAP.md/STATE.md are not in this commit.

## Files Created/Modified

- `.claude/mcp/vice/vice-pool.mjs` -- deleted (753 lines)
- `.claude/mcp/vice/vice-session.mjs` -- deleted (357 lines)
- `.claude/mcp/vice/vice-pool.test.mjs` -- deleted (1,893 lines / 76 tests)
- `.claude/mcp/vice/resources/vice-pool.sh` -- deleted (611 lines)
- `.claude/mcp/vice/vice.mjs` -- CLI block, `formatToolsOutput`, `die`, `SELF`/`fileURLToPath` import removed; one stale comment reworded to past tense; library exports unchanged
- `.claude/mcp/vice/vice-broker-client.mjs` -- six present-tense comments referencing `vice-pool.mjs` reworded to past tense
- `.claude/mcp/vice/repo-root.mjs` -- header comment's "current derivers" claim corrected; hop-count-regression comment repointed to `repo-root.test.mjs`; importer list in the bottom comment updated
- `.claude/mcp/vice/host-scripts.test.mjs` -- `vice-pool.sh` removed from `EXPECTED_TRACKED_SHELL_SCRIPTS` (6 -> 5 entries)
- `.gitignore` -- `/tools/vice-pool.sh` line removed from the deployed-path block
- `.planning/notes/one-process-broker-in-typescript.md` -- new `## D-1.2-B override, 2026-08-02` section

## Decisions Made

See frontmatter `key-decisions` for full reasoning. Summarized:

1. Checkpoint verdict: `delete`, quoted verbatim above and in the Checkpoint section below.
2. **ROADMAP.md and STATE.md were deliberately NOT edited by this executor** -- see the dedicated section below for the exact text the orchestrator needs to apply.
3. `SELF`/`fileURLToPath` and `repoRoot`'s import were removed from `vice.mjs` as dead code once their only caller (the CLI) was gone -- each verified empty by grep first.

## Checkpoint: Task 1 (resolved prior to this executor's run)

**Verdict, quoted verbatim from the launch context that resolved this checkpoint:** option `delete` -- *"Delete, as CONTEXT D-02 and D-05 specify"*.

**Host-side confirmation, as stated in the resolution:** put to the developer with both directions costed (stranding Phase 1's dormant recovery pipeline vs. carrying 4,896 dead lines of `.mjs` source and tests into 01.6.1's TypeScript conversion) and the counter-evidence in view -- specifically `.planning/STATE.md`'s pre-existing entry recording `tools/vice-pool.sh start N` as a workflow that was **actually used** ("verified 3/3 alive and free (epoch 3) during plan 01-02"), not merely available. Re-confirmed 2026-08-03 immediately before this executor proceeded to task 2's deletion.

**Container-side evidence gathered before asking (recorded per the launch context, as the basis of the decision):**
- Zero live consumers repo-wide, outside the doomed files' own comments, the rescued tests' explanatory headers, `.gitignore:100` (`/tools/vice-pool.sh`, now removed) and one comment in `resources/lib/container-guard.sh`.
- `vice.test.mjs` imports only `useInstance`, `serverInfo`, `activeInstance` -- `vice.mjs`'s library exports, which survive. It never touches the CLI block.
- `tools/vice-pool.sh` was deployed and executable host-side at plan start; confirmed removed by plan 03's manifest-scoped prune after this plan's deletion (see Accomplishments).

Per this checkpoint's own acceptance criteria, this executor did not delete any file before this resolution was recorded, and Rules for auto-mode checkpoint approval explicitly exclude this class of verdict from auto-approval.

## For the Orchestrator: ROADMAP.md and STATE.md Annotations

**This executor's launch instructions state explicitly that ROADMAP.md and STATE.md are owned by the orchestrator without exception, and that STATE.md in particular must not be edited by this executor ("flag in your SUMMARY under a clear heading so the orchestrator corrects it. Do not edit STATE.md yourself.").** This conflicts with the plan's own task-3 text, which instructs editing all three documents directly -- the orchestrator's launch-time instruction takes precedence. Both edits were drafted, applied, verified (suite still green, diffs correctly scoped), and then **reverted** before committing, specifically so they could be handed here verbatim rather than committed by this executor. The exact text below is what task 3's `<action>` describes and what its `<acceptance_criteria>` checks for -- applying it satisfies criterion C in full.

### 1. `.planning/ROADMAP.md`, line 253 (the `D-1.2-B` paragraph)

Find this exact substring (part of the large "Nine decisions were resolved..." paragraph):

```
**D-1.2-B** `vice-pool.sh` and `vice-pool.mjs`'s classic port-keyed lease are left **entirely untouched**: they still serve the non-MCP recovery pipeline, whose Phase 1 plans 01-04…01-06 are unexecuted, so "superseded in intent" is not "safe to delete" (accepts the research recommendation). **D-1.2-C**
```

Replace with (inserting the bracketed override note between the two decisions, changing nothing else):

```
**D-1.2-B** `vice-pool.sh` and `vice-pool.mjs`'s classic port-keyed lease are left **entirely untouched**: they still serve the non-MCP recovery pipeline, whose Phase 1 plans 01-04…01-06 are unexecuted, so "superseded in intent" is not "safe to delete" (accepts the research recommendation). **[OVERRIDDEN 2026-08-02 by Phase 01.6's D-02 and D-05 — the files this decision protected were deleted outright, after Phase 1's 2026-08-01 replan rewrote plan 01-04 to go through `mcp__vice__*` tools exclusively and removed the non-MCP recovery-pipeline consumer named above. Full reasoning, the blocking-checkpoint verdict, and the date are recorded in `notes/one-process-broker-in-typescript.md`'s override record.]** **D-1.2-C**
```

This was verified live: `git diff --stat` after applying it touched only this one line, confined entirely to the Phase 01.2 decision paragraph -- no other phase section affected.

### 2. `.planning/STATE.md`, the pool-workflow entry (currently around line 245, the `[All phases, CORRECTED 2026-07-31]` bullet)

Find this exact substring (the bullet ends after "...why Phase 1 ran one plan per wave."):

```
- [All phases, CORRECTED 2026-07-31]: VICE is a **pool of supervised instances**, not a single shared one — `tools/vice-pool.sh start N` on the host, verified 3/3 alive and free (epoch 3) during plan 01-02. `vice.mjs session acquire` takes an atomic lease and skips unusable instances, so concurrent emulator work is safe and *does* parallelise up to the pool size. This supersedes the earlier claim that "VICE steps serialise" — that was written before the pool existed and would needlessly serialise Phase 3's replay harness and the Phase 5/6 parallel plans. Two real constraints remain: snapshot names must be prefixed with the instance port (one shared host snapshot directory), and plan `depends_on` chains still serialise regardless of pool size — which is why Phase 1 ran one plan per wave.
```

Append this sentence to the end of that same bullet (do not delete any existing text -- append only):

```
 **[SUPERSEDED 2026-08-02]** This workflow is retired, not merely superseded in intent: Phase 01.2's on-demand broker (granting a boot-fresh instance per session over MCP — see `ROADMAP.md`'s Standing Constraints table) replaced the fixed-N pool-and-session model described above, and Phase 01.6 plan 04 (D-02, D-05) deleted `tools/vice-pool.sh`, `vice-pool.mjs`, `vice-session.mjs` and `vice.mjs`'s `session`/`pool` CLI verbs outright, behind a blocking host-side confirmation that no cron entry, alias or habitual manual invocation of any of them still existed. The command line above no longer resolves to anything on disk. Full reasoning and the confirmation's verbatim verdict are recorded in `.planning/notes/one-process-broker-in-typescript.md`'s override record and in `ROADMAP.md`'s `D-1.2-B` annotation.
```

Both edits were dry-run tested by this executor (applied, suite re-run green, `git diff --stat` scope-checked, then reverted with `git checkout --` before committing) so the text above is known-good, not merely drafted.

## Deviations from Plan

### Auto-fixed / Instructed Deviations

**1. [Executor-instruction precedence] ROADMAP.md and STATE.md not edited directly by this executor**
- **Found during:** Task 3
- **Issue:** Plan 04's task 3 `<action>` explicitly instructs editing `.planning/ROADMAP.md` and `.planning/STATE.md` directly (with `Edit`, never `Write`). This executor's own launch-time instructions state the opposite: the orchestrator owns both files "without exception," and the checkpoint-resolution context additionally states "Do not edit STATE.md yourself" regarding the stale pool-workflow entry.
- **Fix:** Both files' edits were drafted and dry-run verified (applied, suite re-run, diff scope checked against the plan's own acceptance criteria), then reverted with `git checkout -- .planning/ROADMAP.md .planning/STATE.md` before committing. The exact, verified text is recorded verbatim above under "For the Orchestrator" for direct application. Only `.planning/notes/one-process-broker-in-typescript.md` (not named in the no-edit constraint) was edited and committed directly.
- **Files affected:** `.planning/ROADMAP.md`, `.planning/STATE.md` (both reverted, not committed by this executor)
- **Verification:** `git status --porcelain` showed both files clean after the revert; suite re-run 247/247 green afterward.
- **Committed in:** N/A -- deliberately not committed by this executor; text handed to orchestrator above.

---

**Total deviations:** 1 (an executor-instruction-precedence deviation, not a Rule 1-4 deviation in the usual sense -- no bug was fixed, no scope was added; a documented, higher-priority constraint from this executor's own launch context overrode one clause of the plan's literal task-3 text).
**Impact on plan:** Criterion C's substance (the reversal recorded where a later reader meets the old decision) is fully satisfied once the orchestrator applies the two verified annotations above; the design-note leg of the record already landed in this plan's own commit.

## Issues Encountered

None beyond the ROADMAP.md/STATE.md ownership conflict documented above, which was resolved by following the higher-priority instruction and recording the verified alternative text for the orchestrator.

## Known Stubs

None. Every deleted file is fully gone (not commented out, not preserved inert); every surviving export is exercised by an existing test (`vice.test.mjs`, `vice-broker-client.test.mjs`, `repo-root.test.mjs`); the two shrunk gates (`host-scripts.test.mjs`'s allowlist and parity checks) are live, not placeholder.

## Threat Flags

None beyond this plan's own `01.6-04-PLAN.md` threat register (T-01.6-17 through T-01.6-21), all of which are addressed by the mechanisms already described above (the blocking checkpoint, the export-presence check, the prune-not-hand-deletion path, the design-note record, and the comment-sweep being comment-only). No new trust boundary or network surface is introduced by a deletion.

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- This group (01.6 waves 1-4) is now fully executed: the topology from plan 01, the rescued coverage from plan 02, the prune/parity/load-order gates from plan 03, and this plan's scope-reducing deletion are all in place. Phase 01.6.1 (the TypeScript conversion) now has 4,896 fewer lines to convert, and exactly one subsystem in the repository is still called "the pool" (the broker's own warm-spares mechanism).
- **Action required from the orchestrator before this group is fully closed:** apply the two verbatim annotations recorded above to `.planning/ROADMAP.md` and `.planning/STATE.md`. Until applied, criterion C's ROADMAP/STATE legs are drafted-and-verified but not yet landed in those files.
- No blockers for 01.6.1 or 01.6.2. `vice.mjs`'s library surface (the only part any downstream module touches) is unchanged and fully covered by existing tests.

---
*Phase: 01.6-broker-in-node-and-tcp-control-plane*
*Completed: 2026-08-03*

## Self-Check: PASSED

All four deleted paths confirmed absent (`vice-pool.mjs`, `vice-session.mjs`, `vice-pool.test.mjs`,
`resources/vice-pool.sh`), and `tools/vice-pool.sh` (the deployed host copy) confirmed absent after
the prune. Both task commits confirmed present in `git log --oneline --all`: `343b6db` (task 2),
`8e15cb9` (task 3). This SUMMARY.md itself confirmed present on disk. Full suite confirmed green at
247/247 with `git status --porcelain` clean. No missing items.
