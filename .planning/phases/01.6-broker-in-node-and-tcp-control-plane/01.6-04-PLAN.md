---
phase: 01.6-broker-in-node-and-tcp-control-plane
plan: 04
type: execute
wave: 4
depends_on: ["01.6-03"]
files_modified:
  - .claude/mcp/vice/vice-pool.mjs
  - .claude/mcp/vice/vice-session.mjs
  - .claude/mcp/vice/vice-pool.test.mjs
  - .claude/mcp/vice/resources/vice-pool.sh
  - .claude/mcp/vice/vice.mjs
  - .claude/mcp/vice/vice-broker-client.mjs
  - .claude/mcp/vice/repo-root.mjs
  - .claude/mcp/vice/host-scripts.test.mjs
  - .gitignore
  - .planning/notes/one-process-broker-in-typescript.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [B, C, C8]

must_haves:
  truths:
    - "`vice-pool.mjs`, `vice-session.mjs`, `vice-pool.test.mjs` and `resources/vice-pool.sh` are absent from the working tree (D-02, criterion B)"
    - "`vice.mjs`'s command-line surface is gone, and every export a surviving module imports from it still exists and still works (D-05, criterion B)"
    - "Nothing in the repository imports a deleted module on any code path, static or dynamic"
    - "The deletion landed only after a blocking human confirmation that no host-side automation and no documented manual workflow invokes any of it — the container reached MEDIUM confidence and cannot reach HIGH by construction (criterion B)"
    - "The reversal of the Phase 01.2 locked decision `D-1.2-B` is recorded in writing, with its reasoning and its date, where a later reader meets the old decision (D-03, criterion C)"
    - "`.planning/STATE.md`'s recorded manual host workflow naming the deleted launcher is corrected in the same change, so no document sends a reader to a script that no longer exists (planner finding)"
    - "The deployed copy under `tools/` is removed by plan 03's prune on the next deploy, rather than lingering as a stale host executable (E)"
    - "The shell-script allowlist and the ignore-set parity gate both shrink by one and stay green — the gates authored in plan 03 are what force this"
    - "Exactly one subsystem in this repository is called 'the pool': the broker's own warm-spares set. The registry-keyed lease pool has no surviving referent"
    - "The suite is green, and the reduced test count is recorded as a deletion with its arithmetic, never reported as a regression (C8)"
  artifacts:
    - .planning/notes/one-process-broker-in-typescript.md
  key_links:
    - "vice.mjs's dynamic imports at lines ~660, ~690, ~724 and ~787 are the live edges into the deleted subsystem. They are inside the command-line block, so deleting the modules without cutting those edges breaks vice.mjs at RUNTIME, not at load — nothing would go red until someone ran it"
    - "The confirmation checkpoint is what promotes 'zero live consumers' from MEDIUM to HIGH. Nothing downstream of it may run before it resolves, and its verdict is not auto-approvable"
    - "STATE.md line 243 records `tools/vice-pool.sh start N` and `vice.mjs session acquire` as a real host workflow that was actually used during plan 01-02. It is superseded by the 01.2 broker, but it is a documented manual workflow — which is exactly what the checkpoint asks about, and exactly what must be swept when the answer is yes"
---

<planning_notes>

**Spec-less probe fallback: SKIPPED (visible).** Recorded in full in `01.6-01-PLAN.md`.
`requirements:` holds this group's criteria letters and ROADMAP criterion IDs, not REQ IDs.

**Reversibility: `one-way`.** This plan deletes 3,003 lines of source and 1,893 lines of tests, and
it reverses a decision Phase 01.2 recorded as locked. Files are recoverable from git history, but the
decision is not: once `D-1.2-B` is overridden and the subsystem is gone, every later phase is planned
against its absence. The rule is therefore honoured literally — a `checkpoint:decision` sits
immediately before the deleting task, in the same plan, not two waves upstream where its verdict
would age.

**What the container established, and where it stops.** RESEARCH §A1 grepped `tools/`,
`.claude/skills/` and `.claude/mcp/` exhaustively and found zero live importers outside the module
tree itself, its own tests and historical planning documents — **MEDIUM confidence**, because
absence-of-import is strong evidence of absence-of-use but not proof. What the container cannot see
at all: a host cron entry, a host-side script, a shell alias, or a human typing a command from a
terminal. `01.6-VALIDATION.md` lists this as a manual-only verification and says plainly that it
**gates the deletion**.

**A finding the source artifacts did not carry.** `.planning/STATE.md` line 243 does not merely
tolerate the pool — it documents it as a workflow that was *used*: "`tools/vice-pool.sh start N` on
the host, verified 3/3 alive and free (epoch 3) during plan 01-02". That is a documented manual
workflow, and the checkpoint asks about exactly those. It is superseded by Phase 01.2's broker, so
the expected answer is "yes, delete" — but the entry must be swept in the same change, or the project's
own state file sends a later reader to a script that no longer exists. This project already names that
failure class for confidence grades; it applies identically here.

**Corroborating evidence, for the checkpoint.** `.planning/notes/vice-broker-lifecycle-decisions.md`
reaches the same verdict independently, from a different reading: "`vice-pool.sh` is dead: nothing
invokes it, and the broker carries its own [spares mechanism]". Two independent readings agreeing is
worth more than one, and both are still container-side.

**Boundary held.** `resources/vice-supervisor.sh` and `resources/vice-broker.sh` are **not** deleted
here — they retire in Phase 01.6.2. `hostLaunchInstructions()` still names them and stays unchanged.
No `.mjs` module is converted to TypeScript in this plan; that is 01.6.1's.

</planning_notes>

<objective>
Reduce the conversion's scope to what will actually be converted: delete the `vice-pool` subsystem and
the command-line surface that depends on it, behind a blocking confirmation, and record in writing the
locked decision the deletion reverses.

Purpose: 4,896 lines that 01.6.1 would otherwise convert, test, and carry forward serve a pipeline
that no longer exists. But the evidence for "no longer exists" tops out at MEDIUM from inside a
container that cannot see the host — and getting it wrong strands Phase 1's dormant recovery pipeline.
So the deletion is gated, and the reversal is recorded where the old decision lives rather than left
for a later reader to discover as a contradiction.

Output: four files gone, one command-line surface gone, three planning documents corrected, and the
gates authored in plan 03 shrinking by one apiece to prove the deletion propagated.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-CONTEXT.md
@.planning/notes/one-process-broker-in-typescript.md
</context>

## Artifacts this phase produces

| Artifact | Kind | Notes |
|---|---|---|
| `.claude/mcp/vice/vice-pool.mjs` | deleted | 753 lines. Container-side half of the registry-keyed lease subsystem. |
| `.claude/mcp/vice/vice-session.mjs` | deleted | 357 lines. Its only live importer. |
| `.claude/mcp/vice/vice-pool.test.mjs` | deleted | 1,893 lines / 76 tests. Its surviving coverage was rehomed in plan 02. |
| `.claude/mcp/vice/resources/vice-pool.sh` | deleted | 611 lines. The older, non-MCP host launcher. |
| `.claude/mcp/vice/vice.mjs` | modified | The command-line block and its exclusively-CLI helpers removed. Library exports unchanged. |
| `.planning/notes/one-process-broker-in-typescript.md` | modified | Gains the dated D-03 override record. |
| `.planning/ROADMAP.md` | modified | `D-1.2-B` at line 253 gains an inline override annotation. |
| `.planning/STATE.md` | modified | The pool-workflow entry at line 243 marked superseded. |
| `.gitignore`, `host-scripts.test.mjs` | modified | Both shrink by one entry; plan 03's gates are what force it. |

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>
    Delete the `vice-pool` subsystem and `vice.mjs`'s command-line surface — `vice-pool.mjs` (753),
    `vice-session.mjs` (357), `vice-pool.test.mjs` (1,893), `resources/vice-pool.sh` (611) and the CLI
    block in `vice.mjs` — reversing the Phase 01.2 locked decision `D-1.2-B`, which said these files
    are "left entirely untouched… 'superseded in intent' is not 'safe to delete'."
  </decision>
  <context>
    **This is the one question the container cannot answer.** Everything below is container-side
    evidence at MEDIUM confidence, and the gap it cannot close is a human's own habits and the host's
    own automation.

    **What was checked, and what it found.** An exhaustive grep across `tools/`, `.claude/skills/` and
    `.claude/mcp/` found zero live importers of `vice-pool.mjs` or `vice-session.mjs` outside the module
    tree itself, its own tests, and historical planning documents. `vice-pool.sh` is a *different, older*
    launcher: it starts N raw supervised emulator instances into a `registry.json` for the **non-MCP
    recovery pipeline**, which is not the same thing as the broker's warm-spares mechanism. That
    pipeline's Phase 1 consumers do not exist in the tree — Phase 1's 2026-08-01 replan rewrote plan
    01-04 to go through `mcp__vice__*` tools exclusively, which is what removed the consumer `D-1.2-B`
    was protecting. A second, independent reading in
    `.planning/notes/vice-broker-lifecycle-decisions.md` reaches the same verdict: "`vice-pool.sh` is
    dead: nothing invokes it."

    **What you are being asked, specifically.** Confirm that on the **host**:
    - no cron entry, systemd unit, login script, shell alias or wrapper invokes `tools/vice-pool.sh`;
    - you do not, by habit or from notes outside this repository, run `tools/vice-pool.sh start N`,
      `node .claude/mcp/vice/vice.mjs pool status`, `… session acquire`, `… session release`,
      `… ping`, `… call` or `… tools` from a terminal;
    - nothing you rely on reads `registry.json` or the port-keyed lease files.

    **One thing in this repository says you have.** `.planning/STATE.md` line 243 records
    `tools/vice-pool.sh start N` as a workflow that was actually used — "verified 3/3 alive and free
    (epoch 3) during plan 01-02" — alongside `vice.mjs session acquire`. Phase 01.2's broker supersedes
    it, and the standing-constraints table already describes the replacement. It is surfaced here rather
    than smoothed over, because it is exactly the class of thing this question is asking about.

    **What the CLI deletion adds (D-05).** `vice.mjs` dynamically imports `resolveInstance` from
    `vice-session.mjs` for **every** command-line verb except `session`, `pool` and `install` — so
    `ping`, `call` and `tools` break on this deletion too, not only the two obviously-doomed
    subcommands. Rather than rework them, the CLI goes with the subsystem it depends on. This is
    consistent with the repository's own standing assertion that no module in this directory is named
    in any agent-facing document — "no exemption, not even `vice.mjs`". Its **library** exports are
    untouched: `vice-proxy.mjs`, `vice-sync.mjs` and `refresh-manifest.mjs` keep importing them.

    **Cost of being wrong, in each direction.** Delete wrongly and Phase 1's dormant recovery pipeline
    is stranded — recoverable from git history, but discovered later, by a failure. Keep wrongly and
    Phase 01.6.1 converts 4,896 lines of dead code to TypeScript and every later phase carries it.
  </context>
  <options>
    <option id="delete">
      <name>Delete, as CONTEXT D-02 and D-05 specify</name>
      <pros>Removes 4,896 lines from 01.6.1's conversion scope. Leaves exactly one thing in this repository called "the pool". Matches both independent container-side readings.</pros>
      <cons>Reverses a locked decision. If a host-side or manual consumer exists, it breaks with no warning until someone runs it.</cons>
    </option>
    <option id="keep-as-mjs">
      <name>Leave them as `.mjs`, out of the conversion's scope</name>
      <pros>Reversible. Consistent with `D-1.2-B` as written, and with the treatment `tools/*.mjs` and the skill scripts already get.</pros>
      <cons>"All of `.claude/mcp/vice/` converts" stops being true. Two subsystems keep sharing the word "pool". The question returns, unanswered, in a later phase.</cons>
    </option>
    <option id="delete-partial">
      <name>Delete the subsystem but keep `vice.mjs`'s CLI, reworked to not need it</name>
      <pros>Preserves a diagnostic entry point a human might reach for.</pros>
      <cons>Rework for a surface that no agent-facing document may name, and that the repository's own tests already assert is unnamed. D-05 rejected this explicitly.</cons>
    </option>
  </options>
  <read_first>
    - `.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-CONTEXT.md` D-02, D-03, D-05
    - `.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-RESEARCH.md` §A1
    - `.planning/ROADMAP.md` line 253 — the `D-1.2-B` text being reversed
    - `.planning/STATE.md` line 243 — the recorded manual workflow
    - `.planning/notes/vice-broker-lifecycle-decisions.md` — the corroborating independent reading
  </read_first>
  <acceptance_criteria>
    The human selects one of the three option ids and, when the answer is `delete`, states explicitly
    that no host-side automation and no documented or habitual manual workflow invokes any of the named
    entry points. The verdict and its wording are quoted verbatim into the SUMMARY. This checkpoint is
    **not auto-approvable**: `workflow.auto_advance` does not apply to it, and no file is deleted before
    a human response is recorded.
  </acceptance_criteria>
  <resume-signal>Select: `delete`, `keep-as-mjs`, or `delete-partial` — with the host-side confirmation sentence if `delete`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Delete the pool subsystem and the command-line surface that depends on it</name>
  <precondition>The task-1 checkpoint resolved to `delete` and its verdict is recorded. If it resolved otherwise, stop: this task and task 3 do not run, and the SUMMARY records the phase closing without criterion B.</precondition>
  <reversibility rating="one-way">Reverses the Phase 01.2 locked decision `D-1.2-B`. The files are recoverable from git, the decision is not — every later phase is planned against the subsystem's absence.</reversibility>
  <files>
    .claude/mcp/vice/vice-pool.mjs,
    .claude/mcp/vice/vice-session.mjs,
    .claude/mcp/vice/vice-pool.test.mjs,
    .claude/mcp/vice/resources/vice-pool.sh,
    .claude/mcp/vice/vice.mjs,
    .claude/mcp/vice/vice-broker-client.mjs,
    .claude/mcp/vice/repo-root.mjs,
    .claude/mcp/vice/host-scripts.test.mjs,
    .gitignore
  </files>
  <read_first>
    - `.claude/mcp/vice/vice.mjs` — the whole file, but in particular the boundary at line 626 where the command-line block begins, and the four dynamic imports at roughly lines 660, 690, 724 and 787. Note which exports are defined above that boundary and which helpers exist only to serve it.
    - `.claude/mcp/vice/vice-proxy.mjs` line 16, `.claude/mcp/vice/vice-sync.mjs` line 46, `.claude/mcp/vice/refresh-manifest.mjs` line 11 — the exact library import lists that must keep resolving.
    - `.claude/mcp/vice/install-resources.mjs`, `.claude/mcp/vice/repo-root.mjs`, `.claude/mcp/vice/hostpath.mjs` — the three-module cycle, and the installer's head comment at lines 6–21. `repo-root.mjs` is edited in this task for its comments only; meet the contract before touching it.
    - `.claude/mcp/vice/host-scripts.test.mjs` and `.gitignore` — the allowlist constant and the deployed-path block, both of which shrink by one.
  </read_first>
  <action>
    Remove the subsystem, then remove everything that pointed at it.

    **Delete four files outright:** `vice-pool.mjs`, `vice-session.mjs`, `vice-pool.test.mjs` and
    `resources/vice-pool.sh`. Not converted, not preserved inert, not commented out.

    **Cut the command-line surface out of `vice.mjs`.** Everything from the command-line boundary
    comment and the `process.argv` guard at line 626 to the end of the file goes, including all four
    dynamic imports into the deleted modules. Then remove the helpers that exist only to serve it —
    `formatToolsOutput`, and any local `die`-style helper with no remaining caller. Verify emptiness by
    grep before removing each one rather than by inspection. Every export another module imports must
    survive untouched: at minimum `call`, `callTool`, `activeInstance`, `useInstance`, `mcpHost`,
    `DENY_LIST`, `EPOCH_FILE`, `readEpoch`, `beginSession`, `sessionReconnects`, `lastToolCall`,
    `assertSameMachine`, `serverInfo`, `ViceError` and `MachineRestartedError`. Drop any import at the
    head of the file that only the deleted block used.

    **Sweep the references.** Grep the module directory for the deleted module basenames. Comments that
    describe a deleted module in the present tense, as though a reader could go and read it, are
    corrected — `vice-broker-client.mjs` (several, in its header and around its read helpers),
    `repo-root.mjs` (its header names the deleted modules as current derivers of the state directory)
    and `vice.mjs`'s own header. Rationale-history references may stay where a past-tense rewording is
    the whole fix; a sentence that would send a reader to a nonexistent file may not. Do not touch
    `hostLaunchInstructions()` in `install-resources.mjs`: it names the supervisor and the bash broker,
    neither of which is deleted here.

    **Shrink the two gates plan 03 authored.** Remove the deleted launcher from the shell-script
    allowlist constant in `host-scripts.test.mjs`, and remove its deployed-path line from `.gitignore`.
    Both gates should go red first and green after — that is them doing their job, and the SUMMARY should
    say whether they did.

    **Let the prune remove the deployed copy.** Do not delete `tools/vice-pool.sh` by hand. Plan 03's
    prune step removes it on the next deploy, which any suite run triggers. Confirm afterwards that it is
    gone, because that confirmation is criterion E's only end-to-end evidence in this group.

    Do not convert any module to TypeScript in this task. Do not touch `resources/vice-supervisor.sh` or
    `resources/vice-broker.sh`.
  </action>
  <verify>
    <automated>node --test '.claude/mcp/vice/*.test.*'</automated>
  </verify>
  <acceptance_criteria>
    - The four deleted paths do not exist: `test ! -e .claude/mcp/vice/vice-pool.mjs`,
      `… vice-session.mjs`, `… vice-pool.test.mjs`, `… resources/vice-pool.sh` all succeed.
    - `grep -rnE "from[[:space:]]+[\"'][./]*vice-(pool|session)(\.[a-z]+)?[\"']" --include="*.mjs" --include="*.ts" --include="*.mts" .claude/mcp/vice`
      returns nothing. The pattern matches import specifiers only, deliberately: a past-tense comment
      naming a deleted module is allowed and must not fail this gate.
    - `grep -n "process.argv" .claude/mcp/vice/vice.mjs` no longer reports the command-line guard.
    - `node -e "import('./.claude/mcp/vice/vice.mjs').then(m => { for (const n of ['call','useInstance','DENY_LIST','readEpoch','beginSession','mcpHost','activeInstance','serverInfo','EPOCH_FILE','MachineRestartedError']) if (!(n in m)) { console.error('missing ' + n); process.exit(1); } })"`
      exits 0.
    - `tools/vice-pool.sh` no longer exists after a full suite run.
    - `node --test '.claude/mcp/vice/*.test.*'` exits 0, and the SUMMARY records the count arithmetic:
      the 268 baseline, plus plan 02's rescued assertions, plus this group's new tests, minus the 76
      deleted with `vice-pool.test.mjs`.
    - `git status --porcelain` clean.
  </acceptance_criteria>
  <done>
    The registry-keyed lease subsystem and the command-line surface that depended on it are gone, no
    surviving module imports them, and every guarantee the deleted test file held for a surviving module
    is still asserted somewhere.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the reversal where a later reader meets the old decision</name>
  <files>
    .planning/notes/one-process-broker-in-typescript.md,
    .planning/ROADMAP.md,
    .planning/STATE.md
  </files>
  <read_first>
    - `.planning/ROADMAP.md` line 253 — the paragraph holding `D-1.2-A` through `D-1.2-J`. `D-1.2-B` is one sentence inside it.
    - `.planning/STATE.md` line 243 — the recorded pool workflow, and line 15 of `ROADMAP.md` for the standing constraint that already describes the replacement.
    - `.planning/notes/one-process-broker-in-typescript.md` — its existing decision-record shape, so the amendment matches the document rather than being bolted on.
    - `.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-CONTEXT.md` D-02, D-03, D-05.
  </read_first>
  <action>
    Close criterion C. A locked decision reversed without a record is the failure this project already
    names for confidence grades, and the fix is the same shape: the record goes where the old claim
    lives, not only where the new one does.

    **Use `Edit` for all three files, never `Write`.** `ROADMAP.md` and `STATE.md` are long, shared
    documents; a whole-file rewrite destroys content outside the diff window.

    In `.planning/notes/one-process-broker-in-typescript.md`, add a dated override record in the
    document's existing decision style. It must state: what `D-1.2-B` said, verbatim and in quotes; that
    D-02 and D-05 reverse it; the reasoning, which is that Phase 1's 2026-08-01 replan rewrote plan 01-04
    to go through the emulator tools exclusively and thereby removed the consumer `D-1.2-B` existed to
    protect; the date; the fact that the container's evidence topped out at MEDIUM and was promoted to
    HIGH only by a blocking human confirmation, with that confirmation's verdict quoted; and what was
    deleted, by name and line count. Also record D-05 as part of the same reversal — that the
    command-line surface went with the subsystem it depended on — since no source artifact before
    2026-08-02 carried it.

    In `.planning/ROADMAP.md`, annotate `D-1.2-B` in place, inside the line-253 paragraph. A short,
    dated, inline note is enough: that this decision is **overridden as of 2026-08-02 by Phase 01.6's
    D-02 and D-05**, with a pointer to the design note's override record. Do not rewrite the surrounding
    decisions and do not delete the original `D-1.2-B` text — the point is that a reader meeting the old
    decision is told immediately that it no longer holds, which requires the old text to still be there.

    In `.planning/STATE.md`, correct the entry at line 243 that records `tools/vice-pool.sh start N` and
    the session-acquire command as a live host workflow. Mark it superseded, dated, naming what replaced
    it — Phase 01.2's on-demand broker, which the standing-constraints table already describes — and
    stating that the script and the command-line surface it names were deleted on 2026-08-02. Keep the
    original text: this file's value is its record of when something stopped being true, so the entry is
    annotated in place rather than rewritten to look as though it always said the new thing.

    Do not touch `.planning/RE-FINDINGS.md`: nothing in this plan is a reverse-engineering finding.
  </action>
  <verify>
    <automated>grep -c "D-1.2-B" .planning/notes/one-process-broker-in-typescript.md .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/notes/one-process-broker-in-typescript.md` contains a dated override record naming
      `D-1.2-B`, quoting it, giving the reasoning and the date, and quoting the checkpoint verdict.
    - `.planning/ROADMAP.md` line 253's paragraph still contains the original `D-1.2-B` sentence AND an
      inline dated override note pointing at the design note.
    - `.planning/STATE.md`'s pool-workflow entry still contains its original text AND a dated superseded
      annotation naming the replacement and the deletion.
    - `git diff --stat .planning/ROADMAP.md` shows a change confined to the Phase 01.2 decision paragraph
      — no other phase section is touched.
    - `node --test '.claude/mcp/vice/*.test.*'` exits 0.
  </acceptance_criteria>
  <done>
    A reader who meets `D-1.2-B`, or who follows `STATE.md` to the pool workflow, is told at that spot
    that it was reversed, when, why, and by whom — instead of discovering the contradiction by running a
    script that no longer exists.
  </done>
</task>

</tasks>

<threat_model>
`security_asvs_level: 1` · `block_on: high`. Per `01.6-RESEARCH.md § Security Domain`: V2, V3 and V6
Not Applicable. **V4 partial** — no mode is widened; the deletion removes a deployed executable from
the host rather than adding one. **V5 applies** — the never-throw readers that survive in
`vice-broker-client.mjs` must not be disturbed by the comment sweep.

## Trust Boundaries

| Boundary | Description |
|---|---|
| container's view of the repository → the host's real automation | The boundary the checkpoint exists to cross. Everything the container can check is inside it; the decisive evidence is not. |
| deleted source → deployed host executable | The deployed copy is removed by the prune, not by hand — a host executable's removal must go through the audited path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-01.6-17 | Denial of Service | deleting a subsystem a host workflow depends on | high | mitigate | The blocking, non-auto-approvable `checkpoint:decision` in task 1, which asks about host automation and habitual manual invocation by name, and surfaces the one repository record showing the workflow was actually used. |
| T-01.6-18 | Denial of Service | `vice.mjs` losing an export a surviving module imports | high | mitigate | The CLI block is cut at a known boundary; every surviving importer's exact import list is in `read_first`; and an acceptance criterion imports the module and asserts each named export is present. |
| T-01.6-19 | Tampering | a stale deployed launcher surviving on the host | medium | mitigate | Removal goes through plan 03's manifest-scoped prune rather than a hand deletion, and an acceptance criterion confirms the deployed copy is gone after a suite run. |
| T-01.6-20 | Repudiation | a locked decision reversed with no record | high | mitigate | Task 3. The record lands in three places: the design note, the ROADMAP paragraph where the original decision lives, and the state file whose entry would otherwise send a reader to a deleted script. |
| T-01.6-21 | Denial of Service | the comment sweep disturbing a never-throw reader | medium | mitigate | The sweep is explicitly comment-only in `vice-broker-client.mjs` and `repo-root.mjs`; the suite, including plan 02's rescued never-throws assertions, is the check. |
</threat_model>

<verification>
- `node --test '.claude/mcp/vice/*.test.*'` exits 0.
- The four deleted paths are absent; `tools/vice-pool.sh` is absent after a suite run.
- No `.mjs`, `.ts` or `.mts` file in the repository names a deleted module in an import specifier.
- `git ls-files -- '*.sh'` returns seven paths: the five in the allowlist constant plus the two
  container-provisioning scripts.
- The suite total equals the recorded arithmetic and the SUMMARY states it as a deletion.
</verification>

<success_criteria>
Criterion B is met: the subsystem and the CLI that depends on it are deleted, and the deletion landed
only after a blocking human confirmation that no host-side automation or documented manual workflow
invokes them. Criterion C is met: the `D-1.2-B` reversal is recorded in the design note, annotated
inline where the original decision lives, and swept out of the state file that documented the retired
workflow. This group's contribution to criterion E is proven end to end by the deployed copy
disappearing through the prune.
</success_criteria>

<output>
Create `.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-04-SUMMARY.md` when done.
Record: the checkpoint verdict verbatim; the test-count arithmetic from the 268 baseline to the final
number; whether the ignore-set parity gate and the shell-script allowlist went red before they went
green; and the confirmation that the deployed launcher copy was removed by the prune rather than by hand.
</output>
