# Phase 01.6: One Broker Application in TypeScript - Context

**Gathered:** 2026-08-02
**Status:** Ready for planning
**Source:** Decisions taken in-session during `/gsd-plan-phase 01.6 --chain`, after RESEARCH.md
(commit `fb1ec01`) surfaced two questions that change what gets planned. This file is **not** the
product of `/gsd-discuss-phase` — the developer chose to plan from the ROADMAP + design note, and
these are the decisions that arose anyway. Recorded so they outlive the session and so the
decision-coverage gate can enforce them.

<domain>
## Phase Boundary

One TypeScript application, running as one process, owns broker coordination, per-instance
supervision and the pool, and holds all its state in a single place — with exactly one shell script
left in the repo and **no protocol change**.

The transport/control-plane change is **Phase 01.7** and is out of scope here. The phase boundary,
the ten success criteria and the conversion inventory are fixed by `ROADMAP.md § Phase 01.6`; the
locked design decisions D-1/D-2/D-2a–d/D-3 are fixed by
`.planning/notes/one-process-broker-in-typescript.md`. This file adds only what was decided in
session on 2026-08-02.

</domain>

<decisions>
## Implementation Decisions

### MCP library adoption

- **D-01: Adopt the Mastra MCP packages (`@mastra/mcp`) in this phase.** The developer's decision,
  taken *after* being shown RESEARCH.md §B's HIGH-confidence recommendation against it. The
  recommendation is overridden, not unread. What the planner must therefore carry:
  - `@mastra/mcp`'s `MCPServer` peer-depends on `@mastra/core` (~59 MB unpacked, 33 direct
    dependencies — verified via `npm view`). This is the first runtime dependency this repo has ever
    had.
  - ROADMAP criterion 3 locks **"the host needs `node` and never `tsc` or `npm`."** That criterion is
    not relaxed. So the dependency must be **bundled/vendored into the emitted JS** that
    `install-resources.mjs` deploys — a bundling strategy is a required deliverable of this phase,
    not an optional nicety.
  - Research measured that a generic MCP SDK replaces only ~150–300 of `vice-proxy.mjs`'s 2,441
    lines (~6–12%: JSON-RPC framing, `handleInitialize()`, the `handleToolsCall()` routing skeleton).
    The remaining ~88–94% — broker leasing, epoch/liveness, recycle/diagnose, deny-list enforcement,
    path rewriting, incident capture — is untouched by any SDK and must keep working across the
    swap. Plan the seam, not a rewrite.
  - Dependency legitimacy verification is part of the work, not an assumption.

### Scope of the conversion inventory

- **D-02: `vice-pool.mjs`, `vice-pool.sh` and `vice-session.mjs` are deleted outright**, along with
  their tests — not converted, not preserved inert. The developer's decision against research's
  more conservative reading. Research established (MEDIUM confidence) that these are a **separate,
  older `registry.json`/lease subsystem** built for Phase 1's non-MCP recovery pipeline, with zero
  live importers outside the module tree and its own tests; the broker's *real* pool is its own
  warm-spares mechanism (`SPARES_DIR` / `maintain_spares()`), which does fold into the one process
  as D-1 intends.
  - **Precondition the planner must schedule before the deletion lands:** promote "zero live
    consumers" from MEDIUM to HIGH. The container cannot see host-side automation or a documented
    manual workflow that invokes them. This is a gate task, not a formality — it is the one thing
    standing between this decision and silently stranding Phase 1's dormant recovery pipeline.

- **D-03: The Phase 01.2 `D-1.2-B` contradiction is resolved in writing, not left silent.**
  `D-1.2-B` states these files are *"left entirely untouched… 'superseded in intent' is not 'safe to
  delete'."* D-02 overrides it. The override, its reasoning (Phase 1's 2026-08-01 replan rewrote
  plan 01-04 to go through `mcp__vice__*` tools exclusively, removing the consumer D-1.2-B was
  protecting), and its date must be recorded where a later reader meets the old decision — a
  ROADMAP annotation and/or an amendment to
  `.planning/notes/one-process-broker-in-typescript.md`. A locked decision reversed without a
  record is the failure mode this project already names for confidence grades.

### Claude's Discretion

Explicitly delegated to research + the planner rather than decided by the developer:

- The concrete `resources/` build topology and `tsconfig` shape (RESEARCH.md §A3, §F).
- Whether the test suite compiles or runs TypeScript directly (RESEARCH.md §A2 answers this:
  container Node v24.18.1 strips types natively and unflagged, verified live).
- The slicing strategy that keeps the suite green continuously (RESEARCH.md §A5).
- The Node concurrency idiom that preserves the single `in_flight` owner (RESEARCH.md §D2).
- Destinations for `lib/container-guard.sh` and `lib/repo-root.sh` (ROADMAP criterion 6 requires the
  answer be deliberate, not that it be any particular answer).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### The phase's own design record
- `.planning/notes/one-process-broker-in-typescript.md` — **read first.** The 2026-08-02 scope
  amendment. Locked decisions D-1 (one process), D-2/D-2a–d (TypeScript, all of
  `.claude/mcp/vice/`, conversion runs first, one surviving shell script, in-process state),
  D-3 (SIGKILL orphans accepted). Its `## Open questions` section is what RESEARCH.md answers.
- `.planning/ROADMAP.md` § `### Phase 01.6:` — the ten success criteria and the conversion
  inventory table. The acceptance bar.
- `.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-RESEARCH.md` — §A1 (the
  `vice-pool` finding behind D-02), §B (the Mastra evaluation D-01 overrides), §C (host Node
  version mechanics), §D1–D4 (one-process runtime design), §E (the TDZ hazard, live-reproduced),
  §F (the `resources/` inversion), §G (re-measured inventory — zero drift from the ROADMAP).

### Boundary — read for scope awareness, do NOT implement
- `.planning/notes/broker-control-plane-over-tcp.md` — informs **Phase 01.7**, not this phase.
- `.planning/seeds/broker-restart-reaps-and-voids.md` — likewise 01.7.

### Project rules that bind this phase
- `./.claude/CLAUDE.md` § Emulator Access — `mcp__vice__*` is the only route to the emulator; no
  script, module, test or driver may open its own connection. Also the source of the
  `resources/`-is-authored-source rule that ROADMAP criterion 3 requires this phase to invert.
- `.claude/mcp/vice/install-resources.mjs` — carries the criterion-10 load-order hazard verbatim
  (*"Do not 'clean this up' by adding `import { repoRoot } from './repo-root.mjs'` here"*).
  RESEARCH.md §E reproduced the crash live and found the nuance: it fires only on a top-level
  synchronous call, so **only a static source-text check catches it categorically** — a runtime
  test does not.

</canonical_refs>

<specifics>
## Specific Ideas

- Developer, on the shape of the result: *"Nice and simple, no mixture of different things — the
  broker becomes a server that holds everything it needs to know in a single place."*
- Developer, on why one process rather than one codebase with subcommands: *"the broker has direct
  access to everything and can act fast on requests."*
- Developer, on Mastra: *"i would use the mastra mcp packages"* — reaffirmed after the cost was put
  in front of them.

</specifics>

<deferred>
## Deferred Ideas

- `tools/*.mjs` (~4,900 lines) and `.claude/skills/*/scripts/*.mjs` (~811) stay `.mjs` — a separate
  concern with its own lifecycle, filed as a follow-up (D-2a).
- `.planning/spikes/**` is **never** converted — frozen records of completed experiments; rewriting
  them destroys evidence.
- `.devcontainer/*.sh` is container provisioning, untouched.
- The TCP control plane, retirement of the file protocol, and unconditional reap-on-startup are
  **Phase 01.7**.

</deferred>

---

*Phase: 01.6-broker-in-node-and-tcp-control-plane*
*Context gathered: 2026-08-02*
