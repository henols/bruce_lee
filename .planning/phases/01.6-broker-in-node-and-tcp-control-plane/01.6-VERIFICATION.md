---
phase: 01.6-broker-in-node-and-tcp-control-plane
verified: 2026-08-03T00:00:00Z
status: human_needed
score: 12/14 must-haves verified
behavior_unverified: 0
overrides_applied: 0
scope_note: >
  This phase was split four ways at plan time (01.6 -> 01.6.1 -> 01.6.2 -> 01.6.3). This report
  verifies GROUP 1 (01.6) ONLY, per ROADMAP.md's ownership table: C1 (mechanism), C3 (mechanism),
  C9, the C6/C10 scaffolds, this group's own criteria A-E, and CONTEXT decisions D-02/D-03/D-05.
  C2, C4, C5, C7, C8-in-full, D-01, D-04, D-06 belong to 01.6.1/01.6.2/01.6.3 and are explicitly
  out of scope here -- their absence is not a gap in this group.
behavior_unverified_items:
  - truth: "The host's real `node --version` is recorded in broker.json on the first host-side launch of vice-launcher.sh"
    test: "On the real host, run vice-launcher.sh (deployed to tools/) and confirm the produced broker.json record's node_version field equals that host's actual `node --version`"
    expected: "broker.json exists with pid/started_at/node_version keys, and node_version matches the host's real installed Node"
    why_human: "lib/container-guard.sh makes vice-broker.mjs/vice-launcher.sh refuse to run inside this devcontainer by design; the host is unreachable from this container by construction (not a test gap -- a structural limitation the phase itself names as unverifiable from here)."
  - truth: "vice-launcher.sh's `exec node` reaches a working host node and starts the emitted artifact"
    test: "On the real host, invoke the deployed tools/vice-launcher.sh (no flags) and confirm it execs into node and vice-broker.mjs runs to completion"
    expected: "The launcher's container guard passes on the host, `exec node tools/vice-broker.mjs --repo-root <root>` runs, and the process exits 0 having written a broker record"
    why_human: "Same structural reason as above -- the container guard refuses this exact invocation inside the devcontainer, so it cannot be exercised from here regardless of test design."
human_verification:
  - test: "On the real host (not this container), run the deployed `tools/vice-launcher.sh` with no flags and inspect the resulting `broker.json`"
    expected: "The launcher execs into `node tools/vice-broker.mjs --repo-root <root>`, the process runs to completion, and the written record's `node_version` field equals the host's actual `node --version` output"
    why_human: "`lib/container-guard.sh` makes this refuse to run inside the devcontainer by design (verified live below); `x64sc` and its MCP listeners are all host-side, so the container cannot verify anything about the host by construction. This is exactly the class of truth PLAN 01 marked `verification: backstop` and the phase's own verification-hazard note names as unclosable from inside the container."
---

# Phase 01.6: Conversion Foundation and Scope Reduction — Verification Report

**Phase Goal:** The TypeScript build pipeline is proven end-to-end on one real module, and the
conversion's scope is reduced to what will actually be converted — with the `vice-pool` subsystem
deleted, its removal gated on confirmation, and the locked decision it reverses recorded in writing.

**Verified:** 2026-08-03
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (this group's ownership only)

| # | Truth | Owning Criterion | Status | Evidence |
|---|---|---|---|---|
| 1 | The whole TS build topology runs end-to-end on one real module (authored `.mts` → `tsc` → banner-marked committed JS → installer → host `tools/` → container-guarded launcher → bare `node`) | A | ✓ VERIFIED | `node --test .claude/mcp/vice/vice-broker-launch.test.ts` — 11/11 pass, including live execution of the emitted artifact under bare `node` from a `node_modules`-free temp dir. `resources/vice-broker.mjs` carries the generated banner (verified by reading its first lines). Imports are `node:fs`/`node:path`/`node:url` only (grep-confirmed). |
| 2 | The `resources/`-in-sync gate genuinely fails on a stale build | A / C3 | ✓ VERIFIED | `node --test .claude/mcp/vice/resources-sync.test.ts` — 2/2 pass. SUMMARY 01 records a live demonstration (byte appended → FAIL → rebuild → PASS); mechanism confirmed by reading the test (drives the real `build()` entry point into a scratch dir and does a two-way byte comparison, not a re-implementation). |
| 3 | `./.claude/CLAUDE.md`'s directory-meaning inversion landed: no longer instructs editing host shell scripts in `resources/`, names three tiers | C3 | ✓ VERIFIED | Read `.claude/CLAUDE.md` lines 202–214 directly: "Three tiers exist... **authored TypeScript**... **`resources/` is generated, but committed**... **`tools/` remains generated and gitignored**." No instruction anywhere to hand-edit `resources/` shell scripts as source; the one exception (`vice-launcher.sh`, hand-authored) is stated explicitly. |
| 4 | `install-resources.mjs` deploys new modules with no code change to the deploy walk (C9) | C9 | ✓ VERIFIED | `.claude/mcp/vice/tools/vice-broker.mjs` and `.../vice-launcher.sh` exist as deployed artifacts; `install-resources.test.mjs` (19/19 pass) covers install-when-missing derived from `resourceEntries()`, no hardcoded filename list. |
| 5 | `vice-pool.mjs`, `vice-pool.sh`, `vice-session.mjs`, `vice.mjs`'s CLI, and their tests are deleted, behind a blocking confirmation | B, D-02, D-05 | ✓ VERIFIED | All four paths confirmed absent on disk (`test ! -e`, live-checked); `tools/vice-pool.sh` confirmed absent (pruned, not hand-deleted — see #9 below); `grep -rnE "from...vice-(pool\|session)"` across `.claude/mcp/vice` returns zero matches; `grep -n process.argv vice.mjs` returns nothing (CLI gone); Plan 04's SUMMARY records the checkpoint verdict verbatim (`delete`, re-confirmed 2026-08-03) — this is the class of verdict this workflow requires a human record for, and it exists. |
| 6 | Library exports survive the CLI deletion; no importer breaks | B | ✓ VERIFIED | Live-checked: `node -e "import('./vice.mjs')..."` confirms all 10+ named exports (`call`, `useInstance`, `DENY_LIST`, `readEpoch`, `beginSession`, `mcpHost`, `activeInstance`, `serverInfo`, `EPOCH_FILE`, `MachineRestartedError`) are present. All three surviving importers (`vice-proxy.mjs`, `vice-sync.mjs`, `refresh-manifest.mjs`) load without error when imported directly. |
| 7 | The Phase 01.2 `D-1.2-B` override is recorded in writing where a later reader meets the old decision | C, D-03 | ✓ VERIFIED | Confirmed live in all three locations: (a) `.planning/notes/one-process-broker-in-typescript.md` — new `## D-1.2-B override, 2026-08-02` section quoting the old text, the reasoning, and the checkpoint verdict; (b) `.planning/ROADMAP.md` line 253 — the original `D-1.2-B` sentence still present, with an inline `**[OVERRIDDEN 2026-08-02 ...]**` annotation appended in place; (c) `.planning/STATE.md`'s pool-workflow bullet — original text preserved, with a `**[SUPERSEDED 2026-08-02]**` sentence appended naming the replacement and the deletion. Plan 04's SUMMARY had flagged these last two as pending orchestrator application; **confirmed they were in fact applied** (not merely drafted) by reading both files directly. |
| 8 | `.gitignore` names deployed files individually (never a wholesale `tools/` ignore); two-way parity gate exists; one-shell-script check uses `git ls-files` | D | ✓ VERIFIED | `.gitignore` lines 99–114 list six individual paths plus the new manifest, no directory-wide ignore. `node --test host-scripts.test.mjs` — 5/5 pass, including the two-way parity test and the `git ls-files`-based structural test. Live-confirmed: `git ls-files -- '*.sh'` returns exactly 7 paths (5 under `resources/`, 2 under `.devcontainer/`) — `vice-pool.sh` is gone, matching the post-deletion count. |
| 9 | `.mcp.json`'s `vice-proxy.mjs` path resolves to an existing file | D | ✓ VERIFIED | `.mcp.json` names `.claude/mcp/vice/vice-proxy.mjs`; file confirmed present on disk. `vice-mcp-selector-docs.test.mjs`'s tightened basename assertion passes as part of the full suite. |
| 10 | `install-resources.mjs` has a manifest-scoped prune that cannot remove tracked RE tooling; `tools/vice-pool.sh` is gone via prune, not hand deletion | E | ✓ VERIFIED | `pruneResources()`/`readDeployManifest()`/`writeDeployManifest()` present in `install-resources.mjs`; `install-resources.test.mjs` (19/19 pass) includes the "untracked file protection" test proving a file absent from the manifest is never touched. `tools/vice-pool.sh` confirmed absent after a full suite run — SUMMARY 04 states explicitly it was removed by the prune, not `rm`'d by hand, and the mechanism (manifest-diff, never a directory walk) structurally cannot reach `tools/*.mjs` RE tooling since those were never in the manifest. |
| 11 | A static source-text check exists for the C10 TDZ hazard; does not false-positive on the forbidden import quoted verbatim in `install-resources.mjs`'s own header comment; the pre-existing cycle is left intact and documented | C10 scaffold | ✓ VERIFIED | `node --test .claude/mcp/vice/load-order.test.mjs` — 4/4 pass. Read `install-resources.mjs`'s header comment directly: it quotes `import { repoRoot } from "./repo-root.mjs"` verbatim in prose, and the guard test still passes (statement-anchored regex, confirmed by reading the pattern). Cycle allowlist records exactly the one `hostpath.mjs → install-resources.mjs → repo-root.mjs` cycle, unchanged, not "tidied." |
| 12 | The suite is green across the phase's arc, and the final count is arithmetic, not a regression | C8 (inherited) | ✓ VERIFIED | Ran `node --test '.claude/mcp/vice/*.test.*'` live: **247 passing, 0 fail**, ~35s. Arc confirmed against each SUMMARY: 268 (baseline) → 281 (plan 01, +13 tracer tests) → 309 (plan 02, +28 rescued) → 323 (plan 03, +14 gates) → 247 (plan 04, −76 for the deleted `vice-pool.test.mjs`). 323 − 76 = 247 exactly. `git status --porcelain` clean. |
| 13 | The host's real `node --version` is recorded in `broker.json` on the first host-side launch of `vice-launcher.sh` | C1 (backstop) | ⚠️ PRESENT, unverifiable from container | See Human Verification below. The mechanism is present and tested for correctness of the record shape (three keys, correct types) but the actual host launch cannot be exercised from this container by construction. |
| 14 | `vice-launcher.sh`'s `exec node` reaches a working host node and starts the emitted artifact | C1 (backstop) | ⚠️ PRESENT, unverifiable from container | Same as above — `container-guard.sh` refuses to run this in-container by design (live-confirmed: running the launcher here exits 2 with the guard's refusal). |

**Score:** 12/14 truths verified (2 present-but-unverifiable-from-container, correctly marked backstop rather than passed or failed)

### Explicitly Out of Scope for This Group (not gaps)

Per `ROADMAP.md`'s ownership table and the phase's own split, the following are **not** evaluated here
and their absence from this report is correct, not a shortfall:
- C2, C4, C5 (the one-process broker itself, in-process state, SIGKILL-orphan posture) — 01.6.2
- C7 (duplicated request-id regex deletion) — 01.6.2
- C8 in full (the *complete* conversion suite parity across the whole 16,000-line move) — 01.6.1/01.6.2
- D-01 (`@mastra/mcp` adoption), D-04 (epoch file survives as second on-disk exception), D-06 (bundling decision) — 01.6.3/01.6.2, tagged `[deferred]` in `01.6-CONTEXT.md`
- "The tool surface a session sees is unchanged" — this phase runs before Phase 01.4 by design; there is no live-session verification available at all. `01.6-VALIDATION.md` states this explicitly and the ROADMAP forbids treating it as closable by adding more tests of the same shape. Its absence from this group's must-haves is correct.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.claude/mcp/vice/package.json` / `package-lock.json` | First npm toolchain | ✓ VERIFIED | Present; `typescript@7.0.2`, `@types/node@24.13.3`, both exact-pinned. |
| `.claude/mcp/vice/tsconfig.json` / `tsconfig.build.json` | Typecheck-only + emit projects | ✓ VERIFIED | Present; `npx tsc --noEmit -p tsconfig.json` exits 0. |
| `.claude/mcp/vice/build.ts` | Build script | ✓ VERIFIED | Present, exercised by `resources-sync.test.ts` and directly. |
| `.claude/mcp/vice/vice-broker.mts` | Tracer module | ✓ VERIFIED | Present, `node:*`-only imports confirmed. |
| `.claude/mcp/vice/resources/vice-broker.mjs` | Generated, banner-marked | ✓ VERIFIED | Present, banner confirmed by reading file head. |
| `.claude/mcp/vice/resources/vice-launcher.sh` | Hand-authored, container-guarded | ✓ VERIFIED | Present, container guard confirmed by reading source and by test (exits 2 in-container). |
| `.claude/mcp/vice/load-order.test.mjs` | TDZ guard + cycle allowlist | ✓ VERIFIED | Present, 4/4 tests pass. |
| `.planning/notes/one-process-broker-in-typescript.md` | D-1.2-B override record | ✓ VERIFIED | Present, section confirmed by direct read. |
| `.claude/mcp/vice/vice-pool.mjs`, `vice-session.mjs`, `vice-pool.test.mjs`, `resources/vice-pool.sh` | Deleted | ✓ VERIFIED ABSENT | All four confirmed absent on disk. |

### Data-Flow / Deployment Trace

| Artifact | Data Source | Produces Real Output | Status |
|---|---|---|---|
| `resources/vice-broker.mjs` | `build.ts` compiling `vice-broker.mts` via pinned `tsc` | Yes — live rebuild produces byte-identical output to committed file | ✓ FLOWING |
| `tools/vice-broker.mjs`, `tools/vice-launcher.sh` | `install-resources.mjs`'s directory-listing walk of `resources/` | Yes — both present on disk in `tools/`, correct executable bits | ✓ FLOWING |
| `tools/vice-pool.sh` (retired) | `pruneResources()`'s manifest-diff | Correctly absent — removed by prune on next deploy, confirmed live | ✓ FLOWING (correct removal) |

### Requirements Coverage

This is a tooling phase with no REQ IDs (per ROADMAP's standard for v1.1 phases). Criteria coverage
is the requirement-equivalent register here and is covered in the Observable Truths table above.

### Anti-Patterns Found

None. Scanned all files this group's plans modified/created (`package.json`, `tsconfig*.json`,
`build.ts`, `vice-broker.mts`, `resources/vice-broker.mjs`, `resources/vice-launcher.sh`,
`vice-broker-launch.test.ts`, `resources-sync.test.ts`, `install-resources.mjs`, `load-order.test.mjs`,
`host-scripts.test.mjs`, `vice.mjs`, `vice-broker-client.mjs`, `repo-root.mjs`) for `TBD`/`FIXME`/`XXX`
and `TODO`/`HACK`/`PLACEHOLDER` — zero matches. No stub `return null`/empty-object patterns found in
the reviewed files; every exported function's implementation was read and is substantive.

### Behavioral Spot-Checks / Probe Execution

| Behavior | Command | Result | Status |
|---|---|---|---|
| Emitted artifact runs to completion under bare `node`, no `node_modules` | `node --test vice-broker-launch.test.ts` | 11/11 pass | ✓ PASS |
| `resources/` sync gate catches staleness | `node --test resources-sync.test.ts` | 2/2 pass | ✓ PASS |
| TDZ/load-order guard, no false positive on header comment | `node --test load-order.test.mjs` | 4/4 pass | ✓ PASS |
| `.gitignore`/deployed-set parity, `git ls-files` shell-script gate | `node --test host-scripts.test.mjs` | 5/5 pass | ✓ PASS |
| Prune mechanism, manifest safety rails | `node --test install-resources.test.mjs` | 19/19 pass | ✓ PASS |
| Full suite, arithmetic reconciled | `node --test '.claude/mcp/vice/*.test.*'` | 247/247 pass, 0 fail | ✓ PASS |
| Deleted-subsystem import scan | `grep -rnE "from...vice-(pool\|session)"` | 0 matches | ✓ PASS |
| Library exports survive CLI deletion | `node -e "import('./vice.mjs')..."` | all present | ✓ PASS |
| Deployed `tools/vice-pool.sh` absent (pruned) | `test ! -e tools/vice-pool.sh` | absent | ✓ PASS |

No emulator was driven and no broker/launcher was run against real VICE or on the actual host, per the
hard rule that `mcp__vice__*` is the only route to the emulator and this phase's host-side artifact is
by design unrunnable from this container.

### Human Verification Required

1. **Test:** On the real host, run the deployed `tools/vice-launcher.sh` (no flags) and inspect the resulting `broker.json`.
   **Expected:** The launcher execs into `node tools/vice-broker.mjs --repo-root <root>`; the process completes and writes a record whose `node_version` field equals that host's actual `node --version`.
   **Why human:** `lib/container-guard.sh` makes this refuse to run inside the devcontainer by design (confirmed live: running `vice-launcher.sh` here exits 2). `x64sc` and its MCP listeners are host-side only; the container cannot verify anything about the real host by construction. This is precisely PLAN 01's `verification: backstop` pair and must not be marked passed or failed — it is an honest abstention, not a defect.

### Gaps Summary

No gaps found within this group's ownership. Every truth, artifact, and key link this group is
responsible for (criteria A–E, C1/C3 mechanisms, C9, the C6/C10 scaffolds, and decisions D-02/D-03/D-05)
was independently verified against the live codebase — files read directly, tests run live (not
trusted from SUMMARY prose), deletions confirmed absent on disk, import graphs grepped directly, and
the three D-1.2-B override locations read in full rather than assumed applied from the plan-04
SUMMARY's "pending orchestrator action" note (which turned out to have in fact been resolved — both
`ROADMAP.md` and `STATE.md` carry the annotations).

The only reason overall status is `human_needed` rather than `passed` is the two backstop truths
(C1's host-side `node --version` recording, and the launcher reaching a working host node), which are
*by construction* unverifiable from inside this container and are correctly routed to human
verification rather than faked green or marked failed. This is not a defect in the phase's work — it
is the honest outcome the phase's own VALIDATION.md predicted for exactly these two truths.

---

_Verified: 2026-08-03_
_Verifier: Claude (gsd-verifier)_
