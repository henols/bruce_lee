---
phase: 01.4-tool-surface-reachability
plan: 02
type: execute
wave: 2
depends_on:
  - "01.4-01"
files_modified:
  - .planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md
  - .planning/RE-FINDINGS.md
  - .claude/CLAUDE.md
autonomous: true
requirements:
  - "2"
  - "4"

must_haves:
  truths:
    - "This plan's own executing agent has reported, from direct introspection of its own tool list, whether any mcp__vice__* name is present — the first real test of criterion 4 against an actual gsd-executor dispatch under /gsd-execute-phase, not an inference from a different session type (the orchestrator, or a research subagent, both already known to differ)"
    - "If mcp__vice__* tools are present in this dispatch: criterion 4 is recorded as satisfied directly, with this executor's own reported tool names as the evidence"
    - "If mcp__vice__* tools are absent: the session-shape constraint is documented per 01.4-RESEARCH.md's recommendation, with no CLI escape hatch proposed, and every later plan in this phase is put on notice that a dispatched executor may lack these tools"
    - "The bypass route (criterion 2) is named in writing once more, referencing this phase's own live before/after evidence from 01.4-01, not the stale 64-vs-63-tool figures from 2026-08-02"
  artifacts:
    - "01.4-LIVE-EVIDENCE.md — a new dated '## Criterion 4 — subagent tool schema' section recording the self-report outcome"
    - "RE-FINDINGS.md — an entry naming the criterion-2 bypass route in writing, cross-referencing 01.4-01's live evidence"
  key_links:
    - "this dispatch's own granted tool list (whatever gsd-executor.md's current frontmatter yields) -> the self-report task's direct observation -> criterion 4's verdict"
---

<objective>
Close criterion 4 (subagent tool schema) and criterion 2 (bypass route named in writing) for good.

Criterion 4 has never actually been tested against a real `gsd-executor` dispatch since plan 01-04's
executor was found to have exactly five tools (`Read`, `Write`, `Edit`, `Bash`, `Skill`) with no
`mcp__vice__*` entry at all — every subsequent claim about it (the 2026-08-01 `gsd-executor.md` fix,
`01.4-RESEARCH.md`'s Pitfall 4) is inference from a file read, never a live dispatch. This plan's own
task 1 IS that live dispatch, because `/gsd-execute-phase` spawns a normal `gsd-executor` to run it.

Purpose: turn "not probed" into a dated, unambiguous verdict — either "satisfied, here is the
evidence" or "documented constraint, here is what it means for the rest of this phase" — never left
open, per the roadmap's own explicit rejection of a CLI escape hatch as an answer.

Output: a dated self-report in `01.4-LIVE-EVIDENCE.md`; a criterion-2 finding in
`.planning/RE-FINDINGS.md`; conditionally, a new standing note in `.claude/CLAUDE.md`'s Emulator
Access section if the tools are absent.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-tool-surface-reachability/01.4-RESEARCH.md
@.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md
@.planning/phases/01.4-tool-surface-reachability/01.4-01-SUMMARY.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: self-report this executor's own tool surface</name>
  <files>.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md</files>
  <action>
    Before doing anything else in this task, list your own currently available tool names exactly as
    you would name them to call one. Do not call any mcp__vice__* tool to test this — report what is
    present in your own tool list only, the same way 01.4-RESEARCH.md's own research subagent
    confirmed, from its own frontmatter and its own available tools, that it structurally lacked
    mcp__vice__*. State explicitly: (a) whether any name beginning `mcp__vice__` is present, (b) if
    present, list every one by name, (c) if absent, state that plainly rather than inferring presence
    from a different session's unrelated success (01.4-LIVE-EVIDENCE.md's existing evidence was
    gathered by the orchestrating session, not a dispatched gsd-executor — do not treat it as
    covering this question). This dispatch is a normal gsd-executor run under the standard
    /gsd-execute-phase flow, which is exactly the dispatch shape criterion 4 asks about. Append the
    result, dated, into .planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md under a
    new "## Criterion 4 — subagent tool schema" section, following that file's existing per-criterion
    structure (a short recorded-state line, then the finding).
  </action>
  <verify>
    <human-check>Confirm 01.4-LIVE-EVIDENCE.md now has a dated section unambiguously stating this
    executor's own tool list contents with respect to mcp__vice__*.</human-check>
  </verify>
  <done>
    01.4-LIVE-EVIDENCE.md carries a dated, unambiguous statement of this executor's own tool list
    contents re mcp__vice__*.
  </done>
</task>

<task type="auto">
  <name>Task 2: close criterion 4 per the outcome, and close criterion 2 in writing</name>
  <files>.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md, .planning/RE-FINDINGS.md, .claude/CLAUDE.md</files>
  <action>
    Branch on task 1's outcome, in the same "## Criterion 4" section of 01.4-LIVE-EVIDENCE.md:

    If mcp__vice__* was present: record that criterion 4 is satisfied directly — this dispatch is the
    live proof required, no further mitigation needed. Note explicitly that every later plan in this
    phase (01.4-04, 01.4-05) can rely on a gsd-executor having these tools, since this is the first
    confirmed instance of the exact dispatch shape that failed before.

    If mcp__vice__* was absent: write the documented session-shape constraint 01.4-RESEARCH.md
    recommends. Append a short, standing note to .claude/CLAUDE.md's existing "Emulator Access"
    section, matching that section's terse imperative bullet style (e.g. alongside "Compare the
    restart epoch across a bracket..."), stating: a gsd-executor subagent dispatched under
    /gsd-execute-phase does not receive mcp__vice__* tools; the suspected upstream cause
    (anthropics/claude-code#13898-class, MCP tools stripped from a `tools:`-restricted agent); and —
    explicitly — that no CLI escape hatch is an available workaround. State as a planning constraint
    that any later plan in this phase depending on live emulator access must be executed by a session
    that already has the tools (the orchestrating session, not a dispatched executor), and flag this
    for whoever runs 01.4-04 and 01.4-05 next.

    Regardless of which branch fired: append the criterion-2 finding to .planning/RE-FINDINGS.md —
    name the bypass route in writing once more for the historical record (cross-referencing
    01.4-01-SUMMARY.md's live before/after tools_list evidence as the "re-established now"
    confirmation ROADMAP.md's criterion 2 explicitly asked for, superseding the stale 64-vs-63-tool
    figures the 2026-08-02 research recorded).
  </action>
  <verify>
    <human-check>Read back the branch actually taken (present vs. absent) and confirm the
    corresponding artifact exists (either the "satisfied directly" note, or the new CLAUDE.md bullet)
    plus the RE-FINDINGS.md criterion-2 entry.</human-check>
  </verify>
  <done>
    Criterion 4 has an unambiguous, dated verdict — satisfied, or a documented constraint — never left
    as "not probed." Criterion 2's bypass route is named in writing against today's live evidence.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| dispatched subagent -> harness (Claude Code client) | The tool schema a subagent receives is decided entirely client-side, outside this repo's control; this plan can only observe it, never patch it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-02 | (not classic STRIDE — availability/integrity-of-information gap) | harness tool-schema assignment for a dispatched subagent | medium | mitigate | Document the constraint explicitly (task 2's absent-branch) rather than assume presence; require every later plan needing live emulator access to state this dependency rather than silently inherit it |
| T-01.4-04 | Repudiation | criterion 4's own verdict | low | mitigate | Require the self-report to be dated and unambiguous (task 1), so a later reader cannot mistake "not probed" for "satisfied" |
</threat_model>

<verification>
01.4-LIVE-EVIDENCE.md carries a dated, unambiguous criterion-4 verdict. RE-FINDINGS.md carries the
criterion-2 entry. CLAUDE.md is updated only if the absent branch fired.
</verification>

<success_criteria>
Criterion 4 closed with a dated verdict (satisfied or documented constraint). Criterion 2 closed with
a written, evidence-backed statement of the bypass route.
</success_criteria>

<output>
Create `.planning/phases/01.4-tool-surface-reachability/01.4-02-SUMMARY.md` when done.
</output>
