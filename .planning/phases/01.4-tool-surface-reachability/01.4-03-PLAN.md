---
phase: 01.4-tool-surface-reachability
plan: 03
type: execute
wave: 2
depends_on:
  - "01.4-01"
files_modified:
  - .claude/mcp/vice/vice-proxy.ts
  - .claude/mcp/vice/vice-proxy.test.ts
autonomous: true
requirements:
  - "5"

must_haves:
  truths:
    - "No string returned as tools/call content anywhere in vice-proxy.ts contains the word proxy, except inside console.error/stderr-only lines, which stay unchanged"
    - "The unreachable-shaped messages (never started / dead or hung / alive but rejected / broker variants / replaced-machine variants) remain distinguishable from one another and each still names an absolute host path"
    - "Every unreachable-shaped message ends by telling the agent to surface this to the user and stop, rather than implying the agent itself can run the named host-side command"
    - "Epoch-drift and replaced-machine messages lead with the action (treat every result since the prior successful call as void; redo the work) before the supporting evidence (old value -> new value), not after"
    - "Chunking messages state the continuation token and the tool to call with it, without explaining why the split exists"
  artifacts:
    - "vice-proxy.ts — every agent-visible message-rendering function rewritten under the vice: identity, action-leading, re-derived by function name/grep against the CURRENT file rather than 01.4-PATTERNS.md's stale line numbers"
    - "vice-proxy.test.ts — pinned-string assertions re-pinned or loosened to the actionable substring; one new permanent structural regression test asserting no non-console.error backtick literal begins with vice-proxy:"
  key_links:
    - "every message-rendering function -> its call site inside the CallToolRequestSchema override or forwardToVice()'s error branches -> the content string the agent actually reads on failure"
---

<objective>
Execute `.planning/todos/pending/de-architecture-agent-visible-proxy-messages.md` as specified:
rewrite every agent-visible proxy message so it names the next action, not the three-layer
container/proxy/broker/host topology the agent has no model of and cannot act on. The todo's spec is
already complete (four message categories, a stated "Done when," a stated "Deliberately not in
scope"); this plan executes it against the CURRENT code, not the stale line numbers in
`01.4-PATTERNS.md`, which predate 01.6.2's TCP control plane and 01.6.2.1's replaced-machine handling.

`01.4-PATTERNS.md` counted 8 message-rendering functions; the live set today is at least 13 —
`epochDriftMessage`, `recycleAckOutcomeMessage`, `brokerNeverStartedMessage`, `brokerDeadOrHungMessage`,
`brokerLaunchFailedMessage`, `brokerWarmingMessage`, `brokerControlUnreachableMessage`,
`neverStartedMessage`, `deadOrHungMessage`, `aliveButFailedMessage`, `machineReplacedMessage`,
`replacementFailedMessage`, `sessionMustRestartMessage` — plus the chunking messages inline near
`wrapPossiblyChunked()`/`handleResultContinue()`, and several inline `vice-proxy:`-prefixed strings
(path refusals, `containerizeGrant` errors, the internal-error-shape branch). Re-derive by grepping
`function.*Message\(` and the literal sequence `` `vice-proxy `` in vice-proxy.ts; do not trust any
line number cited anywhere in this phase's own artifacts, including this plan.

Purpose: an agent reading a failure message should learn what to do next, not learn the internal
shape of a system it cannot see or reach.

Output: every agent-visible message rewritten; a permanent regression guard preventing the prefix
from silently returning.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/todos/pending/de-architecture-agent-visible-proxy-messages.md
@.planning/phases/01.4-tool-surface-reachability/01.4-RESEARCH.md
@.claude/mcp/vice/vice-proxy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: rewrite every agent-visible message function</name>
  <files>.claude/mcp/vice/vice-proxy.ts</files>
  <behavior>
    - Every rewritten message begins with the `vice:` identity, never `vice-proxy:`.
    - Every unreachable-shaped message keeps its state distinction and its absolute host path, and
      ends with an instruction to surface this to the user and stop.
    - Every epoch/replaced-machine message states the action (void and redo) before the evidence.
    - Every chunking message states the token and the next tool to call, nothing about why the split
      exists.
    - console.error(...) lines and the VICE_SEAM_HAZARDS_TEST_FIXTURE-guarded synthetic hazard line
      are byte-identical to before this task.
  </behavior>
  <action>
    Grep this file for `function.*Message\(` to enumerate the live set (do not rely on the count or
    names listed in this plan's objective as exhaustive — confirm against the current file first).
    Rewrite each per the todo's four categories:

    (1) Unreachable-shaped (the broker-never-started/dead-or-hung/launch-failed/warming/
    control-unreachable family, and the host-never-started/dead-or-hung/alive-but-rejected family,
    plus sessionMustRestartMessage) — preserve the state distinction and the absolute host path, drop
    the prose explaining why a proxy/broker/host layering exists, and — per 01.4-RESEARCH.md's Open
    Question 3 — end every one of these with an instruction to surface this to the user and stop,
    since the named fix is a host-side shell command this agent cannot itself run.

    (2) Path-refusal-shaped messages (the relative/absolute path-refusal branches) — prefix swap
    only, `vice-proxy:` to `vice:`; the todo already judges this wording good.

    (3) Chunking-shaped (the chunk-index/continuation-token messages near wrapPossiblyChunked(), and
    the vice_result_continue tool's own description text) — keep them mechanical, drop the "why the
    split exists" explanation, lead with the token and the tool name to call with it.

    (4) Epoch/replaced-machine-shaped (epochDriftMessage, machineReplacedMessage,
    replacementFailedMessage) — lead with "treat every result since the prior successful call as
    void; redo the work" before the supporting evidence (old epoch/port -> new epoch/port), not after.

    Swap every remaining `vice-proxy:` prefix that does not fit the four categories above (the
    containerizeGrant error-shape branch, recycleAckOutcomeMessage, the internal-error-shape branch
    in the CallToolRequestSchema override) to `vice:` even where no other restructuring applies.

    Leave every `console.error(...)` line completely unchanged — stderr-only, out of scope by the
    todo's own explicit framing. Leave the `VICE_SEAM_HAZARDS_TEST_FIXTURE`-guarded synthetic hazard
    render() text unchanged too — it fires only under that env var and is itself a test artifact, not
    a production message.
  </action>
  <verify>
    <automated>node --test .claude/mcp/vice/vice-proxy.test.ts</automated>
  </verify>
  <done>
    Every message-rendering function identified by the task-time grep uses the `vice:` identity and
    leads with an action per its category. `grep -n '`vice-proxy:' .claude/mcp/vice/vice-proxy.ts |
    grep -v 'console\.error'` returns zero lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: update tests and add the permanent regression guard</name>
  <files>.claude/mcp/vice/vice-proxy.test.ts</files>
  <action>
    Re-pin or loosen every test in vice-proxy.test.ts that asserts an exact old message string,
    following the todo's own guidance to prefer asserting the actionable substring (the host path,
    the continuation token, the argument position) over the surrounding prose — the todo names this
    the likely-better shape, since that substring is what the message is actually for. Add one new
    structural test that greps this file's own source at test time (mirroring the project's existing
    DENY_LIST-regex-based structural test style already in this suite) asserting no backtick template
    literal outside a `console.error(...)` call begins with the literal sequence `vice-proxy:` — a
    permanent anti-regression gate so a future edit cannot silently reintroduce the topology-naming
    prefix. Run the full suite.
  </action>
  <verify>
    <automated>node --test '.claude/mcp/vice/'*.test.*</automated>
  </verify>
  <done>
    Full suite green — no fewer passes than the 437 tests / 432 pass / 0 fail / 5 todo baseline,
    allowing for the new tests this plan and 01.4-01 add. If broker-kill.test.ts's own known
    banner-ordering flake under full-suite load recurs, re-run once before treating it as a
    regression. The new structural guard test passes now, and would fail if a `vice-proxy:` prefix
    were reintroduced (spot-check by temporarily reverting one call site, confirming the guard test
    goes red, then restoring it).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| proxy -> agent (tool-result content) | Every failure the proxy renders is read by the model on the very next turn; this is the only channel through which internal topology could leak into the agent's reasoning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-03 | (not classic STRIDE — usability/safety gap: a model may attempt to "fix" something it cannot reach, or hallucinate an unavailable capability) | every agent-visible message-rendering function in vice-proxy.ts | medium | mitigate | Rewrite every message to lead with the actionable step and end unreachable-shaped ones with "surface to the user and stop"; add a permanent structural regression test (task 2) |
</threat_model>

<verification>
Full suite green. `grep -n '`vice-proxy:' .claude/mcp/vice/vice-proxy.ts | grep -v 'console\.error'`
returns zero lines. The new structural regression test fails when a reintroduction is injected and
passes otherwise.
</verification>

<success_criteria>
Criterion 5 closed: every agent-visible failure message names an action, not a topology; the
unreachable-triple's state distinction and host paths survive; a permanent regression guard exists.
</success_criteria>

<output>
Create `.planning/phases/01.4-tool-surface-reachability/01.4-03-SUMMARY.md` when done.
</output>
