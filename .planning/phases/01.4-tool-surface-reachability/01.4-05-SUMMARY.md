---
phase: 01.4-tool-surface-reachability
plan: 05
subsystem: infra
tags: [carry-forward, validation-ledger, vice-broker, hv-06, hv-08, epoch, container-guard]

# Dependency graph
requires:
  - phase: 01.4-tool-surface-reachability
    provides: "01.4-01's before/after tool-surface evidence, 01.4-02's tool-schema two-tier finding, 01.4-04's vice_recycle/vice_result_continue live proofs — all cited directly against specific carry-forward rows"
provides:
  - "Every one of the 35 CF-01.4-NN carry-forward rows in 01.6.2-VALIDATION.md now carries a real, dated disposition (discharged / blocked-on-HV-08 / permanently-unverifiable-from-container), each with a one-line reason and, where applicable, a citation to this phase's own evidence"
  - "HV-06 (Phase 01.8's register) discharged — the developer action it names was already satisfied before this plan; this plan is the agent-side ledger walk that closes it"
  - "A confirmed, re-read HV-08 staleness proof: .vice-supervisor/broker.json still carries spares_target (the pre-rename key), not warm_floor, as of 2026-08-05"
affects: ["Phase 01.8's HV-06 register entry", "any future HV-08 redeploy session (rows 25-34 need re-dispositioning once broker.json shows warm_floor instead of spares_target)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Three-way exhaustive disposition for an unverifiable-from-container claim: discharged (live/file evidence settles it now) / blocked-on-HV-08 (settles once a named pending redeploy lands) / permanently-unverifiable-from-container (no tool this container has will ever settle it) — applied row-by-row rather than defaulting ambiguous cases to one bucket"
    - "Spot-check before transcribing a pre-worked disposition: re-read the cited live file/comment/SUMMARY at execution time rather than trusting a plan's own placeholder judgment, since time passes between plan authoring and execution"

key-files:
  created: []
  modified:
    - ".planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md — all 35 CF-01.4-NN rows' trailing status cell filled in, replacing every `| open |` with a disposition and evidence-cited reason"

key-decisions:
  - "Followed the plan's own pre-worked disposition for the large majority of rows after spot-checking each of the specific cases the plan flagged as needing a fresh check (rows 02, 03, 04, 05, 11, 12, 18, 19, 22) against live state re-read at execution time -- no correction was needed to any of those; the plan's own analysis held up under spot-check"
  - "Row 18 was read fresh rather than assumed: 01.6.2-REVIEW.md's own coverage-gap analysis (CR-01 through CR-04) is a genuine finding the replacement test suite did not catch at review time, later closed for CR-01 by 01.6.2.1-07 -- discharged citing that real analysis rather than treating the row as unanswerable"
  - "Row 13 (tool surface unchanged by the transport swap) was discharged by a structural check not anticipated in the plan's own placeholder text: every 01.6.2/01.6.2.1 SUMMARY that lists vice-proxy.ts as modified was read, and each change is confined to broker-communication plumbing (ensureBrokerLease/openBrokerControl, RECYCLE_TIMEOUT_MS), never the tool registration/inputSchema definitions an agent session actually sees"
  - "Confirmed .vice-supervisor/broker.json still carries spares_target (not warm_floor) by direct re-read at execution time, before dispositioning any 01.6.2.1-era row -- this is the single fact that determines whether rows 25-34 are blocked-on-HV-08 or already dischargeable, and the plan explicitly warned against skipping this re-check"

patterns-established: []

requirements-completed: ["CF-01.4-NN", "HV-06"]

coverage:
  - id: D1
    description: "Every CF-01.4-01 through CF-01.4-24 row (the 01.6.2-era rows) carries a non-open disposition, each spot-checked against fresh live/file state rather than transcribed"
    requirement: "CF-01.4-NN"
    verification:
      - kind: other
        ref: "grep -E \"CF-01\\.4-(0[1-9]|1[0-9]|2[0-4]) \" 01.6.2-VALIDATION.md | grep -c \"| open |\" == 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every CF-01.4-25 through CF-01.4-35 row (the 01.6.2.1-era rows plus the closing row) carries a non-open disposition; rows 25-34 correctly reflect that .vice-supervisor/broker.json still shows the pre-rename spares_target key"
    requirement: "CF-01.4-NN"
    verification:
      - kind: other
        ref: "grep -E \"CF-01\\.4-(2[5-9]|3[0-5]) \" 01.6.2-VALIDATION.md | grep -c \"| open |\" == 0"
        status: pass
    human_judgment: false
  - id: D3
    description: "HV-06 discharged: all 35 rows split honestly across the three dispositions with no row silently dropped or overclaimed against the stale host deploy"
    requirement: "HV-06"
    verification:
      - kind: other
        ref: "grep -E \"CF-01\\.4-\" 01.6.2-VALIDATION.md | grep -c \"| open |\" == 0 (whole-table gate); tally: 12 discharged / 11 blocked-on-HV-08 / 12 permanently-unverifiable-from-container = 35"
        status: pass
    human_judgment: true
    rationale: "Whether a given row's disposition is the CORRECT one (not merely non-open) rests on judgment calls this plan made against live evidence -- e.g. reading 01.6.2-REVIEW.md's coverage-gap findings, confirming CONTAINER_WORKSPACE_PATH is genuinely container-only, tracing vice-proxy.ts's diff history for the transport-swap claim. A human or a later audit should spot-check a sample of the 35 dispositions against their cited evidence, the same discipline this plan itself applied to the plan's own pre-worked draft."

duration: ~40min
completed: 2026-08-05
status: complete
---

# Phase 01.4 Plan 05: Discharge the CF-01.4-NN carry-forward list (HV-06) Summary

**Walked all 35 `CF-01.4-NN` carry-forward rows in `01.6.2-VALIDATION.md`, spot-checking the plan's own pre-worked disposition against freshly re-read live broker state and source files rather than transcribing it blindly, and gave every row a real, evidence-cited disposition — 12 discharged, 11 blocked-on-HV-08 (confirmed live: `broker.json` still carries the pre-rename `spares_target` key, not `warm_floor`), 12 permanently-unverifiable-from-container — closing Phase 01.8's HV-06 with zero rows left open.**

## Performance

- **Duration:** ~40 min
- **Tasks:** 2/2 completed
- **Files modified:** 1 (`01.6.2-VALIDATION.md`)

## Accomplishments

- Every one of the 35 `CF-01.4-NN` rows now carries a disposition other than `open`: `discharged`, `blocked-on-HV-08`, or `permanently-unverifiable-from-container`, each with a one-line reason and, where applicable, a citation to this phase's own live evidence, a direct `.vice-supervisor/**` file read, or a specific source-code/document citation.
- Confirmed live, before dispositioning any 01.6.2.1-era row, that `.vice-supervisor/broker.json` still carries `spares_target: 3` (the pre-rename key) rather than `warm_floor` — the host has not yet received HV-08's redeploy, so rows 25-34 are correctly `blocked-on-HV-08` rather than tested against stale code and called done.
- Spot-checked the plan's own pre-worked analysis against fresh state for every row the plan itself flagged as needing a re-check (rows 02, 03, 04, 05, 11, 12, 18, 19, 22) — all held up under spot-check, with row 04's `epoch.json` `spawned_at` timestamps re-read against the CURRENT warm instances (ports 6600/6601/6602, not the ones the plan was drafted against) and row 05's legacy `supervisor.log` files re-checked for any new crash entry (none found).
- Read `01.6.2-REVIEW.md` fresh for row 18 rather than assuming the plan's placeholder judgment: it performed exactly the coverage-gap analysis the row asks about and found four real defects (CR-01 through CR-04) the replacement test suite did not catch at review time — a genuine finding, not a formality, later closed for CR-01 by `01.6.2.1-07-SUMMARY.md`.
- Discharged row 13 (tool surface unchanged by the transport swap) by a structural check not spelled out in the plan's own text: read every 01.6.2/01.6.2.1 SUMMARY listing `vice-proxy.ts` as modified and confirmed each change is confined to broker-communication plumbing, never the tool registration/`inputSchema` definitions an agent session actually sees.

## Task Commits

Each task was committed atomically:

1. **Task 1: discharge CF-01.4-01 through CF-01.4-24** — `ef49f7c` (docs)
2. **Task 2: discharge CF-01.4-25 through CF-01.4-35 and close the ledger** — `4e92836` (docs)

**Plan metadata:** SUMMARY.md commit (this file) — created by this same executor per this plan's own constraint that STATE.md/ROADMAP.md commits belong to the orchestrator.

## Files Created/Modified

- `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md` — all 35 `CF-01.4-NN` rows' trailing status cell filled in with a disposition and evidence-cited reason, replacing every `| open |`.

## Decisions Made

- **Re-read live state before dispositioning any HV-08-adjacent row, rather than assuming the plan's drafted state still held.** `.vice-supervisor/broker.json` was re-read at execution time and confirmed to still carry `spares_target` (pre-rename), not `warm_floor` — this single fact governs whether rows 25-34 are `blocked-on-HV-08` or already dischargeable, and the plan's own text explicitly warned against skipping this check.
- **Followed the plan's own pre-worked disposition for the majority of rows after spot-checking, rather than re-deriving all 35 from scratch.** Every row the plan flagged as needing a fresh check (02, 03, 04, 05, 11, 12, 18, 19, 22) was individually re-verified against live state, source, or a cited SUMMARY; none required a correction from the plan's own draft.
- **Row 18 was read fresh, not assumed.** `01.6.2-REVIEW.md`'s own findings (CR-01 through CR-04) are exactly the "coverage-gap analysis" this row asks whether one exists — it does, and it found a real gap (later closed by a different phase's plan), which is a stronger and more useful disposition than a placeholder "check live" note.
- **Row 13 was discharged via a structural argument built during this plan, not merely transcribed.** Rather than accepting the plan's own vaguer "diff today's tool surface against a baseline" instruction at face value, this plan traced every 01.6.2/01.6.2.1 SUMMARY that touched `vice-proxy.ts` and confirmed none of those changes touch tool-schema definitions — a more precise and independently checkable citation than the plan's own draft offered.
- **Rows 20, 32, 33, 34 carry the plan's own stated nuances verbatim** (the installer double-run caveat for row 20; the "may stay unverifiable even post-redeploy" flags for 32/33/34) rather than being simplified to a bare `blocked-on-HV-08` — these nuances are load-bearing for whoever eventually performs HV-08 and should not be silently dropped.

## Deviations from Plan

None — plan executed exactly as written. Both tasks' automated `<verify>` checks (per-range `grep -c "| open |"` == 0) passed on the first attempt after each task's edits landed, and the whole-table gate in the plan's own `<verification>` section (`grep -E "CF-01\.4-" ... | grep -c "| open |"` == 0) also passed.

## Issues Encountered

None. No `mcp__vice__*` forwarded call was needed for this plan — every disposition was settled by citing this phase's own already-gathered live evidence (`01.4-LIVE-EVIDENCE.md`, `01.4-01`/`01.4-02`/`01.4-04`-SUMMARY.md), a direct `Read` of `.vice-supervisor/**` files, or direct source/document inspection, exactly as the plan's decision procedure anticipated for a ledger-closing plan that discharges from prior evidence rather than gathering new evidence.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- HV-06 (Phase 01.8's register) is discharged: the developer action it names ("same live-broker access as HV-04, once") was already satisfied before this plan ran; this plan's own agent-side ledger walk is the remaining work HV-06 named, and it is now complete.
- The 11 `blocked-on-HV-08` rows (25-31, 33, 34, plus 20's HV-08-adjacent nuance) are not closed by this plan and cannot be — they wait on the developer's own HV-08 redeploy. Whoever performs HV-08 should re-read this plan's row-20/32/33/34 nuances before assuming a single redeploy pass automatically produces every row's evidence.
- The 12 `permanently-unverifiable-from-container` rows are final: no future container-side session will discharge them differently, since each names an action (starting/killing a host process, binding a host port, hand-launching an instance, or waiting real wall-clock hours across a session boundary) this container structurally cannot perform.
- **Requirement-ID mapping note**, inherited from `01.4-01`/`01.4-02`/`01.4-04`'s own precedent: this plan's frontmatter `requirements: ["CF-01.4-NN", "HV-06"]` are Phase 01.8's own register IDs and this phase's own ledger-ID scheme, not `.planning/REQUIREMENTS.md` entries. No `requirements mark-complete` call is appropriate here; the orchestrator should update Phase 01.8's own HV-06 tracking directly.

## Self-Check: PASSED

- FOUND: `.planning/phases/01.4-tool-surface-reachability/01.4-05-SUMMARY.md`
- FOUND: `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md`
- FOUND (git log): `ef49f7c`, `4e92836`

---
*Phase: 01.4-tool-surface-reachability*
*Completed: 2026-08-05*
