---
phase: 01.4-tool-surface-reachability
plan: 02
subsystem: infra
tags: [mcp, vice-proxy, tool-schema, subagent-dispatch, documentation]

# Dependency graph
requires:
  - phase: 01.4-tool-surface-reachability
    provides: "01.4-01's DENY_LIST extension (tools_list/tools_call/initialize/notifications_initialized closed) and its live before/after tools_list evidence, both cross-referenced by this plan's criterion-2 finding"
provides:
  - "Criterion 4 closed with a dated, unambiguous verdict: SATISFIED — a real gsd-executor dispatched under /gsd-execute-phase has mcp__vice__* present in its tool surface (deferred tier), corroborating 01.4-01's own same-shape datapoint"
  - "The operational fact that a gsd-executor's mcp__vice__* names arrive deferred, not directly-callable, and need one ToolSearch(select:...) call before first use in a session — a planning note for 01.4-04/01.4-05, not a limitation"
  - "Criterion 2's bypass route (tools_call's nested-argument confused-deputy path) named in writing again in RE-FINDINGS.md, explicitly superseding 01.4-RESEARCH.md's stale 64-vs-63-tool figures with today's live evidence"
affects: [01.4-04, 01.4-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Self-report as evidence: a task's own tool-list introspection is itself the criterion-4 datapoint, without needing to call the tool under test — mirrors 01.4-RESEARCH.md's own research-subagent method"
    - "Supersede, don't restate: a criterion re-established against current code gets a new dated entry that explicitly names the old figures as stale, rather than quietly repeating them"

key-files:
  created: []
  modified:
    - ".planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md — Criterion 4 section replaced: dated self-report (66-name mcp__vice__* deferred-tier list, vice_disk_list absent even there) plus a SATISFIED verdict with an operational ToolSearch-first-use note"
    - ".planning/RE-FINDINGS.md — dated 2026-08-05 entry naming criterion 2's bypass route (tools_call nested-argument confused deputy) against today's live evidence, superseding the stale 64-vs-63 figures"

key-decisions:
  - "Took the 'present' branch, not the 'absent' branch: this dispatch's own tool list carried the full mcp__vice__* surface (deferred tier), so no CLAUDE.md session-shape-constraint bullet was added — the plan's own instruction is explicit that this branch fires only if the tools are absent"
  - "Did not call any mcp__vice__* tool to test invocability, per task 1's own explicit instruction. Relied on 01.4-01-SUMMARY.md's own live proof of the identical deferred -> ToolSearch -> callable mechanism (same dispatch shape, one plan earlier) as corroborating evidence rather than re-running a live call this plan did not need"
  - "Recorded the SATISFIED verdict as bounded, not absolute: the tool-schema-per-subagent decision is made client-side outside this repo's control (per this plan's own threat model T-01.4-02), so the verdict closes 'not probed' with two consistent post-fix datapoints, not a guarantee against future regression"

patterns-established:
  - "Pattern: a live-evidence file's per-criterion section carries both the dated self-report and the dated verdict together, so a reader sees the raw observation and the conclusion drawn from it in one place rather than split across files"

requirements-completed: ["2", "4"]  # ROADMAP.md's phase-local numbered criteria for Phase 01.4, per 01.4-01-SUMMARY.md's own precedent note that these are not REQUIREMENTS.md REQ-IDs

coverage:
  - id: D1
    description: "Criterion 4 (mcp__vice__* present in a spawned executor subagent's tool schema) closed with a dated, unambiguous verdict from a real gsd-executor dispatch under /gsd-execute-phase"
    requirement: "4"
    verification:
      - kind: manual_procedural
        ref: "01.4-LIVE-EVIDENCE.md#Criterion 4 — subagent tool schema (dated 2026-08-05 self-report + verdict)"
        status: pass
    human_judgment: true
    rationale: "The finding is this executor's own direct introspection of its live tool list at dispatch time — there is no automated test that can independently re-observe what tool schema a past dispatch received, so a human (or a later dispatch) reading the dated record is the verification, matching this plan's own <verify> step (human-check against the LIVE-EVIDENCE.md section)."
  - id: D2
    description: "Criterion 2's bypass route named in writing again against today's live evidence, superseding the stale 64-vs-63-tool figures"
    requirement: "2"
    verification:
      - kind: manual_procedural
        ref: "RE-FINDINGS.md#2026-08-05 — Phase 01.4 criterion 2: the bypass route named in writing again..."
        status: pass
    human_judgment: true
    rationale: "This entry cross-references live evidence gathered in a prior plan (01.4-01) and this same file's own preceding entry rather than making a fresh live call itself — verification is a human (or later reader) confirming the cross-references resolve to real, dated, evidenced entries, per this plan's own <verify> step."

duration: ~12min
completed: 2026-08-05
status: complete
---

# Phase 01.4 Plan 02: Close criteria 2 and 4 with a real subagent self-report Summary

**A real `gsd-executor` dispatch introspected its own tool list and found the full `mcp__vice__*` surface present (deferred tier, 66 names, `vice_disk_list` notably absent even there) — closing criterion 4 as SATISFIED and corroborating 01.4-01's identical prior datapoint — while criterion 2's `tools_call` nested-argument bypass route was named in writing again in `RE-FINDINGS.md`, explicitly superseding the 2026-08-02 research's stale 64-vs-63-tool figures.**

## Performance

- **Duration:** ~12 min
- **Tasks:** 2/2 completed
- **Files modified:** 2 (`01.4-LIVE-EVIDENCE.md`, `RE-FINDINGS.md`)

## Accomplishments

- Closed Phase 01.4 criterion 4 with a dated, unambiguous verdict — the first real test of this criterion against an actual `gsd-executor` dispatch under `/gsd-execute-phase` in this plan's own task 1, corroborated by 01.4-01's independent same-shape dispatch: `mcp__vice__*` tools are present (in the deferred tool-list tier), not structurally absent as the harsher Phase-01-plan-04 concern that originally motivated this criterion described.
- Recorded the precise operational shape of that presence — deferred, not directly-callable — as a planning note for 01.4-04 and 01.4-05 (budget one `ToolSearch` call before the first live emulator call of a plan), rather than letting "satisfied" be read as "no setup needed."
- Named Phase 01.4 criterion 2's bypass route in writing again in `RE-FINDINGS.md`: `tools_call` carrying `vice_disk_list` as a nested `arguments.name` is a confused-deputy path around the named-tool guard, closed by 01.4-01's `DENY_LIST` extension — this entry cross-references that live before/after evidence and explicitly supersedes the 2026-08-02 research's now-stale 64-vs-63-tool figures rather than restating them.
- Confirmed, from a second independent dispatch, that `vice_disk_list` is absent even from this session's own deferred-tool inventory (a stronger absence than the wire-level refusal 01.4-01 proved), consistent with 01.4-01's fix having landed before this plan's own proxy process was spawned.

## Task Commits

Each task was committed atomically:

1. **Task 1: self-report this executor's own tool surface** — `7eeb0f5` (docs)
2. **Task 2: close criterion 4 per the outcome, and close criterion 2 in writing** — `1afd295` (docs)

**Plan metadata:** SUMMARY.md commit (this file) — created by this same executor per this plan's own constraint that STATE.md/ROADMAP.md commits belong to the orchestrator.

## Files Created/Modified

- `.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md` — Criterion 4 section rewritten: task 1's dated self-report (two-tier tool-list split, full 66-name `mcp__vice__*` list, `vice_disk_list` absence noted) followed by task 2's dated SATISFIED verdict and the ToolSearch-first-use operational note.
- `.planning/RE-FINDINGS.md` — dated `2026-08-05` entry naming criterion 2's bypass route against today's live evidence, cross-referencing this file's own preceding entry and `01.4-01-SUMMARY.md`, and superseding the stale 64-vs-63-tool figures explicitly.

## Decisions Made

- **Took the "present" branch of task 2's instruction.** Task 1's self-report found `mcp__vice__*` present (deferred tier) in this dispatch, so criterion 4 closes as satisfied directly rather than as a documented session-shape constraint — no `.claude/CLAUDE.md` change fires from this plan.
- **Did not call any `mcp__vice__*` tool to test invocability**, per task 1's explicit instruction to report tool-list presence only. Invocability of the same deferred -> `ToolSearch` -> callable mechanism was already proven live, in the same dispatch shape, by `01.4-01-SUMMARY.md`'s own task — cited as corroborating evidence rather than re-run.
- **Bounded the SATISFIED verdict explicitly.** The tool-schema-per-subagent decision is made client-side, outside this repo's control (this plan's own threat model, T-01.4-02) — recorded that a future dispatch observing the harsher "structurally absent" shape again should log it as a fresh, dated regression, not assume this entry covers it.
- **Superseded rather than restated the stale 64-vs-63 figures.** ROADMAP.md's criterion 2 explicitly asked for re-establishing "now," not carrying the 2026-08-02 numbers forward; the RE-FINDINGS.md entry names the current live evidence and states plainly that the old count no longer describes the running system.

## Deviations from Plan

None — plan executed exactly as written. Both tasks followed their `<action>` instructions directly; the "present" branch of task 2's conditional logic fired based on task 1's honest self-report, as the plan's own instructions anticipated.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Criterion 4 is closed with a dated verdict (SATISFIED) — 01.4-04 and 01.4-05, both later `gsd-executor` dispatches under this same phase, can rely on `mcp__vice__*` being present in their own tool surface, with the one-time `ToolSearch` load noted as the operational shape to plan around.
- Criterion 2 is closed with a written, evidence-backed statement of the bypass route, cross-referenced against 01.4-01's live evidence and this plan's own corroborating datapoint.
- No blockers for 01.4-03 (running in parallel, disjoint files) or 01.4-04/01.4-05 (which depend on this plan and 01.4-03 both completing).
- **Requirement-ID mapping note**, inherited from 01.4-01's own precedent: this plan's frontmatter `requirements: ["2", "4"]` refers to ROADMAP.md's phase-local numbered criteria for Phase 01.4, not `.planning/REQUIREMENTS.md` entries (that file states this milestone carries no REQ-IDs by design). `requirements-completed` above lists them for traceability within this SUMMARY; the orchestrator should update ROADMAP.md's own criterion checklist directly rather than routing through `requirements mark-complete`.

## Self-Check: PASSED

- FOUND: `.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md`
- FOUND: `.planning/RE-FINDINGS.md`
- FOUND (git log): `7eeb0f5`, `1afd295`

---
*Phase: 01.4-tool-surface-reachability*
*Completed: 2026-08-05*
