---
phase: 01.4-tool-surface-reachability
plan: 04
type: execute
wave: 3
depends_on:
  - "01.4-02"
  - "01.4-03"
files_modified:
  - .planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md
autonomous: true
requirements:
  - "1"

must_haves:
  truths:
    - "mcp__vice__vice_recycle has been called deliberately from a live session, with the loss accounted for in advance, and the resulting machine-restarted signal plus the next call's success against the respawned instance are both recorded"
    - "mcp__vice__vice_result_continue has been called with a real continuation token obtained by deliberately producing an over-ceiling tools/call result, and the next chunk it returns is recorded"
    - "criterion 1 carries an unambiguous, dated verdict for all three synthetic tools — no longer PARTIALLY discharged"
  artifacts:
    - "01.4-LIVE-EVIDENCE.md — a new dated section recording both live proofs (or, if this dispatch structurally lacks mcp__vice__*, a plain negative record in their place)"
  key_links:
    - "vice_recycle call -> broker epoch bump -> next forwarded call's machine-restarted verdict -> success against the new instance"
    - "an oversized vice_memory_read -> wrapPossiblyChunked()'s continuation token -> vice_result_continue(token) -> the next chunk"

precondition: "This session has already made at least one successful mcp__vice__* call before this plan runs (so a granted instance and lease already exist for vice_recycle to act on) — true by construction once 01.4-01's live probes have run in this same session; if this plan's own dispatch is a fresh session with no prior forwarded call, task 1's own vice_ping check establishes it instead."
---

<objective>
Discharge criterion 1's two remaining synthetic tools. `01.4-LIVE-EVIDENCE.md` already proved
`vice_diagnose` executes; `vice_recycle` and `vice_result_continue` are present but honestly unproven,
for two different reasons the roadmap itself names: `vice_recycle` is destructive (kills the granted
instance, bumps the epoch, voids any run in flight — proving it by probing would destroy a healthy
instance), and `vice_result_continue` needs a continuation token from an oversized `tools/call` result,
which does not exist until one is deliberately produced.

This plan runs after 01.4-01 (the deny-list fix) and 01.4-03 (the message rewrite) land, so both
proofs exercise the FINAL system, not a mid-fix one — and after 01.4-02 records whether a dispatched
executor has these tools at all, so this plan opens by re-confirming that fact for its own dispatch
rather than assuming continuity across separate executor invocations.

Purpose: turn "PARTIALLY discharged" into a complete, dated verdict for criterion 1.

Output: two dated, complete live proofs appended to `01.4-LIVE-EVIDENCE.md`.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md
@.planning/phases/01.4-tool-surface-reachability/01.4-02-SUMMARY.md
@.claude/skills/vice-wedge-triage/SKILL.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: confirm this dispatch has live vice access, then deliberately recycle</name>
  <files>.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md</files>
  <action>
    Before anything else, confirm mcp__vice__vice_ping (or any mcp__vice__* tool) is present and
    callable from this dispatch. If it is not: stop this task immediately, append a plain negative
    record to 01.4-LIVE-EVIDENCE.md under a new "## Criterion 1 — remaining live proofs" section
    (this dispatch's own tool list lacked mcp__vice__*; note whether that is consistent with
    01.4-02's own self-report or a new, separate finding), and do not attempt any of this plan's
    remaining actions — escalate rather than fabricate a result.

    If tools are present: read the current epoch via mcp__vice__vice_diagnose first, establishing the
    "before" baseline. Then deliberately call mcp__vice__vice_recycle, accounting for the loss up
    front — this kills the currently granted instance and bumps the epoch by design; that is the
    point of this call, not an accident to avoid or apologise for. Immediately make one more forwarded
    call (e.g. mcp__vice__vice_ping) and record its response: expect a machine-restarted / replaced-
    machine signal on this first post-recycle call, mirroring the epoch 1->2 "verdict: restarted"
    pattern 01.4-LIVE-EVIDENCE.md already recorded from a different, earlier cause. Then make a second
    call and confirm it succeeds cleanly against the new instance. Record the full before/after epoch
    pair and both post-recycle call outcomes, dated, in the same new section.
  </action>
  <verify>
    <human-check>Confirm 01.4-LIVE-EVIDENCE.md records a dated, complete vice_recycle proof: the
    before-epoch, the recycle call, the restarted signal, and the successful next call — or, if tools
    were absent this dispatch, the plain negative record in their place.</human-check>
  </verify>
  <done>
    01.4-LIVE-EVIDENCE.md records the outcome either way, dated and unambiguous.
  </done>
</task>

<task type="auto">
  <name>Task 2: deliberately produce an over-ceiling result and exercise vice_result_continue</name>
  <files>.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md</files>
  <action>
    Skip this task with a plain negative record (same shape as task 1) if task 1 found mcp__vice__*
    absent from this dispatch. Otherwise: call mcp__vice__vice_memory_read with a large range
    (size close to 65535) and encoding:"array" (the legacy per-byte-hex encoding, far larger than the
    compact "hex" encoding for the same byte count, and the shape most likely to exceed the proxy's
    OUTPUT_CHAR_CAP, default 500000 characters) against a large, harmless address range (e.g.
    bank:"ram", address 0, size 65535) — the "64K RAM read is the shape that trips it" case Phase
    01.1 criterion 5 already named. If the response is chunked, it will name a continuation token
    and a chunk count in its trailing marker; call mcp__vice__vice_result_continue with that exact
    token and record the next chunk's own trailing marker. If this particular call does not trip the
    ceiling, escalate — try a larger or repeated request (a second read at a different address, or
    mcp__vice__vice_display_screenshot, which returns image bytes) until one does. Do not treat "it
    didn't chunk on the first attempt" as the finding; criterion 1 needs this tool actually exercised,
    not merely available.
  </action>
  <verify>
    <human-check>Confirm 01.4-LIVE-EVIDENCE.md records a dated, complete vice_result_continue proof:
    the oversized call that produced a token, and the successful continuation call using it.</human-check>
  </verify>
  <done>
    01.4-LIVE-EVIDENCE.md's criterion-1 table shows all three synthetic tools EXECUTED, not merely
    present — or the plain negative record if this dispatch lacked mcp__vice__*.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| agent -> broker (deliberate destructive action) | vice_recycle is a legitimate, intended kill of the session's own granted instance, not an attacker action — but it must not be issued carelessly, since it voids concurrent work in flight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-05 | Denial of Service (self-inflicted) | vice_recycle's deliberate kill | low | accept | The kill is intentional and its loss is accounted for up front (task 1's own wording); no other session-critical work is scheduled concurrently with this plan per the phase's wave ordering |
</threat_model>

<verification>
01.4-LIVE-EVIDENCE.md carries dated, complete records for both remaining synthetic tools (or the
honest negative record if this dispatch structurally lacked the tools).
</verification>

<success_criteria>
Criterion 1 fully closed: all three synthetic tools proven callable and executed from a real agent
session, dated and recorded.
</success_criteria>

<output>
Create `.planning/phases/01.4-tool-surface-reachability/01.4-04-SUMMARY.md` when done.
</output>
