---
phase: 01.4-tool-surface-reachability
plan: 05
type: execute
wave: 4
depends_on:
  - "01.4-04"
files_modified:
  - .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md
autonomous: true
requirements:
  - "CF-01.4-NN"
  - "HV-06"

must_haves:
  truths:
    - "Every one of the 35 CF-01.4-NN rows in 01.6.2-VALIDATION.md carries a disposition other than open: discharged, blocked-on-HV-08, or permanently-unverifiable-from-container, each with a one-line reason and, where applicable, a citation to this phase's own evidence"
    - "No row is marked discharged where the row's own text asks for an action this container structurally cannot perform (starting, killing, or restarting a host process; binding a host port directly; holding a connection open for real hours) — those are permanently-unverifiable-from-container regardless of which plan number originated them"
    - "Rows testing 01.6.2.1-era behaviour not yet deployed to the host (spares_target still present in broker.json, the pre-rename key) are marked blocked-on-HV-08, not silently skipped and not claimed discharged"
    - "CF-01.4-35 (the list's own usability) is marked discharged once every other row above has a real, non-open verdict — its own criterion is that this very process succeeded, not a separate live check"
  artifacts:
    - "01.6.2-VALIDATION.md — the CF-01.4-NN table's status column filled in for all 35 rows, each ending in one of the three dispositions with a reason"
  key_links:
    - "each row's own 'why unverifiable in-repo' text -> the decision procedure below -> a disposition citing this phase's own evidence (01.4-LIVE-EVIDENCE.md, a direct .vice-supervisor/** read, or a stated permanent limitation)"
---

<objective>
Discharge Phase 01.8's HV-06: walk every one of the ~35 `CF-01.4-NN` carry-forward rows in
`01.6.2-VALIDATION.md` (lines ~711-758) and give each a real, dated disposition — discharged,
blocked on HV-08 (the pending host redeploy, developer's call, `spares_target` still the pre-rename
key in `.vice-supervisor/broker.json` as of this phase), or permanently unverifiable from the
container, with a one-line reason either way. HV-06's own developer action ("same live-broker access
as HV-04, once") is already satisfied — this plan is the agent's remaining work, not a new gate.

**The decision procedure, in order, for every row:**
1. If the row's own text names an action this container structurally cannot perform regardless of
   code version — starting, killing, or restarting a host process (the broker or an `x64sc`), binding
   a host port directly, hand-launching an instance, or holding a connection open across real
   wall-clock hours — it is **permanently-unverifiable-from-container**. This holds even for rows
   numbered 01-24 (the 01.6.2-era rows); numeric range does not decide this, the action named does.
2. Otherwise, if the row is testing a specific 01.6.2.1 behaviour that simply is not running on the
   host yet (the five lifecycle-policy changes: warm-floor pre-warming, the grant-time probe race,
   floor defaulting to 1, the raised acquire deadline, the retired-variable banner) — it is
   **blocked-on-HV-08**.
3. Otherwise, attempt the row's own named check using only: (a) `mcp__vice__*` forwarded calls this
   phase already knows are reachable (never `vice_disk_list`, never a generic-dispatch tool carrying
   it), and (b) a direct `Read` of `.vice-supervisor/**` files as pure state observation — the same
   precedent HV-04/HV-01 already established ("satisfied by direct record read, no forwarded call
   needed"). If the check succeeds, it is **discharged**, citing the evidence read. If the file
   evidence needed to attempt it genuinely does not exist on disk (e.g. a startup banner that is only
   ever written to stderr, never persisted), it is **permanently-unverifiable-from-container** with
   that specific reason, independent of HV-08.

**Never**, in the course of this plan: restart the broker, `SIGKILL` anything, start a second broker
process, bind a port directly, or wait idle for real hours. If a row's only path to discharge is one
of these, it is permanently unverifiable, full stop — this plan does not ask the developer to perform
any further action beyond what HV-04 already provided.

This plan's own prior analysis of all 35 rows is given below as a starting disposition — spot-check
it against current `.vice-supervisor/**` state and this phase's own `01.4-01`through`01.4-04`
SUMMARYs before transcribing, since time has passed since this table was drafted and any of the
"discharged" rows below could have been invalidated (or newly enabled) by a live event in the
meantime. Where fresh evidence disagrees with a row below, use the fresh evidence and note the
correction.

Purpose: HV-06 closes with a real per-row verdict for all 35 rows, split honestly between what live
access settles today and what genuinely waits on the developer's own HV-08 redeploy — not a silent
gap and not an overclaimed one.

Output: `01.6.2-VALIDATION.md`'s carry-forward table, every row disposed.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md
@.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md
@.planning/phases/01.4-tool-surface-reachability/01.4-01-SUMMARY.md
@.planning/phases/01.4-tool-surface-reachability/01.4-02-SUMMARY.md
@.planning/phases/01.4-tool-surface-reachability/01.4-04-SUMMARY.md
</context>

## This plan's own prior analysis (spot-check, do not blindly transcribe)

**Rows 01-24 (01.6.2-era) — a starting disposition per row:**

- **01** — permanently-unverifiable-from-container: the container-guard verdict is logged only at
  broker process start and is not persisted anywhere this container can read (checked: no top-level
  broker startup/stderr log exists under `.vice-supervisor/`, only per-instance `epoch.json`/
  `supervisor.log`); starting a fresh broker process is a host action.
- **02** — discharged: `.vice-supervisor/broker.json`'s `node_version` field read directly, present
  (`"v22.22.0"` as of this phase's own reads).
- **03** — discharged: every forwarded `mcp__vice__*` call this phase has made (vice_ping,
  vice_diagnose, and 01.4-01/01.4-04's live probes) already proves the control listener is reachable
  from the container at the host bridge address.
- **04** — discharged, with the literal ask narrowed and stated as such: read the currently-warm
  instances' `epoch.json` `spawned_at` timestamps directly — if they show staggered (not
  simultaneous) launches with `epoch: 1` (no crash/replacement) on each, that is live, real evidence
  serialised warming holds at the CURRENT floor. This is not literally "raise the floor above 1 and
  watch a fresh boot cycle" (that needs a broker restart, a host action) — note the narrowing
  explicitly in the ledger rather than silently upgrading it to the literal ask.
- **05** — permanently-unverifiable-from-container in its literal form: read the legacy per-port
  `supervisor.log`/`logs/*.log` files directly — as of this phase's reads, the ones on disk (ports
  6510/6512/6513/6514) show clean shutdowns ("caught signal, shutting down" / "clean shutdown"), not
  a crash. The read path and mechanism are confirmed real and working; no genuine crash event exists
  on disk today to inspect, and forcing one is a host action. State both halves in the ledger: path
  confirmed readable, crash content not currently available.
- **06** — discharged: `01.4-LIVE-EVIDENCE.md`'s recorded `vice_diagnose` epoch 1->2 transition is
  exactly a live proxy read against a live-written `epoch.json` succeeding; 01.4-04's own deliberate
  `vice_recycle` proof (if it landed) is a second, independent instance of the same mechanism.
- **07, 08** — permanently-unverifiable-from-container: both need an actual broker-process restart or
  `SIGKILL`; no exposed `mcp__vice__*` tool reaches the broker's own process, only the emulator
  instance (`vice_recycle`).
- **09, 10** — permanently-unverifiable-from-container: both need a second host process (a second
  broker, or a non-broker process binding the control port first); the agent cannot start host
  processes.
- **11** — discharged by source inspection, not by waiting: `forwardToVice()`'s own header comment
  ("No touch-on-every-forwarded-call any more... the connection itself is the claim, kernel-enforced,
  with nothing to refresh") establishes no idle-timeout mechanism exists in the code to misfire —
  cite the comment directly rather than literally holding a session idle for hours.
- **12** — discharged: this phase's own sequence of forwarded calls across 01.4-01 and 01.4-04 (and,
  if applicable, 01.4-02's self-report dispatch) IS a full acquire-forward-release round trip; cite
  this phase's own SUMMARYs.
- **13** — discharged: diff today's observed tool surface (01.4-LIVE-EVIDENCE.md, this phase's own
  `tools_list` probes) against the 01.6.3 `COVERAGE.md` baseline — same names present, same absence of
  the three synthetics from the host's own raw list.
- **14** — discharged, with the inference stated explicitly rather than silently merged into row 22:
  the epoch-guard mechanism producing `machineReplacedMessage` does not distinguish a deliberately
  recycled instance from one that died any other way (crash, external kill) — 01.4-04's live
  `vice_recycle` proof (row 22) exercises the identical code path. Note this is an inference from
  mechanism, not a literal external-crash observation.
- **15, 16, 17** — permanently-unverifiable-from-container: 15 needs killing the actual broker
  process mid-session (no tool reaches it); 16 needs host filesystem/shell access outside
  `.vice-supervisor/` to confirm a named path exists and a named invocation is accepted; 17 needs the
  developer to hand-launch an instance in the reserved port band.
- **18** — check live: read `01.6.2-REVIEW.md` for its own coverage-gap analysis of the
  `vice-broker.test.mjs` redesign, cross-referenced against the current test file set, and state
  whether it identifies any gap the replacement suite left uncovered. Discharge citing that reading,
  or note the specific gap if one is found.
- **19** — discharged by a structural inference worth stating explicitly: the row's own premise
  ("where no container workspace variable is set") is not a special condition to arrange — it is the
  ONLY environment a real host broker process ever runs in, since `CONTAINER_WORKSPACE_PATH` is a
  container-only variable that a host process's environment never has. The broker currently running
  successfully (proven by every reachable forwarded call and correctly-translated path this phase has
  made) is therefore already, by construction, exercising the git-marker branch — there is no other
  branch available on a real host.
- **20** — blocked-on-HV-08, with a nuance to flag: HV-08's own action re-runs the installer once as
  part of the redeploy; this row specifically wants the installer run TWICE (once with stale files
  present, once more to prove the second run prunes them) — note in the ledger that HV-08's single
  redeploy may not automatically produce this row's exact evidence unless the installer happens to be
  run an extra time.
- **21** — permanently-unverifiable-from-container: needs killing a real `x64sc` process directly; no
  exposed tool reaches an emulator's host process by pid.
- **22** — discharged by 01.4-04's own deliberate `vice_recycle` proof, if it landed (cite
  `01.4-04-SUMMARY.md` directly: before-epoch, the recycle call, the restarted signal, the successful
  next call). If 01.4-04 recorded a negative (tools absent from that dispatch), this row stays open
  and should say so plainly rather than being marked discharged anyway.
- **23** — permanently-unverifiable-from-container within a single plan's execution: confirming
  "exactly one instance exists" after a session disconnects requires a SEPARATE, later session's
  observation of broker state, which is beyond any one plan's own execution boundary — note this as a
  good candidate for casual confirmation during a future live session rather than something this plan
  can mechanically close alone.
- **24** — permanently-unverifiable-from-container: requires knowledge of the host's own ephemeral
  port range and other bound services, which needs host-side inspection this container cannot perform.

**Rows 25-35 (01.6.2.1-era plus the closing row):**

- **25-30, 33, 34** — blocked-on-HV-08: each tests specific 01.6.2.1 behaviour (warm-floor
  pre-warming, the grant-time probe race and its timeout, the floor-defaults-to-1 change, the
  retired-variable banner, the shared `warm_floor` vocabulary) that is not running on the host today
  (`.vice-supervisor/broker.json` still shows `spares_target`, the pre-rename key, confirming this).
  Flag 33 and 34 with the same nuance as row 01: if the broker's own startup banner is never
  persisted to a readable file, these may stay permanently-unverifiable even after HV-08's redeploy
  unless whoever performs it also captures that stream — note this for whoever executes HV-08.
- **31** — blocked-on-HV-08: the raised 120000ms deadline's sufficiency is measurable by normal live
  observation once the new code is running.
- **32** — blocked-on-HV-08, and flag as possibly staying unverifiable even after redeploy: forcing an
  actual 120-second stall deliberately is speculative future work, not something this plan or a
  routine post-redeploy check can casually produce.
- **35** — discharged once rows 01-34 above have each been given a real, non-open verdict by this
  plan's own two tasks — this row's own criterion is the list's usability, which this very process
  succeeding is the proof of, not a separate live check.

<tasks>

<task type="auto">
  <name>Task 1: discharge CF-01.4-01 through CF-01.4-24</name>
  <files>.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md</files>
  <action>
    Re-read `01.6.2-VALIDATION.md`'s carry-forward table (the `CF-01.4-01` through `CF-01.4-24` rows)
    and this plan's own prior analysis above for the same range. For each row, spot-check the
    starting disposition against current state before transcribing it: for rows citing a direct file
    read (02, 03, 06, 11, 19), re-read the cited file/comment now and confirm it still says what this
    plan claims; for row 04, re-read the currently-warm instances' `epoch.json` `spawned_at`
    timestamps now (the specific ports may have changed since this plan was drafted) and recompute the
    stagger; for row 05, re-check whether any NEW crash log has appeared under
    `.vice-supervisor/*/logs/` since this plan was drafted (if one has, discharge fully rather than
    partially); for row 12, cite whichever of this phase's own plans actually landed the round trip;
    for row 18, actually read `01.6.2-REVIEW.md`'s coverage-gap analysis now, rather than assuming
    this plan's placeholder judgment; for row 22, check `01.4-04-SUMMARY.md`'s actual recorded outcome
    (positive or negative) before marking discharged. Apply the decision procedure in the objective
    for any row this plan's own analysis did not already cover cleanly. Edit `01.6.2-VALIDATION.md` in
    place, replacing each row's trailing `| open |` cell with `| <disposition> — <one-line reason,
    citing evidence> |`.
  </action>
  <verify>
    <automated>grep -E "CF-01\.4-(0[1-9]|1[0-9]|2[0-4]) " .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md | grep -c "| open |"</automated>
  </verify>
  <done>
    The automated check above returns 0 — no row among CF-01.4-01 through CF-01.4-24 is still marked
    `open`.
  </done>
</task>

<task type="auto">
  <name>Task 2: discharge CF-01.4-25 through CF-01.4-35 and close the ledger</name>
  <files>.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md</files>
  <action>
    For CF-01.4-25 through CF-01.4-34: confirm `.vice-supervisor/broker.json` still shows the
    pre-01.6.2.1 `spares_target` key (re-read it now — do not assume it is unchanged from when this
    plan was drafted) and, if so, mark each row `blocked-on-HV-08` with the shared reason plus this
    plan's own per-row nuance above (20's double-run caveat lives with row 20 in task 1's range;
    31/32/33/34's "may stay unverifiable even post-redeploy" flags belong here). If `broker.json`
    shows the RENAMED `warm_floor` key instead (meaning HV-08 already happened between this plan's
    drafting and its execution), re-run the decision procedure against the now-live 01.6.2.1 code
    instead of defaulting to `blocked-on-HV-08` — do not claim a row is blocked on an event that has
    already occurred.

    For CF-01.4-35: confirm every row from CF-01.4-01 through CF-01.4-34 now carries a non-open
    disposition (this task's own automated check below), then mark it `discharged` citing that this
    plan's two tasks completing IS the proof its own criterion asks for.

    Finally, re-read the whole carry-forward table once more and confirm no row was left ambiguous —
    every disposition is one of the three named in the objective's decision procedure, each with a
    reason.
  </action>
  <verify>
    <automated>grep -E "CF-01\.4-(2[5-9]|3[0-5]) " .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md | grep -c "| open |"</automated>
  </verify>
  <done>
    The automated check above returns 0 — no row among CF-01.4-25 through CF-01.4-35 is still marked
    `open`. Every row among all 35 now carries `discharged`, `blocked-on-HV-08`, or
    `permanently-unverifiable-from-container`, each with a reason.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| this plan's own analysis -> the ledger's permanent record | A wrong disposition here is load-bearing: 01.8's HV-06 and any future reader trust this table rather than re-deriving it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.4-06 | Repudiation / false confidence | the carry-forward ledger's disposition column | medium | mitigate | The decision procedure forbids marking a row discharged from inference alone when the row's own text names an action the container cannot perform; task 1/2's automated checks catch any row left `open`, not any row wrongly marked otherwise — spot-checking against fresh state before transcribing (both tasks) is the mitigation for a wrong disposition specifically |
</threat_model>

<verification>
`grep -E "CF-01\.4-" .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md | grep -c "| open |"`
returns 0 across the whole table. Every row's disposition cites either a live-evidence document from
this phase, a direct file read performed during this plan, or a stated structural limitation.
</verification>

<success_criteria>
HV-06 discharged: all 35 CF-01.4-NN rows carry a real, dated, evidence-cited disposition — no row
left `open`, no row overclaimed against a stale deploy, no row silently dropped.
</success_criteria>

<output>
Create `.planning/phases/01.4-tool-surface-reachability/01.4-05-SUMMARY.md` when done.
</output>
