---
phase: 01.4-tool-surface-reachability
verified: 2026-08-05T16:38:30Z
status: human_needed
score: 4/5 truths verified (1 truth partially/honestly-negative, all supporting artifacts real; 1 open live-confirmation gap found during verification, not disclosed as closed by any plan)
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "From a genuinely NEW top-level Claude Code session (not a /gsd-execute-phase subagent dispatch, which appears to inherit the parent session's already-established vice-proxy MCP connection/schema snapshot), call mcp__vice__tools_list and inspect the returned/deferred tool names. Then, from that same fresh session, call mcp__vice__tools_call with a benign nested name such as {\"name\":\"vice_ping\",\"arguments\":{}} — never with vice_disk_list nested, that is the hazard itself."
    expected: "tools_list, tools_call, initialize and notifications_initialized should be absent from the served/deferred tool inventory (construction-time DENY_LIST filtering, vice-proxy.ts:2952-2956), and any direct tools/call naming one of them should be refused with denyListRefusalMessage()'s bypass-hazard wording, matching vice-proxy.test.ts's already-passing unit tests."
    why_human: "Claude Code snapshots an MCP server's tool schema once per session and never refreshes it (01.4-RESEARCH.md, confirmed project mechanism). 01.4-01's fix landed at 14:52-15:00 UTC 2026-08-05; 01.4-02's own recorded live evidence (01.4-LIVE-EVIDENCE.md Criterion 4 section, captured 15:22 UTC, chronologically AFTER the fix) still lists mcp__vice__initialize, mcp__vice__notifications_initialized, mcp__vice__tools_call and mcp__vice__tools_list as present in its deferred tool inventory — the opposite of what a session with a post-fix schema snapshot should show. This is consistent with every subagent dispatch in this phase (01.4-01, 02, 04, 05) sharing one long-lived top-level session's MCP connection established before 01.4-01's fix landed, rather than each dispatch obtaining an independent 'fresh session' as 01.4-01's own SUMMARY assumed when deferring this check. No test process or subagent dispatch available to this verifier can open a genuinely new top-level session, so this is unavoidably a human/live action."
---

# Phase 01.4: Tool Surface Reachability Verification Report

**Phase Goal:** Every tool the proxy implements is callable by the agent that needs it, in every session shape this project actually uses — main session and spawned subagent alike — and no path that can name a tool can reach `vice_disk_list`
**Verified:** 2026-08-05T16:38:30Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Summary verdict

Four of five ROADMAP criteria are solidly established, with code-level evidence I independently re-derived (not merely SUMMARY claims) for every one of them. Criterion 3 — the security-relevant one — is **solidly closed at the code/enforcement level** (DENY_LIST extension, both enforcement seams, unit tests against a stand-in host proving zero real network calls escape), but its own **live "after" confirmation was never actually established**, and the evidence the phase itself recorded as corroborating (01.4-02's tool-schema self-report) is, on close reading, more consistent with a *stale, pre-fix* schema snapshot than a fresh, post-fix one. That is a genuine, previously-unflagged gap this verification surfaces — not a defect in the guard itself, but an unclosed loop in the phase's own "verification for this phase is a live session call and nothing else" standard (ROADMAP.md, Phase 01.4 Risks). This alone routes the phase to `human_needed` rather than `passed`.

The HV-06 carry-forward ledger (35 `CF-01.4-NN` rows in `01.6.2-VALIDATION.md`) is fully and honestly discharged: zero `| open |` rows remain, the 12/11/12 tally is exact, and — critically — the 11 `blocked-on-HV-08` rows are correctly *not* claimed as tested against 01.6.2.1's new code; I independently re-read `.vice-supervisor/broker.json` and confirmed it still carries the pre-rename `spares_target: 3` key with a live, advancing heartbeat, exactly as those rows state. This is Phase 01.8's business (HV-08), not a defect of Phase 01.4's own work, and I have not counted it as a gap of this phase.

## Goal Achievement

### Observable Truths (ROADMAP.md Success Criteria)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | `vice_diagnose`, `vice_recycle`, `vice_result_continue` callable from a normal agent session | ✓ VERIFIED (2 of 3 executed; 1 honestly proven unreachable) | `vice_diagnose`: verdict `restarted`, epoch 1→2 (01.4-01). `vice_recycle`: deliberate kill on port 6603, re-confirmed directly by me reading `.vice-supervisor/6603/epoch.json` (`spawned_at: 2026-08-05T16:01:35.799Z`, matches SUMMARY exactly) and the incident record `.planning/incidents/20260805160045723-port6603-epoch1.md` (read directly, contains pre-kill cycle bracket, PC/register snapshot, checkpoint enumeration). `vice_result_continue`: 4 tool shapes escalated to documented maxima (`vice_memory_read` 65535B array, BMP screenshot, `memory_search`/`memory_compare` at 10,000-result caps) — none crossed the 500,000-char `OUTPUT_CHAR_CAP` (confirmed by direct code read, `vice-proxy.ts:256-258`, default 500000). Escalation covers the highest-output tool shapes in the manifest; other tools (`vice_disassemble` max 100 instructions, `vice_backtrace` max 64 frames, `vice_checkpoint_list`/`vice_symbols_lookup` bounded by session-created state) are schema-bounded well below the cap, so the negative is well-supported though not a literal enumeration of all ~60 tools. |
| 2 | Bypass route named in writing | ✓ VERIFIED | `.planning/RE-FINDINGS.md`'s 2026-08-05 entry ("Phase 01.4 criterion 2...") explicitly supersedes the 2026-08-02 64-vs-63-tool figures, states why they're stale, and names the confused-deputy mechanism (`tools_call` nested `arguments.name`) precisely. Read directly, not taken on SUMMARY's word — it genuinely supersedes rather than restates. |
| 3 | `vice_disk_list` unreachable on the generic call path | ⚠ CODE VERIFIED, LIVE CONFIRMATION GAP FOUND | `DENY_LIST` in `vice.ts:201-206` extended to `[vice_disk_list, tools_list, tools_call, initialize, notifications_initialized]`; both enforcement seams read directly (`vice.ts:698` guard in `call()`, `vice-proxy.ts:2984-2989` `CallToolRequestSchema` override, `vice-proxy.ts:2954` construction-time discovery filter). Unit test `"vice_disk_list is refused at tools/call with no request made"` (`vice-proxy.test.ts:557`) uses a local stand-in host and asserts `requests.length === 0` — proves the refusal happens before any network attempt, not merely that a wrapper text changed. Full suite 442/437/0/5 confirmed by me running it directly. **However:** the manual-only "live after" refusal check this phase's own `01.4-VALIDATION.md` requires was never actually performed — see Human Verification below. This is the criterion the orchestrator flagged for hardest scrutiny, and the gap is real. |
| 4 | `mcp__vice__*` present in a spawned executor subagent's schema | ✓ VERIFIED (against the concern as written) | Two independent `gsd-executor` dispatches (01.4-01, 01.4-02) both found the full `mcp__vice__*` surface present in the deferred tool tier, not structurally absent — directly contradicting the harsher Phase-01/plan-04 finding that motivated this criterion. Confirmed `gsd-executor.md`'s `tools:` key is still deliberately omitted (`.claude/agents/gsd-executor.md:3-9`, read directly) and the file is still gitignored (`git check-ignore` confirms), matching the fragility both SUMMARYs flag. "Deferred, not absent" is a legitimate reading of this criterion: the ROADMAP text's concern is explicitly "structurally absent... with no `mcp__vice__*` entry at all," which is the harsher shape neither dispatch reproduced — not whether the deferred-tool listing is schema-fresh (that is criterion 3's concern, not criterion 4's). |
| 5 | Agent-visible messages name an action, not a topology | ✓ VERIFIED | Read `vice-proxy.ts` directly: every remaining `` `vice-proxy: ``  literal (11 occurrences) is the argument of a `console.error(...)` call (stderr, not agent-visible) — confirmed by grep and inspection, none are returned as `content`. The one exception, the `VICE_SEAM_HAZARDS_TEST_FIXTURE`-gated synthetic hazard string (`vice-proxy.ts:2615-2621`), fires only when that env var is explicitly set to `"1"`, is commented "Never set outside this file's own test suite," and is unreachable by a real agent — a legitimately scoped carve-out, not a loophole. The regression test `"structural: no agent-visible template literal begins with the vice-proxy: prefix"` (`vice-proxy.test.ts:3526`) exists and passes. |

**Score:** 4/5 criteria verified clean; criterion 3 verified at the code/enforcement layer but with a live-confirmation gap surfaced by this verification (routed to Human Verification, not counted as failed since the underlying mechanism is solid and independently code-verified).

### HV-06 / `CF-01.4-NN` Carry-Forward Ledger

| Check | Result |
|---|---|
| Zero `\| open \|` rows remain | ✓ Confirmed by direct grep: 0 |
| Disposition tally | ✓ Confirmed by direct grep: 12 discharged / 11 blocked-on-HV-08 / 12 permanently-unverifiable-from-container = 35, exact match to SUMMARY's claim |
| Spot-check: row 4 (serialised warming, epoch.json timestamps) | ✓ Read `.vice-supervisor/{6600,6601,6602}/epoch.json` directly — `spawned_at` values (08:44:16.359 / .990 / .659) match the row's cited figures exactly |
| Spot-check: row 22 (`vice_recycle` live proof) | ✓ Read `.vice-supervisor/6603/epoch.json` directly — matches 01.4-04-SUMMARY.md and the row's citation exactly |
| Spot-check: HV-08 staleness (governs 11 rows) | ✓ Read `.vice-supervisor/broker.json` directly — `spares_target: 3` present (pre-rename key), no `warm_floor` key; `heartbeat_at` is fresh (16:30:46Z, well within this session), so this is a live, current read, not a stale cache — the 11 rows are correctly **not** claimed discharged against code that isn't running yet |
| Spot-check: permanently-unverifiable rows (01, 07, 08, 09, 10, 15, 21) | ✓ Each names a real host-process action (start/kill/SIGKILL a broker or `x64sc` process, bind a real port) that no `mcp__vice__*` tool reaches — genuinely unreachable, not merely inconvenient, consistent with the project's hard rule that the broker/host process itself has no exposed tool surface (only the emulator instance does, via `vice_recycle`) |

HV-06 is legitimately discharged. The 11 `blocked-on-HV-08` rows are Phase 01.8's open business, not a gap in Phase 01.4's own deliverable — the ledger's own three-way disposition scheme anticipates exactly this outcome, and this phase correctly refused to test 01.6.2.1's new code against a host that isn't running it yet.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.claude/mcp/vice/vice.ts` `DENY_LIST` | 5 entries, `denyListRefusalMessage()` | ✓ VERIFIED | Read directly, matches SUMMARY exactly |
| `.claude/mcp/vice/vice-proxy.ts` enforcement seams | Discovery-time filter + call-time guard, both reading `DENY_LIST` | ✓ VERIFIED | `vice-proxy.ts:2954`, `:2984-2989`, both tool-name scoped, neither touches the MCP handshake |
| `.claude/mcp/vice/vice-proxy.test.ts` | New/repointed refusal tests, structural regression guard | ✓ VERIFIED | Full suite run directly: 442/437/0/5 |
| `.planning/RE-FINDINGS.md` entries | Dated, graded findings for criteria 1–4 | ✓ VERIFIED | 6 dated 2026-08-05 entries read directly, each carries `Evidence:`/`Confidence:` |
| `.planning/phases/01.4-tool-surface-reachability/01.4-LIVE-EVIDENCE.md` | Live evidence for criteria 1, 3, 4 | ✓ VERIFIED as a document; ⚠ its own Criterion 4 section is the source of the criterion-3 discrepancy this report flags |
| `01.6.2-VALIDATION.md` `CF-01.4-NN` table | All 35 rows dispositioned | ✓ VERIFIED, spot-checked against live host state |
| `.planning/incidents/20260805160045723-port6603-epoch1.md` + `.png` | Pre-kill evidence for the deliberate recycle | ✓ VERIFIED, read directly |

### Anti-Patterns Found

None. No `TBD`/`FIXME`/`XXX` markers in any file this phase modified. The one deliberate "vice-proxy:" residual (`VICE_SEAM_HAZARDS_TEST_FIXTURE`) is env-var-gated and out of agent reach — a legitimate carve-out, not a debt marker.

### Requirements Coverage

Not applicable — v1.1 carries no `REQUIREMENTS.md` REQ-IDs by design (matching 01.1/01.2/01.3, confirmed in every plan's own frontmatter note).

### Human Verification Required

#### 1. Fresh-session confirmation that criterion 3's generic-dispatch closure is visible/effective from a genuinely new top-level session

**Test:** Start a brand-new top-level Claude Code session (a new conversation, not a `/gsd-execute-phase` subagent dispatch — subagents appear to inherit the parent session's already-established vice-proxy MCP connection and its schema snapshot from whenever that connection was first made). From that new session, call `mcp__vice__tools_list` and inspect its result/the tool inventory Claude Code reports. Then call `mcp__vice__tools_call` with a **benign** nested name, e.g. `{"name": "vice_ping", "arguments": {}}` — **never** nest `vice_disk_list`, that IS the crash hazard the whole mechanism exists to prevent.

**Expected:** `tools_list`, `tools_call`, `initialize` and `notifications_initialized` are absent from the served tool inventory (per the construction-time filter at `vice-proxy.ts:2952-2956`), and any direct `tools/call` naming one of them by its outer name returns `isError: true` with `denyListRefusalMessage()`'s bypass-hazard wording — matching what `vice-proxy.test.ts`'s already-passing unit tests prove against a stand-in host.

**Why human:** Claude Code snapshots an MCP server's tool schema once per session and never refreshes it — this is this project's own confirmed, load-bearing mechanism (`01.4-RESEARCH.md`). `01.4-01`'s DENY_LIST fix landed in commits `0a1323f`/`ee0cece` at 14:52–15:00 UTC on 2026-08-05. `01.4-02`'s own recorded live evidence (`01.4-LIVE-EVIDENCE.md`'s Criterion 4 section, its task 1 self-report) was captured at 15:22 UTC — chronologically after the fix — yet it still lists `mcp__vice__initialize`, `mcp__vice__notifications_initialized`, `mcp__vice__tools_call` and `mcp__vice__tools_list` as present in the deferred tool inventory it observed. That is the opposite of what a session holding a post-fix schema snapshot should show, and is far more consistent with every subagent dispatch across this phase (01.4-01, -02, -03, -04, -05, all committed within roughly two hours of each other on the same day) sharing one long-lived top-level session's MCP connection established before `01.4-01` ever ran, rather than each dispatch getting the independent "fresh session" `01.4-01`'s own SUMMARY assumed when it deferred this exact check to "the orchestrator or a fresh session." No subagent dispatch — including this verification's own, which has no `mcp__vice__*` tools in its exposed surface at all — can open a genuinely new top-level session to close this loop; it requires a human (or the developer's next real session) to do so.

### Gaps Summary

No `gaps_found`-tier defect was located: every artifact the phase's plans claim to have created exists, is substantive, and is wired exactly as described, and I independently re-derived that from the code and from live host-state files rather than trusting the SUMMARYs' narrative. The single item routing this phase to `human_needed` is a **verification-completeness gap, not a code gap**: criterion 3's own "live session call and nothing else" standard (ROADMAP.md's explicit words for this phase) was not actually met for the generic-dispatch closure's client-visible effect, and the evidence the phase recorded as if it closed that loop in fact reads as evidence it did not. The underlying enforcement — the actual thing standing between an agent and the `vice_disk_list` crash hazard — is solid, code-verified, and unit-tested against a stand-in host with zero real requests escaping. Recommend: perform the one fresh-session check above, record the result (positive or negative — a negative result here would be a real and actionable finding, not a failure of this phase's engineering), and close criterion 3 for good.

---
*Verified: 2026-08-05T16:38:30Z*
*Verifier: Claude (gsd-verifier)*
