---
phase: 01.6.3-mastra-mcp-adoption
plan: 03
type: execute
wave: 3
depends_on: ["01.6.3-02"]
files_modified:
  - .claude/mcp/vice/vice-proxy.ts
  - .claude/mcp/vice/vice-proxy.test.ts
autonomous: true
requirements:
  - "A"
  - "B"
  - "C"
user_setup: []

must_haves:
  truths:
    - "Every manifest tool (all ~43 remaining, plus the `vice_ping` tracer already covered) is registered through `buildViceTool()`, with no per-tool hand-written registration block — one generic loop over `readManifestTools()`"
    - "`tools/list`'s full output (name set, order, description, inputSchema, `_meta`) matches today's pre-swap output exactly, except for `vice_disk_list`'s deliberate absence — proven by a direct comparison against the manifest, not by spot-checking one tool"
    - "`COVERAGE.md`'s existing capability matrix still accurately describes the shipped implementation — verified against real code, not left as a planning-time artifact nobody re-checked"
    - "`resources/` stays byte-for-byte untouched by this whole phase — `vice-proxy.ts` was never in `tsconfig.build.json`'s host-bound include list and still is not"
  artifacts:
    - ".claude/mcp/vice/vice-proxy.ts — the generic tool-registry builder, now covering the full manifest"
    - ".claude/mcp/vice/vice-proxy.test.ts — full tools/list name-set/order/schema parity test"
  key_links:
    - "readManifestTools() (unfiltered by any subset) → DENY_LIST filter → buildViceTool() loop → the full tools map MCPServer serves"
    - "the full tools map's key insertion order → tools/list's wire order — must match today's [...manifestTools, RESULT_CONTINUE_TOOL, RECYCLE_TOOL, DIAGNOSE_TOOL] ordering"
---

<objective>
Expand Plan 02's proven single-tool seam to the full manifest: register every remaining VICE tool
(~43, everything `vice_ping` didn't already cover) through the same `buildViceTool()` mechanism, in
a single generic loop — not 43 hand-written blocks — and prove `tools/list`'s full output matches
today's pre-swap shape exactly, tool-for-tool, aside from `vice_disk_list`'s deliberate absence.

Purpose: Plan 02 deliberately left ~43 tools unregistered as a disclosed, functionality-only gap
(never an architectural one — the tracer proved the exact mechanism this plan now just repeats).
This plan closes that gap and is where "the observable output is unchanged" (COVERAGE.md's own
framing for the `tools/list` surface) actually gets proven at full scale, not on one tool.

Output: every manifest tool reachable through `@mastra/mcp`, `tools/list` parity proven against the
full manifest, `COVERAGE.md` re-confirmed against the shipped code.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.3-mastra-mcp-adoption/COVERAGE.md
@.planning/phases/01.6.3-mastra-mcp-adoption/01.6.3-02-SUMMARY.md
@.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-PATTERNS.md
@.claude/mcp/vice/vice-proxy.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register every remaining manifest tool through the proven `buildViceTool()` loop</name>
  <files>.claude/mcp/vice/vice-proxy.ts</files>
  <read_first>
    - `.claude/mcp/vice/vice-proxy.ts` — Plan 02's `rawJsonSchemaAsStandardSchema()`, `forwardToVice()`,
      `buildViceTool()`, the tool-registry construction site (currently scoped to `vice_ping` +
      the three synthetics), and the `CallToolRequestSchema` override.
    - `.claude/mcp/vice/tools-manifest.json` — the full manifest, to confirm the real tool count and
      spot-check a couple of non-trivial schemas (e.g. one with nested `properties`, one with
      `required`, one with none) before assuming the generic loop handles every shape.
  </read_first>
  <behavior>
    - Given the full, unfiltered `readManifestTools()` output, the registry-building loop produces
      exactly `{count of manifest tools} - 1 (vice_disk_list)` real forwarding tools plus the 3
      synthetics, with no tool requiring a special case beyond the deny-list filter already proven
      in Plan 02.
    - `tools/list`'s response, diffed key-by-key against a manifest-derived expectation (name,
      description, inputSchema, the `_meta` cap stamp), matches for every tool.
  </behavior>
  <action>
Remove the `vice_ping`-only restriction Plan 02's tracer used and replace it with the full,
unfiltered `readManifestTools()` result, still filtered by `DENY_LIST` exactly as before (the
predicate does not change — only the input set widens from one tool to all of them). The loop body
is unchanged from Plan 02: `tools[def.name] = buildViceTool(def, (args) => forwardToVice(def.name,
args))` for each surviving manifest entry, then the three synthetic tools appended in the same
order as today's `[...manifestTools, RESULT_CONTINUE_TOOL, RECYCLE_TOOL, DIAGNOSE_TOOL]` (line
~451) — insertion order into the `tools` object must match, since `MCPServer`'s own `tools/list`
handler iterates its tool registry in the order tools were registered and this project's existing
tests may assert on order.

If any manifest tool's schema shape breaks `rawJsonSchemaAsStandardSchema()`'s assumptions (e.g. a
schema using `oneOf`/`anyOf`/`$ref` the adapter's pass-through does not need to interpret, since it
never actually validates — confirm this is true rather than assuming it), fix it generically in the
adapter, not with a per-tool branch. The adapter's whole value is that it never needs to understand
the schema's contents, only to carry it through unchanged.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && npm --prefix .claude/mcp/vice run typecheck && node --test .claude/mcp/vice/vice-proxy.test.ts 2>&1 | tail -60</automated>
  </verify>
  <done>Every manifest tool other than `vice_disk_list` is callable through the new dispatch; no tool required a hand-written special case in the registration loop itself.</done>
</task>

<task type="auto">
  <name>Task 2: Full `tools/list` parity proof, `resources/` untouched confirmation, `COVERAGE.md` re-check</name>
  <files>.claude/mcp/vice/vice-proxy.test.ts</files>
  <action>
Write a parity test that reads `tools-manifest.json` directly, computes the expected `tools/list`
name set (every manifest name except `vice_disk_list`, plus the three synthetic names, in that
order), spawns the real proxy, sends `tools/list`, and asserts: (a) the name sets are set-equal and
in the same relative order, (b) for every manifest-derived tool, the wire `inputSchema` deep-equals
the manifest's own raw schema for that tool (not just `vice_ping`, as Plan 02 proved — every tool),
(c) every tool entry carries the `_meta["anthropic/maxResultSizeChars"]` stamp equal to
`OUTPUT_CHAR_CAP`.

Confirm `git status --porcelain .claude/mcp/vice/resources/` is empty after this task's changes —
`vice-proxy.ts` has never been in `tsconfig.build.json`'s host-bound `include` list and this plan
does not add it there, so the generated-resources tier should show zero diff. State this explicitly
in the SUMMARY as a checked fact, not an assumption (the hard constraint this phase carries: "a
correct `resources/` diff here is probably empty — say what correct looks like").

Re-read `COVERAGE.md` against the now-complete implementation. Its matrix was written at plan time
(2026-08-02) against the *intended* scope; confirm every `INTEGRATE`/`OPT-OUT` row still describes
what actually shipped (in particular: `MCPServer` — `tools/call` routing skeleton is now more
precisely "INTEGRATE for `tools/list` and `execute()`'s success/failure semantics are NOT relied on
for the wire envelope" per Plan 02's documented mismatch finding — update that row's `reason`
column to say so plainly if it does not already, rather than leaving a stale description standing
next to working code).
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && node --test --test-name-pattern="tools/list" .claude/mcp/vice/vice-proxy.test.ts && git status --porcelain .claude/mcp/vice/resources/ | (! grep .)</automated>
  </verify>
  <done>The full-manifest parity test passes; `resources/` shows zero diff; `COVERAGE.md`'s `tools/call routing skeleton` row is confirmed accurate or corrected in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the full tool surface, expanded from one tool to ~46 | Same boundary Plan 02 established (Claude Code ↔ proxy over stdio); this plan is a scale-up, not a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.3-09 | Tampering | a manifest tool silently missing from the new registry (a loop bug rather than a per-tool hand-edit) | medium | mitigate | Task 2's parity test asserts the FULL name set, not a sample — a missing tool fails the test rather than shipping unnoticed |
| T-01.6.3-10 | Information Disclosure | a manifest tool's real `inputSchema` degraded to a generic/empty schema by the adapter | low | mitigate | Task 2's deep-equal assertion against the manifest's own raw schema, for every tool, not just `vice_ping` |
| T-01.6.3-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan. Row retained so the register is complete rather than silently absent |

No `high` threat is left undispositioned, so nothing here blocks under `workflow.security_block_on: high`.
</threat_model>

<verification>
- `npm --prefix .claude/mcp/vice run typecheck` — exit 0.
- Full `tools/list` parity test passes against the complete manifest.
- `git status --porcelain .claude/mcp/vice/resources/` — empty.
- `COVERAGE.md` re-confirmed or corrected in this plan's commit, not left stale.
</verification>

<success_criteria>
Every VICE tool the manifest names (minus `vice_disk_list`) is reachable through `@mastra/mcp`'s
`MCPServer`, registered by one generic mechanism, with `tools/list`'s full output proven identical
to the pre-swap shape. Plan 04 can now run the full pre-existing regression suite against a
feature-complete implementation.
</success_criteria>

<output>
Create `.planning/phases/01.6.3-mastra-mcp-adoption/01.6.3-03-SUMMARY.md` when done
</output>
