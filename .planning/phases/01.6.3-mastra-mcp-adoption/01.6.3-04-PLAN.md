---
phase: 01.6.3-mastra-mcp-adoption
plan: 04
type: execute
wave: 4
depends_on: ["01.6.3-03"]
files_modified:
  - .claude/mcp/vice/vice-proxy.test.ts
  - .claude/mcp/vice/vice-proxy.ts
  - .planning/phases/01.6.3-mastra-mcp-adoption/COVERAGE.md
autonomous: true
requirements:
  - "B"
  - "C3"
  - "D-01"
user_setup: []

must_haves:
  truths:
    - "The full, pre-existing ~5,300-line vice-proxy.test.ts suite passes against the completed @mastra/mcp implementation, with every deliberate wire-level behavior change disclosed and justified, not silently patched"
    - "The ~88-94% of hand-rolled logic (broker leasing, epoch/liveness, recycle/diagnose, deny-list, path rewriting, incident capture, the ten broker-state message strings) is proven unchanged by the full suite, which is what this criterion has rested on since RESEARCH.md §B4 measured the split"
    - "A rollback path back to the fully hand-rolled seam is documented concretely enough that a future session could execute it without re-deriving the design from scratch"
    - "The whole phase closes with zero net diff under .claude/mcp/vice/resources/"
  artifacts:
    - ".claude/mcp/vice/vice-proxy.test.ts — the full suite, green"
    - ".claude/mcp/vice/vice-proxy.ts — a header comment naming the rollback path"
    - ".planning/phases/01.6.3-mastra-mcp-adoption/COVERAGE.md — final-state confirmation"
  key_links:
    - "the full vice-proxy.test.ts suite → every hand-rolled function named in 01.6-PATTERNS.md's D-01 seam list → proof the swap changed only the wire layer"
---

<objective>
Run the entire pre-existing regression suite against the now-complete `@mastra/mcp` implementation,
fix or honestly document every difference it surfaces, and close the phase: a concrete rollback
path, a final `COVERAGE.md` confirmation, and a clean `resources/` diff across the whole phase.

Purpose: criterion B's own words are "the 4,370-line proxy test suite is what proves the swap" —
that suite is now ~5,300 lines and this is the plan where it actually runs, in full, against the
finished implementation, rather than the tool-scoped subsets Plans 02 and 03 ran. This is also
where the phase's one `costly` (not `one-way`) reversibility rating gets its actual rollback
documentation, per the ROADMAP's own risk framing: "Research rated the fit a mismatch... reversible,
but only by re-hand-rolling the seam."

Output: full suite green, every deliberate behavior change disclosed, a written rollback path, the
phase sealed with `resources/` unchanged.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.3-mastra-mcp-adoption/COVERAGE.md
@.planning/phases/01.6.3-mastra-mcp-adoption/01.6.3-02-SUMMARY.md
@.planning/phases/01.6.3-mastra-mcp-adoption/01.6.3-03-SUMMARY.md
@.planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-PATTERNS.md
@.claude/mcp/vice/vice-proxy.ts
@.claude/mcp/vice/vice-proxy.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full-suite regression repair, every difference disclosed</name>
  <files>.claude/mcp/vice/vice-proxy.test.ts, .claude/mcp/vice/vice-proxy.ts</files>
  <action>
Run the complete `vice-proxy.test.ts` suite (`node --test .claude/mcp/vice/vice-proxy.test.ts`)
against the Plan 03 implementation. Plan 02's Task 2 already produced a classified list of
failures against the tool-scoped tracer (category (a): expects a not-yet-registered tool — should
now be ZERO, since Plan 03 registered everything; category (b): exact wire-level text/shape
assertion that provably changed; category (c): genuine regression). Start from that list rather
than re-triaging from zero, and confirm every category-(a) item is now passing unmodified now that
its tool exists.

For any NEW failure this full run surfaces that Plan 02's tool-scoped run could not have seen
(anything specific to a tool other than `vice_ping`/the three synthetics — e.g. the four
path-resolving tools' trailing path-note behavior, chunking on a large-output tool, an epoch-drift
scenario exercised through a different tool than the one Plan 02 tested), triage it the same way:
category (b) or (c), never silently absorbed.

Pay specific, deliberate attention to these categories, each named because this session's own
reading of the compiled SDK source found a concrete reason it might differ:

1. **Protocol-level malformed-request handling.** Today, `handleMessage()`'s catch block and
   `ProtocolError` turn a missing/malformed `tools/call` `params.name` into a JSON-RPC `error`
   object (code -32602), distinct from a tool-call `isError:true` result (Pattern 2: "reserved for
   genuinely missing/malformed params on this method itself"). `@modelcontextprotocol/sdk`'s own
   `Protocol.setRequestHandler` validates incoming requests against the method's Zod schema before
   ever calling a handler — confirm what code/message it produces for a malformed `tools/call` and
   for an unknown top-level method, and reconcile any existing test asserting the OLD exact code/
   message. If the SDK's own code differs from -32602/-32601, that is a disclosed, deliberate
   change (the SDK now owns protocol-level validation) — record the old and new values in the
   SUMMARY, do not silently update the assertion without saying so.
2. **`initialize`'s exact response shape.** Confirm `capabilities.tools.listChanged`,
   `serverInfo.name`/`version`, and the protocol-version negotiation still satisfy every existing
   assertion; if `MCPServer`'s own `getServerInfo()` produces a different `name`/`version` than the
   hardcoded `{name: "vice", version: PROXY_VERSION}` this file used to answer with directly, that
   is fixable by passing the same values into `MCPServer`'s constructor (already done in Plan 02) —
   confirm it, do not assume it.
3. **The manifest-hot-reload-loss disclosure from Plan 02** — if any existing test exercises
   `VICE_TOOLS_MANIFEST` changing mid-session (unlikely, but check), it now requires a proxy
   restart; if such a test exists, it needs updating with the disclosure recorded, not deleting
   silently.

Every fix in this task lands with the SUMMARY stating, per changed assertion: what it asserted
before, what it asserts now, and why (SDK-owned validation vs. a genuine bug this task fixed in
`vice-proxy.ts` itself). This is the record criterion B's "proves the swap" claim rests on.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && node --test .claude/mcp/vice/vice-proxy.test.ts 2>&1 | tail -20 && npm --prefix .claude/mcp/vice run typecheck</automated>
  </verify>
  <done>The full suite reports 0 failures. Every changed assertion is accompanied by a before/after/why note in the SUMMARY. The pre-existing `broker-kill.test.ts` banner-ordering flake (documented, unrelated to this phase) is the only acceptable non-pass, and only under full-suite load, not in isolation.</done>
</task>

<task type="auto">
  <name>Task 2: Rollback documentation, final COVERAGE.md confirmation, phase close-out</name>
  <files>.claude/mcp/vice/vice-proxy.ts, .planning/phases/01.6.3-mastra-mcp-adoption/COVERAGE.md</files>
  <action>
Add a header comment block near the top of `vice-proxy.ts`, immediately after the existing file-
level header comment, naming: (a) that this file's wire layer is `@mastra/mcp`'s `MCPServer` per
developer decision D-01 (taken against `01.6-RESEARCH.md` §B6's HIGH-confidence recommendation to
stay hand-rolled), (b) the exact list of functions that constitute "the seam" (`writeMessage`/
`respond`/`errorResponse`/`handleInitialize`/`handleToolsList`/`handleMessage`/`handleLine`/the
stdin loop/`ProtocolError` — all deleted; superseded by `MCPServer` construction + the
`CallToolRequestSchema` override), and (c) the concrete rollback path: re-author those deleted
functions from `01.6-PATTERNS.md`'s "Pattern: The D-01 Seam" section (which quotes their pre-swap
bodies verbatim) and `.claude/mcp/vice/vice-proxy.ts`'s own git history at this phase's commits;
remove the `@mastra/mcp`/`@mastra/core` dependency from `package.json`; re-point each tool's
`execute` body (currently `forwardToVice(name, args)`, unchanged by the rollback) back into a
single `handleToolsCall()` dispatcher. State explicitly that this is `costly`, not `one-way`: the
~88-94% hand-rolled logic never moved, so a rollback touches only the wire layer this phase
touched, not the broker/epoch/probe/path-rewrite/incident machinery underneath it.

Re-read `COVERAGE.md` one final time against the fully closed-out implementation (Plan 03's Task 2
already did an interim check; this is the final confirmation after Task 1's regression fixes may
have touched wire-level behavior further). Update the "Matrix produced at plan time" footer note to
record the date this final confirmation happened and by which plan, so a later reader can tell
planning-time intent from shipped-and-verified fact.

Confirm the phase-wide `resources/` diff: `git diff --stat <first commit of this phase>..HEAD --
.claude/mcp/vice/resources/` must be empty. If it is not, that is a genuine problem to fix in this
task, not a note to carry forward — `vice-proxy.ts` was never build-emitted and this phase never
changed that.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && node --test '.claude/mcp/vice/'*.test.* 2>&1 | tail -10 && git status --porcelain .claude/mcp/vice/resources/ | (! grep .)</automated>
  </verify>
  <done>The rollback path is documented in `vice-proxy.ts` itself, concretely enough to execute without re-deriving the design; `COVERAGE.md` carries a final-confirmation footer; the full project test suite (not just this file) is green; `resources/` shows zero diff across the whole phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the completed seam, under full regression load | No new boundary — this plan is verification and documentation, not new surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.3-12 | Repudiation | a silently-adjusted test assertion masking a real behavior regression as an "SDK difference" | high | mitigate | Task 1's explicit before/after/why requirement for every changed assertion, recorded in the SUMMARY, not just in the diff |
| T-01.6.3-13 | Denial of Service | a rollback attempted without documentation, mid-incident, re-deriving the deleted functions from memory | medium | mitigate | Task 2's rollback comment names the exact function list and points at `01.6-PATTERNS.md`'s verbatim quotes plus git history — a future session does not start from zero |
| T-01.6.3-14-SC | Tampering | npm/pip/cargo installs | n/a | accept | No new package installs in this plan. Row retained so the register is complete rather than silently absent |

No `high` threat is left undispositioned, so nothing here blocks under `workflow.security_block_on: high`.
</threat_model>

<verification>
- `node --test '.claude/mcp/vice/'*.test.*` — full project suite, 0 failures (the documented `broker-kill.test.ts` flake under full-suite load excepted).
- `npm --prefix .claude/mcp/vice run typecheck` — exit 0.
- `git status --porcelain .claude/mcp/vice/resources/` — empty, checked as a phase-wide fact.
- `COVERAGE.md` carries a final-confirmation note distinguishing planning-time intent from shipped fact.
</verification>

<success_criteria>
The full pre-existing test suite proves the swap: every hand-rolled function underneath the wire
layer works unchanged, every deliberate wire-level difference is disclosed with its reasoning, a
rollback path exists in writing, and the phase closes with zero `resources/` drift. Criteria A
through E are all satisfied: A (D-01 adopted, seam cut exactly where PATTERNS.md specified), B (the
88-94% proven unchanged by the full suite), C (COVERAGE.md accurate against shipped code), D (D-06
decided and implemented in Plan 01), E (the legitimacy gate discharged in Plan 01 by a pre-recorded,
re-confirmed developer approval).
</success_criteria>

<output>
Create `.planning/phases/01.6.3-mastra-mcp-adoption/01.6.3-04-SUMMARY.md` when done
</output>
