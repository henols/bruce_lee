---
phase: 01.6.3-mastra-mcp-adoption
plan: 01
subsystem: infra
tags: [mastra, mastra-mcp, npm, devcontainer, telemetry, posthog, supply-chain]

# Dependency graph
requires:
  - phase: 01.6-broker-in-node-and-tcp-control-plane
    provides: the TypeScript vice-proxy.ts/.mcp.json seam this phase's Plan 02 will swap onto @mastra/mcp
provides:
  - "@mastra/mcp@1.15.0 and @mastra/core@1.55.0 installed, exact-pinned, in .claude/mcp/vice/package.json's first-ever dependencies block"
  - "D-06 decided and implemented: provision via npm ci in devcontainer.json's postCreateCommand, not bundling"
  - "A structural, source-text/installed-tree test proving @mastra/core/telemetry is unreachable from this project's import graph"
  - "MASTRA_TELEMETRY_DISABLED=1 set in .mcp.json as a disclosed secondary measure"
affects: [01.6.3-02-mastra-mcp-seam-swap]

# Tech tracking
tech-stack:
  added: ["@mastra/mcp@1.15.0", "@mastra/core@1.55.0"]
  patterns:
    - "Exact-pin runtime dependencies in .claude/mcp/vice/package.json (matching the existing typescript/@types/node convention); regenerate package-lock.json via npm install, never hand-edit."
    - "Structural, source-text/installed-tree node:test guards over trusting an env var the upstream project's own issue tracker documents as unreliable -- same idiom as load-order.test.ts's statement-anchored import-cycle guard."
    - "Provision-not-bundle for container-only runtime dependencies: npm ci wired into devcontainer.json's postCreateCommand rather than vendoring a bundled copy into authored source."

key-files:
  created:
    - .claude/mcp/vice/telemetry-import.test.ts
  modified:
    - .claude/mcp/vice/package.json
    - .claude/mcp/vice/package-lock.json
    - .devcontainer/devcontainer.json
    - .claude/CLAUDE.md
    - .mcp.json

key-decisions:
  - "D-06: provision (npm ci --prefix .claude/mcp/vice in devcontainer.json's postCreateCommand), not bundle -- decided against the real measured ~69 MB/35-direct-dependency figure, not the small-transport-seam figure the ROADMAP criterion was originally written against."
  - "Telemetry disablement: a structural node:test guard (statement-anchored source scan plus a live re-derivation-and-grep of the installed @mastra/mcp entry files' @mastra/core subpaths) is the PRIMARY mechanism; MASTRA_TELEMETRY_DISABLED=1 is added only as a disclosed, explicitly-secondary belt-and-braces measure, per upstream mastra-ai/mastra#7813 reporting the env var alone is unreliable."
  - "Do not add @modelcontextprotocol/sdk as a direct dependency -- it arrives as an already-resolved transitive dependency of @mastra/mcp and adding it directly would trigger an unplanned second legitimacy gate."

requirements-completed: ["A", "E", "D-01", "D-06"]

coverage:
  - id: D1
    description: "@mastra/mcp@1.15.0 and @mastra/core@1.55.0 installed, exact-pinned, in package.json's first dependencies block; package-lock.json regenerated; node_modules populated"
    requirement: "E"
    verification:
      - kind: unit
        ref: "npm view @mastra/mcp@1.15.0 version / npm view @mastra/core@1.55.0 version (both print exactly the pinned version)"
        status: pass
      - kind: other
        ref: "test -d .claude/mcp/vice/node_modules/@mastra/mcp && test -d .claude/mcp/vice/node_modules/@mastra/core"
        status: pass
    human_judgment: false
  - id: D2
    description: "D-06 decided (provision, not bundle) and implemented: devcontainer.json's postCreateCommand runs npm ci --prefix .claude/mcp/vice"
    requirement: "D-06"
    verification:
      - kind: other
        ref: "grep 'npm ci --prefix .claude/mcp/vice' .devcontainer/devcontainer.json"
        status: pass
    human_judgment: false
  - id: D3
    description: "Structural test proving @mastra/core/telemetry is never imported by this project's own source or reachable through @mastra/mcp's installed entry files' @mastra/core subpaths"
    requirement: "D-01"
    verification:
      - kind: unit
        ref: ".claude/mcp/vice/telemetry-import.test.ts (5 tests, all pass)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Package-legitimacy gate (Task 1, blocking-human) discharged against the two pre-recorded developer approvals in 01.6.3-APPROVALS.md, re-confirmed live against the registry"
    requirement: "A"
    verification:
      - kind: other
        ref: "npm view @mastra/mcp@1.15.0 version and npm view @mastra/core@1.55.0 version both print exactly the recorded pinned version; both approval quotes present in 01.6.3-APPROVALS.md"
        status: pass
    human_judgment: false

duration: 55min
completed: 2026-08-05
status: complete
---

# Phase 01.6.3 Plan 01: Mastra MCP Dependency Install, D-06 Decision, Telemetry Guard Summary

**`@mastra/mcp@1.15.0`/`@mastra/core@1.55.0` installed exact-pinned as this repo's first runtime dependency, D-06 decided (provision via `npm ci` in devcontainer provisioning, not bundling) against the real 69 MB/35-dependency figure, and a structural node:test guard proving the bundled PostHog telemetry client is unreachable from this project's import graph — both against the installed tree, not just the pre-install snapshot.**

## Performance

- **Duration:** 55 min
- **Started:** 2026-08-05T11:17:00Z
- **Completed:** 2026-08-05T12:12:20Z
- **Tasks:** 3 (1 pre-answered checkpoint, 2 auto)
- **Files modified:** 6 (1 created, 5 modified)

## Accomplishments
- Confirmed both pre-recorded developer approvals in `01.6.3-APPROVALS.md` still hold: `npm view @mastra/mcp@1.15.0 version` and `npm view @mastra/core@1.55.0 version` each printed exactly the pinned version, so Task 1's `blocking-human` package-legitimacy gate was discharged without a new developer prompt.
- Added `.claude/mcp/vice/package.json`'s first-ever `dependencies` block (`@mastra/mcp@1.15.0`, `@mastra/core@1.55.0`, both exact-pinned), ran `npm install` to regenerate `package-lock.json` and populate `node_modules` (237 packages added, no unexpected postinstall output — both packages independently confirmed `postinstall: none`).
- Decided D-06 in writing against the real measured figure (~69 MB unpacked, 35 direct dependencies, including a triple-vendored `@ai-sdk/provider-v5`/`v6`/`v7` set) rather than the small-transport-seam figure the ROADMAP criterion was originally written against: **provision, don't bundle**. Wired `npm ci --prefix .claude/mcp/vice` into `.devcontainer/devcontainer.json`'s `postCreateCommand`, and recorded the decision, its reasoning, and its disclosed cost (a bare `node`-launched, devcontainer-bypassing clone no longer works until `npm ci` runs once) in `.claude/CLAUDE.md`'s Emulator Access section.
- Wrote `.claude/mcp/vice/telemetry-import.test.ts` (5 `node:test` cases): a statement-anchored static guard over this project's own `.ts`/`.mts` sources (matching `load-order.test.ts`'s idiom exactly), plus a live guard that re-derives every `@mastra/core` subpath the *installed* `@mastra/mcp` package's own entry files reference and greps each resolved file for `posthog`/`usage-telemetry`/`telemetry` markers — re-verifying `01.6.3-APPROVALS.md`'s `npm pack`-based finding against what actually shipped into `node_modules`, per the plan's explicit instruction not to trust the pre-install snapshot alone.
- Added `MASTRA_TELEMETRY_DISABLED=1` to `.mcp.json`'s `vice` server `env` block as a disclosed, explicitly-secondary measure (per upstream `mastra-ai/mastra#7813`, the variable alone is not a reliable control — the structural test above is the primary mechanism).
- Full existing suite re-confirmed green after this plan's changes: **430 tests / 425 pass / 0 fail / 5 todo** (420 baseline + 5 new telemetry tests, zero regressions).

## Task Commits

Each task was committed atomically:

1. **Task 1: Package legitimacy gate** — no commit (pre-answered checkpoint; nothing built, both approvals and live registry checks confirmed, proceed-to-install decision recorded here)
2. **Task 2: Install the dependency, exact-pinned, and decide D-06** — `76ce2e6` (feat)
3. **Task 3: Structural telemetry-non-import guard, plus belt-and-braces env var** — `028ea6c` (test, includes an inline Rule-1 fix to Task 2's own `.claude/CLAUDE.md` addition — see Deviations)

_Note: Task 3's commit also carries the fix for the regression its own full-suite run caught; see below._

## Files Created/Modified
- `.claude/mcp/vice/package.json` — first `dependencies` block: `@mastra/mcp@1.15.0`, `@mastra/core@1.55.0`, exact-pinned
- `.claude/mcp/vice/package-lock.json` — regenerated by `npm install`, not hand-edited
- `.devcontainer/devcontainer.json` — `postCreateCommand` gains `&& npm ci --prefix .claude/mcp/vice`
- `.claude/CLAUDE.md` — Emulator Access section records the D-06 decision, its reasoning, and its disclosed cost
- `.claude/mcp/vice/telemetry-import.test.ts` — new, 5 structural `node:test` cases proving telemetry unreachability
- `.mcp.json` — `vice` server entry gains `env: { MASTRA_TELEMETRY_DISABLED: "1" }`

## Decisions Made
- **D-06 (provision, not bundle):** see key-decisions above. Recorded in `.claude/CLAUDE.md` per the plan's instruction to place it beside the existing three-tier `.claude/mcp/` description rather than in a GSD-managed block.
- **Telemetry primary/secondary split:** the structural test is the enforcement mechanism; the env var is disclosed-secondary only, matching this project's own established idiom (skills' import-purity tests, `HOSTPATH_ALLOW_LIST`) rather than trusting an upstream-documented-unreliable env var.
- **No second legitimacy gate for `@modelcontextprotocol/sdk`:** left as an already-resolved transitive dependency of `@mastra/mcp`, per the plan's explicit instruction, rather than added to `package.json`'s own `dependencies`.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `.claude/CLAUDE.md`'s D-06 prose leaked two internal module names, caught by the full-suite run**
- **Found during:** Task 3, running the full existing test suite (`node --test '.claude/mcp/vice/'*.test.*`) as required by the plan's overall `<verification>` step
- **Issue:** the D-06 paragraph added to `.claude/CLAUDE.md` in Task 2's commit named `build.ts` and `vice-proxy.ts` verbatim. `skill-docs.test.ts` polices exactly this — agent-facing docs (`.claude/CLAUDE.md`, and two `SKILL.md` files) must never leak an internal module name from `.claude/mcp/vice/`; the agent-facing surface is the `mcp__vice__` tool prefix, never a module by name. This is a real, if self-inflicted, regression against this repo's own baseline (425/420/0/5 on `main`, confirmed twice today per the orchestrator's brief).
- **Fix:** reworded both references to describe the mechanism without naming the file — "the `tsc` step this directory already has is not a bundler" instead of naming `build.ts`; "launching this directory's MCP entry point on bare `node`" instead of naming `vice-proxy.ts`. No change in meaning, only in how it's expressed.
- **Files modified:** `.claude/CLAUDE.md` (further edit on top of Task 2's addition, folded into Task 3's commit since that is when it was caught and fixed)
- **Verification:** `node --test .claude/mcp/vice/skill-docs.test.ts` — all 3 cases pass; full suite re-run afterward — 430/425/0/5, no other failures.
- **Committed in:** `028ea6c` (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 self-inflicted bug, Rule 1)
**Impact on plan:** The fix corrects a doc-policy regression this plan's own Task 2 introduced; no scope creep, no change to the D-06 decision's substance.

## Issues Encountered
- **Plan's Task 2 `<verify>` automated command has a quoting mismatch, not a real failure.** The literal command `node -e "console.log(require('...').dependencies)" | grep -q '"@mastra/mcp": "1.15.0"'` expects double-quoted keys/values, but Node's `console.log` on a plain object prints single-quoted keys/values (`{ '@mastra/mcp': '1.15.0', ... }`), so the grep never matches regardless of correctness. Verified every underlying condition individually instead (node_modules present for both packages, `package.json`'s `dependencies` object printed and inspected directly, devcontainer.json's `npm ci` line present) — all true. Not fixed in the plan file itself since this plan's own scope is execution, not plan authorship; flagging here so a future re-run of this exact command isn't mistaken for a real regression.
- **A low-severity transitive `npm audit` finding surfaced after install**, unrelated to either pinned package's own version: `@ai-sdk/provider-utils` (bundled as `@ai-sdk/provider-utils-v5@3.0.30`, one of the three triple-vendored provider packages `01.6.3-APPROVALS.md` already priced into its 69 MB/35-dependency figure) has a documented Uncontrolled Resource Consumption advisory (GHSA-866g-f22w-33x8) at `<=3.0.97`. `npm audit fix --dry-run` reports no available non-breaking resolution today (no alternate version satisfies `@mastra/mcp`'s peer range on `@mastra/core@1.55.0` while clearing the advisory). This is a transitive dependency of the exact-pinned, developer-approved tree, not something either approval anticipated by name, and its severity is low (DoS, not data exposure). Left as-is rather than force-resolving to an untested transitive version outside the approved pins; noting it here for visibility rather than silently absorbing it into "the install succeeded."

## User Setup Required
None - no external service configuration required. The devcontainer's own `postCreateCommand` now provisions `node_modules` automatically on rebuild; nothing needs manual action.

## Next Phase Readiness
- Plan 02's seam swap can start against a verified, already-installed `@mastra/mcp@1.15.0`/`@mastra/core@1.55.0` tree — no install work, no legitimacy gate, no telemetry question left open for that plan to discover mid-seam-swap.
- The telemetry guard (`telemetry-import.test.ts`) will automatically catch a future `@mastra/mcp` release that starts importing `@mastra/core/telemetry`, or a future direct import of it from Plan 02's own new source — no manual re-derivation needed.
- Known, disclosed, non-blocking: the low-severity transitive `@ai-sdk/provider-utils` advisory noted above; the devcontainer-provisioning narrowing (bare-clone-outside-devcontainer no longer works until `npm ci` runs once, already this project's only supported path anyway).

## Self-Check: PASSED

All files created/modified confirmed present on disk; both task commits (`76ce2e6`, `028ea6c`) confirmed in git log.

---
*Phase: 01.6.3-mastra-mcp-adoption*
*Completed: 2026-08-05*
