---
phase: 01.6.2.1-broker-lifecycle-policy
plan: 07
type: execute
wave: 7
depends_on: ["01.6.2.1-06"]
files_modified:
  - .claude/mcp/vice/vice-broker.mts
  - .claude/mcp/vice/broker-state.mts
  - .claude/mcp/vice/broker-launch.mts
  - .claude/mcp/vice/vice-broker-acquire.test.ts
  - .claude/mcp/vice/broker-launch.test.ts
  - .claude/mcp/vice/broker-state.test.ts
  - .claude/mcp/vice/resources/vice-broker.mjs
  - .claude/mcp/vice/resources/broker-state.mjs
  - .claude/mcp/vice/resources/broker-launch.mjs
  - .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md
  - .planning/RE-FINDINGS.md
  - .planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md
autonomous: true
gap_closure: true
requirements:
  - "L"
  - "M"
  - "D-05"
  - "D-07"
  - "P-02"
  - "P-03"
  - "CR-01"
  - "WR-01"
  - "WR-02"
  - "WR-03"
  - "WR-04"
user_setup: []

must_haves:
  truths:
    - "selectWarmInstance()'s post-probe recheck rejects a candidate a concurrent sibling has already DROPPED, not only one a sibling has already GRANTED -- proven by a deterministic two-overlapping-acquires regression test that fails against the pre-fix code (RED) and passes against the fix (GREEN)"
    - "handleRelease() never signals or deletes an instance that is not the SAME process its own grant was actually issued for, even when a different instance now occupies the granted port number"
    - "A legitimate recycle keeps working: the SAME grant's own recorded pid tracks the recycled process, so a release issued after a recycle still tears down the CORRECT (post-recycle) instance, not the pre-recycle one"
    - "WR-01 (capacity gate ordering) and WR-03 (pid-null cold-launch cleanup) are fixed and tested; WR-04 (module-scoped log-path variable) is fixed via a structural test; WR-02 (serial kill-await on the probe walk) is recorded as a deliberately deferred, reasoned decision, not silently dropped"
    - "01.6.2-VALIDATION.md's Defect-3 disposition (both the seven-criteria table and the five-row defect map) accurately states that the concurrent-acquire gap existed between 01.6.2.1's seal and this plan, and names this plan as what closes it -- not a re-worded 'DISCHARGED here'/'FIXED here' with no history"
  artifacts:
    - ".claude/mcp/vice/vice-broker.mts -- selectWarmInstance()'s map-membership recheck; handleRelease()'s grant-pid identity check; handleAcquire()'s reordered atCapacity() gate and its cold-launch pid-null cleanup; maintainWarmFloorForRealBroker()'s locally-scoped lastWarmLaunchLogRelPath"
    - ".claude/mcp/vice/broker-state.mts -- GrantRecord.pid"
    - ".claude/mcp/vice/broker-launch.mts -- handleExit()'s recycle branch syncing the matching grant's pid to the respawned record"
    - ".claude/mcp/vice/vice-broker-acquire.test.ts / broker-launch.test.ts -- the CR-01 concurrent regression test, the stale-grant and recycle-pid-sync regression tests, and the WR-01/WR-03/WR-04 regression tests"
    - ".planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md -- corrected Defect-3 rows in both tables under criterion M"
    - ".planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md -- the WR-02 deferral, with reasoning and both fix options"
  key_links:
    - "handleAcquire() -> selectWarmInstance() -> the map-membership recheck (closes CR-01 at the instant of grant, no await between the recheck and the grant-recording step)"
    - "handleExit()'s recycle branch -> the matching GrantRecord's own pid field (keeps handleRelease()'s new identity check valid across a legitimate recycle)"
    - "handleRelease() -> grant.pid vs state.instances.get(grant.port)?.pid (the cross-session-kill guard CR-01's blast radius required)"
---

<objective>
Close the ONE BLOCKER gap `01.6.2.1-VERIFICATION.md` filed (score 8/9): CR-01, a concurrency race in
`selectWarmInstance()` that lets a sibling `handleAcquire()` hand out an instance a DIFFERENT sibling
has already dropped and is tearing down, and the cross-session-kill blast radius that race opens in
`handleRelease()`. Also dispositions the code review's four Warnings (WR-01..WR-04) -- fix three, defer
one with a written reason -- and corrects `01.6.2-VALIDATION.md`'s Defect-3 row, which currently claims
an unqualified "DISCHARGED here"/"FIXED here" that was only ever true for the non-concurrent case.

Purpose: this is the second gap this phase family has needed closed, and the first one's root cause
was planning scope, not coding -- `superviseChild()` was built and exhaustively unit-tested while
`vice-broker.mts` was outside that plan's `files_modified`, so wiring it was nobody's task and a green
368-test suite reported nothing wrong. CR-01 is a DIFFERENT shape of the same lesson: the code here IS
wired (handleAcquire()/handleRelease() are the real, exported, production entry points, already called
from run()'s own control-listener callbacks), but the race lives INSIDE that already-wired logic and
no test ever drove two overlapping callers against it. Every task below is checked by calling those
SAME real exported functions directly against a real (hand-built, but structurally identical)
`BrokerState` -- this is the established idiom `vice-broker-acquire.test.ts` already uses for
`handleAcquire()`/`handleRelease()`, and it is sufficient BECAUSE the functions under test are the
real wired entry points, not a reimplementation in isolation. A fresh, isolated unit that never calls
`handleAcquire()`/`handleRelease()`/`handleExit()` would reproduce this phase's own first defect rather
than closing this one.

Output: `selectWarmInstance()`'s map-membership recheck; a deterministic two-overlapping-acquires
regression test that is RED against today's code and GREEN against the fix; `handleRelease()`'s
grant-pid identity check plus the `handleExit()` recycle-sync it depends on; WR-01/WR-03/WR-04 fixed
and tested; WR-02 deferred with a written, reasoned todo; `01.6.2-VALIDATION.md`'s Defect-3 rows
corrected with real history, not a relabel.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-CONTEXT.md
@.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-REVIEW.md
@.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-VERIFICATION.md
@.claude/mcp/vice/vice-broker.mts
@.claude/mcp/vice/broker-state.mts
@.claude/mcp/vice/broker-launch.mts
@.claude/mcp/vice/vice-broker-acquire.test.ts
@.claude/mcp/vice/broker-launch.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 -- map-membership recheck in selectWarmInstance(), proven by a forced concurrent race</name>
  <files>.claude/mcp/vice/vice-broker.mts, .claude/mcp/vice/vice-broker-acquire.test.ts, .claude/mcp/vice/resources/vice-broker.mjs</files>
  <behavior>
    - A candidate a sibling acquire has already GRANTED is rejected by the recheck (unchanged from today -- record.state flips to "granted" synchronously, so the existing check already catches this case).
    - A candidate a sibling acquire has already DROPPED (a failed grant-time probe: markDeliberateDeath() + state.instances.delete(record.port), which never touches record.state) is now ALSO rejected by the recheck, so it can never be returned as a winner.
    - Two handleAcquire() calls sharing ONE ready candidate, where the first caller's probe resolves false and the second caller's probe resolves true strictly AFTER the first caller has already deleted the record, both fall through to a fresh cold launch -- neither receives a grant naming the deleted record, and the original record's pid is identity-verified-killed exactly once (by the first caller only).
  </behavior>
  <action>
    In `selectWarmInstance()` (`vice-broker.mts`), broaden the existing post-probe recheck --
    currently `if (record.state !== "ready") continue;`, immediately after `await deps.probe(record.port)`
    and again used to decide whether a probe-live candidate may be returned as the winner -- to ALSO
    verify that `state.instances.get(record.port)` still resolves to the SAME record object (`===`
    identity, not merely a port-number lookup), not merely that `record.state` still reads `"ready"`.
    The drop path a few lines below (a failed probe: `markDeliberateDeath(record, false)` then
    `state.instances.delete(record.port)`) removes the record from the map without ever touching
    `record.state`, so the state-only check is blind to a sibling that has already dropped this exact
    candidate while this call's own probe was in flight -- only a sibling GRANT (which does flip
    `record.state`) is visible to it today. Apply the broadened recheck at both existing use sites
    inside the loop: the `continue` immediately after the probe await, and the `if (isReady)` branch
    that returns the winner. This is the exact patch `01.6.2.1-REVIEW.md`'s CR-01 finding specifies
    (its fix code block, ~lines 139-152) -- implement it as specified, do not weaken it into a
    paraphrase that checks only one of the two conditions.

    Then add the regression test in `vice-broker-acquire.test.ts`, in a new section following the
    existing P-02/P-03 tests, reusing that file's own `loadBrokerModule()` / `createState()` /
    `makeReadyInstance()` / `stubColdSpawnFactory()`-style helpers (write a small variant of
    `stubColdSpawnFactory` that mints a distinct, incrementing fake pid per call, keyed per caller, so
    each caller's own cold-launched record is distinguishable from the original candidate's seeded pid
    and from the OTHER caller's cold-launched record):

    - Seed ONE ready instance shared by both callers (port 6600, a known seeded pid).
    - Build a `probe` stub shared by both `handleAcquire()` calls that returns a DISTINCT,
      externally-resolvable deferred `Promise<boolean>` per invocation, selected strictly by
      invocation ORDER via a call counter (first invocation gets the first deferred, second invocation
      gets the second) -- the race is about invocation order, not about which request id happens to
      run first.
    - Call `handleAcquire("race-a", ...)` and `handleAcquire("race-b", ...)` back to back WITHOUT
      awaiting either call before starting the next, capturing both returned promises. Both calls run
      synchronously up to their own `await deps.probe(...)` suspension point over the SAME snapshotted
      record before either yields -- this reproduces two real concurrent connections racing the same
      candidate, deterministically, without relying on real concurrency.
    - Resolve the FIRST invocation's deferred to `false` (the drop path). Before resolving the SECOND
      invocation's deferred, poll in a small bounded loop (`await Promise.resolve()` repeated, not a
      real timer) until the shared candidate's record is confirmed absent from `state.instances` --
      proving the first caller's drop path has already executed (`markDeliberateDeath` + the
      `delete`) -- THEN resolve the second invocation's deferred to `true`. This removes any
      dependence on undocumented microtask-scheduling order: the ordering the verification's gap
      requires is asserted, not assumed.
    - Use a `kill` stub that always resolves immediately (`Promise.resolve("sigterm")`) -- only the two
      PROBE gates need controlled ordering; the kill's own timing is not part of the race.
    - Await both `handleAcquire()` promises together via `Promise.all`.
    - Run this test FIRST against the unmodified `selectWarmInstance()` (i.e. write it before applying
      the fix above) and confirm it fails on the SECOND caller's own cold-launch spawn count reading 0
      instead of 1 -- record the observed RED failure text in this task's own commit message or in
      `01.6.2.1-07-SUMMARY.md`, matching this phase's own established RED-first discipline (see
      `01.6.2.1-01-SUMMARY.md`). Then apply the fix and confirm GREEN.
    - Assertions (post-fix): both outcomes report `ok: true`; EACH caller's own `buildColdSpawnFactory`
      stub is invoked exactly once (both callers fall through to a fresh cold launch, since the one
      shared candidate failed its probe for the first caller and was gone by the time the second
      caller's probe resolved); the identity-verified `kill` stub is invoked exactly ONCE total, for
      the ORIGINAL candidate's own seeded pid, never a second time; neither outcome's granted port
      resolves (via `state.instances.get(...)`) to the ORIGINAL (now-dropped) record object or its
      original pid; `state.instances` no longer contains the original record at all, only whichever
      fresh cold-launched record(s) ended up occupying ports afterward.

    Rebuild `resources/vice-broker.mjs` (`node build.ts`) after the source edit so the generated tier
    carries the identical fix -- do NOT hand-edit `resources/vice-broker.mjs`.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && node --test --test-name-pattern="CR-01|grant-time probe|ready record" .claude/mcp/vice/vice-broker-acquire.test.ts && npm --prefix .claude/mcp/vice run typecheck</automated>
  </verify>
  <done>The concurrent-acquire regression test passes; every pre-existing test in `vice-broker-acquire.test.ts` and `broker-launch.test.ts` still passes unchanged; `resources/vice-broker.mjs` rebuilt and carries the same fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: handleRelease()'s grant-pid identity check, and the recycle sync it depends on</name>
  <files>.claude/mcp/vice/vice-broker.mts, .claude/mcp/vice/broker-state.mts, .claude/mcp/vice/broker-launch.mts, .claude/mcp/vice/vice-broker-acquire.test.ts, .claude/mcp/vice/broker-state.test.ts, .claude/mcp/vice/broker-launch.test.ts, .claude/mcp/vice/resources/broker-state.mjs, .claude/mcp/vice/resources/broker-launch.mjs, .claude/mcp/vice/resources/vice-broker.mjs</files>
  <behavior>
    - handleRelease() on a grant whose recorded pid MATCHES the port's current occupant: unchanged from today (delete the instance, mark it for death, fire-and-forget identity-verified-kill).
    - handleRelease() on a grant whose recorded pid does NOT match the port's current occupant (a stale/orphaned grant -- the port now belongs to an unrelated instance, whether via CR-01's race, an ordinary crash-and-respawn, or a give-up-and-free): the grant's own bookkeeping is removed, but the mismatched CURRENT occupant is left running -- neither deleted nor signalled -- and a distinct log line names both pids.
    - handleExit()'s recycle branch: after a recycle relaunches on the same port with a NEW pid, the matching grant's own recorded pid is updated to that new pid, so a release issued after the recycle still tears down the recycled (not the pre-recycle) process.
  </behavior>
  <action>
    This closes the verification's item 3, explicitly "its own change, not a side effect of" task 1's
    fix: even after task 1 closes CR-01's specific race, `handleRelease()`'s `state.instances.get(grant.port)`
    lookup is unsafe against ANY event that swaps out a port's occupant without also clearing the
    grant -- the clearest independent example is an ordinary (non-deliberate) crash of a GRANTED
    instance that hits the give-up threshold: `handleExit()`'s crash-accounting branch deletes the
    record from `state.instances` regardless of `record.state`, freeing the port for
    `nextFreePort()` to hand to a brand-new, unrelated cold launch, while the original grant is
    untouched in `state.grants` -- a later release of that grant would otherwise kill the new,
    unrelated instance exactly like CR-01's own blast radius.

    Add `pid: number | null` to the `GrantRecord` interface (`broker-state.mts`), as a REQUIRED field
    alongside `id`/`port`/`grantedAt`, documented with a comment stating its purpose: proving, at
    release time, that the port's CURRENT occupant is the SAME process this grant was actually issued
    for, not merely "whatever now holds this port number." Update the ONLY production call site that
    constructs a `GrantRecord` -- `handleAcquire()`'s single `state.grants.set(...)` call, the exact
    call site `broker-launch.test.ts`'s structural anti-regression gate counts -- to also set
    `pid: record.pid`. This adds a field to that ONE object literal; it does not add a second call
    site, so the structural gate's count of exactly 1 is unaffected. Update the two existing hand-built
    `GrantRecord` literals that will otherwise fail to typecheck: `vice-broker-acquire.test.ts`'s
    kill-never-recycle test (`state.grants.set("req-4", ...)`) -- set its `pid` to the SAME pid its own
    seeded instance carries, so that test keeps exercising the MATCHING-identity path its own
    assertions already describe; `broker-state.test.ts`'s `_snapshotState` test -- set its `pid` to any
    value consistent with that test's own seeded instance (this test does not exercise release
    semantics, so the exact value only needs to satisfy the type).

    Rewrite `handleRelease()` (`vice-broker.mts`) to resolve the instance at `grant.port` exactly as
    today, but compare `instance.pid` against `grant.pid` before treating it as this grant's own
    instance. On a match: keep today's exact behaviour unchanged (`markDeliberateDeath`, delete the
    instance, fire-and-forget `verifiedKill`). On a mismatch -- including when there is no instance at
    all at that port: still delete the grant (a release always retires its OWN request's bookkeeping),
    but do NOT delete the instance record and do NOT signal it in any way; log a line naming the
    request id, the port, the grant's own recorded pid, and the current occupant's pid (or an explicit
    "none" when the port is empty), stating plainly that the release found a different instance at
    that port and left it untouched -- worded distinctly from both the shutdown-complete line
    (`broker-kill.mts`) and the grant-time-probe-failure line this same file already emits, per D-07's
    standing constraint that a lifecycle decision must be reconstructable from the log after an
    incident.

    In `broker-launch.mts`'s `handleExit()`, inside the EXISTING recycle branch
    (`record.deliberateKill && record.respawnAfterKill`), immediately after the line that restores a
    pre-kill `"granted"` state onto the freshly respawned record, walk `deps.state.grants.values()`
    (`SuperviseChildDeps.state` is already the full `BrokerState`, grants included -- no interface
    change needed) to find the grant whose own `port` equals this port, and update ONLY that grant's
    `pid` to the respawned record's own `pid`. Without this, a recycle -- the ONE legitimate case where
    the SAME grant continues to own a DIFFERENT pid on the SAME port -- would make `handleRelease()`'s
    new identity check misfire and refuse to tear down the very instance the grant now legitimately
    owns.

    Add tests in `vice-broker-acquire.test.ts`: one seeding an unrelated instance at a port plus a
    grant naming that port but a DIFFERENT pid than the seeded instance's own, calling `handleRelease()`
    on that grant's id, and asserting the grant is gone while the unrelated instance is STILL present
    with `deliberateKill` still unset (proving no kill was attempted); and the updated kill-never-recycle
    test (pid now matching) proving the identity-matched path still tears down exactly as before.

    Add a test in `broker-launch.test.ts`, alongside the existing recycle tests (same
    `makeSuperviseDeps()`/`fakeChild()`/`waitFor()` idiom): seed `deps.state.grants` with a grant
    naming the PRE-recycle pid before triggering the recycle exit event, wait for the respawn to
    complete (the SAME `waitFor(epoch advance)` idiom the existing recycle tests already use), then
    assert the grant's own recorded pid now equals the RESPAWNED record's pid and no longer equals the
    pre-recycle pid.

    Rebuild `resources/broker-state.mjs`, `resources/broker-launch.mjs` and `resources/vice-broker.mjs`
    after the source edits.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && node --test --test-name-pattern="handleRelease|kill-never-recycle|recycle" .claude/mcp/vice/vice-broker-acquire.test.ts .claude/mcp/vice/broker-launch.test.ts && npm --prefix .claude/mcp/vice run typecheck</automated>
  </verify>
  <done>A release on a stale/orphaned grant never touches an unrelated occupant; a release after a legitimate recycle still tears down the correct (post-recycle) process; typecheck passes with the new required `GrantRecord.pid` field threaded through every construction site.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-01/WR-03/WR-04 fixed and tested, WR-02 deferred with a written reason, Defect-3 disposition corrected</name>
  <files>.claude/mcp/vice/vice-broker.mts, .claude/mcp/vice/vice-broker-acquire.test.ts, .claude/mcp/vice/broker-launch.test.ts, .claude/mcp/vice/resources/vice-broker.mjs, .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md, .planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md, .planning/RE-FINDINGS.md</files>
  <behavior>
    - At the instance ceiling with one probe-live ready candidate present: handleAcquire() grants the candidate rather than refusing (WR-01 fixed).
    - At the instance ceiling with zero ready candidates: handleAcquire() still refuses with at_capacity, unchanged (WR-01 regression guard).
    - A cold launch whose spawned child never receives a pid: handleAcquire() returns { ok: false, reason: "internal" } AND the broken record is gone from state.instances immediately, not merely eventually via crash supervision (WR-03 fixed).
  </behavior>
  <action>
    WR-01 fix: in `handleAcquire()` (`vice-broker.mts`), move the `atCapacity(state)` refusal so it
    gates ONLY the cold-launch arm, not the warm-instance arm -- call `selectWarmInstance()` first;
    only when it returns `null` (no probe-live candidate available) check `atCapacity()` and return
    `{ ok: false, reason: "at_capacity" }` before attempting the cold launch. A ready, probe-live warm
    instance must be grantable even when the ceiling is already reached, since granting it creates no
    NEW instance and does not raise `countTotal()`. This is the review's own suggested reordering.
    Update this function's own header comment, which currently documents the OLD ordering
    ("`atCapacity()` runs before EITHER arm") as the design intent -- correct it to describe the NEW
    ordering and why a full-but-not-really-full host must still be able to serve a warm candidate.

    WR-03 fix: in the cold-launch arm, immediately after the existing check that returns
    `{ ok: false, reason: "internal" }` when the freshly spawned record's pid is null, delete that
    just-created broken record from `state.instances` before returning. The record's pid is null,
    meaning the spawn never forked a real process, so there is nothing to signal -- the fix is the
    delete alone. This closes the gap where a configuration failure (e.g. a bad `VICE_BIN` path)
    otherwise silently occupies a port slot and counts toward `countTotal()`/`atCapacity()` until
    crash supervision's own respawn/give-up machinery eventually notices and frees it.

    WR-04 fix: move the module-level warm-launch log-path variable (`vice-broker.mts`, declared after
    `maintainWarmFloorForRealBroker()`'s own closing brace today) INTO that function's own body,
    declared fresh once per call -- exactly mirroring how `handleAcquire()`'s own equivalent
    cold-launch log-path variable is already scoped locally to that function rather than to the
    module. Both the write site (the spawn-wrapping closure) and the read site (the `onLaunched`
    callback) already live inside this SAME function body, so this is a pure relocation with no
    behavioural change -- it removes the cross-call-sharing risk the review names, which is currently
    safe only because of an invariant (at most one launch per call, never invoked concurrently with
    itself) enforced elsewhere and never checked at the point this variable is declared.

    WR-02 disposition: DEFER. Do not change the kill-await ordering in `selectWarmInstance()`'s drop
    path in this plan. Write a new pending todo at
    `.planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md`
    stating: the finding (a grant-time probe failure awaits the identity-verified kill per candidate,
    up to the configured kill-wait ceiling, before the walk continues to the next candidate); why it
    is bounded and not urgent today (the warm floor defaults to 1 per D-06, so there is normally at
    most one candidate to walk through in the first place, and the worst case sits comfortably inside
    the acquire deadline P-08 raised); and the two fix options the review itself offered -- making the
    kill fire-and-forget (matching `handleRelease()`'s own posture), which would need the
    grant-time-probe-failure log line's own ordering test re-examined since that line currently reads
    the kill's resolved outcome; or capping how many failed candidates a single acquire will wait
    through, which preserves today's log ordering exactly and only matters on a host deliberately
    configured to a warm floor above 1 -- so whoever next configures a warm floor above 1 (the one
    circumstance where this bound actually bites) has both options and the reasoning already written
    down, not just the finding.

    Log the CR-01 race and its two-part fix (task 1's map-membership recheck, task 2's grant-identity
    check) as a hazard finding in `.planning/RE-FINDINGS.md` -- append only, dated today, never editing
    an existing entry. State what was found (a state-field-only recheck missed a concurrent drop path,
    letting a grant outlive its process and letting a stale release cross into an unrelated instance)
    and how it was verified (independent code reading at HEAD by this phase's own verifier, closed here
    by a deterministic two-overlapping-acquires regression test, not by trusting the review's narrative
    alone). Grade it: Evidence -- read the landed code and reproduced the race with a deterministic
    regression test; Confidence -- HIGH.

    Correct `01.6.2-VALIDATION.md`'s Defect-3 disposition. Two rows need editing under
    "## Phase 01.6.2.1 Disposition — Criterion M": the seven-criteria table's row 3 ("A grant is
    proven live before it is honoured and cannot outlive its process"), and the five-row defect map's
    row "3 — grants outlive their process". Both currently name their Disposition column with an
    unqualified two-word verdict citing only the non-concurrent grant-time-re-probe tests as evidence.
    Edit both rows' Evidence columns to ALSO name this plan's own SUMMARY and the two fixes it landed
    (the map-membership recheck plus its concurrent regression test; the grant-pid identity check plus
    its stale-grant regression test), and add one explanatory note directly beneath the two tables (a
    single shared note is fine, since both rows describe the same defect) stating plainly that the
    original wording was accurate only for the non-concurrent case, that `01.6.2.1-VERIFICATION.md`'s
    CR-01 finding is what caught the concurrent-acquire gap between seal and this plan, and that this
    gap-closure plan is what closes it. Do not simply reword the verdict without this explanation --
    the point of the correction is to make the disposition's own history readable, not to erase that a
    real gap existed.

    Add tests in `vice-broker-acquire.test.ts`: one seeding exactly the DEFAULT instance ceiling's
    worth of records (verify the current default by reading `broker-state.mts`'s own ceiling resolver
    and note that value in the test's own comment so a future default change makes the test visibly
    wrong rather than silently so), one of them ready and probe-live, the rest in a non-selectable
    state, asserting `handleAcquire()` still grants the ready candidate rather than refusing; a second
    test with the same seeding but no ready candidate, asserting the refusal is unchanged; and a third
    injecting a `buildColdSpawnFactory` stub whose spawned child carries a null pid, asserting the
    outcome is `{ ok: false, reason: "internal" }` and that `state.instances` has NO entry at all
    afterward for the port that was attempted.

    Add a structural test in `broker-launch.test.ts`, alongside the existing structural test over
    `vice-broker.mts`'s source text (same comment-stripping helper): assert the warm-launch log-path
    variable's declaration no longer appears at module scope (column zero) in `vice-broker.mts`, while
    the identifier itself still appears, indented, inside `maintainWarmFloorForRealBroker()`'s own
    body -- proving it moved rather than merely vanished.

    Rebuild `resources/vice-broker.mjs` after the source edits.
  </action>
  <verify>
    <automated>cd /workspaces/bruce_lee && node --test --test-name-pattern="at_capacity|ceiling|internal|structural" .claude/mcp/vice/vice-broker-acquire.test.ts .claude/mcp/vice/broker-launch.test.ts && test -f .planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md && grep -c "CR-01" .planning/RE-FINDINGS.md | grep -qv '^0$' && grep -c "01.6.2.1-07" .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md | grep -qv '^0$'</automated>
  </verify>
  <done>WR-01/WR-03/WR-04 fixed and tested (the ceiling no longer blocks a servable warm candidate; a pid-null cold launch leaves no broken record; the log-path variable is no longer module-scoped); WR-02 recorded as a deliberately deferred, reasoned todo; `01.6.2-VALIDATION.md`'s Defect-3 disposition accurately reflects that the concurrent-acquire gap existed and is closed by this plan, not merely relabelled; `.planning/RE-FINDINGS.md` carries the dated, graded CR-01 hazard entry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Two control-plane connections' `handleAcquire()`/`handleRelease()` calls | Both mutate the SAME shared in-process `BrokerState` (`state.instances`, `state.grants`) with no locking; a race at this boundary is exactly CR-01's shape, and the same boundary is where an orphaned grant's later release can misdirect a kill signal. |
| `handleRelease()`/`handleExit()`'s recycle branch → the spawned `x64sc` child | A kill signal crossing this boundary must be proven to target the SAME process a grant was actually issued for, not merely "whatever now occupies this port number." |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.2.1-03 | Information disclosure | `selectWarmInstance()`'s post-probe recheck (AMENDED here) | critical | mitigate | Originally mitigated only the already-granted case ("no await between selection and the grant-recording step"); `01.6.2.1-VERIFICATION.md`'s CR-01 finding proved the already-DROPPED case was unmitigated. Task 1's map-membership recheck (`state.instances.get(record.port) === record`, not merely `record.state`) closes the case the original mitigation missed, proven by a deterministic two-overlapping-acquires regression test. |
| T-01.6.2.1-28 | Tampering / DoS (cross-session process termination via port reuse) | `handleRelease()`'s port-based kill-target resolution | critical | mitigate | Task 2's `GrantRecord.pid` identity check: a release only signals the port's current occupant when that occupant's own pid matches the pid recorded at grant time; a mismatch retires the grant's bookkeeping without touching the current (unrelated) occupant. `handleExit()`'s recycle branch keeps a legitimate recycle's grant-pid binding in sync so the check never misfires against the project's own kill-never-recycle design. |
| T-01.6.2.1-29 | Denial of service | `atCapacity()`'s gate ordering in `handleAcquire()` (WR-01) | medium | mitigate | Task 3 reorders the ceiling check to gate only the cold-launch arm, so a ready, probe-live warm candidate is still grantable at the ceiling — granting it creates no new instance and does not raise `countTotal()`. |
| T-01.6.2.1-30 | Denial of service | A cold-launch record with `pid === null` occupying a port slot (WR-03) | low | mitigate | Task 3 deletes the broken record from `state.instances` at the point the failure is detected, rather than relying on crash supervision's delayed respawn/give-up cleanup. |
| T-01.6.2.1-31 | Denial of service | Serial `await`ed kill on a failed grant-time probe walk (WR-02) | low | accept (deferred) | Bounded well inside the raised acquire deadline (P-08) and only matters at a warm floor above 1 (not this project's current default, D-06). Deferred with both fix options recorded in `.planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md` rather than fixed now, to avoid destabilising the existing grant-time-probe-failure log-ordering test for a bound that does not currently bite. |
| T-01.6.2.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package installs in this plan.** No install task exists, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint is owed. Row retained so the register is complete rather than silently absent. |

No `critical` threat is left undispositioned as anything other than `mitigate`; T-01.6.2.1-31 is the
only `accept`, and it is a pre-existing, bounded, low-severity Warning the review itself flagged as
lower priority than the two Critical findings this plan exists to close.
</threat_model>

<verification>
Run the full suite and confirm the new total against the pre-plan baseline (417 tests / 412 pass /
0 fail / 5 todo, verified twice on `main` before this plan) -- report exactly what the suite prints,
do not fabricate the new count:

- `cd /workspaces/bruce_lee && node --test --test-concurrency=1 '.claude/mcp/vice/'*.test.*`
- `npm --prefix .claude/mcp/vice run typecheck`
- `cd /workspaces/bruce_lee/.claude/mcp/vice && node build.ts && git status --porcelain resources/`
  -- a correct diff here shows ONLY `resources/vice-broker.mjs`, `resources/broker-state.mjs` and
  `resources/broker-launch.mjs` changed, at exactly the lines mirroring this plan's three `.mts`
  source edits (the map-membership recheck, the `handleRelease()` identity check, the `handleExit()`
  recycle-sync, the `GrantRecord.pid` field, the `atCapacity()` reorder, the cold-launch pid-null
  delete, and the relocated log-path variable) -- no unrelated generated file differs, and every
  generated file's own banner (naming its source, warning against hand-edits) stays byte-identical to
  every other generated file's banner. Commit this diff alongside the source changes in the same
  commit as the task that produced it, per this project's own generated-tier discipline -- do not
  leave `resources/` un-rebuilt or the diff uncommitted.
- If `broker-kill.test.ts` shows a banner-ordering failure under the full-suite run, re-run it in
  isolation (`node --test .claude/mcp/vice/broker-kill.test.ts`) before treating anything as a real
  regression -- this is a known pre-existing flake under full-suite load, not something this plan
  introduces or is responsible for chasing.
- Three commits, one per task, matching this phase's own established one-task-one-commit discipline.
- No test in this plan opens a real connection to a host emulator or a real host broker; every probe,
  kill, spawn and clock is injected through the existing dependency seams (`HandleAcquireDeps`,
  `SuperviseChildDeps`), and no `x64sc` runs.
</verification>

<success_criteria>
- [ ] `selectWarmInstance()`'s recheck verifies map membership by identity, not merely `record.state`, and the two-overlapping-acquires regression test is RED against the pre-fix code and GREEN against the fix (both states observed and recorded).
- [ ] `handleRelease()` never signals or deletes an instance whose pid does not match its own grant's recorded pid; `handleExit()`'s recycle branch keeps a legitimate recycle's grant-pid binding in sync.
- [ ] WR-01 and WR-03 are fixed and each has a passing regression test; WR-04 has a passing structural test; WR-02 is recorded as a deliberately deferred, reasoned todo naming both fix options.
- [ ] `01.6.2-VALIDATION.md`'s Defect-3 rows (both tables) name this plan's own fixes and explain that the original "DISCHARGED here"/"FIXED here" wording covered only the non-concurrent case.
- [ ] `.planning/RE-FINDINGS.md` carries a dated, separately-graded (`Evidence:`/`Confidence:`) entry for the CR-01 race.
- [ ] Full suite green against the pre-plan baseline (417/412/0/5, re-counted and reported, not assumed); typecheck exits 0; `resources/` rebuilt and its diff committed alongside the source changes it mirrors.
- [ ] No test opens a real connection to a host emulator or a real host broker.
</success_criteria>

<output>
Create `.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-07-SUMMARY.md` when done.
</output>
