---
phase: 01.6.2.1-broker-lifecycle-policy
plan: 06
subsystem: infra
tags: [vice-broker, documentation, validation, criterion-disposition, carry-forward]

# Dependency graph
requires:
  - phase: 01.6.2.1-broker-lifecycle-policy
    provides: "plans 01-05's landed lifecycle-policy changes and their SUMMARY frontmatters -- the evidence this plan disposition against and harvest from"
  - phase: 01.6.2-the-one-process-host-broker
    provides: "01.6.2-VALIDATION.md (extended in place, not replaced) and 01.6.2-VERIFICATION.md's observable-truths table -- the evidence this plan cites for criteria discharged in that phase"
provides:
  - "All seven of Phase 01.5's criteria dispositioned one by one with a decision ID and named evidence, closing criterion M's gate"
  - "The five-row defect mapping (P-01 supersedes D-02's four-defect enumeration), and the five-defect todo closed in .planning/todos/completed/ on that gate"
  - "The P-05 amendment to locked decision D-05 recorded in writing as an artifact section"
  - "Criterion E's equivalence framing replaced (not annotated) per D-19, with the genuinely-unchanged set named separately"
  - "D-20's per-change proof table for every change this phase landed"
  - "Criterion E's carry-forward list: 35 rows, CF-01.4-NN IDs, Phase 01.4 named as owner, covering both 01.6.2 and 01.6.2.1"
  - "This phase's measured suite arithmetic (390 -> 402, reconciled with zero discrepancy)"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pattern: when a mechanical harvest command's raw count diverges from the number of rows actually written, both figures are stated side by side with the cause of each contributing delta named individually (self-referential prose in the harvesting document itself; a SUMMARY's coverage rationale restating rather than extending its own plan's PLAN-frontmatter truth) -- never smoothed into agreement."
    - "Pattern: a project's own automated <verify> command can be stricter than its prose acceptance_criteria (the literal grep for a retired phrase forbids ANY survival, where the acceptance criteria's prose allows a labelled-as-retired survival) -- when the two disagree, the artifact honors the automated command (zero occurrences) and moves the auditable verbatim quote to the SUMMARY, which is where the task's own action text asked for it in the first place."

key-files:
  created: []
  modified:
    - .planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md
    - .planning/todos/pending/2026-08-01-vice-broker-spare-warming-and-stale-grant-defects.md (moved to .planning/todos/completed/)

key-decisions:
  - "Split the append across two separate git commits (task 1: Sections 1-3 / criterion M; task 2: Part A in-place edits + Parts B/C/D / criterion E) even though a single Edit tool call had briefly combined them in the working tree -- reverted with `git checkout --` and redid the two appends as genuinely separate operations so each task's commit contains only that task's own content, per the plan's own per-task atomic-commit requirement."
  - "Removed the verbatim quote of the retired 'language and structural equivalence only' phrase from 01.6.2-VALIDATION.md entirely, rather than keeping it inside the new section labelled as retired. The task's own automated <verify> command greps for that exact phrase with no exception; the task's own acceptance_criteria prose allows the exception but the mechanical gate does not. Resolved by moving the verbatim before/after quote to this SUMMARY (which is what the plan's own action text asked for: 'quote the before-and-after of each replaced sentence in the SUMMARY') and describing the retired claim in the artifact without reproducing its exact wording."
  - "The carry-forward list's cross-reference language ('batches with CF-01.4-01') was reworded to positional references ('batches with the row above/below') after the first draft's own literal ID repetitions tripped the no-duplicate-ID acceptance criterion via grep. An ID is only allowed to appear once, in its own row -- referencing a sibling row by ID from within another row's prose is exactly the kind of literal repetition the duplicate-ID check (correctly) cannot distinguish from an actual duplicate row."

patterns-established:
  - "Pattern: a carry-forward/discharge list's ID scheme is only as stable as the fact that no row's ID appears verbatim inside another row's own prose -- cross-references between rows must use positional language (the row above/below) rather than repeating the ID literal, or a mechanical uniqueness check cannot tell a legitimate cross-reference from an actual duplicate."

requirements-completed: ["E", "M", "D-02", "D-19", "D-20", "D-21", "D-22", "P-05", "P-09", "P-10", "Defect-1", "Defect-2", "Defect-3", "Defect-4", "Defect-5"]

coverage:
  - id: D1
    description: "All seven of Phase 01.5's criteria carry an explicit disposition (DISCHARGED/PRESERVED/DISSOLVED/ADOPTED), each naming a decision ID and named evidence (a landed test or a 01.6.2-VERIFICATION.md truth number) -- no row's evidence is a cross-reference alone"
    requirement: "M"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md, 'Phase 01.6.2.1 Disposition -- Criterion M', Section 1 -- seven-row table, verified by direct read against 01.6.2-CONTEXT.md's D-numbers and 01.6.2-VERIFICATION.md's truth table"
        status: pass
    human_judgment: false
  - id: D2
    description: "Criterion 4 (lazy replenishment R1/R2) recorded DISSOLVED BY D-06 with full prose reasoning, explicitly stating P-01 is what makes the dissolution real rather than nominal; criterion 6 (GRANT_POLL_TIMEOUT_MS) recorded ADOPTED -- done on the successor, explicitly overruling ROADMAP.md's proposed 'dissolved' wording"
    requirement: "M"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md, Section 1, the two prose paragraphs beneath the seven-row table"
        status: pass
    human_judgment: false
  - id: D3
    description: "The defect mapping has exactly five rows (not four), with a note above the table stating P-01 supersedes D-02's four-defect enumeration and how Defect 5 was found; the five-defect todo closed only after this mapping was confirmed complete"
    requirement: "M"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md Section 2 (five-row table) + .planning/todos/completed/2026-08-01-vice-broker-spare-warming-and-stale-grant-defects.md's closing note (gate check recorded, all five rows confirmed dispositioned before the git mv)"
        status: pass
    human_judgment: false
  - id: D4
    description: "P-05 is consolidated in the artifact as a written amendment to locked decision D-05: original wording, the amendment, the reason (quoted from the landed code's own comment), and that D-05's intent is honoured rather than reversed"
    requirement: "P-05"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md Section 3 -- the P-05 amendment record"
        status: pass
    human_judgment: false
  - id: D5
    description: "Criterion E's equivalence framing is REPLACED (not annotated) in all three original locations (Sampling Rate bullet, criterion-E row, Manual-Only Verifications hazard note); zero unqualified survival of the retired phrase anywhere in the file; the genuinely-unchanged set (emulator data plane, epoch.json contract, tool surface) named separately as the only three equivalence claims"
    requirement: "E"
    verification:
      - kind: other
        ref: "grep -c 'language and structural equivalence' 01.6.2-VALIDATION.md returns 0; 01.6.2-VALIDATION.md 'Phase 01.6.2.1 Reframing -- Criterion E', Part A"
        status: pass
    human_judgment: false
  - id: D6
    description: "D-20's per-change proof table: one row per change this phase landed, every row's proof column names a file and test name, rows whose deliverable was a test rather than code (D-07 non-preemption, D-08 FIFO/structural gate) marked [test-only]"
    requirement: "D-20"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md 'Phase 01.6.2.1 Reframing -- Criterion E', Part B -- 16-row proof table"
        status: pass
    human_judgment: false
  - id: D7
    description: "Criterion E's carry-forward list exists with the harvest command reproduced verbatim, a stated ordering rule, 35 rows with stable non-duplicate CF-01.4-NN IDs, a concrete discharge route per row, both phases covered, and Phase 01.4 named as owner in the section's own opening sentence"
    requirement: "D-19"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md 'Phase 01.6.2.1 Reframing -- Criterion E', Part C; grep -o 'CF-01\\.4-[0-9][0-9]' | sort | uniq -d returns no lines; grep -c '^| CF-01\\.4-' returns 35"
        status: pass
    human_judgment: false
  - id: D8
    description: "The raw mechanical harvest (46) versus the 35 rows actually written is reconciled and stated rather than smoothed: +7 self-referential prose hits in this plan's own PLAN file, +5 SUMMARY hits that restate (not extend) an already-counted PLAN-frontmatter truth -- reconciling to exactly the plan's own stated expected arithmetic of 34 backstop truths + 1 control-port row = 35"
    requirement: "D-22"
    verification:
      - kind: other
        ref: "01.6.2-VALIDATION.md Part C's reconciliation paragraph, cross-checked by direct read of every one of the 46 raw grep hits (not sampled)"
        status: pass
    human_judgment: false
  - id: D9
    description: "This phase's measured suite arithmetic: 390 baseline (01.6.2 seal) through each plan's own declared delta to a MEASURED (not computed) final total of 402/397/0/5, with zero discrepancy against the summed deltas"
    requirement: "D-21"
    verification:
      - kind: other
        ref: "live run: node --test --test-concurrency=1 '.claude/mcp/vice/'*.test.* -> 402 tests / 397 pass / 0 fail / 5 todo, this session, recorded in 01.6.2-VALIDATION.md Part D"
        status: pass
    human_judgment: false
  - id: D10
    description: "On a real host, Phase 01.4 can actually work through the 34 host-side carry-forward rows (CF-01.4-01..34) one at a time and discharge each against the concrete observable it names"
    verification: []
    human_judgment: true
    rationale: "Backstop truth per this plan's own must_haves (CF-01.4-35, this plan's own authored backstop truth). No live host session exists from inside this container; carried forward exactly as every prior plan in this milestone has carried forward its own no-live-host-session claims, with no live-session verification until Phase 01.4. This is the plan's own most self-referential claim -- that its own deliverable is usable -- and only Phase 01.4's actual use of the list can confirm it."

# Metrics
duration: 110min
completed: 2026-08-04
status: complete
---

# Phase 01.6.2.1 Plan 06: Criteria M and E — Absorption Disposition and the Carry-Forward List Summary

**All seven of Phase 01.5's criteria dispositioned with decision IDs and named evidence, the five-defect todo closed on that gate, criterion E's equivalence framing replaced (not annotated) per D-19 with a 16-row per-change proof table, and a 35-row carry-forward list with stable `CF-01.4-NN` IDs handing Phase 01.4 a concrete, re-runnable discharge route for every unverified claim across both 01.6.2 and 01.6.2.1.**

## Performance

- **Duration:** ~110 min
- **Tasks:** 3
- **Files modified:** 2 (both `.planning/` — `01.6.2-VALIDATION.md` extended in place, one todo moved pending → completed)

## Accomplishments

- **Criterion M (Task 1).** Appended three sections to `01.6.2-VALIDATION.md`, dated and authored to
  Phase 01.6.2.1: a seven-row table dispositioning every one of Phase 01.5's historical criteria
  (DISCHARGED/PRESERVED/DISSOLVED/ADOPTED, each naming a decision ID and a landed test or
  `01.6.2-VERIFICATION.md` truth number); the two previously-unsettled items (criterion 4 dissolved
  by D-06 with P-01 stated explicitly as what makes the dissolution real rather than nominal;
  criterion 6 adopted as done-on-the-successor, overruling ROADMAP.md's own proposed "dissolved"
  wording); the five-row defect mapping (P-01 supersedes D-02's four-defect enumeration, with a
  note naming how and when Defect 5 was found); and the P-05 amendment record consolidating D-05's
  original wording, the amendment, the reason, and that D-05's intent is honoured rather than
  reversed.
- **The five-defect todo closed (Task 3).** Re-read the five-row mapping immediately before moving
  the file, confirmed every row carries a disposition and named evidence (the gate D-02 requires),
  then `git mv`'d the todo to `completed/` and appended a closing note: one disposition line per
  defect, a dedicated sentence on why Defect 1 was preserved rather than re-fixed (re-fixing an
  already-correct serialised-warming mechanism risks re-creating the exact concurrent-spawn window
  the 2026-08-01 outage came from), the detection technique for the Defect 5 class (an e2e test
  against the real assembled artifact plus a structural entry-point assertion — a unit test alone
  proved insufficient the first time), and that absorbing Phase 01.5 seals here rather than at
  01.6.2's seal.
- **Criterion E's framing replaced, not annotated (Task 2, Part A).** Edited three locations in
  place — the Sampling Rate "Phase gate" bullet, the criterion-E row in the Phase-Criteria →
  Validation Map, and the Manual-Only Verifications "standing hazard" note. The retired claim (a
  green suite alone proves the new implementation matches the old bash daemon at the language/
  structure level) does not survive anywhere in the file (`grep -c 'language and structural
  equivalence' 01.6.2-VALIDATION.md` → `0`); its verbatim before-text is quoted below instead, per
  the task's own instruction to keep that quoting in the SUMMARY. The genuinely-unchanged set — the
  emulator data plane, the `epoch.json` contract, the agent-facing tool surface — is named as the
  **only three** equivalence claims left in the artifact.
- **D-20's per-change proof table (Task 2, Part B).** 16 rows covering every change this phase
  landed — Defect 5's fix, the grant-time re-probe, the probe collapse, the warm-floor default, the
  idle-state proof, non-preemptive launch priority, the FIFO proof and its structural gate, the
  acquire-deadline raise, the never-started re-anchor, and the `spares`→`warm floor` rename with its
  banner and structural gate — each naming the test that proves it, and marking test-only
  deliverables (D-07's non-preemption confirmation, D-08's FIFO/structural proof — both against
  already-correct code) as such.
- **The carry-forward list (Task 2, Part C) — the phase's most load-bearing deliverable.** Ran the
  exact four-glob `grep -h "verification: backstop"` command specified by the plan: **46** raw
  hits. Read every one of the 46 by hand (not sampled) rather than trusting the count, and found two
  genuinely distinct causes of inflation over the plan's own expected 35: **+7** false positives —
  this very plan's own PLAN file (`01.6.2.1-06-PLAN.md`) matches the `01.6.2.1-*-PLAN.md` glob, and
  its own objective section quotes the literal phrase seven times while *describing* the harvest
  mechanism (only one line in that file is an actual `must_haves.truths` entry); **+5** hits that
  restate rather than extend — every one of `01.6.2`'s five SUMMARY-file hits (plans 06/07/08/09/10)
  sits inside `coverage[].rationale` prose quoting the literal phrase while describing a claim
  **already counted** in that same plan's own PLAN-frontmatter `must_haves.truths` (confirmed by
  side-by-side read of each pair). Reconciliation: 46 − 7 − 5 = 34 distinct backstop truths, plus 1
  for the medium-confidence control-port item (named in `01.6.2-VERIFICATION.md`'s own
  carried-forward paragraph, not produced by the grep at all) = **35**, matching the plan's own
  stated expected arithmetic exactly and confirming the reconciliation independently rather than
  assuming it. Wrote all 35 rows with stable `CF-01.4-NN` IDs (ordered: source phase, then plan
  number, then order of appearance in frontmatter, non-frontmatter rows last per source phase),
  every row naming a concrete real-host observable in its discharge column, and named **Phase 01.4**
  as the owner in the section's own opening sentence.
- **This phase's measured suite arithmetic (Task 2, Part D).** No code changes in this plan, so the
  live run is a measurement, not a gate on this plan's own changes. Ran
  `node --test --test-concurrency=1 '.claude/mcp/vice/'*.test.*` against the final tree: **402
  tests / 397 pass / 0 fail / 5 todo.** Reconciled against each of plans 01–05's own declared net
  test-count delta (390 baseline + 7 − 3 + 6 + 0 + 2 = 402) — **zero discrepancy**, exact match.

## Before/after text of the two sentences Part A replaced (verbatim, per the task's own instruction to quote this in the SUMMARY)

**Location 1 — Sampling Rate, "Phase gate (beyond a green suite)" bullet.**

Before:
> A green suite here proves **language and structural equivalence only**.

After:
> **Correctness is claimed against specification**, not against the retired bash daemon, with the
> genuinely-unchanged set held to real equivalence and everything else itemised as a carry-forward
> list Phase 01.4 owns.

**Location 2 — Manual-Only Verifications, "The standing hazard, restated" note.**

Before:
> **This phase's green suite proves language and structural equivalence only.** Criterion E
> requires stating this explicitly rather than letting a green suite imply otherwise.

After:
> **Correctness for this phase and Phase 01.6.2.1 is claimed against specification, not against the
> retired bash daemon** — the genuinely-unchanged set (the emulator data plane, the `epoch.json`
> contract, the agent-facing tool surface) is named separately and held to real equivalence, and
> everything else neither phase could verify live is itemised in the carry-forward list appended
> below, which **Phase 01.4 owns and must discharge**.

The criterion-E row in the Phase-Criteria → Validation Map table was also edited (not merely these
two prose locations) to carry the same reframing and a pointer to the appended sections.

## Task Commits

Each task was committed atomically, all three confined to `.planning/` (verified via
`git diff --stat <hash>~1 <hash>`, each showing only the touched `.planning/` file):

1. **Task 1: criterion M — disposition Phase 01.5's seven criteria and the five-row defect mapping** - `4987e77` (docs)
2. **Task 2: criterion E — replace the equivalence framing and add the carry-forward list** - `845e753` (docs)
3. **Task 3: close the five-defect todo, gated on the complete disposition mapping** - `bc4011f` (docs)

_No plan-metadata commit in worktree mode — the orchestrator makes the metadata commit centrally
after merge; this plan's own final commit below covers only this SUMMARY._

## Files Created/Modified

- `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md` — extended in place
  (D-21, no new artifact) with three appended sections (criterion M's disposition tables and P-05
  amendment record), three in-place edits (the criterion-E reframing per D-19), and two further
  appended sections (D-20's per-change proof table, the criterion-E carry-forward list and this
  phase's measured suite arithmetic).
- `.planning/todos/completed/2026-08-01-vice-broker-spare-warming-and-stale-grant-defects.md` —
  moved from `pending/` via `git mv` (one rename in history, confirmed via `git log --follow
  --name-status`), with a closing note appended below the five pre-existing defect sections (none
  of which were edited or renumbered).

## Decisions Made

- **Split the append into two genuinely separate commits after a first draft briefly combined
  them.** A single `Edit` call had appended both Task 1's criterion-M sections and Task 2's Part A
  discussion in one operation; recognized before committing that this would blend two tasks into
  one commit, reverted the file with `git checkout --` (a specific-file revert of my own uncommitted
  edit, not a blanket reset), and redid the two appends as separate operations with a commit between
  them.
- **Moved the retired phrase's verbatim quote from the artifact to this SUMMARY.** The task's own
  automated `<verify>` command (`! grep -q 'proves \*\*language and structural equivalence
  only\*\*'`) forbids the exact phrase anywhere in the file with no exception, while the prose
  acceptance criteria alone would have permitted a labelled-as-retired survival. Resolved in favor
  of the stricter mechanical gate — the artifact now describes the retired claim without reproducing
  its exact wording, and the verbatim before/after text lives here instead, which is what the task's
  own action text asked for in the first place ("quote the before-and-after of each replaced
  sentence in the SUMMARY").
- **Reworded carry-forward-row cross-references from literal ID repetition to positional language.**
  A first draft's "(batches with CF-01.4-01)" style cross-references tripped the no-duplicate-ID
  acceptance check, because the check cannot distinguish a legitimate cross-reference to a sibling
  row from an actual duplicate row — both look like the same literal string appearing twice. Fixed
  by rewording every cross-reference to "(batches with the row above/below)" instead.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 — self-caught structural bug] A single combined Edit call briefly blended Task 1 and Task 2 content into one uncommitted draft**
- **Found during:** Task 1, immediately before staging, while re-reading the plan's own per-task atomic-commit requirement against what had actually been written to the file.
- **Issue:** The first draft appended both Task 1's three sections (criterion M) and Task 2's Part A discussion (criterion E's reframing) in a single `Edit` tool call, which would have produced one commit covering two tasks' content if committed as-is.
- **Fix:** Reverted the file to HEAD with `git checkout -- <file>` and redid the two appends as two separate operations, staging and committing Task 1's content alone before starting Task 2's edits.
- **Files modified:** `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md` (no net change to final content beyond the correction itself)
- **Verification:** `git diff --stat 4987e77~1 4987e77` shows only Task 1's own additions (119 insertions, 0 deletions); `git diff --stat 845e753~1 845e753` shows only Task 2's own changes.
- **Committed in:** `4987e77` (corrected Task 1 commit)

**2. [Rule 1 — verify-command mismatch] The retired-phrase verbatim quote in the artifact tripped the task's own automated `<verify>` command**
- **Found during:** Task 2, running the plan's exact verify command (`! grep -q 'proves \*\*language and structural equivalence only\*\*' "$V"`) after drafting Part A, rather than assuming it would pass because the phrase was clearly labelled as retired.
- **Issue:** The first draft quoted the retired phrase verbatim inside the new appended section, explicitly labelling it as retired — which satisfies the prose acceptance criteria's "or every surviving occurrence is inside the new section" exception, but the automated grep has no such exception and matched anyway.
- **Fix:** Reworded the artifact's discussion of the retired claim to describe it without reproducing the exact bolded phrase, and moved the verbatim before/after quote to this SUMMARY.
- **Files modified:** `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md`
- **Verification:** `grep -c 'language and structural equivalence' 01.6.2-VALIDATION.md` returns `0`; `! grep -q 'proves \*\*language and structural equivalence only\*\*' 01.6.2-VALIDATION.md` now succeeds.
- **Committed in:** `845e753` (Task 2 commit)

**3. [Rule 1 — acceptance-criterion mismatch] Cross-references between carry-forward rows tripped the no-duplicate-ID check**
- **Found during:** Task 2, running the plan's exact duplicate-ID check (`grep -o 'CF-01\.4-[0-9][0-9]' | sort | uniq -d`) after drafting Part C's table, rather than assuming distinct rows meant distinct IDs.
- **Issue:** Several rows' "how Phase 01.4 discharges it" column named a sibling row by its literal ID (e.g. "batches with CF-01.4-01") to indicate the two rows discharge together on the same host action. The literal ID then appeared twice in the file — once as its own row's ID, once inside another row's prose — which the mechanical uniqueness check correctly cannot distinguish from an actual duplicate row.
- **Fix:** Reworded every such cross-reference to positional language ("batches with the row above/below") instead of repeating the literal ID.
- **Files modified:** `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-VALIDATION.md`
- **Verification:** `grep -o 'CF-01\.4-[0-9][0-9]' 01.6.2-VALIDATION.md | sort | uniq -d` returns no lines.
- **Committed in:** `845e753` (Task 2 commit)

---

**Total deviations:** 3 auto-fixed (1 structural self-catch before any commit, 2 verify-command-mismatch corrections found by running the plan's own exact checks rather than assuming they would pass)
**Impact on plan:** No scope creep — all three are necessary for the plan's own stated verify commands and acceptance criteria to actually pass as written, discovered by running them directly.

## Issues Encountered

- The worktree had no `.claude/mcp/vice/node_modules` (gitignored, not carried into a fresh
  worktree) — symlinked it to the main checkout's copy solely to run the full test suite for Part
  D's measured arithmetic (this plan makes no code changes, so this was a read-only measurement, not
  a build step), then removed the symlink before returning (worktree hygiene), matching every prior
  plan in this phase's own established precedent.
- The mechanical harvest's raw count (46) diverging from the written row count (35) was
  investigated fully rather than adjusted to match — see Accomplishments above. This is not treated
  as a defect in the plan's own calibration; it is exactly the kind of discrepancy this project's
  own "measured, not smoothed" convention exists to surface, and the plan's own objective section
  had already flagged that the SUMMARY-only calibration figure (5) was "correct for SUMMARY files
  and incomplete as a harvest" — this session's full reconciliation confirms and extends that
  finding with the exact causes of every inflating hit.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Phase 01.6.2.1 is now fully executed (6/6 plans).** Criteria E, L and M are all closed: L by
  plans 01–05 (the five lifecycle-policy changes, each its own labelled task with its own test); M
  and E by this plan.
- **The carry-forward list is Phase 01.4's own work item now**, not a caveat filed elsewhere. It
  lives in `01.6.2-VALIDATION.md`, covers both 01.6.2 and 01.6.2.1, and every row names a concrete
  real-host observable — no row is a general area of doubt.
- **The five-defect todo is closed.** Absorbing Phase 01.5 has now fully sealed — 01.6.2 built the
  machine, 01.6.2.1 (this plan) is where the defect-disposition gate actually closed.
- No blockers for whatever runs next in v1.1's sequence (`01.6.3 → 01.4 → 01.3`, per STATE.md's
  AUTONOMOUS RUN PROTOCOL).

## Self-Check: PASSED

All modified files verified present on disk (`01.6.2-VALIDATION.md` with all five new/edited
sections confirmed by direct read; the completed todo file at its new path). All three commit
hashes (`4987e77`, `845e753`, `bc4011f`) verified present in `git log --oneline -5`. Working tree
confirmed clean (`git status --porcelain` empty) after removing the local `node_modules` symlink.

---
*Phase: 01.6.2.1-broker-lifecycle-policy*
*Completed: 2026-08-04*
