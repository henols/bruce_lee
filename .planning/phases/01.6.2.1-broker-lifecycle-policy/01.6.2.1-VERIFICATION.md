---
phase: 01.6.2.1-broker-lifecycle-policy
verified: 2026-08-05T00:00:00Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
requirements_traceability: "NONE CLAIMED — Phase 01.6.2.1's plans all declare requirements: [] in frontmatter (the plan-level requirement IDs used, e.g. \"L\", \"M\", \"D-05\", \"CR-01\", \"WR-01\"…\"WR-04\", are ROADMAP decision/criterion IDs and code-review finding IDs, not REQUIREMENTS.md IDs). REQUIREMENTS.md contains no rows mapped to Phase 01.6.2.1. Verification below is against the ROADMAP goal text and each plan's own must_haves/success_criteria, per the task instruction — no REQUIREMENTS.md traceability exists to report."
re_verification:
  previous_status: gaps_found
  previous_score: 8/9
  gaps_closed:
    - "CR-01: selectWarmInstance()'s post-probe recheck rejects a candidate a concurrent sibling has already DROPPED, not only one already GRANTED (map-membership identity recheck)"
    - "A deterministic two-overlapping-acquires regression test proven RED against the pre-fix code and GREEN against the fix"
    - "handleRelease()'s port-based lookup re-examined so an orphaned grant's release cannot kill an unrelated instance that took the same port (GrantRecord.pid identity check, plus handleExit()'s recycle-sync companion fix)"
    - "01.6.2-VALIDATION.md's Defect-3 disposition corrected to state the concurrent-acquire gap existed between seal and this plan, and to name this plan as what closes it"
  gaps_remaining: []
  regressions: []
---

# Phase 01.6.2.1: Broker Lifecycle Policy Verification Report (Re-Verification)

**Phase Goal:** Fix the broker's five lifecycle-policy defects rather than translate them — five
labelled tasks, five tests, separate commits — disposition Phase 01.5's seven absorbed criteria
one by one against real work (criterion M is where absorbing 01.5 actually seals), and itemise
every claim neither 01.6.2 nor this phase could verify live into the carry-forward list Phase 01.4
must discharge.

**Verified:** 2026-08-05
**Status:** passed
**Re-verification:** Yes — after gap-closure plan 07 (CR-01)

## Re-Verification Summary

The previous verification (2026-08-04) scored 8/9 and filed ONE BLOCKER gap rooted in CR-01: a
concurrency race in `selectWarmInstance()` that let a sibling `handleAcquire()` hand out a grant
for an instance a different sibling had already dropped and was tearing down, with a
cross-session-kill blast radius in `handleRelease()`. Plan 07 (`gap_closure: true`) landed three
commits closing this. Each of the four `missing` items from the previous gap was independently
re-checked against the code at HEAD, not accepted from `01.6.2.1-07-SUMMARY.md`'s narrative.

### Item-by-item disposition

| # | Previously-missing item | Now closed? | Evidence |
|---|---|---|---|
| 1 | Map-membership recheck in `selectWarmInstance()` | ✓ CLOSED | `vice-broker.mts:398` reads `if (record.state !== "ready" \|\| state.instances.get(record.port) !== record) { continue; }` — read directly at HEAD. This is the drop-path recheck; the identical broadened condition also gates the `isReady` winner branch a few lines below, matching `01.6.2.1-REVIEW.md`'s specified fix exactly (not a weakened paraphrase). |
| 2 | A regression test proving RED-then-GREEN against two overlapping acquires | ✓ CLOSED | Independently reproduced by this verifier, not merely re-read: temporarily reverted ONLY the recheck line in a scratch copy of `vice-broker.mts` (removing the `state.instances.get(...) !== record` clause, leaving everything else — including the required `GrantRecord.pid` field — untouched) and ran `node --test --test-name-pattern="CR-01" vice-broker-acquire.test.ts`. Observed the SAME RED failure the SUMMARY reports verbatim: `AssertionError [ERR_ASSERTION]: race-b's own cold-spawn factory must be invoked exactly once — 0 !== 1`. Restored the file (`git diff` confirmed clean) and re-ran: GREEN. The test's construction (a shared deferred-promise probe gated by invocation-order counter, a bounded microtask-poll loop asserting the first caller's drop has already executed in `state.instances` before resolving the second caller's probe) is a genuine deterministic discriminator, not a flaky race — it does not depend on wall-clock timing or scheduler luck. |
| 3 | `handleRelease()`'s port-based lookup re-examined for cross-session-kill blast radius | ✓ CLOSED | `vice-broker.mts:699-720` (read directly): `handleRelease()` now compares `instance.pid === grant.pid` before treating the port's current occupant as this grant's own instance; on mismatch (including an empty port) it deletes only the grant's own bookkeeping and leaves the mismatched occupant untouched, logging a line naming both pids. Two new tests (`vice-broker-acquire.test.ts`, lines 229-260) directly exercise both the mismatch case and the empty-port case and pass. The companion fix holds: `broker-launch.mts:711-726`'s recycle branch syncs the matching grant's `pid` to the respawned record's `pid` (read directly), preventing the new identity check from misfiring against a legitimate recycle — proven by a real end-to-end `superviseChild()` test (`broker-launch.test.ts:1377-1416`) that triggers a real exit event, waits for the real respawn via the epoch-advance idiom, and asserts the grant's pid tracks the NEW pid, not the pre-recycle one. This fix's scope is broader than CR-01 alone (it also covers an ordinary crash-and-give-up swapping a port's occupant without a concurrent acquire), which is the right generalization for the cross-session blast radius the previous verification flagged as the highest-scrutiny item. |
| 4 | Corrected Defect-3 disposition in `01.6.2-VALIDATION.md`'s criterion-M table | ✓ CLOSED | Read directly at lines 474 and 536: both rows now read "DISCHARGED here (non-concurrent case), closed by 01.6.2.1-07 (concurrent case...)" / "FIXED here (non-concurrent case), closed by 01.6.2.1-07 (concurrent case...)", with a shared explanatory note (lines 540-550) stating plainly that the original wording covered only the non-concurrent case and naming `01.6.2.1-VERIFICATION.md`'s CR-01 finding as what caught the gap. This is real history, not a silent relabel. |

**WR-02 disposition:** Deliberately DEFERRED, and honestly recorded — `.planning/todos/pending/2026-08-05-wr-02-grant-time-probe-kill-serialises-the-acquire-hot-path.md` exists (read directly), states the finding, why it is bounded (warm floor defaults to 1 per D-06; worst case sits inside the raised ~120s acquire deadline; only bites a host configured above the default), and both fix options the review offered. This is a reasoned deferral, not a silent drop.

**WR-01/WR-03/WR-04:** All three fixed and tested, confirmed by direct code read plus live test run:
- WR-01: `handleAcquire()` (`vice-broker.mts:446-460`) now calls `selectWarmInstance()` first and only checks `atCapacity()` when it returns `null` — a ready, probe-live candidate is grantable at the ceiling. Both the fix-exercising test and the regression guard (no-candidate-at-capacity still refuses) pass.
- WR-03: the cold-launch arm (`vice-broker.mts:489-499`) deletes the just-created record from `state.instances` immediately when the spawned child's pid is null, before returning `internal`. Test passes and confirms `state.instances` has no entry for the attempted port afterward.
- WR-04: `lastWarmLaunchLogRelPath` moved from module scope into `maintainWarmFloorForRealBroker()`'s own body. A structural test (comment-stripped source scan) confirms the declaration no longer appears at column zero while the identifier still appears indented inside the function body.

**CR-01 hazard entry:** `.planning/RE-FINDINGS.md` carries a dated (2026-08-05), graded (`Evidence:`/`Confidence: HIGH`) hazard entry describing the finding, the two-part fix, and what it saves — read directly, append-only, no existing entry edited.

## Independently Re-Verified in This Session

- `node --test --test-concurrency=1 '.claude/mcp/vice/'*.test.*` → **425 tests / 420 pass / 0 fail / 5 todo**, matching the orchestrator's own count and `01.6.2.1-07-SUMMARY.md`'s claim exactly.
- `npm --prefix .claude/mcp/vice run typecheck` → exit 0, clean.
- `node build.ts && git status --porcelain resources/` → clean (no diff), confirming the generated tier is byte-identical to a fresh build from the committed `.mts` sources.
- The CR-01 regression test's RED claim (item 2 above) — reproduced independently by this verifier from scratch, not accepted from the SUMMARY.
- `git log --oneline` on `main` confirms the three task commits (`5aff2e2`, `24dc691`, `bf845c8`) plus the SUMMARY commit (`d2740ae`) and the merge (`e13371a`) are present; working tree is clean.
- Carry-forward list unaffected by this plan: `01.6.2-VALIDATION.md` still has exactly 35 `CF-01.4-NN` rows, no duplicate IDs — criterion E's own must-have is untouched by this gap closure and remains satisfied as the previous verification found.
- No `TBD`/`FIXME`/`XXX` debt markers in any file this plan modified.

## Goal Achievement (Full Truth Table, Carried Forward + Re-Verified)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | Five lifecycle-policy defects fixed rather than translated, each its own labelled task/test/commit (criterion L) | ✓ VERIFIED | Unchanged from previous verification (re-confirmed: no regression). |
| 2 | Phase 01.5's seven criteria dispositioned one by one against real work, criterion M sealing absorption | ✓ VERIFIED | Now unqualified — the previous verification's caveat on Defect 3's row is resolved (see item 4 above); the seven-criteria table's row 3 now accurately states the concurrent case was open between seal and plan 07, and names plan 07 as what closes it. Criterion M's absorption of Phase 01.5 is now genuinely sealed: CR-01 was the one open thread. |
| 3 | Carry-forward list: every claim neither phase could verify live itemised, Phase 01.4 named owner (criterion E) | ✓ VERIFIED | Unchanged and re-confirmed: 35 rows, no duplicates, Phase 01.4 named owner. |
| 4 | D-07 gated on criterion C's race test being green, checked rather than assumed | ✓ VERIFIED | Unchanged from previous verification. |
| 5 | No live emulator verification claimed as passed | ✓ VERIFIED | Unchanged; plan 07's own tests are all in-process dependency-seam unit/e2e tests, no real `x64sc` connection, confirmed by direct read of the plan's own verification constraint and the test files. |
| 6 | handleAcquire() consults the warm floor before cold-launching and a grant can never outlive its process, including under concurrent acquires (Defect 5 + Defect 3, P-01/P-02/P-03) | ✓ VERIFIED | **Previously FAILED (CR-01 BLOCKER) — now closed.** Both the specific concurrent-acquire race (map-membership identity recheck) and the broader cross-session-kill blast radius (`GrantRecord.pid` identity check) are fixed, tested, and independently reproduced RED-then-GREEN by this verifier. |
| 7 | Probe collapses to exactly one in-process mechanism requiring evidence the emulator answered (D-05/P-05/P-06/P-07) | ✓ VERIFIED | Unchanged from previous verification. |
| 8 | Rename lands cleanly, one commit, last code commit, no alias (D-10/D-11/P-11/P-12/P-13) | ✓ VERIFIED | Unchanged from previous verification. |
| 9 | Acquire deadline raised to ~120s, `.mcp.json` ordering invariant kept true unedited (P-08/P-09) | ✓ VERIFIED | Unchanged from previous verification. |

**Score:** 9/9 truths verified.

## Required Artifacts (Updated)

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.claude/mcp/vice/vice-broker.mts` | `handleAcquire()`/`selectWarmInstance()` warm-pool consult, one grant-recording call site, CR-01 fix, WR-01/WR-03 fixes | ✓ VERIFIED | All confirmed by direct read at HEAD; single `state.grants.set()` call site unaffected by this plan's edits. |
| `.claude/mcp/vice/broker-state.mts` | `GrantRecord.pid` required field | ✓ VERIFIED | Confirmed present, documented, threaded through the single production construction site and both hand-built test literals that needed updating. |
| `.claude/mcp/vice/broker-launch.mts` | `handleExit()`'s recycle branch syncing the matching grant's pid | ✓ VERIFIED | Confirmed at lines 711-726; proven by a real end-to-end recycle test. |
| `.claude/mcp/vice/vice-broker-acquire.test.ts` / `broker-launch.test.ts` / `broker-state.test.ts` | CR-01 regression test, stale-grant tests, recycle-pid-sync test, WR-01/WR-03/WR-04 tests | ✓ VERIFIED | All present, all pass; CR-01 test's RED claim independently reproduced. |
| `01.6.2-VALIDATION.md` | Corrected Defect-3 rows with real history | ✓ VERIFIED | Confirmed at lines 474, 536, 540-550. |
| `.planning/todos/pending/2026-08-05-wr-02-...md` | WR-02 deferral with reasoning and both fix options | ✓ VERIFIED | Present, complete. |
| `.planning/RE-FINDINGS.md` | Dated, graded CR-01 hazard entry | ✓ VERIFIED | Present at 2026-08-05, Evidence/Confidence both filled in (HIGH). |
| `.claude/mcp/vice/resources/*.mjs` | Generated tier, rebuilt, byte-identical to source | ✓ VERIFIED | `node build.ts` produces a clean `git status --porcelain resources/` at HEAD (confirmed live in this session). |

## Key Link Verification (Updated)

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `handleAcquire()` | `selectWarmInstance()` → `state.grants.set()` | single call site, race-safe | ✓ WIRED, race-safe | Structural gate passes; the recheck now verifies map membership by identity, closing the race the previous verification flagged. |
| `selectWarmInstance()`'s probe | `broker-launch.mts probeReady()` | single mechanism | ✓ WIRED | Unchanged. |
| `handleExit()`'s recycle branch | matching `GrantRecord.pid` | pid-sync on respawn | ✓ WIRED | Confirmed by a real end-to-end test triggering a genuine exit event and respawn. |
| `handleRelease()` | `grant.pid` vs `state.instances.get(grant.port)?.pid` | identity guard | ✓ WIRED | Confirmed by two direct tests (mismatch, empty-port) and direct code read. |
| Carry-forward rows | Phase 01.4 | named owner | ✓ WIRED | Unaffected by this plan; re-confirmed unchanged. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full test suite | `node --test --test-concurrency=1 '.claude/mcp/vice/'*.test.*` | 425 tests / 420 pass / 0 fail / 5 todo | ✓ PASS |
| Typecheck | `npm --prefix .claude/mcp/vice run typecheck` | exit 0 | ✓ PASS |
| Resources sync | `node build.ts` then `git status --porcelain resources/` | clean | ✓ PASS |
| CR-01 regression test, GREEN (post-fix, current tree) | `node --test --test-name-pattern="CR-01" vice-broker-acquire.test.ts` | 1/1 pass | ✓ PASS |
| CR-01 regression test, RED (independently reproduced against a scratch revert of ONLY the map-membership clause) | same test, temporarily-reverted recheck line | `0 !== 1` assertion failure, matching SUMMARY's reported RED text verbatim | ✓ PASS — confirms this is a real discriminator, not a claimed-but-unverified RED |
| WR-01/WR-03/WR-04 tests + structural gate | `node --test --test-name-pattern="at_capacity\|ceiling\|internal\|structural" vice-broker-acquire.test.ts broker-launch.test.ts` | 8/8 pass | ✓ PASS |
| Recycle-pid-sync end-to-end test | included in full suite run above | pass | ✓ PASS |

## Probe Execution

Not applicable — no `scripts/*/tests/probe-*.sh` probes declared by this phase or plan 07.

## Requirements Coverage

No REQUIREMENTS.md IDs are claimed by this plan (frontmatter lists ROADMAP/review IDs — `L`, `M`,
`D-05`, `D-07`, `P-02`, `P-03`, `CR-01`, `WR-01`…`WR-04` — not REQUIREMENTS.md rows), matching the
phase's own established shape. Nothing to trace here by design.

## Anti-Patterns Found

None found in any file this plan modified. The four Warnings the previous verification carried
forward from `01.6.2.1-REVIEW.md` (WR-01…WR-04) are now dispositioned: WR-01, WR-03, WR-04 fixed
and tested; WR-02 deliberately deferred with a written, reasoned todo naming both fix options — not
silently dropped. No `TBD`/`FIXME`/`XXX` markers found.

## Human Verification Required

None. As with the previous verification, no live-emulator claim is made anywhere in this phase's
artifacts, and the carry-forward list already owns every such claim for Phase 01.4 to discharge.
The pre-existing `broker-kill.test.ts` banner-ordering flake under full-suite load did not fire in
this session's run and is documented context, not a gap.

## Gaps Summary

None. The sole BLOCKER the previous verification filed (CR-01) is closed, independently confirmed
by this verifier through direct code reading, a live reproduction of the fix's RED-then-GREEN
discriminating power (not merely a re-read of the SUMMARY's claimed transcript), a live full-suite
run matching the reported count exactly, and a live typecheck/resources-rebuild pass. The
cross-session-kill blast radius (`handleRelease()`'s port-based lookup) — flagged in the previous
verification as the item warranting the highest scrutiny — is closed by a fix whose scope is
correctly generalized beyond CR-01's specific race (it also covers an ordinary crash-and-give-up
swapping a port's occupant), with a companion recycle-sync fix proven by a genuine end-to-end test
rather than an isolated unit that never reaches the real exit-driven respawn path. WR-01/WR-03/WR-04
are fixed and tested; WR-02 is honestly recorded as a deliberately deferred, reasoned decision.
`01.6.2-VALIDATION.md`'s Defect-3 disposition now carries real history rather than a relabelled
verdict. Criterion M's absorption of Phase 01.5 is now genuinely sealed — this was the gate CR-01
held open, and its closure means Phase 01.4 does not silently inherit any lost defect scope from
Phase 01.5.

Phase 01.6.2.1 is verified complete: 9/9 must-haves, no gaps, no human verification items.

---

_Verified: 2026-08-05_
_Verifier: Claude (gsd-verifier)_
