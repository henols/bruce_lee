---
phase: 01.6.2.1-broker-lifecycle-policy
plan: 05
type: execute
wave: 5
depends_on: ["01.6.2.1-04"]
files_modified:
  - .claude/mcp/vice/broker-launch.mts
  - .claude/mcp/vice/vice-broker.mts
  - .claude/mcp/vice/broker-state.mts
  - .claude/mcp/vice/broker-kill.mts
  - .claude/mcp/vice/broker-launch.test.ts
  - .claude/mcp/vice/vice-broker-launch.test.ts
  - .claude/mcp/vice/broker-control.test.ts
  - .claude/mcp/vice/broker-kill.test.ts
  - .claude/mcp/vice/broker-e2e.test.ts
  - .claude/mcp/vice/resources/broker-launch.mjs
  - .claude/mcp/vice/resources/vice-broker.mjs
  - .claude/mcp/vice/resources/broker-state.mjs
  - .claude/mcp/vice/resources/broker-kill.mjs
  - .planning/RE-FINDINGS.md
autonomous: true
requirements:
  - "L"
  - "M"
  - "D-10"
  - "D-11"
  - "D-25"
  - "P-11"
  - "P-12"
  - "P-13"
user_setup: []

must_haves:
  truths:
    - "One vocabulary reaches every surface: the environment knob, the on-disk discovery record, the control-plane reply, every internal identifier, every log line and every comment all say warm floor (D-10, P-11)"
    - "The retired environment variable breaks cleanly — no alias, no fallback, no deprecation path — and setting it does not change behaviour (D-10, unweakened)"
    - "An operator who still has the retired variable set is told so by the mandatory start-time banner, naming the replacement (D-25, P-13)"
    - "A structural gate proves the retired variable name and the retired on-disk field name appear nowhere in tracked TypeScript, so the clean break cannot be silently re-softened (D-10)"
    - "The whole rename is one commit, and it is the last code commit of the phase, so backing out the only costly-to-reverse decision here is one revert (D-11, P-12)"
    - { statement: "On a real host, an operator with the retired variable in a shell profile sees the banner naming it as ignored and naming its replacement, rather than silently getting the default", verification: backstop }
    - { statement: "On a real host, a human reading the discovery record by hand finds one vocabulary in it and in the broker's own log lines", verification: backstop }
  artifacts:
    - ".claude/mcp/vice/vice-broker.mts — the discovery record's floor field renamed on disk"
    - ".claude/mcp/vice/broker-kill.mts — the start-time banner naming the retired variable when it is set"
    - "a structural gate asserting neither retired name survives in tracked TypeScript"
  key_links:
    - "the on-disk discovery-record field and the control-plane host-state reply now use the same spelling, so the two cannot drift into two vocabularies again"
    - "the retired variable is read nowhere, and is named only by the banner — the one place naming it does not weaken the break"
---

<objective>
Land the `spares` → `warm floor` rename per **D-10**, reaching the on-disk discovery record per
**P-11**, with the start-time warning of **P-13** and no shim of any kind. **One commit, and the last
code commit of the phase** (**D-11**, **P-12**).

Purpose: `spares` denoted the entire ready pool while reading as *"extras beyond what is in use"*, and
was misread as exactly that during a live outage. This is criterion **L**'s final labelled change.

Output: one vocabulary on every surface, a clean break with a loud banner, and a structural gate that
keeps it clean.

## Scope this against what is actually left, not against a fresh rename

The landed code is **already half-renamed**, and reading it as un-renamed would produce a large
pointless diff and miss the one surface that matters. Confirmed at plan time:

| Surface | State today | This plan |
|---|---|---|
| internal identifiers (`resolveWarmFloor`, `resolveWarmFloorForRecord`, the `warmFloor` option and locals) | **already correct** | leave alone |
| control-plane host-state reply and the container-side client's type | **already emits `warm_floor`** | leave alone |
| the environment knob | still the retired name, read in two places | rename |
| the on-disk discovery-record field | still the retired name | rename — this is P-11 |
| log lines and comments | mixed; several still say *spare* / *spares* | one vocabulary |
| test fixtures and assertions | several carry the retired variable or the retired field name | rename in place, never delete |

**The two identifier spellings, settled here (CONTEXT delegates them).**

- The knob becomes **`VICE_BROKER_WARM_FLOOR`** — CONTEXT names it, and the banner text P-13 requires
  quotes it.
- The record field becomes **`warm_floor`** — chosen, not defaulted: the control-plane host-state reply
  and the container-side client's own type **already** use exactly that spelling. Picking anything else
  would leave the discovery record and the wire disagreeing about the same number, which is the
  two-vocabularies failure D-10 exists to end. It is also the snake_case shape every other field in the
  record already uses.

## Where P-12 met criterion E, and how it was resolved

**P-12 says the rename is the last commit of the phase.** Criterion **E**'s carry-forward list and
criterion **M**'s disposition (plan 06) must land after it, so a literal reading of P-12 is not
satisfiable alongside criteria E and M as specified. Resolved as follows, deliberately and on the
record:

**The rename is the last *code* commit. Plan 06 commits only `.planning/` files after it.**

P-12's deciding reason is mechanical and is preserved exactly: *"landing it last means backing it out
is one revert; landing it mid-phase means backing it out drags four later tasks with it."* A revert of
this commit drags nothing in plan 06 — its commits touch no source file, no test and no generated
artifact. Meanwhile criterion E must itemise the final state and criterion M must disposition against
landed work, and 01.6.2's own precedent put its ledger-and-arithmetic plan last for the same reason
(its suite total was measured against the final green tree rather than computed). Plan 06 records the
same resolution.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-CONTEXT.md
@.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-CONTEXT.md
@.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-04-SUMMARY.md
@.claude/CLAUDE.md
</context>

## Artifacts this phase produces

See plan 01's table for the full phase-wide list. This plan's own contributions:

| Kind | Name | Note |
|---|---|---|
| renamed env var | `VICE_BROKER_WARM_FLOOR` | replaces the retired name; clean break, no alias |
| renamed record field | `warm_floor` in the discovery record | matches the control-plane spelling already in use |
| banner line (new) | a start-time line naming the retired variable as set-and-ignored, and naming its replacement | D-25 / P-13 |
| test (new, structural) | neither retired name survives anywhere in tracked TypeScript | |
| vocabulary | *spare* / *spares* retired from log lines and comments in favour of *warm floor* / *warm instance* | |

<!-- planner-discipline-allow: VICE_BROKER_SPARES, spares_target, spares, spare -->

<tasks>

<task type="auto">
  <name>Task 1 — D-10/D-11/P-11/P-12/P-13: the rename, in one commit, as the phase's last code commit</name>

  <files>.claude/mcp/vice/broker-launch.mts, .claude/mcp/vice/vice-broker.mts, .claude/mcp/vice/broker-state.mts, .claude/mcp/vice/broker-kill.mts, .claude/mcp/vice/broker-launch.test.ts, .claude/mcp/vice/vice-broker-launch.test.ts, .claude/mcp/vice/broker-control.test.ts, .claude/mcp/vice/broker-kill.test.ts, .claude/mcp/vice/broker-e2e.test.ts, .planning/RE-FINDINGS.md</files>

  <read_first>
    - Run the census first and work from its output rather than from this plan's counts: `grep -rn --include='*.mts' --include='*.ts' -e 'VICE_BROKER_SPARES' -e 'spares_target' -e 'spares' -e 'spare' .claude/mcp/vice/ | grep -v '/resources/'`. Read **every** hit before editing any of them; several are prose in comments and one or two may be unrelated English usage.
    - `.claude/mcp/vice/vice-broker.mts` lines 102-128 — the discovery record's field-set comment and the `BrokerRecord` interface, which is where the on-disk field is declared; line ~658 — where the record is populated; lines 136-161 — the two local env-var readers and their mirrored-defaults comment.
    - `.claude/mcp/vice/broker-launch.mts` lines 377-383 — the floor reader; lines 405-437 — the options-bag doc comments; lines 439-547 — the floor pass's log lines, including the *"warmed 1 spare this pass"* and *"warming no further spares"* wordings.
    - `.claude/mcp/vice/broker-control.mts` lines 82-90, 152-162 and 480-490 — the host-state field, its wire shape and where it is emitted. **Read these to confirm the wire already uses the target spelling, then leave the file alone.** It is deliberately absent from this plan's `files_modified`.
    - `.claude/mcp/vice/vice-broker-client.ts` lines 395-400 and 640-650 — the container-side host-state type and its parse. Same reason: confirm, do not change.
    - `.claude/mcp/vice/broker-kill.mts` — `startupBanner()` in full, and how it composes its lines.
    - `.claude/mcp/vice/broker-kill.test.ts` around lines 390-410 — the banner's three landed assertions and the structural source-order check that the banner precedes the control listener. Both must keep passing.
    - `.claude/mcp/vice/vice-broker-launch.test.ts` around lines 108-155 — the expected discovery-record key list and the floor-value assertion plan 03 flipped to 1. The key list is the assertion that catches a half-done field rename.
    - `.claude/mcp/vice/broker-control.test.ts` around line 694 — a hand-written discovery-record fixture carrying the retired field name.
    - `.claude/mcp/vice/broker-e2e.test.ts` — every environment fixture naming the retired variable, including the ones passing zero for test isolation and the one plan 01 left a migration note on.
    - `.claude/mcp/vice/broker-state.mts` around line 125 — a comment naming the retired concept.
    - `.claude/mcp/vice/broker-launch.test.ts` — remaining prose hits, including a comment around line 406 referring to a retired bash function name.
    - `.planning/phases/01.6.2-the-one-process-host-broker/01.6.2-CONTEXT.md` D-10 — that the break is deliberate, that the developer was shown the silent-default failure mode, and chose the clean break anyway.
  </read_first>

  <action>
One commit. Work through the census output in this order so no surface is left half-renamed:

1. **The knob.** Rename the environment variable to `VICE_BROKER_WARM_FLOOR` at both read sites — the
   floor reader in `broker-launch.mts` and the record reader in `vice-broker.mts`. Keep both readers'
   coercion behaviour and their default of 1 exactly as plan 03 left them. **Add no alias, no
   fallback, no deprecation read of the old name anywhere.** An unknown environment variable is simply
   ignored, which is the whole of D-10's clean break; a fallback read would be the shim D-10 refuses.

2. **The on-disk field (P-11).** Rename the discovery record's floor field to `warm_floor` — in the
   `BrokerRecord` interface, at the site that populates it, and in the fourteen-field comment above
   the interface that enumerates the record's contents. This is safe because the record is rewritten
   by the broker at every start and the container-side liveness classifier parses only the process id
   and the heartbeat timestamp; the field is a configuration echo for a human reading the file by
   hand, which is the audience D-10's rename exists for. Confirm that claim by reading the classifier
   rather than trusting this sentence, and record the confirmation.

3. **Log lines and comments.** Retire the words *spare* and *spares* from every log line and every
   comment in the four host-bound modules, in favour of *warm floor* and *warm instance*. Two log
   lines in the floor pass name a spare explicitly and must be reworded. Leave any hit that is
   genuinely unrelated English prose alone, and say in the SUMMARY which hits were left and why —
   a rename that also rewrites unrelated prose is harder to review, and D-11's whole purpose is a
   commit a bisect can attribute.

4. **Internal identifiers: change nothing.** They already read correctly. Do not rename
   `resolveWarmFloor`, `resolveWarmFloorForRecord`, the `warmFloor` option or any local. A diff that
   churns already-correct identifiers hides the three changes above.

5. **The control plane and the container-side client: change nothing.** Both already use the target
   field spelling. Their absence from this task's file list is deliberate.

6. **Tests: rename in place, never delete.** The discovery-record key list, the floor-value
   assertion's field name, the hand-written record fixture, and every environment fixture naming the
   retired variable. The key-list assertion is the one that catches a half-done field rename, so it
   must end up naming the new field exactly. Strike plan 01's remaining migration note now that this
   plan has done what it pointed at.

7. **The banner (D-25 / P-13).** Add one line to `startupBanner()`, emitted **only when the retired
   variable is set**, stating that it is set and ignored and naming `VICE_BROKER_WARM_FLOOR` as its
   replacement. This is the one place naming the retired variable does not weaken the break: the
   banner does not *read* the variable for its value, it only reports its presence. It closes the
   single failure mode the developer was shown when choosing the clean break — an operator with it in
   a shell profile silently getting the default. Add a test asserting the line appears when the
   variable is set and does **not** appear when it is unset, saving and restoring the variable around
   the test body. Keep all three landed banner assertions and the structural banner-order check
   passing without editing them.

8. **The structural gate.** Add a test asserting that neither retired name — the variable nor the
   record field — appears anywhere in tracked TypeScript under `.claude/mcp/vice/`, excluding the
   generated tier and excluding the banner's own single mention, which it must allow explicitly and by
   name rather than by a loose pattern. Strip comment lines before matching, following the landed
   structural gates' idiom. Demonstrate its discriminating power: temporarily reintroduce one
   occurrence, observe RED, remove it, observe GREEN; record both.

9. **Rebuild and diff-check the generated tier**, which four of this plan's modules feed:
   `node .claude/mcp/vice/build.ts`, then confirm
   `git status --porcelain .claude/mcp/vice/resources/` is clean once the emitted files are staged.

10. **Append one findings entry**, inside this same commit rather than a later one, so P-12's
    last-code-commit property holds. The finding: a rename that lands across three tiers at different
    times produces, inside the very change meant to end it, the two-vocabularies confusion it exists
    to prevent — internals said one thing, the wire said another, and the on-disk record said a third,
    for the length of a whole phase. The reusable lesson is that the on-disk and wire surfaces are the
    ones a human reads under pressure, so they are the ones to rename **first**, not last. Grade it:
    `**Type:** hazard`; `**Evidence:**` — observed directly in this repository's own landed code at
    plan time, with the three tiers named; `**Confidence:** HIGH`.

**Run the full suite before committing, not only the touched files.** This commit renames an
environment variable that end-to-end fixtures set, so a missed fixture presents as a test that quietly
runs on the default rather than as a compile error.
  </action>

  <verify>
    <automated>cd /workspaces/bruce_lee && node --test '.claude/mcp/vice/'*.test.* && npm --prefix .claude/mcp/vice run typecheck && node .claude/mcp/vice/build.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn --include='*.mts' --include='*.ts' 'VICE_BROKER_SPARES' .claude/mcp/vice/ | grep -v '/resources/' | grep -v 'startupBanner\|banner'` returns no lines — the only surviving mention is the banner's own.
    - `grep -rn --include='*.mts' --include='*.ts' 'spares_target' .claude/mcp/vice/ | grep -v '/resources/'` returns no lines.
    - `grep -rn --include='*.mts' --include='*.ts' 'VICE_BROKER_WARM_FLOOR' .claude/mcp/vice/ | grep -v '/resources/'` returns at least three lines: the two readers and the banner.
    - Comment-stripped host-bound sources contain no read of the retired variable's value: for each of `broker-launch.mts` and `vice-broker.mts`, `grep -vE '^\s*(//|\*|/\*)' <file> | grep -c 'env.VICE_BROKER_SPARES'` returns `0`.
    - A real broker start with no floor override writes a discovery record whose floor field is named `warm_floor` and whose value is 1 — asserted by `vice-broker-launch.test.ts`'s key-list and value assertions, both passing.
    - `git diff` for this commit shows **zero** changes to `broker-control.mts` and `vice-broker-client.ts`.
    - `git diff` for this commit shows no rename of `resolveWarmFloor`, `resolveWarmFloorForRecord`, or the `warmFloor` option identifier.
    - A new test asserts the banner names the retired variable when it is set and omits the line when it is unset; both halves pass, and the variable is saved and restored around the test body.
    - The three landed banner assertions and the structural banner-order check pass without being edited — `git diff` shows no change inside those test bodies.
    - A new test whose name begins `structural:` asserts neither retired name survives in tracked TypeScript, strips comment lines before matching, and allows the banner's single mention by name rather than by a loose pattern. Its discriminating power is demonstrated live and both observations are recorded.
    - `node --test '.claude/mcp/vice/'*.test.*` reports 0 failures; the SUMMARY states the final total and reconciles it against the running baseline.
    - `npm --prefix .claude/mcp/vice run typecheck` exits 0; `node .claude/mcp/vice/build.ts` exits 0 and `git status --porcelain .claude/mcp/vice/resources/` is empty once the emitted files are committed.
    - The SUMMARY lists every census hit left unchanged, with its reason.
    - `.planning/RE-FINDINGS.md` grew by exactly one entry, with additions only, inside this same commit.
    - **Exactly one commit for this whole task** (D-11's own-commit rule and P-12's last-code-commit rule), and it is the last commit in the phase that touches any file under `.claude/mcp/vice/`.
  </acceptance_criteria>

  <done>One vocabulary reaches the knob, the on-disk record, every log line and every comment; the retired variable is read nowhere and breaks cleanly with no alias or fallback; the banner names it as set-and-ignored alongside its replacement; a comment-stripped structural gate keeps the break clean and was shown able to fail; the whole change is one commit and it is the phase's last code commit.</done>

  <reversibility rating="costly">D-10 is operator-facing and the break is the point. This is why the change is one commit and the phase's last code commit — backing it out is one revert. No decision checkpoint is owed: CONTEXT already rates P-11 costly rather than one-way, the developer was shown the silent-default failure mode and chose the clean break, and P-12 exists precisely as the mitigation. Do not re-derive the rating.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator environment to broker process | The floor knob is read at the broker's own uid; after this commit the old name is read by nothing |
| broker to the on-disk discovery record | The record is written by the broker and read by the container-side liveness classifier and by humans |
| broker to operator, at startup | The banner is the only channel that tells an operator what their configuration is actually doing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.2.1-19 | Repudiation | the retired variable silently ignored, so an operator's configured floor is not the one in effect and nothing says so | high | mitigate | The mandatory start-time banner emits a line naming the retired variable as set-and-ignored and naming its replacement, asserted by a test in both directions. This closes the exact failure mode the developer was shown when choosing the clean break, without weakening the break — the banner reports the variable's presence and never reads its value |
| T-01.6.2.1-20 | Tampering | a consumer still expecting the retired on-disk field name reads a missing value and coerces it to something harmless-looking | low | accept | The container-side liveness classifier parses only the process id and the heartbeat timestamp, confirmed by direct read in this task; the field is a human-facing configuration echo. The structural gate additionally proves no reader in tracked TypeScript references the old name |
| T-01.6.2.1-21 | Tampering | a partially-applied rename leaving a fallback read of the old name, silently re-softening the clean break | medium | mitigate | A comment-stripped structural gate asserts the retired variable appears nowhere except the banner's single, explicitly-allowed mention, and an acceptance criterion greps specifically for a read of its value returning zero |
| T-01.6.2.1-22 | Repudiation | a missed end-to-end environment fixture quietly running on the default instead of failing loudly | medium | mitigate | The rename of an environment variable produces no compile error, so the full suite is run before committing and the SUMMARY reconciles the final total; the fixtures are enumerated from a census whose output is read in full before any edit |
| T-01.6.2.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | No package installs in this phase; no legitimacy checkpoint owed. Row retained for register completeness |

No `high` threat is left undispositioned.
</threat_model>

<verification>
- `node --test '.claude/mcp/vice/'*.test.*` — full suite, 0 failures, final total stated and reconciled.
- `npm --prefix .claude/mcp/vice run typecheck` — exit 0.
- `node .claude/mcp/vice/build.ts` then `git status --porcelain .claude/mcp/vice/resources/` — clean.
- **Exactly one commit**, and it is the last commit of the phase touching anything under `.claude/mcp/vice/` (P-12, as resolved in the objective).
- One live discriminating-power demonstration recorded, for the structural gate.
</verification>

<success_criteria>
The rename reaches the knob, the on-disk discovery record, every log line and every comment, with
internal identifiers and the already-correct control-plane spelling untouched; the retired variable is
read nowhere and breaks cleanly with no alias, fallback or deprecation path; the start-time banner names
it as ignored and names its replacement; a structural gate proves the break stays clean and was shown
able to fail; and the whole change is one commit, the last code commit of the phase.
</success_criteria>

<output>
Create `.planning/phases/01.6.2.1-broker-lifecycle-policy/01.6.2.1-05-SUMMARY.md` when done.
</output>
