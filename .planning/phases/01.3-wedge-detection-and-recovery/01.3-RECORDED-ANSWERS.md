# Phase 01.3 — Recorded Answers

Answers the four success criteria that no container-side automated check can settle on its own,
plus the continuity confirmation and the guard re-proof. Each section quotes the ROADMAP criterion
verbatim before answering it — no criterion is reworded to fit what this plan achieved (D-12, and
the same rule extended to criteria 3, 9 and 11).

**Topology note, read before anything below.** Phase 01.3's plans (01–05) were written and executed
against a topology where `vice-supervisor.sh` was a standalone per-instance host process that
outlived broker restarts. Phase 01.6 (`vice-broker.mts`, `broker-launch.mts`) collapsed that process
inward: the broker is now the one process that spawns, supervises and respawns every instance, and
the exit-driven respawn step (`handleExit()` in `broker-launch.mts`) is what used to be
`vice-supervisor.sh`'s daemon loop. `resources/vice-supervisor.sh` no longer exists on disk. The
mechanism this phase's tools depend on — kill the child, an exit handler respawns it on the same
port with a bumped epoch — is unchanged in substance; only which file and which process implements
it changed. Citations below point at the current TypeScript location, not at the retired shell
script the original plan text names.

---

## Criterion 3 — Partial staleness is classified, not treated as a wedge

**Roadmap text, verbatim:** *"Partial staleness is classified, not treated as a wedge. Plan 01-04's
Task 2 incident had `vice_registers_get` returning a byte-for-byte identical snapshot while
`vice_vicii_get_state` reported genuine post-hard-reset change — one tool's stale response is not
proof the machine is frozen. The procedure names which reads are load-bearing for the verdict and
which are corroborating, so a single misbehaving response path does not trigger a recycle of a
healthy instance."*

**Outcome:** MET LIVE, WITH A CAVEAT — on attempt 2 of the 2-attempt budget. **Tempered from a plain
"MET LIVE":** the reproduction below may equally be an artefact of a boot-fresh instance with no disk
attached sitting in an idle KERNAL/BASIC wait loop, which is the honest caveat two paragraphs down.
The label carries that caveat forward rather than reading as an unqualified live proof.

The host broker was up and this session already held a granted, healthy instance (`vice_ping`:
`{"status":"ok","version":"3.10","machine":"C64SC","execution":"running"}` — not a broker-absent
state), so the precondition was met and both attempts were live, this session, 2026-08-05.

- **Attempt 1.** Baseline `vice_registers_get` (`PC:58836`), `vice_execution_run`, three `vice_ping`
  polls, `vice_execution_pause`, `vice_registers_get` again (`PC:58833` — moved) and
  `vice_vicii_get_state` (`raster_line` moved 311→ a fresh value). Both reads changed. Called
  `vice_diagnose`: verdict **`live`**, load-bearing evidence a 31,374-cycle bracket. No staleness
  observed on this attempt.

- **Attempt 2.** Resumed again, polled `vice_ping` five times, paused, read registers and VIC-II
  state. `vice_registers_get` returned **byte-for-byte identical** output to the immediately
  preceding read (`PC:58833 A:0 X:0 Y:10 SP:243`, every flag identical) while `vice_vicii_get_state`
  reported a genuinely different `raster_line` (311 → 57) and a different internal `registers`
  array — the exact shape the criterion names. Called `vice_diagnose`, which returned, verbatim:

  > `vice_diagnose verdict: stale_read_path`
  >
  > Load-bearing evidence: the restart epoch (already checked, at zero emulator cost) and the
  > stopwatch cycle delta across the bracket -- bracket 1 retired 50756 cycles (1 bracket run, 1
  > resume call).
  > ...
  > The register-read path returned a byte-identical snapshot across both ends of an advancing
  > bracket -- that read path is stale, but the machine is demonstrably not frozen.

  The verdict is **not** `wedged`, and the report **names the stale read** explicitly (the register
  path) while citing the non-zero cycle bracket as the load-bearing evidence that overrides it. This
  is a live reproduction of the criterion's exact shape, and the detector classified it as the
  criterion requires — not `wedged`. **Softened from an earlier draft's "correctly classified":** that
  word imports a judgement about the true underlying cause that this session's evidence does not
  support (see the honest caveat immediately below, which the project's own confidence scale grades
  LOW) — what is established is that the branch fires and is not `wedged`, not that the classification
  is "correct" in the deeper sense of having identified the true cause.

**Honest caveat, stated so this is not overclaimed.** This session's reproduction does not by itself
prove the *mechanism* behind the original 01-04 Task 2 incident (a genuinely stale/cached read path)
rather than a more mundane explanation: a boot-fresh instance with no disk attached sits almost all
its time in a KERNAL/BASIC keyboard-wait idle loop, so two register reads taken moments apart can
legitimately sample the identical instruction while the VIC-II raster position — which advances every
line regardless of what the CPU is doing — has moved. Both explanations produce the identical
observable shape from outside the `mcp__vice__*` surface, and D-11 puts distinguishing them out of
scope (it would need host-side introspection of *why* the read returned what it returned, not just
that it did). What this attempt proves, and all it claims to prove: **the ambiguous shape occurs on a
real, live, healthy instance, and `vice_diagnose` classifies it as `stale_read_path`, never as
`wedged`** — which is exactly what the criterion requires and what a recycle-on-any-mismatch policy
would have gotten wrong. Whether the underlying cause is a genuine stale read path or a legitimate
idle-loop PC revisit is not settled by this attempt and is not what the criterion asks for.

Per D-07, the detector's classification logic is *also* proven deterministically against the
synthetic fixture in `vice-proxy.test.ts` (`registers byte-identical with an advancing counter is
stale_read_path`), which remains the CI-permanent proof; this live attempt is corroborating evidence
on top of that fixture, not a replacement for it.

**Machine left running** (`vice_execution_run` called after the second `vice_diagnose`), per the
skill's own "leave the machine sane" rule.

---

## Criterion 11 — The recovery path is exercised against an actual non-advancing emulator, or the phase records that one could not be produced on demand

**Roadmap text, verbatim:** *"The recovery path is exercised against an actual non-advancing
emulator, or the phase records that one could not be produced on demand. This phase cannot be
verified by unit tests alone: its subject is a failure state that has occurred five times unbidden
and never once on request. The strongest reproduction candidate on record is the
`$DD00`-attribution-then-resume technique, which froze two independent sessions at the identical
`PC:2014`. Whether that reproduces a third time is itself a finding worth having."*

**Outcome:** NOT MET. **Corrected: never attempted, not "could not be produced on demand."** An
earlier draft of this line overstated a narrower fact: the sole blocker in the one session that tried
was that `vice_diagnose`/`vice_recycle` were unreachable from that session's own tool surface — a
session-specific gap, not a property of the emulator or the technique. That gap cleared 2026-08-04
(Phase 01.4), and this very record's own Criterion 3 section above makes live `vice_diagnose` calls in
this session, which is itself proof the blocker no longer applies. Denominator: 0 of 6 (from
`01.3-TRIGGER-HUNT.md`, blocked before its first attempt), plus two further `vice_recycle` calls on
record, both against healthy — not wedged — instances.

`01.3-TRIGGER-HUNT.md` (plan 01.3-05) is the load-bearing record for this criterion: its bounded,
six-attempt budget (three per variant) was never spent because `vice_diagnose`/`vice_recycle` were
confirmed unreachable from that session's own tool surface before Variant A's first attempt, and the
plan's own contingency for "cannot be reached, record why, stop" was invoked rather than gambling the
shared instance. Verdict there: **0 of 6**, fully documented with the reason.

Two `.planning/incidents/` records exist on disk today, and both are explicitly **not** wedge
recoveries:

- `.planning/incidents/20260803171235702-port6510-epoch13.md` — *"Phase 01.6.1 UAT test 2:
  exercising the recycle path of the TypeScript-converted vice-proxy against a real emulator, to
  verify behavioral equivalence... No run in flight; this is a deliberate verification call, not a
  wedge recovery."* Reported kill stage `sigterm`, epoch 13 → 14.
- `.planning/incidents/20260805160045723-port6603-epoch1.md` — Phase 01.4-04's deliberate criterion-1
  verification of `vice_recycle` itself, against an instance `vice_diagnose` had just confirmed
  `live` (35,145-cycle bracket). Reported kill stage `sigterm`, epoch before/after both reported `1`
  (the separately-filed epoch-text reporting defect, not a real non-move — the replacement's fresh
  `pid`/`spawned_at` in the next call's error confirms a genuine kill occurred).

Neither record documents the machine failing to advance before the kill; both name their own healthy
starting state explicitly.

**Corrected reasoning.** An earlier draft of this section said re-attempting the trigger hunt here
"would duplicate that plan's bounded-budget discipline rather than answer this criterion with new
evidence." That is not true — a first attempt is the only new evidence available for a hunt that made
zero attempts (`01.3-TRIGGER-HUNT.md`'s own verdict: "Denominator: 0 of 6 attempts... The budget is
entirely unspent"). The real state of play is narrower and more interesting than that reasoning
implied: the blocker that stopped 01.3-05's single session (`vice_diagnose`/`vice_recycle` unreachable
from that session's own tool surface) cleared 2026-08-04, `01.3-TRIGGER-HUNT.md`'s own stated resume
condition ("re-attempt once `vice_diagnose` and `vice_recycle` are confirmed reachable") is satisfied
as of this writing, and this record's Criterion 3 section above is itself evidence the tools are
reachable in a session like this one.

**Orchestrator decision, recorded here rather than acted on.** The hunt is deliberately **not** being
run now. The developer has no machine access during this session, so if a reproduction attempt wedges
the emulator in a way `vice_recycle` cannot recover, nobody can restore it. That downside is
asymmetric against the upside of reproducing a state this phase can already *detect* (`vice_diagnose`)
and *recover from* (`vice_recycle`) without a live reproduction. Criterion 11's escape hatch therefore
rests on this choice — available and not taken — which is a different, more honest state than "not
producible."

**What therefore remains unproven:** the recovery path (`vice_recycle`) has been exercised twice
against a real, healthy, live instance and once against a synthetic non-advancing stand-in
(`vice-proxy.test.ts`'s wedge fixtures) — but **never once against a real, non-advancing emulator**.
The subject of this criterion has occurred five times unbidden across this project's history.
**Corrected:** an earlier draft of this line said the wedge had been "sought on six separate
deliberate attempts... without reproducing." That overstates what happened — `01.3-TRIGGER-HUNT.md`'s
own verdict is unambiguous: *"Denominator: 0 of 6 attempts... The budget is entirely unspent,"* with
an attempt log that is explicitly empty. Zero attempts were made, not six unsuccessful ones. (The
likely origin: `01.3-06-PLAN.md:279`, written 2026-08-02 before the hunt ran, asserted "the trigger
hunt ran to its stated six-attempt budget" — a plan-time expectation this record reproduced instead of
checking against the hunt's own recorded outcome.) This is the honest, bounded-negative result D-07
anticipates: the recycle mechanism is proven sound
against every case that could be produced on demand, and the one case that matters most for
criterion 11's literal text remains a gap in what could be produced, not in what was built to handle
it.

---

## Criterion 8 — Host-side detectability

**Roadmap text, verbatim:** *"Whether a wedge is detectable host-side is answered with evidence, and
a negative answer is a real result. The hypothesis on record is a hung GTK/OpenGL event loop, never
tested. If the host cannot distinguish a wedged `x64sc` from a busy one, the phase says so and
delivers container-side detection plus unconditional recycle rather than quietly shipping a detector
that guesses."*

**Outcome:** DISCHARGED BY DECISION, not by evidence. Rewording was considered and rejected (D-12).

Three things, and nothing more:

1. **Not investigated.** No host-side introspection of `x64sc` (a stack trace, thread state,
   GTK/OpenGL event-loop inspection, a debugger attached to the emulator process) was attempted at
   any point in this phase.
2. **The boundary, and why — corrected.** An earlier draft of this point attributed the boundary to
   `.claude/CLAUDE.md`'s hard rule that `mcp__vice__*` is the only permitted route to the emulator.
   That rule forbids a **second route** to the emulator — a script or module opening its own
   connection, reading broker state to find a port, importing a transport module as a library. It
   does **not** forbid this project's own host-side broker — which already spawns `x64sc` as its
   child, records its pid, and writes heartbeats — from observing that child host-side; that would be
   the existing route's own owner looking at what it already holds, not a second route. The boundary
   was drawn by **decision, not by the rule**: D-11 chose container-side observation through
   `mcp__vice__*` alone as this investigation's scope, and D-12 records that criterion 8 is discharged
   by that choice rather than by evidence. It is a defensible choice — host-process introspection is a
   different, larger undertaking than this phase's scope — but it is a decision this phase made, not
   a categorical prohibition the hard rule imposed on it.
3. **The hypothesis stays on record, untested.** A hung GTK/OpenGL event loop is still the leading
   candidate explanation (CONTEXT.md § Deferred Ideas), recorded so the investigation remains
   *possible* later without being attempted now.

**The delivered container-side detection plus unconditional recycle matches the fallback this
criterion's own text names for a negative answer — but whether the negative answer actually holds
was never established, which is a narrower claim than "the criterion is answered."** The criterion's
own text is conditional: **"If** the host cannot distinguish a wedged instance from a busy one,"
then the container-side-detection-plus-recycle fallback applies. Point 1 above stands unchanged by
this correction — nothing was investigated, so nothing establishes that the antecedent ("the host
cannot distinguish...") is true rather than merely untested. An earlier draft of this paragraph said
"the criterion is answered by the shape of the deliverable, not evaded by it," which assumes the
antecedent without evidence for it. The honest statement: the deliverable happens to be the one the
criterion's own text names for a negative outcome, and D-12 discharges the criterion on that basis by
decision — a real resolution, recorded honestly as resting on a choice, not as a claim that the
antecedent was shown true.

**Rewording considered and rejected.** D-12 records this explicitly: *"Rewording the criterion to
fit the plan was considered and rejected as editing the scorecard."* The same discipline applies
here — this section restates the criterion as written above and does not suggest a different
wording that would make "discharged by decision" read as "discharged by evidence."

---

## Criterion 9 — The trigger investigation

**Roadmap text, verbatim (abridged for length; full text is in `.planning/ROADMAP.md` § Phase 01.3,
item 9):** *"The question 'is a specific call or sequence causing this?' is investigated and
answered, not assumed away... A confirmed trigger changes the deliverable — a call or ordering that
reliably wedges the machine is prevented at the seam, the way `vice_disk_list` is, rather than
recovered from after the fact. Prevention beats recovery, and recovery is the fallback for what
prevention cannot catch."*

**Outcome:** INVESTIGATED, ANSWERED AS "COULD NOT ATTEMPT" — carried forward from `01.3-TRIGGER-HUNT.md` by reference, not restated.

`01.3-TRIGGER-HUNT.md` is the authoritative record and is cited here rather than duplicated: its
Setup section states the six-attempt, three-per-variant budget as a **count**, its Blocking Discovery
section documents why zero attempts were made (`vice_diagnose`/`vice_recycle` unreachable from that
session's own tool surface, confirmed four independent ways, before either variant's first attempt),
and its Verdict section states the denominator explicitly: **0 of 6**, with the reason. That
denominator is this plan's answer to criterion 9 as well — nothing new was attempted or discovered
about the trigger question by this plan, and nothing in `01.3-TRIGGER-HUNT.md` needed correcting.

**No trigger was confirmed**, so criterion 9's own "a confirmed trigger changes the deliverable"
clause does not fire. Consistent with that: `SEAM_HAZARDS` in `vice-proxy.ts` still carries exactly
one entry (the armed-checkpoint hazard from plan 01.3-04) plus the test-only synthetic fixture entry
gated behind `VICE_SEAM_HAZARDS_TEST_FIXTURE` — no second, trigger-specific entry was added, and none
should have been, because nothing was confirmed to close at the seam.

---

## Continuity — Criterion 7 and D-20, confirmed rather than invented

**Criterion 7, verbatim:** *"Recovery bumps the restart epoch, so a deliberate recycle voids a run by
exactly the mechanism a crash already does. No second voiding channel is invented. Every caller that
already compares epochs across a bracket gets correct behaviour for free, and a recycle can never be
mistaken for continuity."*

**Outcome:** CONFIRMED IN WRITING. No second continuity story was invented.

A `vice_recycle` call kills the target's emulator child via an identity-verified signal
(`handleRecycle()`, `vice-proxy.ts`). What happens next, in the current (post-01.6) topology, is the
exit-driven respawn step: `handleExit()` in `broker-launch.mts` (the function absorbing what
`vice-supervisor.sh`'s daemon loop used to do — see its own header comment, "absorbs
resources/vice-supervisor.sh WHOLESALE"). For a broker-ordered death whose instance record carries
`deliberateKill` **and** `respawnAfterKill` set, `handleExit()` resolves the outcome as `"recycled"`:
it relaunches on the **same port** through `launchSupervised()`, bypassing the ordinary crash-loop
accounting (no crash timestamp appended, no give-up evaluation, no backoff) because — in the
function's own words — "a deliberate recycle is not evidence of instability." The relaunch writes a
fresh epoch record via `broker-epoch.mts`'s `nextEpochFor()`/`writeEpochRecord()`, the same primitives
an ordinary crash-and-respawn uses.

**This is the identical identity change a crash already produces.** A deliberate recycle and an
unexplained crash both end at the same place: the child process is gone, a new one is running on the
same port, and the epoch file names a new value. `handleExit()` does not have two code paths for
"epoch bump because deliberate" versus "epoch bump because crash" — the epoch-writing primitives are
shared, and the branch that differs (`"recycled"` vs `"respawned"`) governs only whether the
crash-accounting bookkeeping runs, not whether the epoch moves. **No second voiding channel was
invented.** Plan 01-04's existing rule — resume from the last recorded milestone snapshot after an
identity change — therefore already covers a deliberate recycle with no new rule required, and **every
caller that already compares epochs across a bracket gets correct behaviour for free**: the epoch
comparison a caller runs today does not know or care whether the identity change behind it was a
crash or a deliberate recycle, and it does not need to.

**The one wrinkle the implementation had to handle, recorded rather than smoothed over:**
`rebaselineEpochAfterRecycle()` (`vice-proxy.ts:559-562`) clears the proxy's own cached
`viceSession`/`epochBaseline` immediately after a *confirmed* recycle. Without this, the very next
forwarded call would read the proxy's stale pre-recycle baseline, see the epoch it just deliberately
caused, and raise a false-positive drift error against its own action — the recycle tool would trip
the guard it exists to make correct use of. This mirrors `ensureBrokerLease()`'s own
`viceSession = null` re-baseline elsewhere in the same file (the pattern already existed for the
ordinary "lease was lost and re-acquired" case); `vice_recycle` reuses it rather than inventing a
second one.

---

## Criterion 10 — The guard, re-proven

**Roadmap text, verbatim:** *"`vice_disk_list` stays unreachable, and the guard is re-proven rather
than trusted. Verified 2026-08-02: absent from the 63-tool committed manifest, stripped from
`tools/list` discovery, refused at `tools/call` before any network request, re-filtered proxy-side
even from a manifest that names it, with 119/119 tests passing. Any tool this phase adds inherits the
same treatment, and any *new* call found to wedge the machine under criterion 9 joins the same
deny-list by the same mechanism."*

**Outcome:** RE-PROVEN, current count recorded, supersedes the roadmap's stale citation. **The
mechanism description below also needed correcting** — an earlier draft's "four-layer guard" no
longer matches the current source: two of the four described layers are functions that were retired,
and one of the surviving two was mischaracterised as filtering something it does not filter.

The roadmap criterion's own "119/119" figure was already stale by 2026-08-02 (the phase's own
`01.3-VALIDATION.md` corrects it to 200/200 as of that date) and is stale again now: the current full
suite, run against today's TypeScript-converted codebase (`node --test .claude/mcp/vice/*.test.ts`,
from the worktree root), is **450 tests / 445 pass / 0 fail / 5 todo** on `main`, confirmed by running
it during this correction pass. Per this plan's own planning note, correcting the roadmap's prose is
not this plan's job; the current count is recorded here instead of the placeholder an earlier draft
left in this spot, and supersedes the roadmap's citation without editing the roadmap.

**Both tools this phase adds inherit the same treatment.** `vice_diagnose` and `vice_recycle` are
absent from `tools-manifest.json` (confirmed by direct grep: 0 matches for either name against the
committed 63-entry manifest) exactly like `vice_result_continue` before them — they are dispatched
proxy-locally in `vice-proxy.ts`. **Corrected ordering:** an earlier draft said this dispatch happens
"before the `DENY_LIST` branch is ever reached" — read against the current `CallToolRequestSchema`
override, that is backwards. The `DENY_LIST` check is the literal first statement of that handler,
before any tool lookup; `vice_diagnose`/`vice_recycle` reach their own dispatch by passing through
that check (neither name is on `DENY_LIST`), not by preceding it. Being proxy-local and
manifest-absent is not the same enforcement mechanism `vice_disk_list` uses (which is a deny-list
entry, actively refused), and it does not need to be: `vice_disk_list`'s hazard is that it must never
be reachable at all, while `vice_diagnose`/`vice_recycle` are meant to be reachable — the "same
treatment" this criterion asks for is structural safety from an unintentional manifest regeneration
exposing or hiding either tool, which both already have by construction.

**The guard, current state — corrected from a stale four-layer description.** `DENY_LIST` (`vice.ts`)
now names **five** entries, not one: `vice_disk_list` plus four generic-surface meta-tool names
(`tools_list`, `tools_call`, `initialize`, `notifications_initialized`) added by Phase 01.4 plan 01 to
close the confused-deputy bypass those names permitted. An earlier draft described "the same four
layers `vice_disk_list` alone used at this phase's start" — (1) `refresh-manifest.ts` stripping,
(2) a `vice.ts` discovery-time filter "re-applying `DENY_LIST` to whatever the manifest contains,"
(3) `vice.ts`'s `call()` guard, (4) `vice-proxy.ts`'s own `handleToolsList()`/`handleToolsCall()`.
Checked directly against today's source, that description does not hold:

- **`handleToolsList()` and `handleToolsCall()` no longer exist.** Phase 01.6.3 replaced the
  hand-rolled dispatch both used to do: `tools/list` is now answered entirely by `@mastra/mcp`'s own
  unmodified `ListToolsRequestSchema` handler (the code's own comment calls this "the one piece of
  genuine library value this swap adopts"), reading from an in-memory `tools` object built once at
  startup; `tools/call` is answered by a `CallToolRequestSchema` override that replaced
  `handleToolsCall()`.
- **The described "layer 2" filters the host's live RPC response, not the manifest.** `vice.ts`'s
  discovery-time filter is `serverInfo()`, and it filters whatever `rpc("tools/list", {})` returns
  from the host at call time. `refresh-manifest.ts` calls this same function to build the committed
  manifest — so this is not an independent layer sitting behind a separately-filtered manifest, it is
  the one mechanism both the manifest-refresh path and any ad-hoc discovery call share.
- **The read-time filter was deliberately removed, not renamed.** Enforcement moved to construction
  time: `if (DENY_LIST.includes(def.name)) continue;` when the in-memory `tools` object is built from
  `readManifestTools()` at proxy startup, with the code's own comment noting this is "construction-time,
  not read-time... a manifest hot-reload mid-session is therefore no longer picked up until the proxy
  restarts."
- **`tools-manifest.json` does not omit four of the five names.** Direct check of the committed
  63-entry manifest: `vice_disk_list` is absent, but `tools_list`, `tools_call`, `initialize` and
  `notifications_initialized` are all still **present** in the file. The safety property still holds —
  the construction-time skip above filters by `DENY_LIST` membership as it reads the manifest,
  independent of whether the manifest file itself was pre-filtered — but the claim that "layer 1"
  (manifest stripping) covers all five names is false for four of them.

**What is actually true, re-checked directly against today's source rather than trusted from the
retired description:** two independent checks stand between an incoming call and any of the five
denied names — (1) the `CallToolRequestSchema` override's `DENY_LIST` refusal, the literal first
statement of the handler, before any tool lookup or network attempt; (2) the construction-time skip
that keeps all five names out of the in-memory `tools` object `tools/list` is served from, regardless
of what the manifest file itself contains. `vice_disk_list` additionally never appears in the manifest
file (via `refresh-manifest.ts`'s own use of `serverInfo()`), but that third property is specific to
`vice_disk_list` today and not shared by the other four names — a gap worth closing, not one this
correction papers over.

This phase (01.3) added no new `DENY_LIST` entries — no trigger was confirmed under criterion 9 — so
this section records the guard's current shape rather than a change this phase made to it.

---

## What remains unproven (D-07)

Stated plainly, with no claim beyond the evidence and no hedging that obscures a clean result:

- **The detector half (`vice_diagnose`)** met a real healthy instance (this plan's own criterion 3
  attempts, plus 01.4-01/01.4-04's live evidence) and a synthetic non-advancing stand-in
  (`vice-proxy.test.ts`'s wedge fixtures, exercised in CI on every run). **It has never been proven
  against a real, non-advancing emulator.** The `wedged` verdict's classification logic is proven
  deterministically against the fixture; it has not been proven to fire correctly against the real
  failure state it exists to catch, because that state has never been produced on demand.

- **The recycle half (`vice_recycle`)** met a real, healthy instance **twice** — not three times as an
  earlier draft of this section claimed. This phase's own tracer (plan 01.3-01) proved the mechanism
  against a **fabricated stub "x64sc" process**, in the tracer's own SUMMARY's words: "against a
  fabricated stub 'x64sc' process." A stub is not a real instance, so it does not add to this tally —
  it proves the mechanism's plumbing (identity-verified kill, host dispatch, respawn, epoch bump)
  without proving anything about a real emulator process. The two real instances are
  `20260803171235702-port6510-epoch13` and `20260805160045723-port6603-epoch1`. **Corrected
  attribution:** an earlier draft called both "01.4's two live proofs" — only the second is Phase
  01.4's (plan 04's criterion-1 verification); the first is **Phase 01.6.1**'s UAT test 2, verifying
  the TypeScript-converted proxy's recycle path against a real emulator. Both proved the
  kill/respawn/epoch-bump mechanism end to end against a machine that was never actually stuck. **It
  has never been exercised against a real wedge**, for the same reason: none has been produced on
  demand.

- **Neither half has met a real wedge.** This is the honest D-07 position for this phase, not a gap
  to apologise for: the subject occurred five times unbidden before this phase existed, and a
  six-attempt trigger hunt was budgeted to seek it — but never got underway. **Corrected:** an earlier
  draft said the subject "has now been actively sought on six further bounded attempts (the trigger
  hunt) without reproducing once." `01.3-TRIGGER-HUNT.md`'s own verdict says otherwise: "Denominator: 0
  of 6 attempts... The budget is entirely unspent," blocked before Variant A's first attempt when
  `vice_diagnose`/`vice_recycle` proved unreachable from that session's tool surface. What is proven is
  that the tools work correctly on every case that has been produced — healthy instances, and a
  synthetic wedge stand-in. What is not proven, and cannot be proven without a reproduction this
  project has not achieved, is that they work identically against the real thing.
