---
phase: 01.3
slug: wedge-detection-and-recovery
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-02
---

# Phase 01.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `01.3-RESEARCH.md` § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node's built-in test runner (`node:test`) — no external test package |
| **Config file** | none — invoked directly per file |
| **Quick run command** | `node --test .claude/mcp/vice/vice-proxy.test.mjs .claude/mcp/vice/vice-broker.test.mjs` |
| **Full suite command** | `node --test .claude/mcp/vice/*.test.mjs` |
| **Estimated runtime** | to be measured at Wave 0; full suite is **200/200 green today** (live-verified 2026-08-02) |

> **Criterion 10 citation correction.** ROADMAP.md criterion 10 cites "119/119 tests passing". The
> suite is **200/200** as of 2026-08-02 (live-verified during research). Re-proving the
> `vice_disk_list` guard means re-running the current suite, not matching the stale count.

---

## Sampling Rate

- **After every task commit:** `node --test .claude/mcp/vice/vice-proxy.test.mjs .claude/mcp/vice/vice-broker.test.mjs`
- **After every plan wave:** `node --test .claude/mcp/vice/*.test.mjs`
- **Before `/gsd-verify-work`:** Full suite must be green, **plus** the human-verify checkpoint for
  criteria 3 / 8 / 9 / 11 — host-side behaviour is not self-verifiable from the container, exactly as
  Phase 01.1 and 01.2 both required ("verification asymmetry, again", ROADMAP § Phase 01.3 Risks).
- **Max feedback latency:** target < 30 seconds for the quick command

---

## Per-Task Verification Map

Task IDs are assigned when PLAN.md files are written; this table is seeded at the **success-criterion**
level from research and is refined to per-task rows by `/gsd-validate-phase`.

| Criterion | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|-----------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 1 — four-state classification never trusts `vice_ping.execution` | TBD | TBD | none (tooling phase) | — | N/A | unit | `node --test .claude/mcp/vice/vice-proxy.test.mjs` (new `diagnose` fixtures; synthetic non-advancing-cycles stand-in per D-07) | ❌ W0 | ⬜ pending |
| 2 — cycle bracket defined in exactly one place, no wall-clock delay | TBD | TBD | none | — | N/A | unit + structural | structural "exactly one definition" test, mirroring `maintain_spares has exactly one definition` in `vice-broker.test.mjs` | ❌ W0 | ⬜ pending |
| 3 — partial staleness classified, not treated as a wedge | TBD | TBD | none | — | N/A | manual/live | N/A — human-verify checkpoint | N/A | ⬜ pending |
| 4 — evidence captured before anything is killed | TBD | TBD | none | T-01.3-03 | Recycle result reports the real SIGTERM→poll→SIGKILL outcome, not "requested" | unit | call-order spy asserting the incident-record write precedes the recycle request | ❌ W0 | ⬜ pending |
| 5 — every privileged action via `mcp__vice__*` only | TBD | TBD | none | — | No module opens its own transport/host connection | structural | directory-enumerating test, matching `skill-docs.test.mjs`'s pattern | ❌ W0 | ⬜ pending |
| 6 — identity-verified kill against a recorded pid | TBD | TBD | none | T-01.3-01 | `ps -o args=` identity match before any `kill -TERM`; never a bare pid kill | unit | `vice-broker.test.mjs` test that a mismatched-identity pid is refused and logged | ❌ W0 | ⬜ pending |
| 7 — recycle bumps the epoch, no second voiding channel | TBD | TBD | none | — | N/A | integration (dry-run) | `--dry-run` recycle asserting the epoch increments exactly once, via `vice-supervisor.sh --dry-run`'s existing test seam | ❌ W0 | ⬜ pending |
| 8 — host-side detectability answered (decided-not-investigated, D-11/D-12) | TBD | TBD | none | — | N/A | manual/live | N/A — recorded decision + human-verify | N/A | ⬜ pending |
| 9 — trigger investigation reaches a stated answer | TBD | TBD | none | — | N/A | manual/live | N/A — bounded attempt budget, negative-with-denominator is a valid result (D-10) | N/A | ⬜ pending |
| 10 — `vice_disk_list` guard re-proven; new tools inherit it | TBD | TBD | none | — | New tools inherit the four-layer guard treatment | unit | `node --test .claude/mcp/vice/vice-proxy.test.mjs` (existing tests, re-run unmodified) | ✅ | ⬜ pending |
| 11 — recovery exercised against a real non-advancing emulator, or recorded as unproducible | TBD | TBD | none | — | N/A | manual/live | N/A — human-verify checkpoint | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `.claude/mcp/vice/vice-proxy.test.mjs` — new `describe`/`test` blocks for the diagnose tool
      (synthetic non-advancing-cycles stand-in per D-07) and the recycle tool (dry-run path)
- [ ] `.claude/mcp/vice/vice-broker.test.mjs` — new tests for the recycle dispatch branch in
      `process_requests()`, including the identity-mismatch-refusal case
- [ ] No new test framework or fixture harness — `node:test` plus the existing spawned-child-proxy
      harness already used by every `vice-proxy.test.mjs` test covers the shape this phase needs

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Partial staleness is classified rather than recycled (criterion 3) | none (tooling phase) | The subject is a live emulator returning a stale response from one read path while another reports genuine change — not reproducible in a unit fixture | Drive a real instance, induce the 01-04 Task 2 shape (`vice_registers_get` byte-identical while `vice_vicii_get_state` changes), confirm the verdict is not `wedged` |
| Host-side detectability recorded as decided-not-investigated (criterion 8) | none | Discharged by decision (D-11/D-12), not by evidence — there is nothing to automate | Confirm the phase output states: not investigated, boundary at container-side observation, and why |
| Trigger hunt reaches a stated answer (criterion 9) | none | Requires deliberately attempting to wedge a real instance; destructive by nature and needs the recycle path working first (D-05) | Run the armed-stopping-checkpoint family attempts up to the named budget; record "could not reproduce in N attempts" with what was attempted if negative |
| Recovery exercised against a real non-advancing emulator (criterion 11) | none | The wedge has occurred five times unbidden and never once on request | Either recycle a genuinely wedged instance, or record that one could not be produced on demand |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] Human-verify checkpoint recorded for criteria 3 / 8 / 9 / 11
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
