# Continue here — Phase 01.3, waves 5 and 6

**Written:** 2026-08-02, at the end of a session that completed waves 1–4 and reached a blocked,
honestly-recorded negative on wave 5.

## Where the phase stands

| Wave | Plan | State | Evidence |
|------|------|-------|----------|
| 1 | 01.3-01 | ✓ merged | `vice_recycle` proven end-to-end against a fabricated stub x64sc |
| 2 | 01.3-02 | ✓ merged | `vice_diagnose`, five closed verdicts, trap check with zero resumes |
| 3 | 01.3-03 | ✓ merged | `gatherWedgeEvidence()` inside `handleRecycle()` |
| 4 | 01.3-04 | ✓ merged | `SEAM_HAZARDS` table, warns and never refuses |
| 5 | 01.3-05 | **reopened** | attempt 1 ran 0 of 6 — blocked, see below |
| 6 | 01.3-06 | pending | depends on 5; also a blocking human-verify checkpoint |

Test suite on `main`: 268/268.

Wave 5's first attempt is preserved as **`01.3-05-SUMMARY-attempt1-blocked.md`** (renamed from
`01.3-05-SUMMARY.md` so plan discovery reopens the plan). Its substantive record — the 0/6
denominator, the four probes, the experimental-setup decision about `$EA31` — is in
**`01.3-TRIGGER-HUNT.md`**, which is the citable artifact. Nothing was discarded. Re-running wave 5
should *extend* that record with a second attempt, not overwrite it.

## Critical Anti-Patterns

| # | Anti-pattern | severity | Recorded because |
|---|---|---|---|
| 1 | Treating a healthy `vice_ping` as evidence that `vice_diagnose` / `vice_recycle` are reachable | blocking | Wave 5 was dispatched on exactly that inference. `vice_ping` returned `{"status":"ok","version":"3.10","machine":"C64SC"}` and both synthetic tools were still absent from the session's tool surface, costing the whole six-attempt budget. |
| 2 | Using `mcp__vice__tools_list` / `tools_call` to reach a proxy-local synthetic tool | blocking | Both forward to the host and bypass `handleToolsList()`. Verified: `tools_list` returned 64 tools containing **no** synthetic tools at all — not even `vice_result_continue`, which *is* exposed on the client tool surface. The host correctly rejects the synthetic names as "Tool not found". |
| 3 | Restarting the host broker to make newly-added synthetic tools appear | blocking | Done this session; did not help and was never going to. The broker does not hold the tool list. The client's tool surface is a snapshot taken at MCP initialization, which is why `vice_result_continue` (predating the session) was visible while `vice_recycle`/`vice_diagnose` (added mid-session by waves 1–2) were not. |

### Required first action for wave 5

Before arming any checkpoint or spending any attempt, confirm **by name, from this session's own
tool surface**, that both tools are reachable:

```
ToolSearch: select:mcp__vice__vice_diagnose,mcp__vice__vice_recycle
```

Both schemas must load. If either does not, **stop and report** — do not fall back to
`tools_call`, and do not spend attempts without a recovery path (D-05). A fresh session is expected
to fix this, because Claude Code spawns `vice-proxy.mjs` per session, so a new process loads the
current on-disk code and its `handleToolsList()` includes both tools. That expectation is
**unverified** — this session never observed it working.

## Host-side state, already done — do not redo

The deployed broker is current and does not need touching:

- `tools/vice-broker.sh` was force-deployed from `.claude/mcp/vice/resources/vice-broker.sh`
  (86,260 → 98,142 bytes). `handle_recycle_request` ×6, `signal_vice_child_pid` ×3,
  `write_recycle_ack` ×6 all present; `bash -n` clean; exec bit preserved.
- All five entries under `resources/` now report `present` (byte-identical) via
  `resourcesStatus()`. The other four were already identical and were not changed in content.
- Pre-deploy backup of the old script is in the previous session's scratchpad and is **not**
  durable — if a rollback is ever needed, regenerate from git history instead.
- The user restarted the broker afterwards and it serves.

Note for any future re-deploy: `installResources()` copies only `missing` entries by default and
*skips* a `diverged` one. Updating an already-deployed script requires `force: true`. Do not
overwrite `tools/vice-broker.sh` while the broker is running — bash resumes parsing the new bytes
at its old file offset.

## Wave 6 sequencing

01.3-06 must not be written against wave 5's 0/6 if wave 5 is about to be retried. D-12 records
that rewording a criterion to fit the achieved outcome was considered and rejected as editing the
scorecard; writing recorded answers against a denominator that is then superseded is the same
failure in a different direction. Run wave 5 to a real verdict first, then wave 6.

Wave 6 is `autonomous: false` with a **blocking** human-verify gate — it must be presented to the
user, never auto-approved.

## Unrelated item still outstanding

The orphaned worktree `.claude/worktrees/agent-a8ecae48321e87a56`
(branch `worktree-agent-a8ecae48321e87a56`, tip `fc81324`) holds 3 commits that are **not** on
`main`: two `2026-08-02` RE-FINDINGS entries (the resume-round-trip latency hazard, and the
`$EA31` checkpoint-gated sync primitive) plus 9 attempt-6 evidence screenshots under
`recovery/danish/dumps/`. It forked from a base older than the 01.3 planning commits, so it needs a
real merge, not a fast-forward — a naive merge would read as deleting the 01.3 artifacts.

The `$EA31` finding's text was passed into wave 5's prompt and wave 5 re-logged it into
`.planning/RE-FINDINGS.md` on `main`, so that content is no longer orphan-only. The 9 screenshots
still are.
