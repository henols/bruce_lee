---
phase: 01.3
plan: 06
type: execute
wave: 6
depends_on:
  - 01.3-05
files_modified:
  - .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md
  - .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md
  - .planning/RE-FINDINGS.md
autonomous: false
requirements: []
must_haves:
  truths:
    - "The phase states in writing that a recycle bumps the restart epoch, which is the same identity change a crash produces, so the existing resume-from-the-last-recorded-milestone-snapshot rule already applies and no second continuity story was invented (C7, D-20)"
    - "The phase states in writing what remains unproven: which halves met a synthetic stand-in and which met a real machine, with no claim beyond the evidence (D-07)"
    - "A capability coverage delta exists for the two capabilities this phase adds to the surface phase 01.1 authored, and the one permanently refused capability is restated with its reason (C10)"
    - statement: "Partial staleness is confirmed classified rather than treated as a wedge against a live instance, or the phase records that the shape could not be induced on demand and states what therefore rests on the synthetic fixture alone (C3)"
      verification: backstop
    - statement: "Whether a wedge is detectable host-side is answered as decided-not-investigated: the boundary is stated, the reason is stated, and the phase notes that the delivered container-side detection plus recycle is exactly the fallback the criterion itself names for a negative answer (C8, D-11, D-12)"
      verification: backstop
    - statement: "The trigger investigation's stated answer is carried into the phase record with its denominator, and the roadmap criterion is not reworded to fit whatever the plan achieved (C9, D-10)"
      verification: backstop
    - statement: "The recovery path is exercised against an actual non-advancing emulator and the incident record is cited, or the phase records that one could not be produced on demand across a stated number of attempts (C11, D-07)"
      verification: backstop
  artifacts:
    - .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md
    - .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md
  key_links:
    - "01.3-RECORDED-ANSWERS.md cites 01.3-TRIGGER-HUNT.md for criterion 9's denominator and the incident records under .planning/incidents/ for criterion 11's evidence, so no claim in it is unsourced"
    - "COVERAGE.md is a delta against .planning/phases/01.1-tool-mediated-emulator-access/COVERAGE.md, which already enumerates the full authored surface — the two new capabilities are rows, not a new baseline"
---

<objective>
Write down the answers this phase reached by decision rather than by evidence, and the ones it reached
by evidence that fell short — honestly, with their denominators, and without rewording anything to
fit.

Purpose: four of the eleven success criteria cannot be settled by an automated check from inside the
container. Two of those are deliberately answered by decision, one by a bounded negative, and one by a
failure state that has occurred five times unbidden and never once on request. A negative with a
stated denominator is a real result; a criterion quietly reworded to match what was achieved is not.
D-12 already records that rewording criterion 8 was considered and rejected as editing the scorecard.

Output: the phase's recorded answers, a capability coverage delta for the two tools it adds, and the
human-verify checkpoint the phase's verification asymmetry has required in 01.1 and 01.2 and requires
again here.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.3-wedge-detection-and-recovery/01.3-CONTEXT.md
@.planning/phases/01.3-wedge-detection-and-recovery/01.3-VALIDATION.md
@.planning/phases/01.3-wedge-detection-and-recovery/01.3-TRIGGER-HUNT.md
@.planning/phases/01.3-wedge-detection-and-recovery/01.3-05-SUMMARY.md
@.planning/phases/01.1-tool-mediated-emulator-access/COVERAGE.md
</context>

## Artifacts this phase produces

Every symbol below is created by phase 01.3. None of them exists in the codebase before this phase,
so a drift check that flags any of them as an unresolved reference is reading them correctly as new.

**New MCP tool names** — proxy-local, never written into `tools-manifest.json`: `vice_recycle`
(plan 01.3-01), `vice_diagnose` (plan 01.3-02).

**New `vice-proxy.mjs` symbols:** `RECYCLE_TOOL`, `DIAGNOSE_TOOL`, `handleRecycle()`,
`handleDiagnose()`, `runCycleBracket()`, `resolveLiveIrqHandler()`, `gatherWedgeEvidence()`,
`classifyLiveness()`, `renderCheckpointTrapReport()`, `renderDiagnoseReport()`,
`annotateCheckpointHazard()`, `SEAM_HAZARDS`, `CHECKPOINT_ARMING_TOOLS`, `DIAGNOSE_VERDICTS`,
`CYCLE_BRACKET_PINGS`, `CYCLE_BRACKET_MAX`, `BASELINE_CYCLES_PER_SECOND`,
`rebaselineEpochAfterRecycle()`.

**New module `.claude/mcp/vice/incident-record.mjs`** (plus `incident-record.test.mjs`):
`INCIDENT_RECORD_VERSION`, `incidentsDir()`, `incidentRecordPath()`, `renderIncidentRecord()`,
`writeIncidentRecord()`, `finaliseIncidentRecord()`.

**New `vice-broker-client.mjs` exports:** `recycleAcksDir()`, `writeRecycleRequest()`,
`pollRecycleAck()`, `RECYCLE_ACK_TIMEOUT_MS`, `RECYCLE_ACK_POLL_INTERVAL_MS`.

**New `resources/vice-broker.sh` globals and functions:** `RECYCLE_ACKS_DIR`, `extract_op_field()`,
`extract_target_id_field()`, `read_grant_field()`, `read_epoch_field()`, `signal_vice_child_pid()`,
`write_recycle_ack()`, `handle_recycle_request()`.

**New request `op` verb:** `"recycle"`.

**New protocol files under `.vice-supervisor/`:** `requests/<id>.json` carrying `op` and `target_id`;
`recycle-acks/<id>.json` carrying `version`, `id`, `target_id`, `port`, `x64sc_pid`, `vice_bin`,
`kill_stage`, `epoch_before`, `outcome`, `reason`, `acked_at`.

**New repo-tracked paths:** `.planning/incidents/` and its `README.md`; records named
`<UTC-compact>-port<N>-epoch<M>.md` with a sibling screenshot; `01.3-TRIGGER-HUNT.md`;
`01.3-RECORDED-ANSWERS.md` (this plan); `COVERAGE.md` (this plan). Append-only entries are added to
the existing `.planning/RE-FINDINGS.md`.

**New test names introduced by this plan:** none — this plan writes records and runs the phase gate.

## Planning notes carried into execution

**Spec-less probe fallback: SKIPPED, visibly** — no requirement IDs and no phase SPEC, so no probe
predicates were generated. Truths derive from the eleven ROADMAP criteria and D-01..D-22.

**Do not reword a criterion to fit what was achieved.** D-12 records that this was considered for
criterion 8 and rejected as editing the scorecard. The same rule applies to criteria 3, 9 and 11: the
record says what was and was not established, and the criterion text stays as written.

**Criterion 10's citation is stale in the roadmap and stays stale there.** The suite was 200/200 green
on 2026-08-02, not the 119/119 the criterion cites. Re-proving the guard means re-running the
*current* suite and recording the current number in the phase record. Correcting the roadmap's own
prose is not this plan's job and inventing a correction inside a success criterion would be the same
scorecard edit D-12 rejected.

**Known pre-existing flake:** the `vice-pool.test.mjs` two-port acquire test reds roughly 1 run in 5,
recorded OPEN in `01.1-deferred-items.md`. A red there is the known flake; re-run and record both
outcomes in the phase gate rather than counting it against the phase.

<tasks>

<task type="auto">
  <name>Task 1: The two live confirmations, attempted and recorded either way</name>
  <files>.planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md, .planning/RE-FINDINGS.md</files>
  <precondition>The host broker is running and this session can be granted an emulator instance: a `vice_ping` call returns rather than reporting one of the three broker-absent states. If it reports a broker-absent state, record that verbatim as the outcome for both confirmations and stop — do not attempt to start anything host-side.</precondition>
  <read_first>
    - `.planning/phases/01.3-wedge-detection-and-recovery/01.3-TRIGGER-HUNT.md` — the six attempts, their outcomes, and whether any produced a genuinely non-advancing instance that was recycled.
    - `.planning/phases/01.3-wedge-detection-and-recovery/01.3-VALIDATION.md` § "Manual-Only Verifications" — the four rows, and specifically the test instructions given for criteria 3 and 11.
    - `.planning/todos/pending/2026-08-01-vice-registers-frozen-after-reset-during-01-04-task2.md` — the partial-staleness shape criterion 3 names: a register read returning a byte-for-byte identical snapshot while the video-chip state read reported genuine post-hard-reset change.
    - `.planning/phases/01.3-wedge-detection-and-recovery/01.3-02-SUMMARY.md` — the verdict vocabulary and which fixture proves the stale-read-path classification synthetically.
    - `/workspaces/bruce_lee/.claude/CLAUDE.md` § "Emulator Access" and § "Reverse-Engineering Findings Log".
    - ROADMAP § Phase 01.3 criteria 3 and 11 verbatim.
  </read_first>
  <action>
    Create `.planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md` with a section
    per criterion this plan answers, and fill the two that need a live instance.

    **Criterion 3 — partial staleness.** Attempt to induce the recorded shape on a live instance: a
    register read returning a byte-for-byte identical snapshot while the video-chip state read reports
    genuine change. The budget is **two attempts**, a count and not a duration, for the same reason the
    trigger hunt's budget is a count. On each attempt, call `vice_diagnose` and record the verdict
    verbatim; the criterion is met when the verdict is anything other than the wedged one and the
    report names the stale read. If the shape cannot be induced, record that plainly with what was
    tried, and state exactly what then rests on the synthetic fixture alone — which is the honest
    position D-07 sets for the detector half, not a gap to apologise for.

    **Criterion 11 — recovery against a real non-advancing emulator.** If the trigger hunt already
    produced a genuinely non-advancing instance and recycled it, the criterion is met: record it, cite
    the incident record path and the trigger-hunt attempt number, and quote the recycle tool's own
    reported kill stage and epoch pair. If the hunt produced none, record that the failure state could
    not be produced on demand — noting that it has occurred five times unbidden and never once on
    request — state the denominator from the hunt, and state precisely what therefore remains
    unproven: that the recovery path has been exercised against a healthy instance and against a
    stand-in, but never against the state it exists for.

    Do not soften either outcome and do not reword either criterion. Both sections state the evidence,
    its provenance, and its limits.

    Log to `.planning/RE-FINDINGS.md`, at the moment of discovery, anything either attempt turned up
    that would make a future session faster — a way to induce the stale-read shape, a read that lied, a
    dead end with its reason. One entry per finding, dated, with separate Evidence and Confidence
    fields. If neither attempt produced a finding worth logging, add nothing and say so in the SUMMARY.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md && grep -q '^## Criterion 3' .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md && grep -q '^## Criterion 11' .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md && grep -qE '^\*\*Outcome:\*\*' .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md && echo RECORDED-ANSWERS-LIVE-OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `RECORDED-ANSWERS-LIVE-OK`.
    - The criterion 3 section carries an `**Outcome:**` line, states the two-attempt budget as a count, and either quotes a live verdict verbatim from the tool's own vocabulary or states plainly that the shape could not be induced and what rests on the synthetic fixture alone.
    - The criterion 11 section carries an `**Outcome:**` line and either cites an existing file under `.planning/incidents/` together with the reported kill stage and both epoch values, or states the denominator from the trigger hunt and names what remains unproven.
    - Neither section reproduces a reworded version of the roadmap criterion; each quotes the criterion as written before answering it.
    - Any entry added to `.planning/RE-FINDINGS.md` by this task carries both an `Evidence:` line and a `Confidence:` line, and `git diff -- .planning/RE-FINDINGS.md | grep -c '^-Confidence:'` returns 0.
    - `node --test .claude/mcp/vice/*.test.mjs` reports no failures beyond the known two-port flake.
  </acceptance_criteria>
  <done>Both live-only criteria have an outcome on record with its evidence or its stated absence, neither criterion has been reworded, and anything learned is in the findings log with its grade.</done>
</task>

<task type="auto">
  <name>Task 2: The answers reached by decision, the continuity confirmation, and the coverage delta</name>
  <files>.planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md, .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md</files>
  <read_first>
    - `.planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md` as task 1 left it.
    - `.planning/phases/01.1-tool-mediated-emulator-access/COVERAGE.md` — the full authored-surface matrix, its counts, and the section explaining how the one refused capability is enforced. This plan's file is a delta against it, not a replacement.
    - CONTEXT.md D-07, D-11, D-12, D-20 verbatim, and § Specific Ideas' "a negative with a stated denominator is a real result".
    - `.claude/mcp/vice/resources/vice-supervisor.sh` L377-443 — the respawn loop and the epoch increment, the concrete code path the continuity confirmation cites.
    - `.planning/phases/01.3-wedge-detection-and-recovery/01.3-01-SUMMARY.md` — the epoch pair the recycle tool reports and the re-baselining it performs, both of which the continuity section must describe accurately.
    - ROADMAP § Phase 01.3 criteria 7, 8, 9 and 10 verbatim.
  </read_first>
  <action>
    Complete `01.3-RECORDED-ANSWERS.md` with the sections that need no live instance, and write the
    coverage delta.

    **Criterion 8 — host-side detectability.** Record it as **discharged by decision, and say so**.
    State three things and nothing more: it was not investigated; the boundary was drawn at
    container-side observation, because anything answerable through the tool surface is in scope and the
    moment a question needs host process internals it leaves the only permitted route; and the
    hypothesis on record — a hung graphics event loop — stays on record, untested, so the investigation
    remains possible later without being attempted now. Add the observation that the delivered
    container-side detection plus unconditional recycle **is exactly the fallback the criterion itself
    names for a negative answer**, so the criterion is answered rather than dodged. Note explicitly that
    rewording the criterion to fit the plan was considered and rejected.

    **Criterion 9 — the trigger investigation.** Carry the trigger hunt's verdict and denominator into
    this file by reference, citing the document rather than restating its contents, and state whether a
    confirmed trigger was closed at the seam.

    **Criterion 7 and D-20 — continuity, confirmed rather than invented.** State in writing that a
    recycle kills the emulator child and the supervisor's existing respawn loop brings a new one up on
    the same port with an incremented epoch; that this is the identical identity change a crash already
    produces, so no second voiding channel exists; that the existing rule — resume from the last
    recorded milestone snapshot after an identity change — therefore already covers a deliberate recycle
    with no new rule required; and that every caller which already compares epochs across a bracket gets
    correct behaviour for free. Cite the supervisor's respawn loop by file and the recycle tool's
    reported epoch pair as the evidence. Also record the one wrinkle the implementation had to handle:
    the proxy re-baselines its own epoch after a confirmed recycle, because without that the deliberate
    identity change would make every subsequent forwarded call fail the drift guard.

    **Criterion 10 — the guard, re-proven.** Record the current full-suite pass count from the phase
    gate, note that it supersedes the roadmap criterion's stale citation, and name the four layers the
    refused capability is held behind plus the fact that both new tools are proxy-local and therefore
    outside the manifest a regenerate could rewrite.

    **What remains unproven (D-07).** One section, stated plainly: which half of the phase met a
    synthetic stand-in, which met a real healthy instance, and which — if any — met a real wedge. No
    claim beyond the evidence, and no hedging that obscures a clean result either.

    **`COVERAGE.md`.** This phase integrates no external API, SDK or service: it extends an in-repo MCP
    proxy and the host broker that serves it. But phase 01.1 *authors* the tool surface a client sees,
    and it recorded that surface as a real matrix — so the honest artifact here is a **delta against
    that matrix**, not a fresh baseline and not a bare declaration. Open with one line stating there is
    no external API integration and naming what the phase touches instead. Then a table with a row per
    capability this phase changes: the diagnose capability as INTEGRATE, the recycle capability as
    INTEGRATE, each with a reason naming that it is proxy-local with no host counterpart, and the one
    permanently refused capability restated as OPT-OUT with its existing reason, because criterion 10
    says any tool this phase adds inherits the same treatment. Close with the new client-visible
    capability count and a pointer to 01.1's file as the full baseline.
  </action>
  <verify>
    <automated>test -f .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md && grep -q 'vice_diagnose' .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md && grep -q 'vice_recycle' .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md && grep -q 'OPT-OUT' .planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md && grep -q '^## Criterion 8' .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md && grep -q '^## What remains unproven' .planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md && echo RECORDED-ANSWERS-DECIDED-OK</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `RECORDED-ANSWERS-DECIDED-OK`.
    - The criterion 8 section states all three of: not investigated, the boundary and its reason, and that the untested hypothesis stays on record. It also states that the delivered deliverable is the fallback the criterion itself names, and that rewording the criterion was considered and rejected.
    - The criterion 9 section cites `01.3-TRIGGER-HUNT.md` by path and names its denominator, without restating the attempt log.
    - The continuity section names the supervisor respawn loop by file path, states that no second voiding channel was invented, and describes the proxy's post-recycle re-baselining and why it is needed.
    - The criterion 10 section records the current full-suite pass count taken from this plan's own gate run and states that it supersedes the roadmap's cited figure.
    - `COVERAGE.md` opens with a one-line statement that no external API is integrated, carries one row per changed capability with a decision, and every OPT-OUT row carries a reason. The gate accepts rows with valid decisions; a malformed or partial matrix blocks the seal, so this file must validate.
    - `COVERAGE.md` links to `.planning/phases/01.1-tool-mediated-emulator-access/COVERAGE.md` as the full baseline it is a delta against.
    - `node --test .claude/mcp/vice/*.test.mjs` reports no failures beyond the known two-port flake, and the exact pass count is recorded in both the SUMMARY and the criterion 10 section.
  </acceptance_criteria>
  <done>Every criterion this phase answers by decision rather than by evidence says so, in writing, with its reason and its boundary; continuity is confirmed against the existing rule rather than paralleled by a new one; and the capability delta validates against the seal-time gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirm the four records claim no more than they establish</name>
  <what-built>
    Phase 01.3 is complete pending your confirmation of the four criteria that cannot be settled by an
    automated check from inside the container.

    Two new MCP tools are live on the `mcp__vice__*` surface. `vice_diagnose` is read-mostly: it
    checks the restart epoch first at no cost, then enumerates armed checkpoints and resolves the live
    IRQ handler before resuming anything, and only then runs the cycle bracket — reset, resume once,
    poll, read back — returning one of five verdicts with the evidence behind it, split into
    load-bearing and corroborating. `vice_recycle` is destructive and separate: it captures a full
    evidence set, writes a repo-tracked incident record under `.planning/incidents/`, attempts a
    best-effort snapshot, and only then asks the host broker to signal the emulator's child process by
    identity-verified kill, letting the existing supervisor respawn loop bring a new instance up on the
    same port with an incremented epoch.

    A hazard annotation now rides back on any call that arms a stopping exec checkpoint — the shape
    common to all three recorded freezes — warning without ever refusing, because that technique is
    what Phase 2's exhaustive trace runs on.

    The trigger hunt ran to its stated six-attempt budget and its outcome is in
    `01.3-TRIGGER-HUNT.md`. The answers this phase reached by decision rather than evidence are in
    `01.3-RECORDED-ANSWERS.md`.
  </what-built>
  <how-to-verify>
    Read `.planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md` and
    `.planning/phases/01.3-wedge-detection-and-recovery/01.3-TRIGGER-HUNT.md`, then confirm each of the
    four in turn.

    **Criterion 3 — partial staleness is classified, not treated as a wedge.** Read the criterion 3
    section. Confirm it either quotes a live verdict that is not the wedged one for an instance showing
    the stale-read shape, or states plainly that the shape could not be induced and names exactly what
    then rests on the synthetic fixture alone. Either answer is acceptable; an answer that blurs the
    two is not.

    **Criterion 8 — host-side detectability.** Read the criterion 8 section. Confirm it says: not
    investigated; here is the boundary; here is why; the hypothesis stays on record untested. Confirm
    it does not claim evidence it does not have, and confirm the roadmap criterion's own text was not
    reworded to fit.

    **Criterion 9 — the trigger investigation.** Read `01.3-TRIGGER-HUNT.md` end to end. Confirm the
    budget is stated as a count of six rather than a duration, that every attempt made has its own
    section naming what was armed and what the diagnostic returned, and that the verdict section states
    the denominator. If a trigger was confirmed, confirm it was closed at the seam in this phase rather
    than deferred, by checking that the hazard table gained an entry and a test.

    **Criterion 11 — recovery against a real non-advancing emulator.** Read the criterion 11 section.
    If a real wedge was met, open the cited file under `.planning/incidents/` and confirm it carries the
    evidence set and the kill outcome. If none was met, confirm the section says so with the
    denominator and names what remains unproven.

    Optionally, exercise the tools yourself in a fresh session: call `vice_diagnose` against a healthy
    instance and confirm the report is understandable without reading any source; arm a stopping exec
    checkpoint and confirm the hazard annotation appears and the call still succeeded; then call
    `vice_recycle` with a reason and confirm a record appears under `.planning/incidents/` and the tool
    reports both epoch values and the kill stage.
  </how-to-verify>
  <resume-signal>Type "approved" to seal the phase, or name which of the four records is not honest about what it establishes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase record → future readers | Claims written here are what later phases will act on without re-deriving them |
| criterion text → phase record | The scorecard must not be edited to match what the plan achieved |
| coverage matrix → seal-time gate | A malformed or partial matrix blocks the seal; a reasoned decision per row passes it |

## STRIDE Threat Register

ASVS level 1; block on `high`.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.3-18 | Repudiation | an optimistically graded or unsourced claim | medium | mitigate | Every criterion section quotes the criterion as written before answering it, cites its evidence by path, and carries an explicit outcome line; the what-remains-unproven section states the limits in one place |
| T-01.3-20 | Repudiation | a criterion reworded to fit the outcome | high | mitigate | D-12 already recorded this as considered and rejected for criterion 8; the same rule is stated for 3, 9 and 11, and the human-verify checkpoint asks the reader to confirm the criterion text was not rewritten |
| T-01.3-21 | Tampering | a coverage delta that silently drops a prior opt-out | medium | mitigate | The one permanently refused capability is restated with its reason rather than inherited by omission, because criterion 10 requires any tool this phase adds to inherit the same treatment |
| T-01.3-04 | Denial of Service (self-inflicted) | recycle target pid selection | critical | mitigate | Carried from plan 01.3-01, unchanged. This plan adds no code |
| T-01.3-SC | Tampering | package-manager installs | high | accept | Zero npm, pip or cargo installs in this phase |
</threat_model>

<verification>
- `.planning/phases/01.3-wedge-detection-and-recovery/01.3-RECORDED-ANSWERS.md` carries a section per
  criterion this plan answers, each with an outcome line and its evidence cited by path.
- `.planning/phases/01.3-wedge-detection-and-recovery/COVERAGE.md` validates: every row has a decision
  and every opt-out a reason.
- `node --test .claude/mcp/vice/*.test.mjs` reports no failures beyond the known two-port flake, and
  the pass count is recorded against the 200/200 baseline of 2026-08-02.
- The human-verify checkpoint is answered.
</verification>

<success_criteria>
The four criteria that no container-side check can settle have honest written answers with their
evidence or their stated absence, continuity is confirmed against the rule that already existed, the
capability delta validates, and a human has read the records and agreed they claim no more than they
establish.
</success_criteria>

<output>
Create `.planning/phases/01.3-wedge-detection-and-recovery/01.3-06-SUMMARY.md` when done
</output>