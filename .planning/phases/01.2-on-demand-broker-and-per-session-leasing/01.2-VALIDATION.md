---
phase: 01.2
slug: on-demand-broker-and-per-session-leasing
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-31
---

# Phase 01.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase 1.2` from `01.2-RESEARCH.md` § Validation Architecture.
> Task IDs are filled in by `/gsd-validate-phase` once plans exist; this draft is keyed to
> ROADMAP success criteria because this phase carries **no REQ-IDs by design**.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node's built-in `node:test` — no `package.json`, no third-party runner anywhere in this repo `[VERIFIED: in-container]` |
| **Config file** | none — invoked directly via `node --test <glob>` |
| **Quick run command** | `node --test .claude/mcp/vice/vice-proxy.test.mjs` |
| **Full suite command** | `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'` |
| **Estimated runtime** | ~60 seconds full suite (168 tests at 01.1 close; spawns real subprocesses and sockets) |

---

## Sampling Rate

- **After every task commit:** Run `node --test .claude/mcp/vice/vice-proxy.test.mjs` — plus the new broker test file once it exists
- **After every plan wave:** Run `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'`
- **Before `/gsd-verify-work`:** Full suite green **and** criterion 13's blocking human-verify checkpoint passed
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

Keyed to ROADMAP's 13 success criteria rather than REQ-IDs — this phase carries none by design
(see ROADMAP § Requirement Coverage). Task IDs are assigned by `/gsd-validate-phase` after plans exist.

| Task ID | Plan | Wave | Criterion | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | C1 — broker starts, mirrors `vice-pool.sh start [N]`, inherits container guard | — | Broker refuses to run in-container rather than half-starting | integration (real subprocess) | `node --test` against new broker test file (`--check-container` / `--print-paths` / `--dry-run`) | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C2 — protocol writes are tmp+rename, never in place | — | A poller can never read a half-written grant | unit | source assertion: no direct `>` redirect into a live protocol path; exercise via `--once --dry-run` | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C3 — `initialize`/`tools/list` never request an emulator | — | A session that never touches VICE launches nothing | unit (extend existing) | `node --test .claude/mcp/vice/vice-proxy.test.mjs` | ✅ existing, extend | ⬜ pending |
| TBD | TBD | TBD | C4 — release wired to SIGINT/SIGTERM **and** stdin end/close, one test per ending | — | No class of session ending leaks a lease | unit | spawn the real proxy, send each signal / close stdin, assert lease file removed | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C5 — shutdown handler does exactly one synchronous fs op | — | Release lands inside the measured ~490ms window | unit + static assertion | grep-based source assertion (mirrors the existing "`process.exit(` occurs exactly once" convention) plus a timing assertion | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C6 — lease existence/mtime/removal is the whole contract | — | Release is a delete, never a write | unit | direct fs assertions against a temp `leases/` dir | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C7 — exactly one teardown path (graceful == swept) | — | The rarely-exercised path *is* the common path | integration | drive missing-lease and stale-lease through `--once`, assert identical resulting state | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C8 — a released instance is killed, never recycled into the spare set | — | No cross-session state contamination | integration | assert a released port's supervisor pid differs from any subsequent spare's | ❌ W0 | ⬜ pending |
| TBD | TBD | TBD | C9 — warm-spare invariant `ready_spares==N`, `total<=MAX`; readiness via real MCP round-trip | — | A half-booted machine is never granted | integration + manual | `--once --dry-run` for bookkeeping; real-`x64sc` readiness half is human-verify (no live VICE calls in CI) | ❌ W0 (bookkeeping) | ⬜ pending |
| TBD | TBD | TBD | C10 — three-state broker-absent reporting | — | Each state names its own fix, quoting the host path | unit (extend 01.1's three-state tests) | `node --test .claude/mcp/vice/vice-proxy.test.mjs` | ✅ existing pattern, extend | ⬜ pending |
| TBD | TBD | TBD | C11 — proxy never exits, never caches a negative result | — | Emulator access survives a broker-absent call | unit (extend existing) | `node --test .claude/mcp/vice/vice-proxy.test.mjs` | ✅ existing pattern, extend | ⬜ pending |
| TBD | TBD | TBD | C12 — ROADMAP standing constraint actually edited | — | The narrowing is recorded, not just implemented | doc check | manual review — not automatable | manual | ⬜ pending |
| TBD | TBD | TBD | C13 — fresh-session + two-concurrent-session proof | — | Per-session leasing is real across sessions | manual, blocking human-verify | reuse `01.1-CRITERION-3-EVIDENCE.md`'s shape, extended to two sessions | manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tools/vice-broker.sh` — does not exist yet; needs `--once` / `--dry-run` / `--check-container` / `--print-paths` / `--help` flags mirroring its siblings so a host-only daemon is testable from the container
- [ ] A new broker test file (`.claude/mcp/vice/vice-broker.test.mjs`, or colocated with `vice-pool.test.mjs`'s `execFile`-real-subprocess idiom) — covers criteria 1, 2, 7, 8
- [ ] `vice-proxy.mjs` shutdown-handler extension plus its own test additions — criteria 4, 5, 6. **Note: the proxy has zero signal handlers today**; this is new code, not an incremental edit
- [ ] A `vice-broker-client.mjs` module (or equivalent) container-side, with unit tests for request-writing, grant-polling, and lease-touching that do not need a live broker
- [ ] Framework install: **none needed** — `node --test` already covers everything automatable here

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| A boot-fresh broker-granted instance answers a real `mcp__vice__*` call from a fresh session | C13 | MCP server definitions are read once at session start (finding 1), so the session that changes the wiring cannot load it; and the broker runs on the host | Reuse `01.1-CRITERION-3-EVIDENCE.md`'s numbered evidence table: host launch command actually used, approval path, listed tool names with deny-list absence confirmed, a real call with its returned bytes, and an honest "not captured" row where something could not be recovered |
| Two concurrent sessions hold two demonstrably different instances | C13 | Per-session leasing is only observable across independent sessions | Two fresh sessions, each granted a different port/instance, both sessions' evidence captured in the same document |
| Spare readiness proven by a real MCP round-trip against a freshly booted `x64sc` | C9 (readiness half) | Needs a live host emulator; no `mcp__vice__*` calls run in CI | Observe `launching` → `ready` transition and confirm only `ready` spares are grantable, with the round-trip response recorded |
| The ROADMAP standing-constraint narrowing is written, not merely satisfied | C12 | Doc-content judgment | Review the § Standing Constraints row and the `vice-mcp-selector` skill for the narrowed wording in the same change |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
