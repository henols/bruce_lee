---
phase: 01.2
slug: on-demand-broker-and-per-session-leasing
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-31
---

# Phase 01.2 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded by `/gsd-plan-phase 1.2` from `01.2-RESEARCH.md` § Validation Architecture.
> Task IDs are filled in by `/gsd-validate-phase` once plans exist; this draft is keyed to
> ROADMAP success criteria because this phase carries **no REQ-IDs by design**.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node's built-in `node:test` — no `package.json`, no third-party runner anywhere in this repo `[VERIFIED: in-container]` |
| **Config file** | none — invoked directly via `node --test <glob>` |
| **Quick run command** | `node --test .claude/mcp/vice/vice-proxy.test.mjs` |
| **Full suite command** | `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'` |
| **Estimated runtime** | ~60 seconds full suite (168 tests at 01.1 close; spawns real subprocesses and sockets) |

---

## Sampling Rate

- **After every task commit:** Run `node --test .claude/mcp/vice/vice-proxy.test.mjs` — plus the new broker test file once it exists
- **After every plan wave:** Run `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'`
- **Before `/gsd-verify-work`:** Full suite green **and** criterion 13's blocking human-verify checkpoint passed
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

Keyed to ROADMAP's 13 success criteria rather than REQ-IDs — this phase carries none by design
(see ROADMAP § Requirement Coverage). Task IDs are assigned by `/gsd-validate-phase` after plans exist.

| Task ID | Plan | Wave | Criterion | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | 01.2-02 T1 | 2 | C1 — broker starts, mirrors `vice-pool.sh start [N]`, inherits container guard | — | Broker refuses to run in-container rather than half-starting | integration (real subprocess) | `node --test .claude/mcp/vice/vice-broker.test.mjs` (`--check-container` / `--print-paths` / `--dry-run`) | ❌ W0 → built in 01.2-01 T1 | ⬜ pending |
| TBD | 01.2-01 T1, 01.2-02 T2 | 1, 2 | C2 — protocol writes are tmp+rename, never in place | T-01.2-01, T-01.2-03 | A poller can never read a half-written grant; a bad id never builds a path | unit + source assertion | `node --test .claude/mcp/vice/vice-broker.test.mjs`; comment-filtered grep for any direct redirect into a live protocol path | ❌ W0 → built in 01.2-01 T1 | ⬜ pending |
| TBD | 01.2-01 T2 | 1 | C3 — `initialize`/`tools/list` never request an emulator | — | A session that never touches VICE launches nothing | unit (extend existing) | `node --test .claude/mcp/vice/vice-proxy.test.mjs` | ✅ existing, extend | ⬜ pending |
| TBD | 01.2-01 T1, T2 | 1 | C4 — release wired to SIGINT/SIGTERM/SIGHUP **and** stdin end/close, one test per ending | — | No class of session ending leaks a lease | unit | spawn the real proxy, send each signal / close stdin, assert lease removed | ❌ W0 → built in 01.2-01 | ⬜ pending |
| TBD | 01.2-01 T2 | 1 | C5 — shutdown handler does exactly one synchronous fs op | — | Release lands inside the measured ~490ms window | unit + region-scoped source assertion | `node --test .claude/mcp/vice/vice-proxy.test.mjs`; assertion confirmed to fire by temporary violation | ❌ W0 → built in 01.2-01 T2 | ⬜ pending |
| TBD | 01.2-01 T1, T2 | 1 | C6 — lease existence/mtime/removal is the whole contract | — | Release is a delete, never a write | unit | `node --test .claude/mcp/vice/vice-broker-client.test.mjs` plus the heartbeat mtime test | ❌ W0 → built in 01.2-01 | ⬜ pending |
| TBD | 01.2-02 T1 | 2 | C7 — exactly one teardown path (graceful == swept) | T-01.2-04, T-01.2-12 | The rarely-exercised path *is* the common path; no kill without an identity check | integration | `node --test .claude/mcp/vice/vice-broker.test.mjs` — missing-lease and stale-lease through `--once`, identical resulting state | ❌ W0 → built in 01.2-02 T1 | ⬜ pending |
| TBD | 01.2-02 T2 | 2 | C8 — a released instance is killed, never recycled into the spare set | T-01.2-10 | No cross-session state contamination | integration | `node --test .claude/mcp/vice/vice-broker.test.mjs` — second grant's supervisor pid and `launched_at` both differ; assertion confirmed to fire | ❌ W0 → built in 01.2-02 T2 | ⬜ pending |
| TBD | 01.2-04 T1, T2; cold path 01.2-03 T1 | 3, 2 | C9 — warm-spare invariant `ready_spares==N`, `total<=MAX`; readiness via real MCP round-trip | T-01.2-05, T-01.2-16 | A half-booted machine is never granted; the ceiling cannot drift from the target | integration + manual | `node --test .claude/mcp/vice/vice-broker.test.mjs` with a stubbed probe for bookkeeping; **readiness half is `verification: backstop`** — human-verify item 3 in 01.2-05 T1 | ❌ W0 (bookkeeping) → built in 01.2-04 | ⬜ pending |
| TBD | 01.2-03 T1 | 2 | C10 — three-state broker-absent reporting | T-01.2-06, T-01.2-08, T-01.2-17, T-01.2-18 | Each state names its own fix, quoting the host path; fails fast; forbids routing around it | unit (extend 01.1's three-state tests) | `node --test .claude/mcp/vice/vice-proxy.test.mjs` | ✅ existing pattern, extend | ⬜ pending |
| TBD | 01.2-03 T1 | 2 | C11 — proxy never exits, never caches a negative result | T-01.2-08 | Emulator access survives a broker-absent call | unit (extend existing) | `node --test .claude/mcp/vice/vice-proxy.test.mjs` — down-then-up on one process, zero explicit exit calls | ✅ existing pattern, extend | ⬜ pending |
| TBD | 01.2-05 T2 | 4 | C12 — ROADMAP standing constraint actually edited | T-01.2-19 | The narrowing is recorded, not just implemented | doc check + one new durable assertion | `node --test .claude/mcp/vice/vice-mcp-selector-docs.test.mjs` for the skill half; ROADMAP row is **`verification: backstop`** | ✅ existing gate, extend | ⬜ pending |
| TBD | 01.2-05 T1 | 4 | C13 — fresh-session + two-concurrent-session proof | T-01.2-11, T-01.2-20 | Per-session leasing is real across sessions; the deny-listed tool is never called | manual, blocking human-verify | `test -s` + content grep over `01.2-CRITERION-13-EVIDENCE.md` printing `CRITERION13-EVIDENCE-OK`; both halves **`verification: backstop`** | manual | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Every Wave 0 gap is built by plan **01.2-01** (wave 1), which is why that plan is the tracer: the
seam under test does not exist until it lands, so nothing downstream can be verified before it.

- [ ] `.claude/mcp/vice/resources/vice-broker.sh` — does not exist yet; needs `--once` / `--dry-run` / `--check-container` / `--print-paths` / `--help` mirroring its siblings so a host-only daemon is testable from the container. **Built in 01.2-01 T1.** Note the tracked source of truth lives under `resources/` and deploys to the gitignored `tools/vice-broker.sh`; `.gitignore` gains that entry in the same task
- [ ] `.claude/mcp/vice/vice-broker.test.mjs` — new, reusing `vice-pool.test.mjs`'s `execFile`-real-subprocess idiom. **Created in 01.2-01 T1**, extended by 01.2-02 and 01.2-04; covers criteria 1, 2, 7, 8, 9
- [ ] `vice-proxy.mjs`'s five-trigger teardown plus its test additions — criteria 4, 5, 6. **Built in 01.2-01 T1, completed in T2.** The proxy has **zero signal handlers today** and its header comment states the pre-spike, disproven ladder as fact: this is new code plus a prose correction, not an incremental edit
- [ ] `.claude/mcp/vice/vice-broker-client.mjs` and `vice-broker-client.test.mjs` — container-side, with unit tests for request-writing, grant-polling, lease-touching and broker-liveness classification that need no live broker. **Built in 01.2-01 T1, tested in T2.** Must not import `hostpath.mjs` — the consumer set is closed to four production modules by an existing durable assertion
- [ ] Framework install: **none needed** — `node --test` already covers everything automatable here

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| A boot-fresh broker-granted instance answers a real `mcp__vice__*` call from a fresh session | C13 | MCP server definitions are read once at session start (finding 1), so the session that changes the wiring cannot load it; and the broker runs on the host | Reuse `01.1-CRITERION-3-EVIDENCE.md`'s numbered evidence table: host launch command actually used, approval path, listed tool names with deny-list absence confirmed, a real call with its returned bytes, and an honest "not captured" row where something could not be recovered |
| Two concurrent sessions hold two demonstrably different instances | C13 | Per-session leasing is only observable across independent sessions | Two fresh sessions, each granted a different port/instance, both sessions' evidence captured in the same document |
| Spare readiness proven by a real MCP round-trip against a freshly booted `x64sc` | C9 (readiness half) | Needs a live host emulator; no `mcp__vice__*` calls run in CI | Observe `launching` → `ready` transition and confirm only `ready` spares are grantable, with the round-trip response recorded |
| The ROADMAP standing-constraint narrowing is written, not merely satisfied | C12 | Doc-content judgment | Review the § Standing Constraints row and the `vice-mcp-selector` skill for the narrowed wording in the same change |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
