---
phase: 01.2-on-demand-broker-and-per-session-leasing
verified: 2026-08-01T00:00:00Z
status: passed
score: 13/13 must-haves verified
behavior_unverified: 0
overrides_applied: 0
requirements_coverage: "Phase 01.2 carries no requirement IDs by design (ROADMAP: 'Deliberately written without a **Requirements**: key so the requirement-coverage gate reads this phase as unmapped'). Confirmed: REQUIREMENTS.md (190 lines) contains zero references to Phase 01.2 or 01.2-prefixed requirement IDs — no orphaned requirements exist for this phase."
human_verification_on_record:
  - item: "Criterion 9 readiness half (real MCP round-trip, not TCP accept) and Criterion 13 (fresh-session grant + two concurrent sessions holding two different instances)"
    disposition: "Already discharged by a blocking checkpoint:human-verify carried out by the human operator and recorded in 01.2-CRITERION-13-EVIDENCE.md (verdict: PASSED, all 10 items observed, verbatim evidence). Read and independently cross-checked during this verification; not re-litigated."
follow_ups_recommended:
  - "CR-01 (01.2-REVIEW.md): vice-broker.sh start has no singleton guard — two concurrent broker daemons can race grant_from_spare()'s read-then-remove and double-grant one ready spare to two sessions. Confirmed in code: cmd_start() (resources/vice-broker.sh:1269-1306) writes broker.json and enters its loop with no check for an existing live broker.json heartbeat/pid first."
  - "CR-02 (01.2-REVIEW.md): the pid-identity guard in teardown()/cmd_stop() only proves 'some vice-supervisor.sh process', not 'this grant's supervisor', because port/directory are passed via env vars, not argv. Plausible under CR-01 or ordinary pid recycling on a long-running host."
  - "CR-03 (01.2-REVIEW.md): tools/README.md's 'Interactive (agent-facing) use' section (lines ~386-398) still asserts the pre-01.2 behavior verbatim ('forwards every mcp__vice__* call to the FIXED port from decision D-A ... it never leases a pooled instance and never redirects') and never mentions vice-broker.sh anywhere in the file. Confirmed: grep -c 'vice-broker' tools/README.md returns 0. This directly contradicts ensureBrokerLease()'s shipped behavior and would misinform an operator."
  - "WR-01/WR-02/WR-03 (01.2-REVIEW.md): narrower races (stale-lease check-then-act, two-step request+lease write, partial containerization re-validation) — all low-probability, self-healing or narrow-blast-radius per the review's own analysis. Recorded for follow-up, not phase-blocking."
---

# Phase 01.2: On-Demand Broker and Per-Session Leasing Verification Report

**Phase Goal:** Add the host-side broker: launch a fresh `x64sc` per session on first use, kill it at
session end, keep N warm spares, sweep orphans on TTL. This is the phase that makes cross-session
concurrency real and narrows the reset ritual.

**Verified:** 2026-08-01
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP § Phase 01.2 detail, 13 numbered success criteria — the contract; no PLAN frontmatter narrows this set, each plan's own `must_haves.truths` map onto these criteria one-for-one)

| # | Truth (criterion) | Status | Evidence |
|---|---|---|---|
| C1 | Broker daemon exists, `start [N]`/`stop`/`status`, default N=3 range 1..16, mirrors `vice-pool.sh`, inherits container guard | ✓ VERIFIED | `resources/vice-broker.sh` `cmd_start`/`cmd_stop`/`cmd_status` read directly (lines 1269-1344+); container guard confirmed live (`bash vice-broker.sh start` inside container → exit 2, per 01.2-01/02 SUMMARYs and re-confirmed by reading `container_guard_enforce` call site); `vice-broker.test.mjs` daemon-lifecycle tests pass |
| C2 | Protocol is files under existing `.vice-supervisor/`; every write is tmp+rename, never in place | ✓ VERIFIED | `write_json_atomic()` (bash) and the client's tmp-then-rename writers confirmed by direct code read; grep-based structural checks in both 01.2-01 and 01.2-02 SUMMARYs (`grep -cE '>[[:space:]]*"?\$(GRANTS_DIR\|DENIALS_DIR\|BROKER_JSON)'` → 0) |
| C3 | Acquisition deferred: `initialize`/`tools/list` never touch the broker | ✓ VERIFIED | Dedicated regression test (`01.2-01-SUMMARY.md` D-line; "with no tool calls: no request file, no lease file"); re-run in the independent full-suite pass (212/212) |
| C4 | Release wired to SIGINT/SIGTERM/SIGHUP **and** stdin end/close, one test per ending | ✓ VERIFIED | 5 dedicated tests in `vice-proxy.test.mjs`, named per trigger; independently re-run (212/212 pass) |
| C5 | Shutdown handler does exactly one synchronous fs op | ✓ VERIFIED | Source-region assertion (`TEARDOWN-REGION-BEGIN/END` markers) confirmed to actually fire by temporary violation + revert, per `01.2-01-SUMMARY.md` |
| C6 | Lease existence/mtime/removal is the whole contract; session id/pid are metadata, not the key | ✓ VERIFIED | `vice-broker-client.test.mjs`'s 14 unit tests plus heartbeat mtime tests in `vice-proxy.test.mjs`; lease body read directly in `01.2-CRITERION-13-EVIDENCE.md` § 5 confirms shape |
| C7 | Exactly one teardown path reached by release and TTL sweep alike | ✓ VERIFIED | Dedicated test asserting released-vs-swept produce identical resulting directory state, differing only in the logged reason (`01.2-02-SUMMARY.md`); confirmed by direct code read of `sweep_grants()`/`teardown()` |
| C8 | A released instance is killed, never recycled | ✓ VERIFIED | Kill-never-recycle test **confirmed to fail when the rule is relaxed** (temporarily commented out `rm -f`, observed red, reverted — transcript in `01.2-02-SUMMARY.md`); further confirmed live in `01.2-CRITERION-13-EVIDENCE.md` § 7 (killed pid 1383151 vs replacement 1438839, port removed from registry entirely, not reset to ready) |
| C9 | `ready_spares == N` under `total <= MAX`; readiness by real MCP round-trip, not a TCP accept | ✓ VERIFIED | Bookkeeping half: parameterised test reproduces all 5 rows of the leased/total/ready table (`01.2-04-SUMMARY.md`). Readiness half (marked `verification: backstop` in PLAN frontmatter, correctly not claimed by test alone): closed by `01.2-CRITERION-13-EVIDENCE.md` § 3 — `broker-instances.json` read live shows both spares in state `ready` with non-null `ready_at` stamped only after a real round-trip, elapsed 0.827s/0.793s |
| C10 | Three broker-absent states (never-started/dead-or-hung/launch-failed), three messages, three fixes, fails fast | ✓ VERIFIED | Dedicated tests per state in `vice-proxy.test.mjs` (`01.2-03-SUMMARY.md`); message builders read directly in `vice-proxy.mjs` |
| C11 | Proxy never exits, never caches a negative broker verdict | ✓ VERIFIED | `grep -c 'process\.exit(' vice-proxy.mjs` (excluding comments) is 0 — reconfirmed directly; "broker never-cache: absent-then-alive-and-granted succeeds on the SAME process" test passes |
| C12 | ROADMAP § Standing Constraints VICE row actually edited (narrowed, not retired), SKILL.md agrees in the same change | ✓ VERIFIED | Read `.planning/ROADMAP.md` § Standing Constraints directly: VICE row states per-session boot-fresh, cross-session concurrency real, ritual "**NARROWED, not retired** (D-1.2-C)", cites `01.2-CRITERION-13-EVIDENCE.md` by path, points at the instance-handles seed for intra-session parallelism. Read `SKILL.md` directly: "only the session's first grant is guaranteed boot-fresh... a procedure that needs a known-clean machine still resets explicitly" — same verdict, same distinction. The two documents agree. |
| C13 | Fresh session + real emulator op through a broker-granted instance; two concurrent sessions demonstrably hold two different instances | ✓ VERIFIED | `01.2-CRITERION-13-EVIDENCE.md`, human-verified, verdict PASSED. Read directly: two grants on two different ports (6512, 6510), two different lease ids, two different session ids, plus an independent `$033C` marker cross-read (`5A A5` in session 1, `00 00` in session 2, `5A A5` again in session 1 afterward) proving routing isolation, not just bookkeeping |

**Score:** 13/13 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.claude/mcp/vice/resources/vice-broker.sh` | Host-only broker daemon | ✓ VERIFIED | 1370 lines; `bash -n` clean; container guard, atomic writes, teardown, spare state machine all present on direct read |
| `.claude/mcp/vice/vice-broker-client.mjs` | Container-side protocol client | ✓ VERIFIED | 276 lines; full exported surface present (`REQUEST_ID_PATTERN`, `newRequestId`, `pollGrant`, `readBrokerLiveness`, etc.) |
| `.claude/mcp/vice/vice-broker.test.mjs` | Broker integration tests | ✓ VERIFIED | 1446 lines; part of the independently re-run 212/212 passing suite |
| `.claude/mcp/vice/vice-broker-client.test.mjs` | Client unit tests | ✓ VERIFIED | 240 lines; part of the 212/212 suite |
| `.claude/mcp/vice/vice-proxy.mjs` (modified) | `ensureBrokerLease()`, three-state diagnostics, two-ladder teardown | ✓ VERIFIED | 1344 lines; message builders, `onTeardown`, heartbeat all present on direct read |
| `.claude/mcp/vice/vice-proxy.test.mjs` (modified) | Full ending-path + broker-state coverage | ✓ VERIFIED | 2496 lines; part of 212/212 suite; `acquireLeaseViaBroker()` helper confirmed updated to the two-pass cold-launch model (pre-plants a ready spare), resolving the plan-03/04 merge-time reconciliation item recorded in `deferred-items.md` |
| `.mcp.json` (modified) | Single `vice` entry gains `timeout` | ✓ VERIFIED | Read directly: exactly one server, no `url`, one-element `args`, `timeout: 60000` — matches plan 03's spec |
| `.planning/ROADMAP.md` (modified) | § Standing Constraints VICE row narrowed | ✓ VERIFIED | Read directly; see C12 above |
| `.claude/skills/vice-mcp-selector/SKILL.md` (modified) | Per-session broker guidance | ✓ VERIFIED | Read directly; matches C12's verdict; no forbidden module/CLI-verb names present |
| `01.2-CRITERION-13-EVIDENCE.md` | Human-verified two-session proof | ✓ VERIFIED | 697 lines; numbered §§ 1-10, verbatim output, explicit not-captured rows where evidence genuinely could not be recovered |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `vice-proxy.mjs`'s `ensureBrokerLease()` | `vice-broker-client.mjs` | request/lease writers, `pollGrant()` | ✓ WIRED | Confirmed by direct read and by the tracer/integration tests |
| `vice-broker-client.mjs` writers | `vice-broker.sh`'s `requests/`/`grants/`/`denials/`/`leases/` dirs, resolved from `VICE_POOL_DIR` on both sides | shared bind mount | ✓ WIRED | Confirmed live in `01.2-CRITERION-13-EVIDENCE.md` (both container-read files and host-side broker output agree on the same request/grant ids) |
| Released lease / stale lease | `teardown()` | `sweep_grants()`'s single call site | ✓ WIRED | Confirmed by code read and by the identical-resulting-state test |
| `readBrokerLiveness()`'s three classifications | `brokerNeverStartedMessage`/`brokerDeadOrHungMessage`/`brokerLaunchFailedMessage` | `ensureBrokerLease()`'s classify-before-write branch | ✓ WIRED | Confirmed by direct code read |
| ROADMAP § Standing Constraints VICE row | `vice-mcp-selector/SKILL.md` | shared verdict (narrowed, not retired) | ✓ WIRED (documents agree) | Both read directly this session; single consistent verdict, cross-referenced by path |
| Broker's `grants/`/`spares/` state | `broker-instances.json` (a projection, never `registry.json`) | `write_instances()` | ✓ WIRED | `grep -c 'registry\.json'` (excluding comments) in `vice-broker.sh` is 0 — reconfirmed; `vice-pool.mjs`'s `acquire()` cannot see a broker-owned instance |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full vice+tools suite passes | `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'` | `pass 212, fail 0, cancelled 0` (independently re-run in this verification session, not taken from a SUMMARY claim) | ✓ PASS |
| `vice-broker.sh` is syntactically valid and container-guarded | direct code read of `cmd_start()`/`container_guard_enforce` call site | present, matches SUMMARY claims | ✓ PASS |
| `.mcp.json` shape | `cat .mcp.json` | one server, no `url`, `timeout: 60000` | ✓ PASS |
| `tools/README.md` mentions the broker | `grep -c 'vice-broker' tools/README.md` | `0` | ✗ FAIL (see CR-03 follow-up; not a roadmap success-criterion failure — see Gaps Summary) |

### Requirements Coverage

Phase 01.2 is deliberately unmapped to any requirement ID (ROADMAP: "Requirement mapping: none — tooling
phase, by design ... Deliberately written without a `**Requirements**:` key so the requirement-coverage
gate reads this phase as unmapped rather than parsing this sentence as a REQ-ID"). Confirmed directly:
`grep -n "01.2" .planning/REQUIREMENTS.md` returns zero matches across the file's 190 lines, and no
requirement section names Phase 01.2. This resolves the prompt's flagged concern (`phase_req_ids`
resolving to `null`) as **legitimate absence, not missing traceability** — there is no orphaned
requirement for this phase to be accountable to.

### Anti-Patterns Found

No debt markers (`TBD`/`FIXME`/`XXX`) found in any file this phase modified. No stub returns, no
hardcoded-empty data flowing to output, no placeholder implementations — every new code path in
`vice-broker.sh` (`launch_instance`, `probe_ready`, `maintain_spares`, `grant_from_spare`, `deny`) is
production-quality per `01.2-04-SUMMARY.md`'s own "Known Stubs: None" declaration, independently
consistent with the direct code reads performed during this verification.

One stale-documentation anti-pattern found and independently confirmed (see Gaps Summary / follow-ups):
`tools/README.md`'s "Interactive (agent-facing) use" section still describes the pre-01.2 fixed-port
behavior verbatim and never mentions `vice-broker.sh`. This is a documentation-accuracy defect, not a
code stub, and does not affect any of the 13 stated success criteria (which name `ROADMAP.md` and
`SKILL.md` specifically, not `tools/README.md`, as the docs criterion 12 requires updating).

### Human Verification Required

None outstanding. Criterion 13 (and criterion 9's readiness half) required a blocking human-verify
checkpoint by the phase's own design (MCP server definitions are read once at session start; per-session
isolation is only observable across two independent sessions). That checkpoint was carried out by the
human operator, the evidence document was read directly during this verification (not taken on faith),
and its verdict — PASSED, with explicit not-captured rows for the two items that genuinely could not be
recovered (§ 4's approval-prompt gap, recovered independently in § 6; § 7's unprompted 11:00 teardown
cause) — is credible on its own terms: verbatim JSON file contents, verbatim command output, and an
internally-consistent narrative (e.g. the granted port's `supervisor_pid` in § 5 matches the spare
recorded in § 3; the killed pid in § 7 matches the leased pid in § 6).

### Gaps Summary

**No roadmap success criterion failed.** All 13 of ROADMAP § Phase 01.2's numbered criteria are
verified with a combination of independently re-run automated tests (212/212, re-run in this session,
not taken from any SUMMARY's claim) and directly-read, internally-consistent human-verified evidence
for the two truths (C9's readiness half, C13) that the phase's own design correctly marked as requiring
a human checkpoint rather than a test.

**Three findings from the independent code review (`01.2-REVIEW.md`) were checked against the codebase
directly and confirmed factually correct, but judged not to be phase-goal gaps:**

- **CR-01** (no singleton guard on `vice-broker.sh start`) and **CR-02** (pid-identity guard cannot
  distinguish two live supervisor instances) describe real races, but both require an operator error
  (starting two broker daemons concurrently) or long-uptime pid recycling to trigger — neither is a
  failure mode any of the 13 stated criteria cover, and the single-broker case those criteria describe
  was proven correct by direct human observation in `01.2-CRITERION-13-EVIDENCE.md`. Recommended as
  follow-up hardening, not as a reason to withhold phase completion.
- **CR-03** (`tools/README.md` still describes pre-01.2 fixed-port behavior, never mentions the broker)
  is confirmed true (`grep -c 'vice-broker' tools/README.md` → 0) and is a genuine documentation-accuracy
  defect that could mislead an operator. However, criterion 12 explicitly scopes the required doc edit
  to `ROADMAP.md`'s § Standing Constraints row and `SKILL.md` — both of which were correctly updated and
  agree with each other. `tools/README.md` was not named by any of the 13 criteria as requiring an update
  in this phase, so its staleness is a real gap in overall documentation quality but not a phase-goal
  failure under this phase's own success criteria. Recommended as an immediate follow-up quick task,
  since an operator who reads only `tools/README.md` would be told the opposite of what the shipped code
  does.

None of these three findings, individually or combined, cause any artifact to be MISSING/STUB, any key
link to be NOT_WIRED, or any of the 13 stated truths to be FAILED — the decision-tree gate for
`gaps_found` is not met. Recorded in the frontmatter `follow_ups_recommended` for tracking, per the
instruction to judge independently rather than inherit the reviewer's BLOCKER severity labels wholesale.

---

*Verified: 2026-08-01*
*Verifier: Claude (gsd-verifier)*
