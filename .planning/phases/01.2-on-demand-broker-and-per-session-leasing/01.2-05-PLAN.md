---
phase: 01.2
plan: 05
type: execute
wave: 4
depends_on: ["01.2-03", "01.2-04"]
files_modified:
  - .planning/ROADMAP.md
  - .claude/skills/vice-mcp-selector/SKILL.md
  - .claude/mcp/vice/install-resources.mjs
  - .claude/mcp/vice/vice-mcp-selector-docs.test.mjs
  - .planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md
autonomous: false
requirements: []
user_setup:
  - service: host-side VICE broker
    why: "Criterion 13 is only observable from outside the container: the broker launches x64sc on the host, and per-session leasing is only visible across two independent Claude Code sessions"
    env_vars: []
    dashboard_config:
      - task: "Run the deployed broker from the HOST workspace: <host workspace>/tools/vice-broker.sh start 2"
        location: "A host terminal in the workspace directory — never a devcontainer terminal; the container guard refuses with exit 2 by design"
      - task: "Confirm x64sc is on the host PATH and a display is available, since each granted instance is a real emulator window"
        location: "Host shell, before starting the broker"
      - task: "Open two fresh Claude Code sessions after the broker is running, so each loads the .mcp.json entry at session start"
        location: "Host — MCP server definitions are read once at session start, so the session that changed the wiring cannot load it"
must_haves:
  truths:
    - statement: "The ROADMAP § Standing Constraints VICE row is edited in place to say the reset/clear-checkpoints/reload ritual is NARROWED to within-session reuse rather than retired, with the evidence for that verdict named (C12)"
      verification: backstop
    - statement: "The vice-mcp-selector SKILL.md is updated in the same change to describe the broker route, the narrowed ritual, and the three broker-absent states (C12)"
      verification: backstop
    - statement: "From a fresh session a real mcp__vice__* operation succeeds through a broker-granted instance, with the recorded evidence naming the host launch command, the granted port and the bytes returned (C13)"
      verification: backstop
    - statement: "Two concurrent sessions demonstrably hold two different instances, with both sessions' evidence captured in one document (C13)"
      verification: backstop
    - "01.2-CRITERION-13-EVIDENCE.md exists, follows 01.1-CRITERION-3-EVIDENCE.md's numbered-table structure, and records an honest not-captured row for anything that could not be recovered"
    - "Intra-session parallelism stays out of scope: the narrowing edit states that subagents share one emulator and points at the instance-handles seed rather than implying per-subagent isolation"
    - "vice_disk_list was never called at any point in the checkpoint procedure, stated explicitly in the evidence document"
  artifacts:
    - .planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md
    - .planning/ROADMAP.md
    - .claude/skills/vice-mcp-selector/SKILL.md
  key_links:
    - "The ROADMAP constraint row, the SKILL.md guidance and the evidence document must agree on one verdict — narrowed, not retired — since the row is what every later phase's planning inherits"
    - "01.1-CRITERION-3-EVIDENCE.md is the structural template, extended with a second-session column that is the whole point of criterion 13"
    - "install-resources.mjs's deploy-time instructions name the broker alongside the supervisor, so a human deploying resources is told to start the thing that is now the route"
---

<objective>
Write the narrowing into the constraint that every later phase inherits, then prove per-session
leasing is real by observing it from outside the container.

Purpose: a constraint declared obsolete but left standing gets re-inherited by the next planner. And
criterion 13 cannot be self-verified by any amount of test code — MCP server definitions are read
once at session start, the broker runs on the host, and "two sessions hold two different instances" is
only observable across two independent sessions. That combination is a sequencing gate, not a
look-at-it check.

Output: the § Standing Constraints VICE row edited in place with its evidence, the skill document
updated in the same change, and a recorded two-session evidence document following 01.1's template.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-01-SUMMARY.md
@.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-02-SUMMARY.md
@.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-03-SUMMARY.md
@.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-04-SUMMARY.md
@.planning/phases/01.1-tool-mediated-emulator-access/01.1-CRITERION-3-EVIDENCE.md
</context>

## Artifacts this phase produces

See the full table in `01.2-01-PLAN.md` § "Artifacts this phase produces". The symbol this plan
introduces on top of it is the new tracked document
`.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md`.

## Planning notes carried into execution

**Deliberate deviation from `workflow.human_verify_mode: end-of-phase`.** Task 1 is a blocking
`checkpoint:human-verify` rather than a `<human-check>` deferred to the end of the phase. The reason
is the same one 01.1 recorded for its structurally identical criterion 3: this is a **sequencing
gate**, not a look-at-it check. It requires a host-side process and two fresh sessions, neither of
which an executor can produce, and the doc edits in task 2 assert a verdict that the checkpoint is
what establishes. Recording the verdict before observing it would be the failure mode. State this
deviation in the SUMMARY.

**Known pre-existing flake.** See `01.2-01-PLAN.md`. If the wave gate runs the full suite and
`vice-pool.test.mjs`'s two-port test reds, that is the known ~1-in-5 socket-readiness flake recorded
in `01.1-deferred-items.md`, not damage from this phase. Re-run and record both outcomes.

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Fresh-session proof, and two concurrent sessions holding two different instances</name>
  <precondition>`x64sc` is on the host PATH and a display is available, and the deployed `tools/vice-broker.sh` exists in the host workspace — the container cannot create either, and `vice-broker.sh` refuses to run in a container on purpose.</precondition>
  <read_first>
    - `.planning/phases/01.1-tool-mediated-emulator-access/01.1-CRITERION-3-EVIDENCE.md` — the structural template for the document this checkpoint produces: numbered section per item, verbatim output rather than paraphrase, and § 6's honest not-captured row with its cause. Read it before writing a single line of the new document.
    - `.claude/skills/vice-mcp-selector/SKILL.md` — the tool-surface conventions the procedure's calls follow, and the standing rule that the deny-listed disk-listing tool is never called, including "just to check".
    - `.planning/STATE.md` § Blockers/Concerns — the six host VICE MCP outages, the pause-on-state-read finding, and the unverified note on whether a `.mcp.json` change re-triggers project-scope approval. Item 4 of the procedure is what turns that last one into recorded evidence.
    - `.planning/phases/01.2-.../01.2-01-SUMMARY.md` through `01.2-04-SUMMARY.md` — what is already proven by test, so the procedure spends host time only on what tests cannot reach.
    - `.claude/mcp/vice/resources/vice-broker.sh` `--help` output — the exact CLI the host commands in this procedure use.
  </read_first>
  <what-built>
    A host-side broker daemon (`vice-broker.sh start [N]`) that launches a boot-fresh `x64sc` per
    session on first use and kills it when that session ends, plus a container-side proxy that
    requests, is granted and leases an instance on its first forwarded `tools/call` and releases the
    lease on every one of the five session-ending paths. Everything automatable is already green:
    the full request→grant→forward→release round trip, all five release triggers, the single
    teardown path shared by graceful release and TTL sweep, kill-never-recycle, the warm-spare
    invariant's bookkeeping, and the three broker-absent messages. What no test can produce is a
    real `x64sc` proving MCP-readiness, and two independent Claude Code sessions holding two
    different instances at the same time.
  </what-built>
  <how-to-verify>
    Record every numbered result below into
    `.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md`,
    following `01.1-CRITERION-3-EVIDENCE.md`'s structure: one numbered section per item, verbatim
    command output or file contents rather than paraphrase, and an explicit not-captured row with its
    cause for anything that genuinely cannot be recovered. Never call `vice_disk_list` or
    `mcp__vice__disk_list` at any point — it crashes the host MCP server and recovery needs a manual
    host-side restart.

    1. **Stop anything already running.** On the HOST, in the workspace directory, stop any existing
       supervisor or pool so the broker is the only thing launching emulators. Record what was
       running beforehand.

    2. **Start the broker with two spares.** On the HOST:
       `./tools/vice-broker.sh start 2`
       Leave it running in that terminal. Record the command exactly as typed and the first ~20 lines
       it prints, which should include the spare launches and the `launching` → `ready` transitions
       with their elapsed times. If it refuses with exit 2, you are in the container — open a host
       terminal instead.

    3. **Confirm readiness came from a round-trip, not a socket.** From the container, read
       `.vice-supervisor/broker-instances.json` and record it verbatim. Both entries must be in state
       `ready` with a non-null `ready_at`. Then record the broker log lines showing the transition.
       This is the readiness half of criterion 9 that no CI test can cover.

    4. **Fresh session, one real emulator operation.** Open a **new** Claude Code session (the
       session that changed the wiring cannot load it — MCP definitions are read once at session
       start). If a project-scope approval prompt appears, record that it appeared and what you chose;
       `.mcp.json` gained a `timeout` field this phase and whether that re-triggers approval is
       recorded as UNVERIFIED, so this observation is itself evidence. In that session call
       `mcp__vice__vice_memory_read` for address `$FFFC`, size 2, and record the exact result. The
       correct answer is the KERNAL reset vector: `["E2","FC"]`, little-endian `$FCE2`. Anything else
       means the machine is not a stock booted C64 and the run should be treated as failed.

    5. **Confirm that session holds a broker-granted lease.** From the container, list
       `.vice-supervisor/leases/` and `.vice-supervisor/grants/` and record both. There must be
       exactly one lease and one grant, the grant must name a real port, and `broker-instances.json`
       must show that port in state `leased` carrying the lease id.

    6. **Second concurrent session, different instance.** With the first session still open and
       **without** closing it, open a second fresh Claude Code session and make one
       `mcp__vice__vice_ping` call there. Then record `.vice-supervisor/grants/` again. There must now
       be **two** grants with **two different ports**, and `broker-instances.json` must show both as
       `leased` with different lease ids. Record both grant files verbatim. This is the criterion's
       core claim and the one thing a single session can never show.

       Two grant files are bookkeeping — prove the **routing** as well, since a second proxy that
       silently forwarded to the first instance would produce identical bookkeeping. Before opening
       the second session, in the first session write a two-byte marker into the unused tape buffer at
       `$033C` with `mcp__vice__vice_memory_write` — bytes `$5A $A5`, a value a boot-fresh machine
       never holds there — and read it back to confirm it stuck. Then in the second session read
       `$033C` size 2: it must **not** be the marker. Finally re-read `$033C` in the first session and
       confirm the marker is still present, which also rules out both sessions having been routed to
       the *second* instance. Record all three reads verbatim. State reads pause the emulator, so
       resume once at the end of this sequence rather than between calls. If the second session sees
       the marker, the two sessions are sharing one machine, criterion 13 has **failed** — record that
       verdict plainly rather than as a caveat and do not proceed to task 2.

    7. **Release, and confirm the instance is killed rather than recycled.** Close the second session.
       Record: its lease file disappears; within one TTL its grant disappears; the broker log shows a
       teardown line and a refill launch line; and the refilled spare's supervisor pid differs from
       the killed instance's. Record the two pids side by side.

    8. **Answer the output-ceiling question while a live emulator is available.** In the still-open
       first session, call `mcp__vice__vice_memory_read` for a large range — start at `$0400` with
       size 16384 — and record which of three things happened: the result arrived inline; the proxy
       split it across a continuation sequence; or the client reported the result too large and named
       a spilled file path. This costs one call and settles the recorded discrepancy between the
       standing 32KB chunking rule and the 500,000-character ceiling the proxy declares. If the answer
       is a spill, say so plainly — it means the declared ceiling is not honoured and chunk sizing
       needs revisiting in a follow-up.

    9. **Stop the broker cleanly.** On the HOST press Ctrl-C in the broker's terminal. Record that
       `broker.json` disappears and that the first session's still-leased instance is left alone —
       stopping the broker is not the same event as a session ending.

    10. **Confirm the deny-list survived the redirect.** In either session, confirm
        `mcp__vice__disk_list` / `mcp__vice__vice_disk_list` is absent from the listed tools. Do not
        attempt to call it to check. Record the confirmation and how it was made.
  </how-to-verify>
  <verify>
    <automated>test -s .planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md &amp;&amp; grep -q 'FCE2' .planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md &amp;&amp; grep -qi 'two different ports\|different port' .planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md &amp;&amp; grep -qi '033c' .planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-CRITERION-13-EVIDENCE.md &amp;&amp; echo CRITERION13-EVIDENCE-OK</automated>
  </verify>
  <acceptance_criteria>
    - The evidence document exists, is non-empty, and the verify command prints `CRITERION13-EVIDENCE-OK`.
    - It contains a numbered section per item 1–10 above, with verbatim output rather than paraphrase for items 2, 3, 4, 5, 6, 7 and 8.
    - Item 4 records `["E2","FC"]` or an explicit statement that a different value was returned and the run was treated as failed.
    - Item 6 records two grant files with two different port numbers and two different lease ids.
    - Item 6 records the marker cross-read as three verbatim results — the marker read back in the first session, the second session's read of the same address showing a different value, and the first session's re-read still showing the marker — or states plainly that the marker was visible in both sessions, that criterion 13 failed, and that task 2 was not started (T-01.2-21).
    - Item 7 records two supervisor pids side by side and states plainly that they differ.
    - Item 8 records exactly one of the three outcomes, named unambiguously.
    - It states explicitly that `vice_disk_list` / `mcp__vice__disk_list` was never called at any point in the procedure.
    - Anything not recoverable carries a not-captured row naming the cause, in the shape 01.1's § 6 uses — no value is guessed.
  </acceptance_criteria>
  <resume-signal>Type "approved" once the evidence document is written, or describe what failed and at which numbered step</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Narrow the standing constraint in place, and update the skill in the same change</name>
  <files>.planning/ROADMAP.md, .claude/skills/vice-mcp-selector/SKILL.md, .claude/mcp/vice/install-resources.mjs, .claude/mcp/vice/vice-mcp-selector-docs.test.mjs</files>
  <read_first>
    - `.planning/ROADMAP.md` § Standing Constraints — the VICE row in full. It currently reads "VICE is a single shared host instance reached only over MCP", says `parallelization: true` does not extend to emulator work, requires the reset/clear-checkpoints/reload ritual, and closes with "until 01.2 lands, this constraint stands exactly as written and plans must not assume otherwise." That closing clause is what this task discharges.
    - `.planning/ROADMAP.md` § Phase 01.2 detail — criterion 12's exact wording, and the second "Decisions to resolve here" bullet, so the edit answers the question that was actually asked.
    - `.claude/skills/vice-mcp-selector/SKILL.md` — all 78 lines. Note the register: usage-only, no module names, no CLI verbs, `mcp__vice__*` tool names throughout.
    - `.claude/mcp/vice/vice-mcp-selector-docs.test.mjs` lines 106-199 — `SHELL_INVOCATION_PATTERN` and `enumerateGuardedDocuments()`, which enumerate every `SKILL.md`; any wording added must not match that pattern.
    - `.claude/mcp/vice/install-resources.mjs` lines 91-115 — `hostLaunchInstructions()`, which currently names only the supervisor.
    - `.planning/phases/01.2-.../01.2-CRITERION-13-EVIDENCE.md` — task 1's recorded evidence, which is the source for the verdict this task writes down. Do not write a verdict the evidence does not support.
    - `.planning/seeds/vice-instance-handles-for-parallel-emulator-work.md` — the seed the narrowing must point at rather than implying intra-session isolation.
  </read_first>
  <action>
    Edit the § Standing Constraints VICE row in place, using scoped replacement rather than rewriting
    the file. Replace the "single shared host instance" framing with what the phase actually
    established, and cite task 1's evidence document by path for the claim. The row must say all of:

    - Emulator access is per-session and boot-fresh: each session's first forwarded tool call is
      granted its own freshly launched instance, killed when that session ends.
    - Cross-**session** concurrency is real, so two plans running in two different sessions no longer
      serialise on one emulator.
    - The reset/clear-checkpoints/reload ritual is **NARROWED, not retired** (D-1.2-C). A fresh boot
      removes cross-session contamination structurally, so a plan no longer needs the ritual to
      protect itself from a *previous session's* state. It does **not** remove contamination within a
      session that reuses one emulator across several plans, so a plan that is not the first
      emulator-touching plan of its session still opens with the ritual.
    - Intra-**session** parallelism is still out of scope: subagents share their parent session's
      single proxy connection and therefore its single instance, so a parallel executor wave inside one
      session still shares one emulator and its steps still queue. Point at
      `.planning/seeds/vice-instance-handles-for-parallel-emulator-work.md` for the deferred design.
    - The broker is started by hand on the host and nothing auto-starts it; a broker-absent tool call
      reports which of three states it is in and what to do.

    Delete the row's closing deferral clause — the one quoted in `<read_first>` above that says the
    constraint stands as written until this phase lands — since it is now discharged. Leave the
    disk-listing hazard row and every other row untouched.

    Update `.claude/skills/vice-mcp-selector/SKILL.md` in the same change, staying in its usage-only
    register: add a short section saying each session gets its own boot-fresh emulator granted on first
    use and released at session end; that the first call of a session may wait a few seconds for a cold
    launch and may report warming-and-retry, in which case retrying the same call is the correct
    action; that a broker-absent result names which of three states applies and the host command to
    run; that within one session the same emulator is reused across calls, so a procedure that needs a
    known-clean machine still resets explicitly; and that snapshot names should be namespaced by
    something session-scoped rather than by port, because ports are recycled under on-demand launch
    (D-1.2-E). Name no module under `.claude/mcp/vice/` and no CLI verb of the transport module — a
    durable documentation gate matches exactly that pattern and will fail the build if you do.

    Update `hostLaunchInstructions()` in `install-resources.mjs` so the deploy-time message names the
    broker as the thing to start for MCP-mediated access, alongside the existing supervisor line for
    the standalone recovery pipeline. Reuse the existing `hostPath()`-with-`SET_ENV_HINT` fallback
    shape; add no second import, since the host-path consumer set is closed to four production modules
    and this file is already one of them.

    Add one assertion to `vice-mcp-selector-docs.test.mjs`: the skill document must describe the
    per-session broker route — assert on a stable, meaning-bearing phrase that the new section
    contains, chosen so a future rewrite that drops the guidance fails rather than a cosmetic reword
    failing. Confirm the new assertion actually fires by temporarily removing that section, observing
    the failure, and reverting.
  </action>
  <verify>
    <automated>node --test .claude/mcp/vice/vice-mcp-selector-docs.test.mjs .claude/mcp/vice/vice-proxy.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test .claude/mcp/vice/vice-mcp-selector-docs.test.mjs` exits 0, including the new skill-content assertion, and the new assertion was confirmed to fire by temporary removal and revert (recorded in the SUMMARY).
    - The deferral clause is gone: `grep -c 'this constraint stands exactly as written' .planning/ROADMAP.md` returns 0.
    - `grep -ci 'narrowed' .planning/ROADMAP.md` is at least 1 within the § Standing Constraints VICE row, and the row cites `01.2-CRITERION-13-EVIDENCE.md` by path.
    - The VICE row states both that cross-session concurrency is real and that intra-session parallelism is not, and names the instance-handles seed by path.
    - `git diff --stat .planning/ROADMAP.md` shows a change confined to the § Standing Constraints VICE row and, if the planning note is appended there, the Phase 01.2 detail section — no other phase's rows or detail sections modified.
    - The disk-listing hazard row survives the edit: `grep -c 'vice_disk_list' .planning/ROADMAP.md` is at least 1.
    - `grep -cE 'vice\.mjs +(call|ping|tools|session|pool)\b' .claude/skills/vice-mcp-selector/SKILL.md` is 0.
    - `grep -c 'vice-broker' .claude/mcp/vice/install-resources.mjs` is at least 1, and `grep -c 'devcontainer-host-path/scripts/hostpath.mjs' .claude/mcp/vice/install-resources.mjs` is still exactly 1.
    - `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'` reports no failures beyond the known two-port flake.
  </acceptance_criteria>
  <done>The § Standing Constraints VICE row says narrowed rather than retired, with the distinction between cross-session and intra-session spelled out and the evidence cited; the skill document carries matching guidance protected by an assertion confirmed to fire; and the deploy-time instructions name the broker.</done>
  <reversibility rating="reversible">The verdict is "narrowed, not retired" (D-1.2-C). It is a documentation edit backed by task 1's evidence, and if later measurement shows within-session reuse is also contamination-free the row can be narrowed further in one scoped edit.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host operator → live emulator | The checkpoint procedure drives a real `x64sc` through real MCP tool calls |
| recorded evidence → later phases | The § Standing Constraints row is what every subsequent planner inherits as fact |
| session A → session B | Two concurrent sessions must never reach the same emulator; the only thing separating them is which granted port each one's proxy forwards to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.2-19 | Repudiation | the recorded verdict | high | mitigate | The narrowing edit is gated behind the checkpoint that establishes it, cites the evidence document by path, and requires verbatim output rather than paraphrase for every load-bearing item — a verdict recorded ahead of its evidence is the failure mode this ordering prevents |
| T-01.2-21 | Tampering | cross-session instance isolation | high | mitigate | Two sessions sharing one machine is the exact failure criterion 13 exists to disprove: one session's writes, checkpoints and pauses would land in the other's capture and corrupt a provenance verdict silently. Item 6 proves isolation twice over — by bookkeeping (two grants, two different ports, both `leased` with different lease ids) and by routing (a two-byte marker planted at `$033C` in the first session must be absent from the second session's read of the same address and still present on the first session's re-read). A marker visible in both sessions fails the criterion outright and blocks task 2, rather than being recorded as a caveat |
| T-01.2-22 | Repudiation | the evidence document's fidelity and completeness | high | mitigate | The document *is* the proof, so paraphrase, a cherry-picked subset, or a quietly omitted failed step would convert an unproven claim into a sealed one that every later phase inherits. Mitigated by requiring verbatim tool output for items 2–8, an explicit not-captured row naming the cause for anything genuinely unrecoverable in the shape `01.1-CRITERION-3-EVIDENCE.md` § 6 established, no guessed values, a resume signal that requires naming the failing numbered step, and an automated gate asserting the load-bearing markers are actually present in the document |
| T-01.2-11 | Tampering | `vice_disk_list` during a live procedure | critical | mitigate | The procedure forbids calling it and forbids calling it "to check", and item 10 confirms absence from the tool listing by inspection only; the three-layer deny-list from 01.1 remains the structural guard |
| T-01.2-20 | Denial of Service | a live host emulator left running or orphaned | medium | mitigate | Item 9 stops the broker cleanly and records that leased instances are deliberately left alone; item 7 confirms teardown and refill, so the procedure ends in a known state rather than an unknown number of processes |
| T-01.2-06 | Information Disclosure | evidence document contents | low | accept | The document records local ports, pids and a KERNAL reset vector on a solo-developer machine; nothing secret is captured, and 01.1's equivalent document set the precedent |
| T-01.2-SC | Tampering | package-manager installs | high | accept | Zero installs in this plan; the changes are documentation and one test assertion |
</threat_model>

<verification>
- `01.2-CRITERION-13-EVIDENCE.md` exists and its verify command prints `CRITERION13-EVIDENCE-OK`.
- The § Standing Constraints VICE row says narrowed, cites the evidence document, and distinguishes cross-session from intra-session.
- `node --test .claude/mcp/vice/vice-mcp-selector-docs.test.mjs` exits 0 with the new assertion confirmed to fire.
- `node --test '.claude/mcp/vice/**/*.test.mjs' 'tools/**/*.test.mjs'` reports no failures beyond the known two-port flake.
</verification>

<success_criteria>
Per-session leasing is proven by observation from outside the container, two concurrent sessions are
recorded holding two different instances, and the constraint every later phase inherits now says what
this phase actually established — narrowed to within-session reuse, with cross-session contamination
gone and intra-session parallelism still out of scope.
</success_criteria>

<output>
Create `.planning/phases/01.2-on-demand-broker-and-per-session-leasing/01.2-05-SUMMARY.md` when done
</output>
