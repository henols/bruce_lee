---
phase: 01.1
slug: tool-mediated-emulator-access
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-31
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `01.1-RESEARCH.md` § Validation Architecture.

**Standing environmental fact:** the host VICE emulator was **not running** when this phase was
researched (`ECONNREFUSED` on 6510/6511/6512; supervisor state files absent). The whole point of the
strategy below is that it does **not** depend on that changing: the proxy is a Node process whose
entire observable surface is stdin/stdout frames, so ~90% of this phase is testable with zero network
activity by stubbing the host client. Only criterion 3 needs a live host, and it is a human
checkpoint by construction (see Manual-Only Verifications).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node's built-in `node:test` + `node:assert/strict` — verified in-container, and already the project's convention (`vice-pool.test.mjs`, `ram-capture.test.mjs`, `skill-docs.test.mjs`) |
| **Config file** | none — a `*.test.mjs` file sits next to the code it covers and is run directly |
| **Quick run command** | `node --test .claude/skills/vice-mcp-selector/scripts/vice-proxy.test.mjs` |
| **Full suite command** | `node --test '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs'` |
| **Estimated runtime** | ~10–20 seconds quick; full suite dominated by the pre-existing `vice-pool.test.mjs` |

---

## Sampling Rate

- **After every task commit:** Run `node --test .claude/skills/vice-mcp-selector/scripts/vice-proxy.test.mjs`
- **After every plan wave:** Run the full suite, **plus** confirm the relocated transport modules
  (`vice.mjs`, `vice-sync.mjs`, `vice-pool.mjs`) still pass their own existing test files unchanged
  at their new location — a relocation that breaks a test it did not author is the failure mode here.
- **Before `/gsd-verify-work`:** Full suite green, and the criterion-3 human checkpoint passed
- **Max feedback latency:** ~20 seconds

---

## Per-Task Verification Map

Phase 01.1 carries **no REQ-IDs** (tooling phase, by design — see ROADMAP § Requirement Coverage).
The map is therefore keyed to the phase's 9 success criteria. Task IDs are filled in by the planner.

| Task ID | Plan | Wave | Criterion | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-----------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | 1 | 1 (diagnosis) | — | N/A | doc-assertion | grep the diagnosis artifact for the recorded probe commands and their observed output | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 2 (`.mcp.json`) | — | Entry has no `url`, so prior approval is never invalidated | unit (durable enumeration) | `node --test .../vice-mcp-selector-docs.test.mjs` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 3 (fresh session, live host) | — | N/A | **manual-only** | — (human checkpoint) | N/A | ⬜ pending |
| TBD | TBD | — | 4 (`tools/list` with no emulator) | — | N/A | unit — spawn proxy, write `initialize`, assert well-formed response **and zero outbound fetch** | `node --test .../vice-proxy.test.mjs -t "initialize with no emulator"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 5 (`vice_disk_list` refused) | T-01.1-01 | Hazardous tool is unreachable, not merely undocumented | unit — absent from `tools/list`; `tools/call` returns `isError:true`; no fetch attempted | `node --test .../vice-proxy.test.mjs -t "vice_disk_list"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 5 (epoch re-check) | T-01.1-02 | A mid-session host restart fails loudly instead of returning blank-machine reads | unit — stub `readEpoch()` to drift mid-sequence; assert `isError:true` naming the restart, never a throw | `node --test .../vice-proxy.test.mjs -t "epoch drift"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 5 (output size) | — | Large results are bounded, never silently truncated | unit — drive a response past the size limit; assert the chosen strategy (chunking or `_meta` override) leaves the payload recoverable and marked | `node --test .../vice-proxy.test.mjs -t "output size"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 6 (never-throw) | T-01.1-03 | A malformed frame cannot cost the session its emulator access | unit — malformed JSON line, unknown method, EPIPE-shaped write failure; assert still alive and answering, and a later valid call succeeds | `node --test .../vice-proxy.test.mjs -t "never-throw"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 7 (three-state diagnostics) | — | N/A | unit — stub connection-refused / connect-then-hang / connects-and-errors; assert three distinct messages, each with a host-path-shaped string and the "do not use `vice.mjs`" language | `node --test .../vice-proxy.test.mjs -t "three states"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 8 (Bash-mediation retired) | — | No documented path routes an agent around the enforcing seam | unit (durable enumeration, not a one-shot grep) | `node --test .../vice-mcp-selector-docs.test.mjs` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 8 (library call sites repointed) | — | N/A | unit — the pre-existing tests of `tools/recover.mjs`, `tools/chip-state.mjs`, `tools/watch-loads.mjs` and `c64-ram-capture` pass unchanged after relocation | full suite | ✅ (tests exist) | ⬜ pending |
| TBD | TBD | — | 9 (proxy-owned path translation) | T-01.1-04 | A container path can never reach the host, where it would fail silently | unit — forward a call carrying a workspace-relative container path; assert the host client received the **host** form; assert the out-of-workspace case takes its decided branch | `node --test .../vice-proxy.test.mjs -t "path translation"` | ❌ W0 | ⬜ pending |
| TBD | TBD | — | 9 (callers stop translating by hand) | — | N/A | unit (durable enumeration) — no consumer outside the proxy imports `tryHostPaths`, or the survivors are enumerated in an explicit allow-list the test asserts against | `node --test .../vice-mcp-selector-docs.test.mjs` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `.claude/skills/vice-mcp-selector/scripts/vice-proxy.test.mjs` — covers criteria 4, 5, 6, 7, 9
      via a spawned-child-process harness (write frames to stdin, assert on stdout). Matches this
      project's convention of testing real process behaviour rather than mocking the module boundary.
- [ ] A stub for the host HTTP client, so criteria 4–7 and 9 run with zero network activity and no
      dependency on a live emulator. **This is the load-bearing Wave 0 item** — it is what makes the
      phase verifiable despite the host being down.
- [ ] `vice-mcp-selector-docs.test.mjs` — durable, directory-enumerating completeness check for
      criteria 2, 8 and 9's caller-side half. Modelled on the existing `skill-docs.test.mjs` pattern
      (already used twice in this repo). Must enumerate rather than grep once: a one-shot grep passes
      the day it is written and rots silently.
- [ ] Framework install: **none needed** — `node:test` ships with the installed Node and is already
      used project-wide.

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| `mcp__vice__*` tools are listed and a real emulator operation succeeds through them | 3 | Doubly impossible in-band: MCP server definitions are read once at session start (finding 1), so the session that adds the `.mcp.json` entry cannot load it; and project-scope servers require approval (finding 10). Independently, the host emulator is currently down. | 1. Start the host emulator on the fixed port (recommended: `tools/vice-supervisor.sh`, single instance — matches "fixed port"; the 3-instance pool is 1.2's territory). 2. Start a **new** Claude Code session in this workspace. 3. Approve the `vice` project-scope MCP server when prompted. 4. Confirm `mcp__vice__*` tools are listed. 5. Call `mcp__vice__memory_read` against a known-value address and confirm the bytes. 6. Record the transcript as the criterion-3 evidence. **Do not call `vice_disk_list` at any point.** |
| Criterion 8's removal is safe to land | 8 | Sequencing gate, not a behaviour: the phase's own rule is that removal follows the criterion-3 proof. | Confirm the criterion-3 checkpoint above passed and its evidence is recorded **before** any removal task commits. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (the host-client stub above is the critical one)
- [ ] No watch-mode flags
- [ ] Feedback latency < 20s
- [ ] Criterion 3 human checkpoint passed and recorded, before any criterion-8 removal task commits
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
