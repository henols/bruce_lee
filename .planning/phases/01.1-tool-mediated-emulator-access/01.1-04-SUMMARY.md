---
phase: 01.1-tool-mediated-emulator-access
plan: 04
subsystem: infra
tags: [mcp, vice-emulator, devcontainer, node-test, tool-relocation]

requires:
  - phase: 01.1-tool-mediated-emulator-access (plan 01.1-03)
    provides: never-throw vice-proxy.mjs with three-state unreachable diagnostics and container->host path translation
provides:
  - Recorded criterion-3 evidence proving mcp__vice__* reaches the real host emulator from a fresh Claude Code session
  - The vice-session transport module tree and its resources/ relocated (git mv, history preserved) into vice-mcp-selector, the skill actually registered in .mcp.json
  - vice-session skill retired entirely, with a durable node:test gate (vice-mcp-selector-docs.test.mjs) closing criterion 8 and criterion 9's caller-side half
affects: [phase-01.1-final-verification, any future plan touching tools/recover.mjs's pipeline or the c64-ram-capture skill]

tech-stack:
  added: []
  patterns:
    - "Directory-enumerating node:test gates over one-shot greps for doc/import hygiene (vice-mcp-selector-docs.test.mjs, skill-docs.test.mjs's inverted entry-point rule)"
    - "Negative-check confirmation discipline: a forbidden-pattern gate is proven to fire by temporarily introducing the violation, observing the failure, then reverting -- not merely asserted to pass"

key-files:
  created:
    - .planning/phases/01.1-tool-mediated-emulator-access/01.1-CRITERION-3-EVIDENCE.md
    - .claude/skills/vice-mcp-selector/scripts/vice-mcp-selector-docs.test.mjs
    - .planning/phases/01.1-tool-mediated-emulator-access/deferred-items.md
  modified:
    - .claude/skills/vice-mcp-selector/scripts/{vice.mjs,vice-pool.mjs,vice-sync.mjs,vice-session.mjs,vice-probe.mjs,repo-root.mjs,install-resources.mjs,vice-pool.test.mjs,skill-docs.test.mjs,vice-proxy.mjs,vice-proxy.test.mjs,refresh-manifest.mjs} (relocated from vice-session/, plus import-specifier repoints)
    - .claude/skills/vice-mcp-selector/resources/{vice-supervisor.sh,vice-pool.sh,lib/repo-root.sh} (relocated, comment-only path updates)
    - tools/{recover.mjs,chip-state.mjs,watch-loads.mjs,README.md}
    - .claude/skills/c64-ram-capture/scripts/{ram-capture.mjs,ram-capture.test.mjs,skill-docs.test.mjs}, .claude/skills/c64-ram-capture/SKILL.md
    - .claude/skills/vice-mcp-selector/SKILL.md
    - .gitignore

key-decisions:
  - "protocolVersion recorded as not-captured with cause, not guessed: handleInitialize() never persists the client's requested value and the proxy has no log/debug hook -- a deliberate, user-approved deviation from the letter of Task 1's acceptance criteria."
  - "Assertion 4's hostpath.mjs consumer enumeration is scoped to production .mjs (excludes *.test.mjs): vice-proxy.test.mjs also imports hostPath() to independently compute an expected value for its own translation assertions, which is not the caller-hand-translates-a-path concern criterion 9 targets. Documented in the test's own header."
  - ".claude/CLAUDE.md is gitignored and was entirely absent from this worktree; copied in from the main checkout to exercise the Project Skills table edit and the new test's assertion 5, but the edit cannot be committed and will not survive worktree teardown."
  - "Three pre-existing vice-pool.test.mjs failures (CONTAINER_WORKSPACE_PATH/worktree path-resolution conflict, and a live host emulator flipping a 'not answering' expectation from Task 1's own checkpoint) are environment artifacts, not caused by this plan's relocation -- logged to deferred-items.md rather than fixed."

requirements-completed: []

coverage: []

duration: ~90min
completed: 2026-07-31
status: complete
---

# Phase 01.1 Plan 04: Fresh-session proof and vice-session retirement Summary

**Recorded criterion-3 fresh-session evidence proving `mcp__vice__*` reaches the real host VICE emulator, then migrated the transport module tree from the retired `vice-session` skill into `vice-mcp-selector` and closed the Bash-mediated path behind a durable, self-verifying `node:test` gate.**

## Performance

- **Duration:** ~90 min
- **Tasks:** 3 (1 continuation from a passed checkpoint, 2 executed fresh)
- **Files modified:** 30 (across all three task commits)

## Accomplishments

- `01.1-CRITERION-3-EVIDENCE.md` records a fresh Claude Code session listing 64 `mcp__vice__*` tools (63 from the committed snapshot plus the proxy's synthetic `vice_result_continue`), confirms `mcp__vice__disk_list` absent, and shows `mcp__vice__vice_memory_read($FFFC, 2)` returning the correct KERNAL reset vector (`$FCE2`) from the live host emulator.
- The nine `.mjs` transport modules and the `resources/` shell tree moved (`git mv`, history preserved) from `vice-session/` into `vice-mcp-selector/`, with all five external library call sites, `vice-proxy.mjs`'s two cross-skill imports, and `refresh-manifest.mjs`'s import repointed. Every pre-existing test that isn't coupled to the worktree-vs-main-checkout path-resolution quirk (see Deviations) passes unchanged at its new location.
- `vice-session` is gone entirely; `vice-mcp-selector-docs.test.mjs` adds five directory-walk-built assertions (one static `.mcp.json` entry, no document instructs an agent to reach the emulator from a shell, the old skill and its imports are gone, the `hostpath.mjs` consumer set is closed to its four traced production members, the skills table is current) — two of the five (the shell-invocation pattern and the consumer allow-list) were confirmed to actually fire by temporarily introducing the violation, observing the failure, then reverting.
- `tools/README.md`, `vice-mcp-selector/SKILL.md` (new Troubleshooting section) and `c64-ram-capture/SKILL.md` no longer send an agent to a shell to reach the emulator.

## Task Commits

1. **Task 1 (01.1-04/T1): Fresh-session proof, resumed after a passed human-verify checkpoint** — `d37559a` (docs)
2. **Task 2 (01.1-04/T2): Relocate the transport module tree and repoint every consumer** — `3e4927c` (feat)
3. **Task 3 (01.1-04/T3): Retire the documented Bash-mediated path behind a durable completeness check** — `e9f5f68` (feat)

## Files Created/Modified

- `.planning/phases/01.1-tool-mediated-emulator-access/01.1-CRITERION-3-EVIDENCE.md` — Task 1's recorded evidence, all six step-7 items plus the `protocolVersion` gap.
- `.claude/skills/vice-mcp-selector/scripts/{vice.mjs, vice-pool.mjs, vice-sync.mjs, vice-session.mjs, vice-probe.mjs, repo-root.mjs, install-resources.mjs, vice-pool.test.mjs, skill-docs.test.mjs}` — relocated via `git mv` from `vice-session/scripts/`; `skill-docs.test.mjs`'s entry-point exemption inverted (no module, including `vice.mjs`, may be named in `SKILL.md`; the `mcp__vice__` prefix is asserted instead).
- `.claude/skills/vice-mcp-selector/resources/{vice-supervisor.sh, vice-pool.sh, lib/repo-root.sh, lib/container-guard.sh}` — relocated via `git mv`; comment-only path updates, confirmed by a rename-aware diff against the wave-start commit.
- `.claude/skills/vice-mcp-selector/scripts/vice-proxy.mjs`, `vice-proxy.test.mjs`, `refresh-manifest.mjs` — repointed cross-skill imports to siblings.
- `tools/recover.mjs`, `tools/chip-state.mjs`, `tools/watch-loads.mjs` — import specifier substitution only, no behaviour change.
- `.claude/skills/c64-ram-capture/scripts/{ram-capture.mjs, ram-capture.test.mjs, skill-docs.test.mjs}`, `.claude/skills/c64-ram-capture/SKILL.md` — import substitution plus dependency-sentence update.
- `.claude/skills/vice-mcp-selector/scripts/vice-mcp-selector-docs.test.mjs` — new, five assertions closing criteria 8 and 9's caller-side half.
- `tools/README.md` — §2's connection check now leads with curl; §3's layout note and links updated; §5's "interactive use" paragraph corrected (no pool lease on the agent-facing path); §6 drops the session-CLI/discovery-verb guidance and gains a "programmatic seam" subsection.
- `.claude/skills/vice-mcp-selector/SKILL.md` — new Troubleshooting section (missing tools, the three unreachable messages, a restart report, disk inspection), naming no module under `scripts/`.
- `.gitignore` — comment block renamed to the new skill path.
- `.planning/phases/01.1-tool-mediated-emulator-access/deferred-items.md` — new, the three pre-existing test failures logged out of scope.
- `.claude/CLAUDE.md` — edited locally to exercise the new test, but gitignored and absent from git history (see Deviations).

## Decisions Made

See `key-decisions` in the frontmatter above for the four load-bearing ones. In addition:

- Kept `vice-session.mjs` (the session-layer module) rather than deleting it during the move, per the plan's own explicit instruction: `vice.mjs`'s CLI block dynamically imports it in four places, and "relocate unchanged" is what keeps the move safe. Criterion 8 targets the *documented, agent-facing* path (retired in Task 3); the module itself remains reachable only by a human debugging by hand, or by `tools/recover.mjs`'s own CLI resolution path.
- The relocated `skill-docs.test.mjs`'s no-exemption rule (no module under `scripts/` may be named in `SKILL.md`, not even `vice.mjs`) is the correct inversion of the old rule, because `vice-mcp-selector`'s agent-facing surface is `mcp__vice__*` tool calls, never a CLI command — there is no entry-point module for an agent to be told about.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `refresh-manifest.mjs` needed the same import repoint, though absent from the plan's `files_modified` list**
- **Found during:** Task 2
- **Issue:** `refresh-manifest.mjs` imports the transport module via `../../vice-session/scripts/vice.mjs`. Left unfixed, the acceptance criteria's own repo-wide `grep -rl 'vice-session/scripts/' --include='*.mjs' .claude tools` would still fail.
- **Fix:** Repointed to a sibling import (`./vice.mjs`).
- **Files modified:** `.claude/skills/vice-mcp-selector/scripts/refresh-manifest.mjs`
- **Committed in:** `3e4927c`

**2. [Rule 3 - Blocking] `vice-pool.test.mjs`'s hardcoded real-file path assertions needed updating despite the plan's "no test-body change" instruction**
- **Found during:** Task 2
- **Issue:** The relocated `vice-pool.test.mjs`'s path-agreement tests and `RESOURCES_DIR` test hardcode `join(repoRoot(), ".claude", "skills", "vice-session", ...)` against REAL files on disk. Left unchanged, both would fail post-move (the plan's own acceptance criteria warns: "A relocation that breaks a test it did not author is the failure mode here").
- **Fix:** Updated the hardcoded segment from `vice-session` to `vice-mcp-selector` in the three affected assertions (two path-agreement tests, one `RESOURCES_DIR` assertion). Left the synthetic-fixture "last-resort fallback" test's arbitrary skill name and the historical `quick-260730-oga` comment untouched — neither touches a real file.
- **Files modified:** `.claude/skills/vice-mcp-selector/scripts/vice-pool.test.mjs`
- **Committed in:** `3e4927c`

**3. [Rule 1 - Bug] Runtime-printed instructions in `vice-session.mjs` and `install-resources.mjs` pointed at a path that no longer exists**
- **Found during:** Task 2
- **Issue:** `vice-session.mjs`'s thrown error messages (session-already-active, expired-session, epoch-mismatch) literally told the reader to run `node .claude/skills/vice-session/scripts/vice.mjs session release/acquire` — a broken instruction after the move. `install-resources.mjs`'s `hostLaunchInstructions()` prefixed every line with `vice-session:`.
- **Fix:** Substituted the stale path in all seven occurrences; renamed the log prefix to `vice-mcp-selector:`.
- **Files modified:** `.claude/skills/vice-mcp-selector/scripts/vice-session.mjs`, `.claude/skills/vice-mcp-selector/scripts/install-resources.mjs`
- **Committed in:** `3e4927c`

**4. [Rule 1 - Bug] Three bare-prose `vice-session/scripts/` mentions failed the acceptance criteria's literal repo-wide grep**
- **Found during:** Task 2
- **Issue:** `repo-root.mjs`'s header, `vice-pool.mjs`'s session-lease comment, and `c64-ram-capture/scripts/skill-docs.test.mjs`'s header all named the old path in a comment, not an import — but the acceptance criteria's grep (`grep -rl 'vice-session/scripts/' --include='*.mjs' .claude tools`) is literal and does not distinguish comments from code.
- **Fix:** Reworded each (historical context preserved where the comment described a past event; the current-usage one in `vice-pool.mjs` updated to the real current path).
- **Files modified:** `.claude/skills/vice-mcp-selector/scripts/repo-root.mjs`, `.claude/skills/vice-mcp-selector/scripts/vice-pool.mjs`, `.claude/skills/c64-ram-capture/scripts/skill-docs.test.mjs`
- **Committed in:** `3e4927c`

**5. [Rule 4-adjacent, resolved by narrow scoping, not an architectural change] Assertion 4's real consumer count is five, not four**
- **Found during:** Task 3, while writing `vice-mcp-selector-docs.test.mjs`
- **Issue:** `vice-proxy.test.mjs` also imports `hostPath()` from `devcontainer-host-path/scripts/hostpath.mjs` — a genuine fifth importer if the enumeration includes test files, contradicting the plan's explicit "exactly four entries."
- **Resolution:** Scoped assertion 4's enumeration to production `.mjs` files (excludes `*.test.mjs`), matching `01.1-PATTERNS.md`'s own four-member consumer classification and the plan's explicit count. `vice-proxy.test.mjs` uses `hostPath()` to independently compute the value its own assertions check the real proxy against — verifying the translator, not hand-translating a path to hand to VICE, which is the different concern criterion 9 targets. Documented inline in the test's header comment, not silently decided.
- **Files modified:** `.claude/skills/vice-mcp-selector/scripts/vice-mcp-selector-docs.test.mjs`
- **Committed in:** `e9f5f68`

**6. [Deviation, recorded not fixed] `.claude/CLAUDE.md` is gitignored and absent from this worktree**
- **Found during:** Task 3
- **Issue:** `.gitignore` ignores `.claude/*` with only `!.claude/skills/` excepted, so `.claude/CLAUDE.md` is never tracked. It was also entirely absent from this worktree's checkout (gitignored files are not carried into a fresh worktree).
- **Handling:** Copied the file in from the main checkout (read-only source, write inside the worktree) so the Project Skills table edit and the new test's assertion 5 could be exercised and verified to pass. The edit is real on disk in this worktree but **cannot be committed** (git silently ignores it) and **will not survive worktree teardown**. Per the plan's own request to "note in the SUMMARY whether it stuck": it did not, and structurally cannot, for a stronger reason than the tooling-regeneration risk the plan anticipated (`01.1-RESEARCH.md` assumption A3). A real fix requires either un-ignoring this specific file or accepting that every developer's local `CLAUDE.md` must be hand-updated.
- **Files modified:** `.claude/CLAUDE.md` (local only, not committed)

**7. [Deviation, logged not fixed — see `deferred-items.md`] Three pre-existing `vice-pool.test.mjs` failures, environment-coupled, not caused by this plan**
- **Found during:** Task 2, confirmed again after Task 3
- **Issue:** `node --test '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs'` reports 165/168 passing. The 3 failures are (a) two path-agreement tests whose `repoRoot()` call resolves against the shared devcontainer's `CONTAINER_WORKSPACE_PATH` (the main checkout) rather than this isolated worktree — verified this is a structural conflict between that resolver's documented precedence and Claude Code's `.claude/worktrees/<id>/` nesting convention, not a defect in the relocation (independently confirmed via direct Node/shell resolver calls and `install --force`/`install` from within this worktree, both of which succeeded); and (b) one test expecting the default port 6510 to be "not answering," which now fails because Task 1's own checkpoint left a real host emulator running there.
- **Handling:** Logged to `deferred-items.md` with the full trace rather than fixed — fixing (a) would mean an architectural change to `repo-root.mjs`'s env-var precedence (out of this plan's scope), and fixing (b) would mean disturbing the live, shared emulator this plan's remaining tasks were explicitly told not to touch. Expected to resolve automatically once this worktree merges to main.
- **Files modified:** none (investigation only)

---

**Total deviations:** 5 auto-fixed (3 blocking, 2 bug), 2 recorded-not-fixed (1 environment gitignore limitation, 1 environment test-coupling).
**Impact on plan:** All auto-fixes were necessary for the relocation's own acceptance criteria to hold (a half-fixed import graph or a broken pre-existing test would have been a regression this plan explicitly warns against). The two recorded-not-fixed items are genuine environment artifacts with no in-scope fix; neither affects the correctness of the relocation itself, which was independently verified from within the worktree.

## Issues Encountered

- Running `grep` with a `cd /workspaces/bruce_lee &&` prefix early in this session briefly surfaced files from the *main checkout* (not this worktree) side by side with worktree paths in the same output — a reminder that `.claude/worktrees/<id>/` worktrees are nested on disk inside the main repo tree, so an unqualified `cd` to the repo root lands in the wrong checkout. No file was written outside the worktree; all Edit/Write calls in this plan targeted paths verified to resolve inside `/workspaces/bruce_lee/.claude/worktrees/agent-adf3aaa2cf070e61b`.
- `gsd-tools.cjs` is not present in this worktree (matches the precedent recorded in `.planning/STATE.md` for plan 01-03's executor), so the broken-windows ledger (`gsd_run windows append`) could not be invoked for the deferred items above. They are recorded in `deferred-items.md` and this SUMMARY instead; population of the ledger itself is documented as best-effort and non-blocking.

## User Setup Required

None — no external service configuration required. (Task 1's host-emulator setup was already completed by the human before this continuation began; see `01.1-CRITERION-3-EVIDENCE.md`.)

## Next Phase Readiness

- ROADMAP criterion 3 is satisfied by recorded human evidence (Task 1).
- ROADMAP criterion 8 is satisfied as a migration: `vice-session` is gone, the module tree and `resources/` are relocated with history, all five library call sites work with their pre-existing tests passing unchanged, host-side launch machinery is behaviourally untouched (comment-only diff, confirmed), and no document instructs an agent to reach the emulator from a shell — enforced by `vice-mcp-selector-docs.test.mjs`, confirmed to fail when violated.
- ROADMAP criterion 9's caller-side half is satisfied: the host-path consumer set is closed to its four traced production members, and `devcontainer-host-path` is unmodified (empty diff, confirmed).
- Outstanding for phase-level verification: the 3 environment-coupled test failures in `deferred-items.md` should re-run clean once this worktree merges to main; `.claude/CLAUDE.md`'s Project Skills table needs a manual update on any machine that keeps a local copy, since it cannot be committed.

## Self-Check: PASSED

- `01.1-CRITERION-3-EVIDENCE.md` exists (found via `test -s`), non-empty, and the plan's own `<automated>` verify command prints `CRITERION3-EVIDENCE-OK`.
- `test ! -e .claude/skills/vice-session` succeeds — old skill gone.
- `grep -rl 'vice-session/scripts/' --include='*.mjs' .claude tools` returns nothing (exit 1, no match).
- `git diff --name-only -- .claude/skills/devcontainer-host-path` is empty.
- Rename-aware diff of `.claude/skills/vice-mcp-selector/resources` against the wave-start commit (`447847a`) touches only `#` comment lines and `echo` string arguments.
- `node .claude/skills/vice-mcp-selector/scripts/vice.mjs install` reports zero `DIVERGED` entries.
- Node's `supervisorDir()` and the shell's `resolve_repo_root()` agree on `/workspaces/bruce_lee` as the repo root, from within this worktree.
- `node --test .claude/skills/vice-mcp-selector/scripts/vice-mcp-selector-docs.test.mjs` and the relocated `skill-docs.test.mjs` both exit 0 (5 + 3 assertions).
- Commits `d37559a`, `3e4927c`, `e9f5f68` all present in `git log --oneline`.
- Full-suite `node --test '.claude/skills/**/*.test.mjs' 'tools/**/*.test.mjs'`: 165/168 pass; the 3 failures are the pre-existing, environment-coupled ones documented above and in `deferred-items.md`, not regressions from this plan.

---
*Phase: 01.1-tool-mediated-emulator-access*
*Completed: 2026-07-31*
