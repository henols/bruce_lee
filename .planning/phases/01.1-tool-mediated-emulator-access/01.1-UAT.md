---
status: testing
phase: 01.1-tool-mediated-emulator-access
source: [01.1-VERIFICATION.md]
started: 2026-07-31T17:28:56Z
updated: 2026-07-31T17:28:56Z
---

## Current Test

number: 1
name: Accept criterion 3's recorded fresh-session evidence, and the protocolVersion-not-captured deviation
expected: |
  A human sign-off that (a) the recorded fresh-session evidence satisfies criterion 3 without a
  live re-run, and (b) the protocolVersion gap is accepted as a deferred note rather than a
  phase-blocking gap.

  On (a), the evidence in `01.1-CRITERION-3-EVIDENCE.md` is:
    - Host emulator running — `.vice-supervisor/epoch.json`, epoch 1, pid 2282407,
      `x64sc -mcpserver -mcpserverhost 0.0.0.0`, spawned 2026-07-31T15:54:47Z, started from the
      HOST via the deployed `tools/vice-supervisor.sh` (the bare-supervisor epoch path, per D-A).
    - Manifest refreshed against that live host — commit `447847a`,
      `generated_at: 2026-07-31T15:56:00.302Z`, 63 entries, `vice_disk_list` stripped.
    - A genuinely fresh session, separate from the one that added the `.mcp.json` entry (finding 1
      makes that separation mandatory, not incidental).
    - Approval path: pre-approved / trusted workspace — the `vice` server was connected at session
      start with no prompt; `/mcp` reported "1 connected" of 15 servers.
    - 64 `mcp__vice__*` tools listed, and `mcp__vice__disk_list` ABSENT from that list — criterion
      5's deny-list working in a real client's discovery layer, not only in a unit test.
    - A real call: `mcp__vice__vice_memory_read($FFFC, 2)` returned `["E2","FC"]`, i.e. reset
      vector `$FCE2` — the correct stock C64 KERNAL reset entry per the memory map.

  On (b), `handleInitialize()` (`vice-proxy.mjs:114-123`) echoes the client's requested
  `protocolVersion` but never persists it, and the proxy has no log file and no debug hook — so the
  value is unrecoverable after the handshake. It was recorded as not-captured with that cause
  rather than guessed. Closing it would need a one-line change to persist `requested`, plus another
  fresh session to observe a new handshake.
awaiting: user response

## Tests

### 1. Accept criterion 3's recorded fresh-session evidence, and the protocolVersion-not-captured deviation

expected: A human sign-off that the recorded fresh-session evidence satisfies criterion 3 without a live re-run, and that the protocolVersion gap is accepted as a deferred note rather than a phase-blocking gap.
result: [pending]

why_human: Criterion 3 is a blocking human-verify checkpoint by the phase's own design — the ROADMAP's risk section names "Verification asymmetry" and states that criterion 3 carries a human checkpoint, because MCP server definitions are read once at session start (finding 1) and project-scope servers need approval (finding 10). The verifier agent was additionally barred from calling any `mcp__vice__*` tool, since a live shared host emulator is running. This item exists so acceptance is recorded explicitly against `01.1-VERIFICATION.md`, rather than inferred from a separate evidence file.

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
