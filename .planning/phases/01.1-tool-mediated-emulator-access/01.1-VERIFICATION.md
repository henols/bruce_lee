---
phase: 01.1-tool-mediated-emulator-access
verified: 2026-07-31T18:00:00Z
status: human_needed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Confirm criterion 3's fresh-session evidence file is accepted as sufficient proof, and confirm the deliberate protocolVersion-not-captured deviation (§6 of 01.1-CRITERION-3-EVIDENCE.md) is knowingly accepted rather than requiring a follow-up fix before the phase is considered closed."
    expected: "A human sign-off that (a) the recorded fresh-session evidence (epoch.json, 64 mcp__vice__* tools listed, disk_list absent, correct reset-vector read) satisfies criterion 3 without a live re-run, and (b) the protocolVersion gap is accepted as a deferred note rather than a phase-blocking gap."
    why_human: "Criterion 3 is explicitly a blocking human-verify checkpoint by design (ROADMAP's own risk section: 'Verification asymmetry ... criterion 3 carries a human checkpoint'), and this verifier is prohibited from calling any mcp__vice__* tool to re-check it live. The verification context for this run states the checkpoint already passed with a human operator; this item exists so that acceptance is recorded against this VERIFICATION.md explicitly, not merely inferred from a separate evidence file."
---

# Phase 01.1: Tool-Mediated Emulator Access Verification Report

**Phase Goal:** The emulator is reached through real `mcp__vice__*` tools instead of through `Bash`,
and every known hazard is enforced by code in the proxy rather than remembered from documentation —
with the existing failure diagnosed first, so nothing is built on an unexamined assumption.

**Verified:** 2026-07-31
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (9 ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Written diagnosis names the actual mechanism and states reachability explicitly | ✓ VERIFIED | `01.1-DIAGNOSIS.md` records three live probes (DNS resolves; all 3 pool ports `Connection refused` immediately, not a timeout; `vice.mjs ping` exhausts its ~50s retry ladder) and applies the stated verdict rule to conclude `host-not-running` (not `structurally-unreachable`), explicitly distinguishing "host down" from "design defective." Corrects the overstated `vice-session-skill-is-dead.md` memory note with concrete evidence (three real recovery captures succeeded through the Bash path). |
| 2 | `.mcp.json` has exactly one static `vice` stdio entry, no `url` field | ✓ VERIFIED | `.mcp.json` (repo root) contains exactly `{"mcpServers":{"vice":{"command":"node","args":[".claude/skills/vice-mcp-selector/scripts/vice-proxy.mjs"]}}}` — no `url` key, single entry. Enforced by a durable regression test: `vice-mcp-selector-docs.test.mjs` "criterion 2: .mcp.json registers exactly one static, url-less stdio entry named vice" — PASS. |
| 3 | Fresh session lists `mcp__vice__*` tools and a real call succeeds | ✓ VERIFIED (human-checkpoint evidence, not re-run by this verifier per instruction) | `01.1-CRITERION-3-EVIDENCE.md`: host `x64sc -mcpserver` running (`.vice-supervisor/epoch.json`, epoch 1, pid 2282407); 63-tool committed manifest refreshed (commit `447847a`, `generated_at` non-null, `vice_disk_list` absent); fresh session confirmed separate from the session that edited `.mcp.json`; 64 `mcp__vice__*` tools listed (63 manifest + synthetic `vice_result_continue`), `mcp__vice__disk_list` confirmed absent; `mcp__vice__vice_memory_read($FFFC,2)` returned `["E2","FC"]` = reset vector `$FCE2`, the correct stock KERNAL value. One recorded, user-approved deviation: `protocolVersion` sent by the client was not captured (proxy never persists it) — documented as a gap with root cause, not silently dropped. Routed to human verification below because this is a blocking checkpoint by the phase's own design, and this verifier is barred from calling any `mcp__vice__*` tool. |
| 4 | `initialize`/`tools/list` answered without acquiring an emulator | ✓ VERIFIED | `vice-proxy.mjs` `handleInitialize()` (lines 110-120) does zero I/O. `handleToolsList()` (lines 202-217) only reads the committed JSON snapshot via `readManifestTools()` — no `call()`, no `probeInstance()`, no session. Test `"tools/list reads the committed snapshot with no emulator"` asserts `requests.length === 0` against the stand-in host server — PASS (see full-suite run below). |
| 5 | Three hazards enforced in code, each with a guard-removal-sensitive test | ✓ VERIFIED (all three, individually) | **`vice_disk_list` refusal:** `handleToolsCall()` line 629 checks `DENY_LIST.includes(name)` before any network attempt; test `"vice_disk_list is refused at tools/call with no request made"` asserts `isError:true` AND `requests.length === 0` — removing the guard would let the call reach the stand-in server, failing the length assertion. **Epoch re-check:** `checkEpochAndRebaseline()` called both before and after every forwarded call (lines 638, 704); test `"epoch drift is reported loudly and not cached"` writes a new epoch mid-sequence and asserts the next call is refused with both old and new epoch values in the message, then re-baselines — removing the check would let call 2 forward normally, failing the `isError` assertion. **Oversized-result chunking:** `wrapPossiblyChunked()` splits at `OUTPUT_CHAR_CAP`, never truncates; test `"an oversized result is recoverable in full across continuations"` reassembles a chunked 5026-char payload and asserts byte-for-byte equality against the original — removing the chunking logic would truncate the first chunk and fail the equality assertion. All three tests pass in the full suite run (18/18 in `vice-proxy.test.mjs`). |
| 6 | Proxy never exits, never caches a negative result, next call succeeds once emulator returns | ✓ VERIFIED | Exactly one `process.exit(` occurrence in `vice-proxy.mjs` (line 831, inside `shutdown()`, wired only to stdin `end`/`close`) — confirmed by direct grep. Global `uncaughtException`/`unhandledRejection` handlers (lines 43-48) log and continue rather than crash. Test `"never-throw: a broken stdout pipe does not kill the process"` and `"never-cache: host down then up succeeds without a restart"` both pass — the latter specifically proves a failed call followed by a successful one succeeds without any restart/reconnect step. |
| 7 | Three distinguishable unreachable diagnostics, each with an absolute host path and "only route" note | ✓ VERIFIED | `neverStartedMessage()`, `deadOrHungMessage()`, `aliveButFailedMessage()` are three distinct functions producing pairwise-distinct text. Test `"three states: each unreachable shape gets its own message and fix"` asserts all three states individually (never-started: refused + no epoch record; dead-or-hung: refused + epoch record with pid; alive-but-failed: host's own error relayed verbatim, no restart advice), asserts pairwise inequality of the three message strings, and asserts every message contains an absolute path (`/\S+`) and the phrase "only route" — PASS. `ONLY_ROUTE_NOTE` explicitly states the MCP surface is the only route and a shell fallback is not a workaround. |
| 8 | Bash-mediated emulator access retired; transport module relocated (not broken); host launch machinery untouched | ✓ VERIFIED | `.claude/skills/vice-session/` no longer exists on disk (confirmed via `ls` — "No such file or directory"). The four traced non-MCP consumers (`tools/recover.mjs`, `tools/chip-state.mjs`, `tools/watch-loads.mjs`, `.claude/skills/c64-ram-capture/scripts/ram-capture.mjs`) now import from `.claude/skills/vice-mcp-selector/scripts/vice.mjs` / `vice-pool.mjs` / `vice-sync.mjs` (confirmed by grep — all four repointed, none broken). `tools/vice-pool.sh`/`tools/vice-supervisor.sh` untouched (host-side launch machinery, out of scope per the criterion's own carve-out; `01.1-CRITERION-3-EVIDENCE.md` confirms `tools/vice-supervisor.sh` is what was actually run). Durable regression gate `vice-mcp-selector-docs.test.mjs`: "criterion 8: no enumerated document instructs an agent to invoke the transport module from a shell" (walks every `SKILL.md`, every `.claude/agents/*.md`, `CLAUDE.md`, `tools/README.md`, `docs/**/*.md`, root `*.md`, checking for `vice.mjs (call|ping|tools|session|pool)`) and "criterion 8: .claude/skills/vice-session no longer exists, and no .mjs anywhere names it in an import specifier" both PASS. |
| 9 | Path translation happens in the proxy seam, not in every caller; a test proves an untranslated container path cannot reach the host; `devcontainer-host-path` NOT retired | ✓ VERIFIED | `rewritePathsIn()`/`rewriteArguments()` in `vice-proxy.mjs` (lines 397-505) apply the structural rule (absolute path → normalize via `resolve()` → check workspace boundary on the *normalized* form → `hostPath()`) to every forwarded call's arguments, ordered before delegating to `call()`. Tests `"path translation: container paths cannot reach the host"` and `"path translation: a lexical .. cannot escape the workspace, and one that resolves back inside still translates"` both PASS — the latter is CR-01's regression test (the fixed path-traversal bypass). Caller-side carve-out confirmed: `devcontainer-host-path/` skill still exists on disk; test `"criterion 9 (caller-side): exactly the traced four production modules import hostpath.mjs, each with a recorded reason"` asserts the consumer set is exactly `{install-resources.mjs, vice-sync.mjs, ram-capture.mjs, vice-proxy.mjs}` — PASS, confirming the proxy is a *third* consumer, not a replacement. |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `01.1-DIAGNOSIS.md` | Diagnosis with live probe evidence and explicit verdict | ✓ VERIFIED | Exists, 200 lines, contains the verdict rule, three probes with raw command output, and the explicit `host-not-running` conclusion with halt-rule application. |
| `.mcp.json` | One static `vice` stdio entry, no `url` | ✓ VERIFIED | Confirmed content above; guarded by a durable test. |
| `.claude/skills/vice-mcp-selector/scripts/vice-proxy.mjs` | Stdio MCP proxy implementing initialize/tools_list/tools_call, never-throw, hazard enforcement, path translation | ✓ VERIFIED | 837 lines; read in full; implements every criterion 4-9 mechanism described above. |
| `.claude/skills/vice-mcp-selector/scripts/vice-proxy.test.mjs` | Per-guard regression tests | ✓ VERIFIED | 1410 lines, 18 tests, all pass (see full-suite run). |
| `.claude/skills/vice-mcp-selector/scripts/vice-mcp-selector-docs.test.mjs` | Durable caller-side gate (criteria 2, 8, 9) | ✓ VERIFIED | 5 tests, all pass. |
| `01.1-CRITERION-3-EVIDENCE.md` | Recorded human-checkpoint proof for criterion 3 | ✓ VERIFIED | Exists, 180 lines, contains the full evidence trail described above including the honestly-recorded protocolVersion gap. |
| `.claude/skills/vice-session/` | Must NOT exist (retired) | ✓ VERIFIED (absence confirmed) | `ls` reports "No such file or directory". |
| `.claude/skills/devcontainer-host-path/` | Must still exist (not retired) | ✓ VERIFIED | `SKILL.md` and `scripts/` present. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `tools/recover.mjs`, `tools/chip-state.mjs`, `tools/watch-loads.mjs` | `vice-mcp-selector/scripts/vice.mjs` + `vice-pool.mjs` (+ `vice-sync.mjs`) | relocated import specifiers | ✓ WIRED | Confirmed by direct `grep -n "import.*from"` — all three files import from the new location, none from the retired `vice-session` path. |
| `.claude/skills/c64-ram-capture/scripts/ram-capture.mjs` (+ its test) | `vice-mcp-selector/scripts/vice.mjs` + `vice-pool.mjs` + `vice-sync.mjs` | relocated import specifiers | ✓ WIRED | Confirmed by grep; imports `call, beginSession, assertSameMachine, serverInfo, readEpoch, lastToolCall` from the relocated module. |
| `vice-proxy.mjs` | `vice.mjs`, `vice-probe.mjs`, `repo-root.mjs`, `devcontainer-host-path/scripts/hostpath.mjs` | ES module imports | ✓ WIRED | Confirmed by reading the top of `vice-proxy.mjs` — all four sibling/cross-skill imports present and used throughout the file (`call`, `activeInstance`, `DENY_LIST`, `readEpoch`, `beginSession`, `MachineRestartedError`, `probeInstance`, `repoRoot`, `hostPath`, `SET_ENV_HINT`). |
| `vice-proxy.mjs`'s `handleToolsCall` | path-rewrite → `hostPath()` → `call()` | ordered pipeline | ✓ WIRED | Order confirmed by reading the function: deny-list check → epoch check → liveness probe → path rewrite → `call()` → post-call epoch check → chunking. Matches the documented ordering rationale in the file's own comments. |

### Behavioral Spot-Checks / Test Execution

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `vice-proxy.mjs`'s 18 unit tests (tracer, chunking, deny-list, epoch, never-throw, three-states, path translation) | `node --test .claude/skills/vice-mcp-selector/scripts/vice-proxy.test.mjs` | 18/18 pass | ✓ PASS |
| Durable docs/caller-side gate (criteria 2, 8, 9) | `node --test .claude/skills/vice-mcp-selector/scripts/vice-mcp-selector-docs.test.mjs` | 5/5 pass | ✓ PASS |
| Full workspace test suite (single run, per Step 7b's "at most once" constraint) | `node --test $(find .claude tools -name "*.test.mjs")` | 170/170 pass | ✓ PASS |
| `process.exit(` occurrence count in `vice-proxy.mjs` | `grep -c "process\.exit(" vice-proxy.mjs` | 1 real call site (line 831, `shutdown()`, wired to stdin end/close) | ✓ PASS |
| `vice_disk_list` never called by this verifier | N/A — hard prohibition honored throughout this verification | — | ✓ PASS (compliance, not a test) |

No probes (`scripts/*/tests/probe-*.sh`) are declared by this phase's PLAN/SUMMARY files; Step 7c is not applicable.

### Requirements Coverage

Not applicable. This phase deliberately carries no `**Requirements**:` key (ROADMAP.md § Phase 01.1
and § Requirement Coverage both state this explicitly: "Phases 1.1 and 1.2 carry no requirements, by
design"). No orphaned requirements found in `REQUIREMENTS.md` mapped to Phase 01.1.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.claude/skills/vice-mcp-selector/scripts/tools-manifest.json` | tools array | 4 of 63 committed manifest entries (`initialize`, `notifications_initialized`, `tools_list`, `tools_call`) are JSON-RPC protocol methods, not real `vice_*` emulator tools — advertised to agents as ordinary callable tools with no filtering beyond `DENY_LIST` | ⚠️ Warning (carried from `01.1-REVIEW.md` WR-01, confirmed still present at HEAD by direct inspection of the committed manifest) | Only 59 of the "63 tools" are real emulator tools; CLAUDE.md's "exact 63-tool surface... matches tool-for-tool" claim is measurably inflated by 4. Calling one of these four wastes a round trip / produces a confusing host error, and neither of the two `DENY_LIST` enforcement layers was designed with this shape in mind. Does not block any of the 9 stated criteria (none require the manifest to contain *only* `vice_*` names), but is a real, unfixed gap worth closing before the manifest count is cited again as ground truth. |
| `.claude/skills/vice-mcp-selector/scripts/vice.mjs` + `vice-proxy.mjs` | `vice.mjs:106,477-482`; `vice-proxy.mjs:604-609` (per `01.1-REVIEW.md` WR-02) | `DENY_LIST` enforcement uses exact-string `Array.prototype.includes()` in both layers, no case-folding/trim, so a hand-crafted `"Vice_Disk_List"` or `"vice_disk_list "` would bypass both layers identically | ⚠️ Warning (open per review, not re-verified live by this verifier since it requires a hostile-input call — not exercised in the current test suite) | Low practical risk (Claude Code echoes tool names verbatim from `tools/list`, which never contains such a variant), but the "two independent layers" framing in the code's own comments overstates the actual independence for this one bypass class. |
| `.claude/skills/c64-ram-capture/scripts/ram-capture.mjs` | 156-176 (per `01.1-REVIEW.md` WR-03) | `attachAndStart()`'s boot-detection retry loop resumes the machine once before the loop, then calls the pausing `vice_registers_get` up to 5 times with no resume in between | ⚠️ Warning (open per review; outside this phase's own criteria — `ram-capture.mjs` is a criterion-8 relocation target, not new code written by this phase) | Could cause a false fallback to the keyboard-driven LOAD/RUN path if native autostart takes longer than one round trip. Pre-existing behavior, not introduced by this phase's relocation. |

No `TBD`/`FIXME`/`XXX` debt markers and no `TODO`/`HACK`/`PLACEHOLDER` markers found in any of the
phase's modified/relocated production files (`vice-proxy.mjs`, `vice-probe.mjs`, `vice.mjs`,
`vice-pool.mjs`, `vice-session.mjs`, `vice-sync.mjs`, `refresh-manifest.mjs`,
`install-resources.mjs`, `ram-capture.mjs`).

### Human Verification Required

### 1. Accept criterion 3's recorded human-checkpoint evidence as sufficient, and accept the `protocolVersion` deviation

**Test:** Review `01.1-CRITERION-3-EVIDENCE.md` in full and confirm the recorded fresh-session
checkpoint (host emulator running, 63-tool manifest refreshed and committed, 64 `mcp__vice__*` tools
listed with `disk_list` confirmed absent, correct reset-vector byte read) satisfies ROADMAP criterion
3 without a fresh live re-run.
**Expected:** Sign-off that the evidence file is accepted as the criterion-3 proof, and that the
documented `protocolVersion`-not-captured gap (§6 of that file) is knowingly accepted as a deferred
note for a future lifecycle-spike touch of `handleInitialize()`, not a blocker to closing this phase.
**Why human:** Criterion 3 is explicitly designed as a blocking `checkpoint:human-verify` in this
phase's own planning note ("Verification asymmetry ... criterion 3 carries a human checkpoint"), and
this verification run is instructed not to call any `mcp__vice__*` tool (the host emulator is a live,
shared resource). The evidence is strong and internally consistent, but formal acceptance of a
blocking human checkpoint is a human decision by the phase's own design, not something a static
verifier can rubber-stamp on its own authority.

### Gaps Summary

No blocking gaps. All 9 ROADMAP success criteria are verified against the current codebase with
concrete evidence (diagnosis document, `.mcp.json` content, proxy source code, 18+5 targeted tests
plus a 170/170 full-suite run, and the recorded criterion-3 human-checkpoint evidence file). Both
code-review blockers (CR-01 path-traversal bypass, CR-02 `voidRun()` ENOENT) are confirmed fixed at
HEAD with their own regression tests passing. The phase's two deliberate carve-outs — the transport
module relocated rather than deleted (four call sites repointed, confirmed), and
`devcontainer-host-path` NOT retired (still present, still exactly its traced four consumers) — were
both honored, not silently violated.

Three review warnings (WR-01 non-emulator entries in the tool manifest, WR-02 no name-normalization
on the deny-list, WR-03 a boot-detection loop that can't detect delayed progress) and one info item
remain open and unfixed, exactly as the phase's own review documented. None of them contradicts any
of the 9 stated success criteria, so they do not block the phase goal, but WR-01 is worth flagging
prominently: the manifest's "63 tools" figure quietly includes 4 non-emulator entries, so only 59 are
real `vice_*` tools — a fact that should be corrected before it is cited again as ground truth
elsewhere in the project (e.g. CLAUDE.md's "exact 63-tool surface... matches tool-for-tool" line).

The overall status is `human_needed` rather than `passed` solely because criterion 3's evidence is a
recorded human checkpoint from an earlier session that this verification run is prohibited from
re-running live — formal acceptance of that checkpoint's sufficiency belongs to a human decision, per
the item above, not because any artifact or link failed.

---

*Verified: 2026-07-31*
*Verifier: Claude (gsd-verifier)*
