---
status: complete
phase: 01.6.1-container-side-conversion-to-typescript
source: [01.6.1-VERIFICATION.md, 01.6.1-08-PLAN.md]
started: 2026-08-03T17:05:00Z
updated: 2026-08-03T17:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. A fresh agent session sees an unchanged `mcp__vice__*` tool surface
expected: Every tool available before this phase's changes is still callable, with no schema drift, from a session spawned AFTER the conversion landed. Requires a genuinely NEW agent session — not the one that executed this phase.
why_human: The tool surface is snapshotted per-session at spawn time, so no session that ran this phase can observe the post-rewire surface. Phase 01.4, which reaches the tool surface, has not landed. Marked `verification: backstop` in `01.6.1-08-PLAN.md` for exactly this reason.
result: pass
verified_in: `/gsd-verify-work 01.6.1` session of 2026-08-03 — a session spawned AFTER the
  conversion commits landed, against the rewired `.mcp.json` naming
  `.claude/mcp/vice/vice-proxy.ts`. This is the NEW-session observer
  `01.6.1-07-BASELINE.md` §G says the claim requires.
evidence: |
  - This session's `mcp__vice__*` surface enumerates **66 tools**, matching the baseline's
    recorded served count of 66 exactly (`01.6.1-07-BASELINE.md` §4).
  - `diff` of the 66 tool names this session exposes against the 66 names the converted
    proxy serves over a live `tools/list`: **empty — the name sets are identical.** No
    tool lost, none added, none renamed.
  - `vice_disk_list` absent from both the served surface and the session surface, so the
    deny-list holds all the way through to the agent-facing surface.
  - All 66 served tools carry a non-empty `description` and an `inputSchema` object — no
    schema stripped or emptied by the conversion.
  - Offline handshake re-run against `.claude/mcp/vice/vice-proxy.ts` reproduces the
    pre-conversion baseline in every recorded dimension: exit 0, protocol `2025-06-18`,
    server `vice`/`0.1.0`, 66 tools, denied tool absent, identical two stderr lines.
  - A real forwarded `mcp__vice__vice_ping` reached the converted proxy and returned
    invariant message producer #1 (`brokerNeverStartedMessage`) verbatim, proving the
    TypeScript entry point runs, parses and executes its broker-discovery path live —
    not merely that it starts.
note: Closes the backstop `01.6.1-07-BASELINE.md` §G explicitly declined to settle
  ("verifiable only in a NEW agent session, not settled by anything in this document").
  Confidence: HIGH (live, in-session, against the rewired entry point).

### 2. The converted proxy behaves identically under a live MCP session against a real emulator
expected: No behavioral difference between the TypeScript proxy and the pre-conversion JavaScript proxy under real emulator load. Drive `vice-proxy.ts` through its broker-lease, recycle, diagnose and checkpoint-bracket paths.
why_human: Phase 01.4's central finding is that a fully green 268/268 suite coexisted with three tools being uncallable from a real session — so a green `node:test` suite proves language and structural equivalence only, never live behavioral equivalence. Marked `verification: backstop` in `01.6.1-08-PLAN.md`.
result: pass
verified_in: `/gsd-verify-work 01.6.1` session of 2026-08-03, against a real emulator on the
  real host (developer started the host broker mid-session). VICE 3.10 / C64SC.
evidence: |
  All four paths this test names, driven live through `.claude/mcp/vice/vice-proxy.ts`:
  - **broker-lease** — first forwarded call acquired a lease; instance answered
    `{"status":"ok","version":"3.10","machine":"C64SC"}`.
  - **diagnose** — verdict `live`; restart epoch checked at zero emulator cost; bracket
    retired 42,920 cycles (~1,129,474 cycles/s beside the ~991,000 PAL baseline); machine
    left PAUSED afterwards, exactly as the tool's contract states.
  - **checkpoint-bracket** — non-stopping exec checkpoint armed at `$EA31`; hit count
    climbed 0 → 228 across one deliberate resume; stopwatch bracket measured 11,459,448
    cycles; `checkpoint_delete` returned clean.
  - **recycle** — kill stage `sigterm`; epoch 13 → 14 (moved); pre-kill snapshot accepted;
    incident record written BEFORE the kill carrying the supplied reason verbatim, with a
    full evidence block (cycle bracket, `PC $E5D1` register snapshot, armed-checkpoint
    state, screenshot, snapshot attempt) and a correct account of the `$01`/KERNAL-banking
    nuance rather than a guess at the IRQ vector; run correctly declared VOID; instance
    respawned and answered on poll (never on wall-clock delay).
  Emulator left resumed by exactly one deliberate `vice_execution_run` at the end.
limit_on_what_this_proves: |
  These paths were exercised against their documented contracts and the baseline's recorded
  invariants — NOT A/B'd side-by-side against the pre-conversion JavaScript proxy under the
  same load, because `vice-proxy.mjs` no longer exists (it was renamed, not copied). So
  "no behavioral difference" is established as *every named path behaves as specified,
  live*, not as *two implementations observed in parallel*. What Phase 01.4's finding
  demanded — live callability, not suite greenness — is what is now demonstrated.
  Confidence: HIGH for live correctness of all four paths; the parallel-implementation
  comparison is structurally unavailable and is not claimed.

### 3. (Carried forward from 01.6-VERIFICATION.md — not this phase's to close) Host-side launcher and broker.json
expected: On the real host, `tools/vice-launcher.sh` runs; `broker.json` exists with `pid`/`started_at`/`node_version` keys matching the real host's `node --version`; the launcher execs into node and `vice-broker.mjs` runs to completion.
why_human: `lib/container-guard.sh` makes `vice-broker.mjs` / `vice-launcher.sh` refuse to run inside this devcontainer by design; the host is unreachable from this container by construction. Every file phase 01.6.1 touched is container-only, so this phase neither closes nor needed to close this pair of Phase 01.6 backstop truths. Restated here only so it is not lost.
result: skipped
reason: "Not this phase's to close — a Phase 01.6 backstop truth restated here for bookkeeping only. It remains correctly open in `01.6-VERIFICATION.md`, which scores 01.6 at 12/14 on exactly this basis and states at line 130 that the pair 'must not be marked passed or failed — it is an honest abstention, not a defect.' Developer disposition, `/gsd-verify-work 01.6.1`, 2026-08-03."
investigated_live: |
  Not abstained blindly. The host broker WAS up during this session, so the live
  `.vice-supervisor/broker.json` was read directly (it sits in the mounted workspace):
  - `pid` present (`1144457`), `started_at` present (`2026-08-03T17:15:13Z`),
    `node_version` **ABSENT**.
  - `"written_by": "vice-broker.sh"` — the record was written by the **2,103-line bash
    daemon** (`resources/vice-broker.sh`), which does not emit `node_version`. That absence
    is correct behaviour for the bash writer, NOT a defect, and NOT evidence about the
    `vice-launcher.sh` → `node vice-broker.mjs` path this truth actually names.
  So the broker being up does not satisfy this test: the Node path never wrote this record.
why_not_retried_with_a_fresh_broker: |
  Considered and rejected on source evidence (logged to `.planning/RE-FINDINGS.md`,
  2026-08-03, as a dead end + hazard):
  1. `vice-broker.mts:132-140` refuses to write if the existing record names a LIVE pid, and
     separately refuses if it carries `heartbeat_at`. Both held. The launcher cannot write a
     `node_version` record at all while a real broker is alive.
  2. `vice-broker.mts` is a 190-line TRACER, not a broker — no `setInterval`, no listener, no
     poll loop; `main()` writes one record and returns. Phase 01.6's "prove the build
     topology on one real module" artifact.
  3. `vice-broker.mts:14-20`: it "DELIBERATELY OMITS heartbeat_at" because a heartbeat-less
     record reads as `never_started` to every session's proxy. The only state in which the
     launcher CAN write is also one in which it leaves no daemon and misinforms the next
     session.
  Net: the remedy would trade a working broker for a misleading record, to close a 01.6 item
  during 01.6.1's UAT. The field arrives honestly in **Phase 01.6.2**, which replaces the
  bash daemon with a TypeScript process that genuinely is the broker.

### 4. Review the two behaviour-affecting decisions taken beyond a pure port
expected: Developer accepts (or reverses) the two changes that went past language-level conversion, both recorded in `01.6.1-06-SUMMARY.md`: (a) `refresh-manifest.ts`'s manifest write was tightened to the atomic tmp-then-rename idiom, closing a gap the module's own doc comment already claimed was closed; (b) `refresh-manifest.ts` gained an exported entry function plus an entry-point guard so it could be tested at all. Both are reversible. Both were taken under the CONTEXT file's Claude's-Discretion delegation rather than by a developer decision.
why_human: A judgement call about scope, not a fact about the tree. Source is `01.6.1-08-PLAN.md`'s Task 2 `<human-check>` block, not `01.6.1-VERIFICATION.md` — recorded here so the plan's own declared manual item is not lost when the plan is archived.
result: pass
disposition: Developer ACCEPTED both decisions as taken. Neither is reversed; no follow-up work.
reviewed_against: |
  Facts presented at the checkpoint, read from the tree rather than from the summary alone:
  - **Decision 1 (atomic write)** — `refresh-manifest.ts:44-48` performs
    `tmp sibling → chmod 0o600 → content → rename`, shape copied verbatim from
    `vice-broker.mts`'s `writeBrokerRecord()`. Pre-conversion was a bare single-shot
    `writeFileSync`. The module's own long-standing doc comment already promised "a partial
    or empty manifest is never written over a good one on failure", but the old code honoured
    that only against a rejected handshake — not against a crash or full disk mid-write. The
    change closes a real doc-comment-vs-behaviour gap. Fourth file in the phase to take this
    idiom (after `vice-broker.mts`, `install-resources.ts`, `incident-record.ts`).
  - **Decision 2 (exported, guarded entry)** — `main()` is exported and guarded at
    `refresh-manifest.ts:97` by the same is-this-the-entry-point check `vice-broker.mts`'s CLI
    uses. Pre-conversion `main()` ran unguarded at module load, so the module could not be
    imported by a test without performing a handshake and writing a manifest. Verified live in
    the phase via a spawned subprocess (the only way to exercise a genuinely fresh module
    evaluation): importing prints NOOP, exits 0, writes no manifest anywhere.
  - Both rated `reversible` in `01.6.1-06-PLAN.md`; both taken under the CONTEXT file's
    Claude's-Discretion delegation; both covered by the module's 7 first-ever tests.

## Summary

total: 4
passed: 3
issues: 0
pending: 0
skipped: 1
blocked: 0

**Session note (2026-08-03).** Two of the four items were `verification: backstop` claims the
phase's own audit declared unclosable from inside the container. Both were closed live in this
session rather than confirmed on trust: the session running this UAT was itself spawned AFTER
the conversion landed (closing `01.6.1-07-BASELINE.md` §G's NEW-session requirement), and the
developer started the host broker mid-session, making a real emulator available for the
four-path live drive. The one remaining abstention (test 3) is another phase's open item and was
investigated to the point of establishing WHY it cannot be closed today — see its
`why_not_retried_with_a_fresh_broker` block and the matching `.planning/RE-FINDINGS.md` entry.

## Gaps
