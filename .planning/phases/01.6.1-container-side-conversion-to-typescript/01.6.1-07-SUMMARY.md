---
phase: 01.6.1-container-side-conversion-to-typescript
plan: 07
subsystem: infra
tags: [typescript, vice-mcp, mcp-json, stdio-handshake, node-test, tsc, discriminated-union, isPlainObject]

# Dependency graph
requires:
  - phase: 01.6.1-container-side-conversion-to-typescript
    provides: "vice-broker-client.ts, vice.ts, refresh-manifest.ts, vice-sync.ts converted; every one of the hub's 7 internal dependencies already .ts; 275/275 baseline (01.6.1-06)"
provides:
  - "vice-proxy.ts -- the hub (~2,443 lines) converted to TypeScript: named interfaces for JSON-RPC request/response/error shapes, tool-call params, the continuation store, checkpoint/bracket/IRQ-handler/evidence shapes, and every broker-lease/path-rewrite result shape; isPlainObject() narrowing at every stdin/host-reply boundary instead of casting; discriminated unions for the null-shaped failure returns the untyped code used by convention"
  - ".mcp.json's args[0] rewired to vice-proxy.ts, in the SAME commit as the rename"
  - "The rewire proven by a real, offline stdio MCP handshake against the exact path .mcp.json now names -- not inferred from the executor's own already-snapshotted session"
  - "01.6.1-07-BASELINE.md -- a mechanically-produced, dated invariant baseline (93 declarations, 9 message producers, 5 bounded constants, 4 deny-list sites, an offline handshake transcript) captured before the rename and diffed against after"
  - "vice-proxy.test.mjs held constant as an unchanged 4,370-line oracle, touched in exactly one line (its PROXY_PATH constant)"
  - "vice-broker.test.mjs (out of this phase's scope) touched in exactly one line (its own PROXY_PATH constant, broken by the same rename) -- the Wave 5 precedent for this file, applied a second time"
affects: ["01.6.1-08"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "isPlainObject() narrowing (vice-broker.mts's own idiom, already used by every other converted file this phase) applied at every JSON boundary in the hub: handleInitialize's params, readManifestTools' parsed JSON, bytesFromMemoryReadResult's memory-read result, gatherCheckpointTrapEvidence's checkpoint-list/registers results, cyclesFromStopwatchResult's stopwatch result, pathArgsFor's manifest schema properties, handleMessage's parsed stdin line, handleToolsCall's params"
    - "Discriminated unions for previously null-shaped failure returns: ToolCallResult (ErrorTextResult | OkTextResult), BrokerLeaseResult ({ok:true} | {ok:false;message}), CaptureStepResult<T> ({available:true;value:T} | {available:false;reason})"
    - "Contextual typing via a const-bound function EXPRESSION (`const f: (args: T) => R = async function f(args) {...}`), used ONLY for handleRecycle -- the one function whose bare, untyped parameter list text (`handleRecycle(args)`) is scanned verbatim by vice-proxy.test.mjs's own structural oracle. Types the parameter via the variable's own annotation with zero characters changed inside the parens the oracle greps for. New pattern for this codebase; see RE-FINDINGS.md for the two rejected alternatives (function-declaration overloads, JSDoc @param) and why each fails noImplicitAny in a plain .ts file."
    - "Per-entry type cast (`{...} as SeamHazardEntry`) inside a heterogeneous array literal, instead of annotating the array/const declaration itself -- keeps SEAM_HAZARDS's own declaration and closing-bracket lines byte-identical (both scanned by the oracle) while still giving the array's inferred element type the shared interface, so the file's own later TEST-ONLY .push() of a structurally different entry type-checks without a second cast."
    - "Method-shorthand interface members (`detect(...): unknown` not `detect: (...) => unknown`) for SeamHazardEntry, deliberately choosing TypeScript's bivariant method-parameter check so each table entry's own narrower detect()/render() pair (e.g. CheckpointArmingHazardDetection, not unknown) slots into the shared, heterogeneous table without weakening either function's own precise type."

key-files:
  created:
    - .planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-07-BASELINE.md
  modified:
    - .claude/mcp/vice/vice-proxy.ts (renamed from .mjs; fully typed, logic/messages/constants/comments unchanged)
    - .mcp.json (args[0] repointed to vice-proxy.ts)
    - .claude/mcp/vice/vice-proxy.test.mjs (one line: PROXY_PATH)
    - .claude/mcp/vice/vice-broker.test.mjs (one line: PROXY_PATH -- out of phase scope, Wave 5 precedent)
    - .planning/RE-FINDINGS.md (three append-only entries: the stdio-handshake verification recipe, the oracle-preserving contextual-typing pattern, a flaky-test observation)

key-decisions:
  - "isPlainObject was added as a NEW top-level function -- the one addition to the 93-name declaration list must_have B nominally forbids. Kept, not inlined: it is the narrowing predicate the plan's own typing guidance mandates ('Narrow every value that arrives from stdin or from a parsed host reply through a type predicate before touching its fields'), copied verbatim in shape from vice-broker.mts and from the same-named predicate every other file in this phase already introduced. The alternative (inlining the 3-clause check at ~10 call sites to preserve a literal headcount) would have made the port less faithful to this phase's own established idiom for the sake of the headcount the invariant exists to protect, not the invariant itself. Recorded in 01.6.1-07-BASELINE.md's Task 3 audit as a found-and-resolved difference, not silently passed over."
  - "Four declarations (handleRecycle, runCycleBracket, DIAGNOSE_VERDICTS, SEAM_HAZARDS's own declaration line, CHECKPOINT_ARMING_TOOLS) kept their pre-conversion, UNTYPED declaration text byte-for-byte, because vice-proxy.test.mjs's own structural tests scan the source for those exact substrings (confirmed live: adding any inline annotation broke five separate tests loudly). Each is still fully, checkedly typed -- via contextual typing on a const/function-expression pair (handleRecycle), plain inference from the initializer (runCycleBracket, DIAGNOSE_VERDICTS, CHECKPOINT_ARMING_TOOLS), or a per-entry cast inside the array literal (SEAM_HAZARDS) -- never via `any` or a suppression comment. This is the sixth extension/format-hardcoded-check hazard this phase has caught, and the first one that failed LOUDLY rather than silently."
  - "vice-broker.test.mjs's own PROXY_PATH constant was touched in exactly one line, matching Wave 5's own precedent for this identical file (RESEARCH §5 / 01.6.1-05-SUMMARY.md: 'exactly one line changed... its cross-language parity test still passes'). This file is explicitly out of this phase's conversion scope, but it spawns a real vice-proxy.mjs child process end-to-end (its 'tracer' test), a coupling RESEARCH's own dependency graph did not name -- discovered only when the full-suite run failed with MODULE_NOT_FOUND after the rename. Rule 3 (auto-fix blocking issues, missing referenced file) applies: the rename caused this, the fix is the minimum one-line correction, and it is documented here rather than silently absorbed."
  - "The .mcp.json rewire was verified by a REAL, offline stdio MCP handshake against the exact path the config now names (piping initialize + tools/list into `node .claude/mcp/vice/vice-proxy.ts` under a timeout, reading stdout/stderr/exit code) -- never by 'the current session still works,' which the plan's own RE-FINDINGS citation proves is worthless evidence (the harness snapshots the proxy per session at spawn time)."

patterns-established:
  - "The const-bound function-expression trick for typing a parameter list a structural test oracle scans verbatim -- to be reused in Plan 08 or any later maintenance of vice-proxy.ts/vice-proxy.test.mjs if another such conflict surfaces."

requirements-completed: []

# Coverage metadata
coverage:
  - id: D1
    description: "vice-proxy.mjs converted to TypeScript (named JSON-RPC/tool-call/continuation/checkpoint interfaces, isPlainObject narrowing, discriminated unions for null-shaped failures), logic/messages/constants/comments unchanged"
    verification:
      - kind: unit
        ref: "node --test '.claude/mcp/vice/'*.test.* (275 tests: 270 pass, 5 todo, 0 fail -- including vice-proxy.test.mjs's own 85 tests, unchanged apart from its one PROXY_PATH line)"
        status: pass
      - kind: unit
        ref: "npm --prefix .claude/mcp/vice run typecheck"
        status: pass
    human_judgment: false
  - id: D2
    description: ".mcp.json rewired to vice-proxy.ts in the same commit as the rename, and the rewire proven by a real offline stdio MCP handshake against that exact path"
    verification:
      - kind: integration
        ref: "printf initialize+tools/list JSON-RPC lines | timeout 10 node .claude/mcp/vice/vice-proxy.ts -- exit 0, protocol 2025-06-18, server vice/0.1.0, 66 tools, vice_disk_list absent, zero host I/O, identical to the Task 1 pre-rename baseline transcript (01.6.1-07-BASELINE.md §A/§4)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Invariant baseline captured before the rename (93 declarations, 9 message producers, 5 bounded constants, 4 deny-list sites, offline handshake transcript) and diffed against the post-rename file"
    verification:
      - kind: other
        ref: "01.6.1-07-BASELINE.md -- Task 1 (pre-rename capture) + Task 3 (post-rename audit); one declaration-list difference found (isPlainObject, a new narrowing helper) and explicitly resolved/justified, not silently passed over"
        status: pass
    human_judgment: false
  - id: D4
    description: "The live-session claim (\"a real agent session started after this change can call every mcp__vice__ tool it could before\") is carried forward as an explicit backstop, not settled by this plan's own automated checks"
    verification: []
    human_judgment: true
    rationale: "Per RE-FINDINGS.md's own per-session tool-schema snapshot mechanism, no in-container check can prove a fresh session's tool surface without actually starting a fresh session, which this executor's own session cannot do on itself. Phase 01.4's own central finding is that a fully green suite has coexisted with real tools being uncallable from a live session -- a human (or a fresh orchestrator session) must confirm this the next time a session genuinely restarts."

# Metrics
duration: ~48min (commit span only, 13:46:12Z -> 14:34:15Z; excludes research/reading time and the ~4-5min the full vice-proxy.test.mjs subprocess-spawning suite itself takes per run)
completed: 2026-08-03
status: complete
---

# Phase 01.6.1 Plan 07: Convert the Hub and Rewire .mcp.json Summary

**vice-proxy.mjs (2,443 lines, the container's MCP entry point) is now TypeScript with real, checked types at every JSON-RPC and host-reply boundary; `.mcp.json` names it in the same commit; and the rewire is proven by an actual offline stdio MCP handshake against the exact path the config now points at -- not inferred from a session that cannot see its own config change.**

## Performance

- **Duration:** ~48 min across three task commits (13:46:12Z -> 14:34:15Z); research/reading time (this plan's own PLAN.md, the 01.6.1-05/06 SUMMARYs, RESEARCH.md, PATTERNS.md, RE-FINDINGS.md, the full pre-conversion source of vice-proxy.mjs and vice-proxy.test.mjs) and the repeated full-suite runs (each ~45-50s, several needed to isolate and fix the structural-oracle conflicts below) both took materially longer than this commit-span figure shows.
- **Started:** 2026-08-03T13:46:12Z (Task 1 commit)
- **Completed:** 2026-08-03T14:34:15Z (Task 3 commit)
- **Tasks:** 3 (all completed, all `tdd="false"`)
- **Files modified:** 6 (1 renamed `.mjs` -> `.ts` and fully typed, 1 new baseline doc, 1 config rewired, 2 test files touched in exactly one line each, 1 RE-FINDINGS append)

## Accomplishments

- **Task 1:** Captured a mechanically-produced, dated invariant baseline (`01.6.1-07-BASELINE.md`) BEFORE touching anything: the sorted 93-name declaration list (every top-level `const`/`function`/`async function`/`class` in the pre-rename `.mjs`), all nine agent-facing message producers by name and line, all five bounded-behaviour constants and their values (`REQUIRED_MAX_MCP_OUTPUT_TOKENS=25000`, `OUTPUT_CHAR_CAP` default `500000`, `CYCLE_BRACKET_MAX=2`, `PATH_REWRITE_MAX_DEPTH=10`, `MAX_CONTINUATIONS=5`), all four deny-list enforcement sites by file/line, and a live offline handshake transcript (`initialize` + `tools/list` piped into bare `node vice-proxy.mjs`: protocol `2025-06-18`, server `vice`/`0.1.0`, 66 tools, `vice_disk_list` absent, zero host I/O, exit 0). `node_modules` was restored via `npm ci` against the committed lockfile first (the known per-worktree hazard).
- **Task 2:** `git mv vice-proxy.mjs vice-proxy.ts`, added full TypeScript typing throughout -- named interfaces for the JSON-RPC request/response/error-response shapes and the tool-call-parameter shape, `isPlainObject()` narrowing at every stdin/host-reply boundary (never a bare cast), discriminated unions everywhere a function previously signalled failure via a null-shaped convention (`ToolCallResult`, `BrokerLeaseResult`, `CaptureStepResult<T>`), and named interfaces for every composite shape the file passes around (`CheckpointInfo`, `CheckpointTrapEvidence`, `IrqHandlerResolution`, `CycleBracketResult`, `BracketEvidence`, `PathResolution`, `ContinuationEntry`, `SeamHazardEntry`, `CheckpointArmingHazardDetection`). `.mcp.json`'s `args[0]` was rewired to `vice-proxy.ts` in the SAME commit. `vice-proxy.test.mjs`'s one import-path constant (`PROXY_PATH`) was updated -- `git diff --numstat` confirms exactly `1 1`. Every doc comment, every agent-facing message string, every bounded constant's value, and the chunking-not-truncating behaviour at the output ceiling were ported verbatim; broker leasing, epoch/liveness handling, recycle/diagnose, deny-list enforcement, path rewriting, incident capture and the seam-hazard table are logic-identical, types added.
- **The single hardest sub-problem:** five of `vice-proxy.test.mjs`'s own structural tests scan the SOURCE FILE for exact, untyped declaration substrings (`handleRecycle`'s bare parameter list, `runCycleBracket`'s bare `()`, `DIAGNOSE_VERDICTS`'/`CHECKPOINT_ARMING_TOOLS`'s untyped `const NAME = ...` form, `SEAM_HAZARDS`'s own declaration and closing-bracket lines) -- adding any inline type annotation there broke those tests LOUDLY, the first time this phase's recurring "extension/format-hardcoded check" hazard has failed loudly rather than silently. Two tested, live-verified fixes resolved all five without touching the test file: a contextually-typed `const f: (args: T) => R = async function f(args) {...}` for `handleRecycle` (types the parameter via the variable's own annotation with zero characters changed inside the parens the oracle greps for -- two alternatives, function-declaration overloads and a JSDoc `@param` comment, were tried and both still fail `noImplicitAny` in a plain `.ts` file, confirmed live), and dropping the type annotation on the other four declarations entirely (their own initializers -- `Object.freeze([...])`, `new Set([...])`, and a per-entry `as SeamHazardEntry` cast inside the array literal -- already infer a concrete, checked type with no annotation needed on the declaration line itself).
- **A second, genuinely unanticipated cross-file coupling:** `vice-broker.test.mjs` (explicitly out of this phase's conversion scope -- it tests the still-bash broker, per RESEARCH's own exclusion table) hardcodes its own `PROXY_PATH = join(HERE, "vice-proxy.mjs")` to spawn a REAL end-to-end tracer test, a dependency RESEARCH's dependency graph never named. This broke with `MODULE_NOT_FOUND` after the rename. Fixed with the identical one-line discipline Wave 5 already established for this same file (`git diff --numstat` confirms `1 1`).
- **Task 3:** Re-drove the SAME offline handshake against the rewired `.mcp.json` path (`.claude/mcp/vice/vice-proxy.ts`) and diffed the result against Task 1's baseline: identical exit code, protocol version, server info, tool count, and denied-tool absence. Diffed the sorted declaration-name list: one addition found (`isPlainObject`, the narrowing predicate this plan's own typing guidance mandates), explained and kept rather than silently inlined away or silently accepted without comment. Confirmed all nine message producers, all five bounded constants, and all four deny-list sites survived by name/value. Full suite reconfirmed at 275 (270 pass, 5 todo, 0 fail); typecheck exits 0.
- **Offline handshake transcript, quoted exactly (Task 3, against the rewired `.ts` entry point):** `protocolVersion: "2025-06-18"`, `serverInfo: {name: "vice", version: "0.1.0"}`, tool count `66`, `vice_disk_list` absent from the returned list, exit code `0`, zero host I/O (confirmed via stderr, which carried only the two expected startup lines and nothing naming a connection attempt) -- byte-for-byte the same as Task 1's pre-rename transcript.
- **Test-count arithmetic:** 275 before this plan (270 pass, 5 todo, 0 fail, per 01.6.1-06's own closing baseline) -> 275 after (270 pass, 5 todo, 0 fail). No net change: this plan adds real type coverage and structural-test-preserving rewrites to an already-passing suite, not new test cases.
- **The backstop this plan explicitly does NOT claim to have proven:** whether a real agent session started after this change can call every `mcp__vice__` tool it could before. Per RE-FINDINGS.md's own confirmed per-session tool-schema snapshot mechanism, this executor's own session was already snapshotted before this change landed and cannot observe it either way -- and Phase 01.4's own central finding is that a fully green suite has coexisted with real tools being uncallable from a live session. This is carried forward as an explicit, unresolved backstop (coverage D4 above), not inferred from the green suite reported here.

## Task Commits

Each task was committed atomically (all `tdd="false"`, single commit each):

1. **Task 1: Capture the invariant baseline BEFORE anything is renamed** - `ca411b7` (docs)
2. **Task 2: Convert the hub and rewire .mcp.json in the same commit** - `07a83e9` (feat)
3. **Task 3: Equivalence audit -- the rewired entry point really starts, and every recorded invariant survived** - `8d94369` (docs)

**Plan metadata:** this SUMMARY's own commit (made immediately after this file is written).

## Files Created/Modified

- `.planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-07-BASELINE.md` (new) -- the pre-rename invariant capture (Task 1) plus the post-rename equivalence audit (Task 3), both mechanically produced against the real files, not recalled
- `.claude/mcp/vice/vice-proxy.ts` (renamed from `.mjs`, 85% textual similarity per `git diff --summary -M`) -- the hub, fully typed: named JSON-RPC/tool-call/continuation/checkpoint/bracket/evidence interfaces, `isPlainObject()` narrowing throughout, discriminated unions for prior null-shaped failure returns; broker leasing, epoch/liveness handling, recycle/diagnose, deny-list enforcement, path rewriting, incident capture, and every doc comment/message string/bounded constant ported verbatim
- `.mcp.json` -- `mcpServers.vice.args[0]` repointed from `vice-proxy.mjs` to `vice-proxy.ts`
- `.claude/mcp/vice/vice-proxy.test.mjs` -- one line (`PROXY_PATH`'s target), confirmed via `git diff --numstat` as exactly `1 1`
- `.claude/mcp/vice/vice-broker.test.mjs` -- one line (its own, separately-hardcoded `PROXY_PATH`), confirmed via `git diff --numstat` as exactly `1 1`; out of this phase's conversion scope, touched only because this file's real end-to-end tracer test spawns the renamed file directly
- `.planning/RE-FINDINGS.md` -- three append-only entries: the stdio-handshake verification recipe (reusable for any future `.mcp.json` rewire), the oracle-preserving contextual-typing pattern (with the two rejected alternatives and why each fails), and the flaky-test observation for `vice-broker.test.mjs`'s stderr-timing-sensitive assertion

## Decisions Made

See `key-decisions` in the frontmatter above for the full account of each. Summarized:

- **`isPlainObject` was added as a new top-level function, not inlined away to preserve a literal declaration-count match.** It is the narrowing predicate this plan's own typing guidance requires and every other converted file in this phase already carries under the same name; inlining it ~10 times to dodge a headcount would have made the port worse, not more faithful. Recorded as a found-and-resolved difference in the Task 3 audit, not silently passed over.
- **Four declarations kept their pre-conversion, untyped declaration text byte-for-byte** (`handleRecycle`, `runCycleBracket`, `DIAGNOSE_VERDICTS`, `SEAM_HAZARDS`'s declaration line, `CHECKPOINT_ARMING_TOOLS`) because `vice-proxy.test.mjs`'s own structural tests scan the source for those exact substrings. Each is still fully, checkedly typed via contextual typing, inference, or a per-entry cast -- never via `any` or a suppression comment. This is the sixth extension/format-hardcoded-check hazard this phase has caught, and the first that failed loudly.
- **`vice-broker.test.mjs`'s own `PROXY_PATH` was touched in exactly one line**, matching Wave 5's identical-shape precedent for this same out-of-scope file, per Rule 3 (a missing referenced file the rename caused).
- **The `.mcp.json` rewire was verified by a real, offline stdio MCP handshake**, never by "the session still works" -- which this project's own RE-FINDINGS entry proves is worthless evidence for this exact check.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `vice-broker.test.mjs`'s own hardcoded `PROXY_PATH` broke after the rename, an out-of-scope-file coupling RESEARCH's dependency graph never named**
- **Found during:** Task 2's own full-suite verification run
- **Issue:** `vice-broker.test.mjs` (explicitly out of this phase's scope per RESEARCH §1's exclusion table) spawns a real `vice-proxy.mjs` child process for its own end-to-end "tracer" test, via a separately-hardcoded `const PROXY_PATH = join(HERE, "vice-proxy.mjs")` at line 38. This broke with `MODULE_NOT_FOUND` once the file was renamed.
- **Fix:** Changed the one line to `vice-proxy.ts`, matching the Wave 5 precedent already established for this exact file/pattern (`01.6.1-05-SUMMARY.md`: "Touched vice-broker.test.mjs (explicitly out of this phase's scope) in exactly one line: its import specifier").
- **Files modified:** `.claude/mcp/vice/vice-broker.test.mjs`
- **Verification:** `git diff --numstat` confirms exactly `1 1`; full suite re-run reconfirms 275/270/5/0.
- **Committed in:** `07a83e9` (Task 2's commit -- the fix landed in the same commit as the rename, since it is a direct, mechanical consequence of the rename and leaving it split across commits would leave an intermediate commit with a broken test)

**2. [Rule 1 - Bug] Five of `vice-proxy.test.mjs`'s own structural tests broke loudly on inline type annotations, requiring two oracle-preserving typing patterns rather than a straightforward annotation**
- **Found during:** Task 2, first full-suite verification after adding types
- **Issue:** `handleRecycle`, `runCycleBracket`, `DIAGNOSE_VERDICTS`, `SEAM_HAZARDS`, and `CHECKPOINT_ARMING_TOOLS` each had a structural test scanning the source for their exact, pre-conversion declaration substring. Annotating any of them broke that test with a clear "definition not found in source" / "found 0" assertion failure.
- **Fix:** `handleRecycle` converted to a contextually-typed `const f: (args: T) => R = async function f(args) {...}` (types the parameter via the variable's own annotation, zero characters changed inside the parens the oracle scans); the other four had their type annotations dropped entirely, relying on inference from their own initializers (`Object.freeze([...])`, `new Set([...])`) or a per-entry `as SeamHazardEntry` cast inside the array literal for `SEAM_HAZARDS`. No behavior change in any case -- every value is still fully, checkedly typed.
- **Files modified:** `.claude/mcp/vice/vice-proxy.ts` only (the test file was never touched for this)
- **Verification:** `npx tsc --noEmit` exits 0; `node --test vice-proxy.test.mjs` reports 85/85 pass, 0 fail (was 1 fail after the first typing pass, then a second failure on `CHECKPOINT_ARMING_TOOLS` surfaced and was fixed the same way; both confirmed resolved by isolated re-runs before the full-suite re-run).
- **Committed in:** `07a83e9` (Task 2's own commit; these fixes preceded the commit, so no separate commit was needed)

---

**Total deviations:** 2 auto-fixed (1 Rule 3 - blocking cross-file coupling, 1 Rule 1 - bug/hazard in the conversion's own interaction with the test oracle). No scope creep: both fixes stayed inside the minimum file(s) each required, and both are the SAME class of fix (a one-line or oracle-preserving-pattern correction) this phase has already established precedent for.

## Issues Encountered

Both covered above under Deviations. One additional non-issue worth recording: a single full-suite run reported a transient failure in an unrelated `vice-broker.test.mjs` test (a stderr-wording assertion against the real bash `vice-broker.sh`'s startup warning) that is not attributable to this plan's changes -- confirmed by an isolated re-run passing in 46ms and a subsequent full-suite re-run reproducing the clean 275/270/5/0 result. Logged to RE-FINDINGS.md as a flaky-test observation, not fixed (nothing in this plan's scope touches `vice-broker.sh` or `vice-broker.mts`).

## User Setup Required

None -- no external service configuration required.

## Next Phase Readiness

- Slice 9 (RESEARCH §2) is now fully complete: both `vice-proxy.mjs` (this plan) and `vice-proxy.test.mjs` (Plan 08's own subject, held constant here) exist, with the source half converted and the test half proven as an unchanged oracle across the conversion.
- **The `.mjs` set under `.claude/mcp/vice/` is now exactly two files: `vice-proxy.test.mjs` (Plan 08's subject) and `vice-broker.test.mjs` (permanently out of this phase's scope, per RESEARCH's own exclusion table).** Confirmed via `ls .claude/mcp/vice/*.mjs` immediately before this SUMMARY was written.
- Both Wave 2 guards (`load-order.test.ts`'s emptied cycle allowlist and module-scope call-site guard) are unaffected: `vice-proxy.ts` does not participate in the repo-root cycle and its own `repoRoot()` calls are all inside function bodies, never at module scope.
- **The two oracle-preserving typing patterns this plan established (contextual typing via a const-bound function expression; a per-entry cast inside a heterogeneous array literal) are directly reusable by Plan 08** if converting `vice-proxy.test.mjs` itself surfaces the same class of conflict against any of ITS OWN structural self-checks.
- **The one open backstop this plan explicitly could not close:** whether a real agent session started after this change can call every `mcp__vice__` tool it could before. This requires a genuinely fresh session (per RE-FINDINGS.md's own per-session snapshot mechanism) and is not something this plan's own automated suite, or this executor's own session, can settle. Recorded as coverage item D4 with `human_judgment: true`.
- Full suite is 275 total (270 pass, 5 todo, 0 fail); typecheck exits 0 with no edit to `tsconfig.json`/`tsconfig.build.json`/`package.json` throughout, matching the plan's explicit prohibition on either.

## Self-Check: PASSED

`test -f .claude/mcp/vice/vice-proxy.ts` succeeds; `test ! -e .claude/mcp/vice/vice-proxy.mjs` succeeds. `.mcp.json`'s `mcpServers.vice.args[0]` reads `.claude/mcp/vice/vice-proxy.ts`, and that path resolves to an existing file. All 3 claimed commit hashes (`ca411b7`, `07a83e9`, `8d94369`) confirmed present via `git log --format='%H %aI %s'`. `git diff --numstat` for both touched test files (`vice-proxy.test.mjs`, `vice-broker.test.mjs`) confirmed as exactly `1 1` each. Full suite re-run immediately before writing this SUMMARY: 275/270/5/0; `npx tsc --noEmit -p tsconfig.json` exit 0.

---
*Phase: 01.6.1-container-side-conversion-to-typescript*
*Completed: 2026-08-03*
