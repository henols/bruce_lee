---
phase: 01.6.1-container-side-conversion-to-typescript
plan: 05
subsystem: infra
tags: [typescript, vice-mcp, node-test, tsc, broker-protocol, transport-seam, c7, never-throw]

# Dependency graph
requires:
  - phase: 01.6.1-container-side-conversion-to-typescript
    provides: "containerpath.ts, incident-record.ts converted, the atomic-write mode-restriction gap closed, 254/254 baseline (01.6.1-04)"
provides:
  - "vice-broker-client.ts, vice-broker-client.test.ts converted -- REQUEST_ID_PATTERN survives as a real, explicitly-typed, named export with an unchanged value (criterion C7's container-side deliverable, verified byte-identical before/after)"
  - "vice-broker.test.mjs (explicitly out-of-scope) touched in exactly one line -- its import specifier -- with its cross-language parity test still passing"
  - "vice.ts, vice.test.ts converted -- export list proven byte-identical before/after (15 names), no CLI process.argv block reintroduced (D-05 stays deleted), deny-list filter still strips at discovery"
  - "vice-proxy.mjs's two remaining Wave-5 specifiers repointed (vice-broker-client.ts, vice.ts), plus its fourth module enumerator (vice-proxy.test.mjs's network-call-construct offenders array, flagged since Wave 1) and its DENY_LIST-reading regex updated to track vice.ts's rename"
  - "vice-sync.mjs, refresh-manifest.mjs, repo-root.test.ts's dynamic-import URL literal all repointed to vice.ts"
affects: ["01.6.1-06", "01.6.1-07", "01.6.1-08"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Named interface/type declarations per converted module (RequestRecord, RecycleRequestRecord, LeaseRecord, BrokerLivenessResult, PollGrantResult/PollRecycleAckResult as discriminated unions; ActiveInstance, ViceErrorOptions, MachineRestartedErrorOptions, EpochResult, SessionInfo, AssertSameMachineOptions, ToolInfo/ServerInfoPayload), mirroring vice-broker.mts's idiom, applied a fourth/fifth time in this phase"
    - "isPlainObject() type-predicate narrowing reused verbatim across vice-broker-client.ts's readJsonMaybe() and vice.ts's rpc()/readEpoch()/serverInfo()/assertSameMachine() JSON-parsing sites -- one narrowing idiom, applied everywhere a parsed JSON-RPC or epoch-file value is touched"
    - "useInstance()'s port/url/epochFile made required fields (cost-free strict-mode tightening, matching Plan 03/04's resourcesStatus()/containerizeRecord() precedent) since every real production call site (vice-proxy.mjs) already supplies all three; the one test call site that previously omitted epochFile now passes it through explicitly"
    - "Plain ViceError/MachineRestartedError classes extending Error, with fields assigned in the constructor body rather than via constructor parameter properties (erasableSyntaxOnly forbids the latter)"
    - "A scoped, narrow cast at point-of-use (result as { content?: ... } | undefined) documented inline for the one genuinely-arbitrary JSON-RPC result shape call() must destructure, rather than typing rpc()'s return as anything more specific than unknown"

key-files:
  created: []
  modified:
    - .claude/mcp/vice/vice-broker-client.ts (renamed from .mjs)
    - .claude/mcp/vice/vice-broker-client.test.ts (renamed from .mjs)
    - .claude/mcp/vice/vice-broker.test.mjs (one import specifier line changed; nothing else)
    - .claude/mcp/vice/vice.ts (renamed from .mjs)
    - .claude/mcp/vice/vice.test.ts (renamed from .mjs; one call site now passes epochFile explicitly)
    - .claude/mcp/vice/vice-proxy.mjs (four specifiers repointed across the two tasks, plus stale prose)
    - .claude/mcp/vice/vice-proxy.test.mjs (two specifiers repointed; network-call-construct offenders array and DENY_LIST regex updated for the vice.ts rename)
    - .claude/mcp/vice/vice-sync.mjs (specifier repointed)
    - .claude/mcp/vice/refresh-manifest.mjs (specifier repointed)
    - .claude/mcp/vice/repo-root.test.ts (VICE_MODULE_URL literal repointed)
    - .planning/RE-FINDINGS.md (append-only entry for the missing-node_modules worktree hazard)

key-decisions:
  - "C7's container-side deliverable landed as specified: REQUEST_ID_PATTERN kept its exact regex source and became an explicitly-typed (`RegExp`), named export. The out-of-scope vice-broker.test.mjs got exactly one line changed (`git diff --numstat` confirms `1  1`), and its cross-language parity test (shell script vs. isValidRequestId()) still passes -- proving both copies still agree on every corpus entry without this plan touching the shell side at all."
  - "useInstance()'s port/url/epochFile tightened from implicitly-optional to required, matching Plan 03/04's resourcesStatus()/containerizeRecord() precedent for cost-free strict-mode tightening: the one real production caller (vice-proxy.mjs's recycle-path useInstance() call) already supplies all three every time. vice.test.ts's own call previously omitted epochFile (silently setting the module's internal activeEpochFile to undefined for the test's duration, harmlessly since nothing read it); it now passes originalInstance.epochFile through explicitly -- zero change to any assertion, and the type surface no longer has to lie about what the runtime state can be."
  - "vice-proxy.test.mjs's DENY_LIST-reading regex was widened, not just its target filename: the pre-conversion pattern `/export const DENY_LIST = \\[([^\\]]*)\\]/` would have gone silently vacuous the moment DENY_LIST gained its `: readonly string[]` type annotation (a fifth instance of this phase's own 'extension/format-hardcoded assertion goes vacuous across a rename' hazard class, this time keyed on syntax shape rather than filename). Widened to `/export const DENY_LIST(?:\\s*:\\s*[^=]+)?\\s*=\\s*\\[([^\\]]*)\\]/`, tested live against the real converted file."
  - "vice-proxy.test.mjs's fourth module enumerator (the network-call-construct offenders array, flagged as a watch item since Wave 1's SUMMARY) had its expected value renamed from `[\"vice-probe.ts\", \"vice.mjs\"]` to `[\"vice-probe.ts\", \"vice.ts\"]` -- updated per the plan's explicit instruction, not deleted."
  - "No discriminated-union return was invented for readEpoch() or the rpc()-level helpers beyond what vice-broker-client.ts already established (PollGrantResult/PollRecycleAckResult) -- assertSameMachine()'s control flow (throw MachineRestartedError vs. return) was kept exactly as the original JS's throw-or-return shape, since introducing a Result-style return there would be a behavior-shape change beyond a language port."
  - "The missing node_modules in this git worktree (a per-worktree artifact, not a code regression) was fixed via `npm ci` against the already-committed package-lock.json -- a mechanical materialization of already-approved, already-locked devDependencies, not a new package-manager install decision requiring a checkpoint. Logged to RE-FINDINGS.md at discovery per CLAUDE.md's rule."

patterns-established: []

requirements-completed: []

duration: ~12min (commit span only; excludes research/reading time)
completed: 2026-08-03
status: complete
---

# Phase 01.6.1 Plan 05: Convert the Broker-Protocol Client and the Transport Library, Landing Criterion C7 Summary

**vice-broker-client.mjs and vice.mjs (plus their test files) are now TypeScript; the request-id pattern (C7's container-side deliverable) and the transport library's full export surface both survive the port byte-identical, proven by direct before/after captures rather than trusted from the port's own logic.**

## Performance

- **Duration:** ~12 min across the two feature commits (12:49:47Z -> 12:59:41Z), plus a RE-FINDINGS append commit at 13:01:44Z; research/reading time (this plan's own PLAN.md, two prior SUMMARYs, RESEARCH §5's full C7 trace, PATTERNS.md's TypeScript idiom section, and the pre-conversion source of both files plus every consumer) took materially longer than this span shows.
- **Started:** 2026-08-03T12:49:47Z (first commit)
- **Completed:** 2026-08-03T13:01:44Z (last commit, the RE-FINDINGS append)
- **Tasks:** 2 (both completed)
- **Files modified:** 11 (4 renamed `.mjs` -> `.ts`, 6 modified in place for specifier/enumerator/regex updates, 1 RE-FINDINGS append)

## Accomplishments

- Converted `vice-broker-client.mjs`/`.test.mjs` to `.ts`, typing the broker-protocol record shapes (`RequestRecord`, `RecycleRequestRecord`, `LeaseRecord`, `BrokerLivenessResult`) as named interfaces and `pollGrant()`/`pollRecycleAck()`'s results as discriminated unions (`PollGrantResult`, `PollRecycleAckResult`), matching `vice-broker.mts`'s own `WriteResult` idiom. `REQUEST_ID_PATTERN` kept its exact regex source (`/^req-[0-9]+-[0-9]+-[0-9a-f]{8}$/`, captured and diffed before/after the rename -- byte-identical) and became an explicitly-typed (`RegExp`), named export -- this is criterion C7's whole container-side deliverable, landing so 01.6.2's in-process broker can `import { REQUEST_ID_PATTERN }` directly rather than re-stating the rule a third time.
- Touched `vice-broker.test.mjs` (explicitly out of this phase's scope) in exactly one line: its import specifier (`git diff --numstat` confirms `1  1`, added and removed). Its named cross-language parity test (`"parity: the shell script's own id validation and isValidRequestId() agree on every corpus entry"`) still passes, driving the same shared corpus through both `isValidRequestId()` and the real spawned bash daemon -- proving the shell copy (`resources/vice-broker.sh`) and the now-typed container copy still agree on every entry, with the shell side completely untouched.
- Converted `vice.mjs`/`.test.mjs` to `.ts` (555 lines, library-only per D-05's already-executed CLI deletion -- confirmed live via `grep -c 'process\.argv' vice.ts` returning `0`). Typed `DENY_LIST` as a `readonly string[]`, `ViceError`/`MachineRestartedError` as plain classes extending `Error` (fields assigned in the constructor body, no constructor parameter properties, since `erasableSyntaxOnly` forbids them), and named interfaces for the instance/handshake/session/epoch shapes (`ActiveInstance`, `EpochResult`, `SessionInfo`, `AssertSameMachineOptions`). An `isPlainObject()` predicate (matching `vice-broker.mts`'s / `vice-broker-client.ts`'s own) narrows every parsed JSON-RPC payload and epoch-file read rather than casting.
- **Export list proven byte-identical, not merely asserted:** `Object.keys(await import(...)).sort()` captured before the rename (`["DENY_LIST","EPOCH_FILE","MachineRestartedError","ViceError","activeInstance","assertSameMachine","beginSession","call","callTool","lastToolCall","mcpHost","readEpoch","serverInfo","sessionReconnects","useInstance"]`, 15 names) and again after (identical, diffed directly). The deny-list filter still strips at discovery: `serverInfo()`'s own test and three of `vice-proxy.test.mjs`'s `vice_disk_list` assertions all still pass.
- Repointed every real consumer specifier: `vice-proxy.mjs` (both `vice-broker-client.ts` and `vice.ts`, across the two tasks), `vice-proxy.test.mjs` (the `GRANT_POLL_TIMEOUT_MS` import), `vice-sync.mjs`, `refresh-manifest.mjs`, and `repo-root.test.ts`'s `VICE_MODULE_URL` dynamic-import URL literal (proven live: the cross-process `dirname(EPOCH_FILE)` path-agreement test spawns a fresh Node process that imports this exact literal and still passes). Verified the 2,441-line hub loads cleanly in a fresh Node process after each task, not merely that grep found no stale specifier.
- Updated `vice-proxy.test.mjs`'s fourth module enumerator (the network-call-construct offenders array, flagged as a watch item since Wave 1's own SUMMARY) from `["vice-probe.ts", "vice.mjs"]` to `["vice-probe.ts", "vice.ts"]` -- renamed per the plan's explicit instruction, not deleted -- and widened its separate `DENY_LIST`-reading regex to tolerate the new `: readonly string[]` type annotation, a fifth instance of this phase's own "extension/format-hardcoded assertion goes vacuous across a rename" hazard class, this time keyed on syntax shape rather than filename.
- Full suite: 254 -> 254 (no count change; this plan adds type coverage and consumer repoints, not new test cases). Typecheck exits 0 throughout, with no edit to `tsconfig.json`/`tsconfig.build.json`/`package.json`.

## Task Commits

Each task was committed atomically:

1. **Task 1: Slice 5 -- the broker client becomes TypeScript, and criterion C7's export survives typed** - `cdb566a` (feat)
2. **Task 2: Slice 6 -- the vice transport library becomes TypeScript** - `fc515df` (feat)

**Plan metadata:**
- `97486ac` (docs: RE-FINDINGS.md append for the missing-node_modules worktree hazard, committed ahead of this SUMMARY since the finding was discovered before Task 1's own work began, per the project's own CLAUDE.md rule to log at discovery, not at session end)
- This SUMMARY's own commit (made immediately after this file is written)

_Note: both tasks are `tdd="false"`, single commits each._

## Files Created/Modified

- `.claude/mcp/vice/vice-broker-client.ts` (renamed) -- broker-protocol client, typed with `RequestRecord`/`RecycleRequestRecord`/`LeaseRecord`/`BrokerLivenessResult` and the `PollGrantResult`/`PollRecycleAckResult` discriminated unions; `REQUEST_ID_PATTERN` kept byte-identical and gained an explicit `RegExp` annotation; every doc comment ported verbatim
- `.claude/mcp/vice/vice-broker-client.test.ts` (renamed) -- typed `withPoolDir()`/`tmpPoolDir()`/`sleepMs()` helpers; one assertion (`result.denial.reason` -> `result.denial?.reason`) adjusted to let TypeScript's discriminated-union narrowing compile without weakening the assertion (the corpus this test writes always produces a real denial object at that point, so the optional-chain's runtime value is unchanged)
- `.claude/mcp/vice/vice-broker.test.mjs` -- exactly one line changed: the import specifier for `vice-broker-client.ts`
- `.claude/mcp/vice/vice.ts` (renamed) -- transport seam, typed with `ActiveInstance`/`UseInstanceOptions`/`ViceErrorOptions`/`MachineRestartedErrorOptions`/`EpochResult`/`SessionInfo`/`AssertSameMachineOptions`/`ToolInfo`/`ServerInfoPayload`; `useInstance()`'s port/url/epochFile made required; no CLI block (D-05 stays deleted)
- `.claude/mcp/vice/vice.test.ts` (renamed) -- its one `useInstance()` call now passes `epochFile: originalInstance.epochFile` explicitly, matching the tightened `UseInstanceOptions`
- `.claude/mcp/vice/vice-proxy.mjs` -- two specifiers repointed (`./vice-broker-client.ts`, `./vice.ts`) plus directly-adjacent stale `.mjs` prose corrected
- `.claude/mcp/vice/vice-proxy.test.mjs` -- two specifiers repointed; network-call-construct offenders array and title renamed to `vice.ts`; `DENY_LIST`-reading regex widened to tolerate a type annotation; stale prose corrected
- `.claude/mcp/vice/vice-sync.mjs` -- specifier repointed to `./vice.ts`
- `.claude/mcp/vice/refresh-manifest.mjs` -- specifier repointed to `./vice.ts`
- `.claude/mcp/vice/repo-root.test.ts` -- `VICE_MODULE_URL` literal repointed to `./vice.ts`
- `.planning/RE-FINDINGS.md` -- append-only entry for the missing-node_modules worktree hazard

## Decisions Made

- **C7 landed exactly as scoped: a typed export, not a deletion.** See key-decisions above for the full account -- the shell copy in `resources/vice-broker.sh` was never touched, matching the plan's explicit framing that its retirement is Phase 01.6.2's job.
- **`useInstance()`'s three positional-feeling fields tightened to required**, with the one real test call site updated to supply the value it previously omitted. Zero behavior change for any assertion in the suite; see key-decisions for the full reasoning.
- **The DENY_LIST-reading regex was widened, not just its target filename**, since typing `DENY_LIST` introduced a syntax shape (`: readonly string[]`) the old bare `= [` pattern could not match. Caught and fixed before any commit, not discovered after a red run.
- **The fourth module enumerator's offenders array was renamed, per the plan's explicit instruction, not deleted or loosened.**

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Missing node_modules in this git worktree**
- **Found during:** the very first baseline suite run, before Task 1 began
- **Issue:** `node --test '.claude/mcp/vice/'*.test.*` reported `253 pass, 1 fail` -- `resources-sync.test.ts`'s own build-and-diff test failed with `spawnSync .../node_modules/.bin/tsc ENOENT`. `node_modules` was entirely absent from this worktree (a per-worktree artifact; git worktrees never share untracked directories), while the main checkout had it populated.
- **Fix:** `cd .claude/mcp/vice && npm ci` against the already-committed `package-lock.json` -- materializing the exact `typescript@7.0.2`/`@types/node@24.13.3` versions already human-approved in Phase 01.6's own Package Legitimacy Audit. This is explicitly excluded from Rule 3's package-manager-install carve-out only when the install is a NEW or different package; `npm ci` against an already-locked, already-approved manifest is a mechanical materialization, not a new install decision, so no package-legitimacy checkpoint applies.
- **Files modified:** none (node_modules is gitignored; no tracked file changed). `.planning/RE-FINDINGS.md` gained the discovery entry.
- **Verification:** Full suite re-run immediately after: 254/254, fail 0. `npx tsc --version` confirmed `7.0.2` (previously silently falling through to `npx`'s own auto-install of an unrelated `tsc@2.0.4` package).
- **Committed in:** `97486ac` (a dedicated RE-FINDINGS append commit, ahead of Task 1's own commit, since the finding was discovered before any conversion work started)

---

**Total deviations:** 1 auto-fixed (Rule 3 - blocking toolchain issue, environment-only, no tracked source changed).
**Impact on plan:** Necessary to get an honest baseline reading before starting; without it, this plan's own "254/254 continuously" claim would have been measured against a false 253/254 floor. No scope creep: no source file was touched to fix this, only the worktree's own untracked `node_modules`.

## Issues Encountered

None beyond the deviation above, caught and fixed before any task work began.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Slices 5 and 6 (RESEARCH §2) are complete: `vice-broker-client`, `vice` and their two test files are all TypeScript. Criterion C7's container-side half is landed: `REQUEST_ID_PATTERN` is a real, typed, named export, unchanged in value, ready for 01.6.2's in-process broker to import directly.
- The `.mjs` set under `.claude/mcp/vice/` is now exactly five files: `refresh-manifest.mjs`, `vice-sync.mjs`, `vice-proxy.mjs`, `vice-proxy.test.mjs`, `vice-broker.test.mjs` -- confirmed by direct `ls *.mjs` immediately before this SUMMARY was written, matching the plan's own `<verification>` block exactly.
- Both Wave 2 guards (`load-order.test.ts`'s emptied cycle allowlist and module-scope call-site guard) are unaffected by this plan (neither of this plan's two files participates in the repo-root cycle) and continue to pass, confirmed by the full suite run.
- **Watch items for Plans 06-08:** `refresh-manifest.mjs` and `vice-sync.mjs` (both import `vice.ts` now) still need their own first-ever test coverage (Plan 06, per RESEARCH §2 Slices 7-8) and their own `.ts` conversion. `vice-proxy.mjs`/`vice-proxy.test.mjs` (Slice 9, Plans 07/08) are the last two files remaining -- every one of their seven internal dependencies is now `.ts`, so their own conversion's specifier updates will touch only themselves.
- Full suite is 254/254; typecheck exits 0 with no edit to `tsconfig.json`/`tsconfig.build.json`/`package.json` throughout. Any later plan's own baseline should reconcile against 254.

## Self-Check: PASSED

All 4 claimed renamed files verified present on disk (`vice-broker-client.ts`, `vice-broker-client.test.ts`, `vice.ts`, `vice.test.ts`); no `.mjs` copy of either module remains (`ls vice-broker-client.*mjs vice.*mjs` finds nothing, confirmed via the file-existence acceptance check run during Task 1/2 execution). All 3 claimed commit hashes (`cdb566a`, `fc515df`, `97486ac`) confirmed present via `git log --format='%H %aI %s'`.

---
*Phase: 01.6.1-container-side-conversion-to-typescript*
*Completed: 2026-08-03*
