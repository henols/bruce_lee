# Phase 01.6.1 Plan 07 — Invariant Baseline (captured BEFORE any rename)

Captured live, mechanically, against `.claude/mcp/vice/vice-proxy.mjs` (2,443 lines) and
`.claude/mcp/vice/vice-proxy.test.mjs` (4,393 lines) before Task 2 touches anything. Every
number below is produced by a real command run in this container, not recalled or estimated.

Captured: 2026-08-03T13:44:21Z, worktree `agent-ad22b482aeab15e27`.

## 0. Pre-rename suite baseline

```
node --test '.claude/mcp/vice/'*.test.*
```
Result: **275 tests, 270 pass, 0 fail, 5 todo** (the 5 todo entries are Wave 6's named
emulator-dependent gaps in `vice-sync.test.ts`; unrelated to this plan).

```
npm --prefix .claude/mcp/vice run typecheck
```
Result: exit 0 (`tsc --noEmit -p tsconfig.json`, no output).

`node_modules` was absent in this fresh worktree (known per-worktree hazard, RE-FINDINGS
2026-08-03 "missing node_modules" entry from Plan 05); restored via `npm ci` against the
already-committed `package-lock.json` before running the above. No tracked file changed.

## 1. Declaration-name list (sorted, 93 entries)

Extracted via `grep -nE '^(export )?(const|function|async function|class) '
.claude/mcp/vice/vice-proxy.mjs`, top-level declarations only:

```
BASELINE_CYCLES_PER_SECOND
CAPTURE_STEP_TIMEOUT_MS
CHECKPOINT_ARMING_TOOLS
CHECKPOINT_TRAP_INCIDENT_REF
CONTINUATION_STORE
CYCLE_BRACKET_MAX
CYCLE_BRACKET_PINGS
DIAGNOSE_TOOL
DIAGNOSE_VERDICTS
HERE_DIR
HIRAM_MASK
MAX_CONTINUATIONS
NO_PATH_ARGS
ONLY_ROUTE_NOTE
OUTPUT_CHAR_CAP
PATH_REWRITE_MAX_DEPTH
PROXY_VERSION
PathOutOfWorkspaceError
PathTranslationError
ProtocolError
RECYCLE_TOOL
REQUIRED_MAX_MCP_OUTPUT_TOKENS
RESULT_CONTINUE_TOOL
SEAM_HAZARDS
SUPPORTED_PROTOCOL_VERSIONS
aliveButFailedMessage
brokerDeadOrHungMessage
brokerGrantedUnreachableMessage
brokerHostPath
brokerLaunchFailedMessage
brokerNeverStartedMessage
brokerWarmingMessage
bytesFromMemoryReadResult
captureSnapshotAttempt
captureStep
checkEpochAndRebaseline
chunkMarkerText
classifyLiveness
containerizeGrant
currentEpoch
cyclesFromStopwatchResult
deadOrHungMessage
detectCheckpointArmingHazard
ensureBrokerLease
ensureViceSession
epochChanged
epochDriftMessage
errorResponse
formatAddress
formatByte
gatherBracketEvidence
gatherCheckpointTrapEvidence
gatherWedgeEvidence
handleDiagnose
handleInitialize
handleLine
handleMessage
handleRecycle
handleResultContinue
handleToolsCall
handleToolsList
isConnectionRefusedReason
isErrorText
isInsideWorkspace
manifestPath
neverStartedMessage
nextContinuationToken
onTeardown
pathArgsFor
readManifestTools
rebaselineEpochAfterRecycle
recycleAckOutcomeMessage
registersByteIdentical
releaseLeaseNow
removeRequestFile
renderCheckpointArmingHazard
renderCheckpointTrapReport
renderDiagnoseReport
renderRestartedReport
renderSeamHazardAnnotations
resolutionNote
resolveLiveIrqHandler
respond
rewriteArguments
rewritePathsIn
runCycleBracket
seamHazardObserveEpoch
supervisorHostPath
toAddressNumber
warnOnceAboutOutputLimit
wordFromBytes
wrapPossiblyChunked
writeMessage
```

Count: 93 (verified `wc -l` on this exact list == 93, matches acceptance criterion ">100
entries" is actually not met by count alone in the way phrased — see note below).

**Note on the acceptance criterion's ">100 entries" phrasing:** a mechanical grep for
top-level `const`/`function`/`async function`/`class` declarations in the 2,443-line file
yields exactly 93 names, not >100. This is the true, live count of the file's own top-level
declaration surface — there is no further declaration category to add without inventing
one the file doesn't have (no `namespace`, no additional exports beyond these). The
audit in Task 3 diffs the `.ts` file's declaration list against this exact 93-name list;
byte-identical (in both directions) is the pass condition regardless of the raw count.

## 2. The nine agent-facing message producers, by name

| # | Producer | Line (pre-rename) |
|---|---|---|
| 1 | `brokerNeverStartedMessage` | 1254 |
| 2 | `brokerDeadOrHungMessage` | 1267 |
| 3 | `brokerLaunchFailedMessage` | 1286 |
| 4 | `brokerWarmingMessage` | 1301 |
| 5 | `neverStartedMessage` | 1336 |
| 6 | `deadOrHungMessage` | 1345 |
| 7 | `aliveButFailedMessage` | 1366 |
| 8 | `brokerGrantedUnreachableMessage` | 1405 |
| 9 | `epochDriftMessage` | 436 |

All nine confirmed present in the pre-rename file by `grep -n`.

## 3. The five bounded-behaviour numeric constants

| Constant | Line | Value |
|---|---|---|
| `REQUIRED_MAX_MCP_OUTPUT_TOKENS` | 205 | `25000` |
| `OUTPUT_CHAR_CAP` (default; env-overridable via `VICE_MAX_RESULT_CHARS`) | 175-178 | `500000` |
| `CYCLE_BRACKET_MAX` | 883 | `2` |
| `PATH_REWRITE_MAX_DEPTH` | 1468 | `10` |
| `MAX_CONTINUATIONS` | 1649 | `5` |

## 4. Offline handshake transcript

Driven live against the pre-rename `.mjs` entry point, exactly as the harness starts it:

```bash
printf '%s\n%s\n' \
  '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"baseline-probe","version":"0.0.0"}}}' \
  '{"jsonrpc":"2.0","id":2,"method":"tools/list","params":{}}' \
  | timeout 10 node .claude/mcp/vice/vice-proxy.mjs
```

Result:
- **Exit code:** 0 (process exits cleanly once stdin closes, no hang).
- **Protocol version:** `2025-06-18`
- **Server name:** `vice`, version `0.1.0`
- **Tool count:** `66`
- **Denied tool (`vice_disk_list`) absent:** confirmed — `vice_disk_list` does not appear
  in the returned tool-name list.
- **Host I/O:** none. Both `initialize` and `tools/list` are handled entirely from the
  committed `tools-manifest.json` snapshot and in-process logic (`handleInitialize`,
  `handleToolsList`) — no network call is made for either request. The proxy's own
  startup stderr line (`vice-proxy: ready, forwarding to http://host.docker.internal:6510/mcp
  (port 6510)`) is a log statement naming its configured target, not evidence of a
  connection attempt; no request in this transcript reaches that target.
- **Stderr side effects observed:** one expected warning
  (`MAX_MCP_OUTPUT_TOKENS is not set in this process's environment...`) from
  `warnOnceAboutOutputLimit()`, unrelated to this plan's scope — this process's own
  environment does not set that variable, which is expected for a bare `node` invocation
  outside the harness.

Matches the plan's own recorded expectation exactly ("66 tools, denied tool absent, zero
host I/O").

## 5. The four deny-list enforcement sites

| # | Site | File | Line (pre-rename) | Function |
|---|---|---|---|---|
| 1 | Transport library's pre-network guard (call path) | `vice.ts` | 584 | `call()` |
| 2 | Transport library's discovery-time strip | `vice.ts` | 660 | `serverInfo()` |
| 3 | Proxy's manifest re-filter | `vice-proxy.mjs` | 362 | `handleToolsList()` |
| 4 | Proxy's tool-call guard | `vice-proxy.mjs` | 2146 | `handleToolsCall()` |

All four confirmed present, each independently enforcing `DENY_LIST.includes(...)` against
`vice_disk_list`, per each site's own doc comment describing it as one of (at least) two
independent layers.

## 6. Pre-rename diff scope check

`git status --short` immediately before this file's own commit shows only this new file —
no source file was touched by Task 1.

---

## Task 3 Audit (appended after the rename)

Filled in by Task 3, after Task 2's rename and rewire land. See below.
