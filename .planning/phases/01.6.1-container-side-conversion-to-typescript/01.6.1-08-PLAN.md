---
phase: 01.6.1-container-side-conversion-to-typescript
plan: 08
type: execute
wave: 8
depends_on: ["01.6.1-07"]
files_modified:
  - .claude/mcp/vice/vice-proxy.test.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The proxy test file is TypeScript and every one of its tests still passes, with the same test count it had as .mjs"
    - "Exactly one .mjs file remains under .claude/mcp/vice/ — vice-broker.test.mjs, deliberately, owned by Phase 01.6.2"
    - "`npm --prefix .claude/mcp/vice run typecheck` exits 0 across the whole converted tree with no edit to any tsconfig"
    - "The full suite is green and its total reconciles arithmetically against the 247 baseline plus every test this phase added"
    - "erasableSyntaxOnly was on from the first converted file and no converted file uses a construct native stripping rejects"
    - "The criterion-B guards still fail against an injected regression at the end of the phase, not only when they were written"
    - "Both `--once` and the broker probe-command environment variable remain real, driven seams"
    - { statement: "The tool surface a real agent session sees is unchanged by this phase", verification: backstop }
    - { statement: "The converted proxy behaves identically under a live MCP session against a real emulator", verification: backstop }
  artifacts:
    - .claude/mcp/vice/vice-proxy.test.ts
  key_links:
    - "vice-broker.test.mjs stays .mjs and keeps importing the converted broker client — the one cross-phase specifier this phase leaves in place"
    - "The phase's own criteria register (C3, C7, C8, C10 plus A, B, C) is rolled up here, not left to the verifier to reconstruct"
---

<objective>
Convert the last in-scope file — the 4,370-line proxy test suite — and close the phase with a
gate that checks the criteria register rather than just the suite.

Purpose: this file was Plan 07's unchanged oracle; now it becomes the last conversion. It is
the largest single file in the phase and the ROADMAP required it have its own plan. With the
hub already converted and green, a bug introduced here surfaces as a test failure against a
source file that is known-good — the reverse of Plan 07's arrangement, and the reason the
two were split in this order.

Then close the phase honestly. A green suite here proves **language and structural
equivalence only**. Phase 01.4 already demonstrated a 268/268 green suite while three tools
were uncallable from a real session, and this phase runs *before* 01.4 by design, so no live
verification exists. The close records what was proven, and marks what was not as a backstop
so verification abstains rather than passing silently.

Output: the last `.ts` conversion, and a criteria roll-up with the evidence for each.

**Inherited decisions:** none of D-01…D-06 is owned here. D-01/D-06 remain deferred to
01.6.3, whose seam swap this phase deliberately did not anticipate.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Convert the proxy test suite — the last in-scope file</name>
  <files>.claude/mcp/vice/vice-proxy.test.mjs → .claude/mcp/vice/vice-proxy.test.ts</files>
  <read_first>
    - .claude/mcp/vice/vice-proxy.test.mjs — 4,370 lines. **Do not read it end to end.** Read its import block and its shared fixture/helper region first (roughly the first 120 lines), then rename, run the typecheck, and let the compiler's reported errors drive which further regions you read. The error list is the work list.
    - .claude/mcp/vice/vice-proxy.ts as left by Plan 07 — the types it now exports are what most of this file's fixtures should be typed against, rather than re-declaring parallel shapes here.
    - .claude/mcp/vice/vice-probe.test.ts and .claude/mcp/vice/vice-broker-client.test.ts — already-converted test files in this tree, for the idiom.
    - .claude/mcp/vice/vice-broker-launch.test.ts — the repo's original `.ts` test conventions.
  </read_first>
  <action>
`git mv vice-proxy.test.mjs vice-proxy.test.ts`, then make the typecheck pass, working
compiler-error-driven. Rename, run the typecheck, fix the reported errors in batches, re-run
until it exits 0. Keep the per-file test run green between batches so a mid-conversion break
is caught in seconds rather than at the end.

**Type the helpers, not the assertions.** The bulk of a 4,370-line test file is assertion
bodies that need no annotation once their fixtures are typed. Concentrate the work on the
shared stub-server, fixture-builder and request-forwarding helpers near the top; annotating
those correctly usually resolves most implicit-parameter errors downstream in one pass.

**Reuse the proxy's own exported types** for message and record shapes rather than declaring
a second, parallel set here. A test that declares its own shape stops catching a drift in the
real one, which is the opposite of what this file exists for.

**Change no assertion's meaning, message or expected value.** Not one. This file is the
equivalence proof for 2,441 lines of project logic that Phase 01.6.3 is about to modify; an
assertion weakened here to satisfy the type checker removes exactly the coverage 01.6.3 will
need. If an assertion cannot be typed without weakening it, annotate the value more precisely
or narrow at the boundary — do not relax the assertion.

**Leave every regression-corpus string literal as written**, including any that name legacy
specifier extensions.

**Do not** edit `tsconfig.json`, `tsconfig.build.json` or `package.json`. **Do not**
introduce `enum`, `namespace` or constructor parameter properties. **Do not** touch
`vice-proxy.ts`; if the typecheck reports an error that seems to require a source change, the
source's type is probably too narrow — widen it deliberately and record why in the SUMMARY,
never by loosening the test.
  </action>
  <acceptance_criteria>
    - `test -f .claude/mcp/vice/vice-proxy.test.ts && test ! -e .claude/mcp/vice/vice-proxy.test.mjs` succeeds.
    - `node --test .claude/mcp/vice/vice-proxy.test.ts` reports `fail 0` with **the same test count** the `.mjs` file reported before the rename — capture that number first and quote both.
    - `npm --prefix .claude/mcp/vice run typecheck` exits 0.
    - `git diff --name-only HEAD~1 -- .claude/mcp/vice/vice-proxy.ts` prints nothing, or, if it does not, the SUMMARY records the deliberate widening and why the test was not relaxed instead.
    - `ls .claude/mcp/vice/*.mjs` lists exactly one file: `vice-broker.test.mjs`.
    - Full suite: `node --test '.claude/mcp/vice/'*.test.*` reports `fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/mcp/vice/vice-proxy.test.ts && npm --prefix .claude/mcp/vice run typecheck && node --test '.claude/mcp/vice/'*.test.*</automated>
  </verify>
  <done>The proxy test suite is TypeScript with an unchanged test count and no weakened assertion, and exactly one deliberately-out-of-scope `.mjs` file remains in the directory.</done>
  <reversibility rating="reversible">Rename plus annotations.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Phase close — roll up the criteria register with evidence, and mark honestly what a green suite cannot prove</name>
  <files>.planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-08-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md § Phase 01.6 — the C1–C10 criteria register in full, and § Phase 01.6.1 — this group's own criteria A, B and C and its ownership row (C3 TS half, C7, C8, C10).
    - Every `01.6.1-0N-SUMMARY.md` produced so far — the running test-count arithmetic and the decisions each recorded.
    - .planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-VALIDATION.md § "Manual-Only Verifications" and its restated standing hazard.
    - .planning/phases/01.6-broker-in-node-and-tcp-control-plane/01.6-VERIFICATION.md — how the parent group recorded its two abstained backstop truths; the same shape applies to this group's.
  </read_first>
  <action>
Close the phase by checking the criteria register, not by reporting that the suite is green.

**A. Run the phase gate.** Full suite, typecheck, and the `.mjs` sweep. Reconcile the test
count as explicit arithmetic from the 247 baseline through every plan's additions to the
final number, and state the todo count separately — a todo is not a pass.

**B. Re-prove the criterion-B guards discriminate, at the end of the phase rather than only
when they were written.** Plan 02 proved it against the tree as it stood then; six plans of
renames have happened since, including the rename of the guard file itself. Repeat Plan 02
Task 3's scratch-copy procedure against the final tree: restore the cycle in a scratch copy
and confirm the cycle assertion fails; drop the explicit override from a module-scope call in
a scratch copy and confirm the call-site guard fails. Restore. This is the difference between
a guard that discriminates and a guard that used to.

**C. Confirm both real seams survived.** Criterion C8 names `--once` and the broker
probe-command environment variable as the seams every test drives. Confirm each is still
referenced by production code and still driven by at least one test, and name the tests.

**D. Confirm the erasable-syntax constraint held from the first converted file.** The
typecheck exiting 0 with the setting on is the mechanical proof; state that the setting was
never disabled or worked around, and that no converted file uses a construct native stripping
rejects.

**E. Roll up the criteria register with evidence per row** — C3's TypeScript half, C7, C8,
C10, and this group's A, B and C — each with the command that proves it and its result.

**F. Mark honestly what was not proven.** Two claims are backstops and must be recorded as
structured abstentions so verification routes them to human review rather than passing them:
that the tool surface a real agent session sees is unchanged, and that the converted proxy
behaves identically under a live session against a real emulator. Neither is knowable from
this container: the tool surface is not reachable until Phase 01.4 lands, and Phase 01.4's
own central finding is that a 268/268 green suite coexisted with three tools being uncallable
from a real session. Do not soften these into "verified by the suite." Also carry forward,
unchanged, the parent group's two abstained host-side truths — this phase neither closes them
nor needed to, since every file it touched is container-only.

**G. Record the decisions this phase took** so a later reader meets them where the work is:
PTD-1 (criterion B resolved as both the cycle break and the guard) and PTD-2 (the
host-scripts test converted its file type now, content rewrite deferred to 01.6.2) were
developer decisions taken at plan time. Plan 01's extension-stripped allow-list keys and
recorded doc-leak exemption, Plan 03's by-stem guard-subject resolution, and Plan 06's
atomic-write tightening and exported entry function were taken by the planner or executor
under the CONTEXT file's Claude's-Discretion delegation. Name each with its rationale.

**H. Append to `.planning/RE-FINDINGS.md`** any toolchain hazard, shortcut or dead end this
phase produced that is not already logged — one entry each, with separate `Evidence:` and
`Confidence:` fields on the HIGH/MEDIUM/LOW scale. Candidates this phase is likely to have
produced: the enumerator-widening trap involving a third-party dependency directory, the
agent-doc name that a widened enumeration newly collides with, and whatever the strict-mode
conversion of the hub taught. Do not deduplicate against what is already there out of
caution — a finding logged twice is free.
  </action>
  <acceptance_criteria>
    - `node --test '.claude/mcp/vice/'*.test.*` reports `fail 0`, and the SUMMARY states the arithmetic from 247 through every plan's delta to the final total, with the todo count on its own line.
    - `npm --prefix .claude/mcp/vice run typecheck` exits 0.
    - `ls .claude/mcp/vice/*.mjs` lists exactly `vice-broker.test.mjs`, and the SUMMARY states why that one file is correctly still `.mjs`.
    - `git diff --name-only HEAD -- .claude/mcp/vice/tsconfig.json .claude/mcp/vice/tsconfig.build.json` prints nothing across the whole phase — verify with a range diff over the phase's commits, not just the last one.
    - The end-of-phase re-proof of both criterion-B guards is recorded with the two failure messages quoted verbatim, and `git status --porcelain` is empty afterwards.
    - Both seams named by C8 are shown still present in production code and still driven, with the driving tests named.
    - The criteria roll-up has a row per owned criterion (C3, C7, C8, C10, A, B, C) with a command and a result, not a prose claim.
    - The two backstop claims appear in the SUMMARY as explicit abstentions with their reason, phrased so verification routes them to human review rather than reading them as passes.
    - Every decision listed in step G is named with its rationale.
  </acceptance_criteria>
  <verify>
    <automated>node --test '.claude/mcp/vice/'*.test.* && npm --prefix .claude/mcp/vice run typecheck && test "$(ls .claude/mcp/vice/*.mjs | wc -l)" = "1"</automated>
    <human-check>Two items, both requiring a NEW agent session — the current one holds a proxy snapshot taken at its own spawn and cannot see this phase's rewiring. (1) Confirm the `mcp__vice__*` tool surface is present in a fresh session and that a call reaches the host. (2) Review the two behaviour-affecting decisions this phase took beyond a pure port, both recorded in Plan 06's SUMMARY: the manifest write was tightened to the atomic tmp-then-rename idiom (closing a gap the module's own doc comment already claimed was closed), and the manifest refresher gained an exported entry function plus an entry-point guard so it could be tested at all. Both are reversible; both were taken under the CONTEXT file's Claude's-Discretion delegation rather than by a developer decision.</human-check>
  </verify>
  <done>The phase gate ran, the criteria register is rolled up with a command and a result per row, both criterion-B guards were re-proven to discriminate against the final tree, the two seams are confirmed live, the decisions are recorded, and the two unprovable claims are marked as structured backstops rather than softened into passes.</done>
  <reversibility rating="reversible">Documentation and verification only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the test suite → the claim of equivalence | The suite is the only equivalence proof available in this phase, and Phase 01.4 already showed a fully green suite coexisting with three tools uncallable from a real session. The boundary between "the suite is green" and "the system works" is the central hazard of this phase and is crossed at exactly this point. |
| this phase → Phase 01.6.3 | The proxy test suite is the coverage 01.6.3 will rely on when it swaps the transport seam. An assertion weakened here to satisfy a type checker removes coverage a later phase is counting on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.1-21 | Tampering | assertions weakened to satisfy the type checker | high | mitigate | Task 1 forbids changing any assertion's meaning, message or expected value, and requires the converted file's test count to equal the pre-rename count exactly. Where a type conflict appears, the instruction is to widen the source's type deliberately and record it, never to relax the test — and any such source edit must be named in the SUMMARY. |
| T-01.6.1-22 | Repudiation | a green suite read as behavioural proof | high | mitigate | Task 2 requires the two unprovable claims to be recorded as structured backstop abstentions, so verification routes them to human review instead of passing them. The precedent is the parent group's own two abstained truths, which were correctly neither passed nor failed. |
| T-01.6.1-04 | Tampering | the criterion-B guards, six plans of renames after they were written | high | mitigate | Task 2 re-runs the scratch-copy injection against the **final** tree and requires both failure messages quoted. A guard proven once, before the file it lives in was itself renamed, is not a guard proven now. |
| T-01.6.1-23 | Tampering | the two real seams C8 names | medium | mitigate | Task 2 requires each to be shown still present in production code and still driven by a named test. A seam that survives as dead code satisfies a grep and nothing else. |
| T-01.6.1-24 | Tampering | the erasable-syntax constraint being disabled to unblock a conversion | high | mitigate | Task 2's acceptance runs a range diff over the whole phase's commits against both tsconfig files and requires an empty result — so the setting cannot have been turned off in an intermediate commit and turned back on at the end. Both the compiler and bare `node` reject the barred constructs independently. |
| T-01.6.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists anywhere in this phase. See Plan 01's register. |
</threat_model>

## Artifacts this phase produces

Phase-wide list. **(P08)** marks what this plan produces.

**File renames (`.mjs` → `.ts`), all 23:** `vice-probe` · `vice-probe.test` ·
`skill-docs.test` · `vice-mcp-selector-docs.test` · `host-scripts.test` · `repo-root` ·
`repo-root.test` · `install-resources` · `install-resources.test` · `hostpath` ·
`load-order.test` · `containerpath` · `containerpath.test` · `incident-record` ·
`incident-record.test` · `vice-broker-client` · `vice-broker-client.test` · `vice` ·
`vice.test` · `refresh-manifest` · `vice-sync` · `vice-proxy` · `vice-proxy.test` **(P08)**

**Deliberately NOT renamed:** `vice-broker.test.mjs` — the one surviving `.mjs` file, owned
by Phase 01.6.2, which redesigns rather than ports it.

**New files:** `refresh-manifest.test.ts` (Plan 06) · `vice-sync.test.ts` (Plan 06) ·
`01.6.1-07-BASELINE.md` (Plan 07, a planning artifact).

**New / changed exported symbols:** the hostpath module's optional `workspaceRoot` parameter
and named resolution error (Plan 02) · a new `repo-root` import in `vice-sync` (Plan 02) ·
an explicit type annotation on the exported request-id pattern (Plan 05) · an exported entry
function and manifest interface in `refresh-manifest` (Plan 06) · JSON-RPC and
continuation-store type declarations in the proxy (Plan 07) · named `interface`/`type`
declarations added to every converted module in its own plan. No module's runtime export set
changed; the vice library's and the proxy's were both asserted identical before and after.

**New test-internal symbols:** the doc-leak exemption map in `skill-docs.test` (Plan 01) ·
the module-scope `repoRoot` call-site guard and emptied allowlist in `load-order.test`
(Plan 02) · a by-stem module resolver in `load-order.test` (Plan 03) · the manifest tmpdir
redirect and handshake stub in `refresh-manifest.test` (Plan 06) · the recorded checkpoint
todos in `vice-sync.test` (Plan 06).

**Non-source files changed:** `.planning/config.json` (Plan 01) · `.mcp.json` (Plan 07).

<verification>
- `node --test '.claude/mcp/vice/'*.test.*` → `fail 0`, total reconciled from 247 by explicit
  arithmetic, todo count stated separately.
- `npm --prefix .claude/mcp/vice run typecheck` → exit 0.
- `ls .claude/mcp/vice/*.mjs` → exactly one file.
- No tsconfig was edited in any commit of this phase.
- Both criterion-B guards re-proven to fail against an injected regression on the final tree.
</verification>

<success_criteria>
- Every in-scope `.mjs` module and test in `.claude/mcp/vice/` is TypeScript; the one
  exception is named and justified.
- The suite is green with the arithmetic reconciled and the todo count reported separately.
- The criteria register is rolled up with a command and a result per owned row.
- The two claims a green suite cannot settle are recorded as backstops, not as passes.
</success_criteria>

<output>
Create `.planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-08-SUMMARY.md` when done.
It carries the phase roll-up: the full test-count arithmetic, the criteria table with
evidence, the two quoted guard-failure messages from the end-of-phase re-proof, the decision
record, and the two backstop abstentions phrased so verification routes them to human review.
Append every unlogged toolchain finding to `.planning/RE-FINDINGS.md` at discovery, one entry
each, with separate `Evidence:` and `Confidence:` fields.
</output>
