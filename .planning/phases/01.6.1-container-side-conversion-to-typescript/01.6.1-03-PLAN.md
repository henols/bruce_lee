---
phase: 01.6.1-container-side-conversion-to-typescript
plan: 03
type: execute
wave: 3
depends_on: ["01.6.1-02"]
files_modified:
  - .claude/mcp/vice/load-order.test.mjs
  - .claude/mcp/vice/repo-root.mjs
  - .claude/mcp/vice/install-resources.mjs
  - .claude/mcp/vice/hostpath.mjs
  - .claude/mcp/vice/repo-root.test.mjs
  - .claude/mcp/vice/install-resources.test.mjs
  - .claude/mcp/vice/containerpath.mjs
  - .claude/mcp/vice/containerpath.test.mjs
  - .claude/mcp/vice/incident-record.mjs
  - .claude/mcp/vice/vice-broker-client.mjs
  - .claude/mcp/vice/vice.mjs
  - .claude/mcp/vice/vice-sync.mjs
  - .claude/mcp/vice/vice-probe.ts
  - .claude/mcp/vice/vice-proxy.mjs
  - .claude/mcp/vice/vice-proxy.test.mjs
  - .claude/mcp/vice/host-scripts.test.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "load-order.test resolves its direct-guard subject by module stem, so no rename can silently re-point or break it"
    - "repo-root, install-resources and hostpath are TypeScript and no `.mjs` copy of any of the three remains"
    - "Every consumer of the three renamed modules resolves — no module-not-found error anywhere in the suite"
    - "install-resources still imports nothing from repo-root, and load-order.test's direct guard still proves it"
    - "The never-throw readers in install-resources keep two separate try/catch layers and keep returning a null-shaped value instead of throwing"
    - "The typecheck exits 0 with no edit to tsconfig.json"
    - "The full suite is green at the total Plan 02 established, with no test lost"
  artifacts:
    - .claude/mcp/vice/repo-root.ts
    - .claude/mcp/vice/install-resources.ts
    - .claude/mcp/vice/hostpath.ts
    - .claude/mcp/vice/repo-root.test.ts
    - .claude/mcp/vice/install-resources.test.ts
    - .claude/mcp/vice/load-order.test.ts
  key_links:
    - "vice-probe.ts's bare side-effect import of repo-root — RESEARCH §2 wrongly recorded vice-probe as having zero internal deps; it has one"
    - "install-resources.test's three dynamic-import URL literals are specifiers and must track the rename"
    - "repo-root.test's repo-root URL literal tracks the rename; its vice URL literal does NOT (that module renames in Plan 05)"
    - "host-scripts.test.ts and vice-proxy.test.mjs each import a renamed module and must be updated even though they are not this slice's subjects"
---

<objective>
Convert RESEARCH §2's Slice 2 — the three modules that were the cycle — plus their three
test files, and repoint every consumer specifier in the tree.

Purpose: these are the highest-risk three files in the phase. They carry the criterion-10
hazard, they are imported by nearly everything else, and the static check that polices them
is itself one of the files being renamed. Plan 02 already removed the cycle, so RESEARCH
§2's stated reason for forcing this slice into a single indivisible commit — *"a partial
conversion means the cycle-allowlist must track a mixed-extension cycle entry mid-flight"* —
**no longer applies**: the allowlist is empty and tracks nothing. What remains is one
hardcoded filename read, and Task 1 removes that constraint properly rather than working
around it, which lets the slice land in three green steps instead of one large one.

Output: six `.ts` files, ~15 repointed specifiers, and a direct guard that no future rename
can defeat.

**Inherited decisions:** none of D-01…D-06 is owned here; D-02's deletions already
determined which files exist to convert. The **Claude's Discretion** slicing item is
exercised again: this plan refines RESEARCH §2's Slice 2 into three green steps on the
strength of Plan 02 having removed the constraint that forced atomicity.
</objective>

<execution_context>
@/workspaces/bruce_lee/.claude/gsd-core/workflows/execute-plan.md
@/workspaces/bruce_lee/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-PATTERNS.md
</context>

## The complete consumer map, measured live at plan time (2026-08-03)

Every real importer, per renamed module. Entries marked *(corpus)* are string literals
inside a test's regression corpus, **not** imports — those literals stay exactly as written,
because they exist to prove the extraction regexes still catch the legacy specifier shapes.

**repo-root** → `vice-broker-client` · `vice-probe.ts` *(bare side-effect import, line 41)* ·
`containerpath` · `vice.mjs` · `incident-record` · `vice-sync` *(the import Plan 02 added)* ·
`vice-proxy.mjs` · `vice-proxy.test.mjs` · `repo-root.test` *(import **and** a URL literal)* ·
`load-order.test` *(corpus only — no change)*. **`install-resources` is NOT an importer** —
its header quotes the forbidden import in prose, which is the criterion-10 hazard itself.

**install-resources** → `repo-root` · `host-scripts.test.ts` · `install-resources.test`
*(import **and** three dynamic-import URL literals at lines 141, 154, 175)* · `repo-root.test` ·
`load-order.test` *(a real hardcoded filename read at line 124 — Task 1 removes it)*.

**hostpath** → `install-resources` · `containerpath` · `vice-sync` · `vice-proxy.mjs` ·
`vice-proxy.test.mjs` · `containerpath.test.mjs` · `vice-mcp-selector-docs.test.ts`
*(corpus only — no change; and its allow-list keys are already extension-stripped stems
after Plan 01, so nothing there needs touching either)*.

**Not to be repointed:** `repo-root.test`'s second URL literal names the `vice` module,
which does not rename until Plan 05.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove the last hardcoded-extension read — load-order.test resolves its guard subject by stem</name>
  <files>.claude/mcp/vice/load-order.test.mjs</files>
  <read_first>
    - .claude/mcp/vice/load-order.test.mjs as left by Plan 02 — specifically Part 1's real-file assertion around line 124, which reads its subject by hardcoded filename, and `listModuleFiles()` around line 152, which already enumerates by extension class.
    - 01.6.1-RESEARCH.md § "Architecture Patterns" → "Extension-hardcoded static checks go silently vacuous across a rename" — this is the last remaining instance in the tree after Plan 01 closed the other three.
  </read_first>
  <behavior>
    - Given the flat module directory, resolving the install-resources module by stem returns exactly one file, whatever its extension.
    - Given zero matches for that stem, the resolution throws a message saying the subject was not found — a loud failure, never a skipped assertion.
    - Given two matches for the same stem (a transient state where both extensions exist on disk), the resolution throws rather than silently picking one. Picking the stale copy while the real one goes unchecked is the specific failure this task exists to prevent.
    - The direct guard's verdict against the real tree is unchanged: install-resources imports nothing from repo-root.
  </behavior>
  <action>
Part 1 of this file reads its subject by a hardcoded filename. Once that subject renames,
the read either throws (loud, survivable) or — if both extensions transiently coexist —
silently checks the stale copy while the real file goes unpoliced. RESEARCH names that
second outcome as the worse one.

Replace the hardcoded read with a stem resolution over the enumeration `listModuleFiles()`
already produces: filter its results to entries whose basename minus extension equals the
subject's stem, require exactly one match, and throw a message naming the stem and the
matches found if the count is anything other than one. Then read that resolved file.

Write the three-case corpus described in `<behavior>` before the resolver, and watch the
zero-match and two-match cases fail against a not-yet-written resolver — this file's Part 1
already models exactly that red-then-green corpus shape for its own regex.

This is a pure test-file change with no rename. The suite must be green when it lands, and
the direct guard's verdict against the real tree must be identical to before.
  </action>
  <acceptance_criteria>
    - `node --test .claude/mcp/vice/load-order.test.mjs` reports `fail 0`, with a test count strictly greater than Plan 02's.
    - `grep -c 'join(HERE, "install-resources' .claude/mcp/vice/load-order.test.mjs` returns 0.
    - The corpus covers all three resolution cases (exactly one match, zero matches, two matches) as named assertions.
    - `git diff --name-only HEAD` lists exactly one file.
    - Full suite: `node --test '.claude/mcp/vice/'*.test.*` reports `fail 0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/mcp/vice/load-order.test.mjs && node --test '.claude/mcp/vice/'*.test.*</automated>
  </verify>
  <done>The direct guard resolves its subject by stem, refuses ambiguity loudly, and no hardcoded module filename remains in this file.</done>
  <reversibility rating="reversible">Test-only change.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Convert the three source modules and repoint every consumer specifier</name>
  <files>.claude/mcp/vice/repo-root.mjs → .claude/mcp/vice/repo-root.ts, .claude/mcp/vice/install-resources.mjs → .claude/mcp/vice/install-resources.ts, .claude/mcp/vice/hostpath.mjs → .claude/mcp/vice/hostpath.ts, .claude/mcp/vice/containerpath.mjs, .claude/mcp/vice/incident-record.mjs, .claude/mcp/vice/vice-broker-client.mjs, .claude/mcp/vice/vice.mjs, .claude/mcp/vice/vice-sync.mjs, .claude/mcp/vice/vice-probe.ts, .claude/mcp/vice/vice-proxy.mjs, .claude/mcp/vice/vice-proxy.test.mjs, .claude/mcp/vice/containerpath.test.mjs, .claude/mcp/vice/repo-root.test.mjs, .claude/mcp/vice/install-resources.test.mjs, .claude/mcp/vice/host-scripts.test.ts</files>
  <read_first>
    - .claude/mcp/vice/vice-broker.mts — the idiom set: named `interface`/`type` for shapes, discriminated-union result types where a function currently signals failure by returning a null-shaped value, `isPlainObject`-style predicates to narrow parsed JSON rather than casting, atomic write as tmp-sibling → chmod → content → rename, `process.exitCode` never `process.exit()`.
    - .claude/mcp/vice/repo-root.mjs (161 lines) — the whole file. Its branch-4 fallback climbs exactly three levels and is pinned by a named regression test; its bottom-of-module install call has a comment saying its POSITION is load-bearing.
    - .claude/mcp/vice/install-resources.mjs (389 lines) — the whole file. Its header carries the criterion-10 hazard verbatim; `pruneResources`, `readDeployManifest` and `writeDeployManifest` are Phase 01.6 plan 03's manifest-scoped prune and must come through behaviour-identical.
    - .claude/mcp/vice/hostpath.mjs (238 lines) as left by Plan 02 — already cycle-free, already carrying the optional workspace-root parameter.
    - 01.6.1-PATTERNS.md § "Never-throw file read", § "Atomic write", § "Narrowing `unknown`, not casting".
    - The consumer map in this plan's body above.
  </read_first>
  <action>
`git mv` all three source modules to `.ts` and add type annotations. Behaviour must be
identical; this is a language port, not a refactor.

**Typing guidance, per PATTERNS.md's one real in-repo precedent:**
  - Declare named `interface`s for the option bags and record shapes each module already
    passes around (the repo-root resolver's options, the deploy-manifest shape, the mount
    record the hostpath module derives, the candidate-result shape).
  - Where a function signals failure today by returning a null-shaped value, keep that
    behaviour and give it a discriminated-union result `type` rather than converting it to
    a throw.
  - Narrow every `JSON.parse` result through a type predicate before touching its fields.
    Do not cast a parsed value directly to an interface.
  - **The never-throw discipline is a security control, not a style.** Where a reader has
    two separate `try`/`catch` layers — one around the file read, one around the parse — keep
    them separate. Do not merge them, and do not replace a returned null-shaped value with a
    thrown error because the type system would prefer explicit errors. A converted reader
    that throws turns a designed degrade-gracefully path into a crash path in a process the
    agent's whole emulator access depends on.
  - Port every doc comment verbatim, including install-resources' hazard header and
    repo-root's position-is-load-bearing note. Comments here encode hazards; PATTERNS names
    trimming them as the single most repeated thing not to do.
  - Update comment prose that names a file this phase has renamed, so the prose stays true —
    including repo-root's line-143 mention of the probe module, stale since Plan 01. The
    install-resources hazard header quotes the forbidden import in prose and stays a
    comment; load-order.test's statement-anchored pattern is what keeps that quote from
    false-positiving, and its own corpus already asserts exactly that.

**Repoint every consumer specifier in the same commit**, per the consumer map above. Two
of them sit inside very large files — `vice-proxy.mjs` (2,441 lines) and
`vice-proxy.test.mjs` (4,370). Do not read those whole; grep for the specifiers and apply
targeted edits. `vice-proxy.mjs` and `vice-proxy.test.mjs` both stay `.mjs` until Plans 07
and 08; a `.mjs` file importing a `.ts` sibling resolves correctly, as Plan 01's tracer
already proved.

**Leave the three test files of this slice as `.mjs`** — only their import specifiers and
dynamic-import URL literals change here. Task 3 renames them. Splitting it this way keeps
each step's failure attributable.

**Do not** edit `tsconfig.json`, `tsconfig.build.json` or `package.json`. **Do not**
introduce `enum`, `namespace` or constructor parameter properties; the typecheck and bare
`node` both reject all three independently.
  </action>
  <acceptance_criteria>
    - `ls .claude/mcp/vice/repo-root.* .claude/mcp/vice/install-resources.* .claude/mcp/vice/hostpath.*` shows no `.mjs` entry for any of the three modules (the two `.test.mjs` files are still present at this point and are expected).
    - `npm --prefix .claude/mcp/vice run typecheck` exits 0.
    - `git diff --name-only HEAD -- .claude/mcp/vice/tsconfig.json .claude/mcp/vice/tsconfig.build.json .claude/mcp/vice/package.json` prints nothing.
    - `node --test .claude/mcp/vice/load-order.test.mjs` reports `fail 0` — the direct guard resolved the renamed subject by stem and still proves install-resources imports nothing from repo-root.
    - `node --test .claude/mcp/vice/repo-root.test.mjs .claude/mcp/vice/install-resources.test.mjs .claude/mcp/vice/containerpath.test.mjs .claude/mcp/vice/host-scripts.test.ts` reports `fail 0`.
    - `node -e "import('./.claude/mcp/vice/vice-proxy.mjs').then(()=>console.log('LOADS'),e=>{console.error(e.message);process.exit(1)})" --input-type=module` prints `LOADS` — the hub still resolves every one of its imports.
    - The install-resources manifest reader still degrades rather than throwing on a corrupt manifest: `node --test .claude/mcp/vice/install-resources.test.mjs` reports `pass 19`, `fail 0`, including its untracked-file-protection case.
    - Full suite: `node --test '.claude/mcp/vice/'*.test.*` reports `fail 0` at the total Plan 02 stated plus Task 1's additions, with no test count decrease anywhere.
  </acceptance_criteria>
  <verify>
    <automated>npm --prefix .claude/mcp/vice run typecheck && node --test '.claude/mcp/vice/'*.test.*</automated>
  </verify>
  <done>The three cycle modules are TypeScript with behaviour and comments intact, every consumer specifier resolves, the never-throw readers still degrade, and the suite is green with no test lost.</done>
  <reversibility rating="reversible">Renames plus annotations plus one-line specifier edits; `git revert` restores exactly.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Convert the three test files of this slice</name>
  <files>.claude/mcp/vice/repo-root.test.mjs → .claude/mcp/vice/repo-root.test.ts, .claude/mcp/vice/install-resources.test.mjs → .claude/mcp/vice/install-resources.test.ts, .claude/mcp/vice/load-order.test.mjs → .claude/mcp/vice/load-order.test.ts</files>
  <read_first>
    - The three files as left by Task 2 — read them after Task 2's specifier edits, not from memory of their prior state.
    - .claude/mcp/vice/vice-probe.test.ts and .claude/mcp/vice/skill-docs.test.ts — Plan 01's own converted test files, the in-phase precedent for typing helpers in a test.
    - .claude/mcp/vice/vice-broker-launch.test.ts — the repo's original `.ts` test conventions.
  </read_first>
  <action>
`git mv` all three to `.ts` and add type annotations to their helper functions —
`listModuleFiles`, `extractRelativeImportSpecifiers`, `buildImportGraph`,
`findCyclesThroughNode`, `canonicalCycles`, the stem resolver Task 1 added, and whatever
tmpdir/env-override helpers the other two carry.

**Leave every regression-corpus string literal exactly as written.** Part 1's corpus in the
load-order guard covers legacy specifier extensions on purpose — those literals are the
proof the extraction pattern catches every shape, and rewriting them to the current
extension deletes the coverage they exist to provide. The same applies to any corpus entry
in the other two files.

Leave the emptied cycle allowlist and the module-scope call-site guard from Plan 02
semantically unchanged; typing them must not weaken an assertion or shorten a failure
message.

The install-resources test's dynamic-import URL literals were already repointed in Task 2;
confirm they still resolve after this file's own rename, since `import.meta.url` now names a
`.ts` file.

Nothing else in this plan's file set changes.
  </action>
  <acceptance_criteria>
    - `ls .claude/mcp/vice/*.mjs` no longer lists `repo-root.test.mjs`, `install-resources.test.mjs` or `load-order.test.mjs`.
    - `node --test .claude/mcp/vice/repo-root.test.ts .claude/mcp/vice/install-resources.test.ts .claude/mcp/vice/load-order.test.ts` reports `fail 0`.
    - `npm --prefix .claude/mcp/vice run typecheck` exits 0.
    - The load-order corpus still contains legacy-extension specifier literals: `grep -c 'repo-root\.mjs' .claude/mcp/vice/load-order.test.ts` returns a non-zero count.
    - Full suite: `node --test '.claude/mcp/vice/'*.test.*` reports `fail 0` at the same total as Task 2, with the SUMMARY reconciling the arithmetic explicitly.
    - After this plan, exactly these files remain `.mjs` under `.claude/mcp/vice/`: `containerpath.mjs`, `incident-record.mjs`, `vice-broker-client.mjs`, `vice.mjs`, `vice-sync.mjs`, `refresh-manifest.mjs`, `vice-proxy.mjs`, plus the test files `containerpath.test.mjs`, `incident-record.test.mjs`, `vice-broker-client.test.mjs`, `vice.test.mjs`, `vice-proxy.test.mjs`, and the out-of-scope `vice-broker.test.mjs`. Confirm with `ls .claude/mcp/vice/*.mjs`.
  </acceptance_criteria>
  <verify>
    <automated>node --test .claude/mcp/vice/load-order.test.ts && npm --prefix .claude/mcp/vice run typecheck && node --test '.claude/mcp/vice/'*.test.*</automated>
  </verify>
  <done>Slice 2 is complete: six files TypeScript, every specifier resolving, both criterion-B guards still green and still discriminating, and the remaining `.mjs` set is exactly the thirteen files listed.</done>
  <reversibility rating="reversible">Renames plus annotations.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted on-disk JSON → install-resources' manifest reader | A half-written or corrupt deploy manifest must degrade, never crash the process that reads it. |
| module graph → process start | The three files converted here are the ones the criterion-10 hazard lives in. |
| static guard → the module it polices | The direct guard's subject is being renamed in the same plan that the guard is renamed in. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01.6.1-01 | Denial of Service | install-resources' manifest reader; repo-root's bottom-of-module install call and its surrounding catch | high | mitigate | Task 2's action forbids merging the two try/catch layers and forbids replacing a returned null-shaped value with a throw. Acceptance requires the install-resources test's full 19 cases, including untracked-file protection, to pass — those are the cases that exercise the degrade paths. The repo-root belt-and-suspenders catch at the bottom of the module ports unchanged. |
| T-01.6.1-04 | Tampering | load-order.test's direct guard, whose subject renames in this plan | high | mitigate | Task 1 replaces the hardcoded read with a stem resolution that **throws on zero matches and throws on two matches**, so the transient both-extensions-present state cannot silently check the stale copy. That two-match case is asserted by name. |
| T-01.6.1-02 | Denial of Service | the retired cycle | high | mitigate | Already retired and guarded in Plan 02. This plan must not reintroduce it; the emptied allowlist plus the module-scope call-site guard both run in every acceptance command, and Task 2 explicitly forbids adding a repo-root import to install-resources. |
| T-01.6.1-07 | Denial of Service | `vice-proxy.mjs`'s import block — the hub imports five of the modules touched here | high | mitigate | Acceptance loads the hub directly in a fresh Node process and requires it to resolve every import. A missed specifier in a 2,441-line file is otherwise only discovered at the next session start. |
| T-01.6.1-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task exists in this phase. See Plan 01's register. |
</threat_model>

## Artifacts this phase produces

Phase-wide list. **(P03)** marks what this plan produces.

**File renames (`.mjs` → `.ts`), all 23:** `vice-probe` · `vice-probe.test` ·
`skill-docs.test` · `vice-mcp-selector-docs.test` · `host-scripts.test` ·
`repo-root` **(P03)** · `repo-root.test` **(P03)** · `install-resources` **(P03)** ·
`install-resources.test` **(P03)** · `hostpath` **(P03)** · `load-order.test` **(P03)** ·
`containerpath` · `containerpath.test` · `incident-record` · `incident-record.test` ·
`vice-broker-client` · `vice-broker-client.test` · `vice` · `vice.test` ·
`refresh-manifest` · `vice-sync` · `vice-proxy` · `vice-proxy.test`

**New files:** `refresh-manifest.test.ts` (Plan 06) · `vice-sync.test.ts` (Plan 06)

**New / changed exported symbols:** the hostpath module's optional `workspaceRoot` parameter
on `hostPathCandidates` / `hostPath` / `tryHostPaths` / `describe` and its new named
resolution error (Plan 02) · a new `repo-root` import in `vice-sync` (Plan 02) · named
`interface`/`type` declarations added to `repo-root`, `install-resources` and `hostpath`
**(P03)** and to every other converted module in its own plan.

**New test-internal symbols:** the doc-leak exemption map in `skill-docs.test` (Plan 01) ·
the module-scope `repoRoot` call-site guard and emptied allowlist in `load-order.test`
(Plan 02) · a by-stem module resolver in `load-order.test` **(P03)**.

**Non-source files changed:** `.planning/config.json` (Plan 01) · `.mcp.json` (Plan 07).

<verification>
- `node --test '.claude/mcp/vice/'*.test.*` → `fail 0`, total reconciled against Plan 02's.
- `npm --prefix .claude/mcp/vice run typecheck` → exit 0.
- The hub loads: importing `vice-proxy.mjs` in a fresh Node process succeeds.
- `ls .claude/mcp/vice/*.mjs` matches the thirteen-file list in Task 3's acceptance.
</verification>

<success_criteria>
- Slice 2 converted: three sources and three tests are TypeScript.
- Every consumer specifier in the tree resolves, including the two inside the 2,441-line and
  4,370-line files.
- The last hardcoded-extension read in the tree is gone, replaced by a stem resolution that
  refuses ambiguity loudly.
- Both criterion-B guards are still green and still policing real subjects.
</success_criteria>

<output>
Create `.planning/phases/01.6.1-container-side-conversion-to-typescript/01.6.1-03-SUMMARY.md` when done.
Reconcile the test count as arithmetic against the running total, and list the remaining
`.mjs` files by name so the next plan starts from a measured state. If the typecheck
surfaced a type error whose cause was not obvious from the JavaScript, append it to
`.planning/RE-FINDINGS.md` at the moment it is found, with separate `Evidence:` and
`Confidence:` fields — those are the toolchain hazards this conversion is most likely to
teach.
</output>
